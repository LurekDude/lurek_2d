# Luna2D Rust API Reference

> Auto-generated by `tools/gen_docs_rust.py` from `docs/api_data.json`.
> Version: `0.4.0` | Generated: 2026-04-01
> **Coverage:** 780/798 public items documented

> For Lua scripting API see `docs/lua-api.md`.
> For engine contributors and extension authors only.

## Contents

- [`ai`](#ai) — 47 items
- [`audio`](#audio) — 15 items
- [`cardgame`](#cardgame) — 13 items
- [`combat`](#combat) — 6 items
- [`compute`](#compute) — 63 items
- [`crafting`](#crafting) — 11 items
- [`data`](#data) — 16 items
- [`dataframe`](#dataframe) — 13 items
- [`dialog`](#dialog) — 4 items
- [`engine`](#engine) — 38 items
- [`entity`](#entity) — 3 items
- [`event`](#event) — 5 items
- [`filesystem`](#filesystem) — 12 items
- [`graph`](#graph) — 21 items
- [`graphics`](#graphics) — 93 items
- [`image`](#image) — 2 items
- [`input`](#input) — 16 items
- [`inventory`](#inventory) — 10 items
- [`lua_api`](#lua-api) — 147 items
- [`math`](#math) — 93 items
- [`modding`](#modding) — 3 items
- [`particle`](#particle) — 13 items
- [`pathfinding`](#pathfinding) — 23 items
- [`physics`](#physics) — 13 items
- [`quest`](#quest) — 7 items
- [`resource`](#resource) — 6 items
- [`savegame`](#savegame) — 6 items
- [`scene`](#scene) — 9 items
- [`sound`](#sound) — 4 items
- [`stats`](#stats) — 11 items
- [`thread`](#thread) — 9 items
- [`tilemap`](#tilemap) — 60 items
- [`timer`](#timer) — 5 items
- [`window`](#window) — 1 items

## `ai` {#ai}

### `ai`

> Game AI toolkit: FSM, Behavior Trees, Steering, Pathfinding, Q-Learning, and more.  All subsystems are decoupled: agents can be driven by FSM, BT, SteeringManager, or any mix. All computation is pure CPU math — no GPU, audio, or window access required.

*Source: [src/ai/mod.rs](src/ai/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`agent`](src/ai/mod.rs#L7) | Autonomous agent with pluggable decision models. |
| `mod` | [`behavior_tree`](src/ai/mod.rs#L9) | Behavior tree nodes (sequence, selector, parallel, decorators). |
| `mod` | [`blackboard`](src/ai/mod.rs#L11) | Shared key-value store for inter-agent communication. |
| `mod` | [`command_queue`](src/ai/mod.rs#L13) | Queue-based action scheduling with undo support. |
| `mod` | [`flowfield`](src/ai/mod.rs#L15) | Dijkstra-based flow field for crowd pathfinding. |
| `mod` | [`fsm`](src/ai/mod.rs#L17) | Finite state machine with named states and transitions. |
| `mod` | [`goap`](src/ai/mod.rs#L19) | Goal-oriented action planning (GOAP) solver. |
| `mod` | [`influence_map`](src/ai/mod.rs#L21) | Grid-based influence map for spatial reasoning. |
| `mod` | [`pathgrid`](src/ai/mod.rs#L23) | Weighted grid pathfinding (A*, Dijkstra) with obstacle support. |
| `mod` | [`qlearner`](src/ai/mod.rs#L25) | Tabular Q-learning agent for reinforcement learning. |
| `mod` | [`squad`](src/ai/mod.rs#L27) | Squad coordination: formation, morale, and group behaviour. |
| `mod` | [`steering`](src/ai/mod.rs#L29) | Steering behaviours (seek, flee, wander, flocking, etc.). |
| `mod` | [`utility_ai`](src/ai/mod.rs#L31) | Utility-AI scorer with weighted action selection. |
| `mod` | [`world`](src/ai/mod.rs#L33) | Spatial AI world with team assignment and awareness radius. |

### `ai::agent`

> Autonomous agent with kinematic state and attached decision subsystems.

*Source: [src/ai/agent.rs](src/ai/agent.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DecisionModel`](src/ai/agent.rs#L16) | Controls which subsystems are ticked during `AIWorld::update`.  # Variants - ... |
| `struct` | [`Agent`](src/ai/agent.rs#L81) | Autonomous agent with kinematic state and attached decision subsystems.  # Fi... |

### `ai::behavior_tree`

> Behavior Tree with composite, decorator, and leaf nodes.  All nodes return `BTStatus`. Composites memo the running-child index to support multi-frame "running" states.

*Source: [src/ai/behavior_tree.rs](src/ai/behavior_tree.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BTNode`](src/ai/behavior_tree.rs#L104) | Recursive behavior-tree node.  # Variants - `Selector` — Selector variant. - ... |
| `enum` | [`BTStatus`](src/ai/behavior_tree.rs#L15) | Return status produced by every BT node after a tick.  # Variants - `Success`... |
| `enum` | [`ParallelPolicy`](src/ai/behavior_tree.rs#L59) | Parallel node success/failure policy.  # Variants - `RequireOne` — RequireOne... |
| `struct` | [`BehaviorTree`](src/ai/behavior_tree.rs#L216) | Root container holding the behavior tree and caching the last tick status.  #... |

### `ai::blackboard`

> Typed key-value store with optional parent chain for hierarchical lookup.

*Source: [src/ai/blackboard.rs](src/ai/blackboard.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BlackboardValue`](src/ai/blackboard.rs#L12) | Typed value stored in a Blackboard slot.  # Variants - `Number` — Number vari... |
| `struct` | [`Blackboard`](src/ai/blackboard.rs#L30) | Key-value store with optional parent chain for hierarchical lookup.  # Fields... |

### `ai::command_queue`

> RTS-style ordered command queue for a single unit.

*Source: [src/ai/command_queue.rs](src/ai/command_queue.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Command`](src/ai/command_queue.rs#L16) | A single RTS unit command with metadata.  # Fields - `kind` — `String`. - `ca... |
| `struct` | [`CommandQueue`](src/ai/command_queue.rs#L38) | RTS-style ordered command queue.  # Fields - `commands` — `VecDeque<Command>`... |

### `ai::flowfield`

> BFS flow field built over a PathGrid.

*Source: [src/ai/flowfield.rs](src/ai/flowfield.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`FlowField`](src/ai/flowfield.rs#L13) | BFS flow field that stores normalized direction vectors toward a goal.  # Fie... |

### `ai::fsm`

> Finite State Machine with priority-ordered guarded transitions.

*Source: [src/ai/fsm.rs](src/ai/fsm.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`StateCallbacks`](src/ai/fsm.rs#L13) | Lua lifecycle hooks for a single FSM state.  # Fields - `on_enter` — `Option<... |
| `struct` | [`StateMachine`](src/ai/fsm.rs#L52) | Finite State Machine with priority-ordered guarded transitions.  # Fields - `... |
| `struct` | [`Transition`](src/ai/fsm.rs#L29) | Guarded edge in the state graph.  # Fields - `from` — `String`. - `to` — `Str... |

### `ai::goap`

> A★ planner over boolean world state for multi-step tactical planning.

*Source: [src/ai/goap.rs](src/ai/goap.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`GOAPAction`](src/ai/goap.rs#L16) | A single GOAP action with boolean preconditions and effects.  # Fields - `nam... |
| `struct` | [`GOAPGoal`](src/ai/goap.rs#L35) | A planning goal expressed as a target boolean world state.  # Fields - `name`... |
| `struct` | [`GOAPPlanner`](src/ai/goap.rs#L84) | A★ planner over boolean world state for multi-step tactical planning.  # Fiel... |

### `ai::influence_map`

> Multi-layer spatial float grid for strategic area analysis.

*Source: [src/ai/influence_map.rs](src/ai/influence_map.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`InfluenceMap`](src/ai/influence_map.rs#L15) | Multi-layer spatial float grid for influence mapping.  # Fields - `width` — `... |

### `ai::pathgrid`

> A★ navigation grid for pathfinding.  Internal coordinates are 0-based. Lua-facing coordinates are 1-based (conversion happens in the API binding layer).

*Source: [src/ai/pathgrid.rs](src/ai/pathgrid.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Cell`](src/ai/pathgrid.rs#L15) | Single cell of the navigation grid.  # Fields - `walkable` — `bool`. - `cost`... |
| `struct` | [`PathGrid`](src/ai/pathgrid.rs#L68) | A★ navigation grid.  # Fields - `width` — `usize`. - `height` — `usize`. - `c... |

### `ai::qlearner`

> Tabular epsilon-greedy Q-learner.

*Source: [src/ai/qlearner.rs](src/ai/qlearner.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`QLearner`](src/ai/qlearner.rs#L17) | Tabular epsilon-greedy Q-learner for reinforcement learning.  # Fields - `sta... |

### `ai::squad`

> Multi-agent formation group with offset computation.

*Source: [src/ai/squad.rs](src/ai/squad.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FormationType`](src/ai/squad.rs#L14) | Formation shapes for squad positioning.  # Variants - `None` — None variant. ... |
| `struct` | [`Squad`](src/ai/squad.rs#L71) | Multi-agent formation group.  # Fields - `name` — `String`. - `members` — `Ve... |

### `ai::steering`

> Reynolds-style steering behaviors with weighted/priority combination.

*Source: [src/ai/steering.rs](src/ai/steering.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CombineMode`](src/ai/steering.rs#L12) | How multiple steering forces are combined.  # Variants - `Weighted` — Weighte... |
| `enum` | [`SteeringBehaviorType`](src/ai/steering.rs#L79) | Enumeration of all concrete steering behavior types.  # Variants - `Seek` — S... |
| `struct` | [`SteeringBase`](src/ai/steering.rs#L52) | Common data for all steering behaviors.  # Fields - `weight` — `f32`. - `enab... |
| `struct` | [`SteeringManager`](src/ai/steering.rs#L308) | Manages a list of steering behaviors and combines their forces each frame.  #... |
| `type` | [`Force`](src/ai/steering.rs#L4) | 2D force vector (fx, fy). |

### `ai::utility_ai`

> Multi-axis utility scorer that chooses the action with highest composite score.

*Source: [src/ai/utility_ai.rs](src/ai/utility_ai.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ResponseCurve`](src/ai/utility_ai.rs#L14) | Response curve shapes for consideration scoring.  # Variants - `Linear` — Lin... |
| `struct` | [`Consideration`](src/ai/utility_ai.rs#L85) | A single axis of evaluation within a UtilityAI action.  # Fields - `name` — `... |
| `struct` | [`UAAction`](src/ai/utility_ai.rs#L109) | One scored option in a UtilityAI decision space.  # Fields - `name` — `String... |
| `struct` | [`UtilityAI`](src/ai/utility_ai.rs#L126) | Multi-axis utility scorer; chooses the action with highest composite score.  ... |

### `ai::world`

> Container that owns agents and ticks them in descending priority order each frame.

*Source: [src/ai/world.rs](src/ai/world.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`AIWorld`](src/ai/world.rs#L17) | Container that owns agents and ticks them each frame.  # Fields - `agents` — ... |

## `audio` {#audio}

### `audio`

> Audio subsystem for Luna2D games.  This module provides sound loading, playback, and volume management through a [`Mixer`] that wraps the [rodio](https://crates.io/crates/rodio) audio library. Game code accesses audio through the `luna.audio` Lua bindings in [`crate::lua_api`], which delegates all work to [`Mixer`] and [`Bus`] instances stored in `SharedState`.  # Key types  - [`Mixer`] — central audio mixer; manages all loaded sounds, their playback state,   volume, pitch, pan, looping, and fade effects. - [`Bus`] — named group that applies a shared volume and pause state to every source   assigned to it (e.g. a "music" bus or "sfx" bus). - [`AudioSource`] — lightweight handle for a loaded audio file, carrying the path and   default playback settings. - [`SourceType`] — controls whether a sound is decoded fully into memory (`Static`) or   read incrementally from disk during playback (`Stream`). - [`PlayState`] — the current playback status of a source: `Stopped`, `Playing`, or `Paused`.  # Design notes  Buses are pure data containers; the mixer multiplies source volume/pitch by bus values on every `set_volume` or `update` call.  Audio sources are tracked in a slot-map keyed by [`crate::engine::resource_keys::SoundKey`], giving O(1) lookup and safe handle invalidation when sources are released.

*Source: [src/audio/mod.rs](src/audio/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`bus`](src/audio/mod.rs#L28) | Named audio bus for grouping sources under shared volume/pitch/pause controls. |
| `mod` | [`midi`](src/audio/mod.rs#L47) | MIDI SoundFont state management. |
| `mod` | [`midi_player`](src/audio/mod.rs#L30) | Software MIDI synthesizer with sine-additive PCM rendering. |
| `mod` | [`mixer`](src/audio/mod.rs#L32) | Low-level audio mixer using rodio for playback and volume control. |
| `mod` | [`sound_data`](src/audio/mod.rs#L43) | Decoded PCM sample buffer with per-sample read/write access. |
| `mod` | [`source`](src/audio/mod.rs#L34) | Audio source handle holding path, volume, and loop settings. |

### `audio::bus`

> Named audio bus for grouping sources under shared volume, pitch, and pause controls.

*Source: [src/audio/bus.rs](src/audio/bus.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Bus`](src/audio/bus.rs#L9) | A named audio bus that applies volume, pitch, and pause overrides to assigned... |

### `audio::midi`

> MIDI SoundFont state management.  Provides `MidiState` for tracking whether a SoundFont (SF2) file has been loaded for MIDI instrument rendering.

*Source: [src/audio/midi.rs](src/audio/midi.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`MidiState`](src/audio/midi.rs#L11) | MIDI SoundFont state.  Tracks whether a SoundFont (SF2) file has been loaded ... |

### `audio::midi_player`

> Software MIDI synthesizer: parses MIDI with `midly`, renders to PCM via sine-additive synthesis, and plays through a rodio `Sink`.

*Source: [src/audio/midi_player.rs](src/audio/midi_player.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`MidiData`](src/audio/midi_player.rs#L22) | Pre-parsed MIDI metadata extracted during `load()`.  # Fields - `duration_sec... |
| `struct` | [`MidiPlayer`](src/audio/midi_player.rs#L44) | Software MIDI player with sine-additive synthesis.  Parses MIDI via `midly`, ... |

### `audio::mixer`

> Core audio mixer that owns every loaded sound and drives playback through rodio.  [`Mixer`] is the single point of entry for all audio operations in Luna2D.  It opens the system's default output stream on construction (via rodio) and maintains a slot-map of [`AudioEntry`] records — one per loaded sound.  Each entry embeds a `rodio::Sink` for playback control and cached metadata (duration, fade parameters, loop flag, filters).  # Source types  The mixer supports two loading strategies selected at load time: - **Static** — the raw file bytes are decoded and held in an `Arc<Vec<u8>>` inside the   entry.  Playback restarts by re-decoding from that in-memory buffer, giving very low   latency at the cost of higher RAM usage.  Best for short sound effects. - **Stream** — the file is opened fresh from disk on each play call and decoded   incrementally.  Lower memory overhead for long music tracks.  # Bus routing  Every sound can be assigned to a named audio bus stored in a secondary slot-map. Volume and pitch for a playing source are the product of the per-source values and the values of its assigned bus.  Setting a bus to paused suspends every source on that bus.  # Master volume  A single master-volume scalar is applied on top of all per-source and per-bus volumes, letting game code provide a global volume slider without touching individual sources.  # Time tracking  Because rodio `Sink` does not expose a playback position, the mixer tracks play-start instants and accumulated pre-pause seconds manually so `luna.audio.getTime` can return the current position in the stream.

*Source: [src/audio/mixer.rs](src/audio/mixer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`PlayState`](src/audio/mixer.rs#L66) | Playback state of an audio source.  # Variants - `Stopped` — Stopped variant.... |
| `enum` | [`SourceType`](src/audio/mixer.rs#L52) | Type of audio source.  # Variants - `Static` — Static variant. - `Stream` — S... |
| `struct` | [`Mixer`](src/audio/mixer.rs#L104) | Manages audio output via rodio: loads sources, controls playback, and adjusts... |

### `audio::sound_data`

> Decoded PCM audio sample buffer.

*Source: [src/audio/sound_data.rs](src/audio/sound_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SoundData`](src/audio/sound_data.rs#L10) | Decoded audio samples in f32 PCM format.  Stores interleaved samples (for ste... |

### `audio::source`

> Audio source type and playback state enums for the audio subsystem.  The primary audio logic lives in `mixer::Mixer`. This module re-exports the public enum types used by the Lua API and engine code.

*Source: [src/audio/source.rs](src/audio/source.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`AudioSource`](src/audio/source.rs#L16) | Handle for a loaded audio asset (legacy compatibility shim).  # Fields - `id`... |

## `cardgame` {#cardgame}

### `cardgame`

> Card game back-end: card types, decks, zones, stack resolution, deck building, and card pools.  Exposed to Lua via `luna.cardgame.*`.

*Source: [src/cardgame/mod.rs](src/cardgame/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`clear_card_types`](src/cardgame/mod.rs#L63) | Clear entire registry. |
| `fn` | [`define_card_type`](src/cardgame/mod.rs#L47) | Define a card type in the global registry. |
| `fn` | [`get_card_type`](src/cardgame/mod.rs#L53) | Look up a card type definition. |
| `fn` | [`get_card_type_names`](src/cardgame/mod.rs#L58) | All registry names. |
| `struct` | [`Card`](src/cardgame/mod.rs#L73) | A single card instance. |
| `struct` | [`CardPool`](src/cardgame/mod.rs#L533) | A pool of card types for drafting or booster pack generation. |
| `struct` | [`CardPoolEntry`](src/cardgame/mod.rs#L524) | Entry in a card pool with an optional weight. |
| `struct` | [`CardTypeDef`](src/cardgame/mod.rs#L13) | Attributes defined on a card type. |
| `struct` | [`Deck`](src/cardgame/mod.rs#L201) | An ordered collection of cards (deck / hand / etc.). |
| `struct` | [`DeckBuilder`](src/cardgame/mod.rs#L455) | Validation rules for a deck-building context. |
| `struct` | [`StackEntry`](src/cardgame/mod.rs#L384) | A pending action on the resolution stack. |
| `struct` | [`StackManager`](src/cardgame/mod.rs#L402) | LIFO stack manager for card game effect resolution. |
| `struct` | [`Zone`](src/cardgame/mod.rs#L317) | A named play area that holds cards (graveyard, battlefield, hand, etc.). |

## `combat` {#combat}

### `combat`

> Turn-based combat system: combatants, actions, damage types, teams, and turn order.  Exposed to Lua via `luna.combat.*`.

*Source: [src/combat/mod.rs](src/combat/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DamageType`](src/combat/mod.rs#L13) | Kind of damage for resistance lookups. |
| `struct` | [`CombatAction`](src/combat/mod.rs#L85) | A combat action that a combatant can take. |
| `struct` | [`CombatBattle`](src/combat/mod.rs#L287) | Manages a full battle with turn ordering, combatant sets, and action resolution. |
| `struct` | [`CombatResult`](src/combat/mod.rs#L270) | Result of a single combat action. |
| `struct` | [`Combatant`](src/combat/mod.rs#L135) | A participant in combat. |
| `struct` | [`StatusEffect`](src/combat/mod.rs#L55) | An active status effect on a combatant. |

## `compute` {#compute}

### `compute`

> Dense N-dimensional numerical arrays with NumPy-style operations.  Provides `NdArray` for 1D/2D/3D row-major arrays with float32, float64, and int32 element types. Used by the `luna.compute` Lua module.

*Source: [src/compute/mod.rs](src/compute/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`array`](src/compute/mod.rs#L7) | N-dimensional array storage with typed element access. |
| `mod` | [`ops`](src/compute/mod.rs#L9) | Element-wise and reduction operations on NdArray. |
| `mod` | [`spatial`](src/compute/mod.rs#L11) | Spatial operations: convolution, pooling, distance transforms. |

### `compute::array`

> Core NdArray type and DataType enum.

*Source: [src/compute/array.rs](src/compute/array.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DataType`](src/compute/array.rs#L13) | Element data type for an NdArray.  # Variants - `Float32` — Float32 variant. ... |
| `struct` | [`NdArray`](src/compute/array.rs#L71) | Dense N-dimensional numerical array with row-major strides.  Data is stored a... |

### `compute::ops`

> Element-wise, reduction, comparison, masking, shape, and bitwise operations on NdArray.

*Source: [src/compute/ops.rs](src/compute/ops.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`abs`](src/compute/ops.rs#L217) | Element-wise absolute value.  # Parameters - `a` — `&NdArray`.  # Returns `Re... |
| `fn` | [`add`](src/compute/ops.rs#L95) | Element-wise addition of two arrays (same shape and dtype).  # Parameters - `... |
| `fn` | [`add_scalar`](src/compute/ops.rs#L107) | Add a scalar to every element.  # Parameters - `a` — `&NdArray`. - `s` — `f64... |
| `fn` | [`all`](src/compute/ops.rs#L635) | Returns `true` if all elements are non-zero.  # Parameters - `a` — `&NdArray`... |
| `fn` | [`any`](src/compute/ops.rs#L619) | Returns `true` if any element is non-zero.  # Parameters - `a` — `&NdArray`. ... |
| `fn` | [`argmax`](src/compute/ops.rs#L599) | Return the flat index of the maximum element (0-based).  # Parameters - `a` —... |
| `fn` | [`argmin`](src/compute/ops.rs#L579) | Return the flat index of the minimum element (0-based).  # Parameters - `a` —... |
| `fn` | [`bitwise_and`](src/compute/ops.rs#L965) | Bitwise AND of two Int32 arrays.  # Parameters - `a` — `&NdArray`. - `b` — `&... |
| `fn` | [`bitwise_lshift`](src/compute/ops.rs#L1038) | Bitwise left shift of an Int32 array by `amount` bits.  # Parameters - `a` — ... |
| `fn` | [`bitwise_not`](src/compute/ops.rs#L1021) | Bitwise NOT of an Int32 array.  # Parameters - `a` — `&NdArray`.  # Returns `... |
| `fn` | [`bitwise_or`](src/compute/ops.rs#L984) | Bitwise OR of two Int32 arrays.  # Parameters - `a` — `&NdArray`. - `b` — `&N... |
| `fn` | [`bitwise_rshift`](src/compute/ops.rs#L1055) | Bitwise right shift (arithmetic) of an Int32 array by `amount` bits.  # Param... |
| `fn` | [`bitwise_xor`](src/compute/ops.rs#L1003) | Bitwise XOR of two Int32 arrays.  # Parameters - `a` — `&NdArray`. - `b` — `&... |
| `fn` | [`clamp`](src/compute/ops.rs#L241) | Clamp every element to `[min_val, max_val]`.  # Parameters - `a` — `&NdArray`... |
| `fn` | [`clone_array`](src/compute/ops.rs#L949) | Clone an array (convenience wrapper).  # Parameters - `a` — `&NdArray`.  # Re... |
| `fn` | [`count_nonzero`](src/compute/ops.rs#L562) | Count the number of non-zero elements.  # Parameters - `a` — `&NdArray`.  # R... |
| `fn` | [`div`](src/compute/ops.rs#L167) | Element-wise division of two arrays (same shape and dtype).  # Parameters - `... |
| `fn` | [`div_scalar`](src/compute/ops.rs#L179) | Divide every element by a scalar.  # Parameters - `a` — `&NdArray`. - `s` — `... |
| `fn` | [`eq`](src/compute/ops.rs#L261) | Element-wise equality comparison of two arrays. Returns Float32 with 0/1.  # ... |
| `fn` | [`eq_scalar`](src/compute/ops.rs#L283) | Element-wise equality comparison against a scalar. Returns Float32.  # Parame... |
| `fn` | [`fill`](src/compute/ops.rs#L936) | Fill all elements of an array with a value (in-place).  # Parameters - `a` — ... |
| `fn` | [`gt`](src/compute/ops.rs#L347) | Element-wise greater-than comparison of two arrays. Returns Float32.  # Param... |
| `fn` | [`gt_scalar`](src/compute/ops.rs#L371) | Element-wise greater-than comparison against a scalar. Returns Float32.  # Pa... |
| `fn` | [`gte`](src/compute/ops.rs#L427) | Element-wise greater-than-or-equal comparison of two arrays. Returns Float32.... |
| `fn` | [`gte_scalar`](src/compute/ops.rs#L451) | Element-wise greater-than-or-equal comparison against a scalar. Returns Float... |
| `fn` | [`lt`](src/compute/ops.rs#L387) | Element-wise less-than comparison of two arrays. Returns Float32.  # Paramete... |
| `fn` | [`lt_scalar`](src/compute/ops.rs#L411) | Element-wise less-than comparison against a scalar. Returns Float32.  # Param... |
| `fn` | [`lte`](src/compute/ops.rs#L467) | Element-wise less-than-or-equal comparison of two arrays. Returns Float32.  #... |
| `fn` | [`lte_scalar`](src/compute/ops.rs#L491) | Element-wise less-than-or-equal comparison against a scalar. Returns Float32.... |
| `fn` | [`max_axis`](src/compute/ops.rs#L843) | Maximum along a given axis.  # Parameters - `a` — `&NdArray`. - `axis` — `usi... |
| `fn` | [`max_val`](src/compute/ops.rs#L699) | Maximum value across all elements.  # Parameters - `a` — `&NdArray`.  # Retur... |
| `fn` | [`mean`](src/compute/ops.rs#L670) | Mean of all elements.  # Parameters - `a` — `&NdArray`.  # Returns `f64`. |
| `fn` | [`mean_axis`](src/compute/ops.rs#L789) | Mean along a given axis.  # Parameters - `a` — `&NdArray`. - `axis` — `usize`... |
| `fn` | [`min_axis`](src/compute/ops.rs#L807) | Minimum along a given axis.  # Parameters - `a` — `&NdArray`. - `axis` — `usi... |
| `fn` | [`min_val`](src/compute/ops.rs#L681) | Minimum value across all elements.  # Parameters - `a` — `&NdArray`.  # Retur... |
| `fn` | [`mul`](src/compute/ops.rs#L143) | Element-wise multiplication of two arrays (same shape and dtype).  # Paramete... |
| `fn` | [`mul_scalar`](src/compute/ops.rs#L155) | Multiply every element by a scalar.  # Parameters - `a` — `&NdArray`. - `s` —... |
| `fn` | [`neg`](src/compute/ops.rs#L228) | Element-wise negation.  # Parameters - `a` — `&NdArray`.  # Returns `Result<N... |
| `fn` | [`neq`](src/compute/ops.rs#L304) | Element-wise not-equal comparison of two arrays. Returns Float32.  # Paramete... |
| `fn` | [`neq_scalar`](src/compute/ops.rs#L326) | Element-wise not-equal comparison against a scalar. Returns Float32.  # Param... |
| `fn` | [`pow_scalar`](src/compute/ops.rs#L191) | Raise every element to a scalar exponent.  # Parameters - `a` — `&NdArray`. -... |
| `fn` | [`reshape`](src/compute/ops.rs#L885) | Reshape an array to a new shape with the same total element count.  # Paramet... |
| `fn` | [`sqrt`](src/compute/ops.rs#L206) | Element-wise square root.  # Parameters - `a` — `&NdArray`.  # Returns `Resul... |
| `fn` | [`sub`](src/compute/ops.rs#L119) | Element-wise subtraction of two arrays (same shape and dtype).  # Parameters ... |
| `fn` | [`sub_scalar`](src/compute/ops.rs#L131) | Subtract a scalar from every element.  # Parameters - `a` — `&NdArray`. - `s`... |
| `fn` | [`sum`](src/compute/ops.rs#L655) | Sum of all elements.  # Parameters - `a` — `&NdArray`.  # Returns `f64`. |
| `fn` | [`sum_axis`](src/compute/ops.rs#L757) | Sum along a given axis, producing an array with that axis removed.  # Paramet... |
| `fn` | [`threshold`](src/compute/ops.rs#L511) | Threshold mask: returns Float32 array with 1.0 where `a >= val`, 0.0 otherwis... |
| `fn` | [`transpose_2d`](src/compute/ops.rs#L911) | Transpose a 2D array (swap rows and columns).  # Parameters - `a` — `&NdArray... |
| `fn` | [`where_mask`](src/compute/ops.rs#L527) | Conditional selection: where `cond != 0`, choose from `a`; otherwise from `b`... |

### `compute::spatial`

> 2D spatial operations and linear algebra for NdArray.

*Source: [src/compute/spatial.rs](src/compute/spatial.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`convolve2d`](src/compute/spatial.rs#L18) | 2D convolution with zero-padding (same-size output).  # Parameters - `input` ... |
| `fn` | [`dilate`](src/compute/spatial.rs#L74) | Morphological dilation with a Manhattan-diamond structuring element.  # Param... |
| `fn` | [`dot`](src/compute/spatial.rs#L366) | Dot product of two 1D arrays (same length). Returns a scalar.  # Parameters -... |
| `fn` | [`erode`](src/compute/spatial.rs#L121) | Morphological erosion with a Manhattan-diamond structuring element.  # Parame... |
| `fn` | [`flood_fill`](src/compute/spatial.rs#L173) | Flood fill using BFS with 4-connectivity.  # Parameters - `a` — `&NdArray`. -... |
| `fn` | [`get_region`](src/compute/spatial.rs#L231) | Extract a rectangular sub-region from a 2D array.  # Parameters - `a` — `&NdA... |
| `fn` | [`matmul`](src/compute/spatial.rs#L315) | Matrix multiplication of two 2D arrays: (m,k) × (k,n) → (m,n).  # Parameters ... |
| `fn` | [`set_region`](src/compute/spatial.rs#L275) | Copy a source 2D array into a target 2D array at position `(row, col)`.  # Pa... |

## `crafting` {#crafting}

### `crafting`

> Crafting system: recipes, ingredients, stations, skill progression, and craft queues.  Exposed to Lua via `luna.crafting.*`.

*Source: [src/crafting/mod.rs](src/crafting/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Quality`](src/crafting/mod.rs#L13) | Output quality tiers for crafted items. |
| `struct` | [`CraftJob`](src/crafting/mod.rs#L319) | A queued crafting job. |
| `struct` | [`CraftQueue`](src/crafting/mod.rs#L352) | A queue that holds and processes craft jobs. |
| `struct` | [`CraftSkill`](src/crafting/mod.rs#L259) | Player crafting skill with XP and level progression. |
| `struct` | [`Ingredient`](src/crafting/mod.rs#L53) | An ingredient required by a recipe. |
| `struct` | [`Recipe`](src/crafting/mod.rs#L90) | Defines how inputs are combined to produce outputs. |
| `struct` | [`RecipeOutput`](src/crafting/mod.rs#L72) | Output of a recipe. |
| `struct` | [`RecipeRegistry`](src/crafting/mod.rs#L153) | Central registry for all known recipes. |
| `struct` | [`Station`](src/crafting/mod.rs#L219) | A crafting station that processes recipes. |
| `struct` | [`UpgradeNode`](src/crafting/mod.rs#L431) | A single node in an upgrade tree. |
| `struct` | [`UpgradeTree`](src/crafting/mod.rs#L452) | DAG of upgrades for weapons or items. |

## `data` {#data}

### `data`

> Binary data manipulation, compression, hashing, and encoding.  Provides `ByteData` for raw byte buffers, plus compression (deflate/gzip/lz4/zlib), cryptographic hashing (MD5/SHA family), and encoding (base64/hex). Also includes TOML parsing and encoding.

*Source: [src/data/mod.rs](src/data/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`byte_data`](src/data/mod.rs#L8) | Raw byte buffer for binary data manipulation. |
| `mod` | [`compress`](src/data/mod.rs#L10) | Deflate, gzip, lz4, and zlib compression/decompression. |
| `mod` | [`encode`](src/data/mod.rs#L12) | Base64 and hex encoding/decoding. |
| `mod` | [`hash`](src/data/mod.rs#L14) | Cryptographic hash functions (MD5, SHA-1, SHA-256, SHA-512). |
| `mod` | [`toml_convert`](src/data/mod.rs#L16) | TOML parsing and encoding. |

### `data::byte_data`

> Contiguous byte buffer accessible from Lua.

*Source: [src/data/byte_data.rs](src/data/byte_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ByteData`](src/data/byte_data.rs#L9) | Contiguous byte buffer for binary data manipulation.  Wraps a `Vec<u8>` with ... |

### `data::compress`

> Data compression and decompression using deflate, gzip, zlib, and LZ4.

*Source: [src/data/compress.rs](src/data/compress.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CompressFormat`](src/data/compress.rs#L13) | Supported compression formats.  # Variants - `Deflate` — Deflate variant. - `... |
| `fn` | [`compress`](src/data/compress.rs#L55) | Compress data using the specified format and compression level (0-9).  # Para... |
| `fn` | [`decompress`](src/data/compress.rs#L99) | Decompress data using the specified format.  # Parameters - `data` — `&[u8]`.... |

### `data::encode`

> Base64 and hex encoding/decoding for data serialization.

*Source: [src/data/encode.rs](src/data/encode.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`EncodeFormat`](src/data/encode.rs#L11) | Supported encoding formats.  # Variants - `Base64` — Base64 variant. - `Hex` ... |
| `fn` | [`decode`](src/data/encode.rs#L61) | Decode a string back into bytes using the specified format.  # Parameters - `... |
| `fn` | [`encode`](src/data/encode.rs#L46) | Encode bytes into a string using the specified format.  # Parameters - `forma... |

### `data::hash`

> Cryptographic hash functions for data integrity verification.

*Source: [src/data/hash.rs](src/data/hash.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`HashAlgorithm`](src/data/hash.rs#L14) | Supported hash algorithms.  # Variants - `Md5` — Md5 variant. - `Sha1` — Sha1... |
| `fn` | [`hash`](src/data/hash.rs#L55) | Compute the hash of data using the specified algorithm, returned as a hex str... |

### `data::toml_convert`

> TOML parsing and encoding for Luna2D.  Converts between TOML strings and Lua tables. Supports the full TOML spec via the `toml` crate, mapping types to their Lua equivalents.

*Source: [src/data/toml_convert.rs](src/data/toml_convert.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`encode_toml`](src/data/toml_convert.rs#L26) | Encode a `toml::Value` into a TOML string.  # Parameters - `value` — `&toml::... |
| `fn` | [`parse_toml`](src/data/toml_convert.rs#L13) | Parse a TOML string into a `toml::Value`.  # Parameters - `input` — `&str`.  ... |

## `dataframe` {#dataframe}

### `dataframe`

> In-memory column-major tabular data with query, analytics, and SQL.  Provides `DataFrame` for named-column data tables and `Database` for multi-table catalogs. Used by the `luna.dataframe` Lua module.

*Source: [src/dataframe/mod.rs](src/dataframe/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`frame`](src/dataframe/mod.rs#L7) | Column-major DataFrame and Database types. |
| `mod` | [`query`](src/dataframe/mod.rs#L9) | Filter, sort, group-by, and aggregate query engine. |
| `mod` | [`serial`](src/dataframe/mod.rs#L11) | CSV and JSON serialization/deserialization for DataFrames. |
| `mod` | [`sql`](src/dataframe/mod.rs#L13) | SQL query parser and executor for in-memory databases. |

### `dataframe::frame`

> Core DataFrame and Database types with CellValue cells.

*Source: [src/dataframe/frame.rs](src/dataframe/frame.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CellValue`](src/dataframe/frame.rs#L14) | A single cell value in a DataFrame column.  # Variants - `Nil` — Nil variant.... |
| `enum` | [`ColRef`](src/dataframe/frame.rs#L120) | Column reference: string name or 1-based integer index.  # Variants - `Name` ... |
| `struct` | [`DataFrame`](src/dataframe/frame.rs#L136) | In-memory column-major tabular data.  # Fields - `column_names` — `Vec<String... |
| `struct` | [`Database`](src/dataframe/frame.rs#L142) | Named catalog of DataFrames. |

### `dataframe::serial`

> CSV, JSON, and LVDF binary serialization for DataFrame.

*Source: [src/dataframe/serial.rs](src/dataframe/serial.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`from_binary`](src/dataframe/serial.rs#L644) | Deserialize a DataFrame from LVDF binary format.  # Parameters - `data` — `&[... |
| `fn` | [`from_csv`](src/dataframe/serial.rs#L19) | Parse a CSV string into a DataFrame.  # Parameters - `s` — `&str`.  # Returns... |
| `fn` | [`from_json`](src/dataframe/serial.rs#L196) | Parse JSON (array-of-objects) into a DataFrame.  # Parameters - `s` — `&str`.... |

### `dataframe::sql`

> SQL-like query parser and executor for DataFrame.  Supports a subset of SQL: SELECT, WHERE, GROUP BY, HAVING, ORDER BY, LIMIT/OFFSET, aggregate functions, and JOIN (for Database queries).

*Source: [src/dataframe/sql.rs](src/dataframe/sql.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`query_sql`](src/dataframe/sql.rs#L663) | Execute a SQL query on a single DataFrame.  # Parameters - `df` — `&DataFrame... |
| `fn` | [`query_sql_database`](src/dataframe/sql.rs#L678) | Execute a SQL query on a Database (supports FROM and JOIN).  # Parameters - `... |

## `dialog` {#dialog}

### `dialog`

> Dialog sequencer for visual novel-style text presentation.  Provides a state-machine-based dialog sequencer with typewriter effect, branching choices, timed pauses, and inline callbacks. Drives text-based narrative sequences from flat script arrays.

*Source: [src/dialog/mod.rs](src/dialog/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DialogNode`](src/dialog/mod.rs#L9) | Dialog node types in a script. |
| `enum` | [`SequencerState`](src/dialog/mod.rs#L34) | Sequencer playback state. |
| `struct` | [`ChoiceOption`](src/dialog/mod.rs#L25) | A single choice option with a label and branch nodes. |
| `struct` | [`Sequencer`](src/dialog/mod.rs#L68) | Dialog sequencer with typewriter effect and branching choices.  Drives a flat... |

## `engine` {#engine}

### `engine`

> Core engine lifecycle: application loop, configuration, error handling, and diagnostics.  This module owns the highest-level runtime structures in the Luna2D engine. [`App`] drives the winit event loop, fires `luna.load/update/draw/keypressed/…` Lua callbacks at the right moments, and holds every domain subsystem inside a `Rc<RefCell<SharedState>>` that is shared with the Lua VM.  # Module overview  - [`app`] — [`App`] struct that implements `winit::ApplicationHandler`.  It creates the   window and renderer, initialises every subsystem, executes `main.lua`, and routes   window events to their corresponding Lua callbacks. - [`config`] — [`Config`], [`WindowConfig`], [`ModulesConfig`], and [`PerformanceConfig`]   structs loaded from `conf.lua` at startup. - [`debug_overlay`] — on-screen FPS and draw-call counter drawn in the bottom-right   corner of the window during development. - [`error`] — [`EngineError`] enum and [`EngineResult`] type alias used throughout the   codebase for structured, recoverable error propagation. - [`error_screen`] — fallback render path that displays a human-readable Lua or engine   error on screen instead of crashing silently. - [`log_messages`] — structured, stable-ID log message constants used by `log::info!`   macros across the engine so message IDs remain consistent between builds. - [`resource_keys`] — typed newtype keys (`SoundKey`, `TextureKey`, `ShaderId`, …) for   all slot-map resource pools in the engine.

*Source: [src/engine/mod.rs](src/engine/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`app`](src/engine/mod.rs#L27) | Entry point for the Luna2D engine lifecycle and game loop. |
| `mod` | [`config`](src/engine/mod.rs#L29) | Engine and window configuration structs (Config, WindowConfig, etc.). |
| `mod` | [`debug_overlay`](src/engine/mod.rs#L31) | Debug overlay for FPS and draw-call statistics. |
| `mod` | [`error`](src/engine/mod.rs#L33) | EngineError enum and EngineResult type alias for engine-level errors. |
| `mod` | [`error_screen`](src/engine/mod.rs#L35) | Visual error screen for Lua and engine errors. |
| `mod` | [`log_messages`](src/engine/mod.rs#L37) | Structured logging with stable message IDs. |
| `mod` | [`resource_keys`](src/engine/mod.rs#L39) | Typed resource keys for generational ID-based resource pools. |

### `engine::app`

> Luna2D application lifecycle using winit 0.30 + wgpu GPU rendering.  Uses a `winit` event loop with `GpuRenderer` for hardware-accelerated rendering. The game loop structure (callbacks, SharedState, Lua VM) follows the standard pattern.

*Source: [src/engine/app.rs](src/engine/app.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`App`](src/engine/app.rs#L1527) | Entry point for the Luna2D engine. Owns the game loop, GPU renderer, and Lua ... |

### `engine::config`

> Engine and window configuration loaded from `conf.lua`.  When the engine starts it looks for a `conf.lua` file in the game directory. If found, it is executed in a minimal Lua VM and the result is read into a [`Config`] struct.  Missing fields fall back to built-in defaults so authors only need to specify the settings they actually want to change.  # Structure  [`Config`] is the top-level container and contains four nested structs: - [`WindowConfig`] — window geometry, title, display placement, and decoration options. - [`ModulesConfig`] — boolean feature-flags for optional engine subsystems (audio,   physics, graphics, etc.).  Disabling a module avoids the startup cost and prevents   the matching `luna.*` API calls from being registered. - [`PerformanceConfig`] — target frame-rate cap (`fps_cap`).  The `identity` field sets the name of the per-user save directory returned by `luna.filesystem.getSaveDirectory()`.  If unset, the engine uses the game directory name as a fallback.  # Example `conf.lua`  ```lua luna.window.setTitle("My Game") luna.window.setDimensions(1280, 720) luna.window.setFullscreen(false) luna.window.setVsync(true) ```

*Source: [src/engine/config.rs](src/engine/config.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Config`](src/engine/config.rs#L45) | Top-level engine configuration.  Can be populated from `conf.lua` or construc... |
| `struct` | [`ModulesConfig`](src/engine/config.rs#L96) | Flags to enable or disable optional engine subsystems.  # Fields - `audio` — ... |
| `struct` | [`PerformanceConfig`](src/engine/config.rs#L110) | Frame rate cap and other performance tuning options.  # Fields - `target_fps`... |
| `struct` | [`WindowConfig`](src/engine/config.rs#L72) | Window dimensions, title, vsync, fullscreen, and resize settings.  # Fields -... |

### `engine::debug_overlay`

> Debug overlay for displaying FPS and draw call statistics.  Renders green text on a semi-transparent background in the top-right corner of the screen. Can be toggled at runtime via F12 or `luna.system.setDebugOverlay()`.

*Source: [src/engine/debug_overlay.rs](src/engine/debug_overlay.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DebugOverlay`](src/engine/debug_overlay.rs#L15) | Debug overlay showing FPS and render statistics.  # Fields - `enabled` — `boo... |

### `engine::error`

> Structured error types and result alias for the Luna2D engine.  All engine-level error conditions are expressed as variants of [`EngineError`], a `thiserror`-derived enum that carries a human-readable description and belongs to one of the six [`ErrorCategory`] groups: `Init`, `Runtime`, `Resource`, `Script`, `System`, or `Filesystem`.  This module also provides [`EngineResult<T>`], a type alias for `Result<T, EngineError>`, which should be used in public APIs that can fail.  # Design goal  Every error variant has a stable four-digit numeric code (e.g. `E1001`) that can be displayed in the error screen and referenced in documentation without changing across releases.  This makes it straightforward for users to search for a specific error online or in the Luna2D issue tracker.  Lua errors are wrapped as [`EngineError::LuaError`] so they can flow through the same result type and be presented by [`crate::engine::error_screen::ErrorScreen`].

*Source: [src/engine/error.rs](src/engine/error.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`EngineError`](src/engine/error.rs#L80) | All possible error conditions that can occur in the Luna2D engine.  Each vari... |
| `enum` | [`ErrorCategory`](src/engine/error.rs#L32) | Error category for grouping related engine errors.  # Variants - `Init` — Ini... |
| `type` | [`EngineResult`](src/engine/error.rs#L193) | Convenience alias for `Result<T, EngineError>` used throughout the engine.  #... |

### `engine::error_screen`

> Visual error screen for displaying Lua and engine errors to the user.  Generates `DrawCommand` sequences that render a engine-standard blue error screen, including the error title, message, traceback, and instructions to quit or restart.

*Source: [src/engine/error_screen.rs](src/engine/error_screen.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`format_traceback`](src/engine/error_screen.rs#L313) | Cleans up a Lua traceback string for display.  # Parameters - `traceback` — `... |
| `fn` | [`wrap_text`](src/engine/error_screen.rs#L263) | Wraps a text string at word boundaries to fit within `max_chars` columns.  # ... |
| `struct` | [`ErrorScreen`](src/engine/error_screen.rs#L31) | Visual error screen that generates `DrawCommand` sequences for the GPU render... |

### `engine::log_messages`

> Structured logging with stable message IDs for the Luna2D engine.  Every engine log message has a stable ID (`L001`..`L099`) so that external tools and Lua scripts can filter or match on them.  Use the [`log_msg!`] macro instead of bare `log::info!` / `log::warn!` / `log::error!` calls.

*Source: [src/engine/log_messages.rs](src/engine/log_messages.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `const` | [`L001_ENGINE_START`](src/engine/log_messages.rs#L58) | Log message: engine starting. |
| `const` | [`L002_ENGINE_STOP`](src/engine/log_messages.rs#L60) | Log message: engine shut down. |
| `const` | [`L003_GAME_LOADED`](src/engine/log_messages.rs#L62) | Log message: game loaded from path. |
| `const` | [`L004_GAME_RESTART`](src/engine/log_messages.rs#L64) | Log message: game restarted. |
| `const` | [`L005_CONF_LOADED`](src/engine/log_messages.rs#L66) | Log message: conf.lua loaded. |
| `const` | [`L010_RENDER_ERROR`](src/engine/log_messages.rs#L73) | Log message: render error occurred. |
| `const` | [`L011_LUA_ERROR`](src/engine/log_messages.rs#L75) | Log message: Lua error caught. |
| `const` | [`L012_AUDIO_ERROR`](src/engine/log_messages.rs#L77) | Log message: audio error. |
| `const` | [`L013_FS_ERROR`](src/engine/log_messages.rs#L79) | Log message: filesystem error. |
| `const` | [`L014_PHYSICS_ERROR`](src/engine/log_messages.rs#L81) | Log message: physics error. |
| `const` | [`L015_RESOURCE_NOT_FOUND`](src/engine/log_messages.rs#L83) | Log message: resource not found. |
| `const` | [`L020_NO_AUDIO_DEVICE`](src/engine/log_messages.rs#L90) | Log message: no audio device available. |
| `const` | [`L021_CLIPBOARD_FAIL`](src/engine/log_messages.rs#L92) | Log message: clipboard access failed. |
| `const` | [`L022_UNKNOWN_LOG_LEVEL`](src/engine/log_messages.rs#L94) | Log message: unknown log level requested. |
| `const` | [`L030_ASYNC_LOAD_REQUEST`](src/engine/log_messages.rs#L101) | Log message: async asset load requested. |
| `const` | [`L031_ASYNC_LOAD_COMPLETE`](src/engine/log_messages.rs#L103) | Log message: async asset load completed. |
| `const` | [`L032_BATCH_STATS`](src/engine/log_messages.rs#L105) | Log message: draw batch statistics. |
| `fn` | [`get_log_level`](src/engine/log_messages.rs#L42) | Returns the current log level name.  # Returns `&'static str`. |
| `fn` | [`set_log_level`](src/engine/log_messages.rs#L21) | Sets the global log level at runtime (called from `luna.system.setLogLevel`).... |

## `entity` {#entity}

### `entity`

> Entity/ECS module — lightweight entity-component-system with ID recycling, bitmap tags, layers, blueprints, and system dispatch.

*Source: [src/entity/mod.rs](src/entity/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`universe`](src/entity/mod.rs#L5) | universe. |

### `entity::universe`

> Universe — a self-contained ECS world. Entities are u32 IDs starting at 1.

*Source: [src/entity/universe.rs](src/entity/universe.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`deep_copy_table`](src/entity/universe.rs#L941) | Deep-copies a Lua table recursively.  # Parameters - `lua` — `&'lua Lua`. - `... |
| `struct` | [`Universe`](src/entity/universe.rs#L13) | A self-contained ECS world.  Manages entities (u32 IDs with recycling), compo... |

## `event` {#event}

### `event`

> Event queue for polling system and custom events.  Provides an alternative to the callback model where game code can poll events from a FIFO queue. Also contains the `Signal` pub-sub type for handle-based event dispatching.

*Source: [src/event/mod.rs](src/event/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`EventArg`](src/event/mod.rs#L21) | Argument values that can be attached to events.  # Variants - `Str` — Str var... |
| `struct` | [`Event`](src/event/mod.rs#L38) | A single event in the event queue.  # Fields - `name` — `String`. - `args` — ... |
| `struct` | [`EventQueue`](src/event/mod.rs#L47) | FIFO event queue for system and custom events. |

### `event::signal`

> Handle-based pub-sub signal system.  `Signal` provides a lightweight event bus where listeners subscribe by name and receive a monotonically increasing handle ID for later removal. Callbacks fire in registration order.

*Source: [src/event/signal.rs](src/event/signal.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Signal`](src/event/signal.rs#L25) | Handle-based pub-sub signal dispatcher.  Listeners register for named events ... |
| `struct` | [`Subscription`](src/event/signal.rs#L11) | A single subscription entry in a [`Signal`]. |

## `filesystem` {#filesystem}

### `filesystem`

*Source: [src/filesystem/mod.rs](src/filesystem/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`async_loader`](src/filesystem/mod.rs#L8) | Background asset-loading worker that reads files off the main thread. |
| `mod` | [`file_handle`](src/filesystem/mod.rs#L5) | File handle with buffered read/write and sandboxed path resolution. |
| `mod` | [`vfs`](src/filesystem/mod.rs#L2) | Sandboxed virtual filesystem that restricts I/O to the game directory. |

### `filesystem::async_loader`

> Background asset-loading worker that reads files off the main thread.  The loader owns a single OS thread that processes read requests from a bounded channel.  Results are polled from the main thread each frame — no `unsafe`, no shared mutable state beyond an `Arc<Mutex<>>` result map.

*Source: [src/filesystem/async_loader.rs](src/filesystem/async_loader.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`LoadResult`](src/filesystem/async_loader.rs#L23) | Outcome of a completed load request.  # Variants - `Ready` — Ready variant. -... |
| `enum` | [`LoadStatus`](src/filesystem/async_loader.rs#L36) | Status returned by [`AsyncLoader::poll`].  # Variants - `Pending` — Pending v... |
| `struct` | [`AsyncLoader`](src/filesystem/async_loader.rs#L56) | A single-threaded background file reader.  Create one per engine session; dro... |
| `struct` | [`LoadHandle`](src/filesystem/async_loader.rs#L15) | Opaque handle returned to callers (and to Lua) that identifies a pending load. |

### `filesystem::file_handle`

> File handle with buffered read/write and sandboxed path resolution.

*Source: [src/filesystem/file_handle.rs](src/filesystem/file_handle.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FileMode`](src/filesystem/file_handle.rs#L16) | File access mode.  # Variants - `Read` — open for reading; file must exist - ... |
| `struct` | [`FileHandle`](src/filesystem/file_handle.rs#L62) | A sandboxed file handle for reading or writing game files. |

### `filesystem::vfs`

*Source: [src/filesystem/vfs.rs](src/filesystem/vfs.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FileType`](src/filesystem/vfs.rs#L31) | File type classification for `FileInfo`.  # Variants - `File` — regular file ... |
| `struct` | [`FileInfo`](src/filesystem/vfs.rs#L12) | File metadata returned by `get_info()`.  # Fields - `file_type` — whether the... |
| `struct` | [`GameFS`](src/filesystem/vfs.rs#L43) | Sandboxed filesystem rooted at the game directory; prevents path-traversal at... |

## `graph` {#graph}

### `graph`

> Directed graph with item flow simulation for Luna2D.  Provides nodes, edges, typed items, simulation (decay, transit, push/pull flow, conversions, queues), Dijkstra pathfinding, graph algorithms (components, cycles, topological sort), and supply/demand processing.

*Source: [src/graph/mod.rs](src/graph/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`algorithms`](src/graph/mod.rs#L8) | Graph algorithms: connected components, topological sort, cycle detection. |
| `mod` | [`core`](src/graph/mod.rs#L10) | Core `Graph` struct — nodes, edges, and item storage. |
| `mod` | [`edge`](src/graph/mod.rs#L12) | Directed edge with capacity, cooldown, and type-filtering. |
| `mod` | [`item`](src/graph/mod.rs#L14) | Typed items that flow through the graph. |
| `mod` | [`node`](src/graph/mod.rs#L16) | Graph node with conversion rules, overflow policy, and flow mode. |
| `mod` | [`simulation`](src/graph/mod.rs#L20) | Tick-based simulation: decay, transit, push/pull flow. |
| `mod` | [`supply_demand`](src/graph/mod.rs#L22) | Supply and demand processing for resource flow graphs. |
| `mod` | [`traversal`](src/graph/mod.rs#L18) | Dijkstra shortest-path on the graph. |

### `graph::core`

> Top-level directed graph container with node, edge, and item management.

*Source: [src/graph/core.rs](src/graph/core.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Graph`](src/graph/core.rs#L52) | A directed graph with typed nodes, edges, and flowing items.  # Fields - `nod... |
| `struct` | [`GraphStats`](src/graph/core.rs#L23) | Statistics snapshot of the graph state.  # Fields - `nodes` — `usize`. - `edg... |

### `graph::edge`

> Graph edge — a directed connection between nodes.

*Source: [src/graph/edge.rs](src/graph/edge.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Edge`](src/graph/edge.rs#L23) | A directed connection between two nodes in the graph.  # Fields - `id` — `u64... |

### `graph::item`

> Graph item — a typed entity that flows through the network.

*Source: [src/graph/item.rs](src/graph/item.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ItemPosition`](src/graph/item.rs#L10) | Where a `GraphItem` currently resides.  # Variants - `AtNode` — AtNode varian... |
| `struct` | [`GraphItem`](src/graph/item.rs#L35) | A typed entity that flows through the graph network.  # Fields - `id` — `u64`... |

### `graph::node`

> Graph node — a vertex with capacity, flow control, conversion rules, and queuing.

*Source: [src/graph/node.rs](src/graph/node.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FlowMode`](src/graph/node.rs#L57) | How a node participates in automatic item flow.  # Variants - `Passive` — Pas... |
| `enum` | [`OverflowPolicy`](src/graph/node.rs#L13) | What happens when items arrive at a full node.  # Variants - `Reject` — Rejec... |
| `struct` | [`ConversionRule`](src/graph/node.rs#L105) | A rule that converts N input items of one type into M output items of another... |
| `struct` | [`Demand`](src/graph/node.rs#L136) | A demand declaration on a node.  # Fields - `item_type` — `String`. - `quanti... |
| `struct` | [`Node`](src/graph/node.rs#L170) | A vertex in the graph with capacity, flow control, conversion, and queuing.  ... |
| `struct` | [`Supply`](src/graph/node.rs#L122) | A supply declaration on a node.  # Fields - `item_type` — `String`. - `quanti... |

### `graph::simulation`

> Simulation engine — update(dt) and step() for item flow, decay, transit, and conversions.

*Source: [src/graph/simulation.rs](src/graph/simulation.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`GraphEvent`](src/graph/simulation.rs#L22) | Events generated during simulation for the Lua callback layer to dispatch.  #... |

### `graph::traversal`

> Dijkstra pathfinding and reachability queries on the graph.

*Source: [src/graph/traversal.rs](src/graph/traversal.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PathResult`](src/graph/traversal.rs#L15) | Result of a successful pathfinding query.  # Fields - `nodes` — `Vec<u64>`. -... |

## `graphics` {#graphics}

### `graphics`

*Source: [src/graphics/mod.rs](src/graphics/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`animation`](src/graphics/mod.rs#L2) | Sprite-sheet frame animation with configurable durations and looping. |
| `mod` | [`camera`](src/graphics/mod.rs#L4) | 2D camera with position, zoom, and rotation producing a view matrix. |
| `mod` | [`canvas`](src/graphics/mod.rs#L6) | Off-screen render targets (canvases) for deferred compositing. |
| `mod` | [`column_batch`](src/graphics/mod.rs#L8) | Wolfenstein-style raycasting column batch renderer. |
| `mod` | [`data_graph_renderer`](src/graphics/mod.rs#L10) | Mathematical function graph and chart renderer. |
| `mod` | [`decal_surface`](src/graphics/mod.rs#L12) | Persistent surface for stamping decal textures. |
| `mod` | [`draw_layer`](src/graphics/mod.rs#L14) | Z-ordered draw layer for controlling render order. |
| `mod` | [`font`](src/graphics/mod.rs#L16) | TTF/OTF font loading, glyph rasterization, and atlas packing. |
| `mod` | [`gpu_renderer`](src/graphics/mod.rs#L18) | GPU-accelerated renderer backed by wgpu (primary runtime renderer). |
| `mod` | [`large_map_renderer`](src/graphics/mod.rs#L20) | Optimized renderer for large tile-based maps with chunking and LOD. |
| `mod` | [`light2d`](src/graphics/mod.rs#L22) | 2D point light data container for lighting systems. |
| `mod` | [`mesh`](src/graphics/mod.rs#L24) | Custom geometry mesh with per-vertex position, UV, and color data. |
| `mod` | [`minimap`](src/graphics/mod.rs#L26) | Grid-based minimap with terrain, fog of war, objects, pings, and markers. |
| `mod` | [`nine_slice`](src/graphics/mod.rs#L28) | Nine-slice (9-patch) image rendering for scalable UI elements. |
| `mod` | [`palette_lut`](src/graphics/mod.rs#L30) | Color palette lookup table for shader-based palette swapping. |
| `mod` | [`polygon_map`](src/graphics/mod.rs#L32) | Polygon map renderer with region management and hit detection. |
| `mod` | [`postfx`](src/graphics/mod.rs#L34) | Stackable post-processing effects pipeline (bloom, blur, CRT, vignette, etc.). |
| `mod` | [`renderer`](src/graphics/mod.rs#L36) | DrawCommand queue, draw enums, and texture data types. |
| `mod` | [`shader`](src/graphics/mod.rs#L38) | Custom WGSL shader support with uniform variables. |
| `mod` | [`sprite`](src/graphics/mod.rs#L40) | Sprite struct combining a texture, transform, and tint color. |
| `mod` | [`sprite_batch`](src/graphics/mod.rs#L42) | Sprite batching for efficient rendering of many sprites sharing one texture. |
| `mod` | [`sprite_sheet`](src/graphics/mod.rs#L44) | Grid-based sprite sheet with directional support and named groups. |
| `mod` | [`srgb`](src/graphics/mod.rs#L46) | RGBA Color struct with named constants and conversion helpers. |
| `mod` | [`texture`](src/graphics/mod.rs#L48) | Texture loading and TextureData storage for the renderer. |
| `mod` | [`texture_atlas`](src/graphics/mod.rs#L50) | CPU-side bin-packing texture atlas using shelf algorithm. |
| `mod` | [`trail`](src/graphics/mod.rs#L52) | Trail renderer for fading ribbon effects. |
| `mod` | [`viewport`](src/graphics/mod.rs#L54) | Virtual resolution viewport with manual transform application. |
| `mod` | [`viewport_scale`](src/graphics/mod.rs#L56) | Virtual resolution viewport with automatic scaling. |

### `graphics::animation`

> Sprite animation system with named clips, speed control, and frame-level events.  An [`Animation`] stores a pool of [`AnimFrame`] entries (each defining a source rectangle and optional per-frame duration) and any number of named [`AnimClip`]s that reference those frames by index. Call [`Animation::update`] each tick and inspect [`Animation::drain_events`] for playback notifications.

*Source: [src/graphics/animation.rs](src/graphics/animation.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`AnimEvent`](src/graphics/animation.rs#L67) | Events emitted by [`Animation::update`].  # Variants - `Finished` — Finished ... |
| `struct` | [`AnimClip`](src/graphics/animation.rs#L47) | A named animation clip that references frames by index into the parent  # Fie... |
| `struct` | [`AnimFrame`](src/graphics/animation.rs#L30) | A single animation frame with a source rectangle and optional duration.  # Fi... |
| `struct` | [`Animation`](src/graphics/animation.rs#L90) | Sprite animation with named clips, speed control, and playback events.  # Usa... |
| `type` | [`AnimationFrame`](src/graphics/animation.rs#L18) | Backward-compatible alias for [`AnimFrame`].  Existing code that imports `Ani... |

### `graphics::camera`

> Camera types for 2D viewport control.  Provides the original [`Camera`] (used by `SharedState` for the flat `luna.graphics.setCamera()` API) and the new Phase 24 [`Camera2D`] with smooth follow, dead zone, bounds clamping, and screen-shake.

*Source: [src/graphics/camera.rs](src/graphics/camera.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Camera`](src/graphics/camera.rs#L21) | Basic camera with position, zoom, and rotation.  Used by `SharedState` for th... |
| `struct` | [`Camera2D`](src/graphics/camera.rs#L118) | Full-featured 2D camera with smooth follow, dead zone, bounds clamping,  # Fi... |

### `graphics::canvas`

> Off-screen render targets (canvases) for deferred compositing.  A `Canvas` stores the logical dimensions of an off-screen texture that the GPU renderer can draw to.  After rendering, the canvas can be composited to the screen (or another canvas) as a regular image.

*Source: [src/graphics/canvas.rs](src/graphics/canvas.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Canvas`](src/graphics/canvas.rs#L16) | An off-screen render target with a fixed pixel resolution.  # Fields - `width... |

### `graphics::column_batch`

> Wolfenstein-style raycasting column batch renderer.  Stores per-column wall data produced by a raycaster and provides helpers for updating columns from raw ray data and querying depth.

*Source: [src/graphics/column_batch.rs](src/graphics/column_batch.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ColumnBatch`](src/graphics/column_batch.rs#L58) | Wolfenstein-style raycasting column batch renderer.  # Fields - `columns` — `... |
| `struct` | [`ColumnData`](src/graphics/column_batch.rs#L18) | Per-column rendering state produced by a raycaster.  # Fields - `tex_u` — `f3... |

### `graphics::data_graph_renderer`

> Mathematical function graph and chart renderer.  [`GraphRenderer`] manages multiple named data series (line, scatter, bar) and provides viewport ↔ world coordinate mapping for rendering charts in a Luna2D game. The renderer stores pure data — actual draw calls are issued by the Lua wrapper in `graphics_api.rs`.

*Source: [src/graphics/data_graph_renderer.rs](src/graphics/data_graph_renderer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`GraphSeries`](src/graphics/data_graph_renderer.rs#L21) | A data series that can be added to a [`GraphRenderer`].  # Variants - `Line` ... |
| `struct` | [`GraphRenderer`](src/graphics/data_graph_renderer.rs#L87) | Mathematical function graph / chart renderer.  # Fields - `viewport` — `(f32,... |

### `graphics::decal_surface`

> Persistent surface for stamping decal textures.  A simple data container describing an off-screen render target onto which decals can be composited.

*Source: [src/graphics/decal_surface.rs](src/graphics/decal_surface.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DecalSurface`](src/graphics/decal_surface.rs#L14) | Persistent render target for stamping decals.  # Fields - `width` — `u32`. - ... |

### `graphics::draw_layer`

> Z-ordered draw layer for controlling render order.  Entries are queued with a z-order value and flushed in ascending order so that lower z-values are drawn first (back-to-front).

*Source: [src/graphics/draw_layer.rs](src/graphics/draw_layer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DrawLayer`](src/graphics/draw_layer.rs#L22) | Z-ordered draw callback queue.  Collects entries during the draw phase and fl... |
| `struct` | [`LayerEntry`](src/graphics/draw_layer.rs#L11) | A queued draw entry with its z-order.  # Fields - `z_order` — `f64`. - `callb... |

### `graphics::font`

> TTF/OTF font loading, glyph rasterization, and atlas packing for GPU text rendering.

*Source: [src/graphics/font.rs](src/graphics/font.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Font`](src/graphics/font.rs#L17) | A loaded TTF/OTF font with a glyph atlas for GPU rendering.  Wraps a `fontdue... |
| `struct` | [`GlyphInfo`](src/graphics/font.rs#L48) | Information about a single rasterized glyph in the atlas.  # Fields - `uv_x` ... |

### `graphics::gpu_renderer`

> GPU-accelerated 2D renderer for Luna2D, backed by wgpu.  Processes a `DrawCommand` queue each frame, tessellates geometry on the CPU, uploads vertex / index data to GPU buffers, and issues a single render pass.  # Design - Two render pipelines: *color* (solid + gradient shapes) and *texture* (images, sprites). - Transform stack maintained during command processing — identical to standard push/pop transform stack. - Colored geometry is batched in one draw call; each distinct texture source is one draw call. - Draw order: colored shapes first (in submission order), textured images second.

*Source: [src/graphics/gpu_renderer.rs](src/graphics/gpu_renderer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`GpuRenderer`](src/graphics/gpu_renderer.rs#L341) | GPU-accelerated renderer that processes `DrawCommand` queues via wgpu.  # Fie... |
| `struct` | [`RenderStats`](src/graphics/gpu_renderer.rs#L99) | Per-frame rendering statistics.  # Fields - `draw_calls` — `u32`. - `texture_... |

### `graphics::large_map_renderer`

> Optimized renderer for large tile-based maps with chunking and LOD.  [`LargeMapRenderer`] stores a flat tile array and divides it into [`MapChunk`]s for efficient culling. Dirty flags allow incremental rebuilds when individual tiles change. The renderer tracks camera position, zoom, and viewport to compute which chunks are visible.

*Source: [src/graphics/large_map_renderer.rs](src/graphics/large_map_renderer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`LargeMapRenderer`](src/graphics/large_map_renderer.rs#L55) | Optimized large tile-map renderer with chunked culling.  # Fields - `tile_wid... |
| `struct` | [`MapChunk`](src/graphics/large_map_renderer.rs#L22) | A chunk of tiles pre-batched for fast culling and rendering.  # Fields - `cx`... |

### `graphics::light2d`

> 2D point light data container for lighting systems.

*Source: [src/graphics/light2d.rs](src/graphics/light2d.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Light2D`](src/graphics/light2d.rs#L17) | 2D point light with position, radius, color, and intensity.  # Fields - `x` —... |

### `graphics::mesh`

> Mesh API for custom geometry rendering.  Meshes allow Lua scripts to define arbitrary vertex data for rendering textured or colored geometry.

*Source: [src/graphics/mesh.rs](src/graphics/mesh.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`MeshDrawMode`](src/graphics/mesh.rs#L15) | Drawing mode for mesh geometry.  # Variants - `Triangles` — Triangles variant... |
| `struct` | [`Mesh`](src/graphics/mesh.rs#L78) | Custom geometry mesh with per-vertex position, UV, and color data.  # Fields ... |
| `struct` | [`MeshVertex`](src/graphics/mesh.rs#L36) | A single vertex in a mesh.  # Fields - `x` — `f32`. - `y` — `f32`. - `u` — `f... |

### `graphics::minimap`

> Minimap data structures for overhead map displays.  Provides a grid-based minimap with terrain coloring, fog of war, tracked objects, pings, markers, and viewport rectangle overlay.

*Source: [src/graphics/minimap.rs](src/graphics/minimap.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ColorMode`](src/graphics/minimap.rs#L10) | How cells are colored on the minimap. |
| `enum` | [`FogLevel`](src/graphics/minimap.rs#L20) | Fog-of-war visibility level for a cell. |
| `struct` | [`Minimap`](src/graphics/minimap.rs#L94) | A grid-based minimap with terrain, fog, objects, pings, and markers. |
| `struct` | [`MinimapMarker`](src/graphics/minimap.rs#L81) | A persistent labeled marker on the minimap. |
| `struct` | [`MinimapObject`](src/graphics/minimap.rs#L53) | A tracked object on the minimap. |
| `struct` | [`MinimapObjectType`](src/graphics/minimap.rs#L42) | A registered object type with a display color and visibility toggle. |
| `struct` | [`MinimapPing`](src/graphics/minimap.rs#L66) | A temporary animated ping on the minimap. |

### `graphics::nine_slice`

> Nine-slice (9-patch) image rendering for scalable UI elements.  A nine-slice divides a texture into 9 regions using 4 border insets: corners are drawn at fixed size, edges stretch in one axis, and the center stretches in both axes. This preserves visual quality when scaling UI panels, buttons, and dialog boxes.

*Source: [src/graphics/nine_slice.rs](src/graphics/nine_slice.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`NineSlice`](src/graphics/nine_slice.rs#L33) | A nine-slice image definition: a texture plus border insets.  The four insets... |
| `type` | [`Patch`](src/graphics/nine_slice.rs#L11) | A single patch rectangle: `(src_x, src_y, src_w, src_h, dst_x, dst_y, dst_w, ... |

### `graphics::palette_lut`

> Color palette lookup table for shader-based palette swapping.  Maps source colors to target colors, allowing sprites to be re-colored at render time without modifying the original texture.

*Source: [src/graphics/palette_lut.rs](src/graphics/palette_lut.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PaletteLUT`](src/graphics/palette_lut.rs#L17) | Color palette lookup table mapping source colors to target colors.  # Fields ... |

### `graphics::polygon_map`

> Polygon map renderer with region management and hit detection.  Manages a collection of named polygon regions with colors, labels, highlighting, and point-in-polygon queries.

*Source: [src/graphics/polygon_map.rs](src/graphics/polygon_map.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PolygonMap`](src/graphics/polygon_map.rs#L41) | Polygon map renderer with region management and hit detection.  # Fields - `o... |
| `struct` | [`PolygonRegion`](src/graphics/polygon_map.rs#L18) | A named polygon region.  # Fields - `name` — `String`. - `vertices` — `Vec<f3... |

### `graphics::postfx`

> Post-processing effects module.  Provides a stackable post-processing pipeline with built-in effects (bloom, blur, CRT, vignette, chromatic aberration, colour grading, godrays) and custom shader passes. Effects are organized into a `PostFxStack` that captures the scene via ping-pong canvases, applies all enabled effects in order, and outputs the final result.

*Source: [src/graphics/postfx.rs](src/graphics/postfx.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`PostFxEffectType`](src/graphics/postfx.rs#L15) | Built-in effect types for the post-processing pipeline.  Each variant produce... |
| `struct` | [`PostFxEffect`](src/graphics/postfx.rs#L118) | A single post-processing effect with named float parameters.  # Fields - `eff... |
| `struct` | [`PostFxStack`](src/graphics/postfx.rs#L232) | An ordered chain of effects that captures and processes the rendered scene.  ... |

### `graphics::renderer`

> Draw command types, blend modes, and texture data for the Luna2D rendering pipeline.

*Source: [src/graphics/renderer.rs](src/graphics/renderer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BlendMode`](src/graphics/renderer.rs#L100) | Blending mode for draw operations.  # Variants - `Alpha` — Alpha variant. - `... |
| `enum` | [`CompareMode`](src/graphics/renderer.rs#L20) | Stencil comparison mode for `luna.graphics.setStencilTest`.  # Variants - `Eq... |
| `enum` | [`DrawCommand`](src/graphics/renderer.rs#L138) | A single deferred draw operation queued during `luna.draw()` and executed by ... |
| `enum` | [`DrawMode`](src/graphics/renderer.rs#L86) | Whether a shape is drawn filled or as an outline.  # Variants - `Fill` — Draw... |
| `enum` | [`StencilAction`](src/graphics/renderer.rs#L48) | Stencil write action for `luna.graphics.stencil`.  # Variants - `Replace` — R... |
| `enum` | [`TextAlign`](src/graphics/renderer.rs#L69) | Text alignment mode for formatted text printing.  # Variants - `Left` — Left ... |
| `struct` | [`TextureData`](src/graphics/renderer.rs#L417) | Raw RGBA pixel data for a loaded texture, stored in the renderer's texture at... |

### `graphics::shader`

> Custom WGSL shader support for Luna2D.  Allows Lua scripts to create and apply custom fragment shaders with uniform variables.

*Source: [src/graphics/shader.rs](src/graphics/shader.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ShaderFragmentInput`](src/graphics/shader.rs#L16) | Which fragment shader input the user's entry point expects.  # Variants - `Co... |
| `enum` | [`UniformValue`](src/graphics/shader.rs#L66) | A uniform value that can be sent to a shader from Lua.  # Variants - `Float` ... |
| `struct` | [`Shader`](src/graphics/shader.rs#L46) | Represents a compiled custom shader with its uniform values.  # Fields - `sou... |

### `graphics::sprite`

*Source: [src/graphics/sprite.rs](src/graphics/sprite.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Sprite`](src/graphics/sprite.rs#L15) | A textured game object with position, scale, rotation, and tint color.  `Spri... |

### `graphics::sprite_batch`

> Sprite batching for efficient rendering of many sprites sharing one texture.

*Source: [src/graphics/sprite_batch.rs](src/graphics/sprite_batch.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BatchEntry`](src/graphics/sprite_batch.rs#L32) | A single sprite in a batch, describing position, region, and transform.  # Fi... |
| `struct` | [`SpriteBatch`](src/graphics/sprite_batch.rs#L9) | A batch of sprites sharing a single texture, drawn in one GPU call.  Created ... |

### `graphics::sprite_sheet`

> Grid-based sprite sheet with directional support and named groups.  Splits a texture into a uniform grid of frames and provides indexed, row/column, named-group, and directional access to the resulting quads.

*Source: [src/graphics/sprite_sheet.rs](src/graphics/sprite_sheet.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DirectionLayout`](src/graphics/sprite_sheet.rs#L32) | Directional layout for sprite sets.  # Variants - `Rows` — Rows variant. - `C... |
| `struct` | [`FrameGroup`](src/graphics/sprite_sheet.rs#L17) | Named frame group within the sprite sheet.  # Fields - `name` — `String`. - `... |
| `struct` | [`SpriteSheet`](src/graphics/sprite_sheet.rs#L51) | Grid-based sprite sheet with directional support and named groups.  # Fields ... |

### `graphics::srgb`

*Source: [src/graphics/srgb.rs](src/graphics/srgb.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Color`](src/graphics/srgb.rs#L12) | RGBA color stored as `f32` components in the range `[0.0, 1.0]`.  Used everyw... |

### `graphics::texture`

*Source: [src/graphics/texture.rs](src/graphics/texture.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Texture`](src/graphics/texture.rs#L16) | A loaded image asset referenced by its index into the renderer's texture list... |

### `graphics::texture_atlas`

> CPU-side bin-packing texture atlas using a shelf algorithm.  Packs named rectangular regions into a fixed-size atlas without any GPU interaction. Useful for building sprite-sheet layouts at load time.

*Source: [src/graphics/texture_atlas.rs](src/graphics/texture_atlas.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`AtlasRegion`](src/graphics/texture_atlas.rs#L17) | A named rectangular region packed into the atlas.  # Fields - `name` — `Strin... |
| `struct` | [`TextureAtlas`](src/graphics/texture_atlas.rs#L50) | CPU-side bin-packing atlas for sprite regions.  # Fields - `width` — `u32`. -... |

### `graphics::trail`

> Trail renderer for fading ribbon effects.  Stores a series of timestamped points that age out over a configurable lifetime, producing a tapered ribbon from head to tail.

*Source: [src/graphics/trail.rs](src/graphics/trail.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Trail`](src/graphics/trail.rs#L38) | Fading textured ribbon renderer.  # Fields - `points` — `Vec<TrailPoint>`. - ... |
| `struct` | [`TrailPoint`](src/graphics/trail.rs#L15) | A point in a trail with age tracking.  # Fields - `x` — `f32`. - `y` — `f32`.... |

### `graphics::viewport`

> Virtual resolution viewport with manual transform application.  Maps a fixed game resolution onto an arbitrary window size using letterboxing, stretching, or pixel-perfect scaling.

*Source: [src/graphics/viewport.rs](src/graphics/viewport.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ScaleMode`](src/graphics/viewport.rs#L13) | Scale mode for virtual resolution mapping.  # Variants - `Letterbox` — Letter... |
| `struct` | [`Viewport`](src/graphics/viewport.rs#L36) | Virtual resolution with manual transform application.  # Fields - `game_width... |

### `graphics::viewport_scale`

> Virtual resolution viewport with automatic scaling and transform stack integration.  Like `Viewport`, but also tracks the scaled content dimensions for use with an automatic graphics transform stack.

*Source: [src/graphics/viewport_scale.rs](src/graphics/viewport_scale.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ViewportScale`](src/graphics/viewport_scale.rs#L24) | Virtual resolution with automatic graphics stack management.  # Fields - `gam... |

## `image` {#image}

### `image`

> CPU-side pixel-level image manipulation.  Provides `ImageData` for reading and writing individual pixels in RGBA8 format.

*Source: [src/image/mod.rs](src/image/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`image_data`](src/image/mod.rs#L6) | RGBA8 pixel buffer with per-pixel read/write access. |

### `image::image_data`

> CPU-side RGBA8 pixel buffer for image manipulation.

*Source: [src/image/image_data.rs](src/image/image_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ImageData`](src/image/image_data.rs#L10) | CPU-side pixel buffer in RGBA8 format.  Stores pixel data in row-major order,... |

## `input` {#input}

### `input`

*Source: [src/input/mod.rs](src/input/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`gamepad`](src/input/mod.rs#L2) | Gamepad button and axis state for a single controller. |
| `mod` | [`keyboard`](src/input/mod.rs#L4) | Keyboard state tracking pressed, held, and released keys. |
| `mod` | [`mouse`](src/input/mod.rs#L6) | Mouse position and button state management. |
| `mod` | [`touch`](src/input/mod.rs#L8) | Touch input state tracking for touchscreens. |

### `input::gamepad`

*Source: [src/input/gamepad.rs](src/input/gamepad.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`gilrs_axis_to_string`](src/input/gamepad.rs#L196) | Converts a `gilrs::Axis` to a engine-compatible string name.  # Parameters - ... |
| `fn` | [`gilrs_button_to_string`](src/input/gamepad.rs#L168) | Converts a `gilrs::Button` to a engine-compatible string name.  # Parameters ... |
| `struct` | [`GamepadState`](src/input/gamepad.rs#L11) | Holds the current button and axis state for a single gamepad identified by it... |

### `input::keyboard`

*Source: [src/input/keyboard.rs](src/input/keyboard.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`get_key_from_scancode`](src/input/keyboard.rs#L237) | Resolves a physical scancode string to the closest logical Luna key name.  # ... |
| `fn` | [`get_scancode_from_key`](src/input/keyboard.rs#L205) | Resolves a logical Luna key name to the closest physical scancode string.  # ... |
| `fn` | [`winit_key_to_string`](src/input/keyboard.rs#L271) | Converts a `winit 0.30` logical `Key` to the lowercase string name used by th... |
| `fn` | [`winit_scancode_to_string`](src/input/keyboard.rs#L322) | Converts a `winit 0.30` physical `KeyCode` to a engine-compatible scancode st... |
| `struct` | [`KeyboardState`](src/input/keyboard.rs#L6) | Tracks which keyboard keys are currently down, just pressed, or just released... |

### `input::mouse`

*Source: [src/input/mouse.rs](src/input/mouse.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`SystemCursor`](src/input/mouse.rs#L16) | Standard system cursor shapes.  # Variants - `Arrow` — Arrow variant. - `IBea... |
| `struct` | [`MouseState`](src/input/mouse.rs#L106) | Tracks mouse cursor position and per-button pressed/down/released state.  # F... |

### `input::touch`

> Touch input state tracking for Luna2D.

*Source: [src/input/touch.rs](src/input/touch.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`TouchPoint`](src/input/touch.rs#L13) | Information about a single touch point.  # Fields - `id` — `u64`. - `x` — `f6... |
| `struct` | [`TouchState`](src/input/touch.rs#L26) | Tracks active touch points. |

## `inventory` {#inventory}

### `inventory`

> Inventory system — slot-based item containers, equipment slots, item sets, subsystem toggles (weight, size, stacking, sets), and callback hooks.

*Source: [src/inventory/mod.rs](src/inventory/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ContainerMode`](src/inventory/mod.rs#L236) | Container storage mode. |
| `enum` | [`SlotState`](src/inventory/mod.rs#L135) | Valid state strings for a slot. |
| `struct` | [`Container`](src/inventory/mod.rs#L268) | A named collection of `Slot`s. |
| `struct` | [`Inventory`](src/inventory/mod.rs#L517) | Top-level inventory managing containers, equip slots, item sets, and callbacks. |
| `struct` | [`Item`](src/inventory/mod.rs#L12) | A single item definition with type, tags, weight, size, and stack limit. |
| `struct` | [`ItemSet`](src/inventory/mod.rs#L454) | A named set defining bonus conditions: all requirements must be satisfied sim... |
| `struct` | [`ItemStack`](src/inventory/mod.rs#L72) | A counted stack of a single Item type. |
| `struct` | [`SetRequirement`](src/inventory/mod.rs#L445) | A single requirement in an `ItemSet`. |
| `struct` | [`Slot`](src/inventory/mod.rs#L167) | A single inventory position that holds an optional `ItemStack`. |
| `struct` | [`SubsystemFlags`](src/inventory/mod.rs#L504) | Active subsystem flags for an `Inventory`. |

## `lua_api` {#lua-api}

### `lua_api`

*Source: [src/lua_api/mod.rs](src/lua_api/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FullscreenType`](src/lua_api/mod.rs#L36) | Fullscreen mode type for window management.  # Variants - `Desktop` — Desktop... |
| `fn` | [`create_lua_vm`](src/lua_api/mod.rs#L454) | Creates and configures the Lua VM, registers all `luna.*` sub-APIs, and retur... |
| `mod` | [`ai_api`](src/lua_api/mod.rs#L156) | Registers the `luna.ai.*` game AI toolkit API. |
| `mod` | [`audio_api`](src/lua_api/mod.rs#L158) | Registers the `luna.audio.*` sound playback API. |
| `mod` | [`cardgame_api`](src/lua_api/mod.rs#L234) | Registers the `luna.cardgame.*` card game API. |
| `mod` | [`combat_api`](src/lua_api/mod.rs#L236) | Registers the `luna.combat.*` combat system API. |
| `mod` | [`compute_api`](src/lua_api/mod.rs#L160) | Registers the `luna.compute.*` array computation API. |
| `mod` | [`crafting_api`](src/lua_api/mod.rs#L246) | Registers the `luna.crafting.*` crafting system API. |
| `mod` | [`data_api`](src/lua_api/mod.rs#L162) | Registers the `luna.data.*` binary data, compression, hashing, and encoding API. |
| `mod` | [`dataframe_api`](src/lua_api/mod.rs#L164) | Registers the `luna.dataframe.*` tabular data API. |
| `mod` | [`debug_api`](src/lua_api/mod.rs#L168) | Registers the `luna.debug.*` runtime diagnostics and developer tools API. |
| `mod` | [`debugbridge_api`](src/lua_api/mod.rs#L166) | Registers the `luna.debugbridge.*` TCP debug server API. |
| `mod` | [`dialog_api`](src/lua_api/mod.rs#L208) | Registers the `luna.dialog.*` dialog sequencer API. |
| `mod` | [`docs_api`](src/lua_api/mod.rs#L170) | Registers the `luna.docs.*` documentation management API. |
| `mod` | [`entity_api`](src/lua_api/mod.rs#L172) | Registers the `luna.entity.*` ECS universe API. |
| `mod` | [`event_api`](src/lua_api/mod.rs#L174) | Registers the `luna.event.*` engine control API. |
| `mod` | [`filesystem_api`](src/lua_api/mod.rs#L176) | Registers the `luna.filesystem.*` sandboxed I/O API. |
| `mod` | [`graph_api`](src/lua_api/mod.rs#L178) | Registers the `luna.graph.*` directed-graph and item-flow simulation API. |
| `mod` | [`graphics_api`](src/lua_api/mod.rs#L180) | Registers the `luna.graphics.*` drawing API. |
| `mod` | [`graphics_ext_api`](src/lua_api/mod.rs#L182) | Registers Phase 24 `luna.graphics.*` extension types (Light2D, Camera2D, etc.). |
| `mod` | [`image_api`](src/lua_api/mod.rs#L184) | Registers the `luna.image.*` pixel-level image manipulation API. |
| `mod` | [`input_api`](src/lua_api/mod.rs#L190) | Registers the `luna.keyboard.*` and `luna.mouse.*` input API. |
| `mod` | [`inventory_api`](src/lua_api/mod.rs#L242) | Registers the `luna.inventory.*` inventory system API. |
| `mod` | [`localization_api`](src/lua_api/mod.rs#L188) | Registers the `luna.localization.*` internationalization API. |
| `mod` | [`log_api`](src/lua_api/mod.rs#L186) | Registers the `luna.log.*` structured game-level logging API. |
| `mod` | [`lua_types`](src/lua_api/mod.rs#L230) | UserData type utilities for Luna2D Lua objects. |
| `mod` | [`math_api`](src/lua_api/mod.rs#L192) | Registers the `luna.math.*` vector and math helper API. |
| `mod` | [`math_ext_api`](src/lua_api/mod.rs#L194) | Registers Phase 25 `luna.math.*` extension types (Vec2, Grid, Noise, etc.). |
| `mod` | [`minimap_api`](src/lua_api/mod.rs#L206) | Registers the `luna.minimap.*` minimap API. |
| `mod` | [`modding_api`](src/lua_api/mod.rs#L196) | Registers the `luna.modding.*` mod management API. |
| `mod` | [`particle_api`](src/lua_api/mod.rs#L198) | Registers the `luna.particle.*` particle-effects API. |
| `mod` | [`pathfinding_api`](src/lua_api/mod.rs#L200) | Registers the `luna.pathfinding.*` grid-based pathfinding API. |
| `mod` | [`patterns_api`](src/lua_api/mod.rs#L202) | Registers the `luna.patterns.*` software design patterns API. |
| `mod` | [`physics_api`](src/lua_api/mod.rs#L204) | Registers the `luna.physics.*` rigid-body simulation API. |
| `mod` | [`postfx_api`](src/lua_api/mod.rs#L210) | Registers the `luna.postfx.*` post-processing effects API. |
| `mod` | [`quest_api`](src/lua_api/mod.rs#L244) | Registers the `luna.quest.*` quest tracking API. |
| `mod` | [`resource_api`](src/lua_api/mod.rs#L238) | Registers the `luna.resource.*` resource management API. |
| `mod` | [`savegame_api`](src/lua_api/mod.rs#L212) | Registers the `luna.savegame.*` save/load system API. |
| `mod` | [`scene_api`](src/lua_api/mod.rs#L214) | Registers the `luna.scene.*` scene stack, registry, data store, and depth-sor... |
| `mod` | [`sound_api`](src/lua_api/mod.rs#L216) | Registers the `luna.sound.*` decoded audio sample manipulation API. |
| `mod` | [`stats_api`](src/lua_api/mod.rs#L240) | Registers the `luna.stats.*` character stats API. |
| `mod` | [`system_api`](src/lua_api/mod.rs#L218) | Registers the `luna.system.*` platform query API. |
| `mod` | [`thread_api`](src/lua_api/mod.rs#L220) | Registers the `luna.thread.*` multithreading API. |
| `mod` | [`tilemap_api`](src/lua_api/mod.rs#L226) | Registers the `luna.tilemap.*` tile map, tileset, autotile, and procedural ge... |
| `mod` | [`timer_api`](src/lua_api/mod.rs#L228) | Registers the `luna.timer.*` frame-timing API. |
| `mod` | [`window_api`](src/lua_api/mod.rs#L232) | Registers the `luna.window.*` window management API. |
| `struct` | [`ErrorInfo`](src/lua_api/mod.rs#L256) | Structured error information for the last engine error.  # Fields - `message`... |
| `struct` | [`SharedState`](src/lua_api/mod.rs#L294) | Shared mutable state passed via `Rc<RefCell<SharedState>>` to all Lua API clo... |
| `struct` | [`WindowState`](src/lua_api/mod.rs#L73) | Tracks window state and queues window operations for the event loop.  # Field... |

### `lua_api::ai_api`

> Registers the `luna.ai.*` game AI toolkit API.  Provides factory functions and UserData wrappers for AI subsystems: AIWorld, Agent, Blackboard, StateMachine, BehaviorTree, SteeringManager, PathGrid, FlowField, QLearner, UtilityAI, GOAPPlanner, InfluenceMap, Squad, and CommandQueue.

*Source: [src/lua_api/ai_api.rs](src/lua_api/ai_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/ai_api.rs#L2128) | Registers the `luna.ai.*` game AI toolkit API.  # Parameters - `lua` — `&Lua`... |

### `lua_api::audio_api`

*Source: [src/lua_api/audio_api.rs](src/lua_api/audio_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/audio_api.rs#L872) | Registers all `luna.audio.*` functions into the Lua VM.  # Parameters - `lua`... |
| `struct` | [`LuaBus`](src/lua_api/audio_api.rs#L293) | Lua UserData wrapper for an audio bus.  # Fields - `state` — `Rc<RefCell<Shar... |
| `struct` | [`LuaMidiPlayer`](src/lua_api/audio_api.rs#L386) | Lua UserData wrapper for the MIDI player.  # Fields - `inner` — `Rc<RefCell<M... |
| `struct` | [`LuaSource`](src/lua_api/audio_api.rs#L20) | Lua UserData wrapper for an audio source resource.  # Fields - `state` — `Rc<... |

### `lua_api::cardgame_api`

> Lua bindings for `luna.cardgame.*`.  Exposes Card, Deck, Zone, StackManager, DeckBuilder, and CardPool to Lua.

*Source: [src/lua_api/cardgame_api.rs](src/lua_api/cardgame_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/cardgame_api.rs#L868) | Register the `luna.cardgame.*` table. |
| `struct` | [`LuaCard`](src/lua_api/cardgame_api.rs#L21) | Lua-facing `Card` userdata. |
| `struct` | [`LuaCardPool`](src/lua_api/cardgame_api.rs#L789) | *(undocumented)* |
| `struct` | [`LuaDeck`](src/lua_api/cardgame_api.rs#L284) | Lua-facing `Deck` userdata. |
| `struct` | [`LuaDeckBuilder`](src/lua_api/cardgame_api.rs#L683) | *(undocumented)* |
| `struct` | [`LuaStackManager`](src/lua_api/cardgame_api.rs#L589) | *(undocumented)* |
| `struct` | [`LuaZone`](src/lua_api/cardgame_api.rs#L460) | Lua-facing `Zone` userdata. |

### `lua_api::combat_api`

> Lua bindings for `luna.combat.*`.

*Source: [src/lua_api/combat_api.rs](src/lua_api/combat_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/combat_api.rs#L653) | Register the `luna.combat.*` table. |
| `struct` | [`LuaCombatAction`](src/lua_api/combat_api.rs#L66) | *(undocumented)* |
| `struct` | [`LuaCombatBattle`](src/lua_api/combat_api.rs#L431) | *(undocumented)* |
| `struct` | [`LuaCombatant`](src/lua_api/combat_api.rs#L152) | *(undocumented)* |
| `struct` | [`LuaStatusEffect`](src/lua_api/combat_api.rs#L15) | *(undocumented)* |

### `lua_api::compute_api`

> Registers the `luna.compute.*` array computation API.

*Source: [src/lua_api/compute_api.rs](src/lua_api/compute_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/compute_api.rs#L549) | Registers the `luna.compute` table with array factory functions.  # Parameter... |

### `lua_api::crafting_api`

> Lua bindings for `luna.crafting.*`.

*Source: [src/lua_api/crafting_api.rs](src/lua_api/crafting_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/crafting_api.rs#L843) | Register the `luna.crafting.*` table. |
| `struct` | [`LuaCraftQueue`](src/lua_api/crafting_api.rs#L505) | *(undocumented)* |
| `struct` | [`LuaCraftSkill`](src/lua_api/crafting_api.rs#L441) | *(undocumented)* |
| `struct` | [`LuaRecipe`](src/lua_api/crafting_api.rs#L15) | *(undocumented)* |
| `struct` | [`LuaRecipeRegistry`](src/lua_api/crafting_api.rs#L236) | *(undocumented)* |
| `struct` | [`LuaStation`](src/lua_api/crafting_api.rs#L353) | *(undocumented)* |
| `struct` | [`LuaUpgradeTree`](src/lua_api/crafting_api.rs#L697) | *(undocumented)* |

### `lua_api::data_api`

> Registers the `luna.data.*` binary data, compression, hashing, and encoding API.

*Source: [src/lua_api/data_api.rs](src/lua_api/data_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/data_api.rs#L15) | Registers the `luna.data` table on the provided `luna` namespace.  # Paramete... |

### `lua_api::dataframe_api`

> Registers the `luna.dataframe.*` tabular data API.

*Source: [src/lua_api/dataframe_api.rs](src/lua_api/dataframe_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/dataframe_api.rs#L806) | Register the `luna.dataframe` namespace.  # Parameters - `lua` — `&Lua`. - `l... |

### `lua_api::debug_api`

> Registers the `luna.devtools.*` runtime diagnostics and developer tools API.  Five subsystems: structured logger, hierarchical profiler, frame statistics, file watcher, and Lua debug bridge. All module-level functions — no registered types.

*Source: [src/lua_api/debug_api.rs](src/lua_api/debug_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/debug_api.rs#L194) | Registers the `luna.devtools` namespace. |

### `lua_api::debugbridge_api`

> Registers the `luna.debugbridge.*` TCP debug server API.  Embeds a JSON-over-TCP server (127.0.0.1 only) inside the running game. External tools (VS Code extension, MCP server) connect to inspect and control the game at runtime. Network I/O runs on a background thread; Lua-dependent methods are queued and dispatched via `poll()` on the main thread.

*Source: [src/lua_api/debugbridge_api.rs](src/lua_api/debugbridge_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/debugbridge_api.rs#L355) | Registers the `luna.debugbridge` namespace. |

### `lua_api::dialog_api`

> Lua API bindings for the `luna.dialog.*` dialog sequencer module.  Provides a `Sequencer` UserData type for visual novel-style text presentation with typewriter effect, branching choices, timed pauses, and inline callbacks.

*Source: [src/lua_api/dialog_api.rs](src/lua_api/dialog_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/dialog_api.rs#L382) | Registers the `luna.dialog` module with the Lua VM. |
| `struct` | [`LuaSequencer`](src/lua_api/dialog_api.rs#L45) | Lua UserData wrapper for a dialog sequencer. |

### `lua_api::docs_api`

> Registers the `luna.docs.*` documentation management API.  Provides engine-integrated documentation management: scanning runtime bindings, loading TOML doc catalogs, validating completeness, computing quality metrics, and exporting structured data for VS Code extension IntelliSense.

*Source: [src/lua_api/docs_api.rs](src/lua_api/docs_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/docs_api.rs#L896) | Registers the `luna.docs` namespace. |

### `lua_api::entity_api`

> Registers the `luna.entity.*` ECS universe API.  Exposes `Universe` as Lua UserData (`LuaUniverse`) with entity lifecycle, component operations, querying, systems, tags, layers, and blueprints.

*Source: [src/lua_api/entity_api.rs](src/lua_api/entity_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/entity_api.rs#L543) | Registers the `luna.entity` table with the `newUniverse` factory function.  #... |

### `lua_api::event_api`

*Source: [src/lua_api/event_api.rs](src/lua_api/event_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/event_api.rs#L138) | Registers `luna.event.quit()` and related engine lifecycle functions into the... |

### `lua_api::filesystem_api`

*Source: [src/lua_api/filesystem_api.rs](src/lua_api/filesystem_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/filesystem_api.rs#L134) | Registers `luna.filesystem.*` functions into the Lua VM.  # Parameters - `lua... |

### `lua_api::graph_api`

> Registers the `luna.graph.*` directed-graph and item-flow simulation API.  Exposes `LuaGraph`, `LuaNode`, `LuaEdge`, and `LuaGraphItem` UserData types wrapping `crate::graph::Graph` with callback dispatch for simulation events.

*Source: [src/lua_api/graph_api.rs](src/lua_api/graph_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/graph_api.rs#L1962) | Register the `luna.graph` API table.  # Parameters - `lua` — `&Lua`. - `luna`... |

### `lua_api::graphics_api`

*Source: [src/lua_api/graphics_api.rs](src/lua_api/graphics_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/graphics_api.rs#L650) | Registers all `luna.graphics.*` drawing commands into the Lua VM.  # Paramete... |
| `struct` | [`LuaCanvas`](src/lua_api/graphics_api.rs#L412) | Lua UserData wrapper for an off-screen canvas resource.  # Fields - `state` —... |
| `struct` | [`LuaFont`](src/lua_api/graphics_api.rs#L187) | Lua UserData wrapper for a loaded font resource.  # Fields - `state` — `Rc<Re... |
| `struct` | [`LuaImage`](src/lua_api/graphics_api.rs#L26) | Lua UserData wrapper for a loaded texture/image resource.  # Fields - `state`... |
| `struct` | [`LuaNineSlice`](src/lua_api/graphics_api.rs#L109) | Lua UserData wrapper for a nine-slice (9-patch) image definition.  Stores the... |
| `struct` | [`LuaSpriteBatch`](src/lua_api/graphics_api.rs#L305) | Lua UserData wrapper for a sprite batch resource.  # Fields - `state` — `Rc<R... |

### `lua_api::graphics_ext_api`

> Lua API bindings for Phase 24 graphics extension types.  Provides UserData wrappers and factory functions for extended graphics subsystems: Light2D, TextureAtlas, DrawLayer, Viewport, ViewportScale, SpriteSheet, PolygonMap, GraphRenderer, LargeMapRenderer, ColumnBatch, Camera2D, Animation, Trail, DecalSurface, and PaletteLUT.

*Source: [src/lua_api/graphics_ext_api.rs](src/lua_api/graphics_ext_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/graphics_ext_api.rs#L2301) | Registers Phase 24 graphics extension factory functions on `luna.graphics`.  ... |
| `struct` | [`LuaDecalSurface`](src/lua_api/graphics_ext_api.rs#L2161) | Lua UserData wrapper for DecalSurface (internal — no factory). |
| `struct` | [`LuaPaletteLUT`](src/lua_api/graphics_ext_api.rs#L2200) | Lua UserData wrapper for PaletteLUT (internal — no factory). |
| `struct` | [`LuaTrail`](src/lua_api/graphics_ext_api.rs#L2044) | Lua UserData wrapper for Trail (internal — no factory). |

### `lua_api::image_api`

> Registers the `luna.image.*` pixel-level image manipulation API.

*Source: [src/lua_api/image_api.rs](src/lua_api/image_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/image_api.rs#L20) | Registers the `luna.image` table on the provided `luna` namespace.  # Paramet... |

### `lua_api::input_api`

*Source: [src/lua_api/input_api.rs](src/lua_api/input_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/input_api.rs#L17) | Registers `luna.keyboard.*` and `luna.mouse.*` query functions into the Lua V... |

### `lua_api::inventory_api`

> Lua API bindings for the `luna.inventory.*` slot-based inventory module.  Provides `Item`, `ItemStack`, `Slot`, `Container`, `ItemSet`, and `Inventory` UserData objects for building RPG-style inventory systems in Lua.

*Source: [src/lua_api/inventory_api.rs](src/lua_api/inventory_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/inventory_api.rs#L1006) | Register the `luna.inventory` module. |
| `struct` | [`LuaContainer`](src/lua_api/inventory_api.rs#L430) | Lua UserData wrapper for a named collection of slots. |
| `struct` | [`LuaInventory`](src/lua_api/inventory_api.rs#L722) | Lua UserData wrapper for the top-level inventory. |
| `struct` | [`LuaItem`](src/lua_api/inventory_api.rs#L23) | Lua UserData wrapper for a single item definition. |
| `struct` | [`LuaItemSet`](src/lua_api/inventory_api.rs#L658) | Lua UserData wrapper for a named item set with requirements. |
| `struct` | [`LuaItemStack`](src/lua_api/inventory_api.rs#L174) | Lua UserData wrapper for a counted stack of items. |
| `struct` | [`LuaSlot`](src/lua_api/inventory_api.rs#L288) | Lua UserData wrapper for a single inventory slot. |

### `lua_api::localization_api`

> Registers the `luna.localization.*` internationalization API.  Provides a structured translation system supporting multiple languages, fallback chains, pluralization, variable interpolation, and language-change callbacks.

*Source: [src/lua_api/localization_api.rs](src/lua_api/localization_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/localization_api.rs#L100) | Registers `luna.localization.*` functions. |

### `lua_api::log_api`

> Structured game-level logging API (`luna.log.*`).  Exposes convenience logging functions so Lua scripts can emit messages at specific severity levels and query or change the active log level at runtime without going through the `luna.system` namespace.

*Source: [src/lua_api/log_api.rs](src/lua_api/log_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/log_api.rs#L15) | Registers the `luna.log.*` namespace into the shared `luna` table.  # Errors ... |

### `lua_api::lua_types`

> UserData type utilities for Luna2D Lua objects.  Provides the `LunaType` trait and `add_type_methods` helper so every Luna2D UserData object exposes `type()` and `typeOf()` methods matching standard game object type system.

*Source: [src/lua_api/lua_types.rs](src/lua_api/lua_types.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`add_type_methods`](src/lua_api/lua_types.rs#L26) | Adds standard `type()` and `typeOf()` methods to a UserData definition.  # Pa... |
| `trait` | [`LunaType`](src/lua_api/lua_types.rs#L11) | Standard type identification for Luna2D UserData objects.  Every Luna2D Lua o... |

### `lua_api::math_api`

*Source: [src/lua_api/math_api.rs](src/lua_api/math_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/math_api.rs#L430) | Registers `luna.math.*` helpers (Vec2, distance, random, noise, transforms, e... |

### `lua_api::math_ext_api`

> Extended math API bindings — Phase 25 types and utility functions.  Adds 7 factory functions and 23 module-level functions to `luna.math`.

*Source: [src/lua_api/math_ext_api.rs](src/lua_api/math_ext_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/math_ext_api.rs#L1386) | Registers extended math API bindings on the `luna.math` table.  # Parameters ... |

### `lua_api::minimap_api`

> Lua API bindings for the `luna.minimap.*` minimap module.  Provides a `Minimap` UserData type with factory functions for creating grid-based minimaps with terrain coloring, fog of war, tracked objects, pings, markers, and viewport rectangle overlay.

*Source: [src/lua_api/minimap_api.rs](src/lua_api/minimap_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/minimap_api.rs#L711) | Register the `luna.minimap` module. |
| `struct` | [`LuaMinimap`](src/lua_api/minimap_api.rs#L21) | Lua UserData wrapper for a grid-based minimap. |

### `lua_api::modding_api`

*Source: [src/lua_api/modding_api.rs](src/lua_api/modding_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/modding_api.rs#L516) | Registers `luna.modding.*` functions into the Lua VM.  # Parameters - `lua` —... |

### `lua_api::particle_api`

> Registers the `luna.particle.*` particle-system API.  Provides functions for creating, updating, drawing, and controlling emitter-based particle effects from Lua game scripts.

*Source: [src/lua_api/particle_api.rs](src/lua_api/particle_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/particle_api.rs#L691) | Registers all `luna.particle.*` particle system functions into the Lua VM.  #... |
| `struct` | [`LuaParticleSystem`](src/lua_api/particle_api.rs#L104) | Lua UserData wrapper for a particle system resource.  # Fields - `state` — `R... |

### `lua_api::pathfinding_api`

> Registers the `luna.pathfinding.*` grid-based pathfinding API.  Exposes `NavGrid`, `UnitPathfinder`, and `FlowField` as Lua UserData with 1-based tile coordinates at the Lua boundary.

*Source: [src/lua_api/pathfinding_api.rs](src/lua_api/pathfinding_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/pathfinding_api.rs#L605) | Register the `luna.pathfinding` namespace.  # Parameters - `lua` — `&Lua`. - ... |

### `lua_api::patterns_api`

> Registers the `luna.patterns.*` software design patterns API.  Provides factory functions and UserData wrappers for reusable patterns: EventBus (prioritized pub-sub), ObjectPool (object reuse), CommandStack (undo/redo), ServiceLocator (DI container), Factory (named constructors), and SimpleState (finite state machine).

*Source: [src/lua_api/patterns_api.rs](src/lua_api/patterns_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/patterns_api.rs#L960) | Registers `luna.patterns.*` factory functions. |

### `lua_api::physics_api`

*Source: [src/lua_api/physics_api.rs](src/lua_api/physics_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/physics_api.rs#L548) | Registers all `luna.physics.*` world and body management functions into the L... |
| `struct` | [`LuaBody`](src/lua_api/physics_api.rs#L228) | Lua UserData wrapper for a physics body.  # Fields - `worlds` — Shared vector... |
| `struct` | [`LuaWorld`](src/lua_api/physics_api.rs#L28) | Lua UserData wrapper for a physics world.  # Fields - `worlds` — Shared vecto... |

### `lua_api::postfx_api`

> Lua API bindings for the `luna.postfx.*` post-processing effects module.  Provides `PostFxEffect` and `PostFxStack` UserData types with factory functions for creating built-in effects, custom shader passes, and effect stacks that manage ping-pong canvas post-processing pipelines.

*Source: [src/lua_api/postfx_api.rs](src/lua_api/postfx_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/postfx_api.rs#L443) | Registers the `luna.postfx.*` API.  # Parameters - `lua` — `&Lua`. - `luna` —... |
| `struct` | [`LuaPostFxEffect`](src/lua_api/postfx_api.rs#L21) | Lua UserData wrapper for a single post-processing effect. |

### `lua_api::quest_api`

> Lua API bindings for `luna.quest.*`.  Exposes quest creation, stage management, objective tracking, and journal entries through the `luna.quest` table.

*Source: [src/lua_api/quest_api.rs](src/lua_api/quest_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/quest_api.rs#L711) | Register `luna.quest.*` API with the Lua VM. |
| `struct` | [`LuaObjective`](src/lua_api/quest_api.rs#L20) | Lua UserData wrapper for a quest objective. |
| `struct` | [`LuaQuest`](src/lua_api/quest_api.rs#L275) | Lua UserData wrapper for a quest. |
| `struct` | [`LuaQuestLog`](src/lua_api/quest_api.rs#L554) | Lua UserData wrapper for a quest log. |
| `struct` | [`LuaQuestStage`](src/lua_api/quest_api.rs#L180) | Lua UserData wrapper for a quest stage. |

### `lua_api::resource_api`

> Lua API bindings for the `luna.resource.*` resource economy module.  Provides `ResourceManager` UserData for managing named resources with capacity, flow rates, decay, interest, conversions, and overflow policies.

*Source: [src/lua_api/resource_api.rs](src/lua_api/resource_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/resource_api.rs#L805) | Registers the `luna.resource` module with the Lua VM. |
| `struct` | [`LuaResourceManager`](src/lua_api/resource_api.rs#L20) | Lua UserData wrapper for a resource economy manager. |

### `lua_api::savegame_api`

*Source: [src/lua_api/savegame_api.rs](src/lua_api/savegame_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/savegame_api.rs#L247) | Registers `luna.savegame.*` functions into the Lua VM.  # Parameters - `lua` ... |

### `lua_api::scene_api`

> Registers the `luna.scene.*` scene stack, registry, data store, and depth-sorter API.  The scene stack manages a LIFO stack of Lua scene tables with optional visual transitions. Scene tables are stored via `mlua::RegistryKey` and identified by `SceneId`. Inter-scene data is stored similarly.  The `DepthSorter` UserData batches draw callbacks by depth for ordered compositing each frame.

*Source: [src/lua_api/scene_api.rs](src/lua_api/scene_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/scene_api.rs#L177) | Registers the `luna.scene` table with scene stack, registry, data store,  # P... |

### `lua_api::sound_api`

> Registers the `luna.sound.*` decoded audio sample manipulation API.

*Source: [src/lua_api/sound_api.rs](src/lua_api/sound_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/sound_api.rs#L20) | Registers the `luna.sound` table on the provided `luna` namespace.  # Paramet... |

### `lua_api::stats_api`

> Lua API bindings for the `luna.stats` RPG character sheet system.  Exposes [`Sheet`] and a global [`StatsRegistry`] as `luna.stats.*` Lua values.

*Source: [src/lua_api/stats_api.rs](src/lua_api/stats_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/stats_api.rs#L907) | Register the `luna.stats` module into the Lua state. |
| `struct` | [`LuaSheet`](src/lua_api/stats_api.rs#L15) | Lua-visible wrapper for a character [`Sheet`]. |

### `lua_api::system_api`

*Source: [src/lua_api/system_api.rs](src/lua_api/system_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`PowerState`](src/lua_api/system_api.rs#L101) | Power state of the device.  # Variants - `Unknown` — cannot determine power s... |
| `fn` | [`get_memory_size`](src/lua_api/system_api.rs#L22) | Returns total system RAM in MiB using the `sysinfo` crate.  # Returns Physica... |
| `fn` | [`get_power_info`](src/lua_api/system_api.rs#L137) | Returns power/battery information: (state, percent, seconds).  On desktop pla... |
| `fn` | [`get_preferred_locales`](src/lua_api/system_api.rs#L79) | Returns the user's preferred locale strings.  # Returns A `Vec<String>` with ... |
| `fn` | [`get_processor_count`](src/lua_api/system_api.rs#L12) | Returns the number of logical processors available.  # Returns Logical CPU co... |
| `fn` | [`open_url`](src/lua_api/system_api.rs#L40) | Opens a URL in the default browser/application.  Only `http://`, `https://`, ... |
| `fn` | [`register`](src/lua_api/system_api.rs#L149) | Registers `luna.system.*` platform query functions into the Lua VM.  # Parame... |

### `lua_api::thread_api`

> Registers the `luna.thread` namespace.  Provides Lua-level multithreading: create background threads that run independent Lua VMs and communicate via thread-safe channels.

*Source: [src/lua_api/thread_api.rs](src/lua_api/thread_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/thread_api.rs#L91) | Registers all `luna.thread.*` functions into the Lua VM.  # Parameters - `lua... |
| `struct` | [`LuaThreadHandle`](src/lua_api/thread_api.rs#L23) | Lua UserData wrapper for a background thread handle.  # Fields - `inner` — `A... |

### `lua_api::tilemap_api`

> Registers the `luna.tilemap.*` tile map, tileset, autotile, and procedural generation API.  Exposes `LuaTileSet`, `LuaTileMap`, `LuaAutoTileSheet`, `LuaMapBlock`, `LuaMapGroup`, `LuaMapScript`, and `LuaMapGen` UserData types wrapping the `crate::tilemap` module.  All Lua-facing tile/layer/segment indices are **1-based**; Rust internals are 0-based.

*Source: [src/lua_api/tilemap_api.rs](src/lua_api/tilemap_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/tilemap_api.rs#L2038) | register.  # Parameters - `lua` — `&Lua`. - `luna` — `&LuaTable`.  # Returns ... |
| `struct` | [`LuaTileMap`](src/lua_api/tilemap_api.rs#L44) | Lua wrapper around a [`TileMap`].  # Fields - `inner` — `Rc<RefCell<TileMap>>`. |

### `lua_api::timer_api`

*Source: [src/lua_api/timer_api.rs](src/lua_api/timer_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/timer_api.rs#L301) | Registers `luna.timer.*` functions into the Lua VM.  Provides frame-timing ut... |

### `lua_api::window_api`

*Source: [src/lua_api/window_api.rs](src/lua_api/window_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/window_api.rs#L15) | Registers `luna.window.*` functions into the Lua VM.  # Parameters - `lua` — ... |

## `math` {#math}

### `math`

*Source: [src/math/mod.rs](src/math/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`bezier`](src/math/mod.rs#L2) | Bezier curve evaluation using De Casteljau's algorithm. |
| `mod` | [`easing`](src/math/mod.rs#L6) | Standard easing functions for smooth animation and interpolation. |
| `mod` | [`geometry`](src/math/mod.rs#L8) | 2D geometry utility functions: intersections, containment, polygon ops, raste... |
| `mod` | [`grid`](src/math/mod.rs#L10) | 2D pathfinding grid with A*, Dijkstra, BFS, and flow field generation. |
| `mod` | [`mat3`](src/math/mod.rs#L12) | 3x3 column-major matrix for 2D transforms (translate, rotate, scale). |
| `mod` | [`noise`](src/math/mod.rs#L14) | 2D Perlin and Simplex noise generators for procedural content. |
| `mod` | [`polygon`](src/math/mod.rs#L16) | Polygon utilities: ear-clipping triangulation and convexity testing. |
| `mod` | [`procgen`](src/math/mod.rs#L18) | Procedural generation: cellular automata, Voronoi, flood fill, Poisson disk, ... |
| `mod` | [`random`](src/math/mod.rs#L20) | Seedable random number generator for reproducible sequences. |
| `mod` | [`raycasting`](src/math/mod.rs#L22) | 2D raycasting and visibility utility functions. |
| `mod` | [`rect`](src/math/mod.rs#L24) | Axis-aligned rectangle with intersection and containment queries. |
| `mod` | [`spatial_hash`](src/math/mod.rs#L26) | Spatial hash for efficient broad-phase AABB collision queries. |
| `mod` | [`srgb`](src/math/mod.rs#L4) | sRGB gamma ↔ linear color space conversion. |
| `mod` | [`transform`](src/math/mod.rs#L28) | 2D affine transform with chainable methods wrapping Mat3. |
| `mod` | [`tween`](src/math/mod.rs#L30) | Value interpolator with easing curves. |
| `mod` | [`vec2`](src/math/mod.rs#L32) | 2D floating-point vector with arithmetic operators and common helpers. |

### `math::bezier`

> Bezier curve evaluation using De Casteljau's algorithm.  Supports arbitrary-degree curves with control point manipulation, rendering to polylines, derivative computation, and geometric transforms.

*Source: [src/math/bezier.rs](src/math/bezier.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BezierCurve`](src/math/bezier.rs#L11) | A Bezier curve defined by control points.  Uses De Casteljau's algorithm for ... |

### `math::easing`

> Standard easing functions for smooth animation and interpolation.  Each function takes a normalised progress value `t` in `[0.0, 1.0]` and returns the eased value, also in `[0.0, 1.0]` for most curves.

*Source: [src/math/easing.rs](src/math/easing.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`apply`](src/math/easing.rs#L326) | Looks up an easing function by name and applies it to progress value `t`.  Su... |
| `fn` | [`ease_in_back`](src/math/easing.rs#L294) | Back ease-in — pulls back before accelerating past the start.  # Parameters -... |
| `fn` | [`ease_in_bounce`](src/math/easing.rs#L283) | Bounce ease-in — simulates a bouncing ball launching.  # Parameters - `t` — n... |
| `fn` | [`ease_in_cubic`](src/math/easing.rs#L63) | Cubic ease-in — starts slow, accelerates sharply.  # Parameters - `t` — norma... |
| `fn` | [`ease_in_elastic`](src/math/easing.rs#L225) | Elastic ease-in — spring-like overshoot at the start.  # Parameters - `t` — n... |
| `fn` | [`ease_in_expo`](src/math/easing.rs#L174) | Exponential ease-in — very slow start, rapid acceleration.  # Parameters - `t... |
| `fn` | [`ease_in_out_cubic`](src/math/easing.rs#L86) | Cubic ease-in-out — smooth S-curve.  # Parameters - `t` — normalised progress... |
| `fn` | [`ease_in_out_expo`](src/math/easing.rs#L204) | Exponential ease-in-out — sharp S-curve with exponential tails.  # Parameters... |
| `fn` | [`ease_in_out_quad`](src/math/easing.rs#L48) | Quadratic ease-in-out — slow start and end, fast middle.  # Parameters - `t` ... |
| `fn` | [`ease_in_out_quart`](src/math/easing.rs#L125) | Quartic ease-in-out — pronounced S-curve.  # Parameters - `t` — normalised pr... |
| `fn` | [`ease_in_out_sine`](src/math/easing.rs#L163) | Sinusoidal ease-in-out — gentle S-curve.  # Parameters - `t` — normalised pro... |
| `fn` | [`ease_in_quad`](src/math/easing.rs#L26) | Quadratic ease-in — starts slow, accelerates.  # Parameters - `t` — normalise... |
| `fn` | [`ease_in_quart`](src/math/easing.rs#L102) | Quartic ease-in — very slow start.  # Parameters - `t` — normalised progress ... |
| `fn` | [`ease_in_sine`](src/math/easing.rs#L141) | Sinusoidal ease-in — gentle sine-based acceleration.  # Parameters - `t` — no... |
| `fn` | [`ease_out_back`](src/math/easing.rs#L306) | Back ease-out — overshoots the target then settles back.  # Parameters - `t` ... |
| `fn` | [`ease_out_bounce`](src/math/easing.rs#L261) | Bounce ease-out — simulates a bouncing ball landing.  # Parameters - `t` — no... |
| `fn` | [`ease_out_cubic`](src/math/easing.rs#L74) | Cubic ease-out — starts fast, decelerates sharply.  # Parameters - `t` — norm... |
| `fn` | [`ease_out_elastic`](src/math/easing.rs#L243) | Elastic ease-out — spring-like overshoot at the end.  # Parameters - `t` — no... |
| `fn` | [`ease_out_expo`](src/math/easing.rs#L189) | Exponential ease-out — rapid start, very slow end.  # Parameters - `t` — norm... |
| `fn` | [`ease_out_quad`](src/math/easing.rs#L37) | Quadratic ease-out — starts fast, decelerates.  # Parameters - `t` — normalis... |
| `fn` | [`ease_out_quart`](src/math/easing.rs#L113) | Quartic ease-out — very slow end.  # Parameters - `t` — normalised progress v... |
| `fn` | [`ease_out_sine`](src/math/easing.rs#L152) | Sinusoidal ease-out — gentle sine-based deceleration.  # Parameters - `t` — n... |
| `fn` | [`linear`](src/math/easing.rs#L15) | Linear interpolation — no easing.  # Parameters - `t` — normalised progress v... |

### `math::geometry`

> 2D geometry utility functions.  Free functions for common geometric computations: intersections, containment, polygon operations, and rasterization.

*Source: [src/math/geometry.rs](src/math/geometry.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`angle_between`](src/math/geometry.rs#L16) | Returns the angle in radians from (x1, y1) to (x2, y2).  # Parameters - `x1` ... |
| `fn` | [`bresenham`](src/math/geometry.rs#L389) | Bresenham line rasterization from (x1, y1) to (x2, y2).  # Parameters - `x1` ... |
| `fn` | [`circle_contains_point`](src/math/geometry.rs#L31) | Returns true if the point (px, py) is inside the circle centered at (cx, cy) ... |
| `fn` | [`circle_intersects_circle`](src/math/geometry.rs#L49) | Returns true if two circles overlap.  # Parameters - `x1` — `f32`. - `y1` — `... |
| `fn` | [`circle_intersects_line`](src/math/geometry.rs#L72) | Line-circle intersection. Returns (intersects, hit1, hit2).  # Parameters - `... |
| `fn` | [`circle_intersects_segment`](src/math/geometry.rs#L126) | Segment-circle intersection. Same as line-circle but clamped to the segment. ... |
| `fn` | [`closest_point_on_segment`](src/math/geometry.rs#L293) | Returns the closest point on a line segment to a given point.  # Parameters -... |
| `fn` | [`convex_hull`](src/math/geometry.rs#L426) | Computes the convex hull of a set of 2D points using Andrew's monotone chain ... |
| `fn` | [`delaunay_triangulate`](src/math/geometry.rs#L484) | Delaunay triangulation using the Bowyer-Watson algorithm.  # Parameters - `po... |
| `fn` | [`line_intersect`](src/math/geometry.rs#L359) | Infinite line intersection. Returns the intersection point if lines are not p... |
| `fn` | [`point_in_polygon`](src/math/geometry.rs#L323) | Tests if a point is inside a polygon using the ray casting algorithm.  # Para... |
| `fn` | [`polygon_area`](src/math/geometry.rs#L180) | Computes the signed area of a polygon using the Shoelace formula.  # Paramete... |
| `fn` | [`polygon_centroid`](src/math/geometry.rs#L206) | Computes the centroid of a polygon.  # Parameters - `vertices` — `&[f32]`.  #... |
| `fn` | [`segment_intersects_segment`](src/math/geometry.rs#L255) | Tests if two line segments intersect. Returns (intersects, intersection_point... |

### `math::grid`

> 2D pathfinding grid with A*, Dijkstra, BFS, and flow field generation.  Cells are addressed with 0-based `(x, y)` coordinates. The Lua layer converts to/from 1-based indices.

*Source: [src/math/grid.rs](src/math/grid.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Grid`](src/math/grid.rs#L45) | 2D pathfinding grid with per-cell walkability and movement costs.  Supports A... |

### `math::mat3`

*Source: [src/math/mat3.rs](src/math/mat3.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Mat3`](src/math/mat3.rs#L11) | A 3×3 column-major matrix used for 2D affine transforms (translation, rotatio... |

### `math::noise`

> 2D Perlin and Simplex noise generators for procedural content.  All functions return values in `[-1.0, 1.0]`. Seed is used to offset the gradient hash, producing different noise patterns per seed.

*Source: [src/math/noise.rs](src/math/noise.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DistType`](src/math/noise.rs#L463) | Distance metric for Worley noise.  # Variants - `Euclidean` — Euclidean varia... |
| `enum` | [`FractalType`](src/math/noise.rs#L492) | Fractal type for multi-octave noise.  # Variants - `Fbm` — Fbm variant. - `Ri... |
| `enum` | [`NoiseKind`](src/math/noise.rs#L478) | Noise algorithm kind used by fractal combinators.  # Variants - `Perlin` — Pe... |
| `fn` | [`fbm`](src/math/noise.rs#L82) | Generates fractal Brownian motion noise by layering multiple octaves of Perli... |
| `fn` | [`perlin2d`](src/math/noise.rs#L15) | Generates 2D Perlin noise at the given coordinates.  # Parameters - `x` — X c... |
| `fn` | [`perlin3d`](src/math/noise.rs#L246) | Generates 3D Perlin noise at the given coordinates.  # Parameters - `x` — X c... |
| `fn` | [`perlin4d`](src/math/noise.rs#L298) | Generates 4D Perlin noise at the given coordinates.  # Parameters - `x` — X c... |
| `fn` | [`simplex2d`](src/math/noise.rs#L43) | Generates 2D Simplex noise at the given coordinates.  # Parameters - `x` — X ... |
| `struct` | [`MapGenOptions`](src/math/noise.rs#L514) | Options for 2D noise map generation.  # Fields - `scale_x` — `f64`. - `scale_... |
| `struct` | [`NoiseGenerator`](src/math/noise.rs#L556) | Seeded procedural noise generator.  Holds a 512-entry permutation table deriv... |

### `math::polygon`

> Polygon utilities: ear-clipping triangulation and convexity testing.

*Source: [src/math/polygon.rs](src/math/polygon.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`is_convex`](src/math/polygon.rs#L73) | Check if a polygon is convex.  Uses cross-product sign consistency at each ve... |
| `fn` | [`triangulate`](src/math/polygon.rs#L14) | Triangulate a simple polygon using the ear-clipping algorithm.  # Parameters ... |

### `math::procgen`

> Procedural generation utility functions.  Cave generation, Voronoi diagrams, flood fill, Poisson disk sampling, and periodic Perlin noise.

*Source: [src/math/procgen.rs](src/math/procgen.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`cellular_automata`](src/math/procgen.rs#L102) | Generates a cave/dungeon map using cellular automata.  # Parameters - `width`... |
| `fn` | [`flood_fill`](src/math/procgen.rs#L261) | BFS flood fill on a grid.  # Parameters - `data` — `&[u8]`. - `width` — `u32`... |
| `fn` | [`perlin_noise_periodic`](src/math/procgen.rs#L436) | Periodic Perlin noise that tiles over period (px, py).  # Parameters - `x` — ... |
| `fn` | [`poisson_disk`](src/math/procgen.rs#L330) | Generates Poisson disk sample points using Bridson's algorithm.  # Parameters... |
| `fn` | [`voronoi_diagram`](src/math/procgen.rs#L169) | Generates a Voronoi diagram.  # Parameters - `width` — `u32`. - `height` — `u... |
| `struct` | [`CellularOpts`](src/math/procgen.rs#L15) | Options for cellular automata generation.  # Fields - `fill` — `f32`. - `iter... |
| `struct` | [`VoronoiOpts`](src/math/procgen.rs#L47) | Options for Voronoi diagram generation.  # Fields - `warp_scale` — `f32`. - `... |

### `math::random`

> Seedable random number generator for reproducible sequences.  Wraps `fastrand::Rng` with engine-compatible API including seeding, state save/restore, and normal distribution sampling.

*Source: [src/math/random.rs](src/math/random.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`RandomGenerator`](src/math/random.rs#L12) | Seedable random number generator exposed as a Lua object.  Wraps `fastrand::R... |

### `math::raycasting`

> 2D raycasting and visibility utility functions.  Provides ray-vs-segment intersection, field of view computation, column projection, and distance-based shading.

*Source: [src/math/raycasting.rs](src/math/raycasting.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`cast_ray_2d`](src/math/raycasting.rs#L40) | Casts a ray from (ox, oy) in direction (dx, dy) against a list of segments.  ... |
| `fn` | [`distance_shade`](src/math/raycasting.rs#L171) | Distance-based shading. Returns brightness in [0, 1].  # Parameters - `distan... |
| `fn` | [`field_of_view`](src/math/raycasting.rs#L92) | Computes a visibility polygon by casting rays at segment endpoints.  # Parame... |
| `fn` | [`project_column`](src/math/raycasting.rs#L147) | Projects a wall column distance to screen-space drawing parameters.  # Parame... |
| `struct` | [`RayHit`](src/math/raycasting.rs#L278) | Result of a single ray cast.  # Fields - `distance` — `f32`. - `raw_distance`... |
| `struct` | [`Raycaster2D`](src/math/raycasting.rs#L320) | 2D grid-based raycaster using DDA traversal.  The grid stores wall types as `... |
| `struct` | [`Segment`](src/math/raycasting.rs#L14) | A line segment for raycasting.  # Fields - `x1` — `f32`. - `y1` — `f32`. - `x... |
| `struct` | [`SpriteProjection`](src/math/raycasting.rs#L305) | Sprite projection result.  # Fields - `screen_x` — `f32`. - `scale` — `f32`. ... |

### `math::rect`

*Source: [src/math/rect.rs](src/math/rect.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Rect`](src/math/rect.rs#L13) | An axis-aligned rectangle defined by its top-left corner and dimensions.  Use... |

### `math::spatial_hash`

> Spatial hash for efficient broad-phase AABB collision queries.  Items are keyed by a string ID and stored with axis-aligned bounding boxes. The grid partitions space into fixed-size cells for O(1) bucket lookup.

*Source: [src/math/spatial_hash.rs](src/math/spatial_hash.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SpatialHash`](src/math/spatial_hash.rs#L35) | Spatial hash for AABB queries.  Divides the 2D plane into a uniform grid of s... |
| `struct` | [`SpatialItem`](src/math/spatial_hash.rs#L17) | Entry in the spatial hash.  # Fields - `id` — `String`. - `x` — `f32`. - `y` ... |

### `math::srgb`

> sRGB gamma ↔ linear color space conversion.  Implements the standard IEC 61966-2-1 (sRGB) transfer functions for converting between gamma-encoded sRGB and linear light values.

*Source: [src/math/srgb.rs](src/math/srgb.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`gamma_to_linear`](src/math/srgb.rs#L15) | Convert a single sRGB gamma-space color component to linear space.  Input and... |
| `fn` | [`linear_to_gamma`](src/math/srgb.rs#L32) | Convert a single linear-space color component to sRGB gamma space.  Input and... |

### `math::transform`

> 2D affine transform wrapping Mat3 with chainable methods.  Compatible with standard 2D transform API for translate, rotate, scale, shear, and point transformation.

*Source: [src/math/transform.rs](src/math/transform.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Transform`](src/math/transform.rs#L14) | 2D affine transform exposed as a Lua object.  Wraps `Mat3` with chainable tra... |

### `math::tween`

> Value interpolator with easing curves.  Uses easing functions from `crate::math::easing` to smoothly interpolate between start and target values over a duration.

*Source: [src/math/tween.rs](src/math/tween.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Tween`](src/math/tween.rs#L25) | Value interpolator using easing functions.  Animates one or more values from ... |
| `struct` | [`TweenValue`](src/math/tween.rs#L14) | A start-to-target value pair for interpolation.  # Fields - `start` — `f64`. ... |

### `math::vec2`

*Source: [src/math/vec2.rs](src/math/vec2.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Vec2`](src/math/vec2.rs#L12) | A 2D floating-point vector used throughout the engine for positions, velociti... |

## `modding` {#modding}

### `modding`

> Mod management framework.  Provides `ModInfo` for mod metadata and `ModManager` for registration, dependency resolution, load ordering, folder scanning, and hot-reload queuing.

*Source: [src/modding/mod.rs](src/modding/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`mod_manager`](src/modding/mod.rs#L6) | *(undocumented)* |

### `modding::mod_manager`

> Mod management framework.  Provides `ModInfo` for mod metadata and `ModManager` for registration, dependency resolution, load ordering, folder scanning, and hot-reload queuing.

*Source: [src/modding/mod_manager.rs](src/modding/mod_manager.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ModInfo`](src/modding/mod_manager.rs#L22) | Metadata describing a mod.  # Fields - `id` — `String`. - `name` — `String`. ... |
| `struct` | [`ModManager`](src/modding/mod_manager.rs#L73) | Centralized registry for managing mods, resolving load order, validating depe... |

## `particle` {#particle}

### `particle`

> Particle system module providing emitter-based 2D particle effects.  A `ParticleSystem` spawns short-lived `Particle` entities each frame, advancing their position, velocity, and lifetime. Dead particles are recycled, keeping allocation bounded by `ParticleConfig::max_particles`.  Supports multi-stop size/color/alpha interpolation, emission shapes, radial/tangential acceleration, linear damping, relative mode, and texture-based rendering.

*Source: [src/particle/mod.rs](src/particle/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`system`](src/particle/mod.rs#L10) | *(undocumented)* |

### `particle::system`

> Particle system module providing emitter-based 2D particle effects.  A `ParticleSystem` spawns short-lived `Particle` entities each frame, advancing their position, velocity, and lifetime. Dead particles are recycled, keeping allocation bounded by `ParticleConfig::max_particles`.  Supports multi-stop size/color/alpha interpolation, emission shapes, radial/tangential acceleration, linear damping, relative mode, and texture-based rendering.

*Source: [src/particle/system.rs](src/particle/system.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`AreaDistribution`](src/particle/system.rs#L23) | Area distribution mode for particle emission.  # Variants - `None` — None var... |
| `enum` | [`EmissionShape`](src/particle/system.rs#L84) | Emission shape controlling where new particles spawn relative to the emitter.... |
| `enum` | [`EmitterState`](src/particle/system.rs#L63) | Emitter lifecycle state.  # Variants - `Active` — Active variant. - `Paused` ... |
| `enum` | [`InsertMode`](src/particle/system.rs#L46) | Insert mode controlling where new particles are placed in the particle list. ... |
| `enum` | [`RelativeMode`](src/particle/system.rs#L149) | Relative mode controlling whether particles move with the emitter.  # Variant... |
| `fn` | [`interpolate_alphas`](src/particle/system.rs#L968) | Interpolate a multi-stop alpha array at normalised time `t` (0 = birth, 1 = d... |
| `fn` | [`interpolate_colors`](src/particle/system.rs#L935) | Interpolate a multi-stop color array at normalised time `t` (0 = birth, 1 = d... |
| `fn` | [`interpolate_sizes`](src/particle/system.rs#L906) | Interpolate a multi-stop size array at normalised time `t` (0 = birth, 1 = de... |
| `fn` | [`lerp`](src/particle/system.rs#L885) | Linearly interpolate between `a` and `b` by factor `t`.  # Parameters - `a` —... |
| `struct` | [`Particle`](src/particle/system.rs#L402) | A single particle managed by a `ParticleSystem`.  # Fields - `x` — `f32`. - `... |
| `struct` | [`ParticleConfig`](src/particle/system.rs#L214) | Configuration for a particle emitter.  # Fields - `max_particles` — `u32`. - ... |
| `struct` | [`ParticleSystem`](src/particle/system.rs#L449) | An emitter-based particle system.  # Fields - `config` — `ParticleConfig`. - ... |

## `pathfinding` {#pathfinding}

### `pathfinding`

> Grid-based pathfinding: A★, HPA★, flow fields, and unit-size-aware navigation.

*Source: [src/pathfinding/mod.rs](src/pathfinding/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`astar`](src/pathfinding/mod.rs#L4) | astar. |
| `mod` | [`async_pool`](src/pathfinding/mod.rs#L6) | async_pool. |
| `mod` | [`flow_field`](src/pathfinding/mod.rs#L8) | flow_field. |
| `mod` | [`hpa`](src/pathfinding/mod.rs#L10) | hpa. |
| `mod` | [`nav_grid`](src/pathfinding/mod.rs#L12) | nav_grid. |
| `mod` | [`unit_pathfinder`](src/pathfinding/mod.rs#L14) | unit_pathfinder. |

### `pathfinding::astar`

> A★ search with octile/Manhattan heuristic and Theta★ path smoothing.

*Source: [src/pathfinding/astar.rs](src/pathfinding/astar.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`astar`](src/pathfinding/astar.rs#L73) | Run A★ search on `grid` from `start` to `goal`.  # Parameters - `grid` — `&Na... |
| `fn` | [`line_of_sight`](src/pathfinding/astar.rs#L280) | Check line-of-sight between two cells using Bresenham's algorithm,  # Paramet... |
| `fn` | [`smooth_path`](src/pathfinding/astar.rs#L322) | Smooth a path by removing unnecessary waypoints via line-of-sight checks  # P... |

### `pathfinding::async_pool`

> Thread pool for asynchronous path computation.  Pathfinding requests are submitted with an ID and processed on worker threads. Results are collected via non-blocking [`PathThreadPool::poll`].

*Source: [src/pathfinding/async_pool.rs](src/pathfinding/async_pool.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PathThreadPool`](src/pathfinding/async_pool.rs#L31) | A pool of worker threads that process pathfinding requests asynchronously. |
| `type` | [`PathResult`](src/pathfinding/async_pool.rs#L28) | A completed path result returned by [`PathThreadPool::poll`]. |

### `pathfinding::flow_field`

> Flow field pathfinding for steering many units toward one or more targets.

*Source: [src/pathfinding/flow_field.rs](src/pathfinding/flow_field.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`FlowField`](src/pathfinding/flow_field.rs#L11) | A pre-computed flow field that stores a direction vector and integrated cost ... |

### `pathfinding::hpa`

> Hierarchical Pathfinding A★ (HPA★) for fast long-range queries.  Divides the grid into chunks, finds boundary entrances, builds an abstract graph, and searches at the chunk level before refining to tile-level paths.

*Source: [src/pathfinding/hpa.rs](src/pathfinding/hpa.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`build_abstract`](src/pathfinding/hpa.rs#L134) | Build the abstract graph from a `NavGrid`.  # Parameters - `grid` — `&NavGrid... |
| `fn` | [`hpa_star`](src/pathfinding/hpa.rs#L379) | Run HPA★ from `start` to `goal` on the abstract graph, then refine to tiles. ... |
| `fn` | [`is_reachable`](src/pathfinding/hpa.rs#L516) | Check if `goal` is reachable from `start` using the abstract graph.  # Parame... |
| `struct` | [`AbstractEdge`](src/pathfinding/hpa.rs#L18) | An edge in the abstract graph connecting two entrance nodes.  # Fields - `to`... |
| `struct` | [`AbstractGraph`](src/pathfinding/hpa.rs#L79) | Pre-computed abstract graph for hierarchical A★ queries.  # Fields - `nodes` ... |
| `struct` | [`AbstractNode`](src/pathfinding/hpa.rs#L32) | A node in the abstract graph, representing an entrance point on a chunk bound... |
| `struct` | [`Chunk`](src/pathfinding/hpa.rs#L52) | A chunk region of the grid used during abstract graph construction.  # Fields... |

### `pathfinding::nav_grid`

> Navigation grid with per-cell traversal costs and diagonal movement modes.

*Source: [src/pathfinding/nav_grid.rs](src/pathfinding/nav_grid.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DiagonalMode`](src/pathfinding/nav_grid.rs#L10) | Controls how diagonal movement is handled during pathfinding.  # Variants - `... |
| `struct` | [`NavGrid`](src/pathfinding/nav_grid.rs#L46) | A 2D grid of traversal costs used by pathfinding algorithms.  Cells are addre... |

### `pathfinding::unit_pathfinder`

> Unit-aware pathfinder with result caching and convenience methods.

*Source: [src/pathfinding/unit_pathfinder.rs](src/pathfinding/unit_pathfinder.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`UnitPathfinder`](src/pathfinding/unit_pathfinder.rs#L34) | A pathfinder that operates on a shared `NavGrid` with optional result caching. |
| `struct` | [`Waypoint`](src/pathfinding/unit_pathfinder.rs#L16) | A waypoint along a computed path.  # Fields - `x` — `u32`. - `y` — `u32`. |

## `physics` {#physics}

### `physics`

> Physics simulation module backed by rapier2d.  Provides rigid-body simulation with circles, rectangles, sensors, raycasting, joints, and collision event recording.

*Source: [src/physics/mod.rs](src/physics/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`body`](src/physics/mod.rs#L7) | Body types, shapes, and the `Body` struct used by the physics world. |
| `mod` | [`collision`](src/physics/mod.rs#L9) | Collision event structs returned by `World::get_collision_events`. |
| `mod` | [`shape`](src/physics/mod.rs#L11) | Extended shape types: Polygon, Edge, and Chain colliders. |
| `mod` | [`world`](src/physics/mod.rs#L13) | The `World` struct and all simulation management: bodies, joints, raycasting,... |

### `physics::body`

*Source: [src/physics/body.rs](src/physics/body.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BodyShape`](src/physics/body.rs#L25) | Describes the collision geometry of a body.  # Variants - `Rect { width, heig... |
| `enum` | [`BodyType`](src/physics/body.rs#L11) | Determines whether a physics body is affected by forces and gravity.  # Varia... |
| `struct` | [`Body`](src/physics/body.rs#L57) | A rigid body with position, velocity, mass, shape, and restitution.  Bodies l... |

### `physics::collision`

*Source: [src/physics/collision.rs](src/physics/collision.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CollisionInfo`](src/physics/collision.rs#L11) | Collision contact data: penetration depth and separating normal.  Retained fo... |

### `physics::shape`

> Extended shape types for physics bodies.  Provides polygon, edge, and chain shapes beyond the basic rect/circle.

*Source: [src/physics/shape.rs](src/physics/shape.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Shape`](src/physics/shape.rs#L19) | Extended collision shape for physics bodies.  Goes beyond `BodyShape` to supp... |

### `physics::world`

> Rapier2d-backed physics world with backward-compatible Luna2D API.  Wraps the rapier2d simulation pipeline while exposing the same `Body`-based interface. The `Body` struct is a sync buffer: mutations via `get_body_mut()` are flushed into rapier on the next `step()` call, and rapier's computed positions/velocities are read back after each step.

*Source: [src/physics/world.rs](src/physics/world.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BodyContact`](src/physics/world.rs#L70) | Collision event generated by `World::step` when two bodies start overlapping.... |
| `struct` | [`ContactInfo`](src/physics/world.rs#L105) | Contact information between two bodies from the narrow phase.  # Fields - `bo... |
| `struct` | [`RaycastHit`](src/physics/world.rs#L85) | Result of a `World::raycast` query.  # Fields - `body_id` — Integer body ID t... |
| `struct` | [`World`](src/physics/world.rs#L125) | Simulates a 2D physics world using rapier2d.  Bodies are stored as a `Vec<Bod... |

## `quest` {#quest}

### `quest`

> Quest and objective tracking system for Luna2D games.  Provides `Quest`, `Objective`, `QuestStage`, `JournalEntry`, and `QuestLog` for building RPG-style quest systems with stages, conditions, and journal notes.

*Source: [src/quest/mod.rs](src/quest/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ObjectiveStatus`](src/quest/mod.rs#L54) | Lifecycle state of a single objective. |
| `enum` | [`QuestStatus`](src/quest/mod.rs#L14) | Lifecycle state of a quest. |
| `struct` | [`JournalEntry`](src/quest/mod.rs#L235) | A timestamped text entry in a quest's journal. |
| `struct` | [`Objective`](src/quest/mod.rs#L100) | A single trackable task within a quest.  Supports count-based progress (`curr... |
| `struct` | [`Quest`](src/quest/mod.rs#L253) | A quest with stages, objectives, and a journal.  Stages are ordered; the ques... |
| `struct` | [`QuestLog`](src/quest/mod.rs#L444) | Registry of all quests for a game session.  Quests are stored by their id. Ac... |
| `struct` | [`QuestStage`](src/quest/mod.rs#L186) | A named group of objectives that represent one phase of a quest. |

## `resource` {#resource}

### `resource`

> Resource economy system: named resources with capacity, flow rates, decay, interest, upkeep, overflow policies, reservations, and conversions.  Designed for RTS, management, survival, and RPG economy patterns where the game tracks multiple named numeric values (gold, wood, mana, food) that change over time with configurable rates.

*Source: [src/resource/mod.rs](src/resource/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ModifierType`](src/resource/mod.rs#L321) | Modifier type for conversion rules. |
| `enum` | [`OverflowPolicy`](src/resource/mod.rs#L12) | Policy governing what happens when adding exceeds capacity. |
| `struct` | [`ConversionRule`](src/resource/mod.rs#L434) | A rule for converting one resource type to another. |
| `struct` | [`Modifier`](src/resource/mod.rs#L352) | A rate modifier that can be attached to conversion rules. |
| `struct` | [`Resource`](src/resource/mod.rs#L43) | A single named numeric resource with rates, capacity, overflow policy, and re... |
| `struct` | [`ResourceManager`](src/resource/mod.rs#L599) | A manager that owns named resources and provides bulk operations and conversi... |

## `savegame` {#savegame}

### `savegame`

> Save/load slot system with collectors, schema versioning, dirty tracking, and auto-save.

*Source: [src/savegame/mod.rs](src/savegame/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`save_data`](src/savegame/mod.rs#L4) | *(undocumented)* |

### `savegame::save_data`

> Save/load slot system with collectors, schema versioning, dirty tracking, and auto-save.

*Source: [src/savegame/save_data.rs](src/savegame/save_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`SaveValue`](src/savegame/save_data.rs#L244) | A simple value type matching the Lua subset we can serialize.  # Variants - `... |
| `fn` | [`serialize_table`](src/savegame/save_data.rs#L193) | Serialize a simple Lua-compatible value hierarchy into a `return { ... }` str... |
| `fn` | [`serialize_value`](src/savegame/save_data.rs#L225) | Serialize a single value.  # Parameters - `value` — `&SaveValue`. - `depth` —... |
| `struct` | [`SaveManager`](src/savegame/save_data.rs#L31) | Pure-data save manager providing registration of named collectors, schema ver... |
| `struct` | [`SlotMeta`](src/savegame/save_data.rs#L14) | Metadata extracted from a save slot.  # Fields - `slot` — `String`. - `timest... |

## `scene` {#scene}

### `scene`

> Scene stack module for managing game scene lifecycle, transitions, and depth-sorted rendering.  Provides a LIFO stack of Lua scene tables with lifecycle callbacks (`enter`, `leave`, `pause`, `resume`, `update`, `draw`), animated transitions (fade, slide), a named scene registry, inter-scene data passing, and a `DepthSorter` for z-ordered draw batching.

*Source: [src/scene/mod.rs](src/scene/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`depth_sorter`](src/scene/mod.rs#L8) | depth_sorter. |
| `mod` | [`stack`](src/scene/mod.rs#L10) | stack. |
| `mod` | [`transition`](src/scene/mod.rs#L12) | transition. |

### `scene::depth_sorter`

> Per-frame depth-sorted draw batcher.

*Source: [src/scene/depth_sorter.rs](src/scene/depth_sorter.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DepthEntry`](src/scene/depth_sorter.rs#L9) | Entry in the depth-sorted draw queue.  # Fields - `depth` — `f32`. - `callbac... |
| `struct` | [`DepthSorter`](src/scene/depth_sorter.rs#L22) | Per-frame depth-sorted draw batcher.  Collects draw callbacks with depth valu... |

### `scene::stack`

> LIFO scene stack with registry and inter-scene data store.

*Source: [src/scene/stack.rs](src/scene/stack.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SceneStack`](src/scene/stack.rs#L14) | The scene stack manages a LIFO stack of scene references.  Scenes are identif... |
| `type` | [`SceneId`](src/scene/stack.rs#L8) | Unique identifier for a scene in the stack. |

### `scene::transition`

> Visual transition types and active transition state for scene changes.

*Source: [src/scene/transition.rs](src/scene/transition.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`TransitionType`](src/scene/transition.rs#L13) | Visual transition types between scenes.  # Variants - `None` — None variant. ... |
| `struct` | [`ActiveTransition`](src/scene/transition.rs#L54) | Active transition state tracking progress between two scenes.  # Fields - `tr... |

## `sound` {#sound}

### `sound`

> Decoded audio sample manipulation and MIDI SoundFont state.  Provides `SoundData` for reading and writing decoded PCM audio samples, and `MidiState` for managing MIDI SoundFont (SF2) loading.

*Source: [src/sound/mod.rs](src/sound/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`midi`](src/sound/mod.rs#L11) | MIDI SoundFont state management. |
| `mod` | [`sound_data`](src/sound/mod.rs#L7) | Decoded PCM sample buffer with per-sample read/write access. |

### `sound::midi`

> MIDI SoundFont state management.  Provides `MidiState` for tracking whether a SoundFont (SF2) file has been loaded for MIDI instrument rendering.

*Source: [src/sound/midi.rs](src/sound/midi.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`MidiState`](src/sound/midi.rs#L11) | MIDI SoundFont state.  Tracks whether a SoundFont (SF2) file has been loaded ... |

### `sound::sound_data`

> Decoded PCM audio sample buffer.

*Source: [src/sound/sound_data.rs](src/sound/sound_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SoundData`](src/sound/sound_data.rs#L10) | Decoded audio samples in f32 PCM format.  Stores interleaved samples (for ste... |

## `stats` {#stats}

### `stats`

> RPG character sheet and stat system.  Provides a flexible attribute system with buffs, derived stats, traits, skills, perks, XP/levelling, action points, morale, and resistances.

*Source: [src/stats/mod.rs](src/stats/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`LevelThresholds`](src/stats/mod.rs#L194) | Level threshold setting: either a static table or a formula. |
| `enum` | [`StackMode`](src/stats/mod.rs#L10) | How a buff stacks with existing buffs of the same name. |
| `struct` | [`ActionPoints`](src/stats/mod.rs#L153) | Action point tracking for turn-based games. |
| `struct` | [`Attribute`](src/stats/mod.rs#L62) | A named stat attribute with base value, constraints, and regen. |
| `struct` | [`Buff`](src/stats/mod.rs#L21) | A single stat modifier attached to an attribute. |
| `struct` | [`Morale`](src/stats/mod.rs#L169) | Morale state with panic/berserk thresholds. |
| `struct` | [`Perk`](src/stats/mod.rs#L124) | A named perk requiring a minimum level to acquire. |
| `struct` | [`Sheet`](src/stats/mod.rs#L227) | The central character sheet, holding all stat data. |
| `struct` | [`Skill`](src/stats/mod.rs#L90) | A named skill with cooldown, resource cost, and level tracking. |
| `struct` | [`StatsRegistry`](src/stats/mod.rs#L546) | Global registry for traits and archetypes. Separate from Sheet for sharing. |
| `struct` | [`TraitDef`](src/stats/mod.rs#L146) | A named trait definition (a bundle of buff descriptors). |

## `thread` {#thread}

### `thread`

> Thread infrastructure: inter-thread channel and background Lua worker.

*Source: [src/thread/mod.rs](src/thread/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`channel`](src/thread/mod.rs#L3) | *(undocumented)* |
| `mod` | [`worker`](src/thread/mod.rs#L4) | *(undocumented)* |

### `thread::channel`

> Thread-safe channel for Lua inter-thread communication.  Provides an MPMC (multi-producer, multi-consumer) queue that can be shared between the main game thread and background Lua threads via `Arc<Channel>`. Only primitive Lua values (nil, bool, number, string) can cross thread boundaries.

*Source: [src/thread/channel.rs](src/thread/channel.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ChannelValue`](src/thread/channel.rs#L26) | Serializable values that can be sent between threads.  # Variants - `Nil` — N... |
| `fn` | [`channel_value_to_lua`](src/thread/channel.rs#L287) | Convert a `ChannelValue` back into a Lua value.  # Parameters - `lua` — `&Lua... |
| `fn` | [`lua_to_channel_value`](src/thread/channel.rs#L266) | Convert a Lua value into a `ChannelValue` for cross-thread transfer.  # Param... |
| `struct` | [`Channel`](src/thread/channel.rs#L42) | Thread-safe MPMC channel for Lua inter-thread communication.  Internally uses... |
| `struct` | [`LuaChannel`](src/thread/channel.rs#L205) | Lua UserData wrapper for a thread-safe channel.  # Fields - `inner` — `Arc<Ch... |

### `thread::worker`

> Background Lua thread with independent VM.  Each `LuaThread` spawns an OS thread running its own `mlua::Lua` instance. Communication with the main thread happens exclusively through `Channel` objects — no Lua state is shared across threads.

*Source: [src/thread/worker.rs](src/thread/worker.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ThreadState`](src/thread/worker.rs#L21) | Execution state of a background Lua thread.  # Variants - `Pending` — Pending... |
| `struct` | [`LuaThread`](src/thread/worker.rs#L37) | A background Lua thread running its own VM.  Created via `luna.thread.newThre... |

## `tilemap` {#tilemap}

### `tilemap`

> Tilemap engine module — TileSet, TileMap, AutoTileSheet, IsoMap, ChunkMap, TMX loader, and procedural generation types.

*Source: [src/tilemap/mod.rs](src/tilemap/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`autotile_sheet`](src/tilemap/mod.rs#L4) | Auto-tile atlas with bitmask-based tile selection. |
| `mod` | [`chunk`](src/tilemap/mod.rs#L6) | Sparse chunk-based tilemap storage for large and infinite maps. |
| `mod` | [`coords`](src/tilemap/mod.rs#L8) | Coordinate helpers for tile ↔ world conversions. |
| `mod` | [`isomap`](src/tilemap/mod.rs#L10) | Multi-level isometric tilemap with painter's-algorithm draw order. |
| `mod` | [`mapgen`](src/tilemap/mod.rs#L12) | Procedural map generation scripts and room placement. |
| `mod` | [`tile_walker`](src/tilemap/mod.rs#L35) | Grid-direction walker for tile stepping. |
| `mod` | [`tilemap`](src/tilemap/mod.rs#L15) | Core tilemap: layers, tile CRUD, sweep queries, and serialization. |
| `mod` | [`tileset`](src/tilemap/mod.rs#L17) | Tile set definition with per-tile properties and animation frames. |
| `mod` | [`tmx`](src/tilemap/mod.rs#L19) | Tiled TMX/TSX map format parser. |

### `tilemap::autotile_sheet`

> AutoTileSheet — loads a pre-arranged autotile image and generates TileSet tiles + bitmask rules.  ## 48-tile quarter-tile format  For high-quality composite autotile rendering, [`AutoTileSheet`] supports a 48-tile sub-tile format where each full tile is assembled from **four quarter-tile pieces** (TL, TR, BL, BR).  The source image must be **`3*tile_w` × `4*tile_h` pixels** — a 6-column × 8-row grid of `(tile_w/2 × tile_h/2)` quarter pieces.  Call [`AutoTileSheet::get_quarter_rects`] with the raw 8-bit neighbour bitmask to obtain the four [`Rect`]s to blit.  Use [`AutoTileSheet::get_composite48_grid_quad`] when the source atlas stores the 48 pre-composed tile variants in a 6 × 8 full-tile grid instead.  ### Bitmask bit layout (raw 8-bit)  ```text bit 0 = N   bit 4 = NE bit 1 = E   bit 5 = SE bit 2 = S   bit 6 = SW bit 3 = W   bit 7 = NW ```

*Source: [src/tilemap/autotile_sheet.rs](src/tilemap/autotile_sheet.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`AutoTileLayout`](src/tilemap/autotile_sheet.rs#L36) | Predefined autotile sheet layout variants.  # Variants - `Blob47` — Blob47 va... |
| `struct` | [`AutoTileSheet`](src/tilemap/autotile_sheet.rs#L50) | An autotile sheet that maps layout conventions to bitmask-based tile selectio... |

### `tilemap::chunk`

> Chunk-based tilemap storage for large and infinite maps.  [`ChunkMap`] stores tiles in fixed-size square chunks using a [`HashMap`]. Only chunks that have been written to are allocated, allowing sparse, large, or infinite maps without up-front memory commitment.  Tile coordinates use `i32` to support negative positions (e.g., a map centred at the origin where negative X/Y are valid map space).

*Source: [src/tilemap/chunk.rs](src/tilemap/chunk.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ChunkMap`](src/tilemap/chunk.rs#L20) | A chunk-based tilemap that supports large and infinite maps through sparse st... |

### `tilemap::coords`

> Coordinate conversion functions for isometric and hexagonal tile grids.

*Source: [src/tilemap/coords.rs](src/tilemap/coords.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`from_screen_hex`](src/tilemap/coords.rs#L137) | Converts screen position back to axial hex coordinates (pointy-top layout).  ... |
| `fn` | [`from_screen_iso`](src/tilemap/coords.rs#L38) | Converts screen position back to tile coordinates for diamond isometric proje... |
| `fn` | [`hex_area`](src/tilemap/coords.rs#L296) | Returns all hex cells within `radius` distance (filled hex circle).  # Parame... |
| `fn` | [`hex_distance`](src/tilemap/coords.rs#L172) | Returns the hex distance between two axial coordinates using cube distance.  ... |
| `fn` | [`hex_line`](src/tilemap/coords.rs#L221) | Returns all hex cells along a line between two axial coordinates.  # Paramete... |
| `fn` | [`hex_neighbors`](src/tilemap/coords.rs#L151) | Returns the six axial neighbor offsets for pointy-top hexagonal grids.  # Par... |
| `fn` | [`hex_reflect`](src/tilemap/coords.rs#L354) | Reflects hex coordinates across an axis through the center.  # Parameters - `... |
| `fn` | [`hex_ring`](src/tilemap/coords.rs#L251) | Returns all cells at exactly `radius` distance from `(q, r)`.  # Parameters -... |
| `fn` | [`hex_rotate`](src/tilemap/coords.rs#L321) | Rotates hex coordinates `(q, r)` around `(center_q, center_r)` by `steps × 60... |
| `fn` | [`hex_round`](src/tilemap/coords.rs#L189) | Rounds fractional axial coordinates to the nearest hex cell using cube roundi... |
| `fn` | [`hex_spiral`](src/tilemap/coords.rs#L279) | Returns all hex cells from center outward to `radius`, ring by ring.  # Param... |
| `fn` | [`iso_direction_from_angle`](src/tilemap/coords.rs#L89) | Snaps an angle (in radians) to the nearest isometric direction (1–4).  # Para... |
| `fn` | [`iso_direction_name`](src/tilemap/coords.rs#L67) | Returns the name of an isometric direction (1–4).  # Parameters - `direction`... |
| `fn` | [`iso_rotate`](src/tilemap/coords.rs#L54) | Rotates an isometric direction (1–4) clockwise by `steps`.  # Parameters - `d... |
| `fn` | [`to_screen_hex`](src/tilemap/coords.rs#L120) | Converts axial hex coordinates to screen position (pointy-top layout).  # Par... |
| `fn` | [`to_screen_iso`](src/tilemap/coords.rs#L24) | Converts tile coordinates to screen position using diamond isometric projecti... |

### `tilemap::isomap`

> Multi-level isometric tilemap for OpenXcom-style rendering.  Each map cell holds four tile parts rendered in painter's algorithm order: `Floor`, `NorthWall`, `WestWall`, `Object`. Z-levels stack vertically with a configurable pixel offset so higher floors appear above lower ones.

*Source: [src/tilemap/isomap.rs](src/tilemap/isomap.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`IsoTilePart`](src/tilemap/isomap.rs#L22) | The four sub-slots within each isometric map cell, rendered in this order.  #... |
| `struct` | [`IsoDrawItem`](src/tilemap/isomap.rs#L167) | One renderable item produced by [`IsoMap::draw_iter`] in painter's order.  # ... |
| `struct` | [`IsoLevel`](src/tilemap/isomap.rs#L88) | One Z-level of the isometric map — a 2-D grid of [`IsoTile`]s.  # Fields - `w... |
| `struct` | [`IsoMap`](src/tilemap/isomap.rs#L217) | Multi-level isometric tilemap with painter's-algorithm draw iteration.  # Fie... |
| `struct` | [`IsoTile`](src/tilemap/isomap.rs#L72) | One map cell containing four GIDs, one per [`IsoTilePart`].  # Fields - `part... |

### `tilemap::mapgen`

> Procedural map generation — MapBlock, MapGroup, MapScript, MapGen.

*Source: [src/tilemap/mapgen.rs](src/tilemap/mapgen.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Edge`](src/tilemap/mapgen.rs#L16) | Cardinal edge direction for block-segment connectivity.  # Variants - `North`... |
| `enum` | [`LayerMode`](src/tilemap/mapgen.rs#L677) | How layers are managed during generation.  # Variants - `Unified` — Unified v... |
| `enum` | [`MapOrientation`](src/tilemap/mapgen.rs#L664) | Map orientation for visual layout hints.  # Variants - `TopDown` — TopDown va... |
| `enum` | [`MapSize`](src/tilemap/mapgen.rs#L692) | Predefined map size presets expressed in segment-grid units.  # Variants - `S... |
| `enum` | [`StepType`](src/tilemap/mapgen.rs#L395) | The type of operation a [`ScriptStep`] performs.  # Variants - `FillRandom` —... |
| `struct` | [`MapBlock`](src/tilemap/mapgen.rs#L64) | A prefab grid of tiles that can be stamped into a generated map.  Contains mu... |
| `struct` | [`MapGen`](src/tilemap/mapgen.rs#L768) | Top-level procedural map generator.  Produces a [`TileMap`] by stamping [`Map... |
| `struct` | [`MapGroup`](src/tilemap/mapgen.rs#L269) | A biome-like container holding [`MapBlock`] prefabs and [`MapScript`] generat... |
| `struct` | [`MapScript`](src/tilemap/mapgen.rs#L579) | A named sequence of [`ScriptStep`]s that drives procedural generation. |
| `struct` | [`MapZone`](src/tilemap/mapgen.rs#L725) | A named horizontal zone within a generated map.  # Fields - `name` — `String`... |
| `struct` | [`ScriptStep`](src/tilemap/mapgen.rs#L486) | A single step in a [`MapScript`] with rich configuration.  # Fields - `step_t... |

### `tilemap::tile_walker`

> Tile-based first-person movement controller with cardinal directions.

*Source: [src/tilemap/tile_walker.rs](src/tilemap/tile_walker.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Facing`](src/tilemap/tile_walker.rs#L17) | Cardinal facing direction.  # Variants - `North` — North variant. - `East` — ... |
| `struct` | [`TileWalker`](src/tilemap/tile_walker.rs#L134) | Tile-based movement controller for first-person grid navigation.  Operates on... |

### `tilemap::tilemap`

> Main tile map container with layers, tiles, viewport, collision, and autotile support.

*Source: [src/tilemap/tilemap.rs](src/tilemap/tilemap.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SweepResult`](src/tilemap/tilemap.rs#L80) | Result of a swept-AABB collision test against solid tiles.  # Fields - `conta... |
| `struct` | [`TileLayer`](src/tilemap/tilemap.rs#L23) | A single layer of tiles in a [`TileMap`].  # Fields - `name` — `String`. - `w... |
| `struct` | [`TileMap`](src/tilemap/tilemap.rs#L97) | A 2D tile map composed of layers, tilesets, and viewport-clipped rendering st... |

### `tilemap::tileset`

> Tile set backed by a texture atlas with animation, solid flags, and autotile rules.

*Source: [src/tilemap/tileset.rs](src/tilemap/tileset.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`TileAnimFrame`](src/tilemap/tileset.rs#L13) | A single frame in a tile animation sequence.  # Fields - `tile_id` — `u32`. -... |
| `struct` | [`TileSet`](src/tilemap/tileset.rs#L25) | A tile set that maps local tile IDs to atlas regions, animations, and collisi... |

### `tilemap::tmx`

> Tiled TMX map format loader.  Parses `.tmx` files produced by the [Tiled Map Editor](https://www.mapeditor.org/). TMX is an XML-based format; this module uses [`roxmltree`] for parsing.  Supports: - Orthogonal, isometric, staggered, and hexagonal map orientations. - Tile data encoding: XML elements, CSV, and Base64 (with optional zlib/gzip compression). - Inline and external tileset references (TSX source returned as a path string for caller resolution). - Object layers and image layers (parsed to metadata; not converted to [`TileMap`] data).  Does NOT support: - Infinite maps (chunk-based TMX format): treat as a warning; use [`ChunkMap`] directly. - Embedded TSX tileset files — the caller must load them separately.  [`ChunkMap`]: super::chunk::ChunkMap

*Source: [src/tilemap/tmx.rs](src/tilemap/tmx.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`TmxLayer`](src/tilemap/tmx.rs#L200) | Variant tag for TMX map layers.  # Variants - `Tile` — Tile variant. - `Objec... |
| `enum` | [`TmxOrientation`](src/tilemap/tmx.rs#L31) | Rendering orientation of the map, as specified in the TMX `orientation` attri... |
| `enum` | [`TmxStaggerAxis`](src/tilemap/tmx.rs#L59) | The axis along which isometric / hexagonal tiles are staggered.  # Variants -... |
| `fn` | [`load_tmx`](src/tilemap/tmx.rs#L289) | Parses a TMX file given its XML content as a string.  # Parameters - `xml` — ... |
| `struct` | [`TmxMap`](src/tilemap/tmx.rs#L221) | A fully-parsed TMX map.  # Fields - `width` — `u32`. - `height` — `u32`. - `t... |
| `struct` | [`TmxObject`](src/tilemap/tmx.rs#L175) | A single Tiled object within an object layer.  # Fields - `id` — `u32`. - `na... |
| `struct` | [`TmxObjectLayer`](src/tilemap/tmx.rs#L154) | An object layer (object group) from a TMX map.  # Fields - `name` — `String`.... |
| `struct` | [`TmxTileLayer`](src/tilemap/tmx.rs#L128) | A standard tile layer from a TMX map.  # Fields - `name` — `String`. - `width... |
| `struct` | [`TmxTileset`](src/tilemap/tmx.rs#L87) | A tileset reference embedded in a TMX map.  # Fields - `first_gid` — `u32`. -... |

## `timer` {#timer}

### `timer`

*Source: [src/timer/mod.rs](src/timer/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`clock`](src/timer/mod.rs#L2) | Frame-based clock providing delta time, total time, and FPS. |
| `mod` | [`scheduler`](src/timer/mod.rs#L4) | Scheduled event manager for delayed and repeating timed callbacks. |

### `timer::clock`

*Source: [src/timer/clock.rs](src/timer/clock.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Clock`](src/timer/clock.rs#L7) | Tracks per-frame delta time, accumulated total time, and a rolling FPS measur... |

### `timer::scheduler`

> Scheduled event manager for delayed and repeating timed callbacks.  Provides [`Scheduler`] for one-shot delays, repeating timers, named events, individual pause/resume, global time-scale, and on-the-fly interval changes.  ## Usage ```rust,no_run # use luna2d::timer::Scheduler; let mut sched = Scheduler::new(); let id = sched.after(2.0);          // fires once after 2 seconds let rid = sched.every(0.5, -1);     // fires every 0.5 s indefinitely sched.set_time_scale(0.5);          // run at half speed let fired = sched.update(0.1);      // advance; returns IDs that fired sched.cancel(rid); ```

*Source: [src/timer/scheduler.rs](src/timer/scheduler.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ScheduledEvent`](src/timer/scheduler.rs#L28) | A single scheduled event with optional name and pause state.  # Fields - `id`... |
| `struct` | [`Scheduler`](src/timer/scheduler.rs#L55) | Manages a collection of timed events (one-shot and repeating).  Each event ha... |

## `window` {#window}

### `window`

*Source: [src/window/mod.rs](src/window/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`event_loop`](src/window/mod.rs#L2) | Placeholder for the platform window event loop integration. |

---
*Generated by `tools/gen_docs_rust.py`. Do not edit by hand.*