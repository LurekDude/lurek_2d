# Luna2D Rust API Reference

> Auto-generated by `tools/gen_docs_rust.py` from `docs/API/api_data.json`.
> Version: `0.4.0` | Generated: 2026-04-03
> **Coverage:** 1132/1132 public items documented

> For Lua scripting API see `docs/API/lua-api.md`.
> For engine contributors and extension authors only.

## Contents

- [`ai`](#ai) — 44 items
- [`audio`](#audio) — 22 items
- [`automation`](#automation) — 4 items
- [`battle`](#battle) — 10 items
- [`cardgame`](#cardgame) — 34 items
- [`combat`](#combat) — 16 items
- [`compute`](#compute) — 63 items
- [`crafting`](#crafting) — 25 items
- [`data`](#data) — 23 items
- [`dataframe`](#dataframe) — 13 items
- [`dialog`](#dialog) — 4 items
- [`economy`](#economy) — 9 items
- [`engine`](#engine) — 44 items
- [`entity`](#entity) — 7 items
- [`event`](#event) — 5 items
- [`filesystem`](#filesystem) — 15 items
- [`graph`](#graph) — 21 items
- [`graphics`](#graphics) — 92 items
- [`gui`](#gui) — 50 items
- [`image`](#image) — 5 items
- [`input`](#input) — 24 items
- [`inventory`](#inventory) — 15 items
- [`item`](#item) — 34 items
- [`lua_api`](#lua-api) — 207 items
- [`math`](#math) — 95 items
- [`minimap`](#minimap) — 7 items
- [`modding`](#modding) — 3 items
- [`overlay`](#overlay) — 19 items
- [`particle`](#particle) — 23 items
- [`pathfinding`](#pathfinding) — 33 items
- [`physics`](#physics) — 14 items
- [`postfx`](#postfx) — 6 items
- [`province_map`](#province-map) — 30 items
- [`quest`](#quest) — 12 items
- [`savegame`](#savegame) — 6 items
- [`scene`](#scene) — 9 items
- [`stats`](#stats) — 14 items
- [`thread`](#thread) — 9 items
- [`tilemap`](#tilemap) — 60 items
- [`timer`](#timer) — 5 items
- [`window`](#window) — 1 items

## `ai` {#ai}

### `ai`

> # AI Module — Game AI Toolkit (Tier 2)  Provides a comprehensive suite of decoupled game AI subsystems for Luna2D. Each subsystem can be used independently or composed together through the [`Agent`] / [`AIWorld`] framework.  ## Architecture Overview  The AI module is a **Tier 2 Engine Extension**. It may import `math`, `engine`, and Tier 1 modules (primarily `pathfinding` for grid and flow-field re-exports). It must not import other Tier 2 modules or any Tier 3 modules.  All AI computation is **pure CPU math** — no GPU, audio, or window access. This means every subsystem can run headlessly in tests without a graphics context.  ## Subsystems  | Subsystem | Description | |-----------|-------------| | [`fsm`] | Finite state machine with priority-ordered guarded transitions | | [`behavior_tree`] | Hierarchical behavior tree with composites, decorators, and leaf callbacks | | [`steering`] | Reynolds-style steering behaviors (seek, flee, arrive, wander, pursue, evade, flock) | | [`goap`] | Goal-Oriented Action Planning using A★ over boolean world state | | [`utility_ai`] | Multi-axis utility scorer with response curves for action selection | | [`qlearner`] | Tabular epsilon-greedy Q-learning for reinforcement learning | | [`influence_map`] | Multi-layer spatial float grid for strategic area analysis | | [`squad`] | Squad coordination with formation offset computation | | [`command_queue`] | RTS-style ordered command queue with interrupt and cancel | | [`blackboard`] | Hierarchical key-value store for inter-agent data sharing |  ## Agent–World Model  [`AIWorld`] owns all [`Agent`] instances. Each agent carries kinematic state (position, velocity, max speed/force), a [`DecisionModel`] that selects which subsystems are ticked, and a local [`Blackboard`] that chains to the world's global blackboard for hierarchical lookup.  The world ticks agents in descending priority order during `update(dt)`. FSM transitions, BT ticks, and steering force calculations all happen during this pass. The Lua API layer (`luna.ai.*`) wraps these Rust types.  ## Dependencies  - [`pathgrid`] and [`flowfield`] are thin re-exports from `crate::pathfinding`   (a Tier 2 sibling — re-exported here so `luna.ai.*` has a unified surface). - All Lua callbacks are stored as `mlua::RegistryKey` references. - No heap allocation happens per-frame in steady state; vectors are grown at   agent/behavior creation time.

*Source: [src/ai/mod.rs](src/ai/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`agent`](src/ai/mod.rs#L52) | Autonomous agent with kinematic state (position, velocity) and pluggable deci... |
| `mod` | [`behavior_tree`](src/ai/mod.rs#L55) | Hierarchical behavior tree with composite nodes (sequence, selector, parallel... |
| `mod` | [`blackboard`](src/ai/mod.rs#L58) | Typed key-value store with optional parent-chain lookup, used for sharing nam... |
| `mod` | [`command_queue`](src/ai/mod.rs#L61) | RTS-style ordered command queue supporting enqueue, interrupt (push-front), r... |
| `mod` | [`flowfield`](src/ai/mod.rs#L64) | Dijkstra-based flow field for efficient crowd pathfinding — re-exported from ... |
| `mod` | [`fsm`](src/ai/mod.rs#L67) | Finite state machine with named states (enter/update/exit callbacks) and prio... |
| `mod` | [`goap`](src/ai/mod.rs#L70) | Goal-Oriented Action Planning (GOAP) solver using A★ search over boolean worl... |
| `mod` | [`influence_map`](src/ai/mod.rs#L73) | Multi-layer spatial float grid for influence mapping — supports stamping, dif... |
| `mod` | [`pathgrid`](src/ai/mod.rs#L76) | Weighted grid pathfinding (A★, Dijkstra) with obstacle support — re-exported ... |
| `mod` | [`qlearner`](src/ai/mod.rs#L79) | Tabular epsilon-greedy Q-learner for discrete-state reinforcement learning wi... |
| `mod` | [`squad`](src/ai/mod.rs#L82) | Squad coordination: named groups of agents with formation offset computation ... |
| `mod` | [`steering`](src/ai/mod.rs#L85) | Reynolds-style autonomous steering behaviors (seek, flee, arrive, wander, pur... |
| `mod` | [`utility_ai`](src/ai/mod.rs#L88) | Multi-axis utility scorer: evaluates actions through response-curve considera... |
| `mod` | [`world`](src/ai/mod.rs#L91) | Spatial AI container that owns all agents, provides O(1) name-based lookup, h... |

### `ai::agent`

> Autonomous agent with kinematic state and attached decision subsystems.  An [`Agent`] is the fundamental unit of AI behavior in Luna2D. Each agent carries its own position, velocity, kinematic constraints (max speed and force), and a [`DecisionModel`] that determines which AI subsystems are ticked during the world's `update(dt)` pass.  Agents are always owned by an [`AIWorld`](crate::ai::world::AIWorld) and are created via `AIWorld::add_agent()`. They are never instantiated standalone in production — the world manages their lifecycle, priority ordering, and blackboard parent-chain wiring.  ## Decision Models  The [`DecisionModel`] enum selects which combination of subsystems the world ticks for this agent each frame:  - `Fsm` — only the [`StateMachine`](crate::ai::fsm::StateMachine) - `Bt` — only the [`BehaviorTree`](crate::ai::behavior_tree::BehaviorTree) - `Steering` — only the [`SteeringManager`](crate::ai::steering::SteeringManager) - `FsmSteering` — FSM first, then steering - `BtSteering` — BT first, then steering  ## Blackboard Hierarchy  Each agent has a local [`Blackboard`]. When created via `AIWorld::add_agent()`, the local blackboard's parent is set to the world's global blackboard, forming a two-level lookup chain: local → global. Writes always go to the local store; reads walk the chain until a match is found.

*Source: [src/ai/agent.rs](src/ai/agent.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DecisionModel`](src/ai/agent.rs#L52) | Controls which AI subsystems are ticked for an agent during `AIWorld::update`... |
| `struct` | [`Agent`](src/ai/agent.rs#L142) | An autonomous AI agent with kinematic state and pluggable decision subsystems... |

### `ai::behavior_tree`

> Behavior Tree with composite, decorator, and leaf nodes.  A behavior tree is a hierarchical decision-making structure where each node returns a [`BTStatus`] (Success, Failure, or Running) after being ticked. The tree is traversed from root each frame. Composite nodes (Selector, Sequence, Parallel) manage child execution order. Decorator nodes (Inverter, Repeater, Succeeder) modify child results. Leaf nodes (Action, Condition) call Lua callbacks to evaluate game state or perform actions.  ## Running State and Multi-Frame Execution  When a leaf node returns `Running`, composite nodes memo the running-child index so the next tick resumes from that child instead of restarting from the first child. This enables multi-frame behaviors like "walk to target" that take several frames to complete.  ## Lua Integration  Leaf callbacks are stored as `mlua::RegistryKey` references: - **Action**: `fn(agent_table, blackboard_table, dt) → "success" | "failure" | "running"` - **Condition**: `fn(agent_table, blackboard_table) → bool`  The tree itself does not call these callbacks — that happens at the [`AIWorld`](crate::ai::world::AIWorld) level during `update(dt)`.

*Source: [src/ai/behavior_tree.rs](src/ai/behavior_tree.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BTNode`](src/ai/behavior_tree.rs#L163) | A node in the behavior tree. Nodes are organized into three categories:  **Co... |
| `enum` | [`BTStatus`](src/ai/behavior_tree.rs#L39) | Execution status returned by every behavior tree node after a tick.  The thre... |
| `enum` | [`ParallelPolicy`](src/ai/behavior_tree.rs#L95) | Policy for determining when a Parallel composite node succeeds or fails.  Par... |
| `struct` | [`BehaviorTree`](src/ai/behavior_tree.rs#L290) | Root container for a behavior tree instance.  Wraps an optional root [`BTNode... |

### `ai::blackboard`

> Typed key-value store with optional parent chain for hierarchical lookup.  Blackboards are the primary mechanism for sharing named data between AI subsystems in Luna2D. Each [`Agent`](crate::ai::agent::Agent) has a local blackboard; each [`Squad`](crate::ai::squad::Squad) has a squad-level blackboard; and the [`AIWorld`](crate::ai::world::AIWorld) has a global one.  ## Hierarchical Lookup  Blackboards can be chained: an agent's blackboard has the world's global blackboard as its parent. When reading a key, the lookup walks the parent chain until a match is found or the chain ends. Writes always target the local store — they never propagate to parents.  ## Supported Value Types  Three value types are supported via [`BlackboardValue`]: - `Number(f64)` — numeric values - `Bool(bool)` — boolean flags - `Text(String)` — string data  Each type has dedicated get/set methods that enforce type safety at the Rust level. The Lua API layer provides a unified `bb:get(key)` interface.

*Source: [src/ai/blackboard.rs](src/ai/blackboard.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BlackboardValue`](src/ai/blackboard.rs#L39) | A typed value stored in a blackboard slot.  Three types are supported, matchi... |
| `struct` | [`Blackboard`](src/ai/blackboard.rs#L71) | A hierarchical key-value store for sharing named data between AI subsystems. ... |

### `ai::command_queue`

> RTS-style ordered command queue for scheduling unit actions.  A [`CommandQueue`] manages a FIFO sequence of [`Command`] entries for a single unit or agent. Commands are dequeued from the front and ticked each frame via their Lua callback. Three insertion modes are supported:  - **`enqueue`** — appends a command to the back (standard queue behavior). - **`push_front`** — inserts at the front, interrupting the current command   without clearing the rest of the queue. - **`replace`** — clears the entire queue and enqueues one new command.  Commands carry metadata: a `kind` string (e.g., `"move"`, `"attack"`, `"patrol"`), target coordinates, a priority hint, and an `interruptible` flag that controls whether `cancel_current()` can remove it.  ## Lua Callback Contract  Each command's `callback` is a Lua function `fn(dt) → bool`: - Returns `true` if the command is still running (tick again next frame). - Returns `false` if the command is done (advance to next in queue).  The AIWorld or Lua game loop is responsible for ticking the front command each frame and calling `advance()` when the callback returns `false`.

*Source: [src/ai/command_queue.rs](src/ai/command_queue.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Command`](src/ai/command_queue.rs#L43) | A single RTS unit command with metadata and a Lua tick callback.  Commands ar... |
| `struct` | [`CommandQueue`](src/ai/command_queue.rs#L79) | A FIFO queue of [`Command`] entries for sequential unit action scheduling.  C... |

### `ai::fsm`

> Finite State Machine with priority-ordered guarded transitions.  The FSM is a classic game AI pattern: the agent is in exactly one named state at a time. Each state has optional Lua lifecycle callbacks (`on_enter`, `on_update`, `on_exit`). Transitions between states are guarded by optional Lua predicates and ordered by descending priority.  ## Update Cycle  During each `AIWorld::update(dt)` tick for an agent with `DecisionModel::Fsm`:  1. All transitions from the current state are checked in descending priority    order. The first transition whose guard returns `true` (or has no guard)    fires. 2. If a transition fires: the current state's `on_exit` is called, the    current state switches, `time_in_state` resets to 0, and the new state's    `on_enter` is called. 3. The current state's `on_update(dt)` is called.  ## Priority Ordering  Transitions are stored sorted by descending priority. When multiple transitions from the same source state could fire, the highest-priority one wins. This allows "panic" transitions (e.g., health < 10% → flee) to override normal patrol logic without complex guard nesting.

*Source: [src/ai/fsm.rs](src/ai/fsm.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`StateCallbacks`](src/ai/fsm.rs#L46) | Lua lifecycle hooks for a single FSM state.  Each state can have up to three ... |
| `struct` | [`StateMachine`](src/ai/fsm.rs#L103) | A finite state machine that manages named states with lifecycle callbacks and... |
| `struct` | [`Transition`](src/ai/fsm.rs#L67) | A directed edge in the FSM state graph with an optional guard predicate.  Tra... |

### `ai::goap`

> Goal-Oriented Action Planning (GOAP) using A★ search over boolean world state.  GOAP is a planning system where the agent has a set of actions (each with boolean preconditions and effects) and a set of goals (target boolean world states). The planner uses A★ search to find the cheapest sequence of actions that transforms the current world state into one that satisfies the highest-priority goal.  ## How Planning Works  1. The planner picks the highest-priority [`GOAPGoal`] (by `priority` field). 2. It initializes an A★ search with the current world state as the start node. 3. For each node, it tries every action whose preconditions are met, generating    successor nodes by applying the action's effects. 4. The heuristic counts unsatisfied goal conditions (admissible: never overestimates). 5. The first path that satisfies all goal conditions is returned as a list of    action names in execution order.  ## Search Limits  The planner enforces two depth/iteration limits: - `max_depth` — maximum number of actions in a plan (prevents infinite chains). - Hard limit of 10,000 A★ iterations (prevents runaway computation on large   action spaces).  ## Lua Integration  Actions can have optional Lua callbacks (`GOAPAction::callback`) that are executed when the game loop processes the planned action sequence. The planner itself is pure logic — it never calls Lua during planning.

*Source: [src/ai/goap.rs](src/ai/goap.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`GOAPAction`](src/ai/goap.rs#L53) | A single GOAP action with boolean preconditions and effects.  Actions are the... |
| `struct` | [`GOAPGoal`](src/ai/goap.rs#L80) | A planning goal expressed as a desired boolean world state.  Goals represent ... |
| `struct` | [`GOAPPlanner`](src/ai/goap.rs#L140) | A★ planner that finds optimal action sequences to satisfy goals over boolean ... |

### `ai::influence_map`

> Multi-layer spatial float grid for strategic area analysis and influence mapping.  An [`InfluenceMap`] divides the game world into a uniform grid of cells, each storing one float value per named layer. Layers allow different types of influence to coexist (e.g., `"danger"`, `"resources"`, `"visibility"`) without interfering with each other.  ## Common Operations  - **Stamping**: `stamp_influence()` adds circular influence with linear falloff   at a world-space position (e.g., stamp enemy threat around each enemy unit). - **Propagation**: `propagate()` applies 3×3 averaging diffusion to spread values   across the grid, controlled by a `momentum` parameter. - **Decay**: `decay()` multiplies all cells by a factor each frame, causing old   influence to fade over time. - **Querying**: `max_position()`, `min_position()`, and `query_rect()` let AI   agents reason about which areas are most/least dangerous, resourceful, etc. - **Blending**: `blend()` combines two layers with weighted addition into a   destination layer (e.g., `combined = 0.7 * danger + 0.3 * distance`).  ## Coordinate System  The grid uses integer cell coordinates internally, but all public APIs accept and return world-space floating-point coordinates. The `cell_size` parameter controls the mapping between world space and grid space.

*Source: [src/ai/influence_map.rs](src/ai/influence_map.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`InfluenceMap`](src/ai/influence_map.rs#L46) | A multi-layer spatial float grid for influence mapping and strategic reasonin... |

### `ai::qlearner`

> Tabular epsilon-greedy Q-learner for discrete-state reinforcement learning.  Implements the classic Q-learning algorithm where an agent learns optimal action-selection policies by iteratively updating a state-action value table (Q-table) using the Bellman equation:  `Q(s,a) ← Q(s,a) + α[r + γ max_a' Q(s',a') - Q(s,a)]`  ## Parameters  - **α (alpha)**: Learning rate ∈ (0,1]. Controls how much new information   overrides old. Default: 0.1. - **γ (gamma)**: Discount factor ∈ [0,1]. Controls how much future rewards   are valued relative to immediate rewards. Default: 0.9. - **ε (epsilon)**: Exploration rate ∈ [0,1]. Probability of choosing a random   action instead of the best known action. Default: 0.1. - **epsilon_decay**: Multiplied into epsilon after each episode, gradually   shifting from exploration to exploitation. Default: 0.995.  ## Storage  The Q-table is a flat `Vec<f64>` of size `state_count × action_count`, indexed as `state * action_count + action`. This is more cache-friendly than a HashMap for small-to-medium state spaces.  ## Serialization  The Q-table can be serialized to JSON (`serialize()`) and deserialized (`deserialize()`) for saving/loading trained policies. The JSON format is a 2D array: `[[q(s0,a0), q(s0,a1), ...], [q(s1,a0), ...], ...]`.

*Source: [src/ai/qlearner.rs](src/ai/qlearner.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`QLearner`](src/ai/qlearner.rs#L52) | Tabular epsilon-greedy Q-learner for discrete-state reinforcement learning.  ... |

### `ai::squad`

> Multi-agent formation groups with offset computation.  A [`Squad`] groups named agents under an optional leader and computes world-space formation positions relative to the leader's current location. Five formation shapes are supported: `Line`, `Wedge`, `Circle`, `Column`, and `None` (no formation — all members collapse to the leader's position).  Squads also carry a shared [`Blackboard`] for intra-group communication. The leader's blackboard and the squad blackboard are independent — link them via parent-chain if you need automatic fact propagation.

*Source: [src/ai/squad.rs](src/ai/squad.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FormationType`](src/ai/squad.rs#L28) | Formation shapes for squad positioning. Consult the module-level documentatio... |
| `struct` | [`Squad`](src/ai/squad.rs#L90) | A named group of agents with formation positioning and shared state.  The squ... |

### `ai::steering`

> Reynolds-style steering behaviors with weighted/priority combination.  This module implements Craig Reynolds' autonomous agent steering behaviors, adapted for 2D game AI. Each behavior produces a 2D force vector that is applied to the agent's velocity by the [`SteeringManager`] (managed at the [`AIWorld`](crate::ai::world::AIWorld) level).  ## Available Behaviors  - **Seek** — Produces a force toward a target position at maximum speed. - **Flee** — Produces a force away from a threat, with a configurable panic   distance beyond which the agent ignores the threat. - **Arrive** — Like seek, but decelerates within a slowing radius to stop   smoothly at the target instead of oscillating around it. - **Wander** — Projects a circle ahead of the agent and picks a random point   on its circumference, creating natural-looking meandering movement. - **Pursue** — Predicts a moving target's future position and steers toward   the intercept point rather than the current position. - **Evade** — Inverse of pursue: predicts a threat's future position and   steers away from the intercept point. - **Flock** — Combines separation, alignment, and cohesion forces for group   movement (Reynolds boids model).  ## Combination Modes  Multiple active behaviors are combined via [`CombineMode`]: - **Weighted**: all forces are summed (each multiplied by its weight),   then truncated to `max_force`. - **Priority**: behaviors are evaluated in order; the first non-zero force   is used and remaining behaviors are skipped.

*Source: [src/ai/steering.rs](src/ai/steering.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CombineMode`](src/ai/steering.rs#L51) | Determines how multiple active steering behaviors are combined into a single ... |
| `enum` | [`SteeringBehaviorType`](src/ai/steering.rs#L133) | All concrete steering behavior types supported by the AI system.  Each varian... |
| `struct` | [`SteeringBase`](src/ai/steering.rs#L97) | Shared parameters common to all steering behavior instances.  Every [`Steerin... |
| `struct` | [`SteeringManager`](src/ai/steering.rs#L368) | Manages a list of steering behaviors and combines their forces each frame.  #... |
| `type` | [`Force`](src/ai/steering.rs#L33) | 2D force vector (fx, fy). Consult the module-level documentation for the broa... |

### `ai::utility_ai`

> Multi-axis utility scorer that chooses the action with highest composite score.  Utility AI evaluates a set of candidate actions by scoring each one across multiple axes ("considerations"). Each consideration queries a game state value via a Lua callback, maps it through a response curve, and multiplies by a weight. The scores for all considerations are multiplied together to produce a composite score for the action.  ## Response Curves  Raw input values from Lua callbacks are transformed through [`ResponseCurve`] functions before weighting. Five curve shapes are available:  - **Linear** — `p1 × input + p2` - **Quadratic** — `p1 × input² + p2 × input + p3` - **Logistic** — `1 / (1 + e^(-p1 × (input - p2)))` (S-curve) - **Logit** — `ln(input/(1−input)) × p1 + p2` (inverse sigmoid) - **Step** — `p2 if input ≥ p1, else p3` (hard threshold)  ## Momentum  Each action can have a `momentum_bonus` that is added when that action was chosen in the previous evaluation. This prevents rapid flip-flopping between equally scored actions (action inertia).  ## Evaluation  The AIWorld evaluates the UtilityAI for agents with the appropriate decision model. `last_action` and `last_scores` are cached for debugging/inspection.

*Source: [src/ai/utility_ai.rs](src/ai/utility_ai.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ResponseCurve`](src/ai/utility_ai.rs#L48) | Mathematical function shapes for transforming raw consideration inputs into n... |
| `struct` | [`Consideration`](src/ai/utility_ai.rs#L130) | A single evaluation axis within a utility action's scoring function.  Each co... |
| `struct` | [`UAAction`](src/ai/utility_ai.rs#L159) | A candidate action in the utility AI decision space.  Each action has a name ... |
| `struct` | [`UtilityAI`](src/ai/utility_ai.rs#L182) | Multi-axis utility scorer that evaluates candidate actions and chooses the on... |

### `ai::world`

> Container that owns agents and ticks them in descending priority order each frame.  The [`AIWorld`] is the top-level manager for the AI subsystem. It owns all [`Agent`](crate::ai::agent::Agent)s, maintains a name→index lookup table, and provides the global [`Blackboard`] that serves as the parent for every agent's local blackboard.  ## Agent Lifecycle  Agents are added via `add_agent(name)`, which creates an agent with default kinematic state and wires its local blackboard's parent to the global one. Agents can be removed by name; removal triggers a full index rebuild.  ## Update Loop  During `update(dt)` (called from the Lua `luna.update` callback), the world ticks all agents in descending `priority` order. For each agent, it checks the agent's [`DecisionModel`](crate::ai::agent::DecisionModel) and ticks the appropriate subsystems (FSM, BehaviorTree, SteeringManager).

*Source: [src/ai/world.rs](src/ai/world.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`AIWorld`](src/ai/world.rs#L41) | Top-level AI container that owns agents and provides global shared state.  Ag... |

## `audio` {#audio}

### `audio`

> Audio subsystem for Luna2D games.  This module provides sound loading, playback, and volume management through a [`Mixer`] that wraps the [rodio](https://crates.io/crates/rodio) audio library. Game code accesses audio through the `luna.audio` Lua bindings in [`crate::lua_api`], which delegates all work to [`Mixer`] and [`Bus`] instances stored in `SharedState`.  # Key types  - [`Mixer`] — central audio mixer; manages all loaded sounds, their playback state,   volume, pitch, pan, looping, and fade effects. - [`Bus`] — named group that applies a shared volume and pause state to every source   assigned to it (e.g. a "music" bus or "sfx" bus). - [`AudioSource`] — lightweight handle for a loaded audio file, carrying the path and   default playback settings. - [`SourceType`] — controls whether a sound is decoded fully into memory (`Static`) or   read incrementally from disk during playback (`Stream`). - [`PlayState`] — the current playback status of a source: `Stopped`, `Playing`, or `Paused`.  # Design notes  Buses are pure data containers; the mixer multiplies source volume/pitch by bus values on every `set_volume` or `update` call.  Audio sources are tracked in a slot-map keyed by [`crate::engine::resource_keys::SoundKey`], giving O(1) lookup and safe handle invalidation when sources are released.

*Source: [src/audio/mod.rs](src/audio/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`get_playback_device`](src/audio/mod.rs#L70) | Returns the name of the currently active audio output device.  # Returns `Str... |
| `fn` | [`get_playback_devices`](src/audio/mod.rs#L62) | Returns the names of all available audio output devices.  On most systems thi... |
| `fn` | [`set_playback_device`](src/audio/mod.rs#L84) | Selects the audio output device by name.  Accepts any name returned by [`get_... |
| `mod` | [`bus`](src/audio/mod.rs#L28) | Named audio bus for grouping sources under shared volume/pitch/pause controls. |
| `mod` | [`decoder`](src/audio/mod.rs#L30) | Streaming audio decoder for chunked PCM reading. |
| `mod` | [`midi`](src/audio/mod.rs#L52) | MIDI SoundFont state management. |
| `mod` | [`midi_player`](src/audio/mod.rs#L32) | Software MIDI synthesizer with sine-additive PCM rendering. |
| `mod` | [`mixer`](src/audio/mod.rs#L34) | Low-level audio mixer using rodio for playback and volume control. |
| `mod` | [`sound_data`](src/audio/mod.rs#L48) | Decoded PCM sample buffer with per-sample read/write access. |
| `mod` | [`source`](src/audio/mod.rs#L36) | Audio source handle holding path, volume, and loop settings. |

### `audio::bus`

> Named audio bus for grouping sources under shared volume, pitch, and pause controls.  This module is part of Luna2D's `audio` subsystem and provides the implementation details for bus-related operations and data management. Key types exported from this module: `Bus`. Primary functions: `new()`, `name()`, `volume()`, `set_volume()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/audio/bus.rs](src/audio/bus.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Bus`](src/audio/bus.rs#L25) | A named audio bus that applies volume, pitch, and pause overrides to all sour... |

### `audio::decoder`

> Streaming audio decoder for chunked PCM reading.  `Decoder` reads an audio file and returns PCM data in fixed-size buffer chunks, enabling streaming playback without loading the entire file into memory.

*Source: [src/audio/decoder.rs](src/audio/decoder.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Decoder`](src/audio/decoder.rs#L22) | Streaming audio decoder that reads PCM in fixed-size chunks.  The decoder eag... |

### `audio::midi`

> MIDI SoundFont state management.  Provides `MidiState` for tracking whether a SoundFont (SF2) file has been loaded for MIDI instrument rendering.  This module is part of Luna2D's `audio` subsystem and provides the implementation details for midi-related operations and data management. Key types exported from this module: `MidiState`. Primary functions: `new()`, `set_soundfont()`, `has_soundfont()`, `clear_soundfont()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/audio/midi.rs](src/audio/midi.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`MidiState`](src/audio/midi.rs#L25) | MIDI SoundFont state. Consult the module-level documentation for the broader ... |

### `audio::midi_player`

> Software MIDI synthesizer: parses MIDI with `midly`, renders to PCM via sine-additive synthesis, and plays through a rodio `Sink`.  This module is part of Luna2D's `audio` subsystem and provides the implementation details for midi player-related operations and data management. Key types exported from this module: `MidiData`, `MidiPlayer`. Primary functions: `new()`, `load()`, `load_data()`, `is_loaded()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/audio/midi_player.rs](src/audio/midi_player.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`MidiData`](src/audio/midi_player.rs#L30) | Pre-parsed MIDI metadata extracted during `load()`.  # Fields - `duration_sec... |
| `struct` | [`MidiPlayer`](src/audio/midi_player.rs#L71) | Software MIDI player with sine-additive synthesis.  Parses MIDI via `midly`, ... |

### `audio::mixer`

> Core audio mixer that owns every loaded sound and drives playback through rodio.  [`Mixer`] is the single point of entry for all audio operations in Luna2D.  It opens the system's default output stream on construction (via rodio) and maintains a slot-map of [`AudioEntry`] records — one per loaded sound.  Each entry embeds a `rodio::Sink` for playback control and cached metadata (duration, fade parameters, loop flag, filters).  # Source types  The mixer supports two loading strategies selected at load time: - **Static** — the raw file bytes are decoded and held in an `Arc<Vec<u8>>` inside the   entry.  Playback restarts by re-decoding from that in-memory buffer, giving very low   latency at the cost of higher RAM usage.  Best for short sound effects. - **Stream** — the file is opened fresh from disk on each play call and decoded   incrementally.  Lower memory overhead for long music tracks.  # Bus routing  Every sound can be assigned to a named audio bus stored in a secondary slot-map. Volume and pitch for a playing source are the product of the per-source values and the values of its assigned bus.  Setting a bus to paused suspends every source on that bus.  # Master volume  A single master-volume scalar is applied on top of all per-source and per-bus volumes, letting game code provide a global volume slider without touching individual sources.  # Time tracking  Because rodio `Sink` does not expose a playback position, the mixer tracks play-start instants and accumulated pre-pause seconds manually so `luna.audio.getTime` can return the current position in the stream.

*Source: [src/audio/mixer.rs](src/audio/mixer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`PlayState`](src/audio/mixer.rs#L69) | Playback state of an audio source. Consult the module-level documentation for... |
| `enum` | [`SourceType`](src/audio/mixer.rs#L55) | Type of audio source. Consult the module-level documentation for the broader ... |
| `struct` | [`Mixer`](src/audio/mixer.rs#L203) | The `Mixer` is the single point of entry for all audio operations in Luna2D. ... |
| `struct` | [`QueueableSource`](src/audio/mixer.rs#L118) | A manually-fed streaming audio source that accepts raw f32 PCM data pushed bu... |

### `audio::sound_data`

> Decoded PCM audio sample buffer with per-sample read/write access.  `SoundData` stores fully decoded f32 PCM samples in interleaved channel order (for stereo: L, R, L, R, ...). It can be created as a silent buffer or decoded from an audio file via rodio. Lua code can read and write individual samples for procedural audio and DSP effects.

*Source: [src/audio/sound_data.rs](src/audio/sound_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SoundData`](src/audio/sound_data.rs#L24) | Decoded audio samples in f32 PCM format.  Stores interleaved samples (for ste... |

### `audio::source`

> Audio source type and playback state enums for the audio subsystem.  The primary audio logic lives in `mixer::Mixer`. This module re-exports the public enum types used by the Lua API and engine code.  This module is part of Luna2D's `audio` subsystem and provides the implementation details for source-related operations and data management. Key types exported from this module: `AudioSource`. Primary functions: `new()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/audio/source.rs](src/audio/source.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`AudioSource`](src/audio/source.rs#L53) | Handle for a loaded audio asset (legacy compatibility shim).  # Fields - `id`... |
| `struct` | [`SpatialState`](src/audio/source.rs#L24) | 3D spatial audio state for an audio source.  Used to compute panning relative... |

## `automation` {#automation}

### `automation::script`

> Script container for the automation simulation module.  This module provides the [`Script`] struct — a named, time-sorted, capacity-capped collection of [`Step`] objects. Scripts are stored in the [`Simulator`](super::Simulator) by name and selected for playback via [`Simulator::start`](super::Simulator::start).  The step cap of [`MAX_STEPS`] guards against unbounded memory allocation from large or adversarially constructed input scripts (CSF-010 allocation guard).

*Source: [src/automation/script.rs](src/automation/script.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Script`](src/automation/script.rs#L38) | A named simulation script containing an ordered sequence of timed steps.  On ... |

### `automation::simulator`

> Playback engine for the automation simulation module.  This module provides the [`Simulator`] struct and the private [`PlaybackState`] enum that drives it. The [`Simulator`] holds a named collection of [`Script`] objects and plays back the active script by injecting synthetic input events into the engine's [`EventQueue`] on each [`Simulator::update`] call.  ## Playback lifecycle  1. Load one or more scripts with [`Simulator::load`]. 2. Call [`Simulator::start`] to select a script and begin playback. 3. Call [`Simulator::update`] once per frame, passing the delta time. 4. The simulator advances its internal elapsed counter and dispatches all    steps whose `time <= elapsed`. Each step fires at most once. 5. When all steps are dispatched the state transitions to `Complete`.  Playback can be paused and resumed at any point. Stopping resets the elapsed counter and step index back to zero.

*Source: [src/automation/simulator.rs](src/automation/simulator.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Simulator`](src/automation/simulator.rs#L88) | Automated input simulation engine.  Holds a named registry of [`Script`] obje... |

### `automation::step`

> Step definitions for the automation simulation module.  This module provides the [`Action`] enum and [`Step`] struct that form the building blocks of a simulation script. A `Step` pairs a wall-clock offset (seconds from script start) with an action type and optional action-specific parameters such as key name, mouse coordinates, button index, and text.  Steps are created programmatically and collected into a [`Script`](super::Script) to be played back by the [`Simulator`](super::Simulator).

*Source: [src/automation/step.rs](src/automation/step.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Action`](src/automation/step.rs#L32) | The action type for a simulation step.  Each variant maps to a synthetic inpu... |
| `struct` | [`Step`](src/automation/step.rs#L165) | A single timed step in a simulation script.  Each step records a wall-clock o... |

## `battle` {#battle}

### `battle`

> Turn-based battle system: combatants, actions, damage types, teams, and turn order.  Exposed to Lua via `luna.battle.*`.  # Module Structure - [`types`]     — `DamageType`, `StatusEffect`, `CombatResult` - [`action`]    — `CombatAction` - [`combatant`] — `Combatant` - [`battle`]    — `CombatBattle`  # Integration with `stats`  `battle` and `stats` are both Tier 3 gameplay modules. Neither imports the other directly; they communicate through Lua.

*Source: [src/battle/mod.rs](src/battle/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`action`](src/battle/mod.rs#L19) | Turn-based actions and ability data. |
| `mod` | [`combatant`](src/battle/mod.rs#L21) | Turn-based battle participants. |
| `mod` | [`lifecycle`](src/battle/mod.rs#L23) | Turn-based battle lifecycle and turn order. |
| `mod` | [`types`](src/battle/mod.rs#L17) | Turn-based battle primitive types. |

### `battle::action`

> CombatAction: named attack or skill with cooldown, cost, and accuracy.  This module is part of Luna2D's `battle` subsystem and provides the implementation details for action-related operations and data management. Key types exported from this module: `CombatAction`. Primary functions: `new()`, `is_ready()`, `use_action()`, `tick_cooldown()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/battle/action.rs](src/battle/action.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CombatAction`](src/battle/action.rs#L31) | A turn-based combat action that a combatant can take.  Represents an attack, ... |

### `battle::combatant`

> Combatant: a single participant in a turn-based battle with HP, MP, statuses, and actions.  This module is part of Luna2D's `battle` subsystem and provides the implementation details for combatant-related operations and data management. Key types exported from this module: `Combatant`. Primary functions: `new()`, `is_alive()`, `take_damage()`, `heal()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/battle/combatant.rs](src/battle/combatant.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Combatant`](src/battle/combatant.rs#L39) | A participant in turn-based battle. Consult the module-level documentation fo... |

### `battle::lifecycle`

> CombatBattle: manages full turn-based battle lifecycle with turn ordering and action resolution.  This module is part of Luna2D's `battle` subsystem and provides the implementation details for battle-related operations and data management. Key types exported from this module: `CombatBattle`. Primary functions: `new()`, `add_combatant()`, `get_combatant()`, `get_combatant_mut()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/battle/lifecycle.rs](src/battle/lifecycle.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CombatBattle`](src/battle/lifecycle.rs#L32) | Manages a full turn-based battle with turn ordering and action resolution.  H... |

### `battle::types`

> Turn-based battle primitive types: damage kinds, status effects, and action results.  This module is part of Luna2D's `battle` subsystem and provides the implementation details for types-related operations and data management. Key types exported from this module: `DamageType`, `StatusEffect`, `CombatResult`. Primary functions: `from_str()`, `as_str()`, `new()`, `tick_turn()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/battle/types.rs](src/battle/types.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DamageType`](src/battle/types.rs#L27) | Kind of damage used for resistance lookups.  Matched against a combatant's `r... |
| `struct` | [`CombatResult`](src/battle/types.rs#L148) | Result of a single turn-based combat action.  Returned by `CombatBattle::reso... |
| `struct` | [`StatusEffect`](src/battle/types.rs#L87) | An active status effect on a combatant. Consult the module-level documentatio... |

## `cardgame` {#cardgame}

### `cardgame`

> Generic card-game-centric data structures.  Provides the building blocks for any system that manages a set of named, tagged, stat-carrying objects (cards) organised into stacks/piles.  # Key types  | Type | Purpose | |---|---| | [`Card`] | A named game object with stats, tags, counters, metadata | | [`CardTypeDef`] | Blueprint/template registered in the global type registry | | [`Stack`] | Ordered collection of cards (draw pile, hand, queue, …) | | [`DeckBuilder`] | Template-based stack construction with validation | | [`StackManager`] | Collection of named stacks; coordinates movement | | [`CardPool`] | Weighted random pool of card types | | [`Slot`] | Bounded single-position holder for cards | | [`StackHistory`] | Append-only change log | | [`CardGroup`] | Index-list for grouping analysis results |  # No gameplay assumptions  This module deliberately contains no player, turn, trick, rule, or scoring semantics.  All such logic belongs in Lua scripts that use these structures.

*Source: [src/cardgame/mod.rs](src/cardgame/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`builder`](src/cardgame/mod.rs#L31) | Builder sub-module. |
| `mod` | [`card`](src/cardgame/mod.rs#L27) | Card sub-module. |
| `mod` | [`group`](src/cardgame/mod.rs#L39) | Group sub-module. |
| `mod` | [`history`](src/cardgame/mod.rs#L41) | History sub-module. |
| `mod` | [`manager`](src/cardgame/mod.rs#L33) | Manager sub-module. |
| `mod` | [`pool`](src/cardgame/mod.rs#L35) | Pool sub-module. |
| `mod` | [`slot`](src/cardgame/mod.rs#L37) | Slot sub-module. |
| `mod` | [`stack`](src/cardgame/mod.rs#L29) | Stack sub-module. |

### `cardgame::builder`

> Stack builder — template-based stack construction with validation.  A `DeckBuilder` accumulates `BuildEntry` records (card type + count + optional overrides) and either builds a ready-to-use `Stack` or validates an existing stack against construction constraints.

*Source: [src/cardgame/builder.rs](src/cardgame/builder.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BuildEntry`](src/cardgame/builder.rs#L24) | One "slot" in a stack template: an card type plus a count and optional overri... |
| `struct` | [`DeckBuilder`](src/cardgame/builder.rs#L83) | Builds a `Stack` from `BuildEntry` templates and validates construction const... |

### `cardgame::card`

> Card type definitions, the global type registry, and card instances.  A `Card` is a named bundle of stats, tags, counters, and metadata. The engine never interprets these fields — all semantic meaning is user-defined in Lua. A card might represent a spell, creature, tile, token, equipment, or any other game object.

*Source: [src/cardgame/card.rs](src/cardgame/card.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`clear_card_types`](src/cardgame/card.rs#L117) | Remove all entries from the registry. After this call the container is in the... |
| `fn` | [`define_card_type`](src/cardgame/card.rs#L92) | Register (or overwrite) an card type in the thread-local type registry.  # Pa... |
| `fn` | [`get_card_type`](src/cardgame/card.rs#L104) | Look up an card type definition by name. Returns `None` if not found.  # Para... |
| `fn` | [`get_card_type_names`](src/cardgame/card.rs#L112) | Return all registered type names. This accessor incurs no allocation; call it... |
| `struct` | [`Card`](src/cardgame/card.rs#L151) | A single card instance. Consult the module-level documentation for the broade... |
| `struct` | [`CardTypeDef`](src/cardgame/card.rs#L37) | Template that describes a class of items (the "blueprint").  Stats, tags, and... |

### `cardgame::group`

> Group analysis — stat-based and tag-based grouping of item slices.  All functions operate on plain `&[Card]` slices and return index-based groups so that the caller can decide what to do with them without taking ownership.  These utilities are domain-agnostic: "n-of-a-stat" works for poker hands, board game scoring, loot classification, or any other scenario.

*Source: [src/cardgame/group.rs](src/cardgame/group.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`find_at_least_n_of_stat`](src/cardgame/group.rs#L152) | Find all groups where at least `n` items share the same integer stat value.  ... |
| `fn` | [`find_n_of_stat`](src/cardgame/group.rs#L131) | Find all maximal groups where items share the exact same integer stat value, ... |
| `fn` | [`find_sequences`](src/cardgame/group.rs#L176) | Find all runs (sequences of consecutive integer stat values) of length ≥ `min... |
| `fn` | [`find_tag_groups`](src/cardgame/group.rs#L224) | Find groups of items that all share the same tag-prefix value and contain at ... |
| `fn` | [`group_by_category`](src/cardgame/group.rs#L79) | Group item indices by category. Consult the module-level documentation for th... |
| `fn` | [`group_by_stat`](src/cardgame/group.rs#L62) | Group item indices by the integer part of a named stat.  Returns a map from `... |
| `fn` | [`group_by_tag_prefix`](src/cardgame/group.rs#L100) | Group item indices by a tag prefix (the part before the first `:`).  Tags mat... |
| `fn` | [`sorted_indices_by_category`](src/cardgame/group.rs#L274) | Return sorted indices grouped alphabetically by category.  # Parameters - `it... |
| `fn` | [`sorted_indices_by_stat`](src/cardgame/group.rs#L251) | Return a sorted list of 0-based indices; does not modify the slice.  `ascendi... |
| `struct` | [`CardGroup`](src/cardgame/group.rs#L26) | A labelled subset of an item slice, referenced by index.  Indices refer to po... |

### `cardgame::history`

> Change history — append-only log of stack mutations.  `StackHistory` is kept separate from `Stack` so that the caller decides what (if anything) to track.  Wrap it in an `Rc<RefCell<StackHistory>>` and pass it around alongside stacks.

*Source: [src/cardgame/history.rs](src/cardgame/history.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`HistoryAction`](src/cardgame/history.rs#L28) | The category of change that was recorded.  # Variants - `An` — An variant. - ... |
| `struct` | [`HistoryEntry`](src/cardgame/history.rs#L70) | A single entry in the history log. Consult the module-level documentation for... |
| `struct` | [`StackHistory`](src/cardgame/history.rs#L92) | Append-only change log with an optional rolling size limit.  # Fields - `entr... |

### `cardgame::manager`

> Stack manager — organises multiple named `Stack` instances as a unit.  Useful for modelling a board where several stacks (draw pile, hand, discard, play area, etc.) coexist and items need to move between them.

*Source: [src/cardgame/manager.rs](src/cardgame/manager.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`StackManager`](src/cardgame/manager.rs#L17) | Manages a collection of named `Stack` instances.  All stack names and their s... |

### `cardgame::pool`

> Weighted item-type pool for random selection.  An `CardPool` holds a set of card type names each with a relative draw weight.  Useful for loot tables, draft formats, booster generation, or any weighted-random-choice scenario.

*Source: [src/cardgame/pool.rs](src/cardgame/pool.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CardPool`](src/cardgame/pool.rs#L29) | A pool of card types for weighted random draws.  # Fields - `name` — `String`... |
| `struct` | [`PoolEntry`](src/cardgame/pool.rs#L15) | A single entry in an item pool. Consult the module-level documentation for th... |

### `cardgame::slot`

> Slot — a bounded named position that holds a small set of items.  A slot is like a named cell on a board: it has an optional capacity limit and enforces it on push.  Useful for equipment slots, board positions, hand slots, or any place where "there can only be N items here" matters.

*Source: [src/cardgame/slot.rs](src/cardgame/slot.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Slot`](src/cardgame/slot.rs#L18) | A bounded named position holding zero or more items.  Semantics (what "this s... |

### `cardgame::stack`

> Ordered card collection — a stack, pile, hand, zone, or any linear sequence of cards.  The last element is the "top" and the first element is the "bottom". All semantics (draw pile, discard pile, hand, battlefield, graveyard) are user-defined.

*Source: [src/cardgame/stack.rs](src/cardgame/stack.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Stack`](src/cardgame/stack.rs#L20) | An ordered collection of cards. Consult the module-level documentation for th... |

## `combat` {#combat}

### `combat`

> Vehicle combat system: chassis, turrets, weapons, projectiles, and collision groups.  Exposed to Lua via `luna.combat.*`.  # Module Structure - [`collision_groups`] — `CollisionGroupSet` - [`chassis`]          — `Chassis`, `MountSlot`, `ArmorZone` - [`weapon`]           — `Turret`, `Weapon`, `ProjectileType` - [`projectile`]       — `Projectile`, `ProjectilePool` - [`world`]            — `CombatWorld`

*Source: [src/combat/mod.rs](src/combat/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`chassis`](src/combat/mod.rs#L15) | Vehicle bodies, armor zones, and mount slots. |
| `mod` | [`collision_groups`](src/combat/mod.rs#L13) | Named collision groups over physics category bits. |
| `mod` | [`projectile`](src/combat/mod.rs#L19) | Projectile instances and fixed-size projectile pools. |
| `mod` | [`weapon`](src/combat/mod.rs#L17) | Turrets, weapons, and projectile type metadata. |
| `mod` | [`world`](src/combat/mod.rs#L21) | Top-level vehicle combat world coordinator. |

### `combat::chassis`

> Vehicle chassis with mount slots and armor zones.  A [`Chassis`] represents a vehicle body in the combat system. It has health, directional armor, and named mount slots where turrets or weapons attach.

*Source: [src/combat/chassis.rs](src/combat/chassis.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ArmorZone`](src/combat/chassis.rs#L41) | Armor zone damage multiplier. Consult the module-level documentation for the ... |
| `struct` | [`Chassis`](src/combat/chassis.rs#L63) | A vehicle chassis with health, armor, and mount slots.  # Fields - `body_id` ... |
| `struct` | [`MountSlot`](src/combat/chassis.rs#L19) | A slot on the chassis where turrets or weapons attach.  # Fields - `id` — `St... |

### `combat::collision_groups`

> Named collision group manager over physics category bits.  Maps human-readable group names (e.g. `"player"`, `"enemy"`, `"projectile"`) to 16-bit category bitmasks and tracks which groups should collide.

*Source: [src/combat/collision_groups.rs](src/combat/collision_groups.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CollisionGroupSet`](src/combat/collision_groups.rs#L12) | Maps named collision groups to 16-bit category bitmasks.  # Fields - `groups`... |

### `combat::projectile`

> Projectile and ProjectilePool for efficient projectile management.  Projectiles are pre-allocated in pools to avoid per-frame heap allocation. Each pool manages a fixed set of projectile slots that can be spawned and released as needed.

*Source: [src/combat/projectile.rs](src/combat/projectile.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `const` | [`MAX_POOL_SIZE`](src/combat/projectile.rs#L10) | Maximum number of projectiles in a single pool. |
| `struct` | [`Projectile`](src/combat/projectile.rs#L30) | A single in-flight projectile. Consult the module-level documentation for the... |
| `struct` | [`ProjectilePool`](src/combat/projectile.rs#L126) | Pre-allocated pool of projectiles for efficient spawn/release cycling.  # Fie... |

### `combat::weapon`

> Turret and Weapon types for fire-rate-based combat.  A [`Turret`] is a rotatable mount attached to a chassis slot via a physics revolute joint. A [`Weapon`] handles fire rate, ammo, bursts, and damage parameters.

*Source: [src/combat/weapon.rs](src/combat/weapon.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ProjectileType`](src/combat/weapon.rs#L19) | The type of projectile a weapon fires. Consult the module-level documentation... |
| `struct` | [`Turret`](src/combat/weapon.rs#L46) | A rotatable weapon mount attached to a chassis slot.  # Fields - `body_id` — ... |
| `struct` | [`Weapon`](src/combat/weapon.rs#L188) | A weapon that handles fire rate, ammo, burst, and damage.  # Fields - `name` ... |

### `combat::world`

> CombatWorld: top-level coordinator for the vehicle/weapon/projectile combat system.  Manages all combat entities: chassis, turrets, weapons, and projectile pools. The Lua layer coordinates between `CombatWorld` and the physics `World`.

*Source: [src/combat/world.rs](src/combat/world.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CombatWorld`](src/combat/world.rs#L20) | Manages all combat entities: chassis, turrets, weapons, and projectile pools.... |

## `compute` {#compute}

### `compute`

> Dense N-dimensional numerical arrays with NumPy-style operations.  Provides `NdArray` for 1D/2D/3D row-major arrays with float32, float64, and int32 element types. Used by the `luna.compute` Lua module.

*Source: [src/compute/mod.rs](src/compute/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`array`](src/compute/mod.rs#L7) | N-dimensional array storage with typed element access. |
| `mod` | [`ops`](src/compute/mod.rs#L9) | Element-wise and reduction operations on NdArray. |
| `mod` | [`spatial`](src/compute/mod.rs#L11) | Spatial operations: convolution, pooling, distance transforms. |

### `compute::array`

> Core NdArray type and DataType enum.  This module is part of Luna2D's `compute` subsystem and provides the implementation details for array-related operations and data management. Key types exported from this module: `DataType`, `NdArray`. Primary functions: `parse()`, `byte_size()`, `name()`, `new()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/compute/array.rs](src/compute/array.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DataType`](src/compute/array.rs#L21) | Element data type for an NdArray. Consult the module-level documentation for ... |
| `struct` | [`NdArray`](src/compute/array.rs#L85) | Dense N-dimensional numerical array with row-major strides.  Data is stored a... |

### `compute::ops`

> Element-wise, reduction, comparison, masking, shape, and bitwise operations on NdArray.  This module is part of Luna2D's `compute` subsystem and provides the implementation details for ops-related operations and data management. Primary functions: `add()`, `add_scalar()`, `sub()`, `sub_scalar()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/compute/ops.rs](src/compute/ops.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`abs`](src/compute/ops.rs#L224) | Element-wise absolute value. Consult the module-level documentation for the b... |
| `fn` | [`add`](src/compute/ops.rs#L102) | Element-wise addition of two arrays (same shape and dtype).  # Parameters - `... |
| `fn` | [`add_scalar`](src/compute/ops.rs#L114) | Add a scalar to every element. The insertion is O(1) amortised unless a resiz... |
| `fn` | [`all`](src/compute/ops.rs#L642) | Returns `true` if all elements are non-zero.  # Parameters - `a` — `&NdArray`... |
| `fn` | [`any`](src/compute/ops.rs#L626) | Returns `true` if any element is non-zero.  # Parameters - `a` — `&NdArray`. ... |
| `fn` | [`argmax`](src/compute/ops.rs#L606) | Return the flat index of the maximum element (0-based).  # Parameters - `a` —... |
| `fn` | [`argmin`](src/compute/ops.rs#L586) | Return the flat index of the minimum element (0-based).  # Parameters - `a` —... |
| `fn` | [`bitwise_and`](src/compute/ops.rs#L972) | Bitwise AND of two Int32 arrays. Consult the module-level documentation for t... |
| `fn` | [`bitwise_lshift`](src/compute/ops.rs#L1045) | Bitwise left shift of an Int32 array by `amount` bits.  # Parameters - `a` — ... |
| `fn` | [`bitwise_not`](src/compute/ops.rs#L1028) | Bitwise NOT of an Int32 array. Consult the module-level documentation for the... |
| `fn` | [`bitwise_or`](src/compute/ops.rs#L991) | Bitwise OR of two Int32 arrays. Consult the module-level documentation for th... |
| `fn` | [`bitwise_rshift`](src/compute/ops.rs#L1062) | Bitwise right shift (arithmetic) of an Int32 array by `amount` bits.  # Param... |
| `fn` | [`bitwise_xor`](src/compute/ops.rs#L1010) | Bitwise XOR of two Int32 arrays. Consult the module-level documentation for t... |
| `fn` | [`clamp`](src/compute/ops.rs#L248) | Clamp every element to `[min_val, max_val]`.  # Parameters - `a` — `&NdArray`... |
| `fn` | [`clone_array`](src/compute/ops.rs#L956) | Clone an array (convenience wrapper). Consult the module-level documentation ... |
| `fn` | [`count_nonzero`](src/compute/ops.rs#L569) | Count the number of non-zero elements. Runs in O(1) time.  # Parameters - `a`... |
| `fn` | [`div`](src/compute/ops.rs#L174) | Element-wise division of two arrays (same shape and dtype).  # Parameters - `... |
| `fn` | [`div_scalar`](src/compute/ops.rs#L186) | Divide every element by a scalar. Consult the module-level documentation for ... |
| `fn` | [`eq`](src/compute/ops.rs#L268) | Element-wise equality comparison of two arrays. Returns Float32 with 0/1.  # ... |
| `fn` | [`eq_scalar`](src/compute/ops.rs#L290) | Element-wise equality comparison against a scalar. Returns Float32.  # Parame... |
| `fn` | [`fill`](src/compute/ops.rs#L943) | Fill all elements of an array with a value (in-place).  # Parameters - `a` — ... |
| `fn` | [`gt`](src/compute/ops.rs#L354) | Element-wise greater-than comparison of two arrays. Returns Float32.  # Param... |
| `fn` | [`gt_scalar`](src/compute/ops.rs#L378) | Element-wise greater-than comparison against a scalar. Returns Float32.  # Pa... |
| `fn` | [`gte`](src/compute/ops.rs#L434) | Element-wise greater-than-or-equal comparison of two arrays. Returns Float32.... |
| `fn` | [`gte_scalar`](src/compute/ops.rs#L458) | Element-wise greater-than-or-equal comparison against a scalar. Returns Float... |
| `fn` | [`lt`](src/compute/ops.rs#L394) | Element-wise less-than comparison of two arrays. Returns Float32.  # Paramete... |
| `fn` | [`lt_scalar`](src/compute/ops.rs#L418) | Element-wise less-than comparison against a scalar. Returns Float32.  # Param... |
| `fn` | [`lte`](src/compute/ops.rs#L474) | Element-wise less-than-or-equal comparison of two arrays. Returns Float32.  #... |
| `fn` | [`lte_scalar`](src/compute/ops.rs#L498) | Element-wise less-than-or-equal comparison against a scalar. Returns Float32.... |
| `fn` | [`max_axis`](src/compute/ops.rs#L850) | Maximum along a given axis. Consult the module-level documentation for the br... |
| `fn` | [`max_val`](src/compute/ops.rs#L706) | Maximum value across all elements. Consult the module-level documentation for... |
| `fn` | [`mean`](src/compute/ops.rs#L677) | Mean of all elements. Consult the module-level documentation for the broader ... |
| `fn` | [`mean_axis`](src/compute/ops.rs#L796) | Mean along a given axis. Consult the module-level documentation for the broad... |
| `fn` | [`min_axis`](src/compute/ops.rs#L814) | Minimum along a given axis. Consult the module-level documentation for the br... |
| `fn` | [`min_val`](src/compute/ops.rs#L688) | Minimum value across all elements. Consult the module-level documentation for... |
| `fn` | [`mul`](src/compute/ops.rs#L150) | Element-wise multiplication of two arrays (same shape and dtype).  # Paramete... |
| `fn` | [`mul_scalar`](src/compute/ops.rs#L162) | Multiply every element by a scalar. Consult the module-level documentation fo... |
| `fn` | [`neg`](src/compute/ops.rs#L235) | Element-wise negation. Consult the module-level documentation for the broader... |
| `fn` | [`neq`](src/compute/ops.rs#L311) | Element-wise not-equal comparison of two arrays. Returns Float32.  # Paramete... |
| `fn` | [`neq_scalar`](src/compute/ops.rs#L333) | Element-wise not-equal comparison against a scalar. Returns Float32.  # Param... |
| `fn` | [`pow_scalar`](src/compute/ops.rs#L198) | Raise every element to a scalar exponent.  # Parameters - `a` — `&NdArray`. -... |
| `fn` | [`reshape`](src/compute/ops.rs#L892) | Reshape an array to a new shape with the same total element count.  # Paramet... |
| `fn` | [`sqrt`](src/compute/ops.rs#L213) | Element-wise square root. Consult the module-level documentation for the broa... |
| `fn` | [`sub`](src/compute/ops.rs#L126) | Element-wise subtraction of two arrays (same shape and dtype).  # Parameters ... |
| `fn` | [`sub_scalar`](src/compute/ops.rs#L138) | Subtract a scalar from every element. Consult the module-level documentation ... |
| `fn` | [`sum`](src/compute/ops.rs#L662) | Sum of all elements. Consult the module-level documentation for the broader u... |
| `fn` | [`sum_axis`](src/compute/ops.rs#L764) | Sum along a given axis, producing an array with that axis removed.  # Paramet... |
| `fn` | [`threshold`](src/compute/ops.rs#L518) | Threshold mask: returns Float32 array with 1.0 where `a >= val`, 0.0 otherwis... |
| `fn` | [`transpose_2d`](src/compute/ops.rs#L918) | Transpose a 2D array (swap rows and columns).  # Parameters - `a` — `&NdArray... |
| `fn` | [`where_mask`](src/compute/ops.rs#L534) | Conditional selection: where `cond != 0`, choose from `a`; otherwise from `b`... |

### `compute::spatial`

> 2D spatial operations and linear algebra for NdArray.  This module is part of Luna2D's `compute` subsystem and provides the implementation details for spatial-related operations and data management. Primary functions: `convolve2d()`, `dilate()`, `erode()`, `flood_fill()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/compute/spatial.rs](src/compute/spatial.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`convolve2d`](src/compute/spatial.rs#L25) | 2D convolution with zero-padding (same-size output).  # Parameters - `input` ... |
| `fn` | [`dilate`](src/compute/spatial.rs#L81) | Morphological dilation with a Manhattan-diamond structuring element.  # Param... |
| `fn` | [`dot`](src/compute/spatial.rs#L373) | Dot product of two 1D arrays (same length). Returns a scalar.  # Parameters -... |
| `fn` | [`erode`](src/compute/spatial.rs#L128) | Morphological erosion with a Manhattan-diamond structuring element.  # Parame... |
| `fn` | [`flood_fill`](src/compute/spatial.rs#L180) | Flood fill using BFS with 4-connectivity.  # Parameters - `a` — `&NdArray`. -... |
| `fn` | [`get_region`](src/compute/spatial.rs#L238) | Extract a rectangular sub-region from a 2D array.  # Parameters - `a` — `&NdA... |
| `fn` | [`matmul`](src/compute/spatial.rs#L322) | Matrix multiplication of two 2D arrays: (m,k) × (k,n) → (m,n).  # Parameters ... |
| `fn` | [`set_region`](src/compute/spatial.rs#L282) | Copy a source 2D array into a target 2D array at position `(row, col)`.  # Pa... |

## `crafting` {#crafting}

### `crafting`

> Mod implementation for the `crafting` subsystem.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.  Crafting system: recipes, ingredients, stations, skill progression, and craft queues.  Exposed to Lua via `luna.crafting.*`.  # Module layout  | File | Contents | |------|----------| | `quality.rs` | [`Quality`] tier enum | | `ingredient.rs` | [`Ingredient`], [`RecipeOutput`] | | `recipe.rs` | [`Recipe`], [`RecipeRegistry`] | | `station.rs` | [`Station`] | | `skill.rs` | [`CraftSkill`], [`PerkNode`] | | `queue.rs` | [`CraftJob`], [`CraftQueue`] | | `upgrade.rs` | [`UpgradeNode`], [`UpgradeTree`] | | `knowledge.rs` | [`RecipeKnowledge`], [`RecipeGroup`] | | `modifier_pool.rs` | [`ModifierPool`], [`ModifierEntry`] |

*Source: [src/crafting/mod.rs](src/crafting/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`ingredient`](src/crafting/mod.rs#L28) | Ingredient and recipe output types. |
| `mod` | [`knowledge`](src/crafting/mod.rs#L30) | Recipe discovery and grouping types. |
| `mod` | [`modifier_pool`](src/crafting/mod.rs#L32) | Weighted modifier pools for random rolls. |
| `mod` | [`quality`](src/crafting/mod.rs#L34) | Crafted item quality tiers. |
| `mod` | [`queue`](src/crafting/mod.rs#L36) | Timed crafting job queue types. |
| `mod` | [`recipe`](src/crafting/mod.rs#L38) | Recipe definitions and registry types. |
| `mod` | [`skill`](src/crafting/mod.rs#L40) | Crafting skill, perks, and specialization types. |
| `mod` | [`station`](src/crafting/mod.rs#L42) | Crafting station requirements and state. |
| `mod` | [`upgrade`](src/crafting/mod.rs#L44) | Upgrade tree types for item progression. |

### `crafting::ingredient`

> Recipe ingredients and output definitions.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for ingredient-related operations and data management. Key types exported from this module: `Ingredient`, `RecipeOutput`. Primary functions: `new()`, `new_tag()`, `is_tag()`, `new()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/ingredient.rs](src/crafting/ingredient.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Ingredient`](src/crafting/ingredient.rs#L21) | An ingredient required by a recipe. Consult the module-level documentation fo... |
| `struct` | [`RecipeOutput`](src/crafting/ingredient.rs#L83) | Output produced by a recipe. Consult the module-level documentation for the b... |

### `crafting::knowledge`

> Recipe knowledge tracking and UI grouping for crafting.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for knowledge-related operations and data management. Key types exported from this module: `RecipeKnowledge`, `RecipeGroup`. Primary functions: `new()`, `discover()`, `forget()`, `is_known()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/knowledge.rs](src/crafting/knowledge.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`RecipeGroup`](src/crafting/knowledge.rs#L190) | Named grouping of recipes for UI organisation and bulk operations.  # Fields ... |
| `struct` | [`RecipeKnowledge`](src/crafting/knowledge.rs#L21) | Tracks which recipes a player has discovered or unlocked.  # Fields - `known`... |

### `crafting::modifier_pool`

> Weighted modifier pools for random item property rolls (Path of Exile / Minecraft Enchanting pattern).  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for modifier pool-related operations and data management. Key types exported from this module: `ModifierEntry`, `ModifierPool`. Primary functions: `new()`, `new()`, `add_modifier()`, `remove_modifier()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/modifier_pool.rs](src/crafting/modifier_pool.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ModifierEntry`](src/crafting/modifier_pool.rs#L20) | A single weighted entry in a modifier pool.  # Fields - `name`: Stable modifi... |
| `struct` | [`ModifierPool`](src/crafting/modifier_pool.rs#L49) | A named pool of weighted modifiers that can be rolled to produce random item ... |

### `crafting::quality`

> Quality tier enum for crafted item outputs.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for quality-related operations and data management. Key types exported from this module: `Quality`. Primary functions: `from_str()`, `as_str()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/quality.rs](src/crafting/quality.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Quality`](src/crafting/quality.rs#L21) | Output quality tiers for crafted items. Consult the module-level documentatio... |

### `crafting::queue`

> Craft job queue for timed crafting operations.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for queue-related operations and data management. Key types exported from this module: `CraftJob`, `CraftQueue`. Primary functions: `new()`, `advance()`, `percent()`, `new()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/queue.rs](src/crafting/queue.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CraftJob`](src/crafting/queue.rs#L23) | A single in-progress or queued crafting job.  # Fields - `id`: Stable queue-l... |
| `struct` | [`CraftQueue`](src/crafting/queue.rs#L95) | Queue that holds and ticks active craft jobs.  # Fields - `jobs` — `Vec<Craft... |

### `crafting::recipe`

> Recipe definition and the central recipe registry.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for recipe-related operations and data management. Key types exported from this module: `Recipe`, `RecipeRegistry`. Primary functions: `new()`, `add_ingredient()`, `add_output()`, `clear_ingredients()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/recipe.rs](src/crafting/recipe.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Recipe`](src/crafting/recipe.rs#L28) | Defines how inputs are combined to produce outputs.  # Fields - `id`, `name`,... |
| `struct` | [`RecipeRegistry`](src/crafting/recipe.rs#L247) | Central registry for all known recipes. Consult the module-level documentatio... |

### `crafting::skill`

> Player crafting skill with XP, level progression, specializations, and perk tree.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for skill-related operations and data management. Key types exported from this module: `PerkNode`, `CraftSkill`. Primary functions: `new()`, `new()`, `add_xp()`, `get_xp_to_next()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/skill.rs](src/crafting/skill.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CraftSkill`](src/crafting/skill.rs#L53) | Player crafting skill in a profession, with XP tracking and optional perk/spe... |
| `struct` | [`PerkNode`](src/crafting/skill.rs#L22) | A single node in the perk tree (Skyrim pattern).  # Fields - `perk_id`: Stabl... |

### `crafting::station`

> Crafting station: type, level, proximity, modules, attachments, and fuel.  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for station-related operations and data management. Key types exported from this module: `Station`. Primary functions: `new()`, `can_process()`, `effective_time()`, `effective_level()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/station.rs](src/crafting/station.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Station`](src/crafting/station.rs#L25) | A crafting station that filters and processes recipes.  # Fields - `station_t... |

### `crafting::upgrade`

> Upgrade tree: directed acyclic graph for weapon/item progression (Monster Hunter pattern).  This module is part of Luna2D's `crafting` subsystem and provides the implementation details for upgrade-related operations and data management. Key types exported from this module: `UpgradeNode`, `UpgradeTree`. Primary functions: `new()`, `new()`, `add_node()`, `add_edge()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/crafting/upgrade.rs](src/crafting/upgrade.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`UpgradeNode`](src/crafting/upgrade.rs#L24) | A single node in an upgrade tree. Consult the module-level documentation for ... |
| `struct` | [`UpgradeTree`](src/crafting/upgrade.rs#L73) | Directed acyclic graph of upgrades for weapons or equipment.  # Fields - `nam... |

## `data` {#data}

### `data`

> Binary data manipulation, compression, hashing, encoding, and pack/unpack.  Provides `ByteData` for raw byte buffers, plus compression (deflate/gzip/lz4/zlib), cryptographic hashing (MD5/SHA family), encoding (base64/hex), TOML parsing, binary pack/unpack (`pack` module), and a windowed byte-buffer view (`dataview` module).

*Source: [src/data/mod.rs](src/data/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`byte_data`](src/data/mod.rs#L8) | Raw byte buffer for binary data manipulation. |
| `mod` | [`compress`](src/data/mod.rs#L10) | Deflate, gzip, lz4, and zlib compression/decompression. |
| `mod` | [`dataview`](src/data/mod.rs#L12) | Windowed read-only byte-buffer view. |
| `mod` | [`encode`](src/data/mod.rs#L14) | Base64 and hex encoding/decoding. |
| `mod` | [`hash`](src/data/mod.rs#L16) | Cryptographic hash functions (MD5, SHA-1, SHA-256, SHA-512). |
| `mod` | [`pack`](src/data/mod.rs#L18) | Binary pack/unpack utilities compatible with the LÖVE2D `data.pack` API. |
| `mod` | [`toml_convert`](src/data/mod.rs#L20) | TOML parsing and encoding. |

### `data::byte_data`

> Contiguous byte buffer accessible from Lua.  This module is part of Luna2D's `data` subsystem and provides the implementation details for byte data-related operations and data management. Key types exported from this module: `ByteData`. Primary functions: `new()`, `from_bytes()`, `from_string()`, `len()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/data/byte_data.rs](src/data/byte_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ByteData`](src/data/byte_data.rs#L20) | Contiguous byte buffer for binary data manipulation.  Wraps a `Vec<u8>` with ... |

### `data::compress`

> Data compression and decompression using deflate, gzip, zlib, and LZ4.  This module is part of Luna2D's `data` subsystem and provides the implementation details for compress-related operations and data management. Key types exported from this module: `CompressFormat`. Primary functions: `parse_str()`, `compress()`, `decompress()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/data/compress.rs](src/data/compress.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CompressFormat`](src/data/compress.rs#L21) | Supported compression formats. Consult the module-level documentation for the... |
| `fn` | [`compress`](src/data/compress.rs#L63) | Compress data using the specified format and compression level (0-9).  # Para... |
| `fn` | [`decompress`](src/data/compress.rs#L107) | Decompress data using the specified format.  # Parameters - `data` — `&[u8]`.... |

### `data::dataview`

> Read-only windowed view into a shared byte buffer.  `DataView` provides typed accessor methods over a slice of a `Vec<u8>` without copying the underlying data. All reads are little-endian. Bounds are checked on every access; out-of-range indices return an error.

*Source: [src/data/dataview.rs](src/data/dataview.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DataView`](src/data/dataview.rs#L18) | A windowed, read-only view into a shared byte buffer.  Uses `Arc<Vec<u8>>` fo... |

### `data::encode`

> Base64 and hex encoding/decoding for data serialization.  This module is part of Luna2D's `data` subsystem and provides the implementation details for encode-related operations and data management. Key types exported from this module: `EncodeFormat`. Primary functions: `parse_str()`, `encode()`, `decode()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/data/encode.rs](src/data/encode.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`EncodeFormat`](src/data/encode.rs#L19) | Supported encoding formats. Consult the module-level documentation for the br... |
| `fn` | [`decode`](src/data/encode.rs#L69) | Decode a string back into bytes using the specified format.  # Parameters - `... |
| `fn` | [`encode`](src/data/encode.rs#L54) | Encode bytes into a string using the specified format.  # Parameters - `forma... |

### `data::hash`

> Cryptographic hash functions for data integrity verification.  This module is part of Luna2D's `data` subsystem and provides the implementation details for hash-related operations and data management. Key types exported from this module: `HashAlgorithm`. Primary functions: `parse_str()`, `hash()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/data/hash.rs](src/data/hash.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`HashAlgorithm`](src/data/hash.rs#L22) | Supported hash algorithms. Consult the module-level documentation for the bro... |
| `fn` | [`hash`](src/data/hash.rs#L63) | Compute the hash of data using the specified algorithm, returned as a hex str... |

### `data::pack`

> Binary pack/unpack utilities compatible with LÖVE2D's `data.pack` API.  Provides format-string based binary serialization for the `luna.data` module. Supports little-endian and big-endian byte order via `<` and `>` prefixes.

*Source: [src/data/pack.rs](src/data/pack.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`PackValue`](src/data/pack.rs#L18) | A Rust-side value that can be packed into or unpacked from a binary buffer.  ... |
| `fn` | [`get_packed_size`](src/data/pack.rs#L361) | Computes the total byte size that `pack` would produce for the given format a... |
| `fn` | [`pack`](src/data/pack.rs#L61) | Packs values according to a format string into a `ByteData` buffer.  Format c... |
| `fn` | [`unpack`](src/data/pack.rs#L205) | Unpacks values from a byte buffer according to a format string.  Returns all ... |

### `data::toml_convert`

> TOML parsing and encoding for Luna2D.  Converts between TOML strings and Lua tables. Supports the full TOML spec via the `toml` crate, mapping types to their Lua equivalents.  This module is part of Luna2D's `data` subsystem and provides the implementation details for toml convert-related operations and data management. Primary functions: `parse_toml()`, `encode_toml()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/data/toml_convert.rs](src/data/toml_convert.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`encode_toml`](src/data/toml_convert.rs#L33) | Encode a `toml::Value` into a TOML string.  # Parameters - `value` — `&toml::... |
| `fn` | [`parse_toml`](src/data/toml_convert.rs#L20) | Parse a TOML string into a `toml::Value`.  # Parameters - `input` — `&str`.  ... |

## `dataframe` {#dataframe}

### `dataframe`

> In-memory column-major tabular data with query, analytics, and SQL.  Provides `DataFrame` for named-column data tables and `Database` for multi-table catalogs. Used by the `luna.dataframe` Lua module.

*Source: [src/dataframe/mod.rs](src/dataframe/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`frame`](src/dataframe/mod.rs#L7) | Column-major DataFrame and Database types. |
| `mod` | [`query`](src/dataframe/mod.rs#L9) | Filter, sort, group-by, and aggregate query engine. |
| `mod` | [`serial`](src/dataframe/mod.rs#L11) | CSV and JSON serialization/deserialization for DataFrames. |
| `mod` | [`sql`](src/dataframe/mod.rs#L13) | SQL query parser and executor for in-memory databases. |

### `dataframe::frame`

> Core DataFrame and Database types with CellValue cells.  This module is part of Luna2D's `dataframe` subsystem and provides the implementation details for frame-related operations and data management. Key types exported from this module: `CellValue`, `ColRef`, `DataFrame`, `Database`. Primary functions: `is_nil()`, `as_number()`, `as_text()`, `as_bool()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/dataframe/frame.rs](src/dataframe/frame.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CellValue`](src/dataframe/frame.rs#L22) | A single cell value in a DataFrame column.  # Variants - `Nil` — Nil variant.... |
| `enum` | [`ColRef`](src/dataframe/frame.rs#L128) | Column reference: string name or 1-based integer index.  # Variants - `Name` ... |
| `struct` | [`DataFrame`](src/dataframe/frame.rs#L144) | In-memory column-major tabular data. Consult the module-level documentation f... |
| `struct` | [`Database`](src/dataframe/frame.rs#L153) | Named catalog of DataFrames. Consult the module-level documentation for the b... |

### `dataframe::serial`

> CSV, JSON, and LVDF binary serialization for DataFrame.  This module is part of Luna2D's `dataframe` subsystem and provides the implementation details for serial-related operations and data management. Primary functions: `from_csv()`, `to_csv()`, `from_json()`, `to_json()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/dataframe/serial.rs](src/dataframe/serial.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`from_binary`](src/dataframe/serial.rs#L651) | Deserialize a DataFrame from LVDF binary format.  # Parameters - `data` — `&[... |
| `fn` | [`from_csv`](src/dataframe/serial.rs#L26) | Parse a CSV string into a DataFrame. Returns a fully initialised instance wit... |
| `fn` | [`from_json`](src/dataframe/serial.rs#L203) | Parse JSON (array-of-objects) into a DataFrame.  # Parameters - `s` — `&str`.... |

### `dataframe::sql`

> SQL-like query parser and executor for DataFrame.  Supports a subset of SQL: SELECT, WHERE, GROUP BY, HAVING, ORDER BY, LIMIT/OFFSET, aggregate functions, and JOIN (for Database queries).  This module is part of Luna2D's `dataframe` subsystem and provides the implementation details for sql-related operations and data management. Primary functions: `query_sql()`, `query_sql_database()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/dataframe/sql.rs](src/dataframe/sql.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`query_sql`](src/dataframe/sql.rs#L670) | Execute a SQL query on a single DataFrame.  # Parameters - `df` — `&DataFrame... |
| `fn` | [`query_sql_database`](src/dataframe/sql.rs#L685) | Execute a SQL query on a Database (supports FROM and JOIN).  # Parameters - `... |

## `dialog` {#dialog}

### `dialog`

> Dialog sequencer for visual novel-style text presentation.  Provides a state-machine-based dialog sequencer with typewriter effect, branching choices, timed pauses, and inline callbacks. Drives text-based narrative sequences from flat script arrays.

*Source: [src/dialog/mod.rs](src/dialog/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DialogNode`](src/dialog/mod.rs#L17) | Dialog node types in a script. Consult the module-level documentation for the... |
| `enum` | [`SequencerState`](src/dialog/mod.rs#L60) | Sequencer playback state. Consult the module-level documentation for the broa... |
| `struct` | [`ChoiceOption`](src/dialog/mod.rs#L37) | A single choice option with a label and branch nodes.  # Fields - `label` — `... |
| `struct` | [`Sequencer`](src/dialog/mod.rs#L105) | Dialog sequencer with typewriter effect and branching choices.  Drives a flat... |

## `economy` {#economy}

### `economy`

> Named resource economy system: capacity, flow rates, decay, interest, upkeep, overflow policies, reservations, and conversions (Tier 3).  Designed for RTS, management, survival, and RPG economy patterns where the game tracks multiple named numeric values (gold, wood, mana, food) that change over time with configurable rates.

*Source: [src/economy/mod.rs](src/economy/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`manager`](src/economy/mod.rs#L19) | ResourceManager: multi-resource economy coordinator. |
| `mod` | [`modifier`](src/economy/mod.rs#L15) | Resource modifiers and conversion rules. |
| `mod` | [`resource`](src/economy/mod.rs#L11) | Resource definition and overflow policy. |

### `economy::manager`

> ResourceManager: multi-resource economy coordinator.  This module is part of Luna2D's `resource` subsystem and provides the implementation details for manager-related operations and data management. Key types exported from this module: `ResourceManager`. Primary functions: `new()`, `new_resource()`, `get_resource()`, `get_resource_mut()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/economy/manager.rs](src/economy/manager.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ResourceManager`](src/economy/manager.rs#L22) | A manager that owns named resources and provides bulk operations and conversi... |

### `economy::modifier`

> Resource modifiers and conversion rules.  This module is part of Luna2D's `resource` subsystem and provides the implementation details for modifier-related operations and data management. Key types exported from this module: `ModifierType`, `Modifier`, `ConversionRule`. Primary functions: `as_str()`, `from_str()`, `new()`, `mod_type()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/economy/modifier.rs](src/economy/modifier.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ModifierType`](src/economy/modifier.rs#L22) | Modifier type for conversion rules. Consult the module-level documentation fo... |
| `struct` | [`ConversionRule`](src/economy/modifier.rs#L206) | A rule for converting one resource type to another.  # Fields - `from` — `Str... |
| `struct` | [`Modifier`](src/economy/modifier.rs#L71) | A rate modifier that can be attached to conversion rules.  # Fields - `mod_ty... |

### `economy::resource`

> Resource definition with capacity, flow, decay, and overflow policy.  This module is part of Luna2D's `resource` subsystem and provides the implementation details for resource-related operations and data management. Key types exported from this module: `OverflowPolicy`, `Resource`. Primary functions: `as_str()`, `from_str()`, `new()`, `name()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/economy/resource.rs](src/economy/resource.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`OverflowPolicy`](src/economy/resource.rs#L23) | Policy governing what happens when adding exceeds capacity.  # Variants - `Ex... |
| `struct` | [`Resource`](src/economy/resource.rs#L81) | A single named numeric resource with rates, capacity, overflow policy, and re... |

## `engine` {#engine}

### `engine`

> Core engine lifecycle: application loop, configuration, error handling, and diagnostics.  This module owns the highest-level runtime structures in the Luna2D engine. [`App`] drives the winit event loop, fires `luna.load/update/draw/keypressed/…` Lua callbacks at the right moments, and holds every domain subsystem inside a `Rc<RefCell<SharedState>>` that is shared with the Lua VM.  # Module overview  - [`app`] — [`App`] struct that implements `winit::ApplicationHandler`.  It creates the   window and renderer, initialises every subsystem, executes `main.lua`, and routes   window events to their corresponding Lua callbacks. - [`config`] — [`Config`], [`WindowConfig`], [`ModulesConfig`], and [`PerformanceConfig`]   structs loaded from `conf.lua` at startup. - [`debug_overlay`] — on-screen FPS and draw-call counter drawn in the bottom-right   corner of the window during development. - [`error`] — [`EngineError`] enum and [`EngineResult`] type alias used throughout the   codebase for structured, recoverable error propagation. - [`error_screen`] — fallback render path that displays a human-readable Lua or engine   error on screen instead of crashing silently. - [`log_messages`] — structured, stable-ID log message constants used by `log::info!`   macros across the engine so message IDs remain consistent between builds. - [`resource_keys`] — typed newtype keys (`SoundKey`, `TextureKey`, `ShaderId`, …) for   all slot-map resource pools in the engine.

*Source: [src/engine/mod.rs](src/engine/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`app`](src/engine/mod.rs#L27) | Entry point for the Luna2D engine lifecycle and game loop. |
| `mod` | [`config`](src/engine/mod.rs#L29) | Engine and window configuration structs (Config, WindowConfig, etc.). |
| `mod` | [`debug_overlay`](src/engine/mod.rs#L31) | Debug overlay for FPS and draw-call statistics. |
| `mod` | [`error`](src/engine/mod.rs#L33) | EngineError enum and EngineResult type alias for engine-level errors. |
| `mod` | [`error_screen`](src/engine/mod.rs#L35) | Visual error screen for Lua and engine errors. |
| `mod` | [`log_messages`](src/engine/mod.rs#L37) | Structured logging with stable message IDs. |
| `mod` | [`resource_keys`](src/engine/mod.rs#L39) | Typed resource keys for generational ID-based resource pools. |
| `mod` | [`shared_state`](src/engine/mod.rs#L41) | Central shared runtime state: SharedState, WindowState, FullscreenType, Error... |

### `engine::app`

> Luna2D application lifecycle using winit 0.30 + wgpu GPU rendering.  Uses a `winit` event loop with `GpuRenderer` for hardware-accelerated rendering. The game loop structure (callbacks, SharedState, Lua VM) follows the standard pattern.

*Source: [src/engine/app.rs](src/engine/app.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`App`](src/engine/app.rs#L1811) | Entry point for the Luna2D engine. Owns the game loop, GPU renderer, and Lua ... |

### `engine::config`

> Engine and window configuration loaded from `conf.lua`.  When the engine starts it looks for a `conf.lua` file in the game directory. If found, it is executed in a minimal Lua VM and the result is read into a [`Config`] struct.  Missing fields fall back to built-in defaults so authors only need to specify the settings they actually want to change.  # Structure  [`Config`] is the top-level container and contains five nested structs: - [`WindowConfig`] — window geometry, title, display placement, and decoration options. - [`GraphicsConfig`] — GPU backend selection and power preference, resolved at startup. - [`ModulesConfig`] — boolean feature-flags for optional engine subsystems (audio,   physics, graphics, etc.).  Disabling a module avoids the startup cost and prevents   the matching `luna.*` API calls from being registered. - [`PerformanceConfig`] — target frame-rate cap (`fps_cap`).  The `identity` field sets the name of the per-user save directory returned by `luna.filesystem.getSaveDirectory()`.  If unset, the engine uses the game directory name as a fallback.  # Example `conf.lua`  ```lua function luna.conf(t)     t.window.title   = "My Game"     t.window.width   = 1280     t.window.height  = 720     t.window.vsync   = true     -- GPU backend: "auto" | "dx12" | "vulkan" | "metal"     t.graphics.backend          = "auto"     -- Power preference: "high" | "low" | "none"     t.graphics.power_preference = "high" end ```

*Source: [src/engine/config.rs](src/engine/config.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Config`](src/engine/config.rs#L56) | Top-level engine configuration. Consult the module-level documentation for th... |
| `struct` | [`GraphicsConfig`](src/engine/config.rs#L90) | GPU backend and power-preference settings resolved once at engine startup.  T... |
| `struct` | [`ModulesConfig`](src/engine/config.rs#L146) | Flags to enable or disable optional engine subsystems.  # Fields - `audio` — ... |
| `struct` | [`PerformanceConfig`](src/engine/config.rs#L160) | Frame rate cap and other performance tuning options.  # Fields - `target_fps`... |
| `struct` | [`WindowConfig`](src/engine/config.rs#L114) | Window dimensions, title, vsync, fullscreen, and resize settings.  # Fields -... |

### `engine::debug_overlay`

> Debug overlay for displaying FPS and draw call statistics.  Renders green text on a semi-transparent background in the top-right corner of the screen. Can be toggled at runtime via F12 or `luna.system.setDebugOverlay()`.

*Source: [src/engine/debug_overlay.rs](src/engine/debug_overlay.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DebugOverlay`](src/engine/debug_overlay.rs#L15) | Debug overlay showing FPS and render statistics.  # Fields - `enabled` — `boo... |

### `engine::error`

> Structured error types and result alias for the Luna2D engine.  All engine-level error conditions are expressed as variants of [`EngineError`], a `thiserror`-derived enum that carries a human-readable description and belongs to one of the six [`ErrorCategory`] groups: `Init`, `Runtime`, `Resource`, `Script`, `System`, or `Filesystem`.  This module also provides [`EngineResult<T>`], a type alias for `Result<T, EngineError>`, which should be used in public APIs that can fail.  # Design goal  Every error variant has a stable four-digit numeric code (e.g. `E1001`) that can be displayed in the error screen and referenced in documentation without changing across releases.  This makes it straightforward for users to search for a specific error online or in the Luna2D issue tracker.  Lua errors are wrapped as [`EngineError::LuaError`] so they can flow through the same result type and be presented by [`crate::engine::error_screen::ErrorScreen`].

*Source: [src/engine/error.rs](src/engine/error.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`EngineError`](src/engine/error.rs#L80) | All possible error conditions that can occur in the Luna2D engine.  Each vari... |
| `enum` | [`ErrorCategory`](src/engine/error.rs#L32) | Error category for grouping related engine errors.  # Variants - `Init` — Ini... |
| `type` | [`EngineResult`](src/engine/error.rs#L193) | Convenience alias for `Result<T, EngineError>` used throughout the engine.  #... |

### `engine::error_screen`

> Visual error screen for displaying Lua and engine errors to the user.  Generates `DrawCommand` sequences that render a engine-standard blue error screen, including the error title, message, traceback, and instructions to quit or restart.

*Source: [src/engine/error_screen.rs](src/engine/error_screen.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`format_traceback`](src/engine/error_screen.rs#L318) | Cleans up a Lua traceback string for display.  # Parameters - `traceback` — `... |
| `fn` | [`wrap_text`](src/engine/error_screen.rs#L268) | Wraps a text string at word boundaries to fit within `max_chars` columns.  # ... |
| `struct` | [`ErrorScreen`](src/engine/error_screen.rs#L36) | Visual error screen that generates `DrawCommand` sequences for the GPU render... |

### `engine::log_messages`

> Structured logging with stable message IDs for the Luna2D engine.  Every engine log message has a stable ID (`L001`..`L099`) so that external tools and Lua scripts can filter or match on them.  Use the [`log_msg!`] macro instead of bare `log::info!` / `log::warn!` / `log::error!` calls.

*Source: [src/engine/log_messages.rs](src/engine/log_messages.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `const` | [`L001_ENGINE_START`](src/engine/log_messages.rs#L58) | Log message: engine starting. Consult the module-level documentation for the ... |
| `const` | [`L002_ENGINE_STOP`](src/engine/log_messages.rs#L60) | Log message: engine shut down. Consult the module-level documentation for the... |
| `const` | [`L003_GAME_LOADED`](src/engine/log_messages.rs#L62) | Log message: game loaded from path. Consult the module-level documentation fo... |
| `const` | [`L004_GAME_RESTART`](src/engine/log_messages.rs#L64) | Log message: game restarted. Consult the module-level documentation for the b... |
| `const` | [`L005_CONF_LOADED`](src/engine/log_messages.rs#L66) | Log message: conf.lua loaded. Consult the module-level documentation for the ... |
| `const` | [`L010_RENDER_ERROR`](src/engine/log_messages.rs#L73) | Log message: render error occurred. Consult the module-level documentation fo... |
| `const` | [`L011_LUA_ERROR`](src/engine/log_messages.rs#L75) | Log message: Lua error caught. Consult the module-level documentation for the... |
| `const` | [`L012_AUDIO_ERROR`](src/engine/log_messages.rs#L77) | Log message: audio error. Consult the module-level documentation for the broa... |
| `const` | [`L013_FS_ERROR`](src/engine/log_messages.rs#L79) | Log message: filesystem error. Consult the module-level documentation for the... |
| `const` | [`L014_PHYSICS_ERROR`](src/engine/log_messages.rs#L81) | Log message: physics error. Consult the module-level documentation for the br... |
| `const` | [`L015_RESOURCE_NOT_FOUND`](src/engine/log_messages.rs#L83) | Log message: resource not found. Consult the module-level documentation for t... |
| `const` | [`L020_NO_AUDIO_DEVICE`](src/engine/log_messages.rs#L90) | Log message: no audio device available. Consult the module-level documentatio... |
| `const` | [`L021_CLIPBOARD_FAIL`](src/engine/log_messages.rs#L92) | Log message: clipboard access failed. Consult the module-level documentation ... |
| `const` | [`L022_UNKNOWN_LOG_LEVEL`](src/engine/log_messages.rs#L94) | Log message: unknown log level requested. |
| `const` | [`L030_ASYNC_LOAD_REQUEST`](src/engine/log_messages.rs#L101) | Log message: async asset load requested. |
| `const` | [`L031_ASYNC_LOAD_COMPLETE`](src/engine/log_messages.rs#L103) | Log message: async asset load completed. |
| `const` | [`L032_BATCH_STATS`](src/engine/log_messages.rs#L105) | Log message: draw batch statistics. Consult the module-level documentation fo... |
| `fn` | [`get_log_level`](src/engine/log_messages.rs#L42) | Returns the current log level name. This accessor incurs no allocation; call ... |
| `fn` | [`set_log_level`](src/engine/log_messages.rs#L21) | Sets the global log level at runtime (called from `luna.system.setLogLevel`).... |

### `engine::shared_state`

> Central shared runtime state for the Luna2D engine.  [`SharedState`] is the hub that connects every subsystem to every API call. It is created once at startup, wrapped in `Rc<RefCell<SharedState>>`, and cloned into every Lua API closure. The engine event loop also holds a clone.  Also defines [`WindowState`], [`FullscreenType`], and [`ErrorInfo`] which are window-management types used by both `engine` and `lua_api`.

*Source: [src/engine/shared_state.rs](src/engine/shared_state.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FullscreenType`](src/engine/shared_state.rs#L37) | Fullscreen mode type for window management.  # Variants - `Desktop` — Desktop... |
| `struct` | [`ErrorInfo`](src/engine/shared_state.rs#L198) | Structured error information for the last engine error.  # Fields - `message`... |
| `struct` | [`SharedState`](src/engine/shared_state.rs#L239) | Shared mutable state passed via `Rc<RefCell<SharedState>>` to all Lua API clo... |
| `struct` | [`WindowState`](src/engine/shared_state.rs#L82) | Tracks window state and queues window operations for the event loop.  # Field... |

## `entity` {#entity}

### `entity`

> Entity/ECS module — lightweight entity-component-system with ID recycling, bitmap tags, layers, blueprints, and system dispatch.  This module is part of Luna2D's `entity` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/entity/mod.rs](src/entity/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`relationships`](src/entity/mod.rs#L13) | Generic relationship system for entity pairs. |
| `mod` | [`universe`](src/entity/mod.rs#L11) | Universe — entity management with ID recycling, components, tags, and systems. |

### `entity::relationships`

> Generic relationship system for entities.  Stores symmetric numeric relations and named-state levels between entity pairs. Entity IDs are `u32` to match [`Universe`](super::Universe).  This module is part of Luna2D's `entity` subsystem and provides the implementation details for relationships-related operations and data management. Key types exported from this module: `RelationType`, `Relationship`, `RelationshipManager`. Primary functions: `new()`, `has_level()`, `new()`, `define_type()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/entity/relationships.rs](src/entity/relationships.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`RelationType`](src/entity/relationships.rs#L28) | Definition of a named relation type with a fixed set of valid level strings. ... |
| `struct` | [`Relationship`](src/entity/relationships.rs#L83) | A relationship between two entities: numeric value plus per-type named levels... |
| `struct` | [`RelationshipManager`](src/entity/relationships.rs#L112) | Manages all relation types and the per-pair relationship records.  # Fields -... |

### `entity::universe`

> Universe — a self-contained ECS world. Entities are u32 IDs starting at 1.  This module is part of Luna2D's `entity` subsystem and provides the implementation details for universe-related operations and data management. Key types exported from this module: `Universe`. Primary functions: `new()`, `get_system_store()`, `spawn()`, `kill()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/entity/universe.rs](src/entity/universe.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`deep_copy_table`](src/entity/universe.rs#L961) | Deep-copies a Lua table recursively. Consult the module-level documentation f... |
| `struct` | [`Universe`](src/entity/universe.rs#L33) | A self-contained ECS world. Consult the module-level documentation for the br... |

## `event` {#event}

### `event`

> Event queue for polling system and custom events.  Provides an alternative to the callback model where game code can poll events from a FIFO queue. Also contains the `Signal` pub-sub type for handle-based event dispatching.

*Source: [src/event/mod.rs](src/event/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`EventArg`](src/event/mod.rs#L21) | Argument values that can be attached to events.  # Variants - `Str` — Str var... |
| `struct` | [`Event`](src/event/mod.rs#L38) | A single event in the event queue. Consult the module-level documentation for... |
| `struct` | [`EventQueue`](src/event/mod.rs#L50) | FIFO event queue for system and custom events.  # Fields - `events` — `VecDeq... |

### `event::signal`

> Handle-based pub-sub signal system.  `Signal` provides a lightweight event bus where listeners subscribe by name and receive a monotonically increasing handle ID for later removal. Callbacks fire in registration order.

*Source: [src/event/signal.rs](src/event/signal.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Signal`](src/event/signal.rs#L35) | Handle-based pub-sub signal dispatcher. Consult the module-level documentatio... |
| `struct` | [`Subscription`](src/event/signal.rs#L16) | A single subscription entry in a [`Signal`].  # Fields - `handle` — `u64`. - ... |

## `filesystem` {#filesystem}

### `filesystem`

> Mod implementation for the `filesystem` subsystem.  This module is part of Luna2D's `filesystem` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/filesystem/mod.rs](src/filesystem/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`async_loader`](src/filesystem/mod.rs#L16) | Background asset-loading worker that reads files off the main thread. |
| `mod` | [`file_data`](src/filesystem/mod.rs#L19) | Raw file data buffer loaded from the VFS. |
| `mod` | [`file_handle`](src/filesystem/mod.rs#L13) | File handle with buffered read/write and sandboxed path resolution. |
| `mod` | [`vfs`](src/filesystem/mod.rs#L10) | Sandboxed virtual filesystem that restricts I/O to the game directory. |

### `filesystem::async_loader`

> Background asset-loading worker that reads files off the main thread.  The loader owns a single OS thread that processes read requests from a bounded channel.  Results are polled from the main thread each frame — no `unsafe`, no shared mutable state beyond an `Arc<Mutex<>>` result map.

*Source: [src/filesystem/async_loader.rs](src/filesystem/async_loader.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`LoadResult`](src/filesystem/async_loader.rs#L23) | Outcome of a completed load request. Returns an error if the source data is m... |
| `enum` | [`LoadStatus`](src/filesystem/async_loader.rs#L36) | Status returned by [`AsyncLoader::poll`].  # Variants - `Pending` — Pending v... |
| `struct` | [`AsyncLoader`](src/filesystem/async_loader.rs#L62) | A single-threaded background file reader.  Create one per engine session; dro... |
| `struct` | [`LoadHandle`](src/filesystem/async_loader.rs#L15) | Opaque handle returned to callers (and to Lua) that identifies a pending load. |

### `filesystem::file_data`

> Raw file data buffer loaded from the VFS.

*Source: [src/filesystem/file_data.rs](src/filesystem/file_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`FileData`](src/filesystem/file_data.rs#L8) | Raw bytes loaded from the virtual filesystem.  # Fields - `path` — `String`. ... |

### `filesystem::file_handle`

> File handle with buffered read/write and sandboxed path resolution.  This module is part of Luna2D's `filesystem` subsystem and provides the implementation details for file handle-related operations and data management. Key types exported from this module: `FileMode`, `FileHandle`. Primary functions: `parse_mode()`, `as_str()`, `open()`, `read()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/filesystem/file_handle.rs](src/filesystem/file_handle.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FileMode`](src/filesystem/file_handle.rs#L24) | File access mode. Consult the module-level documentation for the broader usag... |
| `struct` | [`FileHandle`](src/filesystem/file_handle.rs#L78) | A sandboxed file handle for reading or writing game files.  # Fields - `mode`... |

### `filesystem::vfs`

> Vfs implementation for the `filesystem` subsystem.  This module is part of Luna2D's `filesystem` subsystem and provides the implementation details for vfs-related operations and data management. Key types exported from this module: `FileInfo`, `FileType`, `GameFS`. Primary functions: `new()`, `base_dir()`, `read_string()`, `read_bytes()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/filesystem/vfs.rs](src/filesystem/vfs.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FileType`](src/filesystem/vfs.rs#L41) | File type classification for `FileInfo`.  # Variants - `File` — regular file ... |
| `struct` | [`FileInfo`](src/filesystem/vfs.rs#L22) | File metadata returned by `get_info()`. Consult the module-level documentatio... |
| `struct` | [`GameFS`](src/filesystem/vfs.rs#L71) | Sandboxed filesystem rooted at the game directory; prevents path-traversal at... |
| `struct` | [`MountLayer`](src/filesystem/vfs.rs#L58) | A virtual filesystem mount layer overlaid on top of the game directory.  # Fi... |

## `graph` {#graph}

### `graph`

> Directed graph with item flow simulation for Luna2D.  Provides nodes, edges, typed items, simulation (decay, transit, push/pull flow, conversions, queues), Dijkstra pathfinding, graph algorithms (components, cycles, topological sort), and supply/demand processing.

*Source: [src/graph/mod.rs](src/graph/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`algorithms`](src/graph/mod.rs#L8) | Graph algorithms: connected components, topological sort, cycle detection. |
| `mod` | [`core`](src/graph/mod.rs#L10) | Core `Graph` struct — nodes, edges, and item storage. |
| `mod` | [`edge`](src/graph/mod.rs#L12) | Directed edge with capacity, cooldown, and type-filtering. |
| `mod` | [`item`](src/graph/mod.rs#L14) | Typed items that flow through the graph. |
| `mod` | [`node`](src/graph/mod.rs#L16) | Graph node with conversion rules, overflow policy, and flow mode. |
| `mod` | [`simulation`](src/graph/mod.rs#L20) | Tick-based simulation: decay, transit, push/pull flow. |
| `mod` | [`supply_demand`](src/graph/mod.rs#L22) | Supply and demand processing for resource flow graphs. |
| `mod` | [`traversal`](src/graph/mod.rs#L18) | Dijkstra shortest-path on the graph. |

### `graph::core`

> Top-level directed graph container with node, edge, and item management.  This module is part of Luna2D's `graph` subsystem and provides the implementation details for core-related operations and data management. Key types exported from this module: `GraphStats`, `Graph`. Primary functions: `new()`, `add_node()`, `remove_node()`, `has_node()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graph/core.rs](src/graph/core.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Graph`](src/graph/core.rs#L60) | A directed graph with typed nodes, edges, and flowing items.  # Fields - `nod... |
| `struct` | [`GraphStats`](src/graph/core.rs#L31) | Statistics snapshot of the graph state. Consult the module-level documentatio... |

### `graph::edge`

> Graph edge — a directed connection between nodes.  This module is part of Luna2D's `graph` subsystem and provides the implementation details for edge-related operations and data management. Key types exported from this module: `Edge`. Primary functions: `new()`, `get_type()`, `set_type()`, `is_on_cooldown()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graph/edge.rs](src/graph/edge.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Edge`](src/graph/edge.rs#L31) | A directed connection between two nodes in the graph.  # Fields - `id` — `u64... |

### `graph::item`

> Graph item — a typed entity that flows through the network.  This module is part of Luna2D's `graph` subsystem and provides the implementation details for item-related operations and data management. Key types exported from this module: `ItemPosition`, `GraphItem`. Primary functions: `new()`, `kill()`, `is_alive()`, `get_type()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graph/item.rs](src/graph/item.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ItemPosition`](src/graph/item.rs#L18) | Where a `GraphItem` currently resides. Consult the module-level documentation... |
| `struct` | [`GraphItem`](src/graph/item.rs#L43) | A typed entity that flows through the graph network.  # Fields - `id` — `u64`... |

### `graph::node`

> Graph node — a vertex with capacity, flow control, conversion rules, and queuing.  This module is part of Luna2D's `graph` subsystem and provides the implementation details for node-related operations and data management. Key types exported from this module: `OverflowPolicy`, `FlowMode`, `ConversionRule`, `Supply`, `Demand`. Primary functions: `to_str()`, `to_str()`, `new()`, `get_type()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graph/node.rs](src/graph/node.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`FlowMode`](src/graph/node.rs#L65) | How a node participates in automatic item flow.  # Variants - `Passive` — Pas... |
| `enum` | [`OverflowPolicy`](src/graph/node.rs#L21) | What happens when items arrive at a full node.  # Variants - `Reject` — Rejec... |
| `struct` | [`ConversionRule`](src/graph/node.rs#L113) | A rule that converts N input items of one type into M output items of another... |
| `struct` | [`Demand`](src/graph/node.rs#L144) | A demand declaration on a node. Consult the module-level documentation for th... |
| `struct` | [`Node`](src/graph/node.rs#L178) | A vertex in the graph with capacity, flow control, conversion, and queuing.  ... |
| `struct` | [`Supply`](src/graph/node.rs#L130) | A supply declaration on a node. Consult the module-level documentation for th... |

### `graph::simulation`

> Simulation engine — update(dt) and step() for item flow, decay, transit, and conversions.  This module is part of Luna2D's `graph` subsystem and provides the implementation details for simulation-related operations and data management. Key types exported from this module: `GraphEvent`. Primary functions: `update()`, `step()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graph/simulation.rs](src/graph/simulation.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`GraphEvent`](src/graph/simulation.rs#L30) | Events generated during simulation for the Lua callback layer to dispatch.  #... |

### `graph::traversal`

> Dijkstra pathfinding and reachability queries on the graph.  This module is part of Luna2D's `graph` subsystem and provides the implementation details for traversal-related operations and data management. Key types exported from this module: `PathResult`. Primary functions: `find_path()`, `find_path_for_item()`, `get_distance()`, `get_reachable()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graph/traversal.rs](src/graph/traversal.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PathResult`](src/graph/traversal.rs#L23) | Result of a successful pathfinding query.  # Fields - `nodes` — `Vec<u64>`. -... |

## `graphics` {#graphics}

### `graphics`

> Mod implementation for the `graphics` subsystem.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/mod.rs](src/graphics/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`animation`](src/graphics/mod.rs#L12) | Sprite-sheet frame animation with configurable durations and looping. |
| `mod` | [`camera`](src/graphics/mod.rs#L14) | 2D camera with position, zoom, and rotation producing a view matrix. |
| `mod` | [`canvas`](src/graphics/mod.rs#L16) | Off-screen render targets (canvases) for deferred compositing. |
| `mod` | [`column_batch`](src/graphics/mod.rs#L18) | Wolfenstein-style raycasting column batch renderer. |
| `mod` | [`data_graph_renderer`](src/graphics/mod.rs#L20) | Mathematical function graph and chart renderer. |
| `mod` | [`decal_surface`](src/graphics/mod.rs#L22) | Persistent surface for stamping decal textures. |
| `mod` | [`draw_layer`](src/graphics/mod.rs#L24) | Z-ordered draw layer for controlling render order. |
| `mod` | [`font`](src/graphics/mod.rs#L26) | TTF/OTF font loading, glyph rasterization, and atlas packing. |
| `mod` | [`gpu_renderer`](src/graphics/mod.rs#L28) | GPU-accelerated renderer backed by wgpu (primary runtime renderer). |
| `mod` | [`large_map_renderer`](src/graphics/mod.rs#L30) | Optimized renderer for large tile-based maps with chunking and LOD. |
| `mod` | [`light2d`](src/graphics/mod.rs#L32) | 2D point light data container for lighting systems. |
| `mod` | [`mesh`](src/graphics/mod.rs#L34) | Custom geometry mesh with per-vertex position, UV, and color data. |
| `mod` | [`nine_slice`](src/graphics/mod.rs#L36) | Nine-slice (9-patch) image rendering for scalable UI elements. |
| `mod` | [`palette_lut`](src/graphics/mod.rs#L38) | Color palette lookup table for shader-based palette swapping. |
| `mod` | [`polygon_map`](src/graphics/mod.rs#L40) | Polygon map renderer with region management and hit detection. |
| `mod` | [`renderer`](src/graphics/mod.rs#L42) | DrawCommand queue, draw enums, and texture data types. |
| `mod` | [`shader`](src/graphics/mod.rs#L44) | Custom WGSL shader support with uniform variables. |
| `mod` | [`shape`](src/graphics/mod.rs#L10) | Compound shape builder that accumulates vector primitives for batched drawing. |
| `mod` | [`sprite`](src/graphics/mod.rs#L46) | Sprite struct combining a texture, transform, and tint color. |
| `mod` | [`sprite_batch`](src/graphics/mod.rs#L48) | Sprite batching for efficient rendering of many sprites sharing one texture. |
| `mod` | [`sprite_sheet`](src/graphics/mod.rs#L50) | Grid-based sprite sheet with directional support and named groups. |
| `mod` | [`srgb`](src/graphics/mod.rs#L52) | RGBA Color struct with named constants and conversion helpers. |
| `mod` | [`texture`](src/graphics/mod.rs#L54) | Texture loading and TextureData storage for the renderer. |
| `mod` | [`texture_atlas`](src/graphics/mod.rs#L56) | CPU-side bin-packing texture atlas using shelf algorithm. |
| `mod` | [`trail`](src/graphics/mod.rs#L58) | Trail renderer for fading ribbon effects. |
| `mod` | [`viewport`](src/graphics/mod.rs#L60) | Virtual resolution viewport with manual transform application. |
| `mod` | [`viewport_scale`](src/graphics/mod.rs#L62) | Virtual resolution viewport with automatic scaling. |

### `graphics::animation`

> Sprite animation system with named clips, speed control, and frame-level events.  An [`Animation`] stores a pool of [`AnimFrame`] entries (each defining a source rectangle and optional per-frame duration) and any number of named [`AnimClip`]s that reference those frames by index. Call [`Animation::update`] each tick and inspect [`Animation::drain_events`] for playback notifications.

*Source: [src/graphics/animation.rs](src/graphics/animation.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`AnimEvent`](src/graphics/animation.rs#L67) | Events emitted by [`Animation::update`].  # Variants - `Finished` — Finished ... |
| `struct` | [`AnimClip`](src/graphics/animation.rs#L47) | A named animation clip that references frames by index into the parent  # Fie... |
| `struct` | [`AnimFrame`](src/graphics/animation.rs#L30) | A single animation frame with a source rectangle and optional duration.  # Fi... |
| `struct` | [`Animation`](src/graphics/animation.rs#L101) | Sprite animation with named clips, speed control, and playback events.  # Usa... |
| `type` | [`AnimationFrame`](src/graphics/animation.rs#L18) | Backward-compatible alias for [`AnimFrame`].  Existing code that imports `Ani... |

### `graphics::camera`

> Camera types for 2D viewport control.  Provides the original [`Camera`] (used by `SharedState` for the flat `luna.graphics.setCamera()` API) and the new Phase 24 [`Camera2D`] with smooth follow, dead zone, bounds clamping, and screen-shake.

*Source: [src/graphics/camera.rs](src/graphics/camera.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Camera`](src/graphics/camera.rs#L21) | Basic camera with position, zoom, and rotation.  Used by `SharedState` for th... |
| `struct` | [`Camera2D`](src/graphics/camera.rs#L118) | Full-featured 2D camera with smooth follow, dead zone, bounds clamping,  # Fi... |

### `graphics::canvas`

> Off-screen render targets (canvases) for deferred compositing.  A `Canvas` stores the logical dimensions of an off-screen texture that the GPU renderer can draw to.  After rendering, the canvas can be composited to the screen (or another canvas) as a regular image.

*Source: [src/graphics/canvas.rs](src/graphics/canvas.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Canvas`](src/graphics/canvas.rs#L16) | An off-screen render target with a fixed pixel resolution.  # Fields - `width... |

### `graphics::column_batch`

> Wolfenstein-style raycasting column batch renderer.  Stores per-column wall data produced by a raycaster and provides helpers for updating columns from raw ray data and querying depth.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for column batch-related operations and data management. Key types exported from this module: `ColumnData`, `ColumnBatch`. Primary functions: `new()`, `set_column()`, `get_column()`, `update_from_ray_data()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/column_batch.rs](src/graphics/column_batch.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ColumnBatch`](src/graphics/column_batch.rs#L66) | Wolfenstein-style raycasting column batch renderer.  # Fields - `columns` — `... |
| `struct` | [`ColumnData`](src/graphics/column_batch.rs#L26) | Per-column rendering state produced by a raycaster.  # Fields - `tex_u` — `f3... |

### `graphics::data_graph_renderer`

> Mathematical function graph and chart renderer.  [`GraphRenderer`] manages multiple named data series (line, scatter, bar) and provides viewport ↔ world coordinate mapping for rendering charts in a Luna2D game. The renderer stores pure data — actual draw calls are issued by the Lua wrapper in `graphics_api.rs`.

*Source: [src/graphics/data_graph_renderer.rs](src/graphics/data_graph_renderer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`GraphSeries`](src/graphics/data_graph_renderer.rs#L21) | A data series that can be added to a [`GraphRenderer`].  # Variants - `Line` ... |
| `struct` | [`GraphRenderer`](src/graphics/data_graph_renderer.rs#L87) | Mathematical function graph / chart renderer.  # Fields - `viewport` — `(f32,... |

### `graphics::decal_surface`

> Persistent surface for stamping decal textures.  A simple data container describing an off-screen render target onto which decals can be composited.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for decal surface-related operations and data management. Key types exported from this module: `DecalSurface`. Primary functions: `new()`, `get_dimensions()`, `get_width()`, `get_height()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/decal_surface.rs](src/graphics/decal_surface.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DecalSurface`](src/graphics/decal_surface.rs#L22) | Persistent render target for stamping decals.  # Fields - `width` — `u32`. - ... |

### `graphics::draw_layer`

> Z-ordered draw layer for controlling render order.  Entries are queued with a z-order value and flushed in ascending order so that lower z-values are drawn first (back-to-front).  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for draw layer-related operations and data management. Key types exported from this module: `LayerEntry`, `DrawLayer`. Primary functions: `new()`, `queue()`, `flush()`, `clear()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/draw_layer.rs](src/graphics/draw_layer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DrawLayer`](src/graphics/draw_layer.rs#L34) | Z-ordered draw callback queue. Consult the module-level documentation for the... |
| `struct` | [`LayerEntry`](src/graphics/draw_layer.rs#L19) | A queued draw entry with its z-order. Consult the module-level documentation ... |

### `graphics::font`

> TTF/OTF font loading, glyph rasterization, and atlas packing for GPU text rendering.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for font-related operations and data management. Key types exported from this module: `Font`, `GlyphInfo`. Primary functions: `from_bytes()`, `ensure_glyph()`, `text_width()`, `line_height()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/font.rs](src/graphics/font.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Font`](src/graphics/font.rs#L40) | A loaded TTF/OTF font with a glyph atlas for GPU rendering.  Wraps a `fontdue... |
| `struct` | [`GlyphInfo`](src/graphics/font.rs#L72) | Information about a single rasterized glyph in the atlas.  # Fields - `uv_x` ... |

### `graphics::gpu_renderer`

> GPU-accelerated 2D renderer for Luna2D, backed by wgpu.  Processes a `DrawCommand` queue each frame, tessellates geometry on the CPU, uploads vertex / index data to GPU buffers, and issues a single render pass.  # Design - Two render pipelines: *color* (solid + gradient shapes) and *texture* (images, sprites). - Transform stack maintained during command processing — identical to standard push/pop transform stack. - Colored geometry is batched in one draw call; each distinct texture source is one draw call. - Draw order: colored shapes first (in submission order), textured images second.

*Source: [src/graphics/gpu_renderer/mod.rs](src/graphics/gpu_renderer/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`gpu_resources`](src/graphics/gpu_renderer/mod.rs#L387) | Gpu Resources sub-module. |
| `mod` | [`render_pass`](src/graphics/gpu_renderer/mod.rs#L389) | Render Pass sub-module. |
| `struct` | [`GpuRenderer`](src/graphics/gpu_renderer/mod.rs#L337) | GPU-accelerated renderer that processes `DrawCommand` queues via wgpu.  # Fie... |
| `struct` | [`RenderStats`](src/graphics/gpu_renderer/mod.rs#L95) | Per-frame rendering statistics. Consult the module-level documentation for th... |

### `graphics::gpu_renderer::render_pass`

> Render-frame execution, draw-call dispatch, tessellation, and pipeline management for [`GpuRenderer`].  This module is part of Luna2D's `gpu_renderer` subsystem and provides the implementation details for render pass-related operations and data management. Primary functions: `render_frame()`, `parse_filter_mode()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/gpu_renderer/render_pass.rs](src/graphics/gpu_renderer/render_pass.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`parse_filter_mode`](src/graphics/gpu_renderer/render_pass.rs#L2665) | Parse a Lua string into a wgpu `FilterMode`.  # Returns `wgpu::FilterMode`.  ... |

### `graphics::large_map_renderer`

> Optimized renderer for large tile-based maps with chunking and LOD.  [`LargeMapRenderer`] stores a flat tile array and divides it into [`MapChunk`]s for efficient culling. Dirty flags allow incremental rebuilds when individual tiles change. The renderer tracks camera position, zoom, and viewport to compute which chunks are visible.

*Source: [src/graphics/large_map_renderer.rs](src/graphics/large_map_renderer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`LargeMapRenderer`](src/graphics/large_map_renderer.rs#L55) | Optimized large tile-map renderer with chunked culling.  # Fields - `tile_wid... |
| `struct` | [`MapChunk`](src/graphics/large_map_renderer.rs#L22) | A chunk of tiles pre-batched for fast culling and rendering.  # Fields - `cx`... |

### `graphics::light2d`

> 2D point light data container for lighting systems.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for light2d-related operations and data management. Key types exported from this module: `Light2D`. Primary functions: `new()`, `set_position()`, `get_position()`, `set_radius()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/light2d.rs](src/graphics/light2d.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Light2D`](src/graphics/light2d.rs#L25) | 2D point light with position, radius, color, and intensity.  # Fields - `x` —... |

### `graphics::mesh`

> Mesh API for custom geometry rendering.  Meshes allow Lua scripts to define arbitrary vertex data for rendering textured or colored geometry.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for mesh-related operations and data management. Key types exported from this module: `MeshDrawMode`, `MeshVertex`, `Mesh`. Primary functions: `new()`, `from_vertices()`, `set_vertex()`, `get_vertex()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/mesh.rs](src/graphics/mesh.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`MeshDrawMode`](src/graphics/mesh.rs#L23) | Drawing mode for mesh geometry. Consult the module-level documentation for th... |
| `struct` | [`Mesh`](src/graphics/mesh.rs#L86) | Custom geometry mesh with per-vertex position, UV, and color data.  # Fields ... |
| `struct` | [`MeshVertex`](src/graphics/mesh.rs#L44) | A single vertex in a mesh. Consult the module-level documentation for the bro... |

### `graphics::nine_slice`

> Nine-slice (9-patch) image rendering for scalable UI elements.  A nine-slice divides a texture into 9 regions using 4 border insets: corners are drawn at fixed size, edges stretch in one axis, and the center stretches in both axes. This preserves visual quality when scaling UI panels, buttons, and dialog boxes.

*Source: [src/graphics/nine_slice.rs](src/graphics/nine_slice.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`NineSlice`](src/graphics/nine_slice.rs#L43) | A nine-slice image definition: a texture plus border insets.  The four insets... |
| `type` | [`Patch`](src/graphics/nine_slice.rs#L11) | A single patch rectangle: `(src_x, src_y, src_w, src_h, dst_x, dst_y, dst_w, ... |

### `graphics::palette_lut`

> Color palette lookup table for shader-based palette swapping.  Maps source colors to target colors, allowing sprites to be re-colored at render time without modifying the original texture.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for palette lut-related operations and data management. Key types exported from this module: `PaletteLUT`. Primary functions: `new()`, `get_color_count()`, `set_color()`, `get_from_color()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/palette_lut.rs](src/graphics/palette_lut.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PaletteLUT`](src/graphics/palette_lut.rs#L25) | Color palette lookup table mapping source colors to target colors.  # Fields ... |

### `graphics::polygon_map`

> Polygon map renderer with region management and hit detection.  Manages a collection of named polygon regions with colors, labels, highlighting, and point-in-polygon queries.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for polygon map-related operations and data management. Key types exported from this module: `PolygonRegion`, `PolygonMap`. Primary functions: `new()`, `add_region()`, `remove_region()`, `set_region_color()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/polygon_map.rs](src/graphics/polygon_map.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PolygonMap`](src/graphics/polygon_map.rs#L49) | Polygon map renderer with region management and hit detection.  # Fields - `o... |
| `struct` | [`PolygonRegion`](src/graphics/polygon_map.rs#L26) | A named polygon region. Consult the module-level documentation for the broade... |

### `graphics::renderer`

> Draw command types, blend modes, and texture data for the Luna2D rendering pipeline.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for renderer-related operations and data management. Key types exported from this module: `CompareMode`, `StencilAction`, `TextAlign`, `DrawMode`, `BlendMode`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/renderer.rs](src/graphics/renderer.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BlendMode`](src/graphics/renderer.rs#L179) | Blending mode for draw operations. Consult the module-level documentation for... |
| `enum` | [`CompareMode`](src/graphics/renderer.rs#L27) | Stencil comparison mode for `luna.graphics.setStencilTest`.  # Variants - `Eq... |
| `enum` | [`DepthMode`](src/graphics/renderer.rs#L120) | Depth test comparison mode for `luna.graphics.setDepthMode`.  # Variants - `A... |
| `enum` | [`DrawCommand`](src/graphics/renderer.rs#L219) | A single deferred draw operation queued during `luna.draw()` and executed by ... |
| `enum` | [`DrawMode`](src/graphics/renderer.rs#L165) | Whether a shape is drawn filled or as an outline.  # Variants - `Fill` — Draw... |
| `enum` | [`DrawableKind`](src/graphics/renderer.rs#L623) | Type discriminator for resources that can be passed to luna.graphics.draw.  U... |
| `enum` | [`ParticleRenderShape`](src/graphics/renderer.rs#L550) | Geometric shape used when rendering a single untextured particle via `DrawPar... |
| `enum` | [`StencilAction`](src/graphics/renderer.rs#L58) | Stencil write action for `luna.graphics.stencil` and `luna.graphics.setStenci... |
| `enum` | [`TextAlign`](src/graphics/renderer.rs#L148) | Text alignment mode for formatted text printing.  # Variants - `Left` — Left ... |
| `struct` | [`ParticleInstance`](src/graphics/renderer.rs#L583) | Per-particle render data for a single frame.  Produced by `ParticleSystem::dr... |
| `struct` | [`StencilMode`](src/graphics/renderer.rs#L89) | Combined stencil rendering mode stored in `SharedState`.  Controls what happe... |
| `struct` | [`TextureData`](src/graphics/renderer.rs#L531) | Raw RGBA pixel data for a loaded texture, stored in the renderer's texture at... |

### `graphics::shader`

> Custom WGSL shader support for Luna2D.  Allows Lua scripts to create and apply custom fragment shaders with uniform variables.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for shader-related operations and data management. Key types exported from this module: `ShaderFragmentInput`, `Shader`, `UniformValue`. Primary functions: `new()`, `send()`, `has_uniform()`, `ordered_uniforms()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/shader.rs](src/graphics/shader.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ShaderFragmentInput`](src/graphics/shader.rs#L24) | Which fragment shader input the user's entry point expects.  # Variants - `Co... |
| `enum` | [`UniformValue`](src/graphics/shader.rs#L74) | A uniform value that can be sent to a shader from Lua.  # Variants - `Float` ... |
| `struct` | [`Shader`](src/graphics/shader.rs#L54) | Represents a compiled custom shader with its uniform values.  # Fields - `sou... |

### `graphics::shape`

> Compound shape builder for multi-primitive vector drawing.  This module provides [`CompoundShape`] and [`ShapeCommand`], which together implement an object-space command buffer for vector primitives.  A Lua script builds up a [`CompoundShape`] once, then replays it any number of times via [`crate::graphics::DrawCommand::DrawShape`] with a per-call affine transform.  **Tier position**: Tier 1 (`graphics`). Imports only from within the `graphics` crate-module (`super::renderer`). No imports from Tier 2–3 modules or from `lua_api`.

*Source: [src/graphics/shape.rs](src/graphics/shape.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ShapeCommand`](src/graphics/shape.rs#L35) | A single drawing command stored inside a [`CompoundShape`] command queue.  Th... |
| `struct` | [`CompoundShape`](src/graphics/shape.rs#L164) | A compound shape that accumulates draw primitives in local (object-space) coo... |

### `graphics::sprite`

> Sprite implementation for the `graphics` subsystem.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for sprite-related operations and data management. Key types exported from this module: `Sprite`. Primary functions: `new()`, `set_position()`, `set_scale()`, `set_rotation()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/sprite.rs](src/graphics/sprite.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Sprite`](src/graphics/sprite.rs#L25) | A textured game object with position, scale, rotation, and tint color.  `Spri... |

### `graphics::sprite_batch`

> Sprite batching for efficient rendering of many sprites sharing one texture.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for sprite batch-related operations and data management. Key types exported from this module: `SpriteBatch`, `BatchEntry`. Primary functions: `new()`, `add()`, `clear()`, `texture_key()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/sprite_batch.rs](src/graphics/sprite_batch.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BatchEntry`](src/graphics/sprite_batch.rs#L46) | A single sprite in a batch, describing position, region, and transform.  # Fi... |
| `struct` | [`SpriteBatch`](src/graphics/sprite_batch.rs#L23) | A batch of sprites sharing a single texture, drawn in one GPU call.  Created ... |

### `graphics::sprite_sheet`

> Grid-based sprite sheet with directional support and named groups.  Splits a texture into a uniform grid of frames and provides indexed, row/column, named-group, and directional access to the resulting quads.

*Source: [src/graphics/sprite_sheet.rs](src/graphics/sprite_sheet.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DirectionLayout`](src/graphics/sprite_sheet.rs#L32) | Directional layout for sprite sets. Consult the module-level documentation fo... |
| `struct` | [`FrameGroup`](src/graphics/sprite_sheet.rs#L17) | Named frame group within the sprite sheet.  # Fields - `name` — `String`. - `... |
| `struct` | [`SpriteSheet`](src/graphics/sprite_sheet.rs#L51) | Grid-based sprite sheet with directional support and named groups.  # Fields ... |

### `graphics::srgb`

> Srgb implementation for the `graphics` subsystem.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for srgb-related operations and data management. Key types exported from this module: `Color`. Primary functions: `from_u8()`, `to_u8()`, `to_rgb_u32()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/srgb.rs](src/graphics/srgb.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Color`](src/graphics/srgb.rs#L22) | RGBA color stored as `f32` components in the range `[0.0, 1.0]`.  Used everyw... |

### `graphics::texture`

> Texture implementation for the `graphics` subsystem.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for texture-related operations and data management. Key types exported from this module: `Texture`. Primary functions: `load()`, `from_rgba()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/texture.rs](src/graphics/texture.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Texture`](src/graphics/texture.rs#L26) | A loaded image asset referenced by its index into the renderer's texture list... |

### `graphics::texture_atlas`

> CPU-side bin-packing texture atlas using a shelf algorithm.  Packs named rectangular regions into a fixed-size atlas without any GPU interaction. Useful for building sprite-sheet layouts at load time.

*Source: [src/graphics/texture_atlas.rs](src/graphics/texture_atlas.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`AtlasRegion`](src/graphics/texture_atlas.rs#L17) | A named rectangular region packed into the atlas.  # Fields - `name` — `Strin... |
| `struct` | [`TextureAtlas`](src/graphics/texture_atlas.rs#L50) | CPU-side bin-packing atlas for sprite regions.  # Fields - `width` — `u32`. -... |

### `graphics::trail`

> Trail renderer for fading ribbon effects.  Stores a series of timestamped points that age out over a configurable lifetime, producing a tapered ribbon from head to tail.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for trail-related operations and data management. Key types exported from this module: `TrailPoint`, `Trail`. Primary functions: `new()`, `push_point()`, `update()`, `set_width()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/trail.rs](src/graphics/trail.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Trail`](src/graphics/trail.rs#L46) | Fading textured ribbon renderer. Consult the module-level documentation for t... |
| `struct` | [`TrailPoint`](src/graphics/trail.rs#L23) | A point in a trail with age tracking.  # Fields - `x` — `f32`. - `y` — `f32`.... |

### `graphics::viewport`

> Virtual resolution viewport with manual transform application.  Maps a fixed game resolution onto an arbitrary window size using letterboxing, stretching, or pixel-perfect scaling.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for viewport-related operations and data management. Key types exported from this module: `ScaleMode`, `Viewport`. Primary functions: `new()`, `resize()`, `get_scale()`, `get_offset()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/viewport.rs](src/graphics/viewport.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ScaleMode`](src/graphics/viewport.rs#L21) | Scale mode for virtual resolution mapping.  # Variants - `Letterbox` — Letter... |
| `struct` | [`Viewport`](src/graphics/viewport.rs#L44) | Virtual resolution with manual transform application.  # Fields - `game_width... |

### `graphics::viewport_scale`

> Virtual resolution viewport with automatic scaling and transform stack integration.  Like `Viewport`, but also tracks the scaled content dimensions for use with an automatic graphics transform stack.  This module is part of Luna2D's `graphics` subsystem and provides the implementation details for viewport scale-related operations and data management. Key types exported from this module: `ViewportScale`. Primary functions: `new()`, `resize()`, `get_game_dimensions()`, `get_scaled_dimensions()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/graphics/viewport_scale.rs](src/graphics/viewport_scale.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ViewportScale`](src/graphics/viewport_scale.rs#L32) | Virtual resolution with automatic graphics stack management.  # Fields - `gam... |

## `gui` {#gui}

### `gui`

> Retained-mode 2D widget system for building in-game menus, HUDs, dialog boxes, and inventory screens.  The `gui` module provides a tree of widgets rooted at an invisible `Panel` returned by `getRoot()`.  Concrete widget types — `Button`, `Label`, `TextInput`, `CheckBox`, `Slider`, `ProgressBar`, `ListBox`, `ComboBox`, `ScrollPanel`, `NinePatch`, `Panel`, `Toast`, and many more — inherit shared base properties (position, size, visibility, padding, margin, z-order, anchor constraints, and flexbox layout settings) from `WidgetBase`.  A `Theme` maps `(WidgetType, WidgetState)` pairs to `WidgetStyle` records containing colours, font size, border width, and corner radius.  Input events are forwarded manually from `luna.mousepressed` / `luna.keypressed` etc., giving scripts full control over which GUI instance is active.  ## Sub-modules  | Sub-module | Exported types | Purpose | |---|---|---| | `widget` | [`WidgetBase`], [`WidgetState`], [`WidgetType`] | Shared base fields, state enum, type tag | | `theme` | [`Theme`], [`WidgetStyle`] | Per-widget-type per-state styling | | `containers` | [`Panel`], [`Layout`], [`ScrollPanel`], [`NinePatch`] | Layout containers and nine-patch slicer | | `controls` | [`Button`], [`Label`], [`TextInput`], [`CheckBox`], [`Slider`], [`ProgressBar`], [`ComboBox`], [`ListBox`], [`TabBar`] | Interactive and display controls | | `extras` | [`Toast`], [`Separator`], [`Spacer`], [`TreeNode`], [`TreeView`] | Utility widgets: notifications, separators, tree views | | `context` | [`GuiContext`] | Root widget tree, focus tracking, toast queue, input routing |  ## Tier  `gui` is a **Tier 2 — Engine Extension** module.  It may import `math`, `engine`, and all Tier 1 modules.  It must not import other Tier 2 modules.

*Source: [src/gui/mod.rs](src/gui/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`containers`](src/gui/mod.rs#L33) | Layout primitives: panels, dock, scroll, window, nine-slice, split, and grid ... |
| `mod` | [`context`](src/gui/mod.rs#L35) | Root widget tree, focus management, input routing, and toast queue. |
| `mod` | [`controls`](src/gui/mod.rs#L37) | Interactive leaf widgets: buttons, checkboxes, sliders, combo-boxes, text inp... |
| `mod` | [`extras`](src/gui/mod.rs#L39) | Extended widgets: accordion, color picker, dialog, menu bar, status bar, tree... |
| `mod` | [`theme`](src/gui/mod.rs#L41) | Visual theme tokens (colors, fonts, spacing) used across all widgets. |
| `mod` | [`widget`](src/gui/mod.rs#L43) | Core widget trait and shared widget-state types. |

### `gui::containers`

> Container and layout widgets: Panel, Layout, ScrollPanel, NinePatch.  Containers hold child widgets and optionally apply layout rules to them. [`Panel`] is the simplest container.  [`Layout`] adds flexbox-inspired positioning (horizontal, vertical, grid).  [`ScrollPanel`] provides a scrollable viewport.  [`NinePatch`] computes nine-slice rectangles for scalable panel borders.

*Source: [src/gui/containers.rs](src/gui/containers.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`LayoutDirection`](src/gui/containers.rs#L65) | Direction in which a [`Layout`] positions its children.  # Variants - `Vertic... |
| `struct` | [`DockPanel`](src/gui/containers.rs#L505) | A dock-based layout container with left/right/top/bottom/center regions.  # F... |
| `struct` | [`GUIWindow`](src/gui/containers.rs#L414) | A draggable, closeable window container.  # Fields - `base` — `WidgetBase`. S... |
| `struct` | [`Layout`](src/gui/containers.rs#L122) | Flexbox-inspired layout container.  Arranges child widgets along a main axis ... |
| `struct` | [`NinePatch`](src/gui/containers.rs#L313) | Nine-slice data for scalable panel border rendering.  Given the source image ... |
| `struct` | [`Panel`](src/gui/containers.rs#L24) | Generic container widget that holds an ordered list of children.  The invisib... |
| `struct` | [`ScrollPanel`](src/gui/containers.rs#L234) | Scrollable viewport container.  Clips child content to its own bounds and off... |
| `struct` | [`SplitPanel`](src/gui/containers.rs#L461) | A resizable split panel with two child regions.  # Fields - `base` — `WidgetB... |
| `type` | [`NineSlice`](src/gui/containers.rs#L295) | A nine-slice rectangle: `(sx, sy, sw, sh, dx, dy, dw, dh)`. |

### `gui::context`

> Root widget tree, focus tracking, toast queue, and input routing.  [`GuiContext`] is the central coordinator for the GUI system.  It owns a flat pool of type-erased widgets (stored as [`WidgetKind`] enum variants), tracks which widget has keyboard focus, manages a queue of active [`Toast`](super::Toast) notifications, and optionally holds a [`Theme`] for styled rendering.  Widgets are identified by a `usize` index into the internal `widgets` vector.  The root panel is always at index `0`.  Container widgets (`Panel`, `Layout`, `ScrollPanel`) store their children's indices.  Input events are forwarded from Lua callbacks and dispatched to the widget tree by hit-testing against widget bounds.

*Source: [src/gui/context.rs](src/gui/context.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`WidgetKind`](src/gui/context.rs#L66) | Type-erased widget storage.  Each variant wraps a concrete widget type so tha... |
| `struct` | [`GuiContext`](src/gui/context.rs#L262) | Central GUI coordinator: widget pool, focus, toasts, and theme.  The context ... |

### `gui::controls`

> Interactive and display control widgets.  This sub-module provides the leaf-level widgets that users interact with or read information from: [`Button`], [`Label`], [`TextInput`], [`CheckBox`], [`Slider`], [`ProgressBar`], [`ComboBox`], [`ListBox`], and [`TabBar`].  Each widget embeds a [`WidgetBase`](super::WidgetBase) for shared properties and adds type-specific data fields.

*Source: [src/gui/controls.rs](src/gui/controls.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Button`](src/gui/controls.rs#L22) | Clickable button widget.  Displays a text label and triggers an `onClick` cal... |
| `struct` | [`CheckBox`](src/gui/controls.rs#L179) | Toggle check-box widget with an associated label.  Fires an `onChange` callba... |
| `struct` | [`ComboBox`](src/gui/controls.rs#L336) | Drop-down selection widget.  Maintains a list of string items and a 0-based s... |
| `struct` | [`Label`](src/gui/controls.rs#L56) | Static text label widget.  Displays read-only text.  Does not receive focus o... |
| `struct` | [`ListBox`](src/gui/controls.rs#L431) | Scrollable list of selectable items.  Maintains a list of string items, a 0-b... |
| `struct` | [`ProgressBar`](src/gui/controls.rs#L280) | Read-only progress indicator widget.  Displays a filled bar proportional to `... |
| `struct` | [`RadioButton`](src/gui/controls.rs#L589) | A grouped radio button with mutually exclusive selection.  # Fields - `base` ... |
| `struct` | [`ScrollBar`](src/gui/controls.rs#L630) | A scroll bar for scrollable content areas.  # Fields - `base` — `WidgetBase`.... |
| `struct` | [`Slider`](src/gui/controls.rs#L219) | Numeric value slider widget.  Allows the user to drag a thumb along a track t... |
| `struct` | [`TabBar`](src/gui/controls.rs#L522) | Tabbed page selector widget.  Manages a list of tab labels and tracks the cur... |
| `struct` | [`TextInput`](src/gui/controls.rs#L94) | Editable single-line text input field.  Supports placeholder text, maximum ch... |

### `gui::extras`

> Utility widgets: toast notifications, separators, spacers, and tree views.  These widgets serve auxiliary UI roles — visual dividers, empty spacing, auto-expiring notification overlays, and collapsible hierarchical trees.

*Source: [src/gui/extras.rs](src/gui/extras.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Accordion`](src/gui/extras.rs#L507) | A collapsible accordion container with named sections.  # Fields - `base` — `... |
| `struct` | [`AccordionSection`](src/gui/extras.rs#L491) | A single section in an [`Accordion`].  # Fields - `title` — `String`. Section... |
| `struct` | [`ColorPicker`](src/gui/extras.rs#L588) | A color picker widget with RGB/HSV/HSL modes.  # Fields - `base` — `WidgetBas... |
| `struct` | [`Dialog`](src/gui/extras.rs#L419) | A modal dialog window.  # Fields - `base` — `WidgetBase`. Shared widget prope... |
| `struct` | [`GUITable`](src/gui/extras.rs#L653) | A data table widget with sortable columns and selectable rows.  # Fields - `b... |
| `struct` | [`ImageWidget`](src/gui/extras.rs#L697) | An image display widget.  # Fields - `base` — `WidgetBase`. Shared widget pro... |
| `struct` | [`MenuBar`](src/gui/extras.rs#L340) | A horizontal menu bar.  # Fields - `base` — `WidgetBase`. Shared widget prope... |
| `struct` | [`MenuItem`](src/gui/extras.rs#L377) | A menu item usable in menus and context menus.  # Fields - `base` — `WidgetBa... |
| `struct` | [`Separator`](src/gui/extras.rs#L91) | Visual divider line widget.  Draws a horizontal or vertical line with a confi... |
| `struct` | [`Spacer`](src/gui/extras.rs#L135) | Empty layout filler widget.  Takes up space in a layout without rendering any... |
| `struct` | [`StatusBar`](src/gui/extras.rs#L456) | A status bar with named sections.  # Fields - `base` — `WidgetBase`. Shared w... |
| `struct` | [`TableColumn`](src/gui/extras.rs#L637) | A single column in a [`GUITable`].  # Fields - `header` — `String`. Column he... |
| `struct` | [`Toast`](src/gui/extras.rs#L21) | Auto-expiring notification overlay.  A toast displays a message for a configu... |
| `struct` | [`Toolbar`](src/gui/extras.rs#L306) | A toolbar container for buttons and separators.  # Fields - `base` — `WidgetB... |
| `struct` | [`TooltipPanel`](src/gui/extras.rs#L546) | A rich tooltip panel attached to a target widget.  # Fields - `base` — `Widge... |
| `struct` | [`TreeNode`](src/gui/extras.rs#L173) | A single node in a [`TreeView`] hierarchy.  # Fields - `text` — `String`. Dis... |
| `struct` | [`TreeView`](src/gui/extras.rs#L215) | Collapsible hierarchical tree widget.  Stores nodes in a flat `Vec` with pare... |

### `gui::theme`

> Per-widget-type, per-state styling system.  A [`Theme`] maps `(WidgetType, WidgetState)` pairs to [`WidgetStyle`] records.  When the GUI context draws a widget it looks up the style for the widget's type and current state, falling back to the `Normal` state style if no state-specific entry exists, and finally to a hard-coded default if the type has no theme entry at all.  The Lua API exposes `luna.gui.newTheme()`, `theme:setStyle()`, and `luna.gui.setTheme()` so game scripts can fully customise appearance without touching Rust.

*Source: [src/gui/theme.rs](src/gui/theme.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Theme`](src/gui/theme.rs#L68) | Theme registry that maps `(WidgetType, WidgetState)` pairs to [`WidgetStyle`]... |
| `struct` | [`WidgetStyle`](src/gui/theme.rs#L30) | Visual style record applied to a specific widget type in a specific state.  A... |

### `gui::widget`

> Shared widget base fields, state enum, and type tag.  Every concrete widget embeds a [`WidgetBase`] that provides position, size, visibility, enable state, padding, margin, z-order, min/max size constraints, anchor edges, and flexbox layout properties.  The [`WidgetState`] enum models the five visual states a widget can be in (normal, hovered, pressed, focused, disabled), and [`WidgetType`] tags each concrete kind so the theme system can key its style lookup.

*Source: [src/gui/widget.rs](src/gui/widget.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`WidgetState`](src/gui/widget.rs#L23) | Visual interaction state of a widget.  The GUI system transitions a widget th... |
| `enum` | [`WidgetType`](src/gui/widget.rs#L112) | Type tag identifying a concrete widget kind.  Used as a key (together with [`... |
| `struct` | [`WidgetBase`](src/gui/widget.rs#L256) | Shared base properties embedded by every concrete widget.  `WidgetBase` does ... |

## `image` {#image}

### `image`

> CPU-side pixel-level image manipulation.  Provides `ImageData` for reading and writing individual pixels in RGBA8 format, and `CompressedImageData` for holding DDS/DXT compressed GPU texture data.  This module is part of Luna2D's `image` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/image/mod.rs](src/image/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`compressed`](src/image/mod.rs#L17) | DDS/DXT compressed GPU texture data, loaded without CPU decompression. |
| `mod` | [`image_data`](src/image/mod.rs#L13) | RGBA8 pixel buffer with per-pixel read/write access. |

### `image::compressed`

> Compressed GPU texture data from DDS/DXT files.  Provides [`CompressedImageData`] for loading DXT1/DXT3/DXT5/BC7 textures from DDS files without CPU-side decompression.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/image/compressed.rs](src/image/compressed.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CompressedFormat`](src/image/compressed.rs#L23) | GPU-compressed texture format identifier.  # Variants - `Dxt1` — Dxt1 variant... |
| `struct` | [`CompressedImageData`](src/image/compressed.rs#L66) | CPU-side holder for GPU-compressed texture data loaded from a DDS file.  Does... |

### `image::image_data`

> CPU-side RGBA8 pixel buffer for image manipulation.  This module is part of Luna2D's `image` subsystem and provides the implementation details for image data-related operations and data management. Key types exported from this module: `ImageData`. Primary functions: `new()`, `from_file()`, `from_bytes()`, `width()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/image/image_data.rs](src/image/image_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ImageData`](src/image/image_data.rs#L23) | CPU-side pixel buffer in RGBA8 format. Consult the module-level documentation... |

## `input` {#input}

### `input`

> Mod implementation for the `input` subsystem.  This module is part of Luna2D's `input` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/input/mod.rs](src/input/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`gamepad`](src/input/mod.rs#L10) | Gamepad button and axis state for a single controller. |
| `mod` | [`keyboard`](src/input/mod.rs#L12) | Keyboard state tracking pressed, held, and released keys. |
| `mod` | [`mouse`](src/input/mod.rs#L14) | Mouse position and button state management. |
| `mod` | [`touch`](src/input/mod.rs#L16) | Touch input state tracking for touchscreens. |

### `input::gamepad`

> Gamepad implementation for the `input` subsystem.  This module is part of Luna2D's `input` subsystem and provides the implementation details for gamepad-related operations and data management. Key types exported from this module: `GamepadState`, `GamepadMappings`. Primary functions: `new()`, `update_button()`, `update_axis()`, `is_button_pressed()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/input/gamepad.rs](src/input/gamepad.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`gilrs_axis_to_string`](src/input/gamepad.rs#L208) | Converts a `gilrs::Axis` to a engine-compatible string name.  # Parameters - ... |
| `fn` | [`gilrs_button_to_string`](src/input/gamepad.rs#L180) | Converts a `gilrs::Button` to a engine-compatible string name.  # Parameters ... |
| `struct` | [`GamepadMappings`](src/input/gamepad.rs#L228) | Stores SDL2 GameControllerDB-format mapping strings keyed by GUID.  Each entr... |
| `struct` | [`GamepadState`](src/input/gamepad.rs#L23) | Holds the current button and axis state for a single gamepad identified by it... |

### `input::keyboard`

> Keyboard implementation for the `input` subsystem.  This module is part of Luna2D's `input` subsystem and provides the implementation details for keyboard-related operations and data management. Key types exported from this module: `KeyboardState`. Primary functions: `new()`, `begin_frame()`, `press_scancode()`, `release_scancode()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/input/keyboard.rs](src/input/keyboard.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `const` | [`MOD_ALT`](src/input/keyboard.rs#L18) | Bitmask constant for the Alt/Option modifier key. |
| `const` | [`MOD_CTRL`](src/input/keyboard.rs#L16) | Bitmask constant for the Control modifier key. |
| `const` | [`MOD_META`](src/input/keyboard.rs#L20) | Bitmask constant for the Super/Meta modifier key. |
| `const` | [`MOD_SHIFT`](src/input/keyboard.rs#L14) | Bitmask constant for the Shift modifier key. |
| `fn` | [`get_key_from_scancode`](src/input/keyboard.rs#L315) | Resolves a physical scancode string to the closest logical Luna key name.  # ... |
| `fn` | [`get_scancode_from_key`](src/input/keyboard.rs#L283) | Resolves a logical Luna key name to the closest physical scancode string.  # ... |
| `fn` | [`winit_key_to_string`](src/input/keyboard.rs#L349) | Converts a `winit 0.30` logical `Key` to the lowercase string name used by th... |
| `fn` | [`winit_scancode_to_string`](src/input/keyboard.rs#L400) | Converts a `winit 0.30` physical `KeyCode` to a engine-compatible scancode st... |
| `struct` | [`KeyboardState`](src/input/keyboard.rs#L37) | Tracks which keyboard keys are currently down, just pressed, or just released... |

### `input::mouse`

> Mouse implementation for the `input` subsystem.  This module is part of Luna2D's `input` subsystem and provides the implementation details for mouse-related operations and data management. Key types exported from this module: `SystemCursor`, `MouseState`. Primary functions: `from_name()`, `as_str()`, `new()`, `begin_frame()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/input/mouse.rs](src/input/mouse.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`CursorKind`](src/input/mouse.rs#L336) | The cursor type — either a named system icon or user-supplied pixel data.  # ... |
| `enum` | [`SystemCursor`](src/input/mouse.rs#L26) | Standard system cursor shapes. Consult the module-level documentation for the... |
| `fn` | [`is_cursor_supported`](src/input/mouse.rs#L368) | Returns whether cursor customisation is supported on this platform.  # Return... |
| `struct` | [`CursorHandle`](src/input/mouse.rs#L359) | A held cursor — either a system cursor icon or custom pixel-data cursor.  # F... |
| `struct` | [`MouseState`](src/input/mouse.rs#L116) | Tracks mouse cursor position and per-button pressed/down/released state.  # F... |

### `input::touch`

> Touch input state tracking for Luna2D.  This module is part of Luna2D's `input` subsystem and provides the implementation details for touch-related operations and data management. Key types exported from this module: `TouchPoint`, `TouchState`. Primary functions: `new()`, `touch_start()`, `touch_move()`, `touch_end()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/input/touch.rs](src/input/touch.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`TouchPoint`](src/input/touch.rs#L21) | Information about a single touch point. Consult the module-level documentatio... |
| `struct` | [`TouchState`](src/input/touch.rs#L37) | Tracks active touch points. Consult the module-level documentation for the br... |

## `inventory` {#inventory}

### `inventory`

> Inventory system — slot-based item containers, equipment slots, item sets, subsystem toggles (weight, size, stacking, sets), and callback hooks.  This module is part of Luna2D's `inventory` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/inventory/mod.rs](src/inventory/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`container`](src/inventory/mod.rs#L19) | Item containers. |
| `mod` | [`inventory`](src/inventory/mod.rs#L28) | Top-level inventory with subsystem management. |
| `mod` | [`item`](src/inventory/mod.rs#L11) | Item definition and item stacks. |
| `mod` | [`item_set`](src/inventory/mod.rs#L23) | Item sets, set requirements, subsystem flags. |
| `mod` | [`slot`](src/inventory/mod.rs#L15) | Inventory slot and slot state. |

### `inventory::container`

> InventoryEntry containers with configurable modes.  This module is part of Luna2D's `inventory` subsystem and provides the implementation details for container-related operations and data management. Key types exported from this module: `ContainerMode`, `Container`. Primary functions: `from_str()`, `as_str()`, `new()`, `slot_count()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/inventory/container.rs](src/inventory/container.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ContainerMode`](src/inventory/container.rs#L26) | Container storage mode. Consult the module-level documentation for the broade... |
| `struct` | [`Container`](src/inventory/container.rs#L75) | A named collection of `Slot`s. Consult the module-level documentation for the... |

### `inventory::inventory`

> Inventory: the top-level item storage with subsystem management.  This module is part of Luna2D's `inventory` subsystem and provides the implementation details for inventory-related operations and data management. Key types exported from this module: `SubsystemFlags`, `Inventory`. Primary functions: `new()`, `add_container()`, `get_container()`, `get_container_mut()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/inventory/inventory.rs](src/inventory/inventory.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Inventory`](src/inventory/inventory.rs#L51) | Top-level inventory managing containers, equip slots, item sets, and callback... |
| `struct` | [`SubsystemFlags`](src/inventory/inventory.rs#L30) | Active subsystem flags for an `Inventory`.  # Fields - `weight` — `bool`. - `... |

### `inventory::item`

> Item definition and item stack.  This module is part of Luna2D's `inventory` subsystem and provides the implementation details for item-related operations and data management. Key types exported from this module: `InventoryEntry`, `ItemStack`. Primary functions: `new()`, `has_tag()`, `add_tag()`, `remove_tag()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/inventory/item.rs](src/inventory/item.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`InventoryEntry`](src/inventory/item.rs#L25) | A single item definition with type, tags, weight, size, and stack limit.  # F... |
| `struct` | [`ItemStack`](src/inventory/item.rs#L114) | A counted stack of a single Item type.  # Fields - `item` — `InventoryEntry`.... |

### `inventory::item_set`

> Item sets, set requirements, and subsystem flags.  This module is part of Luna2D's `inventory` subsystem and provides the implementation details for item set-related operations and data management. Key types exported from this module: `SetRequirement`, `ItemSet`. Primary functions: `new()`, `add_requirement()`, `is_active()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/inventory/item_set.rs](src/inventory/item_set.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ItemSet`](src/inventory/item_set.rs#L37) | A named set defining bonus conditions: all requirements must be satisfied sim... |
| `struct` | [`SetRequirement`](src/inventory/item_set.rs#L24) | A single requirement in an `ItemSet`. Consult the module-level documentation ... |

### `inventory::slot`

> Inventory slots and slot state.  This module is part of Luna2D's `inventory` subsystem and provides the implementation details for slot-related operations and data management. Key types exported from this module: `SlotState`, `Slot`. Primary functions: `from_str()`, `as_str()`, `new()`, `is_empty()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/inventory/slot.rs](src/inventory/slot.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`SlotState`](src/inventory/slot.rs#L25) | Valid state strings for a slot. Consult the module-level documentation for th... |
| `struct` | [`Slot`](src/inventory/slot.rs#L74) | A single inventory position that holds an optional `ItemStack`.  # Fields - `... |

## `item` {#item}

### `item`

> Generic item-centric data structures.  Provides the building blocks for any system that manages a set of named, tagged, stat-carrying objects (items) organised into stacks/piles.  # Key types  | Type | Purpose | |---|---| | [`Item`] | A named game object with stats, tags, counters, metadata | | [`ItemTypeDef`] | Blueprint/template registered in the global type registry | | [`Stack`] | Ordered collection of items (draw pile, hand, queue, …) | | [`StackBuilder`] | Template-based stack construction with validation | | [`StackManager`] | Collection of named stacks; coordinates movement | | [`ItemPool`] | Weighted random pool of item types | | [`Slot`] | Bounded single-position holder for items | | [`StackHistory`] | Append-only change log | | [`ItemGroup`] | Index-list for grouping analysis results |  # No gameplay assumptions  This module deliberately contains no player, turn, trick, rule, or scoring semantics.  All such logic belongs in Lua scripts that use these structures.

*Source: [src/item/mod.rs](src/item/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`builder`](src/item/mod.rs#L31) | Builder sub-module. |
| `mod` | [`group`](src/item/mod.rs#L39) | Group sub-module. |
| `mod` | [`history`](src/item/mod.rs#L41) | History sub-module. |
| `mod` | [`item`](src/item/mod.rs#L27) | Item sub-module. |
| `mod` | [`manager`](src/item/mod.rs#L33) | Manager sub-module. |
| `mod` | [`pool`](src/item/mod.rs#L35) | Pool sub-module. |
| `mod` | [`slot`](src/item/mod.rs#L37) | Slot sub-module. |
| `mod` | [`stack`](src/item/mod.rs#L29) | Stack sub-module. |

### `item::builder`

> Stack builder — template-based stack construction with validation.  A `StackBuilder` accumulates `BuildEntry` records (item type + count + optional overrides) and either builds a ready-to-use `Stack` or validates an existing stack against construction constraints.

*Source: [src/item/builder.rs](src/item/builder.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BuildEntry`](src/item/builder.rs#L24) | One "slot" in a stack template: an item type plus a count and optional overri... |
| `struct` | [`StackBuilder`](src/item/builder.rs#L79) | Builds a `Stack` from `BuildEntry` templates and validates construction const... |

### `item::group`

> Group analysis — stat-based and tag-based grouping of item slices.  All functions operate on plain `&[Item]` slices and return index-based groups so that the caller can decide what to do with them without taking ownership.  These utilities are domain-agnostic: "n-of-a-stat" works for poker hands, board game scoring, loot classification, or any other scenario.

*Source: [src/item/group.rs](src/item/group.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`find_at_least_n_of_stat`](src/item/group.rs#L152) | Find all groups where at least `n` items share the same integer stat value.  ... |
| `fn` | [`find_n_of_stat`](src/item/group.rs#L131) | Find all maximal groups where items share the exact same integer stat value, ... |
| `fn` | [`find_sequences`](src/item/group.rs#L176) | Find all runs (sequences of consecutive integer stat values) of length ≥ `min... |
| `fn` | [`find_tag_groups`](src/item/group.rs#L224) | Find groups of items that all share the same tag-prefix value and contain at ... |
| `fn` | [`group_by_category`](src/item/group.rs#L79) | Group item indices by category. Consult the module-level documentation for th... |
| `fn` | [`group_by_stat`](src/item/group.rs#L62) | Group item indices by the integer part of a named stat.  Returns a map from `... |
| `fn` | [`group_by_tag_prefix`](src/item/group.rs#L100) | Group item indices by a tag prefix (the part before the first `:`).  Tags mat... |
| `fn` | [`sorted_indices_by_category`](src/item/group.rs#L274) | Return sorted indices grouped alphabetically by category.  # Parameters - `it... |
| `fn` | [`sorted_indices_by_stat`](src/item/group.rs#L251) | Return a sorted list of 0-based indices; does not modify the slice.  `ascendi... |
| `struct` | [`ItemGroup`](src/item/group.rs#L26) | A labelled subset of an item slice, referenced by index.  Indices refer to po... |

### `item::history`

> Change history — append-only log of stack mutations.  `StackHistory` is kept separate from `Stack` so that the caller decides what (if anything) to track.  Wrap it in an `Rc<RefCell<StackHistory>>` and pass it around alongside stacks.

*Source: [src/item/history.rs](src/item/history.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`HistoryAction`](src/item/history.rs#L28) | The category of change that was recorded.  # Variants - `An` — An variant. - ... |
| `struct` | [`HistoryEntry`](src/item/history.rs#L70) | A single entry in the history log. Consult the module-level documentation for... |
| `struct` | [`StackHistory`](src/item/history.rs#L92) | Append-only change log with an optional rolling size limit.  # Fields - `entr... |

### `item::item`

> Generic item type definitions, the global type registry, and item instances.  An `Item` is a named bundle of stats, tags, counters, and metadata. The engine never interprets these fields — all semantic meaning is user-defined in Lua.  An item might represent a card, tile, token, ingredient, power-up, or any other game object.

*Source: [src/item/item.rs](src/item/item.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`clear_item_types`](src/item/item.rs#L98) | Remove all entries from the registry. After this call the container is in the... |
| `fn` | [`define_item_type`](src/item/item.rs#L73) | Register (or overwrite) an item type in the thread-local type registry.  # Pa... |
| `fn` | [`get_item_type`](src/item/item.rs#L85) | Look up an item type definition by name. Returns `None` if not found.  # Para... |
| `fn` | [`get_item_type_names`](src/item/item.rs#L93) | Return all registered type names. This accessor incurs no allocation; call it... |
| `struct` | [`Item`](src/item/item.rs#L122) | A single item instance. Consult the module-level documentation for the broade... |
| `struct` | [`ItemTypeDef`](src/item/item.rs#L27) | Template that describes a class of items (the "blueprint").  Stats, tags, and... |

### `item::manager`

> Stack manager — organises multiple named `Stack` instances as a unit.  Useful for modelling a board where several stacks (draw pile, hand, discard, play area, etc.) coexist and items need to move between them.

*Source: [src/item/manager.rs](src/item/manager.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`StackManager`](src/item/manager.rs#L17) | Manages a collection of named `Stack` instances.  All stack names and their s... |

### `item::pool`

> Weighted item-type pool for random selection.  An `ItemPool` holds a set of item type names each with a relative draw weight.  Useful for loot tables, draft formats, booster generation, or any weighted-random-choice scenario.

*Source: [src/item/pool.rs](src/item/pool.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ItemPool`](src/item/pool.rs#L28) | A pool of item types for weighted random draws.  # Fields - `name` — `String`... |
| `struct` | [`PoolEntry`](src/item/pool.rs#L15) | A single entry in an item pool. Consult the module-level documentation for th... |

### `item::slot`

> Slot — a bounded named position that holds a small set of items.  A slot is like a named cell on a board: it has an optional capacity limit and enforces it on push.  Useful for equipment slots, board positions, hand slots, or any place where "there can only be N items here" matters.

*Source: [src/item/slot.rs](src/item/slot.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Slot`](src/item/slot.rs#L18) | A bounded named position holding zero or more items.  Semantics (what "this s... |

### `item::stack`

> Ordered item collection — a stack, pile, hand, queue, or any linear sequence of items.  The last element is the "top" and the first element is the "bottom". All semantics (draw pile, discard pile, hand, queue) are user-defined.

*Source: [src/item/stack.rs](src/item/stack.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Stack`](src/item/stack.rs#L17) | An ordered collection of items. Consult the module-level documentation for th... |

## `lua_api` {#lua-api}

### `lua_api`

> Lua API binding bridge for the Luna2D engine.  This is the integration layer that registers all `luna.*` API sub-modules on top of the types defined in `engine`. `SharedState`, `WindowState`, `FullscreenType`, and `ErrorInfo` are defined in `engine::shared_state` and re-exported here for sub-module convenience.  The primary entry point is `create_lua_vm()` which constructs a configured LuaJIT VM with every `luna.*` namespace bound.

*Source: [src/lua_api/mod.rs](src/lua_api/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`create_lua_vm`](src/lua_api/mod.rs#L132) | Creates and configures the Lua VM, registers all `luna.*` sub-APIs, and retur... |
| `mod` | [`ai_api`](src/lua_api/mod.rs#L19) | Registers the `luna.ai.*` game AI toolkit API. |
| `mod` | [`audio_api`](src/lua_api/mod.rs#L23) | Registers the `luna.audio.*` sound playback API. |
| `mod` | [`automation_api`](src/lua_api/mod.rs#L21) | Registers the `luna.simulator.*` automated input simulation API. |
| `mod` | [`battle_api`](src/lua_api/mod.rs#L99) | Registers the `luna.battle.*` turn-based battle API. |
| `mod` | [`cardgame_api`](src/lua_api/mod.rs#L101) | Registers the `luna.cardgame.*` card game backend API. |
| `mod` | [`combat_api`](src/lua_api/mod.rs#L103) | Registers the `luna.combat.*` combat system API. |
| `mod` | [`compute_api`](src/lua_api/mod.rs#L25) | Registers the `luna.compute.*` array computation API. |
| `mod` | [`crafting_api`](src/lua_api/mod.rs#L105) | Registers the `luna.crafting.*` crafting system API. |
| `mod` | [`data_api`](src/lua_api/mod.rs#L27) | Registers the `luna.data.*` binary data, compression, hashing, and encoding API. |
| `mod` | [`dataframe_api`](src/lua_api/mod.rs#L29) | Registers the `luna.dataframe.*` tabular data API. |
| `mod` | [`debug_api`](src/lua_api/mod.rs#L31) | Registers the `luna.debug.*` runtime diagnostics and developer tools API. |
| `mod` | [`debugbridge_api`](src/lua_api/mod.rs#L33) | Registers the `luna.debugbridge.*` TCP debug server API. |
| `mod` | [`dialog_api`](src/lua_api/mod.rs#L35) | Registers the `luna.dialog.*` dialog sequencer API. |
| `mod` | [`docs_api`](src/lua_api/mod.rs#L37) | Registers the `luna.docs.*` documentation management API. |
| `mod` | [`economy_api`](src/lua_api/mod.rs#L107) | Registers the `luna.economy.*` named resource economy API. |
| `mod` | [`entity_api`](src/lua_api/mod.rs#L39) | Registers the `luna.entity.*` ECS universe API. |
| `mod` | [`event_api`](src/lua_api/mod.rs#L41) | Registers the `luna.event.*` engine control API. |
| `mod` | [`filesystem_api`](src/lua_api/mod.rs#L43) | Registers the `luna.filesystem.*` sandboxed I/O API. |
| `mod` | [`font_api`](src/lua_api/mod.rs#L47) | Registers the `luna.font.*` font rasterizer and glyph metrics API. |
| `mod` | [`graph_api`](src/lua_api/mod.rs#L45) | Registers the `luna.graph.*` directed-graph and item-flow simulation API. |
| `mod` | [`graphics_api`](src/lua_api/mod.rs#L49) | Registers the `luna.graphics.*` drawing API. |
| `mod` | [`gui_api`](src/lua_api/mod.rs#L51) | Registers the `luna.gui.*` retained-mode widget UI API. |
| `mod` | [`image_api`](src/lua_api/mod.rs#L53) | Registers the `luna.image.*` pixel-level image manipulation API. |
| `mod` | [`input_api`](src/lua_api/mod.rs#L55) | Registers the `luna.keyboard.*` and `luna.mouse.*` input API. |
| `mod` | [`inventory_api`](src/lua_api/mod.rs#L109) | Registers the `luna.inventory.*` inventory system API. |
| `mod` | [`localization_api`](src/lua_api/mod.rs#L57) | Registers the `luna.localization.*` internationalization API. |
| `mod` | [`log_api`](src/lua_api/mod.rs#L59) | Registers the `luna.log.*` structured game-level logging API. |
| `mod` | [`lua_types`](src/lua_api/mod.rs#L113) | UserData type utilities for Luna2D Lua objects. |
| `mod` | [`math_api`](src/lua_api/mod.rs#L61) | Registers the `luna.math.*` vector and math helper API. |
| `mod` | [`minimap_api`](src/lua_api/mod.rs#L65) | Registers the `luna.minimap.*` minimap API. |
| `mod` | [`modding_api`](src/lua_api/mod.rs#L67) | Registers the `luna.modding.*` mod management API. |
| `mod` | [`overlay_api`](src/lua_api/mod.rs#L79) | Registers the `luna.overlay.*` screen-effect overlay API. |
| `mod` | [`particle_api`](src/lua_api/mod.rs#L69) | Registers the `luna.particle.*` particle-effects API. |
| `mod` | [`pathfinding_api`](src/lua_api/mod.rs#L71) | Registers the `luna.pathfinding.*` grid-based pathfinding API. |
| `mod` | [`patterns_api`](src/lua_api/mod.rs#L73) | Registers the `luna.patterns.*` software design patterns API. |
| `mod` | [`physics_api`](src/lua_api/mod.rs#L75) | Registers the `luna.physics.*` rigid-body simulation API. |
| `mod` | [`postfx_api`](src/lua_api/mod.rs#L77) | Registers the `luna.postfx.*` post-processing effects API. |
| `mod` | [`quest_api`](src/lua_api/mod.rs#L115) | Registers the `luna.quest.*` quest log and objective tracking API. |
| `mod` | [`savegame_api`](src/lua_api/mod.rs#L81) | Registers the `luna.savegame.*` save/load system API. |
| `mod` | [`scene_api`](src/lua_api/mod.rs#L83) | Registers the `luna.scene.*` scene stack, registry, data store, and depth-sor... |
| `mod` | [`sprite_api`](src/lua_api/mod.rs#L85) | Registers Phase 24 `luna.graphics.*` extension types (Light2D, Camera2D, etc.). |
| `mod` | [`stats_api`](src/lua_api/mod.rs#L117) | Registers the `luna.stats.*` character stats API. |
| `mod` | [`steering_api`](src/lua_api/mod.rs#L87) | Registers the `luna.steering.*` AI steering behaviours API. |
| `mod` | [`system_api`](src/lua_api/mod.rs#L91) | Registers the `luna.system.*` platform query API. |
| `mod` | [`thread_api`](src/lua_api/mod.rs#L93) | Registers the `luna.thread.*` multithreading API. |
| `mod` | [`tilemap_api`](src/lua_api/mod.rs#L119) | Registers the `luna.tilemap.*` tile map, tileset, autotile, and procedural ge... |
| `mod` | [`timer_api`](src/lua_api/mod.rs#L121) | Registers the `luna.timer.*` frame-timing API. |
| `mod` | [`window_api`](src/lua_api/mod.rs#L123) | Registers the `luna.window.*` window management API. |

### `lua_api::ai_api`

> Registers the `luna.ai.*` game AI toolkit API.  Provides factory functions and UserData wrappers for AI subsystems: AIWorld, Agent, Blackboard, StateMachine, BehaviorTree, SteeringManager, QLearner, UtilityAI, GOAPPlanner, InfluenceMap, Squad, and CommandQueue.

*Source: [src/lua_api/ai_api.rs](src/lua_api/ai_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/ai_api.rs#L1994) | Registers the `luna.ai.*` game AI toolkit API.  # Parameters - `lua` — `&Lua`... |

### `lua_api::audio_api`

> Audio Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for audio api-related operations and data management. Key types exported from this module: `LuaSource`, `LuaBus`, `LuaMidiPlayer`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/audio_api.rs](src/lua_api/audio_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/audio_api.rs#L1057) | Registers all `luna.audio.*` functions into the Lua VM.  # Parameters - `lua`... |
| `struct` | [`LuaBus`](src/lua_api/audio_api.rs#L328) | Lua UserData wrapper for an audio bus. Consult the module-level documentation... |
| `struct` | [`LuaDecoder`](src/lua_api/audio_api.rs#L960) | Lua UserData wrapper for a streaming audio `Decoder`.  # Fields - `inner` — `... |
| `struct` | [`LuaMidiPlayer`](src/lua_api/audio_api.rs#L427) | Lua UserData wrapper for the MIDI player.  # Fields - `inner` — `Rc<RefCell<M... |
| `struct` | [`LuaSource`](src/lua_api/audio_api.rs#L33) | Lua UserData wrapper for an audio source resource.  # Fields - `state` — `Rc<... |

### `lua_api::automation_api`

> Automation Api implementation for the `lua_api` subsystem.  Registers the `luna.simulator.*` namespace for automated input simulation via timed step scripts. Scripts inject synthetic input events into the engine's event queue during playback.  ## Exposed Lua API  | Function | Purpose | |---|---| | `luna.simulator.load(name, data)` | Load a script from a Lua step table | | `luna.simulator.loadFromToml(name, toml_str)` | Load a script from a TOML string | | `luna.simulator.loadFile(name, path)` | Load a script from a TOML file on disk | | `luna.simulator.unload(name)` | Remove a loaded script by name | | `luna.simulator.hasScript(name)` | Return `true` if a script is registered | | `luna.simulator.getScripts()` | Return an array of all registered script names | | `luna.simulator.start(name)` | Start playback from step zero | | `luna.simulator.stop()` | Stop playback and reset to `Idle` | | `luna.simulator.pause()` | Pause playback at the current position | | `luna.simulator.resume()` | Resume from a paused position | | `luna.simulator.update(dt)` | Advance the clock and dispatch due steps | | `luna.simulator.isRunning()` | Return `true` if actively playing | | `luna.simulator.isPaused()` | Return `true` if paused | | `luna.simulator.isComplete()` | Return `true` if all steps dispatched | | `luna.simulator.getCurrentStep()` | Return the index of the next step | | `luna.simulator.getStepCount()` | Return the total step count | | `luna.simulator.getCurrentScript()` | Return the active script name or `nil` | | `luna.simulator.getElapsedTime()` | Return seconds elapsed since `start` |  ## Implementation Pattern  A single `Rc<RefCell<Simulator>>` is created at registration time and captured by all closures via Rc clone. The `SharedState` borrow is held only long enough to push events — never across yield points.

*Source: [src/lua_api/automation_api.rs](src/lua_api/automation_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/automation_api.rs#L111) | Register the `luna.simulator` namespace.  # Parameters - `lua` \u2014 `&Lua`.... |

### `lua_api::battle_api`

> Lua bindings for the `luna.battle.*` turn-based battle API.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for battle api-related operations and data management. Key types exported from this module: `LuaStatusEffect`, `LuaCombatAction`, `LuaCombatant`, `LuaCombatBattle`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/battle_api.rs](src/lua_api/battle_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/battle_api.rs#L540) | Registers the `luna.battle.*` table. Panics in debug mode if the same entity ... |
| `struct` | [`LuaCombatAction`](src/lua_api/battle_api.rs#L71) | Lua wrapper for [`CombatAction`]. Consult the module-level documentation for ... |
| `struct` | [`LuaCombatBattle`](src/lua_api/battle_api.rs#L396) | Lua wrapper for [`CombatBattle`]. Consult the module-level documentation for ... |
| `struct` | [`LuaCombatant`](src/lua_api/battle_api.rs#L162) | Lua wrapper for [`Combatant`]. Consult the module-level documentation for the... |
| `struct` | [`LuaStatusEffect`](src/lua_api/battle_api.rs#L21) | Lua wrapper for [`StatusEffect`]. Consult the module-level documentation for ... |

### `lua_api::cardgame_api`

> Lua bindings for `luna.cardgame.*`.  Exposes Card, Stack, StackBuilder, StackManager, CardPool, Slot, StackHistory, and group-analysis utilities to Lua scripts via the `luna.cardgame` namespace.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for cardgame api-related operations and data management. Key types exported from this module: `LuaCard`, `LuaStack`, `LuaDeckBuilder`, `LuaZoneManager`, `LuaCardPool`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/cardgame_api.rs](src/lua_api/cardgame_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/cardgame_api.rs#L1849) | Registers the `luna.cardgame.*` API. Panics in debug mode if the same entity ... |
| `struct` | [`LuaCard`](src/lua_api/cardgame_api.rs#L41) | Lua-facing `Card` userdata with per-instance script and asset storage.  # Fie... |
| `struct` | [`LuaCardPool`](src/lua_api/cardgame_api.rs#L1323) | Lua-facing `CardPool` userdata. Consult the module-level documentation for th... |
| `struct` | [`LuaDeckBuilder`](src/lua_api/cardgame_api.rs#L944) | Lua-facing `StackBuilder` userdata with optional Lua custom validation rules.... |
| `struct` | [`LuaEffectStack`](src/lua_api/cardgame_api.rs#L1653) | Lua-facing LIFO effect stack.  Each entry is a Lua table describing an effect... |
| `struct` | [`LuaHistory`](src/lua_api/cardgame_api.rs#L1583) | Lua-facing `StackHistory` userdata. Consult the module-level documentation fo... |
| `struct` | [`LuaSlot`](src/lua_api/cardgame_api.rs#L1481) | Lua-facing `Slot` userdata. Consult the module-level documentation for the br... |
| `struct` | [`LuaStack`](src/lua_api/cardgame_api.rs#L567) | Lua-facing `Stack` userdata. Consult the module-level documentation for the b... |
| `struct` | [`LuaZoneManager`](src/lua_api/cardgame_api.rs#L1225) | Lua-facing `StackManager` userdata. Consult the module-level documentation fo... |

### `lua_api::combat_api`

> Lua bindings for the `luna.combat.*` vehicle combat API.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for combat api-related operations and data management. Key types exported from this module: `LuaCollisionGroupSet`, `LuaChassis`, `LuaTurret`, `LuaWeapon`, `LuaProjectile`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/combat_api.rs](src/lua_api/combat_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/combat_api.rs#L661) | Register the `luna.combat.*` vehicle combat table.  # Parameters - `lua` — `&... |
| `struct` | [`LuaChassis`](src/lua_api/combat_api.rs#L120) | Lua wrapper for `Chassis`. Consult the module-level documentation for the bro... |
| `struct` | [`LuaCollisionGroupSet`](src/lua_api/combat_api.rs#L54) | Lua wrapper for `CollisionGroupSet`. Consult the module-level documentation f... |
| `struct` | [`LuaCombatWorld`](src/lua_api/combat_api.rs#L558) | Lua wrapper for `CombatWorld`. Consult the module-level documentation for the... |
| `struct` | [`LuaProjectile`](src/lua_api/combat_api.rs#L446) | Lua wrapper for `Projectile`. Consult the module-level documentation for the ... |
| `struct` | [`LuaProjectilePool`](src/lua_api/combat_api.rs#L508) | Lua wrapper for `ProjectilePool`. Consult the module-level documentation for ... |
| `struct` | [`LuaTurret`](src/lua_api/combat_api.rs#L245) | Lua wrapper for `Turret`. Consult the module-level documentation for the broa... |
| `struct` | [`LuaWeapon`](src/lua_api/combat_api.rs#L312) | Lua wrapper for fire-rate-based `Weapon`. |

### `lua_api::compute_api`

> Registers the `luna.compute.*` array computation API.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for compute api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/compute_api.rs](src/lua_api/compute_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/compute_api.rs#L594) | Registers the `luna.compute` table with array factory functions.  # Parameter... |

### `lua_api::crafting_api`

> Lua bindings for `luna.crafting.*`.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for crafting api-related operations and data management. Key types exported from this module: `LuaRecipe`, `LuaRecipeRegistry`, `LuaStation`, `LuaCraftSkill`, `LuaCraftQueue`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/crafting_api.rs](src/lua_api/crafting_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/crafting_api.rs#L1994) | Register the `luna.crafting.*` table. Panics in debug mode if the same entity... |
| `struct` | [`LuaCraftQueue`](src/lua_api/crafting_api.rs#L1100) | Lua-callable handle wrapping [`CraftQueue`]. |
| `struct` | [`LuaCraftSkill`](src/lua_api/crafting_api.rs#L877) | Lua-callable handle wrapping [`CraftSkill`]. |
| `struct` | [`LuaModifierPool`](src/lua_api/crafting_api.rs#L1867) | Lua-callable handle wrapping [`ModifierPool`]. |
| `struct` | [`LuaRecipe`](src/lua_api/crafting_api.rs#L24) | Lua-callable handle wrapping [`Recipe`]. |
| `struct` | [`LuaRecipeGroup`](src/lua_api/crafting_api.rs#L1761) | Lua-callable handle wrapping [`RecipeGroup`]. |
| `struct` | [`LuaRecipeKnowledge`](src/lua_api/crafting_api.rs#L1590) | Lua-callable handle wrapping [`RecipeKnowledge`]. |
| `struct` | [`LuaRecipeRegistry`](src/lua_api/crafting_api.rs#L419) | Lua-callable handle wrapping [`RecipeRegistry`]. |
| `struct` | [`LuaStation`](src/lua_api/crafting_api.rs#L609) | Lua-callable handle wrapping [`Station`]. |
| `struct` | [`LuaUpgradeTree`](src/lua_api/crafting_api.rs#L1311) | Lua-callable handle wrapping [`UpgradeTree`]. |

### `lua_api::data_api`

> Registers the `luna.data.*` binary data, compression, hashing, and encoding API.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for data api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/data_api.rs](src/lua_api/data_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/data_api.rs#L24) | Registers the `luna.data` table on the provided `luna` namespace.  # Paramete... |

### `lua_api::dataframe_api`

> Registers the `luna.dataframe.*` tabular data API.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for dataframe api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/dataframe_api.rs](src/lua_api/dataframe_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/dataframe_api.rs#L878) | Register the `luna.dataframe` namespace.  # Parameters - `lua` — `&Lua`. - `l... |

### `lua_api::debug_api`

> Registers the `luna.devtools.*` runtime diagnostics and developer tools API.  Five subsystems: structured logger, hierarchical profiler, frame statistics, file watcher, and Lua debug bridge. All module-level functions — no registered types.

*Source: [src/lua_api/debug_api.rs](src/lua_api/debug_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/debug_api.rs#L201) | Registers the `luna.devtools` namespace.  # Parameters - `lua` — `&Lua`. - `l... |

### `lua_api::debugbridge_api`

> Registers the `luna.debugbridge.*` TCP debug server API.  Embeds a JSON-over-TCP server (127.0.0.1 only) inside the running game. External tools (VS Code extension, MCP server) connect to inspect and control the game at runtime. Network I/O runs on a background thread; Lua-dependent methods are queued and dispatched via `poll()` on the main thread.

*Source: [src/lua_api/debugbridge_api.rs](src/lua_api/debugbridge_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/debugbridge_api.rs#L360) | Registers the `luna.debugbridge` namespace.  # Parameters - `lua` — `&Lua`. -... |

### `lua_api::dialog_api`

> Lua API bindings for the `luna.dialog.*` dialog sequencer module.  Provides a `Sequencer` UserData type for visual novel-style text presentation with typewriter effect, branching choices, timed pauses, and inline callbacks.

*Source: [src/lua_api/dialog_api.rs](src/lua_api/dialog_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/dialog_api.rs#L409) | Registers the `luna.dialog` module with the Lua VM.  # Parameters - `lua` — `... |
| `struct` | [`LuaSequencer`](src/lua_api/dialog_api.rs#L49) | Lua UserData wrapper for a dialog sequencer.  # Fields - `inner` — `Rc<RefCel... |

### `lua_api::docs_api`

> Registers the `luna.docs.*` documentation management API.  Provides engine-integrated documentation management: scanning runtime bindings, loading TOML doc catalogs, validating completeness, computing quality metrics, and exporting structured data for VS Code extension IntelliSense.

*Source: [src/lua_api/docs_api.rs](src/lua_api/docs_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/docs_api.rs#L960) | Registers the `luna.docs` namespace. Panics in debug mode if the same entity ... |

### `lua_api::economy_api`

> Lua API bindings for the `luna.economy.*` named resource economy module.  Provides `ResourceManager` UserData for managing named resources with capacity, flow rates, decay, interest, conversions, and overflow policies.

*Source: [src/lua_api/economy_api.rs](src/lua_api/economy_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/economy_api.rs#L903) | Registers the `luna.resource` module with the Lua VM.  # Parameters - `lua` —... |
| `struct` | [`LuaResourceManager`](src/lua_api/economy_api.rs#L23) | Lua UserData wrapper for a resource economy manager.  # Fields - `inner` — `R... |

### `lua_api::entity_api`

> Registers the `luna.entity.*` ECS universe API.  Exposes `Universe` as Lua UserData (`LuaUniverse`) with entity lifecycle, component operations, querying, systems, tags, layers, and blueprints.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for entity api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/entity_api.rs](src/lua_api/entity_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/entity_api.rs#L605) | Registers the `luna.entity` table with the `newUniverse` factory function.  #... |

### `lua_api::event_api`

> Event Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for event api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/event_api.rs](src/lua_api/event_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/event_api.rs#L156) | Registers `luna.event.quit()` and related engine lifecycle functions into the... |

### `lua_api::filesystem_api`

> Filesystem Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for filesystem api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/filesystem_api.rs](src/lua_api/filesystem_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/filesystem_api.rs#L173) | Registers `luna.filesystem.*` functions into the Lua VM.  # Parameters - `lua... |

### `lua_api::font_api`

> Lua API bindings for the `luna.font.*` font rasterizer module.  Provides `newRasterizer`, `newTrueTypeRasterizer`, `newBMFontRasterizer`, and `newGlyphData` factory functions matching the LÖVE2D `love.font` module surface.  Font loading delegates to the same `Font::from_bytes` path used by `luna.graphics.newFont` so the returned `LuaFont` userdata is fully compatible with all existing graphics API functions.

*Source: [src/lua_api/font_api.rs](src/lua_api/font_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/font_api.rs#L159) | Registers the `luna.font.*` API onto the `luna` global table.  # Parameters -... |
| `struct` | [`GlyphData`](src/lua_api/font_api.rs#L36) | Glyph metrics returned by `luna.font.newGlyphData`.  Stores the pixel dimensi... |

### `lua_api::graph_api`

> Registers the `luna.graph.*` directed-graph and item-flow simulation API.  Exposes `LuaGraph`, `LuaNode`, `LuaEdge`, and `LuaGraphItem` UserData types wrapping `crate::graph::Graph` with callback dispatch for simulation events.

*Source: [src/lua_api/graph_api.rs](src/lua_api/graph_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/graph_api.rs#L2076) | Register the `luna.graph` API table. Panics in debug mode if the same entity ... |

### `lua_api::graphics_api`

> Registers the `luna.graphics.*` graphics API.

*Source: [src/lua_api/graphics_api.rs](src/lua_api/graphics_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`batch_key_from_value`](src/lua_api/graphics_api.rs#L866) | Extract a `SpriteBatchKey` from either a `LuaSpriteBatch` UserData or a numer... |
| `fn` | [`canvas_key_from_value`](src/lua_api/graphics_api.rs#L887) | Extract a `CanvasKey` from either a `LuaCanvas` UserData or a numeric ID.  # ... |
| `fn` | [`font_key_from_value`](src/lua_api/graphics_api.rs#L847) | Extract a `FontKey` from either a `LuaFont` UserData or a numeric ID.  # Para... |
| `fn` | [`invalid_batch_handle`](src/lua_api/graphics_api.rs#L936) | Returns a `LuaError` for an invalid or released sprite batch handle.  # Param... |
| `fn` | [`invalid_canvas_handle`](src/lua_api/graphics_api.rs#L950) | Returns a `LuaError` for an invalid or released canvas handle.  # Parameters ... |
| `fn` | [`invalid_font_handle`](src/lua_api/graphics_api.rs#L922) | Returns a `LuaError` for an invalid or released font handle.  # Parameters - ... |
| `fn` | [`invalid_mesh_handle`](src/lua_api/graphics_api.rs#L964) | Returns a `LuaError` for an invalid or released mesh handle.  # Parameters - ... |
| `fn` | [`invalid_texture_handle`](src/lua_api/graphics_api.rs#L908) | Returns a `LuaError` for an invalid or released texture handle.  # Parameters... |
| `fn` | [`register`](src/lua_api/graphics_api.rs#L2752) | Registers all `luna.graphics.*` drawing and resource management functions int... |
| `fn` | [`require_batch_key`](src/lua_api/graphics_api.rs#L1040) | Resolves a sprite-batch key and validates the batch is still alive.  # Parame... |
| `fn` | [`require_canvas_key`](src/lua_api/graphics_api.rs#L1062) | Resolves a canvas key and validates the canvas is still alive.  # Parameters ... |
| `fn` | [`require_font_key`](src/lua_api/graphics_api.rs#L1018) | Resolves a font key and validates the font is still loaded.  # Parameters - `... |
| `fn` | [`require_mesh_key`](src/lua_api/graphics_api.rs#L1084) | Resolves a mesh key and validates the mesh is still alive.  # Parameters - `s... |
| `fn` | [`require_texture_key`](src/lua_api/graphics_api.rs#L980) | Resolves a texture key, validating the handle is still alive.  # Parameters -... |
| `fn` | [`texture_key_from_value`](src/lua_api/graphics_api.rs#L828) | Extract a `TextureKey` from either a `LuaImage` UserData or a numeric ID.  # ... |
| `struct` | [`LuaCanvas`](src/lua_api/graphics_api.rs#L420) | Lua UserData wrapper for an off-screen canvas resource.  # Fields - `state` Ô... |
| `struct` | [`LuaFont`](src/lua_api/graphics_api.rs#L195) | Lua UserData wrapper for a loaded font resource.  # Fields - `state` ÔÇö `Rc<... |
| `struct` | [`LuaImage`](src/lua_api/graphics_api.rs#L24) | Lua UserData wrapper for a loaded texture resource.  # Fields - `state` — `Rc... |
| `struct` | [`LuaNineSlice`](src/lua_api/graphics_api.rs#L117) | Lua UserData wrapper for a nine-slice (9-patch) image definition.  Stores the... |
| `struct` | [`LuaShape`](src/lua_api/graphics_api.rs#L490) | Lua handle for a compound shape resource stored in `SharedState::shapes`.  Pr... |
| `struct` | [`LuaSpriteBatch`](src/lua_api/graphics_api.rs#L313) | Lua UserData wrapper for a sprite batch resource.  # Fields - `state` ÔÇö `Rc... |

### `lua_api::gui_api`

> Lua API bindings for the `luna.gui.*` retained-mode widget system.  Registers the `luna.gui` namespace with functions for creating widgets, managing the widget tree, routing input events, theming, focus navigation, and toast notifications.  ## Exposed Lua API  | Function | Purpose | |---|---| | `luna.gui.newButton(text?)` | Create a button widget | | `luna.gui.newLabel(text?)` | Create a text label | | `luna.gui.newTextInput()` | Create a text input field | | `luna.gui.newCheckbox(text?)` | Create a checkbox | | `luna.gui.newSlider(min?, max?)` | Create a value slider | | `luna.gui.newProgressBar(min?, max?)` | Create a progress bar | | `luna.gui.newComboBox()` | Create a dropdown | | `luna.gui.newList()` | Create a selectable list | | `luna.gui.newPanel()` | Create a container panel | | `luna.gui.newLayout(direction?)` | Create a flexbox layout | | `luna.gui.newScrollPanel()` | Create a scrollable panel | | `luna.gui.newNinePatch()` | Create a 9-patch slicer | | `luna.gui.newTabBar()` | Create a tab bar | | `luna.gui.newSeparator(vertical?)` | Create a separator line | | `luna.gui.newSpacer(w?, h?)` | Create spacing filler | | `luna.gui.newToast(msg?, duration?)` | Create a toast notification | | `luna.gui.newTreeView()` | Create a collapsible tree | | `luna.gui.newRadioButton(text?, group?)` | Create a grouped radio button | | `luna.gui.newScrollBar(vertical?)` | Create a scroll bar | | `luna.gui.newWindow(title?)` | Create a draggable window | | `luna.gui.newSplitPanel(orientation?)` | Create a split panel | | `luna.gui.newDockPanel()` | Create a dock panel | | `luna.gui.newToolbar(orientation?)` | Create a toolbar | | `luna.gui.newMenuBar()` | Create a menu bar | | `luna.gui.newMenuItem(text?)` | Create a menu item | | `luna.gui.newDialog(title?)` | Create a modal dialog | | `luna.gui.newStatusBar()` | Create a status bar | | `luna.gui.newAccordion()` | Create a collapsible accordion | | `luna.gui.newTooltipPanel(text?)` | Create a tooltip panel | | `luna.gui.newColorPicker()` | Create a color picker | | `luna.gui.newTable()` | Create a data table | | `luna.gui.newImageWidget()` | Create an image widget | | `luna.gui.newTheme()` | Create a theme instance | | `luna.gui.setTheme(theme)` | Set the active theme | | `luna.gui.getTheme()` | Get the active theme | | `luna.gui.getRoot()` | Get the root panel index | | `luna.gui.setFocus(idx/nil)` | Set keyboard focus | | `luna.gui.getFocus()` | Get focused widget index | | `luna.gui.focusNext()` | Tab to next focusable | | `luna.gui.focusPrev()` | Tab to previous focusable | | `luna.gui.clearFocus()` | Clear focus | | `luna.gui.addToast(toast)` | Queue a toast overlay | | `luna.gui.getToastCount()` | Active toast count | | `luna.gui.mousepressed(x,y,btn?)` | Forward mouse press | | `luna.gui.mousereleased(x,y,btn?)` | Forward mouse release | | `luna.gui.mousemoved(x,y)` | Forward mouse move | | `luna.gui.keypressed(key)` | Forward key press | | `luna.gui.textinput(text)` | Forward text input | | `luna.gui.wheelmoved(x,y)` | Forward mouse wheel | | `luna.gui.update(dt)` | Advance toast timers | | `luna.gui.getWidgetCount()` | Total widget count | | Widget methods | See per-type method tables in gui.md |  ## Implementation Pattern  A single `Rc<RefCell<GuiContext>>` is created at registration time and captured by all closures via Rc clone.  Widget indices (pool positions) are returned to Lua as integers.  Container/list/tab indices are 1-based in Lua.

*Source: [src/lua_api/gui_api.rs](src/lua_api/gui_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/gui_api.rs#L3611) | Register the `luna.gui` namespace.  Creates the `gui` table on the `luna` glo... |

### `lua_api::image_api`

> Registers the `luna.image.*` pixel-level image manipulation API.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for image api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/image_api.rs](src/lua_api/image_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/image_api.rs#L66) | Registers the `luna.image` table on the provided `luna` namespace.  # Paramet... |

### `lua_api::input_api`

> Input Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for input api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/input_api.rs](src/lua_api/input_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/input_api.rs#L43) | Registers `luna.keyboard.*` and `luna.mouse.*` query functions into the Lua V... |

### `lua_api::inventory_api`

> Lua API bindings for the `luna.inventory.*` slot-based inventory module.  Provides `Item`, `ItemStack`, `Slot`, `Container`, `ItemSet`, and `Inventory` UserData objects for building RPG-style inventory systems in Lua.

*Source: [src/lua_api/inventory_api.rs](src/lua_api/inventory_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/inventory_api.rs#L1372) | Register the `luna.inventory` module. Panics in debug mode if the same entity... |
| `struct` | [`LuaContainer`](src/lua_api/inventory_api.rs#L543) | Lua UserData wrapper for a named collection of slots. |
| `struct` | [`LuaInventory`](src/lua_api/inventory_api.rs#L929) | Lua UserData wrapper for the top-level inventory.  # Fields - `inner` — `Rc<R... |
| `struct` | [`LuaItem`](src/lua_api/inventory_api.rs#L28) | Lua UserData wrapper for a single item definition.  # Fields - `inner` — `Rc<... |
| `struct` | [`LuaItemSet`](src/lua_api/inventory_api.rs#L799) | Lua UserData wrapper for a named item set with requirements.  # Fields - `inn... |
| `struct` | [`LuaItemStack`](src/lua_api/inventory_api.rs#L255) | Lua UserData wrapper for a counted stack of items. |
| `struct` | [`LuaSlot`](src/lua_api/inventory_api.rs#L377) | Lua UserData wrapper for a single inventory slot. |

### `lua_api::localization_api`

> Registers the `luna.localization.*` internationalization API.  Provides a structured translation system supporting multiple languages, fallback chains, pluralization, variable interpolation, and language-change callbacks.

*Source: [src/lua_api/localization_api.rs](src/lua_api/localization_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/localization_api.rs#L104) | Registers `luna.localization.*` functions.  # Parameters - `lua` — `&Lua`. - ... |

### `lua_api::log_api`

> Structured game-level logging API (`luna.log.*`).  Exposes convenience logging functions so Lua scripts can emit messages at specific severity levels and query or change the active log level at runtime without going through the `luna.system` namespace.

*Source: [src/lua_api/log_api.rs](src/lua_api/log_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/log_api.rs#L22) | Registers the `luna.log.*` namespace into the shared `luna` table.  # Errors ... |

### `lua_api::lua_types`

> UserData type utilities for Luna2D Lua objects.  Provides the `LunaType` trait and `add_type_methods` helper so every Luna2D UserData object exposes `type()` and `typeOf()` methods matching standard game object type system.

*Source: [src/lua_api/lua_types.rs](src/lua_api/lua_types.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`add_type_methods`](src/lua_api/lua_types.rs#L26) | Adds standard `type()` and `typeOf()` methods to a UserData definition.  # Pa... |
| `trait` | [`LunaType`](src/lua_api/lua_types.rs#L11) | Standard type identification for Luna2D UserData objects.  Every Luna2D Lua o... |

### `lua_api::math_api`

*Source: [src/lua_api/math_api.rs](src/lua_api/math_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/math_api.rs#L1810) | Registers `luna.math.*` into the Lua VM, including Vec2, noise, tween, spatia... |

### `lua_api::minimap_api`

> Lua API bindings for the `luna.minimap.*` minimap module.  Provides a `Minimap` UserData type with factory functions for creating grid-based minimaps with terrain coloring, fog of war, tracked objects, pings, markers, and viewport rectangle overlay.

*Source: [src/lua_api/minimap_api.rs](src/lua_api/minimap_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/minimap_api.rs#L771) | Register the `luna.minimap` module. Panics in debug mode if the same entity i... |
| `struct` | [`LuaMinimap`](src/lua_api/minimap_api.rs#L24) | Lua UserData wrapper for a grid-based minimap.  # Fields - `inner` — `Rc<RefC... |

### `lua_api::modding_api`

> Modding Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for modding api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/modding_api.rs](src/lua_api/modding_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/modding_api.rs#L559) | Registers `luna.modding.*` functions into the Lua VM.  # Parameters - `lua` —... |

### `lua_api::overlay_api`

> Lua API bindings for the `luna.overlay.*` screen-effect overlay module.  Registers the `luna.overlay` table and exposes a single factory:  - `luna.overlay.newOverlay(width?, height?)` — creates a `LuaOverlay`   UserData object.  All overlay state lives entirely inside an `Rc<RefCell<Overlay>>`; the `LuaOverlay` wrapper holds a clone of that smart pointer. Calling any method borrows the `RefCell` for the duration of the call, then releases it — the engine never holds a long-lived `borrow_mut` across Lua callbacks.  The `register` function is called once during engine startup by the `lua_api` module registration chain; it adds the `luna.overlay` table to the root `luna` global.

*Source: [src/lua_api/overlay_api.rs](src/lua_api/overlay_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/overlay_api.rs#L728) | Registers the `luna.overlay.*` Lua API.  Creates the `luna.overlay` sub-table... |
| `struct` | [`LuaOverlay`](src/lua_api/overlay_api.rs#L41) | Lua UserData wrapper for the composable screen-effect overlay.  This is a thi... |

### `lua_api::particle_api`

> Registers the `luna.particle.*` particle-system API.

*Source: [src/lua_api/particle_api.rs](src/lua_api/particle_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`emission_shape_to_lua`](src/lua_api/particle_api.rs#L662) | Convert an `EmissionShape` to a Lua table with type and parameter fields.  # ... |
| `fn` | [`ensure_particle_exists`](src/lua_api/particle_api.rs#L38) | Checks that a particle system key refers to an existing slot-map entry.  # Pa... |
| `fn` | [`invalid_particle_handle`](src/lua_api/particle_api.rs#L22) | Returns a `LuaError` for an invalid or released particle system handle.  # Pa... |
| `fn` | [`lua_value_to_f64`](src/lua_api/particle_api.rs#L794) | Helper to extract an f64 from a `LuaValue`.  # Parameters - `v` — `&LuaValue`... |
| `fn` | [`parse_color`](src/lua_api/particle_api.rs#L139) | Helper: parse a Lua color table `{r, g, b, a}` into `[f32; 4]`.  # Parameters... |
| `fn` | [`parse_emission_shape`](src/lua_api/particle_api.rs#L562) | Parse an emission shape from a Lua string name and optional parameters table.... |
| `fn` | [`particle_key_from_value`](src/lua_api/particle_api.rs#L117) | Extract a `ParticleKey` from either a `LuaParticleSystem` UserData or a numer... |
| `fn` | [`particle_system`](src/lua_api/particle_api.rs#L78) | Returns a shared reference to an existing particle system, or an error.  # Pa... |
| `fn` | [`particle_system_mut`](src/lua_api/particle_api.rs#L97) | Returns a mutable reference to an existing particle system, or an error.  # P... |
| `fn` | [`register`](src/lua_api/particle_api.rs#L1724) | Registers all `luna.particle.*` particle system management functions into the... |
| `fn` | [`require_particle_key`](src/lua_api/particle_api.rs#L59) | Extracts and validates a `ParticleKey` from a Lua value.  # Parameters - `par... |
| `struct` | [`LuaParticleSystem`](src/lua_api/particle_api.rs#L156) | Lua UserData wrapper for a particle system resource.  # Fields - `state` ÔÇö ... |

### `lua_api::pathfinding_api`

> Registers the `luna.pathfinding.*` grid-based pathfinding API.  Exposes `NavGrid`, `UnitPathfinder`, and `FlowField` as Lua UserData with 1-based tile coordinates at the Lua boundary.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for pathfinding api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/pathfinding_api.rs](src/lua_api/pathfinding_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/pathfinding_api.rs#L797) | Register the `luna.pathfinding` namespace.  # Parameters - `lua` — `&Lua`. - ... |

### `lua_api::patterns_api`

> Registers the `luna.patterns.*` software design patterns API.  Provides factory functions and UserData wrappers for reusable patterns: EventBus (prioritized pub-sub), ObjectPool (object reuse), CommandStack (undo/redo), ServiceLocator (DI container), Factory (named constructors), and SimpleState (finite state machine).

*Source: [src/lua_api/patterns_api.rs](src/lua_api/patterns_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/patterns_api.rs#L1015) | Registers `luna.patterns.*` factory functions.  # Parameters - `lua` — `&Lua`... |

### `lua_api::physics_api`

> Registers the `luna.physics.*` physics API.

*Source: [src/lua_api/physics_api.rs](src/lua_api/physics_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`body_index_from_value`](src/lua_api/physics_api.rs#L633) | Extract a body index from either a `LuaBody` UserData or an integer.  # Param... |
| `fn` | [`parse_body_type`](src/lua_api/physics_api.rs#L103) | Parses a body type string into a [`BodyType`]. Defaults to `Dynamic` for unre... |
| `fn` | [`register`](src/lua_api/physics_api.rs#L1589) | Registers all `luna.physics.*` simulation and collision functions into the Lu... |
| `fn` | [`world_index_from_value`](src/lua_api/physics_api.rs#L614) | Extract a world index from either a `LuaWorld` UserData or an integer.  # Par... |
| `struct` | [`LuaBody`](src/lua_api/physics_api.rs#L321) | Lua UserData wrapper for a physics body.  # Fields - `worlds` ÔÇö Shared vect... |
| `struct` | [`LuaShape`](src/lua_api/physics_api.rs#L21) | Lua UserData wrapper for a standalone physics shape.  Created by `luna.physic... |
| `struct` | [`LuaWorld`](src/lua_api/physics_api.rs#L121) | Lua UserData wrapper for a physics world.  # Fields - `worlds` ÔÇö Shared vec... |

### `lua_api::postfx_api`

> Lua API bindings for the `luna.postfx.*` post-processing effects module.  Registers the `luna.postfx` table and exposes three factory functions:  - `luna.postfx.newEffect(name)` — creates a built-in `PostFxEffect`   by name (`"bloom"`, `"blur"`, `"crt"`, `"godrays"`, `"vignette"`,   `"colourgrade"`, `"chromatic"`). - `luna.postfx.newPass(shaderId)` — creates a `Custom` `PostFxEffect`   backed by an external shader resource ID. - `luna.postfx.newStack(width?, height?)` — creates an empty   `PostFxStack` with the given canvas dimensions.  `LuaPostFxEffect` and `LuaPostFxStack` are thin `mlua` UserData wrappers that hold `Rc<RefCell<...>>` smart pointers. `LuaPostFxStack` additionally owns the concrete effect vector so that effect objects added via Lua are kept alive as long as the stack is alive.  The `register` function is called once during engine startup.

*Source: [src/lua_api/postfx_api.rs](src/lua_api/postfx_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/postfx_api.rs#L584) | Registers the `luna.postfx.*` Lua API.  Creates the `luna.postfx` sub-table a... |
| `struct` | [`LuaPostFxEffect`](src/lua_api/postfx_api.rs#L42) | Lua UserData wrapper for a single post-processing effect.  Wraps a `PostFxEff... |

### `lua_api::quest_api`

> Lua API bindings for `luna.quest.*`.  Exposes quest creation, stage management, objective tracking, and journal entries through the `luna.quest` table.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for quest api-related operations and data management. Key types exported from this module: `LuaObjective`, `LuaQuestStage`, `LuaQuest`, `LuaQuestLog`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/quest_api.rs](src/lua_api/quest_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/quest_api.rs#L872) | Register `luna.quest.*` API with the Lua VM.  # Parameters - `lua` — `&Lua`. ... |
| `struct` | [`LuaObjective`](src/lua_api/quest_api.rs#L28) | Lua UserData wrapper for a quest objective. |
| `struct` | [`LuaQuest`](src/lua_api/quest_api.rs#L311) | Lua UserData wrapper for a quest. Consult the module-level documentation for ... |
| `struct` | [`LuaQuestLog`](src/lua_api/quest_api.rs#L686) | Lua UserData wrapper for a quest log. Consult the module-level documentation ... |
| `struct` | [`LuaQuestStage`](src/lua_api/quest_api.rs#L207) | Lua UserData wrapper for a quest stage. Consult the module-level documentatio... |

### `lua_api::savegame_api`

> Savegame Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for savegame api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/savegame_api.rs](src/lua_api/savegame_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/savegame_api.rs#L265) | Registers `luna.savegame.*` functions into the Lua VM.  # Parameters - `lua` ... |

### `lua_api::scene_api`

> Registers the `luna.scene.*` scene stack, registry, data store, and depth-sorter API.  The scene stack manages a LIFO stack of Lua scene tables with optional visual transitions. Scene tables are stored via `mlua::RegistryKey` and identified by `SceneId`. Inter-scene data is stored similarly.  The `DepthSorter` UserData batches draw callbacks by depth for ordered compositing each frame.

*Source: [src/lua_api/scene_api.rs](src/lua_api/scene_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/scene_api.rs#L181) | Registers the `luna.scene` table with scene stack, registry, data store,  # P... |

### `lua_api::sprite_api`

> Registers the `luna.sprite.*` sprite system API (SpriteBatch, NineSlice, Camera, Animation)..  Provides UserData wrappers and factory functions for extended graphics subsystems: Light2D, TextureAtlas, DrawLayer, Viewport, ViewportScale, SpriteSheet, PolygonMap, GraphRenderer, LargeMapRenderer, ColumnBatch, Camera2D, Animation, Trail, DecalSurface, and PaletteLUT.

*Source: [src/lua_api/sprite_api.rs](src/lua_api/sprite_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/sprite_api.rs#L2495) | Registers Phase 24 graphics extension factory functions on `luna.graphics`.  ... |
| `struct` | [`LuaDecalSurface`](src/lua_api/sprite_api.rs#L2344) | Lua UserData wrapper for DecalSurface (internal — no factory).  # Fields - `i... |
| `struct` | [`LuaPaletteLUT`](src/lua_api/sprite_api.rs#L2389) | Lua UserData wrapper for PaletteLUT (internal — no factory).  # Fields - `inn... |
| `struct` | [`LuaTrail`](src/lua_api/sprite_api.rs#L2214) | Lua UserData wrapper for Trail (internal — no factory).  # Fields - `inner` —... |

### `lua_api::stats_api`

> Lua API bindings for the `luna.stats` RPG character sheet system.  Exposes [`Sheet`] and a global [`StatsRegistry`] as `luna.stats.*` Lua values.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for stats api-related operations and data management. Key types exported from this module: `LuaSheet`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/stats_api.rs](src/lua_api/stats_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/stats_api.rs#L1005) | Register the `luna.stats` module into the Lua state.  # Parameters - `lua` — ... |
| `struct` | [`LuaSheet`](src/lua_api/stats_api.rs#L27) | Lua-visible wrapper for a character [`Sheet`].  # Fields - `inner` — `Rc<RefC... |

### `lua_api::steering_api`

> Registers the `luna.steering.*` steering and spatial AI API.  Provides Reynolds-style steering behaviours, influence maps, and squad formation management. These are movement and spatial reasoning systems, distinct from the decision-making AI in `luna.ai`.

*Source: [src/lua_api/steering_api.rs](src/lua_api/steering_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/steering_api.rs#L65) | Registers the `luna.steering` table on the provided `luna` namespace.  # Para... |

### `lua_api::system_api`

> System Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for system api-related operations and data management. Key types exported from this module: `PowerState`. Primary functions: `get_processor_count()`, `get_memory_size()`, `open_url()`, `get_preferred_locales()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/system_api.rs](src/lua_api/system_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`PowerState`](src/lua_api/system_api.rs#L111) | Power state of the device. Consult the module-level documentation for the bro... |
| `fn` | [`get_memory_size`](src/lua_api/system_api.rs#L32) | Returns total system RAM in MiB using the `sysinfo` crate.  # Returns Physica... |
| `fn` | [`get_power_info`](src/lua_api/system_api.rs#L147) | Returns power/battery information: (state, percent, seconds).  On desktop pla... |
| `fn` | [`get_preferred_locales`](src/lua_api/system_api.rs#L89) | Returns the user's preferred locale strings.  # Returns A `Vec<String>` with ... |
| `fn` | [`get_processor_count`](src/lua_api/system_api.rs#L22) | Returns the number of logical processors available.  # Returns Logical CPU co... |
| `fn` | [`open_url`](src/lua_api/system_api.rs#L50) | Opens a URL in the default browser/application.  Only `http://`, `https://`, ... |
| `fn` | [`register`](src/lua_api/system_api.rs#L159) | Registers `luna.system.*` platform query functions into the Lua VM.  # Parame... |

### `lua_api::thread_api`

> Registers the `luna.thread` namespace.  Provides Lua-level multithreading: create background threads that run independent Lua VMs and communicate via thread-safe channels.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for thread api-related operations and data management. Key types exported from this module: `LuaThreadHandle`. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/thread_api.rs](src/lua_api/thread_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/thread_api.rs#L102) | Registers all `luna.thread.*` functions into the Lua VM.  # Parameters - `lua... |
| `struct` | [`LuaThreadHandle`](src/lua_api/thread_api.rs#L31) | Lua UserData wrapper for a background thread handle.  # Fields - `inner` — `A... |

### `lua_api::tilemap_api`

> Registers the `luna.tilemap.*` tilemap API.

*Source: [src/lua_api/tilemap_api.rs](src/lua_api/tilemap_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`parse_edge`](src/lua_api/tilemap_api.rs#L203) | Parses a cardinal direction string into an [`Edge`] enum variant.  # Paramete... |
| `fn` | [`parse_script_step`](src/lua_api/tilemap_api.rs#L1278) | Parses a Lua table into a [`ScriptStep`].  # Parameters - `t` — `&LuaTable`. ... |
| `fn` | [`rect_to_table`](src/lua_api/tilemap_api.rs#L167) | Converts a [`Rect`] to a Lua table with `x`, `y`, `width`, and `height` field... |
| `fn` | [`register`](src/lua_api/tilemap_api.rs#L2379) | Registers all `luna.tilemap.*` tile map and procedural generation functions i... |
| `fn` | [`step_to_table`](src/lua_api/tilemap_api.rs#L1347) | Converts a [`ScriptStep`] back to a Lua table.  # Parameters - `lua` — `&'lua... |
| `struct` | [`LuaAutoTileSheet`](src/lua_api/tilemap_api.rs#L46) | Lua wrapper around an [`AutoTileSheet`].  # Fields - `inner` — `Rc<RefCell<Au... |
| `struct` | [`LuaChunkMap`](src/lua_api/tilemap_api.rs#L93) | Lua wrapper around a [`ChunkMap`].  # Fields - `inner` — `Rc<RefCell<ChunkMap... |
| `struct` | [`LuaIsoMap`](src/lua_api/tilemap_api.rs#L102) | Lua wrapper around an [`IsoMap`].  # Fields - `inner` — `Rc<RefCell<IsoMap>>`. |
| `struct` | [`LuaMapBlock`](src/lua_api/tilemap_api.rs#L55) | Lua wrapper around a [`MapBlock`].  # Fields - `inner` — `Rc<RefCell<MapBlock... |
| `struct` | [`LuaMapGen`](src/lua_api/tilemap_api.rs#L83) | Lua wrapper around a [`MapGen`], storing the associated group for generation.... |
| `struct` | [`LuaMapGroup`](src/lua_api/tilemap_api.rs#L64) | Lua wrapper around a [`MapGroup`].  # Fields - `inner` — `Rc<RefCell<MapGroup... |
| `struct` | [`LuaMapScript`](src/lua_api/tilemap_api.rs#L73) | Lua wrapper around a [`MapScript`].  # Fields - `inner` — `Rc<RefCell<MapScri... |
| `struct` | [`LuaTileMap`](src/lua_api/tilemap_api.rs#L37) | Lua wrapper around a [`TileMap`].  # Fields - `inner` ÔÇö `Rc<RefCell<TileMap... |
| `struct` | [`LuaTileSet`](src/lua_api/tilemap_api.rs#L28) | Lua wrapper around a [`TileSet`].  # Fields - `inner` — `Rc<RefCell<TileSet>>`. |

### `lua_api::timer_api`

> Timer Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for timer api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/timer_api.rs](src/lua_api/timer_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/timer_api.rs#L341) | Registers `luna.timer.*` functions into the Lua VM.  Provides frame-timing ut... |

### `lua_api::window_api`

> Window Api implementation for the `lua_api` subsystem.  This module is part of Luna2D's `lua_api` subsystem and provides the implementation details for window api-related operations and data management. Primary functions: `register()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/lua_api/window_api.rs](src/lua_api/window_api.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`register`](src/lua_api/window_api.rs#L24) | Registers `luna.window.*` functions into the Lua VM.  # Parameters - `lua` — ... |

## `math` {#math}

### `math`

> Mod implementation for the `math` subsystem.  This module is part of Luna2D's `math` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/mod.rs](src/math/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`bezier`](src/math/mod.rs#L10) | Bezier curve evaluation using De Casteljau's algorithm. |
| `mod` | [`easing`](src/math/mod.rs#L12) | Standard easing functions for smooth animation and interpolation. |
| `mod` | [`geometry`](src/math/mod.rs#L14) | 2D geometry utility functions: intersections, containment, polygon ops, raste... |
| `mod` | [`grid`](src/math/mod.rs#L16) | 2D pathfinding grid with A*, Dijkstra, BFS, and flow field generation. |
| `mod` | [`mat3`](src/math/mod.rs#L18) | 3x3 column-major matrix for 2D transforms (translate, rotate, scale). |
| `mod` | [`noise`](src/math/mod.rs#L20) | 2D Perlin and Simplex noise generators for procedural content. |
| `mod` | [`polygon`](src/math/mod.rs#L22) | Polygon utilities: ear-clipping triangulation and convexity testing. |
| `mod` | [`procgen`](src/math/mod.rs#L24) | Procedural generation: cellular automata, Voronoi, flood fill, Poisson disk, ... |
| `mod` | [`random`](src/math/mod.rs#L26) | Seedable random number generator for reproducible sequences. |
| `mod` | [`raycasting`](src/math/mod.rs#L28) | 2D raycasting and visibility utility functions. |
| `mod` | [`rect`](src/math/mod.rs#L30) | Axis-aligned rectangle with intersection and containment queries. |
| `mod` | [`spatial_hash`](src/math/mod.rs#L32) | Spatial hash for efficient broad-phase AABB collision queries. |
| `mod` | [`srgb`](src/math/mod.rs#L34) | sRGB gamma ↔ linear color space conversion. |
| `mod` | [`transform`](src/math/mod.rs#L36) | 2D affine transform with chainable methods wrapping Mat3. |
| `mod` | [`tween`](src/math/mod.rs#L38) | Value interpolator with easing curves. |
| `mod` | [`vec2`](src/math/mod.rs#L40) | 2D floating-point vector with arithmetic operators and common helpers. |

### `math::bezier`

> Bezier curve evaluation using De Casteljau's algorithm.  Supports arbitrary-degree curves with control point manipulation, rendering to polylines, derivative computation, and geometric transforms.  This module is part of Luna2D's `math` subsystem and provides the implementation details for bezier-related operations and data management. Key types exported from this module: `BezierCurve`. Primary functions: `new()`, `evaluate()`, `render()`, `render_segment()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/bezier.rs](src/math/bezier.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BezierCurve`](src/math/bezier.rs#L22) | A Bezier curve defined by control points.  Uses De Casteljau's algorithm for ... |

### `math::easing`

> Standard easing functions for smooth animation and interpolation.  Each function takes a normalised progress value `t` in `[0.0, 1.0]` and returns the eased value, also in `[0.0, 1.0]` for most curves.

*Source: [src/math/easing.rs](src/math/easing.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`apply`](src/math/easing.rs#L326) | Looks up an easing function by name and applies it to progress value `t`.  Su... |
| `fn` | [`ease_in_back`](src/math/easing.rs#L294) | Back ease-in — pulls back before accelerating past the start.  # Parameters -... |
| `fn` | [`ease_in_bounce`](src/math/easing.rs#L283) | Bounce ease-in — simulates a bouncing ball launching.  # Parameters - `t` — n... |
| `fn` | [`ease_in_cubic`](src/math/easing.rs#L63) | Cubic ease-in — starts slow, accelerates sharply.  # Parameters - `t` — norma... |
| `fn` | [`ease_in_elastic`](src/math/easing.rs#L225) | Elastic ease-in — spring-like overshoot at the start.  # Parameters - `t` — n... |
| `fn` | [`ease_in_expo`](src/math/easing.rs#L174) | Exponential ease-in — very slow start, rapid acceleration.  # Parameters - `t... |
| `fn` | [`ease_in_out_cubic`](src/math/easing.rs#L86) | Cubic ease-in-out — smooth S-curve. Consult the module-level documentation fo... |
| `fn` | [`ease_in_out_expo`](src/math/easing.rs#L204) | Exponential ease-in-out — sharp S-curve with exponential tails.  # Parameters... |
| `fn` | [`ease_in_out_quad`](src/math/easing.rs#L48) | Quadratic ease-in-out — slow start and end, fast middle.  # Parameters - `t` ... |
| `fn` | [`ease_in_out_quart`](src/math/easing.rs#L125) | Quartic ease-in-out — pronounced S-curve.  # Parameters - `t` — normalised pr... |
| `fn` | [`ease_in_out_sine`](src/math/easing.rs#L163) | Sinusoidal ease-in-out — gentle S-curve.  # Parameters - `t` — normalised pro... |
| `fn` | [`ease_in_quad`](src/math/easing.rs#L26) | Quadratic ease-in — starts slow, accelerates.  # Parameters - `t` — normalise... |
| `fn` | [`ease_in_quart`](src/math/easing.rs#L102) | Quartic ease-in — very slow start. Consult the module-level documentation for... |
| `fn` | [`ease_in_sine`](src/math/easing.rs#L141) | Sinusoidal ease-in — gentle sine-based acceleration.  # Parameters - `t` — no... |
| `fn` | [`ease_out_back`](src/math/easing.rs#L306) | Back ease-out — overshoots the target then settles back.  # Parameters - `t` ... |
| `fn` | [`ease_out_bounce`](src/math/easing.rs#L261) | Bounce ease-out — simulates a bouncing ball landing.  # Parameters - `t` — no... |
| `fn` | [`ease_out_cubic`](src/math/easing.rs#L74) | Cubic ease-out — starts fast, decelerates sharply.  # Parameters - `t` — norm... |
| `fn` | [`ease_out_elastic`](src/math/easing.rs#L243) | Elastic ease-out — spring-like overshoot at the end.  # Parameters - `t` — no... |
| `fn` | [`ease_out_expo`](src/math/easing.rs#L189) | Exponential ease-out — rapid start, very slow end.  # Parameters - `t` — norm... |
| `fn` | [`ease_out_quad`](src/math/easing.rs#L37) | Quadratic ease-out — starts fast, decelerates.  # Parameters - `t` — normalis... |
| `fn` | [`ease_out_quart`](src/math/easing.rs#L113) | Quartic ease-out — very slow end. Consult the module-level documentation for ... |
| `fn` | [`ease_out_sine`](src/math/easing.rs#L152) | Sinusoidal ease-out — gentle sine-based deceleration.  # Parameters - `t` — n... |
| `fn` | [`linear`](src/math/easing.rs#L15) | Linear interpolation — no easing. Consult the module-level documentation for ... |

### `math::geometry`

> 2D geometry utility functions.  Free functions for common geometric computations: intersections, containment, polygon operations, and rasterization.  This module is part of Luna2D's `math` subsystem and provides the implementation details for geometry-related operations and data management. Primary functions: `angle_between()`, `circle_contains_point()`, `circle_intersects_circle()`, `circle_intersects_line()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/geometry.rs](src/math/geometry.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`angle_between`](src/math/geometry.rs#L23) | Returns the angle in radians from (x1, y1) to (x2, y2).  # Parameters - `x1` ... |
| `fn` | [`bresenham`](src/math/geometry.rs#L396) | Bresenham line rasterization from (x1, y1) to (x2, y2).  # Parameters - `x1` ... |
| `fn` | [`circle_contains_point`](src/math/geometry.rs#L38) | Returns true if the point (px, py) is inside the circle centered at (cx, cy) ... |
| `fn` | [`circle_intersects_circle`](src/math/geometry.rs#L56) | Returns true if two circles overlap. Consult the module-level documentation f... |
| `fn` | [`circle_intersects_line`](src/math/geometry.rs#L79) | Line-circle intersection. Returns (intersects, hit1, hit2).  # Parameters - `... |
| `fn` | [`circle_intersects_segment`](src/math/geometry.rs#L133) | Segment-circle intersection. Same as line-circle but clamped to the segment. ... |
| `fn` | [`closest_point_on_segment`](src/math/geometry.rs#L300) | Returns the closest point on a line segment to a given point.  # Parameters -... |
| `fn` | [`convex_hull`](src/math/geometry.rs#L433) | Computes the convex hull of a set of 2D points using Andrew's monotone chain ... |
| `fn` | [`delaunay_triangulate`](src/math/geometry.rs#L491) | Delaunay triangulation using the Bowyer-Watson algorithm.  # Parameters - `po... |
| `fn` | [`line_intersect`](src/math/geometry.rs#L366) | Infinite line intersection. Returns the intersection point if lines are not p... |
| `fn` | [`point_in_polygon`](src/math/geometry.rs#L330) | Tests if a point is inside a polygon using the ray casting algorithm.  # Para... |
| `fn` | [`polygon_area`](src/math/geometry.rs#L187) | Computes the signed area of a polygon using the Shoelace formula.  # Paramete... |
| `fn` | [`polygon_centroid`](src/math/geometry.rs#L213) | Computes the centroid of a polygon. Consult the module-level documentation fo... |
| `fn` | [`segment_intersects_segment`](src/math/geometry.rs#L262) | Tests if two line segments intersect. Returns (intersects, intersection_point... |

### `math::grid`

> 2D pathfinding grid with A*, Dijkstra, BFS, and flow field generation.  Cells are addressed with 0-based `(x, y)` coordinates. The Lua layer converts to/from 1-based indices.  This module is part of Luna2D's `math` subsystem and provides the implementation details for grid-related operations and data management. Key types exported from this module: `Grid`. Primary functions: `new()`, `width()`, `height()`, `set_walkable()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/grid.rs](src/math/grid.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Grid`](src/math/grid.rs#L59) | 2D pathfinding grid with per-cell walkability and movement costs.  Supports A... |

### `math::mat3`

> Mat3 implementation for the `math` subsystem.  This module is part of Luna2D's `math` subsystem and provides the implementation details for mat3-related operations and data management. Key types exported from this module: `Mat3`. Primary functions: `identity()`, `from_row_major()`, `from_translation()`, `from_rotation()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/mat3.rs](src/math/mat3.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Mat3`](src/math/mat3.rs#L21) | A 3×3 column-major matrix used for 2D affine transforms (translation, rotatio... |

### `math::noise`

> 2D Perlin and Simplex noise generators for procedural content.  All functions return values in `[-1.0, 1.0]`. Seed is used to offset the gradient hash, producing different noise patterns per seed.  This module is part of Luna2D's `math` subsystem and provides the implementation details for noise-related operations and data management. Key types exported from this module: `DistType`, `NoiseKind`, `FractalType`, `MapGenOptions`, `NoiseGenerator`. Primary functions: `perlin2d()`, `simplex2d()`, `fbm()`, `perlin3d()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/noise.rs](src/math/noise.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DistType`](src/math/noise.rs#L501) | Distance metric for Worley noise. Consult the module-level documentation for ... |
| `enum` | [`FractalType`](src/math/noise.rs#L530) | Fractal type for multi-octave noise. Consult the module-level documentation f... |
| `enum` | [`NoiseKind`](src/math/noise.rs#L516) | Noise algorithm kind used by fractal combinators.  # Variants - `Perlin` — Pe... |
| `fn` | [`fbm`](src/math/noise.rs#L120) | Generates fractal Brownian motion noise by layering multiple octaves of Perli... |
| `fn` | [`perlin2d`](src/math/noise.rs#L23) | Generates 2D Perlin noise at the given coordinates.  # Parameters - `x` — X c... |
| `fn` | [`perlin3d`](src/math/noise.rs#L284) | Generates 3D Perlin noise at the given coordinates.  # Parameters - `x` — X c... |
| `fn` | [`perlin4d`](src/math/noise.rs#L336) | Generates 4D Perlin noise at the given coordinates.  # Parameters - `x` — X c... |
| `fn` | [`simplex2d`](src/math/noise.rs#L51) | Generates 2D Simplex noise at the given coordinates.  # Parameters - `x` — X ... |
| `fn` | [`simplex_noise_2d`](src/math/noise.rs#L89) | Returns 2D simplex noise for the given coordinates using seed 0.  Convenience... |
| `fn` | [`simplex_noise_3d`](src/math/noise.rs#L104) | Returns 3D simplex noise for the given coordinates using seed 0.  Delegates t... |
| `struct` | [`MapGenOptions`](src/math/noise.rs#L552) | Options for 2D noise map generation. Consult the module-level documentation f... |
| `struct` | [`NoiseGenerator`](src/math/noise.rs#L598) | Seeded procedural noise generator. Consult the module-level documentation for... |

### `math::polygon`

> Polygon utilities: ear-clipping triangulation and convexity testing.  This module is part of Luna2D's `math` subsystem and provides the implementation details for polygon-related operations and data management. Primary functions: `triangulate()`, `is_convex()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/polygon.rs](src/math/polygon.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`is_convex`](src/math/polygon.rs#L80) | Check if a polygon is convex. This accessor incurs no allocation; call it fre... |
| `fn` | [`triangulate`](src/math/polygon.rs#L21) | Triangulate a simple polygon using the ear-clipping algorithm.  # Parameters ... |

### `math::procgen`

> Procedural generation utility functions.  Cave generation, Voronoi diagrams, flood fill, Poisson disk sampling, and periodic Perlin noise.  This module is part of Luna2D's `math` subsystem and provides the implementation details for procgen-related operations and data management. Key types exported from this module: `CellularOpts`, `VoronoiOpts`. Primary functions: `cellular_automata()`, `voronoi_diagram()`, `flood_fill()`, `poisson_disk()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/procgen.rs](src/math/procgen.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`cellular_automata`](src/math/procgen.rs#L110) | Generates a cave/dungeon map using cellular automata.  # Parameters - `width`... |
| `fn` | [`flood_fill`](src/math/procgen.rs#L269) | BFS flood fill on a grid. Consult the module-level documentation for the broa... |
| `fn` | [`perlin_noise_periodic`](src/math/procgen.rs#L444) | Periodic Perlin noise that tiles over period (px, py).  # Parameters - `x` — ... |
| `fn` | [`poisson_disk`](src/math/procgen.rs#L338) | Generates Poisson disk sample points using Bridson's algorithm.  # Parameters... |
| `fn` | [`voronoi_diagram`](src/math/procgen.rs#L177) | Generates a Voronoi diagram. Consult the module-level documentation for the b... |
| `struct` | [`CellularOpts`](src/math/procgen.rs#L23) | Options for cellular automata generation.  # Fields - `fill` — `f32`. - `iter... |
| `struct` | [`VoronoiOpts`](src/math/procgen.rs#L55) | Options for Voronoi diagram generation. Consult the module-level documentatio... |

### `math::random`

> Seedable random number generator for reproducible sequences.  Wraps `fastrand::Rng` with engine-compatible API including seeding, state save/restore, and normal distribution sampling.  This module is part of Luna2D's `math` subsystem and provides the implementation details for random-related operations and data management. Key types exported from this module: `RandomGenerator`. Primary functions: `new()`, `with_seed()`, `random()`, `random_int()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/random.rs](src/math/random.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`RandomGenerator`](src/math/random.rs#L24) | Seedable random number generator exposed as a Lua object.  Wraps `fastrand::R... |

### `math::raycasting`

> 2D raycasting and visibility utility functions.  Provides ray-vs-segment intersection, field of view computation, column projection, and distance-based shading.  This module is part of Luna2D's `math` subsystem and provides the implementation details for raycasting-related operations and data management. Key types exported from this module: `Segment`, `RayHit`, `SpriteProjection`, `Raycaster2D`. Primary functions: `cast_ray_2d()`, `field_of_view()`, `project_column()`, `distance_shade()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/raycasting.rs](src/math/raycasting.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`cast_ray_2d`](src/math/raycasting.rs#L48) | Casts a ray from (ox, oy) in direction (dx, dy) against a list of segments.  ... |
| `fn` | [`distance_shade`](src/math/raycasting.rs#L179) | Distance-based shading. Returns brightness in [0, 1].  # Parameters - `distan... |
| `fn` | [`field_of_view`](src/math/raycasting.rs#L100) | Computes a visibility polygon by casting rays at segment endpoints.  # Parame... |
| `fn` | [`project_column`](src/math/raycasting.rs#L155) | Projects a wall column distance to screen-space drawing parameters.  # Parame... |
| `struct` | [`RayHit`](src/math/raycasting.rs#L286) | Result of a single ray cast. Consult the module-level documentation for the b... |
| `struct` | [`Raycaster2D`](src/math/raycasting.rs#L333) | 2D grid-based raycaster using DDA traversal.  The grid stores wall types as `... |
| `struct` | [`Segment`](src/math/raycasting.rs#L22) | A line segment for raycasting. Consult the module-level documentation for the... |
| `struct` | [`SpriteProjection`](src/math/raycasting.rs#L313) | Sprite projection result. Consult the module-level documentation for the broa... |

### `math::rect`

> Rect implementation for the `math` subsystem.  This module is part of Luna2D's `math` subsystem and provides the implementation details for rect-related operations and data management. Key types exported from this module: `Rect`. Primary functions: `new()`, `center()`, `area()`, `contains()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/rect.rs](src/math/rect.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Rect`](src/math/rect.rs#L23) | An axis-aligned rectangle defined by its top-left corner and dimensions.  Use... |

### `math::spatial_hash`

> Spatial hash for efficient broad-phase AABB collision queries.  Items are keyed by a string ID and stored with axis-aligned bounding boxes. The grid partitions space into fixed-size cells for O(1) bucket lookup.

*Source: [src/math/spatial_hash.rs](src/math/spatial_hash.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SpatialHash`](src/math/spatial_hash.rs#L40) | Spatial hash for AABB queries. Consult the module-level documentation for the... |
| `struct` | [`SpatialItem`](src/math/spatial_hash.rs#L17) | Entry in the spatial hash. Consult the module-level documentation for the bro... |

### `math::srgb`

> sRGB gamma ↔ linear color space conversion.  Implements the standard IEC 61966-2-1 (sRGB) transfer functions for converting between gamma-encoded sRGB and linear light values.  This module is part of Luna2D's `math` subsystem and provides the implementation details for srgb-related operations and data management. Primary functions: `gamma_to_linear()`, `linear_to_gamma()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/srgb.rs](src/math/srgb.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`gamma_to_linear`](src/math/srgb.rs#L22) | Convert a single sRGB gamma-space color component to linear space.  Input and... |
| `fn` | [`linear_to_gamma`](src/math/srgb.rs#L39) | Convert a single linear-space color component to sRGB gamma space.  Input and... |

### `math::transform`

> 2D affine transform wrapping Mat3 with chainable methods.  Compatible with standard 2D transform API for translate, rotate, scale, shear, and point transformation.  This module is part of Luna2D's `math` subsystem and provides the implementation details for transform-related operations and data management. Key types exported from this module: `Transform`. Primary functions: `new()`, `from_components()`, `translate()`, `rotate()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/transform.rs](src/math/transform.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Transform`](src/math/transform.rs#L25) | 2D affine transform exposed as a Lua object.  Wraps `Mat3` with chainable tra... |

### `math::tween`

> Value interpolator with easing curves.  Uses easing functions from `crate::math::easing` to smoothly interpolate between start and target values over a duration.  This module is part of Luna2D's `math` subsystem and provides the implementation details for tween-related operations and data management. Key types exported from this module: `TweenValue`, `Tween`. Primary functions: `new()`, `add_value()`, `update()`, `get_value()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/tween.rs](src/math/tween.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Tween`](src/math/tween.rs#L40) | Value interpolator using easing functions.  Animates one or more values from ... |
| `struct` | [`TweenValue`](src/math/tween.rs#L22) | A start-to-target value pair for interpolation.  # Fields - `start` — `f64`. ... |

### `math::vec2`

> Vec2 implementation for the `math` subsystem.  This module is part of Luna2D's `math` subsystem and provides the implementation details for vec2-related operations and data management. Key types exported from this module: `Vec2`. Primary functions: `new()`, `zero()`, `splat()`, `dot()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/math/vec2.rs](src/math/vec2.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Vec2`](src/math/vec2.rs#L22) | A 2D floating-point vector used throughout the engine for positions, velociti... |

## `minimap` {#minimap}

### `minimap`

> Minimap data structures for overhead map displays (Tier 1).  Provides a grid-based minimap with terrain coloring, fog of war, tracked objects, pings, markers, and viewport rectangle overlay.  This is a **pure CPU data-model module** — it has no GPU dependencies. Extracted from `graphics` so that other modules can reference minimap types without pulling in the full rendering pipeline.  Key types: `ColorMode`, `FogLevel`, `MinimapObjectType`, `MinimapObject`, `MinimapPing`. Primary entry point: `Minimap::new(width, height)`.

*Source: [src/minimap/mod.rs](src/minimap/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ColorMode`](src/minimap/mod.rs#L22) | How cells are colored on the minimap. Consult the module-level documentation ... |
| `enum` | [`FogLevel`](src/minimap/mod.rs#L38) | Fog-of-war visibility level for a cell. Consult the module-level documentatio... |
| `struct` | [`Minimap`](src/minimap/mod.rs#L167) | A grid-based minimap with terrain, fog, objects, pings, and markers.  # Field... |
| `struct` | [`MinimapMarker`](src/minimap/mod.rs#L129) | A persistent labeled marker on the minimap.  # Fields - `x` — `f32`. - `y` — ... |
| `struct` | [`MinimapObject`](src/minimap/mod.rs#L88) | A tracked object on the minimap. Consult the module-level documentation for t... |
| `struct` | [`MinimapObjectType`](src/minimap/mod.rs#L71) | A registered object type with a display color and visibility toggle.  # Field... |
| `struct` | [`MinimapPing`](src/minimap/mod.rs#L108) | A temporary animated ping on the minimap.  # Fields - `x` — `f32`. - `y` — `f... |

## `modding` {#modding}

### `modding`

> Mod management framework.  Provides `ModInfo` for mod metadata and `ModManager` for registration, dependency resolution, load ordering, folder scanning, and hot-reload queuing.  This module is part of Luna2D's `modding` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/modding/mod.rs](src/modding/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`mod_manager`](src/modding/mod.rs#L13) | Mod Manager sub-module. |

### `modding::mod_manager`

> Mod management framework.  Provides `ModInfo` for mod metadata and `ModManager` for registration, dependency resolution, load ordering, folder scanning, and hot-reload queuing.  This module is part of Luna2D's `modding` subsystem and provides the implementation details for mod manager-related operations and data management. Key types exported from this module: `ModInfo`, `ModManager`. Primary functions: `new()`, `new()`, `register_mod()`, `unregister_mod()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/modding/mod_manager.rs](src/modding/mod_manager.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ModInfo`](src/modding/mod_manager.rs#L30) | Metadata describing a mod. Consult the module-level documentation for the bro... |
| `struct` | [`ModManager`](src/modding/mod_manager.rs#L87) | Centralized registry for managing mods, resolving load order, validating depe... |

## `overlay` {#overlay}

### `overlay`

> Composable per-frame screen-effect overlay (Tier 2).  Provides a rich set of full-screen atmospheric and one-shot screen effects that a Lua script can layer over the rendered scene. Each subsystem is independently enabled and parameterised:  - **Weather** — rain, snow, hail, dust, leaves, ash, and pollen particles   driven by intensity, wind direction, and wind speed. - **Ambient** — tints the entire screen with a time-of-day colour that   cycles through night → dawn → day → dusk automatically. - **Flash** — a one-shot full-screen colour burst that fades out linearly. - **Shake** — a screen-space jitter that decays over its duration using   an xorshift PRNG for deterministic-ish movement. - **Fade** — smooth alpha transition between two screen-fill colour states. - **Cloud shadows** — a scrolling set of soft shadow blobs. - **Fog** — a uniform atmospheric colour overlay with configurable density. - **Heat haze** — distortion intensity for shimmer effects. - **Vignette** — screen-edge darkening controlled by a strength value. - **Film grain** — randomised per-frame noise overlay. - **Lightning** — a hard white flash used as a single-frame lightning bolt.  This is a **pure CPU data-model module** — it has no GPU dependencies. GPU rendering of effects is handled in `lua_api`. Extracted from `graphics` so that other modules can reference overlay types without pulling in the full rendering pipeline.  Call `Overlay::update(dt)` every frame to advance all active subsystems, then read state fields (or use the Lua API methods) to drive GPU draw calls.

*Source: [src/overlay/mod.rs](src/overlay/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`ambient`](src/overlay/mod.rs#L31) | Ambient lighting state with time-of-day colour cycling. |
| `mod` | [`atmosphere`](src/overlay/mod.rs#L33) | Atmospheric environment effects: clouds, fog, heat haze, vignette, film grain... |
| `mod` | [`effects`](src/overlay/mod.rs#L35) | One-shot screen effects: flash, shake, and fade. |
| `mod` | [`state`](src/overlay/mod.rs#L37) | Main Overlay struct combining all subsystems. |
| `mod` | [`weather`](src/overlay/mod.rs#L39) | Weather types, particles, and simulation state. |

### `overlay::ambient`

*Source: [src/overlay/ambient.rs](src/overlay/ambient.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`AmbientState`](src/overlay/ambient.rs#L14) | Ambient lighting state with time-of-day colour cycling.  When `enabled` is `t... |

### `overlay::atmosphere`

*Source: [src/overlay/atmosphere.rs](src/overlay/atmosphere.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CloudState`](src/overlay/atmosphere.rs#L18) | Cloud shadow overlay state.  Renders `count` soft shadow blobs that drift hor... |
| `struct` | [`FilmGrainState`](src/overlay/atmosphere.rs#L152) | Film grain noise overlay state.  Adds randomised per-pixel luminance noise ov... |
| `struct` | [`FogState`](src/overlay/atmosphere.rs#L61) | Atmospheric fog state.  Renders a uniform translucent colour rectangle over t... |
| `struct` | [`HeatHazeState`](src/overlay/atmosphere.rs#L93) | Heat haze distortion state.  Provides a UV-space shimmer distortion driven by... |
| `struct` | [`LightningState`](src/overlay/atmosphere.rs#L184) | Lightning flash state.  A single-shot full-screen hard flash distinct from `F... |
| `struct` | [`VignetteState`](src/overlay/atmosphere.rs#L123) | Vignette screen-edge darkening state.  Applies a smooth radial darkening that... |

### `overlay::effects`

*Source: [src/overlay/effects.rs](src/overlay/effects.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`FadeState`](src/overlay/effects.rs#L119) | Fade screen effect state.  A fade interpolates `color[3]` (the alpha channel ... |
| `struct` | [`FlashState`](src/overlay/effects.rs#L16) | Flash screen effect state.  A flash overlays an opaque colour rectangle on th... |
| `struct` | [`ShakeState`](src/overlay/effects.rs#L56) | Shake screen effect state.  The shake applies a per-frame pixel offset to the... |

### `overlay::state`

*Source: [src/overlay/state.rs](src/overlay/state.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Overlay`](src/overlay/state.rs#L38) | Composable per-frame screen-effect overlay managing multiple visual subsystem... |

### `overlay::weather`

*Source: [src/overlay/weather.rs](src/overlay/weather.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`WeatherType`](src/overlay/weather.rs#L19) | Weather particle types supported by the overlay system.  Each variant changes... |
| `struct` | [`WeatherParticle`](src/overlay/weather.rs#L96) | A single weather particle in the overlay's weather system.  Particles are cre... |
| `struct` | [`WeatherState`](src/overlay/weather.rs#L128) | Weather subsystem state.  Controls the full particle simulation for the curre... |

## `particle` {#particle}

### `particle`

> Particle system module providing emitter-based 2D particle effects.  **Tier**: Tier 2 — Engine Extensions.  A `ParticleSystem` spawns short-lived `Particle` entities each frame, advancing their position, velocity, and lifetime. Dead particles are recycled, keeping allocation bounded by `ParticleConfig::max_particles`.  Supports multi-stop size/color/alpha interpolation, emission shapes, radial/tangential acceleration, linear damping, relative mode, texture-based rendering, and five built-in geometric particle shapes (Square, Circle, Triangle, Spark, Diamond).  ## Sub-files  | File | Purpose | |------|---------| | `config.rs`   | Enums (`AreaDistribution`, `InsertMode`, `EmitterState`, `EmissionShape`, `RelativeMode`) and `ParticleConfig` (~50 fields) | | `shapes.rs`   | `ParticleShape` enum — five geometric render primitives | | `particle.rs` | `Particle` per-particle live state (pos, vel, life, rotation, …) | | `emitter.rs`  | `ParticleSystem` simulation loop, physics integration, and `draw_commands()` | | `math.rs`     | Math helpers: `lerp`, `interpolate_sizes`, `interpolate_colors`, `interpolate_alphas` | | `emission.rs` | Spawn-offset calculators for area distribution and emission shapes |

*Source: [src/particle/mod.rs](src/particle/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`config`](src/particle/mod.rs#L25) | Emitter configuration enums and `ParticleConfig` struct. |
| `mod` | [`emission`](src/particle/mod.rs#L27) | Particle spawn-offset helpers for area distribution and emission shapes. |
| `mod` | [`emitter`](src/particle/mod.rs#L29) | `ParticleSystem` simulation and draw-command generation. |
| `mod` | [`math`](src/particle/mod.rs#L31) | Math helpers for particle interpolation and random sampling. |
| `mod` | [`particle`](src/particle/mod.rs#L34) | Per-particle live state struct. |
| `mod` | [`shapes`](src/particle/mod.rs#L36) | `ParticleShape` geometric render primitive enum. |

### `particle::config`

> Particle emitter configuration types and enums.

*Source: [src/particle/config.rs](src/particle/config.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`AreaDistribution`](src/particle/config.rs#L16) | Area distribution mode for particle emission.  # Variants - `None` — All part... |
| `enum` | [`EmissionShape`](src/particle/config.rs#L77) | Emission shape controlling where new particles spawn relative to the emitter.... |
| `enum` | [`EmitterState`](src/particle/config.rs#L56) | Emitter lifecycle state controlling whether the system emits and updates part... |
| `enum` | [`InsertMode`](src/particle/config.rs#L39) | Insert mode controlling where new particles are placed in the particle list. ... |
| `enum` | [`RelativeMode`](src/particle/config.rs#L142) | Relative mode controlling whether particles move with the emitter.  # Variant... |
| `struct` | [`ParticleConfig`](src/particle/config.rs#L208) | Configuration for a particle emitter. Consult the module-level documentation ... |

### `particle::emission`

> Particle spawn offset calculations for area distribution and emission shapes.

*Source: [src/particle/emission.rs](src/particle/emission.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`emission_offset`](src/particle/emission.rs#L13) | Compute an emission offset `(dx, dy)` based on the config's area distribution... |
| `fn` | [`emission_shape_offset`](src/particle/emission.rs#L76) | Compute an emission offset `(dx, dy)` based on the emission shape.  # Paramet... |

### `particle::emitter`

> Particle emitter struct and update/draw logic.

*Source: [src/particle/emitter.rs](src/particle/emitter.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ParticleSystem`](src/particle/emitter.rs#L26) | An emitter-based particle system. Consult the module-level documentation for ... |

### `particle::math`

> Math helper functions for particle interpolation and random sampling.

*Source: [src/particle/math.rs](src/particle/math.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`interpolate_alphas`](src/particle/math.rs#L95) | Interpolate a multi-stop alpha array at normalised time `t` (0 = birth, 1 = d... |
| `fn` | [`interpolate_colors`](src/particle/math.rs#L62) | Interpolate a multi-stop color array at normalised time `t` (0 = birth, 1 = d... |
| `fn` | [`interpolate_sizes`](src/particle/math.rs#L33) | Interpolate a multi-stop size array at normalised time `t` (0 = birth, 1 = de... |
| `fn` | [`lerp`](src/particle/math.rs#L12) | Linearly interpolate between `a` and `b` by factor `t`.  # Parameters - `a` —... |
| `fn` | [`rand_normal`](src/particle/math.rs#L129) | Approximate a standard-normal random value using Box-Muller transform.  # Ret... |
| `fn` | [`rand_range`](src/particle/math.rs#L118) | Sample a uniform random value in `[min, max]`.  # Parameters - `min` — `f32`.... |

### `particle::particle`

> Individual particle data structure.

*Source: [src/particle/particle.rs](src/particle/particle.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Particle`](src/particle/particle.rs#L21) | A single particle managed by a `ParticleSystem`.  # Fields - `x` — `f32`. - `... |

### `particle::shapes`

> Particle shape enum defining the geometric primitive used to render untextured particles.

*Source: [src/particle/shapes.rs](src/particle/shapes.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ParticleShape`](src/particle/shapes.rs#L16) | Geometric shape used when drawing untextured particles.  Defaults to `Particl... |

## `pathfinding` {#pathfinding}

### `pathfinding`

> Grid-based pathfinding: A★, HPA★, flow fields, and unit-size-aware navigation. Also includes province-level A★ on adjacency graphs.  This module is part of Luna2D's `pathfinding` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/pathfinding/mod.rs](src/pathfinding/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`ai_flow_field`](src/pathfinding/mod.rs#L27) | AI flow field (PathGrid-based), moved from `ai/flowfield`. |
| `mod` | [`astar`](src/pathfinding/mod.rs#L11) | astar. |
| `mod` | [`async_pool`](src/pathfinding/mod.rs#L13) | async_pool. |
| `mod` | [`flow_field`](src/pathfinding/mod.rs#L15) | flow_field. |
| `mod` | [`hpa`](src/pathfinding/mod.rs#L17) | hpa. |
| `mod` | [`nav_grid`](src/pathfinding/mod.rs#L19) | nav_grid. |
| `mod` | [`pathgrid`](src/pathfinding/mod.rs#L25) | PathGrid-based pathfinding, moved from `ai/pathgrid`. |
| `mod` | [`province_path`](src/pathfinding/mod.rs#L21) | Province-level pathfinding on adjacency graphs. |
| `mod` | [`unit_pathfinder`](src/pathfinding/mod.rs#L23) | unit_pathfinder. |

### `pathfinding::ai_flow_field`

> Dijkstra-based flow field for crowd pathfinding. Moved from `ai/flowfield`; used by the Lua `luna.ai.newFlowField` API.  Note: this type operates on [`PathGrid`].  The higher-level [`crate::pathfinding::FlowField`] operates on [`NavGrid`] instead.

*Source: [src/pathfinding/ai_flow_field.rs](src/pathfinding/ai_flow_field.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`FlowField`](src/pathfinding/ai_flow_field.rs#L17) | BFS flow field that stores normalized direction vectors toward a goal.  # Fie... |

### `pathfinding::astar`

> A★ search with octile/Manhattan heuristic and Theta★ path smoothing.  This module is part of Luna2D's `pathfinding` subsystem and provides the implementation details for astar-related operations and data management. Primary functions: `astar()`, `line_of_sight()`, `smooth_path()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/pathfinding/astar.rs](src/pathfinding/astar.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`astar`](src/pathfinding/astar.rs#L80) | Run A★ search on `grid` from `start` to `goal`.  # Parameters - `grid` — `&Na... |
| `fn` | [`line_of_sight`](src/pathfinding/astar.rs#L287) | Check line-of-sight between two cells using Bresenham's algorithm,  # Paramet... |
| `fn` | [`smooth_path`](src/pathfinding/astar.rs#L329) | Smooth a path by removing unnecessary waypoints via line-of-sight checks  # P... |

### `pathfinding::async_pool`

> Thread pool for asynchronous path computation.  Pathfinding requests are submitted with an ID and processed on worker threads. Results are collected via non-blocking [`PathThreadPool::poll`].  This module is part of Luna2D's `pathfinding` subsystem and provides the implementation details for async pool-related operations and data management. Key types exported from this module: `PathThreadPool`. Primary functions: `new()`, `submit()`, `poll()`, `cancel()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/pathfinding/async_pool.rs](src/pathfinding/async_pool.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PathThreadPool`](src/pathfinding/async_pool.rs#L47) | A pool of worker threads that process pathfinding requests asynchronously.  #... |
| `type` | [`PathResult`](src/pathfinding/async_pool.rs#L36) | A completed path result returned by [`PathThreadPool::poll`]. |

### `pathfinding::flow_field`

> Flow field pathfinding for steering many units toward one or more targets.  This module is part of Luna2D's `pathfinding` subsystem and provides the implementation details for flow field-related operations and data management. Key types exported from this module: `FlowField`. Primary functions: `new()`, `calculate()`, `calculate_multi()`, `get_direction()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/pathfinding/flow_field.rs](src/pathfinding/flow_field.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`FlowField`](src/pathfinding/flow_field.rs#L29) | A pre-computed flow field that stores a direction vector and integrated cost ... |

### `pathfinding::hpa`

> Hierarchical Pathfinding A★ (HPA★) for fast long-range queries.  Divides the grid into chunks, finds boundary entrances, builds an abstract graph, and searches at the chunk level before refining to tile-level paths.

*Source: [src/pathfinding/hpa.rs](src/pathfinding/hpa.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`build_abstract`](src/pathfinding/hpa.rs#L134) | Build the abstract graph from a `NavGrid`.  # Parameters - `grid` — `&NavGrid... |
| `fn` | [`hpa_star`](src/pathfinding/hpa.rs#L379) | Run HPA★ from `start` to `goal` on the abstract graph, then refine to tiles. ... |
| `fn` | [`is_reachable`](src/pathfinding/hpa.rs#L516) | Check if `goal` is reachable from `start` using the abstract graph.  # Parame... |
| `struct` | [`AbstractEdge`](src/pathfinding/hpa.rs#L18) | An edge in the abstract graph connecting two entrance nodes.  # Fields - `to`... |
| `struct` | [`AbstractGraph`](src/pathfinding/hpa.rs#L79) | Pre-computed abstract graph for hierarchical A★ queries.  # Fields - `nodes` ... |
| `struct` | [`AbstractNode`](src/pathfinding/hpa.rs#L32) | A node in the abstract graph, representing an entrance point on a chunk bound... |
| `struct` | [`Chunk`](src/pathfinding/hpa.rs#L52) | A chunk region of the grid used during abstract graph construction.  # Fields... |

### `pathfinding::nav_grid`

> Navigation grid with per-cell traversal costs and diagonal movement modes.  This module is part of Luna2D's `pathfinding` subsystem and provides the implementation details for nav grid-related operations and data management. Key types exported from this module: `DiagonalMode`, `NavGrid`. Primary functions: `from_lua_str()`, `new()`, `from_costs()`, `get_width()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/pathfinding/nav_grid.rs](src/pathfinding/nav_grid.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`DiagonalMode`](src/pathfinding/nav_grid.rs#L18) | Controls how diagonal movement is handled during pathfinding.  # Variants - `... |
| `struct` | [`NavGrid`](src/pathfinding/nav_grid.rs#L62) | A 2D grid of traversal costs used by pathfinding algorithms.  Cells are addre... |

### `pathfinding::pathgrid`

> Weighted grid pathfinding (A*, Dijkstra) with obstacle support. Moved from `ai/pathgrid`; used by the Lua `luna.ai.newPathGrid` API.  This module is part of Luna2D's `pathfinding` subsystem and provides the implementation details for pathgrid-related operations and data management. Key types exported from this module: `Cell`, `PathGrid`. Primary functions: `new()`, `in_bounds()`, `set_walkable()`, `is_walkable()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/pathfinding/pathgrid.rs](src/pathfinding/pathgrid.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Cell`](src/pathfinding/pathgrid.rs#L21) | Single cell of the navigation grid. Consult the module-level documentation fo... |
| `struct` | [`PathGrid`](src/pathfinding/pathgrid.rs#L74) | A★ navigation grid. Consult the module-level documentation for the broader us... |

### `pathfinding::province_path`

> Province-level pathfinding on an adjacency graph using A* and Dijkstra.  Operates on abstract province graphs: neighbors, centroids, edge tags. Does not depend on `ProvinceMap` directly — accepts data via function arguments.

*Source: [src/pathfinding/province_path.rs](src/pathfinding/province_path.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`find_province_path`](src/pathfinding/province_path.rs#L132) | Find a path between two provinces using A* with centroid distance heuristic. ... |
| `fn` | [`province_reachable`](src/pathfinding/province_path.rs#L243) | Find all provinces reachable from `start` within a cost budget using Dijkstra... |
| `struct` | [`ProvinceCostFn`](src/pathfinding/province_path.rs#L35) | Configurable cost function for province pathfinding.  `tag_costs` adds extra ... |
| `struct` | [`ProvincePath`](src/pathfinding/province_path.rs#L15) | A path through the province adjacency graph.  # Fields - `provinces` — `Vec<u... |

### `pathfinding::unit_pathfinder`

> Unit-aware pathfinder with result caching and convenience methods.  This module is part of Luna2D's `pathfinding` subsystem and provides the implementation details for unit pathfinder-related operations and data management. Key types exported from this module: `Waypoint`, `UnitPathfinder`. Primary functions: `new()`, `find_path()`, `find_path_smooth()`, `get_path_length()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/pathfinding/unit_pathfinder.rs](src/pathfinding/unit_pathfinder.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`UnitPathfinder`](src/pathfinding/unit_pathfinder.rs#L49) | A pathfinder that operates on a shared `NavGrid` with optional result caching... |
| `struct` | [`Waypoint`](src/pathfinding/unit_pathfinder.rs#L24) | A waypoint along a computed path. Consult the module-level documentation for ... |

## `physics` {#physics}

### `physics`

> Physics simulation module backed by rapier2d.  Provides rigid-body simulation with circles, rectangles, sensors, raycasting, joints, and collision event recording.  This module is part of Luna2D's `physics` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/physics/mod.rs](src/physics/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`body`](src/physics/mod.rs#L13) | Body types, shapes, and the `Body` struct used by the physics world. |
| `mod` | [`collision`](src/physics/mod.rs#L15) | Collision event structs returned by `World::get_collision_events`. |
| `mod` | [`shape`](src/physics/mod.rs#L17) | Extended shape types: Polygon, Edge, and Chain colliders. |
| `mod` | [`world`](src/physics/mod.rs#L19) | The `World` struct and all simulation management: bodies, joints, raycasting,... |

### `physics::body`

> Body implementation for the `physics` subsystem.  This module is part of Luna2D's `physics` subsystem and provides the implementation details for body-related operations and data management. Key types exported from this module: `BodyType`, `BodyShape`, `Body`. Primary functions: `new()`, `new_circle()`, `new_polygon()`, `new_edge()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/physics/body.rs](src/physics/body.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`BodyShape`](src/physics/body.rs#L35) | Describes the collision geometry of a body.  # Variants - `Rect { width, heig... |
| `enum` | [`BodyType`](src/physics/body.rs#L21) | Determines whether a physics body is affected by forces and gravity.  # Varia... |
| `struct` | [`Body`](src/physics/body.rs#L67) | A rigid body with position, velocity, mass, shape, and restitution.  Bodies l... |

### `physics::collision`

> Collision implementation for the `physics` subsystem.  This module is part of Luna2D's `physics` subsystem and provides the implementation details for collision-related operations and data management. Key types exported from this module: `CollisionInfo`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/physics/collision.rs](src/physics/collision.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`CollisionInfo`](src/physics/collision.rs#L20) | Collision contact data: penetration depth and separating normal.  Retained fo... |

### `physics::shape`

> Extended shape types for physics bodies.  Provides polygon, edge, and chain shapes beyond the basic rect/circle.  This module is part of Luna2D's `physics` subsystem and provides the implementation details for shape-related operations and data management. Key types exported from this module: `Shape`. Primary functions: `to_rapier_collider()`, `regular_polygon()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/physics/shape.rs](src/physics/shape.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Shape`](src/physics/shape.rs#L27) | Extended collision shape for physics bodies.  Goes beyond `BodyShape` to supp... |
| `struct` | [`StandaloneShape`](src/physics/shape.rs#L137) | A standalone shape value holding geometry and default fixture parameters.  Cr... |

### `physics::world`

> Rapier2d-backed physics world with backward-compatible Luna2D API.  Wraps the rapier2d simulation pipeline while exposing the same `Body`-based interface. The `Body` struct is a sync buffer: mutations via `get_body_mut()` are flushed into rapier on the next `step()` call, and rapier's computed positions/velocities are read back after each step.

*Source: [src/physics/world.rs](src/physics/world.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`BodyContact`](src/physics/world.rs#L70) | Collision event generated by `World::step` when two bodies start overlapping.... |
| `struct` | [`ContactInfo`](src/physics/world.rs#L105) | Contact information between two bodies from the narrow phase.  # Fields - `bo... |
| `struct` | [`RaycastHit`](src/physics/world.rs#L85) | Result of a `World::raycast` query. Consult the module-level documentation fo... |
| `struct` | [`World`](src/physics/world.rs#L154) | Simulates a 2D physics world using rapier2d.  Bodies are stored as a `Vec<Bod... |

## `postfx` {#postfx}

### `postfx`

> Provides a stackable post-processing pipeline with built-in effects (bloom, blur, CRT, vignette, chromatic aberration, colour grading, godrays, pixelate, sepia, grayscale, invert, scanlines, edge-detect, hue-shift, noise) and custom shader passes. Effects are organised into a `PostFxStack` that captures the rendered scene via ping-pong canvases, applies all enabled effects in insertion order, and outputs the final composited result.  This is a **pure CPU data-model module** -- it has no GPU dependencies. GPU application of the stack is handled in `lua_api`. Extracted from `graphics` so that other modules can reference postfx types without pulling in the full rendering pipeline.

*Source: [src/postfx/mod.rs](src/postfx/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`effect`](src/postfx/mod.rs#L19) | Per-effect parameter bag and helper methods. |
| `mod` | [`effect_type`](src/postfx/mod.rs#L17) | Built-in and custom effect type enum. |
| `mod` | [`stack`](src/postfx/mod.rs#L21) | Ordered chain of effects for full-screen post-processing. |

### `postfx::effect`

*Source: [src/postfx/effect.rs](src/postfx/effect.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PostFxEffect`](src/postfx/effect.rs#L20) | A single post-processing effect with named float parameters.  Acts as a param... |

### `postfx::effect_type`

*Source: [src/postfx/effect_type.rs](src/postfx/effect_type.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`PostFxEffectType`](src/postfx/effect_type.rs#L29) | Built-in effect types for the post-processing pipeline.  Each variant maps to... |

### `postfx::stack`

*Source: [src/postfx/stack.rs](src/postfx/stack.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`PostFxStack`](src/postfx/stack.rs#L25) | An ordered chain of effects that captures and processes the rendered scene.  ... |

## `province_map` {#province-map}

### `province_map`

> Province map module — spatial province data from colour-coded PNG images or definitions.  A province map is a pure spatial data structure, like a tilemap — game-specific data (owner, terrain type, politics) lives externally in the stats or entity modules. Worldgen and minimap rendering are handled by game-developer code using the `crate::math` Voronoi utilities and existing minimap infrastructure.

*Source: [src/province_map/mod.rs](src/province_map/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`adjacency`](src/province_map/mod.rs#L13) | Adjacency detection between provinces from the pixel grid. |
| `mod` | [`borders`](src/province_map/mod.rs#L15) | Border segment extraction for province rendering. |
| `mod` | [`core`](src/province_map/mod.rs#L9) | Core data structures: [`Province`], [`AdjacencyEdge`], [`ProvinceMap`]. |
| `mod` | [`definition_loader`](src/province_map/mod.rs#L23) | Load province definitions from structured data (TOML tables, Lua tables). |
| `mod` | [`events`](src/province_map/mod.rs#L21) | Province map event bus. |
| `mod` | [`graph_bridge`](src/province_map/mod.rs#L25) | Bridge to convert adjacency data to a [`crate::graph::Graph`]. |
| `mod` | [`loader`](src/province_map/mod.rs#L11) | Province map loader from colour-coded PNG images. |
| `mod` | [`map_mode`](src/province_map/mod.rs#L19) | Map mode colour assignment for province rendering. |
| `mod` | [`positions`](src/province_map/mod.rs#L17) | Province centre-point calculation. |

### `province_map::adjacency`

> Adjacency detection between provinces from the pixel grid.  Scans the province map's pixel-lookup buffer in a single O(W*H) pass, checking right and bottom neighbours to discover shared borders.  This module is part of Luna2D's `province_map` subsystem and provides the implementation details for adjacency-related operations and data management. Primary functions: `detect_adjacency()`, `detect_adjacency_with_tags()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/province_map/adjacency.rs](src/province_map/adjacency.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`detect_adjacency`](src/province_map/adjacency.rs#L25) | Detect province adjacencies from the pixel grid using a single-pass scan.  Fo... |
| `fn` | [`detect_adjacency_with_tags`](src/province_map/adjacency.rs#L40) | Detect province adjacencies with optional tagged-pixel detection.  Operates i... |

### `province_map::borders`

> Border segment extraction for province rendering.  Converts adjacency edge border pixel coordinates into ordered polylines that can be rendered as province borders. Tag-based filtering allows extracting specific border types (e.g. rivers, walls).

*Source: [src/province_map/borders.rs](src/province_map/borders.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`extract_all_borders`](src/province_map/borders.rs#L65) | Convert all adjacency edge border segments into ordered polylines.  Each adja... |
| `fn` | [`extract_borders_by_property`](src/province_map/borders.rs#L131) | Extract borders where the two provinces have different group values.  The cal... |
| `fn` | [`extract_borders_with_tag`](src/province_map/borders.rs#L112) | Extract only borders that have a specific tag.  Use this to get e.g. all rive... |
| `struct` | [`BorderSegment`](src/province_map/borders.rs#L45) | A border segment as a list of pixel coordinates forming a polyline.  # Fields... |
| `struct` | [`BorderStyle`](src/province_map/borders.rs#L18) | Visual style for a border line. Consult the module-level documentation for th... |

### `province_map::core`

> Core province map data structures: [`Province`], [`AdjacencyEdge`], and [`ProvinceMap`].  This module is part of Luna2D's `province_map` subsystem and provides the implementation details for core-related operations and data management. Key types exported from this module: `ProvinceError`, `Province`, `AdjacencyEdge`, `ProvinceMap`. Primary functions: `new()`, `new()`, `width()`, `height()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/province_map/core.rs](src/province_map/core.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ProvinceError`](src/province_map/core.rs#L27) | Error type for province map operations. Consult the module-level documentatio... |
| `struct` | [`AdjacencyEdge`](src/province_map/core.rs#L106) | An edge between two adjacent provinces. Consult the module-level documentatio... |
| `struct` | [`Province`](src/province_map/core.rs#L52) | A single province in a province map. Consult the module-level documentation f... |
| `struct` | [`ProvinceMap`](src/province_map/core.rs#L130) | The complete province map — contains all provinces and their relationships.  ... |

### `province_map::definition_loader`

> Province definition loader — build a [`ProvinceMap`] from structured data.  Instead of parsing a colour-coded PNG, this module creates a province map from a list of [`ProvinceDefinition`] records (e.g. loaded from TOML or a Lua table). Useful for games that define provinces declaratively.

*Source: [src/province_map/definition_loader.rs](src/province_map/definition_loader.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`load_from_definitions`](src/province_map/definition_loader.rs#L49) | Build a [`ProvinceMap`] from a list of province definitions.  Creates a logic... |
| `struct` | [`ProvinceDefinition`](src/province_map/definition_loader.rs#L20) | A single province definition with metadata and neighbour list.  # Fields - `i... |

### `province_map::events`

> Event bus for province map lifecycle events.  Wraps [`crate::event::EventQueue`] and provides typed emit helpers covering the full lifecycle of a province map: loading, adjacency, borders, map modes, position calculation, and interaction events.  # Event names  | Name | Args | Meaning | |---|---|---| | `map_loaded` | `count` | Province map loaded/created with `count` provinces | | `adjacency_detected` | `edge_count` | Adjacency scan completed | | `adjacency_changed` | `a`, `b` | An edge between provinces `a` and `b` was added or modified | | `adjacency_removed` | `a`, `b` | The edge between provinces `a` and `b` was removed | | `borders_extracted` | `segment_count` | Border segments extracted from adjacency data | | `map_mode_applied` | `name` | A map mode colour pass was applied | | `positions_calculated` | `count` | Center positions calculated for `count` provinces | | `province_selected` | `id`, `x`, `y` | A province was clicked or selected at map coords `(x, y)` | | `province_deselected` | `id` | A previously selected province was deselected | | `province_hovered` | `id`, `x`, `y` | Cursor moved over province `id` at `(x, y)` | | `province_added` | `id` | A new province was inserted into the map | | `province_removed` | `id` | A province was removed from the map |

*Source: [src/province_map/events.rs](src/province_map/events.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ProvinceMapEventBus`](src/province_map/events.rs#L35) | Event bus for province map events. Consult the module-level documentation for... |

### `province_map::graph_bridge`

> Bridge between province map adjacency and the [`crate::graph`] module.  Converts a [`ProvinceMap`]'s provinces and adjacency edges into a directed [`Graph`], allowing graph algorithms (Dijkstra, flow, connected components) to operate on the province network.

*Source: [src/province_map/graph_bridge.rs](src/province_map/graph_bridge.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`adjacency_to_graph`](src/province_map/graph_bridge.rs#L27) | Convert province map adjacency into a [`Graph`].  Each province becomes a nod... |

### `province_map::loader`

> Province map loader: parses colour-coded PNG images into [`ProvinceMap`].  Converts each pixel's RGB colour to a province ID via [`color_to_id`], then accumulates area, centroid, and bounding-box statistics in a single pass.

*Source: [src/province_map/loader.rs](src/province_map/loader.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`color_to_id`](src/province_map/loader.rs#L24) | Convert an RGB colour triple to a province ID.  The encoding is `(r << 16) | ... |

### `province_map::map_mode`

> Map mode colour resolution for province map rendering.  Map modes assign colours to provinces for different visualisations — political ownership, terrain type, economic value, etc. Colour data is passed in directly (no dependency on an external property system).

*Source: [src/province_map/map_mode.rs](src/province_map/map_mode.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`MapModeColorFn`](src/province_map/map_mode.rs#L39) | How colours are assigned to provinces. Consult the module-level documentation... |
| `fn` | [`resolve_colors`](src/province_map/map_mode.rs#L145) | Resolve colours for every pixel in the map, producing an RGBA pixel buffer.  ... |
| `struct` | [`MapMode`](src/province_map/map_mode.rs#L17) | A named map mode with its colour assignment function.  # Fields - `name` — `S... |

### `province_map::positions`

> Auto-position calculation for province centers.  Provides an algorithm to find a good interior position within a province for use as its center point (e.g. capital placement, label anchor).  This module is part of Luna2D's `province_map` subsystem and provides the implementation details for positions-related operations and data management. Primary functions: `calculate_capital()`, `calculate_all_positions()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/province_map/positions.rs](src/province_map/positions.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`calculate_all_positions`](src/province_map/positions.rs#L102) | Compute and set the `center` field for all provinces.  Finds the best interio... |
| `fn` | [`calculate_capital`](src/province_map/positions.rs#L30) | Find a good interior position for a province center.  Uses an approximate dis... |

## `quest` {#quest}

### `quest`

> Quest and objective tracking system for Luna2D games.  Provides `Quest`, `Objective`, `QuestStage`, `JournalEntry`, and `QuestLog` for building RPG-style quest systems with stages, conditions, and journal notes.

*Source: [src/quest/mod.rs](src/quest/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`journal`](src/quest/mod.rs#L16) | Journal entry. |
| `mod` | [`log`](src/quest/mod.rs#L25) | Quest log. |
| `mod` | [`objective`](src/quest/mod.rs#L12) | Objectives and quest stages. |
| `mod` | [`quest`](src/quest/mod.rs#L21) | Quest definition. |
| `mod` | [`status`](src/quest/mod.rs#L8) | Quest and objective status enums. |

### `quest::journal`

> Journal entry for recording quest events.  This module is part of Luna2D's `quest` subsystem and provides the implementation details for journal-related operations and data management. Key types exported from this module: `JournalEntry`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/quest/journal.rs](src/quest/journal.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`JournalEntry`](src/quest/journal.rs#L21) | A timestamped text entry in a quest's journal.  # Fields - `index` — `u32`. -... |

### `quest::log`

> Quest log: the player's active quest tracker.  This module is part of Luna2D's `quest` subsystem and provides the implementation details for log-related operations and data management. Key types exported from this module: `QuestLog`. Primary functions: `new()`, `add_quest()`, `get_quest()`, `get_quest_mut()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/quest/log.rs](src/quest/log.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`QuestLog`](src/quest/log.rs#L29) | Registry of all quests for a game session.  Quests are stored by their id. Ac... |

### `quest::objective`

> Quest objectives and quest stages.  This module is part of Luna2D's `quest` subsystem and provides the implementation details for objective-related operations and data management. Key types exported from this module: `Objective`, `QuestStage`. Primary functions: `new()`, `advance()`, `set_progress()`, `is_complete()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/quest/objective.rs](src/quest/objective.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Objective`](src/quest/objective.rs#L32) | A single trackable task within a quest. Consult the module-level documentatio... |
| `struct` | [`QuestStage`](src/quest/objective.rs#L152) | A named group of objectives that represent one phase of a quest.  # Fields - ... |

### `quest::quest`

> Quest definition with stages, objectives, and journal.  This module is part of Luna2D's `quest` subsystem and provides the implementation details for quest-related operations and data management. Key types exported from this module: `Quest`. Primary functions: `new()`, `add_stage()`, `get_current_stage()`, `get_stage()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/quest/quest.rs](src/quest/quest.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Quest`](src/quest/quest.rs#L39) | A quest with stages, objectives, and a journal.  Stages are ordered; the ques... |

### `quest::status`

> Quest and objective status enums.  This module is part of Luna2D's `quest` subsystem and provides the implementation details for status-related operations and data management. Key types exported from this module: `QuestStatus`, `ObjectiveStatus`. Primary functions: `from_str()`, `as_str()`, `from_str()`, `as_str()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/quest/status.rs](src/quest/status.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ObjectiveStatus`](src/quest/status.rs#L85) | Lifecycle state of a single objective. Consult the module-level documentation... |
| `enum` | [`QuestStatus`](src/quest/status.rs#L25) | Lifecycle state of a quest. Consult the module-level documentation for the br... |

## `savegame` {#savegame}

### `savegame`

> Save/load slot system with collectors, schema versioning, dirty tracking, and auto-save.  This module is part of Luna2D's `savegame` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/savegame/mod.rs](src/savegame/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`save_data`](src/savegame/mod.rs#L11) | Save Data sub-module. |

### `savegame::save_data`

> Save/load slot system with collectors, schema versioning, dirty tracking, and auto-save.  This module is part of Luna2D's `savegame` subsystem and provides the implementation details for save data-related operations and data management. Key types exported from this module: `SlotMeta`, `SaveManager`, `SaveValue`. Primary functions: `new()`, `register()`, `unregister()`, `registered_names()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/savegame/save_data.rs](src/savegame/save_data.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`SaveValue`](src/savegame/save_data.rs#L260) | A simple value type matching the Lua subset we can serialize.  # Variants - `... |
| `fn` | [`serialize_table`](src/savegame/save_data.rs#L209) | Serialize a simple Lua-compatible value hierarchy into a `return { ... }` str... |
| `fn` | [`serialize_value`](src/savegame/save_data.rs#L241) | Serialize a single value. Consult the module-level documentation for the broa... |
| `struct` | [`SaveManager`](src/savegame/save_data.rs#L47) | Pure-data save manager providing registration of named collectors, schema ver... |
| `struct` | [`SlotMeta`](src/savegame/save_data.rs#L22) | Metadata extracted from a save slot. Consult the module-level documentation f... |

## `scene` {#scene}

### `scene`

> Scene stack module for managing game scene lifecycle, transitions, and depth-sorted rendering.  Provides a LIFO stack of Lua scene tables with lifecycle callbacks (`enter`, `leave`, `pause`, `resume`, `update`, `draw`), animated transitions (fade, slide), a named scene registry, inter-scene data passing, and a `DepthSorter` for z-ordered draw batching.

*Source: [src/scene/mod.rs](src/scene/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`depth_sorter`](src/scene/mod.rs#L8) | depth_sorter. |
| `mod` | [`stack`](src/scene/mod.rs#L10) | stack. |
| `mod` | [`transition`](src/scene/mod.rs#L12) | transition. |

### `scene::depth_sorter`

> Per-frame depth-sorted draw batcher.  This module is part of Luna2D's `scene` subsystem and provides the implementation details for depth sorter-related operations and data management. Key types exported from this module: `DepthEntry`, `DepthSorter`. Primary functions: `new()`, `add()`, `add_object()`, `sort()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/scene/depth_sorter.rs](src/scene/depth_sorter.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`DepthEntry`](src/scene/depth_sorter.rs#L17) | Entry in the depth-sorted draw queue. Consult the module-level documentation ... |
| `struct` | [`DepthSorter`](src/scene/depth_sorter.rs#L33) | Per-frame depth-sorted draw batcher. Consult the module-level documentation f... |

### `scene::stack`

> LIFO scene stack with registry and inter-scene data store.  This module is part of Luna2D's `scene` subsystem and provides the implementation details for stack-related operations and data management. Key types exported from this module: `SceneStack`. Primary functions: `new()`, `next_scene_id()`, `push()`, `pop()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/scene/stack.rs](src/scene/stack.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SceneStack`](src/scene/stack.rs#L29) | The scene stack manages a LIFO stack of scene references.  Scenes are identif... |
| `type` | [`SceneId`](src/scene/stack.rs#L16) | Unique identifier for a scene in the stack. |

### `scene::transition`

> Visual transition types and active transition state for scene changes.  This module is part of Luna2D's `scene` subsystem and provides the implementation details for transition-related operations and data management. Key types exported from this module: `TransitionType`, `ActiveTransition`. Primary functions: `from_lua_str()`, `new()`, `progress()`, `is_complete()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/scene/transition.rs](src/scene/transition.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`TransitionType`](src/scene/transition.rs#L21) | Visual transition types between scenes. Consult the module-level documentatio... |
| `struct` | [`ActiveTransition`](src/scene/transition.rs#L62) | Active transition state tracking progress between two scenes.  # Fields - `tr... |

## `stats` {#stats}

### `stats`

> RPG character sheet and stat system.  Provides a flexible attribute system with buffs, derived stats, traits, skills, perks, XP/levelling, action points, morale, and resistances.

*Source: [src/stats/mod.rs](src/stats/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`attribute`](src/stats/mod.rs#L27) | How a buff stacks with existing buffs of the same name. Stat attributes, buff... |
| `mod` | [`sheet`](src/stats/mod.rs#L35) | Character sheet and stats registry. |
| `mod` | [`skill`](src/stats/mod.rs#L31) | Skills, perks, traits, action points, morale, level thresholds. |

### `stats::attribute`

> Stat attributes, stack mode, and buff descriptors.  This module is part of Luna2D's `stats` subsystem and provides the implementation details for attribute-related operations and data management. Key types exported from this module: `StackMode`, `Buff`, `Attribute`. Primary functions: `new()`, `is_expired()`, `new()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/stats/attribute.rs](src/stats/attribute.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`StackMode`](src/stats/attribute.rs#L20) | How a buff stacks with existing buffs of the same name.  # Variants - `Reappl... |
| `struct` | [`Attribute`](src/stats/attribute.rs#L100) | A named stat attribute with base value, constraints, and regen.  # Fields - `... |
| `struct` | [`Buff`](src/stats/attribute.rs#L39) | A single stat modifier attached to an attribute.  # Fields - `stat` — `String... |

### `stats::sheet`

> Character sheet (Sheet) and stats registry (StatsRegistry).  This module is part of Luna2D's `stats` subsystem and provides the implementation details for sheet-related operations and data management. Key types exported from this module: `Sheet`, `StatsRegistry`. Primary functions: `new()`, `define()`, `get()`, `get_base()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/stats/sheet.rs](src/stats/sheet.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Sheet`](src/stats/sheet.rs#L38) | The central character sheet, holding all stat data.  # Fields - `attributes` ... |
| `struct` | [`StatsRegistry`](src/stats/sheet.rs#L478) | Global registry for traits and archetypes. Separate from Sheet for sharing.  ... |

### `stats::skill`

> Skills, perks, trait definitions, action points, morale, and level thresholds.  This module is part of Luna2D's `stats` subsystem and provides the implementation details for skill-related operations and data management. Key types exported from this module: `Skill`, `Perk`, `TraitDef`, `ActionPoints`, `Morale`. Primary functions: `new()`, `new()`, `new()`, `new()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/stats/skill.rs](src/stats/skill.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`LevelThresholds`](src/stats/skill.rs#L178) | Level threshold setting: either a static table or a formula.  # Variants - `P... |
| `struct` | [`ActionPoints`](src/stats/skill.rs#L114) | Action point tracking for turn-based games.  # Fields - `current` — `f64`. - ... |
| `struct` | [`Morale`](src/stats/skill.rs#L142) | Morale state with panic/berserk thresholds.  # Fields - `current` — `f64`. - ... |
| `struct` | [`Perk`](src/stats/skill.rs#L70) | A named perk requiring a minimum level to acquire.  # Fields - `require_level... |
| `struct` | [`Skill`](src/stats/skill.rs#L22) | A named skill with cooldown, resource cost, and level tracking.  # Fields - `... |
| `struct` | [`TraitDef`](src/stats/skill.rs#L103) | A named trait definition (a bundle of buff descriptors).  # Fields - `s` — `(... |

## `thread` {#thread}

### `thread`

> Thread infrastructure: inter-thread channel and background Lua worker.  This module is part of Luna2D's `thread` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/thread/mod.rs](src/thread/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`channel`](src/thread/mod.rs#L10) | Channel sub-module. |
| `mod` | [`worker`](src/thread/mod.rs#L12) | Worker sub-module. |

### `thread::channel`

> Thread-safe channel for Lua inter-thread communication.  Provides an MPMC (multi-producer, multi-consumer) queue that can be shared between the main game thread and background Lua threads via `Arc<Channel>`. Only primitive Lua values (nil, bool, number, string) can cross thread boundaries.

*Source: [src/thread/channel.rs](src/thread/channel.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ChannelValue`](src/thread/channel.rs#L25) | Serializable values that can be sent between threads.  # Variants - `Nil` — N... |
| `fn` | [`channel_value_to_lua`](src/thread/channel.rs#L290) | Convert a `ChannelValue` back into a Lua value.  # Parameters - `lua` — `&Lua... |
| `fn` | [`lua_to_channel_value`](src/thread/channel.rs#L269) | Convert a Lua value into a `ChannelValue` for cross-thread transfer.  # Param... |
| `struct` | [`Channel`](src/thread/channel.rs#L47) | Thread-safe MPMC channel for Lua inter-thread communication.  Internally uses... |
| `struct` | [`LuaChannel`](src/thread/channel.rs#L210) | Lua UserData wrapper for a thread-safe channel.  # Fields - `inner` — `Arc<Ch... |

### `thread::worker`

> Background Lua thread with independent VM.  Each `LuaThread` spawns an OS thread running its own `mlua::Lua` instance. Communication with the main thread happens exclusively through `Channel` objects — no Lua state is shared across threads.

*Source: [src/thread/worker.rs](src/thread/worker.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`ThreadState`](src/thread/worker.rs#L21) | Execution state of a background Lua thread.  # Variants - `Pending` — Pending... |
| `struct` | [`LuaThread`](src/thread/worker.rs#L43) | A background Lua thread running its own VM.  Created via `luna.thread.newThre... |

## `tilemap` {#tilemap}

### `tilemap`

> Tilemap engine module — TileSet, TileMap, AutoTileSheet, IsoMap, ChunkMap, TMX loader, and procedural generation types.  This module is part of Luna2D's `tilemap` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/tilemap/mod.rs](src/tilemap/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`autotile_sheet`](src/tilemap/mod.rs#L10) | Auto-tile atlas with bitmask-based tile selection. |
| `mod` | [`chunk`](src/tilemap/mod.rs#L12) | Sparse chunk-based tilemap storage for large and infinite maps. |
| `mod` | [`coords`](src/tilemap/mod.rs#L14) | Coordinate helpers for tile ↔ world conversions. |
| `mod` | [`isomap`](src/tilemap/mod.rs#L16) | Multi-level isometric tilemap with painter's-algorithm draw order. |
| `mod` | [`mapgen`](src/tilemap/mod.rs#L18) | Procedural map generation scripts and room placement. |
| `mod` | [`tile_walker`](src/tilemap/mod.rs#L41) | Grid-direction walker for tile stepping. |
| `mod` | [`tilemap`](src/tilemap/mod.rs#L21) | Core tilemap: layers, tile CRUD, sweep queries, and serialization. |
| `mod` | [`tileset`](src/tilemap/mod.rs#L23) | Tile set definition with per-tile properties and animation frames. |
| `mod` | [`tmx`](src/tilemap/mod.rs#L25) | Tiled TMX/TSX map format parser. |

### `tilemap::autotile_sheet`

> AutoTileSheet — loads a pre-arranged autotile image and generates TileSet tiles + bitmask rules.  ## 48-tile quarter-tile format  For high-quality composite autotile rendering, [`AutoTileSheet`] supports a 48-tile sub-tile format where each full tile is assembled from **four quarter-tile pieces** (TL, TR, BL, BR).  The source image must be **`3*tile_w` × `4*tile_h` pixels** — a 6-column × 8-row grid of `(tile_w/2 × tile_h/2)` quarter pieces.  Call [`AutoTileSheet::get_quarter_rects`] with the raw 8-bit neighbour bitmask to obtain the four [`Rect`]s to blit.  Use [`AutoTileSheet::get_composite48_grid_quad`] when the source atlas stores the 48 pre-composed tile variants in a 6 × 8 full-tile grid instead.  ### Bitmask bit layout (raw 8-bit)  ```text bit 0 = N   bit 4 = NE bit 1 = E   bit 5 = SE bit 2 = S   bit 6 = SW bit 3 = W   bit 7 = NW ```

*Source: [src/tilemap/autotile_sheet.rs](src/tilemap/autotile_sheet.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`AutoTileLayout`](src/tilemap/autotile_sheet.rs#L36) | Predefined autotile sheet layout variants.  # Variants - `Blob47` — Blob47 va... |
| `struct` | [`AutoTileSheet`](src/tilemap/autotile_sheet.rs#L58) | An autotile sheet that maps layout conventions to bitmask-based tile selectio... |

### `tilemap::chunk`

> Chunk-based tilemap storage for large and infinite maps.  [`ChunkMap`] stores tiles in fixed-size square chunks using a [`HashMap`]. Only chunks that have been written to are allocated, allowing sparse, large, or infinite maps without up-front memory commitment.  Tile coordinates use `i32` to support negative positions (e.g., a map centred at the origin where negative X/Y are valid map space).

*Source: [src/tilemap/chunk.rs](src/tilemap/chunk.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ChunkMap`](src/tilemap/chunk.rs#L24) | A chunk-based tilemap that supports large and infinite maps through sparse st... |

### `tilemap::coords`

> Coordinate conversion functions for isometric and hexagonal tile grids.  This module is part of Luna2D's `tilemap` subsystem and provides the implementation details for coords-related operations and data management. Primary functions: `to_screen_iso()`, `from_screen_iso()`, `iso_rotate()`, `iso_direction_name()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/tilemap/coords.rs](src/tilemap/coords.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `fn` | [`from_screen_hex`](src/tilemap/coords.rs#L144) | Converts screen position back to axial hex coordinates (pointy-top layout).  ... |
| `fn` | [`from_screen_iso`](src/tilemap/coords.rs#L45) | Converts screen position back to tile coordinates for diamond isometric proje... |
| `fn` | [`hex_area`](src/tilemap/coords.rs#L303) | Returns all hex cells within `radius` distance (filled hex circle).  # Parame... |
| `fn` | [`hex_distance`](src/tilemap/coords.rs#L179) | Returns the hex distance between two axial coordinates using cube distance.  ... |
| `fn` | [`hex_line`](src/tilemap/coords.rs#L228) | Returns all hex cells along a line between two axial coordinates.  # Paramete... |
| `fn` | [`hex_neighbors`](src/tilemap/coords.rs#L158) | Returns the six axial neighbor offsets for pointy-top hexagonal grids.  # Par... |
| `fn` | [`hex_reflect`](src/tilemap/coords.rs#L361) | Reflects hex coordinates across an axis through the center.  # Parameters - `... |
| `fn` | [`hex_ring`](src/tilemap/coords.rs#L258) | Returns all cells at exactly `radius` distance from `(q, r)`.  # Parameters -... |
| `fn` | [`hex_rotate`](src/tilemap/coords.rs#L328) | Rotates hex coordinates `(q, r)` around `(center_q, center_r)` by `steps × 60... |
| `fn` | [`hex_round`](src/tilemap/coords.rs#L196) | Rounds fractional axial coordinates to the nearest hex cell using cube roundi... |
| `fn` | [`hex_spiral`](src/tilemap/coords.rs#L286) | Returns all hex cells from center outward to `radius`, ring by ring.  # Param... |
| `fn` | [`iso_direction_from_angle`](src/tilemap/coords.rs#L96) | Snaps an angle (in radians) to the nearest isometric direction (1–4).  # Para... |
| `fn` | [`iso_direction_name`](src/tilemap/coords.rs#L74) | Returns the name of an isometric direction (1–4).  # Parameters - `direction`... |
| `fn` | [`iso_rotate`](src/tilemap/coords.rs#L61) | Rotates an isometric direction (1–4) clockwise by `steps`.  # Parameters - `d... |
| `fn` | [`to_screen_hex`](src/tilemap/coords.rs#L127) | Converts axial hex coordinates to screen position (pointy-top layout).  # Par... |
| `fn` | [`to_screen_iso`](src/tilemap/coords.rs#L31) | Converts tile coordinates to screen position using diamond isometric projecti... |

### `tilemap::isomap`

> Multi-level isometric tilemap for OpenXcom-style rendering.  Each map cell holds four tile parts rendered in painter's algorithm order: `Floor`, `NorthWall`, `WestWall`, `Object`. Z-levels stack vertically with a configurable pixel offset so higher floors appear above lower ones.

*Source: [src/tilemap/isomap.rs](src/tilemap/isomap.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`IsoTilePart`](src/tilemap/isomap.rs#L22) | The four sub-slots within each isometric map cell, rendered in this order.  #... |
| `struct` | [`IsoDrawItem`](src/tilemap/isomap.rs#L167) | One renderable item produced by [`IsoMap::draw_iter`] in painter's order.  # ... |
| `struct` | [`IsoLevel`](src/tilemap/isomap.rs#L88) | One Z-level of the isometric map — a 2-D grid of [`IsoTile`]s.  # Fields - `w... |
| `struct` | [`IsoMap`](src/tilemap/isomap.rs#L217) | Multi-level isometric tilemap with painter's-algorithm draw iteration.  # Fie... |
| `struct` | [`IsoTile`](src/tilemap/isomap.rs#L72) | One map cell containing four GIDs, one per [`IsoTilePart`].  # Fields - `part... |

### `tilemap::mapgen`

> Procedural map generation — MapBlock, MapGroup, MapScript, MapGen.  This module is part of Luna2D's `tilemap` subsystem and provides the implementation details for mapgen-related operations and data management. Key types exported from this module: `Edge`, `MapBlock`, `MapGroup`, `StepType`, `ScriptStep`. Primary functions: `from_str()`, `as_str()`, `new()`, `set_tile()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/tilemap/mapgen.rs](src/tilemap/mapgen.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Edge`](src/tilemap/mapgen.rs#L24) | Cardinal edge direction for block-segment connectivity.  # Variants - `North`... |
| `enum` | [`LayerMode`](src/tilemap/mapgen.rs#L704) | How layers are managed during generation.  # Variants - `Unified` — Unified v... |
| `enum` | [`MapOrientation`](src/tilemap/mapgen.rs#L691) | Map orientation for visual layout hints.  # Variants - `TopDown` — TopDown va... |
| `enum` | [`MapSize`](src/tilemap/mapgen.rs#L719) | Predefined map size presets expressed in segment-grid units.  # Variants - `S... |
| `enum` | [`StepType`](src/tilemap/mapgen.rs#L418) | The type of operation a [`ScriptStep`] performs.  # Variants - `FillRandom` —... |
| `struct` | [`MapBlock`](src/tilemap/mapgen.rs#L82) | A prefab grid of tiles that can be stamped into a generated map.  Contains mu... |
| `struct` | [`MapGen`](src/tilemap/mapgen.rs#L807) | Top-level procedural map generator. Consult the module-level documentation fo... |
| `struct` | [`MapGroup`](src/tilemap/mapgen.rs#L292) | A biome-like container holding [`MapBlock`] prefabs and [`MapScript`] generat... |
| `struct` | [`MapScript`](src/tilemap/mapgen.rs#L606) | A named sequence of [`ScriptStep`]s that drives procedural generation.  # Fie... |
| `struct` | [`MapZone`](src/tilemap/mapgen.rs#L752) | A named horizontal zone within a generated map.  # Fields - `name` — `String`... |
| `struct` | [`ScriptStep`](src/tilemap/mapgen.rs#L509) | A single step in a [`MapScript`] with rich configuration.  # Fields - `step_t... |

### `tilemap::tile_walker`

> Tile-based first-person movement controller with cardinal directions.  This module is part of Luna2D's `tilemap` subsystem and provides the implementation details for tile walker-related operations and data management. Key types exported from this module: `Facing`, `TileWalker`. Primary functions: `parse()`, `to_str()`, `angle()`, `dx()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/tilemap/tile_walker.rs](src/tilemap/tile_walker.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`Facing`](src/tilemap/tile_walker.rs#L25) | Cardinal facing direction. Consult the module-level documentation for the bro... |
| `struct` | [`TileWalker`](src/tilemap/tile_walker.rs#L151) | Tile-based movement controller for first-person grid navigation.  Operates on... |

### `tilemap::tilemap`

> Main tile map container with layers, tiles, viewport, collision, and autotile support.  This module is part of Luna2D's `tilemap` subsystem and provides the implementation details for tilemap-related operations and data management. Key types exported from this module: `TileLayer`, `SweepResult`, `TileMap`. Primary functions: `new()`, `add_tileset()`, `get_tileset()`, `get_tileset_count()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/tilemap/tilemap.rs](src/tilemap/tilemap.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`SweepResult`](src/tilemap/tilemap.rs#L88) | Result of a swept-AABB collision test against solid tiles.  # Fields - `conta... |
| `struct` | [`TileLayer`](src/tilemap/tilemap.rs#L31) | A single layer of tiles in a [`TileMap`].  # Fields - `name` — `String`. - `w... |
| `struct` | [`TileMap`](src/tilemap/tilemap.rs#L115) | A 2D tile map composed of layers, tilesets, and viewport-clipped rendering st... |

### `tilemap::tileset`

> Tile set backed by a texture atlas with animation, solid flags, and autotile rules.  This module is part of Luna2D's `tilemap` subsystem and provides the implementation details for tileset-related operations and data management. Key types exported from this module: `TileAnimFrame`, `TileSet`. Primary functions: `new()`, `get_first_gid()`, `get_tile_count()`, `get_columns()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/tilemap/tileset.rs](src/tilemap/tileset.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`TileAnimFrame`](src/tilemap/tileset.rs#L21) | A single frame in a tile animation sequence.  # Fields - `tile_id` — `u32`. -... |
| `struct` | [`TileSet`](src/tilemap/tileset.rs#L46) | A tile set that maps local tile IDs to atlas regions, animations, and collisi... |

### `tilemap::tmx`

> Tiled TMX map format loader.  Parses `.tmx` files produced by the [Tiled Map Editor](https://www.mapeditor.org/). TMX is an XML-based format; this module uses [`roxmltree`] for parsing.  Supports: - Orthogonal, isometric, staggered, and hexagonal map orientations. - Tile data encoding: XML elements, CSV, and Base64 (with optional zlib/gzip compression). - Inline and external tileset references (TSX source returned as a path string for caller resolution). - Object layers and image layers (parsed to metadata; not converted to [`TileMap`] data).  Does NOT support: - Infinite maps (chunk-based TMX format): treat as a warning; use [`ChunkMap`] directly. - Embedded TSX tileset files — the caller must load them separately.  [`ChunkMap`]: super::chunk::ChunkMap

*Source: [src/tilemap/tmx.rs](src/tilemap/tmx.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `enum` | [`TmxLayer`](src/tilemap/tmx.rs#L200) | Variant tag for TMX map layers. Consult the module-level documentation for th... |
| `enum` | [`TmxOrientation`](src/tilemap/tmx.rs#L31) | Rendering orientation of the map, as specified in the TMX `orientation` attri... |
| `enum` | [`TmxStaggerAxis`](src/tilemap/tmx.rs#L59) | The axis along which isometric / hexagonal tiles are staggered.  # Variants -... |
| `fn` | [`load_tmx`](src/tilemap/tmx.rs#L289) | Parses a TMX file given its XML content as a string.  # Parameters - `xml` — ... |
| `struct` | [`TmxMap`](src/tilemap/tmx.rs#L221) | A fully-parsed TMX map. Consult the module-level documentation for the broade... |
| `struct` | [`TmxObject`](src/tilemap/tmx.rs#L175) | A single Tiled object within an object layer.  # Fields - `id` — `u32`. - `na... |
| `struct` | [`TmxObjectLayer`](src/tilemap/tmx.rs#L154) | An object layer (object group) from a TMX map.  # Fields - `name` — `String`.... |
| `struct` | [`TmxTileLayer`](src/tilemap/tmx.rs#L128) | A standard tile layer from a TMX map.  # Fields - `name` — `String`. - `width... |
| `struct` | [`TmxTileset`](src/tilemap/tmx.rs#L87) | A tileset reference embedded in a TMX map.  # Fields - `first_gid` — `u32`. -... |

## `timer` {#timer}

### `timer`

> Mod implementation for the `timer` subsystem.  This module is part of Luna2D's `timer` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/timer/mod.rs](src/timer/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`clock`](src/timer/mod.rs#L10) | Frame-based clock providing delta time, total time, and FPS. |
| `mod` | [`scheduler`](src/timer/mod.rs#L12) | Scheduled event manager for delayed and repeating timed callbacks. |

### `timer::clock`

> Clock implementation for the `timer` subsystem.  This module is part of Luna2D's `timer` subsystem and provides the implementation details for clock-related operations and data management. Key types exported from this module: `Clock`. Primary functions: `new()`, `tick()`, `delta()`, `total()`.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/timer/clock.rs](src/timer/clock.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`Clock`](src/timer/clock.rs#L30) | Tracks per-frame delta time, accumulated total time, and a rolling FPS measur... |

### `timer::scheduler`

> Scheduled event manager for delayed and repeating timed callbacks.  Provides [`Scheduler`] for one-shot delays, repeating timers, named events, individual pause/resume, global time-scale, and on-the-fly interval changes.  ## Usage ```rust,no_run # use luna2d::timer::Scheduler; let mut sched = Scheduler::new(); let id = sched.after(2.0);          // fires once after 2 seconds let rid = sched.every(0.5, -1);     // fires every 0.5 s indefinitely sched.set_time_scale(0.5);          // run at half speed let fired = sched.update(0.1);      // advance; returns IDs that fired sched.cancel(rid); ```

*Source: [src/timer/scheduler.rs](src/timer/scheduler.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `struct` | [`ScheduledEvent`](src/timer/scheduler.rs#L28) | A single scheduled event with optional name and pause state.  # Fields - `id`... |
| `struct` | [`Scheduler`](src/timer/scheduler.rs#L60) | Manages a collection of timed events (one-shot and repeating).  Each event ha... |

## `window` {#window}

### `window`

> Mod implementation for the `window` subsystem.  This module is part of Luna2D's `window` subsystem and provides the implementation details for mod-related operations and data management.  All public items are documented. See the parent module for architectural context and the `luna.*` Lua API for the scripting interface.

*Source: [src/window/mod.rs](src/window/mod.rs)*

| Kind | Name | Description |
|------|------|-------------|
| `mod` | [`event_loop`](src/window/mod.rs#L10) | Placeholder for the platform window event loop integration. |

---
*Generated by `tools/gen_docs_rust.py`. Do not edit by hand.*