# `i18n` — Agent Reference

| Property | Value |
|----------|-------|
| **Tier** | Feature Systems |
| **Status** | Implemented |
| **Lua API** | `lurek.i18n` |
| **Source** | `src/i18n/` |
| **Rust Tests** | none found in the workspace |
| **Lua Tests** | none found in the workspace |
| **Architecture** | `docs/architecture/engine-architecture.md § Feature Systems` |

---

## Summary

The `i18n` module provides the core localization backend for translated strings, placeholder interpolation, and plural selection. It stores catalogs by locale, resolves fallback chains, and applies lightweight text substitution rules so scripts can fetch already-localized strings through a consistent API.

It exists to keep translation lookup and plural rules separate from UI widgets, script-side table parsing, and platform locale detection. The module owns the reusable text rules and catalog semantics; the Lua bridge handles script-facing table loading and namespace exposure.

It intentionally does not own full document-format parsing, date or number formatting, bidirectional text layout, or automatic locale selection from the OS. It is a compact string-catalog backend rather than a full internationalization platform.

**Scope boundary**: This module currently acts as a mostly self-contained part of the Feature Systems layer. Cross-module behavior should remain anchored to the top-level source files and Lua bindings listed below.

---

## Architecture

```
lurek.i18n.* (Lua API — src/lua_api/i18n_api.rs)
    |
    v
src/i18n/mod.rs
    |- catalog.rs - catalog
    |- interpolation.rs - interpolation
    |- plural.rs - plural
```

---

## Source Files

| File | Purpose |
|------|---------|
| `catalog.rs` | Implements `Catalog` storage, locale fallback handling, and typed catalog errors. |
| `interpolation.rs` | Provides placeholder substitution helpers for localized strings. |
| `mod.rs` | Declares the localization submodules and re-exports the public catalog, interpolation, and pluralization APIs. |
| `plural.rs` | Defines plural categories and locale-style plural selection helpers, including Slavic-specific rules. |

---

## Submodules

### `i18n::catalog`

Implements `Catalog` storage, locale fallback handling, and typed catalog errors.

- **`CatalogError`** (enum): Errors produced by catalog operations.
- **`Catalog`** (struct): Multi-locale string catalog with dot-path key resolution.

### `i18n::interpolation`

Provides placeholder substitution helpers for localized strings.

- **No exported Rust types in this file**: this submodule is primarily supporting logic or free functions.

### `i18n::plural`

Defines plural categories and locale-style plural selection helpers, including Slavic-specific rules.

- **`PluralForm`** (enum): CLDR plural category.

---

## Key Types

### Public Types

#### `Catalog`

The main locale-keyed translation store with fallback resolution and flat string lookup.

#### `CatalogError`

Typed error enum for missing locales, missing keys, and related catalog lookup failures.

#### `PluralForm`

The plural-category enum used to select the correct localized string variant.

---

## Lua API

Exposed under `lurek.i18n.*` by `src/lua_api/i18n_api.rs`.

### Module Functions

| Function | Description |
|----------|-------------|
| `lurek.i18n.loadTable` | Loads a language table under the given locale code. |
| `lurek.i18n.unloadTable` | Unloads a locale from the catalog. |
| `lurek.i18n.setLanguage` | Sets the active translation language. |
| `lurek.i18n.getLanguage` | Returns the currently active locale code, or nil if unset. |
| `lurek.i18n.getLanguages` | Returns all loaded locale codes. |
| `lurek.i18n.setFallbacks` | Sets the ordered list of fallback locale codes tried when a key is missing. |
| `lurek.i18n.getFallbacks` | Returns the current fallback locale array. |
| `lurek.i18n.t` | Translates a key against the active locale with optional variable |
| `lurek.i18n.hasKey` | Returns whether a key exists in the active locale. |
| `lurek.i18n.getKeys` | Returns all known keys for the active locale. |
| `lurek.i18n.setKey` | Inserts or overwrites a single key in the given locale. |
| `lurek.i18n.interpolate` | Interpolates {name} placeholders in a template string. |
| `lurek.i18n.pluralFor` | Returns the CLDR plural category for a number ("one" or "other", etc.). |
| `lurek.i18n.onLanguageChange` | Registers a callback invoked when setLanguage() is called. |
| `lurek.i18n.hasLanguage` | Returns whether a locale has been loaded. |
| `lurek.i18n.getAvailableLanguages` | Returns all loaded locale codes (alias for getLanguages). |
| `lurek.i18n.setBase` | Sets the base/fallback language (adds it as first fallback). |
| `lurek.i18n.getBase` | Returns the base/fallback language. |
| `lurek.i18n.onChange` | Registers a callback invoked when setLanguage() is called (alias: onChange). |
| `lurek.i18n.offChange` | Unregisters all onChange callbacks. |
| `lurek.i18n.keyCount` | Returns the number of keys loaded in the active locale. |
| `lurek.i18n.categories` | Returns unique first-path-segment category prefixes for all active locale keys. |
| `lurek.i18n.keysInCategory` | Returns all keys in the active locale whose first path segment matches category. |
| `lurek.i18n.search` | Searches active locale values for a substring query (case-insensitive). Returns {key, value} pairs. |
| `lurek.i18n.buildIndex` | Builds an inverted word index for the active locale. Returns index as {word → {keys}}. |
| `lurek.i18n.searchIndexed` | Searches the provided pre-built index for entries matching all words in query. |
| `lurek.i18n.mergeLocale` | Merges a flat key→value table into an existing locale without replacing the whole table. |

---

## Lua Examples

```lua
-- Minimal namespace check for lurek.i18n.
if lurek.i18n then
    -- Call the documented functions in the Lua API tables above.
end
```

---

## Item Summary

| Kind | Count |
|------|-------|
| `struct` | 1 |
| `enum` | 2 |
| `fn` (Lua API) | 27 |
| **Total** | **30** |

---

## References

| Module | Relationship | Notes |
|--------|--------------|-------|
| — | No top-level `crate::<module>` imports were detected in this module's source files. | Keep the source files as the primary dependency reference. |

---

## Notes

- **Source of truth**: Keep this spec synchronized with `src/i18n/`, the matching AGENT files, and any relevant Lua bindings.
- **Generation note**: This file was generated from current source and AGENT metadata, then intended for manual refinement when behavior changes.
