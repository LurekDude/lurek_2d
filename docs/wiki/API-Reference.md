# API Reference

> One method per line in valid Lua. Full `lurek.*` path for module functions; `obj:method()` for instance methods.
> Inline comment: return type and description.
>
> Auto-generated by `tools/gen_wiki_api.py` from `logs/data/lua_api_data.json`.
> Version: `1.0.0` | Generated: 2026-04-25
> Regenerate: `python tools/gen_wiki_api.py`

---

## Table of Contents

- [Callbacks](#callbacks)
- [lurek.render](#render)
- [lurek.audio](#audio)
- [lurek.timer](#timer)
- [lurek.math](#math)
- [lurek.physics](#physics)
- [lurek.filesystem](#filesystem)
- [lurek.window](#window)
- [lurek.event](#event)
- [lurek.system](#system)
- [lurek.thread](#thread)
- [lurek.particle](#particle)
- [lurek.tilemap](#tilemap)
- [lurek.scene](#scene)
- [lurek.ecs](#ecs)
- [lurek.pathfind](#pathfind)
- [lurek.ai](#ai)
- [lurek.data](#data)
- [lurek.image](#image)
- [lurek.compute](#compute)
- [lurek.dataframe](#dataframe)
- [lurek.graph](#graph)
- [lurek.minimap](#minimap)
- [lurek.save](#save)
- [lurek.log](#log)
- [lurek.mods](#mods)
- [lurek.i18n](#i18n)
- [lurek.input](#input)
- [lurek.animation](#animation)
- [lurek.automation](#automation)
- [lurek.camera](#camera)
- [lurek.debugbridge](#debugbridge)
- [lurek.devtools](#devtools)
- [lurek.docs](#docs)
- [lurek.effect](#effect)
- [lurek.engine](#engine)
- [lurek.globe](#globe)
- [lurek.html](#html)
- [lurek.light](#light)
- [lurek.network](#network)
- [lurek.parallax](#parallax)
- [lurek.patterns](#patterns)
- [lurek.pipeline](#pipeline)
- [lurek.procgen](#procgen)
- [lurek.raycaster](#raycaster)
- [lurek.serial](#serial)
- [lurek.spine](#spine)
- [lurek.sprite](#sprite)
- [lurek.terminal](#terminal)
- [lurek.tween](#tween)
- [lurek.ui](#ui)

---

## Callbacks

Define any of these in `main.lua`. All are optional.

```lua
  function lurek.load()                                    end  -- called once after script loads
  function lurek.update(dt)                                end  -- dt: number; called every frame before draw
  function lurek.draw()                                    end  -- called every frame; push draw commands here
  function lurek.keypressed(key, scancode, isrepeat)       end  -- key: string; isrepeat: bool
  function lurek.keyreleased(key, scancode)                end  -- key: string; scancode: string
  function lurek.textinput(text)                           end  -- text: string; Unicode character input
  function lurek.mousepressed(x, y, button)                end  -- button: 1=left 2=right 3=middle
  function lurek.mousereleased(x, y, button)               end  -- button: number
  function lurek.wheelmoved(x, y)                          end  -- x,y: scroll delta this frame
  function lurek.input.gamepadpressed(id, button)          end  -- id: number; button: string
  function lurek.input.gamepadreleased(id, button)         end  -- id: number; button: string
  function lurek.input.gamepadaxis(id, axis, value)        end  -- value: -1.0 to 1.0
  function lurek.joystickadded(id)                         end  -- gamepad connected
  function lurek.joystickremoved(id)                       end  -- gamepad disconnected
  function lurek.touchpressed(id, x, y, dx, dy, pressure)  end  -- touch start
  function lurek.touchmoved(id, x, y, dx, dy, pressure)    end  -- touch move
  function lurek.touchreleased(id, x, y, dx, dy, pressure) end  -- touch end
  function lurek.focus(focused)                            end  -- focused: bool; window focus changed
  function lurek.visible(visible)                          end  -- visible: bool; window hidden or shown
  function lurek.resize(w, h)                              end  -- w, h: new window dimensions
  function lurek.quit()                                    end  -- return true to cancel shutdown
  function lurek.errorhandler(msg)                         end  -- return replacement string or nil
```

---

## lurek.render {#render}

> Immediate-mode 2D rendering: images, shapes, text, canvas, shaders, blend modes, transforms.

```lua
  lurek.render.applyTransform( mat )  -- Applies an affine transform matrix.
  lurek.render.arc( )  -- Draws a partial circle arc at the given position with specified radius and angle range.
  lurek.render.beginSortGroup( id )  -- Begins a Y/Z depth sort group. Draw commands until flushSortGroup are depth-sortable.
  lurek.render.beginSortGroup( id )  -- Begins a Y/Z depth sort group identified by id.
  lurek.render.captureScreenshot( callback )  -- Calls the given callback with an ImageData captured from the current frame (stub: creat...
  lurek.render.circle( mode, x, y, radius )  -- Draws a filled or outlined circle at the given world-space position.
  lurek.render.clear( [r], [g], [b] )  -- Clears the draw command queue (resets the screen).
  lurek.render.clearStencil( )  -- Resets the stencil mode to the default (keep / always / 0).
  lurek.render.currentLayer( )  -- Returns the name of the currently active named layer.
  lurek.render.draw( ... )  -- Draws a drawable (Image, Canvas, SpriteBatch, Mesh) at the given position.
  lurek.render.drawBevelRect( )  -- Queues a beveled border rectangle with inner fill.
  lurek.render.drawBevelRect( )  -- Queues a beveled border rectangle.
  lurek.render.drawColoredPolygon( vertices, colors, [mode] )  -- Queues a convex polygon with per-vertex colours.
  lurek.render.drawColoredPolygon( vertices, colors, [mode] )  -- Queues a convex polygon with per-vertex colours.
  lurek.render.drawCubicBezier( )  -- Queues a cubic BĂ©zier curve from (x1,y1) to (x2,y2) with two control points.
  lurek.render.drawCubicBezier( )  -- Queues a cubic BĂ©zier curve from (x1,y1) to (x2,y2) with two control points.
  lurek.render.drawGradientRect( )  -- Queues a gradient-filled rectangle. color1/color2 are {r,g,b,a} tables.
  lurek.render.drawGradientRect( )  -- Queues a gradient-filled rectangle. Both colors are RGBA tables {r,g,b,a} or positional...
  lurek.render.drawHexTile( )  -- Queues a hexagonal tile at centre (cx, cy) with given circumradius.
  lurek.render.drawHexTile( )  -- Queues a hexagonal tile at centre (cx, cy) with given circumradius.
  lurek.render.drawIsoCubeTile( sx, sy, half_w, half_h, [opts] )  -- Queues a three-face isometric cube tile at screen position (sx, sy).
  lurek.render.drawIsoCubeTile( sx, sy, half_w, half_h, [opts] )  -- Queues a three-face isometric cube tile at screen position (sx, sy).
  lurek.render.drawNineSlice( slice, x, y, w, h )  -- Queues a 9-slice draw call inside lurek.draw / lurek.draw_ui.
  lurek.render.drawPath( path, [mode], [close] )  -- Queues a multi-segment vector path.
  lurek.render.drawPath( path, [mode], [close] )  -- Queues a multi-segment vector path.
  lurek.render.drawQuadBezier( )  -- Queues a quadratic BĂ©zier curve from (x1,y1) to (x2,y2) with one control point.
  lurek.render.drawQuadBezier( )  -- Must be called inside lurek.draw or lurek.draw_ui.
  lurek.render.drawq( )  -- Draws a portion of an image defined by a Quad.
  lurek.render.ellipse( mode, x, y, rx, ry )  -- Draws a filled or outlined ellipse with independent x/y radii.
  lurek.render.flushSortGroup( id )  -- Sorts and flushes all draw commands in the sort group.
  lurek.render.flushSortGroup( id )  -- Sorts and flushes all draw commands in the sort group.
  lurek.render.getBackgroundColor( )  -> BackgroundColor  -- Returns the current background color.
  lurek.render.getBlendMode( )  -> BlendMode  -- Returns the current blend mode as a string.
  lurek.render.getCanvas( )  -> Canvas  -- Returns the current canvas, or nil if drawing to screen.
  lurek.render.getCanvasSize( ud )  -> CanvasSize  -- Returns the dimensions of a canvas.
  lurek.render.getColor( )  -> Color  -- Returns the current drawing color.
  lurek.render.getColorMask( )  -> ColorMask  -- Returns the current color mask.
  lurek.render.getDefaultFilter( )  -> DefaultFilter  -- Returns the default texture filter mode.
  lurek.render.getDefaultFont( [pixel_height] )  -> DefaultFont  -- Returns a built-in font by pixel height (snaps to nearest available size).
  lurek.render.getDepthMode( )  -> DepthMode  -- Returns the current depth mode as (mode, write).
  lurek.render.getDimensions( )  -> Dimensions  -- Returns window width and height.
  lurek.render.getFont( )  -> Font  -- Returns the currently active font, or nil.
  lurek.render.getFontAscent( ud )  -> FontAscent  -- Returns the ascent of the given font.
  lurek.render.getFontCellWidth( ud )  -> FontCellWidth  -- Returns the cell width of the given font (for monospaced bitmap fonts).
  lurek.render.getFontDescent( ud )  -> FontDescent  -- Returns the descent of the given font.
  lurek.render.getFontHeight( ud )  -> FontHeight  -- Returns the line height of the given font.
  lurek.render.getFontLineHeight( ud )  -> FontLineHeight  -- Returns the line height of the given font (alias for getFontHeight).
  lurek.render.getFontSizes( )  -> FontSizes  -- Returns a table of available built-in font pixel heights.
  lurek.render.getFontWidth( ud, text )  -> FontWidth  -- Returns the pixel width of text in the given font.
  lurek.render.getFontWrap( text, limit )  -> FontWrap  -- Returns wrapped lines and the maximum line width.
  lurek.render.getHeight( )  -> Height  -- Returns the window height in pixels.
  lurek.render.getLayerZOrder( name )  -> LayerZOrder  -- Returns the z-order of the named layer, or `0` if unregistered.
  lurek.render.getLineWidth( )  -> LineWidth  -- Returns the current line width.
  lurek.render.getPointSize( )  -> PointSize  -- Returns the current point size.
  lurek.render.getScissor( )  -> Scissor  -- Returns the active scissor rectangle, or nothing.
  lurek.render.getShader( )  -> Shader  -- Returns the active shader, or nil.
  lurek.render.getStats( )  -> Stats  -- Returns a table of renderer statistics.
  lurek.render.getStencilMode( )  -> StencilMode  -- Returns the current stencil mode as (action, compare, value).
  lurek.render.getWidth( )  -> Width  -- Returns the window width in pixels.
  lurek.render.intersectScissor( x, y, w, h )  -- Intersects the current scissor with a new rectangle.
  lurek.render.isLayerVisible( name )  -- Returns `true` if the named layer is visible (default: `true`).
  lurek.render.isWireframe( )  -- Returns whether wireframe mode is active.
  lurek.render.line( ... )  -- Draws a line between two points.
  lurek.render.newCanvas( width, height )  -> Canvas  -- Creates an off-screen render canvas.
  lurek.render.newDrawLayer( )  -> DrawLayer  -- Creates a new z-ordered draw-call queue.
  lurek.render.newFont( ... )  -> Font  -- Loads a bitmap font PNG from a file, or selects a built-in size by pixel height.
  lurek.render.newImage( arg )  -> Image  -- Loads an image from a file path or creates one from ImageData.
  lurek.render.newLayer( name, [z_order] )  -> Layer  -- Registers a named render layer with an optional z-order (default 0).
  lurek.render.newMesh( verts, [mode] )  -> Mesh  -- Creates a custom mesh from vertex data.
  lurek.render.newNineSlice( image, top, right, bottom, left )  -> NineSlice  -- Creates a 9-slice descriptor from a texture and inset values.
  lurek.render.newQuad( x, y, w, h, sw, sh )  -> Quad  -- Creates a new Quad viewport into a texture.
  lurek.render.newShader( code )  -> Shader  -- Compiles a custom WGSL shader and returns its handle.
  lurek.render.newShape( )  -> Shape  -- Creates a new empty [`CompoundShape`] stored in the resource pool.
  lurek.render.newSpriteBatch( ud, [max] )  -> SpriteBatch  -- Creates a new sprite batch for the given image.
  lurek.render.origin( )  -- Resets the transform to the identity.
  lurek.render.points( ... )  -- Draws a batch of individual points at the specified world-space coordinates.
  lurek.render.polygon( ... )  -- Draws a polygon from a list of vertices.
  lurek.render.pop( )  -- Pops the transform from the stack.
  lurek.render.popLayer( id )  -- Ends and composites the named layer back to its parent.
  lurek.render.popLayer( id )  -- Ends and composites the named layer.
  lurek.render.print( text, [x], [y], [scale] )  -- Draws text at the given position.
  lurek.render.printRich( spans_table, x, y )  -- Draws a sequence of individually-styled text spans at `(x, y)`.
  lurek.render.printf( text, x, y, limit, [align] )  -- Draws word-wrapped text within a given width.
  lurek.render.push( )  -- Pushes the current transform onto the stack.
  lurek.render.pushLayer( id, [alpha], [blend_mode] )  -- Begins a named compositing layer with optional alpha and blend mode.
  lurek.render.pushLayer( id, [alpha], [blend_mode] )  -- Begins a named compositing layer. Provides alpha and blend mode for composite.
  lurek.render.pushSortKey( depth )  -- Associates the previous draw command with a depth value within the active sort group.
  lurek.render.pushSortKey( depth )  -- Associates the previous draw command with a depth value within the active sort group.
  lurek.render.rectangle( )  -- Draws a filled or outlined axis-aligned rectangle at the given position.
  lurek.render.rotate( angle )  -- Rotates the coordinate system.
  lurek.render.saveScreenshot( path )  -- Queues a screenshot to be saved after the current frame.
  lurek.render.scale( sx, [sy] )  -- Scales the coordinate system.
  lurek.render.setBackgroundColor( r, g, b )  -- Sets the background clear color.
  lurek.render.setBlendMode( mode )  -- Sets the blend mode for drawing.
  lurek.render.setCanvas( [ud] )  -- Sets the active render target to a Canvas, or back to the screen.
  lurek.render.setColor( r, g, b, [a] )  -- Sets the current drawing color.
  lurek.render.setColorMask( ... )  -- Sets which RGBA channels are written. Reset with no args.
  lurek.render.setDefaultFilter( min, mag, [anisotropy] )  -- Sets the default texture filter mode.
  lurek.render.setDepthMode( mode, [write] )  -- Sets the depth test comparison and write enable.
  lurek.render.setFont( ud )  -- Sets the active font for print calls.
  lurek.render.setFontLineHeight( font, lh )  -- Sets the line height of the given font (stub â€” returns nil; fonts are immutable in he...
  lurek.render.setLayer( name )  -- Sets the active named layer. Draw calls made after this will be
  lurek.render.setLayerVisible( name, visible )  -- Shows or hides the named layer. Hidden layers are excluded from
  lurek.render.setLayerZOrder( name, z )  -- Updates the z-order of the named layer. Auto-creates the layer if
  lurek.render.setLineWidth( w )  -- Sets the line width for outline drawing.
  lurek.render.setPointSize( size )  -- Sets the point diameter in pixels.
  lurek.render.setScissor( ... )  -- Restricts drawing to a rectangle, or clears scissor if no args.
  lurek.render.setShader( [ud] )  -- Sets the active shader, or clears it.
  lurek.render.setStencilMode( action, [compare], [value] )  -- Sets the stencil buffer write/test mode.
  lurek.render.setStencilTest( [compare], [value] )  -- Sets the stencil comparison test, or disables stencil testing.
  lurek.render.setWireframe( enabled )  -- Enables or disables wireframe rendering.
  lurek.render.shear( kx, ky )  -- Shears the coordinate system.
  lurek.render.stencil( [action], [value] )  -- Begins stencil writing with the given action and value.
  lurek.render.translate( x, y )  -- Translates the coordinate system.
  lurek.render.triangle( mode, x1, y1, x2, y2, x3, y3 )  -- Draws a filled or outlined triangle connecting three world-space vertices.
```

**`LCanvas`** methods:

> Lua-side handle to an off-screen render target stored in SharedState.

```lua
  LCanvas:getDimensions( )  -> Dimensions  -- Returns width and height of this canvas.
  LCanvas:getHeight( )  -> Height  -- Returns the height of this canvas in pixels.
  LCanvas:getWidth( )  -> Width  -- Returns the width of this canvas in pixels.
  LCanvas:release( )  -- Releases GPU framebuffer memory for this canvas.
  LCanvas:type( )  -- Returns the type name of this object.
  LCanvas:typeOf( )  -- Returns the type name of this object.
```

**`LDrawLayer`** methods:

> Lua-side z-ordered draw queue. Callbacks are sorted by z and called on `flush()`.

```lua
  LDrawLayer:clear( )  -- Removes all queued callbacks without calling them.
  LDrawLayer:flush( )  -- Sorts and calls all queued callbacks, then empties the queue.
  LDrawLayer:getCount( )  -> Count  -- Returns the number of queued callbacks.
  LDrawLayer:queue( z, f )  -- Queues a draw callback at the given z-order.
  LDrawLayer:type( )  -- Returns the string type identifier of this draw layer (e.g. `'sprite'`).
  LDrawLayer:typeOf( name )  -- Returns true if this object is an instance of the given type name.
```

**`LFont`** methods:

> Lua-side handle to a loaded font stored in SharedState.

```lua
  LFont:getAscent( )  -> Ascent  -- Returns the ascent of this font in pixels.
  LFont:getDescent( )  -> Descent  -- Returns the descent of this font in pixels.
  LFont:getHeight( )  -> Height  -- Returns the line height of this font.
  LFont:getLineHeight( )  -> LineHeight  -- Returns the line height multiplier of this font.
  LFont:getWidth( text )  -> Width  -- Returns the rendered width of the given text string.
  LFont:getWrap( text, limit )  -> Wrap  -- Wraps text to the given width and returns the lines.
  LFont:release( )  -- Releases this font and frees its atlas memory.
  LFont:setLineHeight( height )  -- Sets the line height multiplier for this font.
  LFont:type( )  -- Returns the type name of this object.
  LFont:typeOf( )  -- Returns the type name of this object.
```

**`LImage`** methods:

> Lua-side handle to a loaded GPU texture stored in the engine's texture pool.

```lua
  LImage:getDimensions( )  -> Dimensions  -- Returns width and height of this image.
  LImage:getHeight( )  -> Height  -- Returns the height of this image in pixels.
  LImage:getWidth( )  -> Width  -- Returns the width of this image in pixels.
  LImage:release( )  -- Releases the GPU texture memory for this image.
  LImage:type( )  -- Returns the type name of this object.
  LImage:typeOf( )  -- Returns the type name of this object.
```

**`LImageData`** methods:

> Lua-side handle to a loaded texture stored in SharedState.

```lua
  LImageData:blit( src_ud, dst_x, dst_y )  -- Blits the source ImageData onto this image at (dst_x, dst_y) using Porter-Duff `over`.
  LImageData:diff( other_ud )  -- Returns the sum of absolute per-channel differences between this image and `other`.
  LImageData:getHeight( )  -> Height  -- Returns the pixel height of this image buffer.
  LImageData:getRegion( x, y, w, h )  -> Region  -- Returns a copy of the rectangular sub-region as a new ImageData.
  LImageData:getWidth( )  -> Width  -- Returns the pixel width of this image buffer.
  LImageData:mapPixels( callback )  -- Applies a Lua function to every pixel in-place.
  LImageData:resize( w, h )  -> ImageData  -- Returns a new ImageData scaled to the given dimensions using bilinear interpolation.
  LImageData:type( )  -- Returns the type name "ImageData".
  LImageData:typeOf( name )  -- Returns true when the given name matches "ImageData" or a parent type.
```

**`LMesh`** methods:

> Lua-side handle to a mesh stored in SharedState.

```lua
  LMesh:getVertex( index )  -> Vertex  -- Returns vertex data at the given 1-based index.
  LMesh:getVertexCount( )  -> VertexCount  -- Returns the number of vertices in this mesh.
  LMesh:release( )  -- Releases the GPU mesh resource, freeing VRAM immediately.
  LMesh:setTexture( [ud] )  -- Assigns a texture to this mesh.
  LMesh:setVertex( index, data )  -- Sets vertex data at the given 1-based index.
  LMesh:type( )  -- Returns the type name of this object.
  LMesh:typeOf( )  -- Returns the type name of this object.
```

**`LNineSlice`** methods:

> Lua-side 9-slice descriptor.

```lua
  LNineSlice:draw( x, y, w, h )  -- Compatibility stub: queuing handled by lurek.graphic.drawNineSlice.
  LNineSlice:getInsets( )  -> Insets  -- Returns the four inset values as (top, right, bottom, left).
  LNineSlice:getTextureSize( )  -> TextureSize  -- Returns the width and height of the source texture.
  LNineSlice:type( )  -- Returns the type name "NineSlice".
  LNineSlice:typeOf( name )  -- Returns true when the given name matches "NineSlice" or a parent type.
```

**`LQuad`** methods:

> Lua-side quad viewport into a texture.

```lua
  LQuad:getTextureDimensions( )  -> TextureDimensions  -- Returns the reference texture dimensions.
  LQuad:getViewport( )  -> Viewport  -- Returns the quad viewport rectangle.
  LQuad:setViewport( x, y, w, h )  -- Sets the quad viewport rectangle.
  LQuad:type( )  -- Returns the type name of this object.
  LQuad:typeOf( )  -- Returns the type name of this object.
```

**`LShader`** methods:

> Lua-side handle to a compiled shader stored in SharedState.

```lua
  LShader:hasUniform( name )  -- Returns whether this shader has a uniform with the given name.
  LShader:release( )  -- Releases the compiled GPU shader, freeing VRAM and shader slots.
  LShader:send( name, value )  -- Sends a uniform value to this shader.
  LShader:type( )  -- Returns the type name of this object.
  LShader:typeOf( )  -- Returns the type name of this object.
```

**`LShape`** methods:

> Lua-side handle to a [`CompoundShape`] stored in [`SharedState::shapes`].

```lua
  LShape:arc( )  -- Queues a filled or outlined arc draw command onto this shape.
  LShape:circle( mode, x, y, r )  -- Queues a filled or outlined circle draw command onto this shape.
  LShape:clear( )  -- Removes all commands and resets the shape to empty.
  LShape:draw( )  -- Queues a draw command for this shape at the given position.
  LShape:ellipse( mode, x, y, rx, ry )  -- Queues an ellipse command.
  LShape:getCommandCount( )  -> CommandCount  -- Returns the number of drawing commands currently stored.
  LShape:line( x1, y1, x2, y2 )  -- Queues a line segment command.
  LShape:polygon( mode, coords )  -- Queues a polygon command from variadic (x, y) coordinate pairs.
  LShape:polyline( )  -- Queues a polyline command from variadic (x, y) coordinate pairs.
  LShape:rectangle( mode, x, y, w, h )  -- Queues a rectangle command.
  LShape:roundedRectangle( mode, x, y, w, h, rx, [ry] )  -- Queues a rounded rectangle command.
  LShape:setColor( r, g, b, [a] )  -- Sets the drawing color for subsequent primitives.
  LShape:setLineWidth( w )  -- Sets the stroke width for subsequent outlined primitives.
  LShape:triangle( mode, x1, y1, x2, y2, x3, y3 )  -- Queues a triangle command.
  LShape:type( )  -- Returns the type name of this object.
  LShape:typeOf( name )  -- Returns true if the given type name matches this object's type or any parent type.
```

**`LSpriteBatch`** methods:

> Lua-side handle to a sprite batch stored in SharedState.

```lua
  LSpriteBatch:add( )  -- Adds a sprite entry to this batch.
  LSpriteBatch:clear( )  -- Removes all sprites from this batch.
  LSpriteBatch:getBufferSize( )  -> BufferSize  -- Returns the maximum capacity of this batch.
  LSpriteBatch:getCount( )  -> Count  -- Returns the number of sprites in this batch.
  LSpriteBatch:release( )  -- Releases this sprite batch.
  LSpriteBatch:type( )  -- Returns the type name of this object.
  LSpriteBatch:typeOf( )  -- Returns the type name of this object.
```

## lurek.audio {#audio}

> Sound playback: load audio files, play/pause/stop, volume, pitch, looping, panning.

```lua
  lurek.audio.add_effect( bus_name, effect_type_str, [params] )  -- Adds a DSP effect to a bus.
  lurek.audio.applyBandpass( sd_ud, low_hz, high_hz )  -- Applies a bandpass filter (high-pass then low-pass) to a SoundData in-place.
  lurek.audio.applyGain( sd_ud, gain )  -- Scales every sample by gain (clamped to [-1, 1]).
  lurek.audio.applyHighpass( sd_ud, cutoff_hz )  -- Applies a first-order IIR high-pass filter to a SoundData in-place.
  lurek.audio.applyLowpass( sd_ud, cutoff_hz )  -- Applies a first-order IIR low-pass filter to a SoundData in-place.
  lurek.audio.clearFilter( id_val )  -- Removes any active filter from a source.
  lurek.audio.clearMidiSoundFont( )  -- Unloads the active SoundFont.
  lurek.audio.clearRandomPitch( src_ud )  -- Clears any random pitch range on a source, restoring fixed pitch.
  lurek.audio.clone( id_val )  -- Creates an independent copy of a source.
  lurek.audio.create_bus( name, [parent_name] )  -- Creates a bus by name (functional style).
  lurek.audio.crossfade( from_ud, to_ud, duration )  -- Crossfades from one source to another over a duration.
  lurek.audio.fadeIn( id_val, dur )  -- Fades a source in from silence over the given duration.
  lurek.audio.getActiveSourceCount( )  -> ActiveSourceCount  -- Returns the number of currently playing sources.
  lurek.audio.getBusPeak( bus_name )  -> BusPeak  -- Returns the peak signal level of the named bus (stub: always 0.0).
  lurek.audio.getBusRms( bus_name )  -> RMS  -- Returns the RMS signal level of the named bus (stub: always 0.0).
  lurek.audio.getDistanceModel( )  -> DistanceModel  -- Returns the current distance model name.
  lurek.audio.getDopplerScale( )  -> DopplerScale  -- Returns the current Doppler scale.
  lurek.audio.getDuration( id_val )  -> Duration  -- Returns the total duration of a source in seconds.
  lurek.audio.getFadeIn( id_val )  -> FadeIn  -- Returns the fade-in duration of a source.
  lurek.audio.getFreeBufferCount( qsource_id )  -> FreeBufferCount  -- Returns the free buffer slots in a queueable source.
  lurek.audio.getHighpass( id_val )  -> Highpass  -- Returns the high-pass filter cutoff of a source.
  lurek.audio.getListener( )  -> Listener  -- Returns the 3D listener position (x, y, z).
  lurek.audio.getListener2D( )  -> Listener2D  -- Returns the 2D listener position (x, y).
  lurek.audio.getLowpass( id_val )  -> Lowpass  -- Returns the low-pass filter cutoff of a source.
  lurek.audio.getMasterVolume( )  -> MasterVolume  -- Returns the global master volume.
  lurek.audio.getMaxSources( )  -> MaxSources  -- Returns the maximum number of simultaneous sources.
  lurek.audio.getMeter( )  -> Meter  -- Returns the stored master peak meter level.
  lurek.audio.getOrientation( id_val )  -> Orientation  -- Returns the 6-component orientation of a source.
  lurek.audio.getPan( id_val )  -> Pan  -- Returns the source stereo panning.
  lurek.audio.getPitch( id_val )  -> Pitch  -- Returns the source pitch multiplier.
  lurek.audio.getPlaybackDevice( )  -> PlaybackDevice  -- Returns the current audio output device name.
  lurek.audio.getPlaybackDevices( )  -> PlaybackDevices  -- Returns a table of available audio output device names.
  lurek.audio.getPosition( id_val )  -> Position  -- Returns the 3D position of a source (x, y, z).
  lurek.audio.getSourceBus( id_val )  -> SourceBus  -- Returns the bus a source is assigned to, or nil.
  lurek.audio.getSourceCount( )  -> SourceCount  -- Returns the total number of registered sources.
  lurek.audio.getSourceType( id_val )  -> SourceType  -- Returns the type string ("static" or "stream") of a source.
  lurek.audio.getStereoWidth( src_ud )  -> StereoWidth  -- Returns the current stereo width for a source.
  lurek.audio.getVelocity( id_val )  -> Velocity  -- Returns the velocity of a source (x, y, z).
  lurek.audio.getVolume( id_val )  -> Volume  -- Returns the source volume.
  lurek.audio.hasMidiSoundFont( )  -- Returns true if a SoundFont is loaded.
  lurek.audio.isLooping( id_val )  -- Returns true if looping is enabled.
  lurek.audio.isPaused( id_val )  -- Returns true if the source is paused.
  lurek.audio.isPlaying( id_val )  -- Returns true if the source is playing.
  lurek.audio.isStopped( id_val )  -- Returns true if the source is stopped.
  lurek.audio.mixInto( dest_ud, src_ud )  -- Additively mixes another SoundData into the destination in-place.
  lurek.audio.newBus( name )  -> Bus  -- Creates a named audio bus for grouping sources.
  lurek.audio.newDecoder( source, [buffersize] )  -> Decoder  -- Creates a streaming audio decoder.
  lurek.audio.newMidiPlayer( [path] )  -> MIDI  -- Creates a MIDI player, optionally loading a file.
  lurek.audio.newPool( file_path, voice_count )  -> Pool  -- Creates a polyphonic sound pool for the given file with N simultaneous voices.
  lurek.audio.newQueueableSource( )  -> QueueableSource  -- Creates a queueable source for manual PCM buffering.
  lurek.audio.newSawtoothWave( freq, duration, sample_rate, amplitude )  -> SawtoothWave  -- Generate a mono sawtooth-wave SoundData buffer.
  lurek.audio.newSineWave( freq, duration, sample_rate, amplitude )  -> SineWave  -- Generate a mono sine-wave SoundData buffer.
  lurek.audio.newSoundData( ... )  -> SoundData  -- Creates a SoundData from a file or as a silent buffer.
  lurek.audio.newSource( ... )  -> Source  -- Loads an audio file and returns a Source handle.
  lurek.audio.newSquareWave( freq, duration, sample_rate, amplitude )  -> SquareWave  -- Generate a mono square-wave SoundData buffer.
  lurek.audio.newTriangleWave( freq, duration, sample_rate, amplitude )  -> TriangleWave  -- Generate a mono triangle-wave SoundData buffer.
  lurek.audio.newWhiteNoise( duration, sample_rate, amplitude, seed )  -> WhiteNoise  -- Generate a reproducible white-noise SoundData buffer.
  lurek.audio.normalizeFile( input, output, target )  -- Normalizes a WAV file peak amplitude to target_level and writes output.
  lurek.audio.pause( id_val )  -- Pauses playback at the current position.
  lurek.audio.pauseAll( )  -- Pauses all currently playing sources.
  lurek.audio.play( id_val, [options] )  -- Plays a source, with optional bus routing via options table.
  lurek.audio.playLooping( id_val )  -- Plays the source in a continuous loop.
  lurek.audio.playQueueable( qsource_id )  -- Starts playback of a queueable source.
  lurek.audio.processOffline( input, output, effects_tbl )  -- Applies a DSP effect chain to a WAV file and writes output.
  lurek.audio.queueSource( qsource_id, sd )  -- Pushes a SoundData buffer into a queueable source.
  lurek.audio.release( id_val )  -- Releases a source and frees its memory.
  lurek.audio.remove_effect( bus_name, effect_id )  -- Removes a DSP effect from a bus.
  lurek.audio.resume( id_val )  -- Resumes playback from pause.
  lurek.audio.resumeAll( )  -- Resumes all paused sources.
  lurek.audio.saveWAV( sd_ud, filename )  -- Saves a SoundData as a 16-bit PCM WAV file at the given path.
  lurek.audio.seek( id_val, pos )  -- Seeks to a time position in seconds.
  lurek.audio.setDistanceModel( model )  -- Sets the distance attenuation model.
  lurek.audio.setDopplerScale( scale )  -- Sets the global Doppler effect scale.
  lurek.audio.setHighpass( id_val, cutoff_hz )  -- Applies a high-pass filter to a source.
  lurek.audio.setListener( x, y, [z] )  -- Sets the 3D listener position.
  lurek.audio.setListener2D( x, y )  -- Sets the 2D listener position for spatial audio.
  lurek.audio.setLooping( id_val, looping )  -- Enables or disables looping.
  lurek.audio.setLowpass( id_val, cutoff_hz )  -- Applies a low-pass filter to a source.
  lurek.audio.setMasterVolume( vol )  -- Sets the global master volume.
  lurek.audio.setMeter( level )  -- Sets the master peak meter level (0.0â€“1.0).
  lurek.audio.setMidiSoundFont( path )  -- Sets the global SoundFont for MIDI synthesis.
  lurek.audio.setOrientation( id_val, fx, fy, fz, ux, uy, uz )  -- Sets the 6-component orientation of a source.
  lurek.audio.setPan( id_val, pan )  -- Sets stereo panning (-1.0 left to 1.0 right).
  lurek.audio.setPitch( id_val, pitch )  -- Sets source pitch multiplier.
  lurek.audio.setPlaybackDevice( name )  -- Selects an audio output device by name.
  lurek.audio.setPosition( id_val, x, y, [z] )  -- Sets the 3D position of a source.
  lurek.audio.setRandomPitch( src_ud, min, max )  -- Sets a random pitch range applied each time the source is played.
  lurek.audio.setSourceBus( id_val, bus_val )  -- Assigns a source to a bus.
  lurek.audio.setStereoWidth( src_ud, width )  -- Sets the stereo width multiplier for a source (1.0 = normal, 0.0 = mono).
  lurek.audio.setVelocity( id_val, x, y, [z] )  -- Sets the velocity of a source for Doppler.
  lurek.audio.setVolume( id_val, vol )  -- Sets source playback volume.
  lurek.audio.set_bus_volume( name, volume )  -- Sets a bus volume by name.
  lurek.audio.set_effect_param( bus_name, effect_id, param_name, value )  -- Sets a parameter on a DSP effect.
  lurek.audio.spectrogramToPng( input, output, width, height )  -- Renders a time-frequency spectrogram of a WAV file to a PNG image.
  lurek.audio.stop( id_val )  -- Stops playback and resets seek position.
  lurek.audio.stopAll( )  -- Stops all currently playing sources.
  lurek.audio.stopQueueable( qsource_id )  -- Stops a queueable source and drains its buffers.
  lurek.audio.tell( id_val )  -- Returns the current playback position in seconds.
  lurek.audio.waveformToPng( input, output, width, height )  -- Renders the waveform of a WAV file to a PNG image.
```

**`LBus`** methods:

> Lua-side wrapper for an audio bus resource.

```lua
  LBus:clearDuck( )  -- Removes the ducking target from this bus, restoring the target bus
  LBus:getName( )  -> Name  -- Returns the unique name string assigned to this audio bus.
  LBus:getPeak( )  -> Peak  -- Returns the average peak amplitude of all sources currently on this bus.
  LBus:getPitch( )  -> Pitch  -- Returns the bus pitch multiplier.
  LBus:getVolume( )  -> Volume  -- Returns the current volume multiplier applied to all sources on this bus.
  LBus:isPaused( )  -- Returns true if this bus is paused.
  LBus:pause( )  -- Pauses all sources on this bus.
  LBus:resume( )  -- Resumes all sources on this bus.
  LBus:setDuckTarget( target_name, duck_vol )  -- Configures this bus to duck (lower the volume of) another bus when
  LBus:setPitch( pitch )  -- Sets the pitch multiplier for all sources on this bus.
  LBus:setVolume( vol )  -- Sets the volume for all sources on this bus.
  LBus:type( )  -- Returns the type name of this object.
  LBus:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LDecoder`** methods:

> Lua-side wrapper for a streaming audio decoder.

```lua
  LDecoder:decode( )  -- Decodes the next chunk of samples, or nil at EOF.
  LDecoder:getBitDepth( )  -> BitDepth  -- Returns the per-sample bit depth of this decoded audio stream.
  LDecoder:getChannelCount( )  -> ChannelCount  -- Returns the number of audio channels.
  LDecoder:getDuration( )  -> Duration  -- Returns the total duration in seconds.
  LDecoder:getSampleRate( )  -> SampleRate  -- Returns the sample rate in Hz.
  LDecoder:isSeekable( )  -- Returns true if seeking is supported.
  LDecoder:release( )  -- Releases the decoder (no-op).
  LDecoder:rewind( )  -- Rewinds to the beginning.
  LDecoder:seek( offset )  -- Seeks to a time offset in seconds.
  LDecoder:tell( )  -- Returns the current position in seconds.
  LDecoder:type( )  -- Returns the type name of this object.
  LDecoder:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LMidiPlayer`** methods:

> Lua-side wrapper for the MIDI player.

```lua
  LMidiPlayer:getBus( )  -> Bus  -- Returns the assigned bus, or nil.
  LMidiPlayer:getChannelCount( )  -> ChannelCount  -- Returns the number of MIDI channels.
  LMidiPlayer:getChannelInstrument( ch )  -> GM  -- Returns the GM instrument for a MIDI channel (1-indexed).
  LMidiPlayer:getChannelVolume( ch )  -> ChannelVolume  -- Returns the volume for a MIDI channel (1-indexed).
  LMidiPlayer:getChannels( )  -> PCM  -- Returns the PCM output channel count (1 = mono, 2 = stereo).
  LMidiPlayer:getDuration( )  -> Duration  -- Returns the total MIDI duration in seconds.
  LMidiPlayer:getFilePath( )  -> FilePath  -- Returns the file path of the loaded MIDI, or nil.
  LMidiPlayer:getNoteCount( )  -> NoteCount  -- Returns the total note count in the MIDI sequence.
  LMidiPlayer:getOriginalTempo( )  -> OriginalTempo  -- Returns the original MIDI file tempo in BPM.
  LMidiPlayer:getSampleRate( )  -> PCM  -- Returns the PCM output sample rate in Hz.
  LMidiPlayer:getSoundFontPath( )  -> SoundFont  -- Returns the SoundFont file path, or nil (stub).
  LMidiPlayer:getTempo( )  -> Tempo  -- Returns the current tempo in BPM.
  LMidiPlayer:getTempoScale( )  -> TempoScale  -- Returns the current tempo scale factor.
  LMidiPlayer:getTicksPerBeat( )  -> PPQ  -- Returns the PPQ resolution from the MIDI header.
  LMidiPlayer:getTrackCount( )  -> TrackCount  -- Returns the number of tracks in the MIDI sequence.
  LMidiPlayer:getTrackName( idx )  -> TrackName  -- Returns the name of a MIDI track (1-indexed), or nil.
  LMidiPlayer:getVolume( )  -> Volume  -- Returns the current MIDI volume.
  LMidiPlayer:isChannelMuted( ch )  -- Returns true if a MIDI channel is muted (1-indexed).
  LMidiPlayer:isLoaded( )  -- Returns true if a MIDI sequence is loaded.
  LMidiPlayer:isLooping( )  -- Returns true if looping is enabled.
  LMidiPlayer:isPaused( )  -- Returns true if MIDI playback is paused.
  LMidiPlayer:isPlaying( )  -- Returns true if MIDI is currently playing.
  LMidiPlayer:isTrackMuted( idx )  -- Returns true if a track is muted (1-indexed).
  LMidiPlayer:load( path )  -> MIDI  -- Loads a MIDI file from the given path.
  LMidiPlayer:loadData( )  -> MIDI  -- Loads MIDI data from a Lua string.
  LMidiPlayer:pause( )  -- Pauses the MIDI sequence at the current position; resume with `play()`.
  LMidiPlayer:play( )  -- Starts or resumes MIDI sequence playback from the current position.
  LMidiPlayer:seek( secs )  -- Seeks to a time position in seconds.
  LMidiPlayer:setBus( bus_val )  -- Routes MIDI output through a bus (or nil to clear).
  LMidiPlayer:setChannelInstrument( ch, inst )  -- Sets the GM instrument for a MIDI channel (1-indexed).
  LMidiPlayer:setChannelMuted( ch, muted )  -- Mutes or unmutes a MIDI channel (1-indexed).
  LMidiPlayer:setChannelVolume( ch, vol )  -- Sets volume for a MIDI channel (1-indexed).
  LMidiPlayer:setChannels( channels )  -- Sets the PCM output channel count (clamped 1â€“2).
  LMidiPlayer:setLooping( looping )  -- Enables or disables looping.
  LMidiPlayer:setOnEnd( cb )  -- Registers a playback-end callback (stub).
  LMidiPlayer:setOnNoteOff( cb )  -- Registers a note-off callback (stub).
  LMidiPlayer:setOnNoteOn( cb )  -- Registers a note-on callback (stub).
  LMidiPlayer:setSampleRate( rate )  -- Sets the PCM output sample rate in Hz (clamped 8000â€“192000).
  LMidiPlayer:setSoundFont( path )  -> SoundFont  -- Loads a SoundFont file into this player (stub).
  LMidiPlayer:setTempo( bpm )  -- Sets playback tempo in BPM.
  LMidiPlayer:setTempoScale( scale )  -- Sets the tempo scale factor (1.0 = original speed).
  LMidiPlayer:setTrackMuted( idx, muted )  -- Mutes or unmutes a track (1-indexed).
  LMidiPlayer:setVolume( vol )  -- Sets MIDI playback volume.
  LMidiPlayer:soloChannel( ch )  -- Solos a MIDI channel (1-indexed).
  LMidiPlayer:stop( )  -- Stops MIDI playback and resets the playhead to the beginning.
  LMidiPlayer:tell( )  -- Returns the current playback position in seconds.
  LMidiPlayer:type( )  -- Returns the type name of this object.
  LMidiPlayer:typeOf( name )  -- Returns true if this object is of the given type.
  LMidiPlayer:unsoloAll( )  -- Clears solo on all channels.
  LMidiPlayer:useDefaultSoundFont( )  -- Reverts to the built-in default SoundFont (stub).
```

**`LSoundData`** methods:

> Decoded PCM audio buffer that can be created from a file or synthesised sample-by-sample.

```lua
  LSoundData:drawWaveform( )  -- Draws the waveform onto an ImageData buffer.
  LSoundData:getBitDepth( )  -> BitDepth  -- Returns the bit depth of this audio buffer (typically 16 or 32 bits per sample).
  LSoundData:getChannelCount( )  -> ChannelCount  -- Get the number of channels.
  LSoundData:getDuration( )  -> Duration  -- Get the audio duration in seconds.
  LSoundData:getSample( index )  -> Sample  -- Get a specific sample by index.
  LSoundData:getSampleCount( )  -> SampleCount  -- Get the total number of samples.
  LSoundData:getSampleRate( )  -> SampleRate  -- Returns the sample rate of this audio buffer in Hz (e.g. 44100 or 48000).
  LSoundData:setSample( index, value )  -- Set a specific sample by index.
```

**`LSoundPool`** methods:

> Lua-side wrapper for a polyphonic [`crate::audio::SoundPool`].

```lua
  LSoundPool:getVoiceCount( )  -> VoiceCount  -- Returns the total number of voices in this pool.
  LSoundPool:play( )  -- Plays the next available voice and returns its SoundKey as an integer.
  LSoundPool:release( )  -- Releases all voices from the mixer and invalidates this pool.
  LSoundPool:setBus( name )  -- Routes all voices through the named bus.
  LSoundPool:setVolume( vol )  -- Sets the volume for all voices in this pool.
  LSoundPool:stopAll( )  -- Stops all voices in this pool.
  LSoundPool:type( )  -- Returns the type name of this object.
  LSoundPool:typeOf( name )  -- Returns true if the type name matches.
```

**`LSource`** methods:

> Lua-side wrapper for an audio source resource.

```lua
  LSource:clearFilter( )  -- Removes any active filter from this source.
  LSource:clone( )  -- Creates an independent copy of this source.
  LSource:fadeIn( dur )  -- Fades in from silence over the given duration in seconds.
  LSource:getDuration( )  -> Duration  -- Returns the total duration in seconds.
  LSource:getFadeIn( )  -> FadeIn  -- Returns the current fade-in duration in seconds.
  LSource:getHighpass( )  -> Highpass  -- Returns the high-pass filter cutoff frequency.
  LSource:getLowpass( )  -> Lowpass  -- Returns the low-pass filter cutoff frequency.
  LSource:getPan( )  -> Pan  -- Returns the current stereo panning value.
  LSource:getPitch( )  -> Pitch  -- Returns the current pitch multiplier.
  LSource:getType( )  -> Type  -- Returns the source type ("static" or "stream").
  LSource:getVolume( )  -> Volume  -- Returns the current volume multiplier.
  LSource:isLooping( )  -- Returns true if looping is enabled.
  LSource:isPaused( )  -- Returns true if playback is paused.
  LSource:isPlaying( )  -- Returns true if currently playing.
  LSource:isStopped( )  -- Returns true if playback has stopped.
  LSource:pause( )  -- Pauses playback at the current position.
  LSource:play( )  -- Starts or resumes playback.
  LSource:resume( )  -- Resumes playback from the paused position.
  LSource:seek( pos )  -- Seeks to a time position in seconds.
  LSource:setHighpass( cutoff_hz )  -- Applies a high-pass filter at the given cutoff frequency.
  LSource:setLooping( looping )  -- Enables or disables looping playback.
  LSource:setLowpass( cutoff_hz )  -- Applies a low-pass filter at the given cutoff frequency.
  LSource:setPan( pan )  -- Sets stereo panning (-1.0 left to 1.0 right).
  LSource:setPitch( pitch )  -- Sets the pitch multiplier (1.0 = normal).
  LSource:setVolume( vol )  -- Sets playback volume (0.0 = silent, 1.0 = full).
  LSource:stop( )  -- Stops playback and resets seek position.
  LSource:tell( )  -- Returns the current playback position in seconds.
  LSource:type( )  -- Returns the type name of this object.
  LSource:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.timer {#timer}

> Frame timing: delta time, FPS, sleep, scheduled callbacks.

```lua
  lurek.timer.afterReal( delay, func )  -- Schedules a one-shot callback that fires after `delay` wall-clock seconds,
  lurek.timer.chain( steps )  -> Scheduler  -- Creates a new Scheduler loaded with a sequenced one-shot chain.
  lurek.timer.getAverageDelta( )  -> AverageDelta  -- Returns the rolling-average frame delta time in seconds.
  lurek.timer.getDelta( )  -> Delta  -- Returns the delta time in seconds for the current frame.
  lurek.timer.getFPS( )  -> FPS  -- Returns the current frames-per-second measurement.
  lurek.timer.getFrameCount( )  -> FrameCount  -- Returns the total number of frames rendered since engine start.
  lurek.timer.getMicroTime( )  -> MicroTime  -- Returns the high-resolution elapsed time since engine start in seconds.
  lurek.timer.getPhysicsDelta( )  -> PhysicsDelta  -- Returns the fixed timestep used by `process_physics` callbacks (seconds).
  lurek.timer.getPhysicsMaxSteps( )  -> PhysicsMaxSteps  -- Returns the maximum number of physics sub-steps allowed per frame.
  lurek.timer.getSmoothedDelta( )  -> SmoothedDelta  -- Returns the exponential moving-average of frame deltas in seconds.
  lurek.timer.getTime( )  -> Time  -- Returns the total elapsed time since engine start in seconds.
  lurek.timer.newScheduler( )  -> Scheduler  -- Creates a new independent Scheduler for managing timed callbacks.
  lurek.timer.setPhysicsDelta( dt )  -- Sets the fixed timestep for `process_physics` callbacks (seconds).
  lurek.timer.setPhysicsMaxSteps( n )  -- Sets the maximum number of physics sub-steps allowed per frame (clamped 1â€“64).
  lurek.timer.setSmoothingFactor( alpha )  -- Sets the smoothing factor (alpha) for `getSmoothedDelta`. Must be in [0.01, 1.0].
  lurek.timer.sleep( seconds )  -- Suspends execution for the given number of seconds.
  lurek.timer.step( )  -- Advances the timer by one frame, returning the delta time.
  lurek.timer.tickRealTimers( )  -- Advances all real-time timers by one tick; called automatically each frame.
  lurek.timer.tickWaits( )  -- Advances all `lurek.timer.wait()` coroutines by one tick; called each frame.
  lurek.timer.waitFrames( frames )  -- Yields the current Lua coroutine for at least `frames` engine frames.
  lurek.timer.waitSeconds( seconds )  -- Yields the current Lua coroutine for at least `seconds` wall-clock seconds.
```

**`LScheduler`** methods:

> Lua-side wrapper around a [`Scheduler`] with per-event callback storage.

```lua
  LScheduler:after( delay, func )  -- Schedules a callback to fire once after a delay.
  LScheduler:afterFrames( n, func )  -- Schedules a callback to fire once after `n` frames.
  LScheduler:afterNamed( name, delay, func )  -- Schedules a named one-shot callback, replacing any existing event with the same name.
  LScheduler:cancel( id )  -- Cancels a scheduled event by its numeric ID.
  LScheduler:cancelAll( )  -- Cancels all scheduled events and returns the count removed.
  LScheduler:cancelNamed( name )  -- Cancels a scheduled event by its string name.
  LScheduler:every( interval, func, [count] )  -- Schedules a callback to fire repeatedly at the given interval.
  LScheduler:everyFrames( n, func, [count] )  -- Schedules a callback to fire every `n` frames.
  LScheduler:everyNamed( name, interval, func, [count] )  -- Schedules a named repeating callback, replacing any existing event with the same name.
  LScheduler:getCount( )  -> Count  -- Returns the number of active scheduled events.
  LScheduler:getInterval( id )  -> Interval  -- Returns the base interval in seconds for an event, or nil.
  LScheduler:getRemaining( id )  -> Remaining  -- Returns the seconds remaining until the next fire for an event, or nil.
  LScheduler:getRepeatCount( id )  -> RepeatCount  -- Returns the repeat count remaining for an event, or nil.
  LScheduler:getTimeScale( )  -> TimeScale  -- Returns the current time-scale multiplier.
  LScheduler:isEmpty( )  -- Returns whether the scheduler has no active events.
  LScheduler:isPaused( id )  -- Returns whether the given event is currently paused.
  LScheduler:isPausedNamed( name )  -- Returns whether the named event is currently paused.
  LScheduler:pause( id )  -- Pauses a scheduled event by its ID.
  LScheduler:pauseNamed( name )  -- Pauses a scheduled event by its string name.
  LScheduler:resetEvent( id )  -- Resets an event's remaining time back to its original interval.
  LScheduler:resume( id )  -- Resumes a paused event by its ID.
  LScheduler:resumeNamed( name )  -- Resumes a paused event by its string name.
  LScheduler:setInterval( id, interval )  -- Changes the repeat interval of an existing event.
  LScheduler:setTimeScale( scale )  -- Sets a global time-scale multiplier for this scheduler.
  LScheduler:type( )  -- Returns the type name of this object.
  LScheduler:typeOf( name )  -- Returns true if this object is of the given type.
  LScheduler:update( dt )  -- Advances all timers by dt seconds, firing due callbacks.
  LScheduler:updateFrames( )  -- Advances frame-based events by one frame, firing due callbacks.
```

## lurek.math {#math}

> Core math: vectors, random, interpolation, geometry utilities.

```lua
  lurek.math.Vec2( x, y )  -- Compatibility alias for `vec2`.
  lurek.math.Vec3( x, y, z )  -- Compatibility alias for `vec3`.
  lurek.math.aabbTree( )  -- Creates a new empty AABB tree for efficient broad-phase overlap queries.
  lurek.math.abs( x )  -- Returns the absolute value of x.
  lurek.math.acos( x )  -- Returns the arccosine of x, in radians.
  lurek.math.angleBetween( x1, y1, x2, y2 )  -- Returns the angle in radians from (x1, y1) to (x2, y2).
  lurek.math.applyEasing( name, t )  -- Applies a named easing function to progress value t.
  lurek.math.asin( x )  -- Returns the arcsine of x, in radians.
  lurek.math.atan( y, [x] )  -- Returns the arctangent of x (or atan2(y, x) when two args given).
  lurek.math.atan2( y, x )  -- Returns atan(y/x) using the signs of both args to determine the quadrant.
  lurek.math.bresenham( x1, y1, x2, y2 )  -- Rasterizes a line from (x1,y1) to (x2,y2) using Bresenham's algorithm. Returns a table ...
  lurek.math.catmullRom( points )  -> Catmull  -- Creates a Catmull-Rom spline through the given control points.
  lurek.math.ceil( x )  -- Returns the smallest integer ≥ x.
  lurek.math.circleContainsPoint( cx, cy, r, px, py )  -- Returns true if the point (px, py) lies inside the circle.
  lurek.math.circleIntersectsCircle( x1, y1, r1, x2, y2, r2 )  -- Returns true if two circles overlap.
  lurek.math.circleIntersectsLine( cx, cy, r, lx1, ly1, lx2, ly2 )  -- Tests an infinite line against a circle. Returns hit, then two optional hit-point pairs.
  lurek.math.circleIntersectsSegment( cx, cy, r, sx1, sy1, sx2, sy2 )  -- Tests a line segment against a circle. Returns hit, then two optional hit-point pairs.
  lurek.math.clamp( v, min, max )  -- Clamps `v` between `min` and `max`.
  lurek.math.closestPointOnSegment( px, py, x1, y1, x2, y2 )  -- Returns the closest point on segment (x1,y1)-(x2,y2) to point (px,py).
  lurek.math.convexHull( pts )  -- Computes the convex hull of a flat {x1,y1,...} point list. Returns a flat table.
  lurek.math.cos( x )  -- Returns the cosine of x (radians).
  lurek.math.deg( rad )  -- Converts radians to degrees.
  lurek.math.delaunayTriangulate( pts )  -- Delaunay triangulation of a flat {x1,y1,...} point list. Returns a table of flat 6-numb...
  lurek.math.distance( x1, y1, x2, y2 )  -> Euclidean  -- Returns the Euclidean distance between (x1,y1) and (x2,y2).
  lurek.math.distanceSq( x1, y1, x2, y2 )  -- Returns the squared Euclidean distance between (x1,y1) and (x2,y2) (avoids sqrt).
  lurek.math.exp( x )  -- Returns e raised to the power x.
  lurek.math.fbm( )  -- Returns fractal Brownian motion noise at (x, y).
  lurek.math.floor( x )  -- Returns the largest integer ≤ x.
  lurek.math.fmod( x, y )  -- Returns the remainder of x / y (fmod).
  lurek.math.fromHex( hex )  -- Parses a hex color string (#RRGGBB or #RRGGBBAA) into (r, g, b, a) floats.
  lurek.math.gammaToLinear( c )  -- Converts a gamma-encoded sRGB value to linear space.
  lurek.math.hermite( p0x, p0y, p1x, p1y, m0x, m0y, m1x, m1y )  -> Hermite  -- Creates a Hermite spline defined by two endpoints and tangents.
  lurek.math.hslToRgb( h, s, l )  -- Converts HSL (h: 0-360, s: 0-1, l: 0-1) to RGBA (r, g, b, a) floats.
  lurek.math.inBack( t )  -- Back ease-in — overshoots slightly before settling at the target.
  lurek.math.inBounce( t )  -- Bounce ease-in — reverse bounce effect that accelerates into the motion.
  lurek.math.inCubic( t )  -- Cubic ease-in — acceleration starts slowly then increases sharply.
  lurek.math.inElastic( t )  -- Elastic ease-in — spring-like overshoot at the beginning of the motion.
  lurek.math.inExpo( t )  -- Exponential ease-in — very slow start that accelerates sharply near the end.
  lurek.math.inOutBack( t )  -- Back ease-in-out — overshoot on both ends.
  lurek.math.inOutBounce( t )  -- Bounce ease-in-out — bouncing motion on both ends.
  lurek.math.inOutCubic( t )  -- Cubic ease-in-out — slow start and end with fast cubic middle.
  lurek.math.inOutElastic( t )  -- Elastic ease-in-out — spring-like oscillation on both ends.
  lurek.math.inOutExpo( t )  -- Exponential ease-in-out — very slow start and end with an exponential surge.
  lurek.math.inOutQuad( t )  -- Quadratic ease-in-out — slow start, fast middle, slow end.
  lurek.math.inOutQuart( t )  -- Quartic ease-in-out — very slow start and end with a sharp middle peak.
  lurek.math.inOutSine( t )  -- Sinusoidal ease-in-out — smooth S-curve based on cosine interpolation.
  lurek.math.inQuad( t )  -- Quadratic ease-in — acceleration that starts at zero and increases.
  lurek.math.inQuart( t )  -- Quartic ease-in — strongly delayed acceleration using a power-of-4 curve.
  lurek.math.inSine( t )  -- Sinusoidal ease-in — gentle acceleration based on a sine curve.
  lurek.math.inverseLerp( a, b, v )  -- Returns the interpolation parameter t for `v` in [a, b].
  lurek.math.isConvex( pts )  -- Returns true if the polygon (flat table {x1,y1,...}) is convex.
  lurek.math.lerp( a, b, t )  -- Linear interpolation between two numbers: a + (b - a) * t.
  lurek.math.lineIntersect( x1, y1, x2, y2, x3, y3, x4, y4 )  -- Infinite line intersection. Returns (x, y) or (nil, nil) if lines are parallel.
  lurek.math.linear( t )  -- Linear easing (identity).
  lurek.math.linearToGamma( c )  -- Converts a linear-space value to gamma-encoded sRGB.
  lurek.math.log( x, [b] )  -- Returns the natural log of x, or log base b if b is supplied.
  lurek.math.max( )  -- Returns the largest of the supplied numbers.
  lurek.math.min( )  -- Returns the smallest of the supplied numbers.
  lurek.math.newBezierCurve( points )  -> BezierCurve  -- Creates a new BezierCurve from a flat table of coordinates {x1,y1, x2,y2, ...}.
  lurek.math.newCircle( x, y, radius )  -> Circle  -- Creates a new Circle value type with the given centre and radius.
  lurek.math.newNoiseGenerator( [seed] )  -> NoiseGenerator  -- Creates a new seeded noise generator.
  lurek.math.newRandomGenerator( [seed] )  -> RandomGenerator  -- Creates a new random number generator with an optional seed.
  lurek.math.newSpatialHash( cell_size )  -> SpatialHash  -- Creates a new SpatialHash with the given cell size.
  lurek.math.newTransform( )  -> Transform  -- Creates a new Transform, optionally initialised from full parameters.
  lurek.math.newTween( duration, [easing_name] )  -> Tween  -- Creates a new Tween with the given duration and easing name.
  lurek.math.outBack( t )  -- Back ease-out — overshoots the target then snaps back into place.
  lurek.math.outBounce( t )  -- Bounce ease-out — simulates a ball bouncing against the target value.
  lurek.math.outCubic( t )  -- Cubic ease-out — rapid deceleration using a cubic power curve.
  lurek.math.outElastic( t )  -- Elastic ease-out — spring-like oscillation that settles at the target.
  lurek.math.outExpo( t )  -- Exponential ease-out — sharp initial speed that decelerates exponentially.
  lurek.math.outQuad( t )  -- Quadratic ease-out — deceleration that starts fast and ends at zero.
  lurek.math.outQuart( t )  -- Quartic ease-out — rapid deceleration using a power-of-4 curve.
  lurek.math.outSine( t )  -- Sinusoidal ease-out — gentle deceleration based on a cosine curve.
  lurek.math.perlin2d( x, y, [seed] )  -- Returns 2D Perlin noise at (x, y) with the given seed.
  lurek.math.perlin3d( x, y, z, [seed] )  -- Returns 3D Perlin noise at (x, y, z) with the given seed.
  lurek.math.pointInPolygon( pts, px, py )  -- Returns true if (px, py) is inside the polygon given as a flat {x1,y1,...} table.
  lurek.math.polygonArea( pts )  -- Returns the signed area of a polygon given as a flat {x1,y1,...} table.
  lurek.math.polygonCentroid( pts )  -- Returns the centroid (cx, cy) of a polygon given as a flat {x1,y1,...} table.
  lurek.math.polygonClip( pts, nx, ny, d )  -- Clips a polygon against a single half-plane using the Sutherland-Hodgman algorithm.
  lurek.math.polygonDifference( a, b )  -- Computes the approximate difference `A - B` (the part of A not covered by B).
  lurek.math.polygonIntersection( a, b )  -- Computes the intersection of two convex polygons using the Sutherland-Hodgman
  lurek.math.polygonUnion( a, b )  -- Computes the approximate union of two convex polygons as the convex hull of
  lurek.math.pow( x, y )  -- Returns x raised to the power y.
  lurek.math.rad( deg )  -- Converts degrees to radians.
  lurek.math.random( [a], [b] )  -- Returns a pseudo-random number in [0,1) with no args,
  lurek.math.randomInt( lo, hi )  -- Returns a pseudo-random integer in [lo, hi] (inclusive).
  lurek.math.rectFromCenter( cx, cy, w, h )  -- Creates a rectangle centered at (cx, cy) with the given width and height.
  lurek.math.rectUnion( x1, y1, w1, h1, x2, y2, w2, h2 )  -- Returns the union (bounding box) of two rectangles.
  lurek.math.remap( v, in_min, in_max, out_min, out_max )  -- Remaps `v` from [in_min, in_max] to [out_min, out_max].
  lurek.math.rgbToHsl( r, g, b )  -- Converts RGBA floats to HSL (h: 0-360, s: 0-1, l: 0-1).
  lurek.math.round( x )  -- Returns x rounded to the nearest integer (half-up).
  lurek.math.segmentIntersectsSegment( x1, y1, x2, y2, x3, y3, x4, y4 )  -- Tests if two line segments intersect. Returns (hit, ix?, iy?).
  lurek.math.sign( v )  -- Returns -1, 0, or 1 depending on the sign of `v`.
  lurek.math.simplex2d( x, y, [seed] )  -- Returns 2D Simplex noise at (x, y) with the given seed.
  lurek.math.simplexNoise( x, y, [z] )  -- Returns a simplex noise value in [-1, 1] for 2D or 3D coordinates.
  lurek.math.sin( x )  -- Returns the sine of x (radians).
  lurek.math.smoothstep( edge0, edge1, x )  -- Hermite smoothstep between `edge0` and `edge1`.
  lurek.math.sqrt( x )  -- Returns the square root of x.
  lurek.math.tan( x )  -- Returns the tangent of x (radians).
  lurek.math.triangulate( pts )  -- Triangulates a simple polygon given as a flat table {x1,y1, x2,y2, ...}.
  lurek.math.vec2( x, y )  -- Creates a 2D vector with x and y components.
  lurek.math.vec3( x, y, z )  -- Creates a 3D vector `{x, y, z}` table with numeric components.
  lurek.math.voronoi( points )  -- Computes the Voronoi diagram for a list of 2-D seed points.
```

**`LAabbTree`** methods:

> Lua-side wrapper around an [`AabbTree`].

```lua
  LAabbTree:clear( )  -- Removes all entries from the tree.
  LAabbTree:contains( id )  -- Returns true if an entry with the given id exists in the tree.
  LAabbTree:insert( id, min_x, min_y, max_x, max_y )  -- Inserts an entry with the given AABB into the tree.
  LAabbTree:isEmpty( )  -- Returns true if the tree contains no entries.
  LAabbTree:len( )  -- Returns the number of entries in the tree.
  LAabbTree:query( min_x, min_y, max_x, max_y )  -- Returns the ids of all entries whose AABBs overlap the query rectangle.
  LAabbTree:queryPoint( x, y )  -- Returns the ids of all entries whose AABBs contain the given point.
  LAabbTree:remove( id )  -- Removes the entry with the given id.
  LAabbTree:type( )  -- Returns the type name of this object.
  LAabbTree:typeOf( name )  -- Returns true if this object is of the given type.
  LAabbTree:update( id, min_x, min_y, max_x, max_y )  -- Updates the AABB for an existing entry.
```

**`LBezierCurve`** methods:

> Lua-side wrapper around a [`BezierCurve`].

```lua
  LBezierCurve:evaluate( t )  -- Evaluates the curve at parameter t, returning (x, y).
  LBezierCurve:getControlPoint( index )  -> ControlPoint  -- Returns the control point at 1-based index as (x, y), or nil.
  LBezierCurve:getControlPointCount( )  -> ControlPointCount  -- Returns the number of control points.
  LBezierCurve:getDerivative( )  -> BezierCurve  -- Returns a new BezierCurve representing the first derivative.
  LBezierCurve:insertControlPoint( x, y, [index] )  -- Inserts a control point. If index is given (1-based), inserts at that position.
  LBezierCurve:length( )  -- Returns the approximate arc length of the curve.
  LBezierCurve:removeControlPoint( index )  -- Removes a control point at 1-based index.
  LBezierCurve:render( segments )  -- Renders the curve as a polyline with the given number of segments.
  LBezierCurve:rotate( angle, ox, oy )  -- Rotates all control points around a pivot by angle radians.
  LBezierCurve:scale( s, ox, oy )  -- Scales all control points around a pivot by factor s.
  LBezierCurve:setControlPoint( index, x, y )  -- Sets the control point at 1-based index.
  LBezierCurve:translate( dx, dy )  -- Translates all control points by (dx, dy).
  LBezierCurve:type( )  -- Returns the type name of this object.
  LBezierCurve:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LCatmullRom`** methods:

> Lua-side wrapper around a [`CatmullRomSpline`].

```lua
  LCatmullRom:addPoint( x, y )  -- Appends a control point to the spline.
  LCatmullRom:len( )  -- Number of control points.
  LCatmullRom:removePoint( idx )  -- Removes the control point at `index` (0-based) and returns it.
  LCatmullRom:sample( t )  -- Sample the spline at global t in [0, 1].
  LCatmullRom:sampleSegment( seg, t )  -- Sample a specific segment at local t in [0, 1].
  LCatmullRom:type( )  -- Returns the type name of this object.
  LCatmullRom:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LCircle`** methods:

> Lua-side wrapper around a [`Circle`].

```lua
  LCircle:aabb( )  -- Returns the axis-aligned bounding box as (min_x, min_y, max_x, max_y).
  LCircle:area( )  -- Returns the area of the circle (π r²).
  LCircle:contains( px, py )  -- Returns true if the point (px, py) lies inside or on the boundary.
  LCircle:intersects( other )  -- Returns true if this circle overlaps another circle.
  LCircle:perimeter( )  -- Returns the circumference of the circle (2 π r).
  LCircle:radius( )  -- Returns the circle radius.
  LCircle:type( )  -- Returns the type name of this object.
  LCircle:typeOf( name )  -- Returns true if this object is of the given type.
  LCircle:x( )  -- Returns the circle centre X.
  LCircle:y( )  -- Returns the circle centre Y.
```

**`LHermite`** methods:

> Lua-side wrapper around a [`HermiteSpline`].

```lua
  LHermite:sample( t )  -- Evaluate the spline at parameter t in [0, 1].
  LHermite:type( )  -- Returns the type name of this object.
  LHermite:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LNoiseGenerator`** methods:

> Lua-side wrapper around a [`NoiseGenerator`].

```lua
  LNoiseGenerator:fbm( )  -- Returns fractal Brownian motion noise at (x, y).
  LNoiseGenerator:generateMap( w, h, [opts] )  -- Generates a 2D noise map as a flat table (row-major).
  LNoiseGenerator:getSeed( )  -> Seed  -- Returns the current seed.
  LNoiseGenerator:perlin1d( x )  -- Returns 1D Perlin noise at x.
  LNoiseGenerator:perlin2d( x, y )  -- Returns 2D Perlin noise at (x, y).
  LNoiseGenerator:perlin3d( x, y, z )  -- Returns 3D Perlin noise at (x, y, z).
  LNoiseGenerator:perlin4d( x, y, z, w )  -- Returns 4D Perlin noise at (x, y, z, w).
  LNoiseGenerator:ridged( )  -- Returns ridged multi-fractal noise at (x, y).
  LNoiseGenerator:setSeed( seed )  -- Sets the seed and rebuilds the permutation table.
  LNoiseGenerator:simplex1d( x )  -- Returns 1D Simplex noise at x.
  LNoiseGenerator:simplex2d( x, y )  -- Returns 2D Simplex noise at (x, y).
  LNoiseGenerator:simplex3d( x, y, z )  -- Returns 3D Simplex noise at (x, y, z).
  LNoiseGenerator:turbulence( )  -- Returns turbulence noise at (x, y).
  LNoiseGenerator:type( )  -- Returns the type name of this object.
  LNoiseGenerator:typeOf( name )  -- Returns true if this object is of the given type.
  LNoiseGenerator:warpDomain( x, y, strength )  -- Applies domain warping, returning offset (x, y).
  LNoiseGenerator:worley2d( x, y, [dist_name], [f2] )  -- Returns 2D Worley (cellular) noise at (x, y).
  LNoiseGenerator:worley3d( x, y, z, [dist_name], [f2] )  -- Returns 3D Worley (cellular) noise at (x, y, z).
```

**`LRandomGenerator`** methods:

> Lua-side wrapper around a [`RandomGenerator`].

```lua
  LRandomGenerator:getSeed( )  -> Seed  -- Returns the seed used to initialise this generator.
  LRandomGenerator:getState( )  -> State  -- Serialises the generator state as a string for later restoration.
  LRandomGenerator:random( )  -- Returns a uniform random number in [0, 1).
  LRandomGenerator:randomFloat( min, max )  -- Returns a uniform random float in [min, max).
  LRandomGenerator:randomInt( min, max )  -- Returns a uniform random integer in [min, max].
  LRandomGenerator:randomNormal( [stddev], [mean] )  -- Returns a random number from a normal (Gaussian) distribution.
  LRandomGenerator:setSeed( seed )  -- Sets the seed, fully resetting the generator state.
  LRandomGenerator:setState( state )  -- Restores the generator state from a previously serialised string.
  LRandomGenerator:type( )  -- Returns the type name of this object.
  LRandomGenerator:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LSpatialHash`** methods:

> Lua-side wrapper around a [`SpatialHash`].

```lua
  LSpatialHash:clear( )  -- Removes all registered items from this spatial hash, leaving it empty.
  LSpatialHash:getCellSize( )  -> CellSize  -- Returns the cell size used to partition the spatial hash grid.
  LSpatialHash:getItemCount( )  -> ItemCount  -- Returns the number of items in the hash.
  LSpatialHash:insert( id, x, y, w, h )  -- Inserts an item with the given AABB.
  LSpatialHash:queryCircle( cx, cy, radius )  -> IDs  -- Returns IDs of items overlapping the query circle.
  LSpatialHash:queryRect( x, y, w, h )  -> IDs  -- Returns IDs of items overlapping the query rectangle.
  LSpatialHash:querySegment( x1, y1, x2, y2 )  -> IDs  -- Returns IDs of items whose AABBs are intersected by the line segment.
  LSpatialHash:remove( id )  -- Removes an item by its ID.
  LSpatialHash:type( )  -- Returns the type name of this object.
  LSpatialHash:typeOf( name )  -- Returns true if this object is of the given type.
  LSpatialHash:update( id, x, y, w, h )  -- Updates an existing item's AABB.
```

**`LTransform`** methods:

> Lua-side wrapper around a [`Transform`].

```lua
  LTransform:clone( )  -- Returns a copy of this transform.
  LTransform:decompose( )  -- Decomposes this transform into translation, rotation, and scale.
  LTransform:getMatrix( )  -> Matrix  -- Returns the 3x3 matrix as a flat table of 9 numbers (row-major).
  LTransform:inverse( )  -> Transform  -- Returns a new Transform that undoes this transform.
  LTransform:inverseTransformPoint( x, y )  -- Transforms a point from world space back to local space.
  LTransform:reset( )  -- Resets the transform to identity.
  LTransform:rotate( angle )  -- Applies a rotation in radians.
  LTransform:scale( sx, [sy] )  -- Applies non-uniform scaling.
  LTransform:setTransformation( )  -- Replaces the transform with full transformation parameters.
  LTransform:shear( kx, ky )  -- Applies horizontal and vertical shear factors to this transform matrix.
  LTransform:transformPoint( x, y )  -- Transforms a point from local space to world space.
  LTransform:translate( dx, dy )  -- Applies translation to the transform.
  LTransform:type( )  -- Returns the type name of this object.
  LTransform:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LTween`** methods:

> Lua-side wrapper around a [`Tween`].

```lua
  LTween:addValue( start, target )  -- Adds a start/target value pair. Returns the 1-based index.
  LTween:getAllValues( )  -> AllValues  -- Returns all interpolated values as a table.
  LTween:getClock( )  -> Clock  -- Alias for getTime(). Returns the current clock time.
  LTween:getDuration( )  -> Duration  -- Returns the tween duration in seconds.
  LTween:getEasingName( )  -> EasingName  -- Returns the easing function name.
  LTween:getTime( )  -> Time  -- Returns the current clock time.
  LTween:getValue( [index] )  -> Value  -- Returns the interpolated value at 1-based index, or all values as a
  LTween:getValueCount( )  -> ValueCount  -- Returns the number of values in this tween.
  LTween:isComplete( )  -- Returns true if the tween has finished.
  LTween:reset( )  -- Resets the tween elapsed time to zero, restarting the animation.
  LTween:set( t )  -- Alias for setTime(). Sets the clock to t, clamped to [0, duration].
  LTween:setTime( t )  -- Sets the clock to a specific time, clamped to [0, duration].
  LTween:type( )  -- Returns the type name of this object.
  LTween:typeOf( name )  -- Returns true if this object is of the given type.
  LTween:update( dt )  -- Advances the clock by dt seconds. Returns true when complete.
```

**`LVec2`** methods:

> Lua-side wrapper around a [`Vec2`] value type.

```lua
  LVec2:angle( )  -- Returns the angle of this vector in radians (atan2(y, x)).
  LVec2:cross( other )  -- Returns the 2D cross product (scalar) with another vector.
  LVec2:distance( other )  -> Euclidean  -- Returns the Euclidean distance from this vector to another.
  LVec2:dot( other )  -- Returns the dot product with another vector.
  LVec2:fromAngle( radians )  -- Creates a unit vector from an angle in radians.
  LVec2:length( )  -> Euclidean  -- Returns the Euclidean length of the vector.
  LVec2:lengthSquared( )  -- Returns the squared length of the vector (faster than length).
  LVec2:lerp( other, t )  -- Returns a linearly interpolated vector between this and other at parameter t.
  LVec2:normalize( )  -- Returns a unit-length copy of this vector. Returns zero if length is zero.
  LVec2:normalized( )  -- Compatibility alias for `normalize`.
  LVec2:perpendicular( )  -- Returns the perpendicular vector (-y, x).
  LVec2:reflect( normal )  -- Reflects this vector off a surface with the given normal.
  LVec2:rotate( angle )  -- Returns a new vector rotated by the given angle in radians.
  LVec2:type( )  -- Returns the type name of this object.
  LVec2:typeOf( name )  -- Returns true if this object is of the given type.
  LVec2:x( )  -- Returns the horizontal component of the vector.
  LVec2:y( )  -- Returns the vertical component of the vector.
```

**`LVec3`** methods:

> Lua-side wrapper around a [`Vec3`] value type.

```lua
  LVec3:add( other )  -- Add another Vec3 and return the result.
  LVec3:cross( other )  -- Cross product with another Vec3.
  LVec3:distance( other )  -- Euclidean distance to another Vec3.
  LVec3:dot( other )  -- Dot product with another Vec3.
  LVec3:length( )  -> Euclidean  -- Returns the Euclidean length of the vector.
  LVec3:lengthSquared( )  -- Returns the squared Euclidean length (avoids sqrt).
  LVec3:lerp( other, t )  -- Linear interpolation towards another Vec3.
  LVec3:normalize( )  -- Returns a unit-length version of this vector.
  LVec3:scale( s )  -- Scale this vector by a scalar and return the result.
  LVec3:splat( v )  -> Vec3  -- Creates a Vec3 with all components set to `v`.
  LVec3:sub( other )  -- Subtract another Vec3 and return the result.
  LVec3:type( )  -- Returns the type name of this object.
  LVec3:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.physics {#physics}

> 2D physics simulation via rapier2d: bodies, colliders, joints, sensors, raycasts.

```lua
  lurek.physics.attachShape( body_ud, shape_ud )  -- Attaches a standalone shape to a body as an additional fixture.
  lurek.physics.debugDraw( enable )  -- Enables or disables the physics debug overlay (AABB boxes and velocity vectors).
  lurek.physics.destroyWorld( world_ud )  -- Marks a physics world for destruction. Subsequent operations on the world
  lurek.physics.drawDebugGpu( world_ud, config_val )  -- Extracts collider geometry from a World and queues a GPU physics debug
  lurek.physics.getBody( world_ud, body_ud )  -> Body  -- Returns the position and velocity of a body (x, y, vx, vy).
  lurek.physics.getCollisions( world_ud )  -> Collisions  -- Returns all collision events from the last simulation step.
  lurek.physics.isSleepingAllowed( world_ud, body_ud )  -- Returns whether the body is allowed to sleep.
  lurek.physics.newBody( world_ud, x, y, bt )  -> Body  -- Creates a new rectangular body in the given world.
  lurek.physics.newCellular( width, height )  -> Cellular  -- Creates a falling-sand cellular automaton grid.
  lurek.physics.newChainShape( closed, coords )  -> ChainShape  -- Creates a chain shape userdata from flat variadic vertex pairs.
  lurek.physics.newCircleShape( r )  -> CircleShape  -- Creates a circle shape userdata.
  lurek.physics.newEdgeShape( x1, y1, x2, y2 )  -> EdgeShape  -- Creates an edge (line segment) shape userdata.
  lurek.physics.newPolygonShape( )  -> PolygonShape  -- Creates a convex polygon shape userdata from flat variadic vertex pairs.
  lurek.physics.newRectangleShape( w, h )  -> RectangleShape  -- Creates a rectangle shape userdata.
  lurek.physics.newTerrain( width, height, cell_size, world_ud )  -> Terrain  -- Creates a destructible terrain grid.
  lurek.physics.newWorld( gx, gy )  -> World  -- Creates a new physics world with the given gravity vector.
  lurek.physics.setBodyVelocity( world_ud, body_ud, vx, vy )  -- Sets the velocity of a body.
  lurek.physics.setSleepingAllowed( world_ud, body_ud, allowed )  -- Sets whether the body is allowed to sleep.
  lurek.physics.step( world_ud, dt )  -- Advances the physics world by dt seconds.
  lurek.physics.testAABB( ax, ay, aw, ah, bx, by, bw, bh )  -- Returns true when two axis-aligned bounding boxes overlap.
  lurek.physics.testCircleAABB( cx, cy, cr, ax, ay, aw, ah )  -- Returns true when a circle overlaps an AABB.
  lurek.physics.testCircles( ax, ay, ar, bx, by, br )  -- Returns true when two circles overlap.
  lurek.physics.testPoint( px, py, ax, ay, aw, ah )  -- Returns true when point (px, py) lies inside the AABB.
```

**`LBody`** methods:

> Lua-side handle to a physics body accessed through its world.

```lua
  LBody:applyAngularImpulse( impulse )  -- Applies an angular impulse.
  LBody:applyForce( fx, fy )  -- Applies a continuous force to the body.
  LBody:applyForceAtPoint( fx, fy, px, py )  -- Applies a force at a specific world-space point.
  LBody:applyImpulse( ix, iy )  -- Applies a linear impulse to the body.
  LBody:applyTorque( torque )  -- Applies a torque (rotational force).
  LBody:destroy( )  -- Removes this body from the world.
  LBody:getAngle( )  -> Angle  -- Returns the body angle in radians.
  LBody:getAngularDamping( )  -> AngularDamping  -- Returns the angular damping coefficient.
  LBody:getAngularVelocity( )  -> AngularVelocity  -- Returns the angular velocity in radians/s.
  LBody:getFriction( )  -> Friction  -- Returns the body friction coefficient.
  LBody:getGravityScale( )  -> GravityScale  -- Returns the per-body gravity multiplier.
  LBody:getHeight( )  -> Height  -- Returns the height of this body's primary collider shape in world units.
  LBody:getId( )  -> Id  -- Returns the body's integer ID.
  LBody:getLayer( )  -> Layer  -- Returns the collision layer bitmask.
  LBody:getLinearDamping( )  -> LinearDamping  -- Returns the linear damping coefficient.
  LBody:getMask( )  -> Mask  -- Returns the collision mask bitmask.
  LBody:getMass( )  -> Mass  -- Returns the body mass in kilograms used for force and impulse calculations.
  LBody:getPosition( )  -> Position  -- Returns the body position (x, y).
  LBody:getRestitution( )  -> Restitution  -- Returns the body restitution (bounciness).
  LBody:getType( )  -> Type  -- Returns the body type as a string.
  LBody:getVelocity( )  -> Velocity  -- Returns the body velocity (vx, vy).
  LBody:getWidth( )  -> Width  -- Returns the width of this body's primary collider shape in world units.
  LBody:getX( )  -- Returns the body X position.
  LBody:getY( )  -- Returns the body Y position.
  LBody:isBullet( )  -- Returns whether CCD is enabled.
  LBody:isFixedRotation( )  -- Returns whether rotation is locked.
  LBody:isSleeping( )  -- Returns true if this body is currently sleeping (inactive).
  LBody:isSleepingAllowed( )  -- Returns whether the body can sleep.
  LBody:setAngle( angle )  -- Sets the body angle in radians.
  LBody:setAngularDamping( damping )  -- Sets the angular damping coefficient.
  LBody:setAngularVelocity( omega )  -- Sets the angular velocity.
  LBody:setBullet( bullet )  -- Enables or disables continuous collision detection (CCD) for fast-moving bodies.
  LBody:setFixedRotation( fixed )  -- Locks or unlocks rotation.
  LBody:setFriction( friction )  -- Sets the body friction coefficient.
  LBody:setGravityScale( scale )  -- Sets the per-body gravity multiplier.
  LBody:setLayer( layer )  -- Sets the collision layer bitmask.
  LBody:setLinearDamping( damping )  -- Sets the linear damping coefficient.
  LBody:setMask( mask )  -- Sets the collision mask bitmask.
  LBody:setMass( mass )  -- Sets the body mass; affects how forces and impulses change velocity.
  LBody:setPosition( x, y )  -- Teleports the body to the given world-space position, bypassing collision.
  LBody:setRestitution( restitution )  -- Sets the body restitution (bounciness).
  LBody:setSleepingAllowed( allowed )  -- Sets whether the body can sleep.
  LBody:setType( bt )  -- Changes the body type: `"dynamic"`, `"static"`, or `"kinematic"`.
  LBody:setVelocity( vx, vy )  -- Sets the body's linear velocity in world units per second.
  LBody:sleep( )  -- Puts this body to sleep immediately.
  LBody:type( )  -- Returns the type name of this object.
  LBody:typeOf( name )  -- Returns true if this object is of the given type.
  LBody:wakeUp( )  -- Forcibly wakes up this body.
```

**`LCellular`** methods:

> Lua-side handle to a falling-sand [`CellularWorld`].

```lua
  LCellular:countCells( t )  -- Counts cells of the given material type.
  LCellular:fillCircle( cx, cy, r, t )  -- Fills a circle of cells with the given material.
  LCellular:fillRect( cx0, cy0, cw, ch, t )  -- Fills a rectangular region of cells with the given material.
  LCellular:findCells( t )  -- Returns positions of all cells of the given material as an array of `{x, y}` tables.
  LCellular:getCell( cx, cy )  -> Cell  -- Returns the material at `(cx, cy)` as an integer constant.
  LCellular:loadFromBytes( data )  -- Loads grid data from bytes produced by `toBytes`.
  LCellular:setCell( cx, cy, t )  -- Sets the material of a cell.
  LCellular:step( )  -- Advances the simulation by one tick.
  LCellular:stepN( n )  -- Advances the simulation by `n` ticks.
  LCellular:toBytes( )  -- Serialises the grid to a byte string.
  LCellular:toImageData( )  -- Returns the full grid as an RGBA byte string using the default colour palette.
  LCellular:toImageDataRegion( cx0, cy0, cw, ch )  -- Returns a sub-region as an RGBA byte string.
  LCellular:type( )  -- Returns the type name of this object.
  LCellular:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LPhysicsShape`** methods:

> Lua-side standalone shape object (circle, rectangle, edge, polygon, chain).

```lua
  LPhysicsShape:destroy( )  -- Releases this shape handle (GC handles cleanup).
  LPhysicsShape:getBoundingBox( )  -> BoundingBox  -- Returns the axis-aligned bounding box (x1, y1, x2, y2).
  LPhysicsShape:getRadius( )  -> Radius  -- Returns the radius. Only valid for circle shapes.
  LPhysicsShape:getType( )  -> Type  -- Returns the shape type string: "circle", "rectangle", "polygon", "edge", or "chain".
  LPhysicsShape:setDensity( density )  -- Sets the density for this shape (used when attaching to a body).
  LPhysicsShape:setFriction( friction )  -- Sets the friction coefficient.
  LPhysicsShape:setRestitution( restitution )  -- Sets the restitution (bounciness) coefficient.
  LPhysicsShape:setSensor( sensor )  -- Sets whether this shape is a sensor (non-colliding trigger).
  LPhysicsShape:type( )  -- Returns the type name of this object.
  LPhysicsShape:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LTerrain`** methods:

> Lua-side handle to a destructible [`TerrainMap`].

```lua
  LTerrain:collapseColumns( )  -- Removes unsupported cells, returning the number of cells that fell.
  LTerrain:fillAll( solid )  -- Sets every cell in the grid to `solid`.
  LTerrain:fillCircle( wx, wy, radius, solid )  -- Fills a circle of cells centred at world position `(wx, wy)`.
  LTerrain:fillRect( wx, wy, w, h, solid )  -- Fills a rectangular region of cells.
  LTerrain:flush( )  -- Rebuilds physics bodies for all dirty chunks.
  LTerrain:getCell( cx, cy )  -> Cell  -- Returns whether a cell is solid.
  LTerrain:isDirty( )  -- Returns `true` when at least one chunk needs flushing.
  LTerrain:loadFromBytes( data )  -- Loads terrain cell data from bytes produced by `toBytes`.
  LTerrain:setCell( cx, cy, solid )  -- Sets a single terrain cell to solid or empty.
  LTerrain:solidPositions( )  -- Returns the world-space centres of all solid cells as an array of `{x, y}` tables.
  LTerrain:spawnDebris( positions, mass, restitution )  -- Spawns dynamic debris bodies at the given positions.
  LTerrain:toBytes( )  -- Serialises the terrain grid to a byte string for save/load.
  LTerrain:toImageData( sr, sg, sb, er, eg, eb )  -- Returns the terrain as an RGBA byte string.
  LTerrain:type( )  -- Returns the type name of this object.
  LTerrain:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LWorld`** methods:

> Lua-side handle wrapping a physics World.

```lua
  LWorld:addDistanceJoint( a, b, ax1, ay1, ax2, ay2, len )  -- Creates a distance joint between two bodies.
  LWorld:addFixture( )  -- Adds an extra fixture (collider) to a body.
  LWorld:addFrictionJoint( a, b, ax, ay, max_f, max_t )  -- Creates a friction joint that resists relative motion.
  LWorld:addGearJoint( a, b, ax, ay )  -- Creates a gear joint (stub — falls back to weld joint).
  LWorld:addMotorJoint( a, b, factor )  -- Creates a motor joint that drives body_b toward body_a.
  LWorld:addMouseJoint( body_id, tx, ty, max_f )  -- Creates a mouse joint connecting a body to a target point.
  LWorld:addPrismaticJoint( a, b, ax, ay, axis_x, axis_y )  -- Creates a prismatic (slider) joint between two bodies.
  LWorld:addPulleyJoint( a, b, ax, ay )  -- Creates a pulley joint (stub — falls back to weld joint).
  LWorld:addRevoluteJoint( a, b, ax, ay )  -- Creates a revolute (pin) joint between two bodies.
  LWorld:addRopeJoint( a, b, ax1, ay1, ax2, ay2, max )  -- Creates a rope joint with a maximum distance.
  LWorld:addWeldJoint( a, b, ax, ay )  -- Creates a weld (rigid) joint between two bodies.
  LWorld:addWheelJoint( a, b, ax, ay, axis_x, axis_y )  -- Creates a wheel joint (prismatic + rotation).
  LWorld:addZone( x, y, w, h )  -- Creates a rectangular gravity/damping zone and returns a LuaZone handle.
  LWorld:clear( )  -- Resets the world, removing all bodies and joints.
  LWorld:clearBeginContact( )  -- Removes the begin-contact callback.
  LWorld:clearBodyData( id )  -- Removes the Lua data attached to a body.
  LWorld:clearBodyOneWay( id )  -- Removes the one-way platform flag from a body.
  LWorld:clearEndContact( )  -- Removes the end-contact callback.
  LWorld:destroyBody( id )  -- Removes a body from the world.
  LWorld:destroyJoint( jid )  -- Removes a joint from the world.
  LWorld:drawDebug( )  -- Draws physics objects for debugging
  LWorld:fixtureCount( body_id )  -- Returns the number of fixtures on a body.
  LWorld:getBeginContactEvents( )  -> BeginContactEvents  -- Returns begin-contact events from the last step.
  LWorld:getBodyAtPoint( x, y )  -> BodyAtPoint  -- Returns the body ID at a world-space point, or nil.
  LWorld:getBodyCCD( id )  -> BodyCCD  -- Returns whether CCD is enabled for a body.
  LWorld:getBodyContacts( body_id )  -> BodyContacts  -- Returns contacts involving a specific body.
  LWorld:getBodyCount( )  -> BodyCount  -- Returns the total number of bodies in the world.
  LWorld:getBodyData( id )  -> Lua  -- Returns the Lua data previously attached to a body, or nil if none is set.
  LWorld:getBodyIds( )  -> BodyIds  -- Returns all body IDs in the world.
  LWorld:getBodyOneWay( id )  -> BodyOneWay  -- Returns the one-way normal for a body, or nil if not configured.
  LWorld:getBodyType( id )  -> BodyType  -- Returns the body type as a string.
  LWorld:getCollisionEvents( )  -> CollisionEvents  -- Returns collision events from the last step.
  LWorld:getContacts( )  -> Contacts  -- Returns all contact pairs from the narrow phase.
  LWorld:getEndContactEvents( )  -> EndContactEvents  -- Returns end-contact events from the last step.
  LWorld:getGravity( )  -> Gravity  -- Returns the gravity vector (gx, gy).
  LWorld:getJointBodies( jid )  -> JointBodies  -- Returns the two body IDs connected by a joint.
  LWorld:getJointBreakForce( jid )  -> JointBreakForce  -- Returns the break threshold for a joint, or nil if not set.
  LWorld:getJointIds( )  -> JointIds  -- Returns a table of integer IDs for every joint attached to this world.
  LWorld:getJointLimits( jid )  -> JointLimits  -- Returns the angular limits on a joint.
  LWorld:getJointMotorSpeed( jid )  -> JointMotorSpeed  -- Returns the motor speed on a joint's angular axis.
  LWorld:getJointType( jid )  -> JointType  -- Returns the type name of a joint.
  LWorld:getMeter( )  -> Meter  -- Returns the pixels-per-meter scaling factor.
  LWorld:getSolverIterations( )  -> SolverIterations  -- Returns the current number of solver iterations per step.
  LWorld:getZoneEvents( )  -> ZoneEvents  -- Returns zone enter/leave events produced by the most recent step.
  LWorld:isBodySleeping( id )  -- Returns true if a body is currently sleeping (inactive).
  LWorld:jointCount( )  -- Returns the total number of joints.
  LWorld:newBodies( specs )  -> Bodies  -- Creates multiple bodies in one call.
  LWorld:newBody( x, y, bt )  -> Body  -- Creates a new rectangular body and adds it to the world.
  LWorld:newChainBody( x, y, tbl, closed, bt )  -> ChainBody  -- Creates a new chain body from a flat vertex table and adds it to the world.
  LWorld:newCircleBody( x, y, radius, bt )  -> CircleBody  -- Creates a new circular body and adds it to the world.
  LWorld:newEdgeBody( x, y, x1, y1, x2, y2, bt )  -> EdgeBody  -- Creates a new edge (line segment) body and adds it to the world.
  LWorld:newPolygonBody( x, y, tbl, bt )  -> PolygonBody  -- Creates a new polygon body from a flat vertex table and adds it to the world.
  LWorld:queryAABB( x, y, w, h )  -- Returns body IDs within an axis-aligned bounding box.
  LWorld:raycast( x1, y1, x2, y2 )  -- Casts a ray and returns the nearest hit, or nil.
  LWorld:raycastAll( x1, y1, dx, dy, max_dist )  -- Casts a ray and returns all hits.
  LWorld:raycastClosest( x1, y1, dx, dy, max_dist )  -- Casts a ray and returns the closest hit using the query pipeline.
  LWorld:setBeginContact( f )  -- Registers a Lua function called with (bodyIdA, bodyIdB) when two
  LWorld:setBodyCCD( id, enabled )  -- Enables or disables Continuous Collision Detection for a body.
  LWorld:setBodyData( id, value )  -- Attaches arbitrary Lua data to a body for retrieval in collision callbacks.
  LWorld:setBodyOneWay( id, nx, ny )  -- Marks a body as a one-way platform.  Bodies approaching from the
  LWorld:setBodyType( id, bt )  -- Changes the simulation type of the body: `"dynamic"`, `"static"`, or `"kinematic"`.
  LWorld:setEndContact( f )  -- Registers a Lua function called with (bodyIdA, bodyIdB) when two
  LWorld:setFixtureFriction( body_id, fix_idx, friction )  -- Sets friction on a fixture by index.
  LWorld:setFixtureRestitution( body_id, fix_idx, restitution )  -- Sets restitution on a fixture by index.
  LWorld:setFixtureSensor( body_id, fix_idx, sensor )  -- Sets whether a fixture is a sensor.
  LWorld:setGravity( gx, gy )  -- Sets the world gravity vector; default is `(0, 9.81)` (downward).
  LWorld:setJointBreakForce( jid, f )  -- Sets the relative-velocity threshold above which a joint breaks.
  LWorld:setJointLimits( jid, lower, upper )  -- Sets the angular limits on a joint.
  LWorld:setJointLimitsEnabled( jid, enabled )  -- Enables or disables angular limits on a joint.
  LWorld:setJointMotorSpeed( jid, speed )  -- Sets the motor speed on a joint's angular axis.
  LWorld:setMeter( ppm )  -- Sets the pixels-per-meter scaling factor.
  LWorld:setMouseJointTarget( jid, x, y )  -- Updates the target position of a mouse joint.
  LWorld:setSolverIterations( n )  -- Sets the number of constraint solver iterations per step.
  LWorld:sleepBody( id )  -- Puts a body to sleep immediately.
  LWorld:step( dt )  -- Advances the physics simulation by dt seconds, firing onBeginContact /
  LWorld:stepFixed( accum, step_dt, max_steps )  -- Steps the world using a fixed sub-step size to consume accumulated time.
  LWorld:toPhysics( px )  -- Converts a pixel value to physics units.
  LWorld:toPixels( m )  -- Converts a physics-unit value to pixels.
  LWorld:type( )  -- Returns the type name of this object.
  LWorld:typeOf( name )  -- Returns true if this object is of the given type.
  LWorld:wakeUpBody( id )  -- Forcibly wakes up a sleeping body.
```

**`LZone`** methods:

> Lua-side handle to a [`PhysicsZone`] living inside a [`World`].

```lua
  LZone:destroy( )  -- Removes the zone from the world.
  LZone:getId( )  -> Id  -- Returns the zone's integer ID.
  LZone:setAngularDampingOverride( [value] )  -- Sets an optional angular damping override for bodies inside the zone.
  LZone:setCircle( cx, cy, radius )  -- Replaces the zone boundary with a circle.
  LZone:setEnabled( enabled )  -- Enables or disables the zone.
  LZone:setGravityDirectional( gx, gy )  -- Sets directional gravity inside the zone.
  LZone:setGravityPoint( cx, cy, strength )  -- Sets point-attractor gravity inside the zone.
  LZone:setGravityRepulsor( cx, cy, strength )  -- Sets point-repulsor gravity inside the zone.
  LZone:setGravityZero( )  -- Suppresses gravity inside the zone (zero-g pocket).
  LZone:setLayerMask( mask )  -- Sets the layer bitmask; only bodies whose `layer & mask != 0` are affected.
  LZone:setLinearDampingOverride( [value] )  -- Sets an optional linear damping override for bodies inside the zone.
  LZone:setPriority( priority )  -- Sets the zone priority; higher values win over lower when zones overlap.
  LZone:type( )  -- Returns the type name of this object.
  LZone:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.filesystem {#filesystem}

> Game filesystem: read/write files, directory listing, save paths, sandboxed I/O.

```lua
  lurek.filesystem.append( path, data )  -- Opens the file in append mode and writes the given string at the end.
  lurek.filesystem.copy( src, dst )  -- Copies a file within the sandbox.
  lurek.filesystem.createDirectory( path )  -> Directory  -- Creates a directory and any missing parent directories in the save area.
  lurek.filesystem.createTempFile( [prefix] )  -> TempFile  -- Creates an empty temporary file in the `save/` sandbox and returns its
  lurek.filesystem.exists( path )  -- Returns whether the given file or directory exists.
  lurek.filesystem.getDirectoryItems( path )  -> DirectoryItems  -- Returns a table containing the names of every file and subdirectory in the given path.
  lurek.filesystem.getIdentity( )  -> Identity  -- Returns the identity string used to locate the game's save directory.
  lurek.filesystem.getInfo( path )  -> Info  -- Returns a table of metadata for a path, or nil if the path does not exist.
  lurek.filesystem.getSaveDirectory( )  -> SaveDirectory  -- Returns the sandboxed save data directory path.
  lurek.filesystem.getSource( )  -> Source  -- Returns the absolute path of the directory the game was loaded from.
  lurek.filesystem.getUserDirectory( )  -> UserDirectory  -- Returns the current user's home directory path.
  lurek.filesystem.getWorkingDirectory( )  -> WorkingDirectory  -- Returns the current working directory path.
  lurek.filesystem.glob( pattern )  -- Returns a sorted list of paths matching a simple wildcard pattern.
  lurek.filesystem.isDirectory( path )  -- Returns whether the given path is a directory.
  lurek.filesystem.isFile( path )  -- Returns whether the given path is a regular file.
  lurek.filesystem.lines( path )  -- Returns an iterator function over the lines of a text file.
  lurek.filesystem.listRecursive( path )  -- Returns a sorted list of all files under `path`, recursively.
  lurek.filesystem.load( path )  -- Loads and compiles a Lua file from the VFS, returning it as a callable function.
  lurek.filesystem.mkdir( path )  -- Creates a directory (and any missing parents) relative to the game root.
  lurek.filesystem.mount( src, mp )  -- Mounts a directory at a virtual path inside the game filesystem.
  lurek.filesystem.mountZip( archive_path, prefix )  -- Mounts a ZIP archive at a virtual path prefix, making its contents readable
  lurek.filesystem.move( src, dst )  -- Moves (renames) a file within the `save/` directory.
  lurek.filesystem.newFileData( path )  -> FileData  -- Loads a file from the VFS into a FileData buffer.
  lurek.filesystem.openFile( path, mode )  -- Opens a file and returns a readable/writable file handle.
  lurek.filesystem.pollAsync( handle_id )  -- Polls an async load handle, returning status and optional data.
  lurek.filesystem.pollWatchers( )  -- Polls all watched paths and returns an array of paths that changed since the
  lurek.filesystem.read( path )  -- Reads a text file and returns its contents as a string.
  lurek.filesystem.readAsync( path )  -- Starts loading a file in the background and returns an opaque handle.
  lurek.filesystem.remove( path )  -- Permanently deletes a file or empty directory from the save directory.
  lurek.filesystem.removeDir( path )  -- Recursively deletes a directory and all its contents within `save/`.
  lurek.filesystem.setIdentity( name )  -- Sets the identity string that names the game's sandboxed save-data directory.
  lurek.filesystem.stat( path )  -- Returns lightweight file statistics for the given path.
  lurek.filesystem.toAbsolutePath( path )  -- Resolves a path relative to the game root to an absolute OS path string.
  lurek.filesystem.unmount( mp )  -- Removes a virtual mount layer by mountpoint.
  lurek.filesystem.unwatchPath( path )  -- Removes `path` from the polled file-watch list.  No-op if not watched.
  lurek.filesystem.watchPath( path )  -- Adds `path` to the polled file-watch list.
  lurek.filesystem.write( path, data )  -- Writes a string to a file in the save directory.
```

**`LFileData`** methods:

> Lua-side wrapper around a [`FileData`] buffer.

```lua
  LFileData:getFilename( )  -> Filename  -- Returns the virtual path this data was loaded from.
  LFileData:getSize( )  -> Size  -- Returns the file size in bytes.
  LFileData:getString( )  -> String  -- Returns the file content as a Lua string.
  LFileData:type( )  -- Returns the type name of this object.
  LFileData:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LFileHandle`** methods:

> Lua-side wrapper around a [`FileHandle`] with interior mutability.

```lua
  LFileHandle:close( )  -- Flushes any pending writes and closes the file handle.
  LFileHandle:flush( )  -- Flushes all buffered writes to disk without closing the handle.
  LFileHandle:getMode( )  -> Mode  -- Returns the access mode the file was opened with.
  LFileHandle:getSize( )  -> Size  -- Returns the size of the open file in bytes.
  LFileHandle:isEOF( )  -- Returns whether the read cursor has reached the end of the file.
  LFileHandle:read( [count] )  -- Reads bytes from the file, returning them as a string.
  LFileHandle:readLine( )  -- Reads the next line from the file without the trailing newline.
  LFileHandle:seek( pos )  -- Seeks the file position to the given byte offset from the start.
  LFileHandle:tell( )  -- Returns the current read/write byte offset from the start of the file.
  LFileHandle:type( )  -- Returns the type name of this object.
  LFileHandle:typeOf( name )  -- Returns true if this object is of the given type.
  LFileHandle:write( data )  -- Writes a string to the file and returns the number of bytes written.
```

**`LZipMount`** methods:

> Lua userdata wrapper around a [`ZipMount`].

```lua
  LZipMount:contains( virtual_path )  -- Returns true if `virtual_path` exists inside this ZIP mount.
  LZipMount:listFiles( )  -- Returns a sorted array of all virtual paths exposed by this ZIP mount.
  LZipMount:prefix( )  -- Returns the virtual path prefix this archive was mounted under.
  LZipMount:readFile( virtual_path )  -- Reads a file from the ZIP and returns it as a string of bytes.
  LZipMount:type( )  -- Returns the type name of this object.
  LZipMount:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.window {#window}

> Window management: size, title, fullscreen, cursor, display info.

```lua
  lurek.window.close( )  -- Requests the window to close.
  lurek.window.focus( )  -- Requests the window manager to bring the window to the foreground.
  lurek.window.fromPixels( value )  -- Converts physical pixels to device-independent coordinates.
  lurek.window.getDPIScale( )  -> DPI  -- Returns the DPI scaling factor for the window.
  lurek.window.getDesktopDimensions( )  -> DesktopDimensions  -- Returns the desktop resolution as width, height.
  lurek.window.getDimensions( )  -> Dimensions  -- Returns the window dimensions as width, height.
  lurek.window.getDisplayCount( )  -> DisplayCount  -- Returns the number of connected displays.
  lurek.window.getDisplayName( [display] )  -> DisplayName  -- Returns the name of the current display.
  lurek.window.getDisplayOrientation( )  -> DisplayOrientation  -- Returns the current display orientation.
  lurek.window.getFullscreen( )  -> Fullscreen  -- Returns the fullscreen state and type string.
  lurek.window.getFullscreenModes( )  -> FullscreenModes  -- Returns all available fullscreen video modes.
  lurek.window.getGameHeight( )  -> GameHeight  -- Returns the logical game height in virtual pixels.
  lurek.window.getGameWidth( )  -> GameWidth  -- Returns the logical game width in virtual pixels.
  lurek.window.getHeight( )  -> Height  -- Returns the window height in pixels.
  lurek.window.getMode( )  -> Mode  -- Returns the window dimensions and mode flags as width, height, flags.
  lurek.window.getNativeDPIScale( )  -> NativeDPIScale  -- Returns the native DPI scale factor.
  lurek.window.getPixelDimensions( )  -> PixelDimensions  -- Returns the window dimensions in physical pixels.
  lurek.window.getPosition( )  -> Position  -- Returns the window position as x, y in screen coordinates.
  lurek.window.getSafeArea( )  -> SafeArea  -- Returns the safe display area as x, y, w, h.
  lurek.window.getScaleInfo( )  -> ScaleInfo  -- Returns viewport scale and offset information as a table.
  lurek.window.getScaleMode( )  -> ScaleMode  -- Returns the current viewport scale mode string.
  lurek.window.getSystemTheme( )  -> OS  -- Returns the OS color theme preference.
  lurek.window.getTitle( )  -> Title  -- Returns the current window title.
  lurek.window.getVSync( )  -> VSync  -- Returns the current VSync mode integer.
  lurek.window.getWidth( )  -> Width  -- Returns the window width in pixels.
  lurek.window.hasFocus( )  -- Returns whether the window has keyboard focus.
  lurek.window.hasMouseFocus( )  -- Returns whether the mouse cursor is inside the window.
  lurek.window.isFullscreen( )  -- Returns whether the window is in fullscreen mode.
  lurek.window.isHighDPIAllowed( )  -- Returns whether high-DPI rendering is allowed.
  lurek.window.isMaximized( )  -- Returns whether the window is maximized.
  lurek.window.isMinimized( )  -- Returns whether the window is minimized.
  lurek.window.isOpen( )  -- Returns whether the window is open.
  lurek.window.isResizable( )  -- Returns whether the window can be resized by the user.
  lurek.window.isVisible( )  -- Returns whether the window is visible.
  lurek.window.maximize( )  -- Maximizes the window to fill the desktop.
  lurek.window.minimize( )  -- Minimizes the window to the taskbar.
  lurek.window.onDpiChange( func )  -- Registers a callback invoked (with the new scale factor) when the display
  lurek.window.openFileDialog( [opts] )  -- Opens a blocking native file-open dialog. Returns the chosen path string
  lurek.window.pollDpiChange( )  -- Polls for a pending DPI change event and returns the new scale factor if any.
  lurek.window.requestAttention( )  -- Flashes the window in the taskbar to request user attention.
  lurek.window.restore( )  -- Restores the window from minimized or maximized state.
  lurek.window.setFullscreen( enabled, [fstype] )  -- Enables or disables fullscreen mode.
  lurek.window.setIcon( path )  -- Sets the window icon from a file path.
  lurek.window.setMode( w, h, [flags] )  -- Resizes the window and optionally changes fullscreen and vsync.
  lurek.window.setPosition( x, y )  -- Moves the window to the given screen position.
  lurek.window.setScaleMode( mode )  -- Sets the viewport scale mode.
  lurek.window.setTitle( title )  -- Sets the window title bar text.
  lurek.window.setVSync( mode )  -- Sets the VSync mode (1=on, 0=off, -1=adaptive).
  lurek.window.showMessageBox( )  -- Shows a platform-native message box dialog.
  lurek.window.toPixels( value )  -- Converts a device-independent coordinate to physical pixels.
```

## lurek.event {#event}

> Event queue: push/poll custom events between systems.

```lua
  lurek.event.clear( )  -- Discards all pending events in the queue.
  lurek.event.clearHistory( )  -- Clears all recorded event history.
  lurek.event.enableHistory( capacity )  -- Enables event history recording, keeping the last `capacity` pushed events.
  lurek.event.exit( [code] )  -- Pushes an exit event, requesting the engine to stop.
  lurek.event.flushDeferred( )  -- Moves all buffered deferred events into the main event queue and clears the buffer.
  lurek.event.getHistory( )  -> History  -- Returns an array of recent events as `{name, args}` tables.
  lurek.event.newSignal( )  -> Signal  -- Creates a new pub-sub Signal dispatcher.
  lurek.event.poll( )  -- Returns an iterator function that pops events from the queue.
  lurek.event.pump( )  -- Syncs OS-level events into the queue (no-op in Lurek2D push model).
  lurek.event.push( ... )  -- Adds an event item to the end of the event queue for processing.
  lurek.event.pushDeferred( ... )  -- Pushes a named event to the deferred buffer; it will not reach the main queue
  lurek.event.quit( )  -- Alias for `exit()` â€” requests the engine to stop at the end of the current frame.
  lurek.event.restart( )  -- Requests that the engine restart at the beginning of the next frame.
  lurek.event.wait( [timeout] )  -- Blocks until the next event arrives or the optional timeout elapses.
```

**`LSignal`** methods:

> Lua-side wrapper around a [`Signal`] with registry-stored callbacks.

```lua
  LSignal:clear( name )  -- Removes all callbacks for the named event.
  LSignal:clearAll( )  -- Removes all callbacks across all events.
  LSignal:connect( name, func )  -- Subscribes to an event name or wildcard pattern. When the pattern contains
  LSignal:emit( ... )  -- Emits the named event, calling all registered callbacks with extra arguments.
  LSignal:getCount( name )  -> Count  -- Returns the callback count for the named event.
  LSignal:getTotalCount( )  -> TotalCount  -- Returns the total callback count across all events.
  LSignal:once( name, callback )  -- Registers a one-shot callback that fires at most once then auto-removes itself.
  LSignal:register( name, callback )  -- Registers a callback for the named event and returns its handle ID.
  LSignal:registerWithFilter( name, callback, filter )  -- Registers a callback with a filter predicate. The callback only fires if the
  LSignal:remove( handle )  -- Removes a subscription by handle ID.
  LSignal:type( )  -- Returns the type name of this object.
  LSignal:typeOf( name )  -- Returns true if the given type name matches this object's type or any parent type.
```

## lurek.system {#system}

> System info: OS, clipboard, locale, memory, Lurek2D version.

```lua
  lurek.system.errorSnapshot( msg )  -- Serialises an engine error message to a compact JSON string.
  lurek.system.getArch( )  -> CPU  -- Returns the CPU architecture string for the current machine.
  lurek.system.getArgs( )  -> Args  -- Returns the command-line arguments as a table.
  lurek.system.getBatchResults( results )  -> BatchResults  -- Returns the output table from the most recently completed runBatch call.
  lurek.system.getClipboardText( )  -> ClipboardText  -- Returns the current contents of the system clipboard.
  lurek.system.getDebugOverlay( )  -> DebugOverlay  -- Returns whether the debug overlay is currently visible.
  lurek.system.getEnv( name )  -> Env  -- Returns the value of an environment variable, or nil if not set.
  lurek.system.getInfo( )  -> Info  -- Returns a table of system information including OS name, CPU model, and installed RAM.
  lurek.system.getLastError( )  -> LastError  -- Returns the last unhandled error message, or nil.
  lurek.system.getLogLevel( )  -> LogLevel  -- Returns the name of the current minimum log level for runtime messages.
  lurek.system.getMemorySize( )  -> MemorySize  -- Returns the total amount of installed system RAM in megabytes.
  lurek.system.getMessage( id )  -> Message  -- Resolves a stable runtime message ID such as 'L001' to its human-readable text.
  lurek.system.getMessageCount( )  -> MessageCount  -- Returns the total number of message entries loaded into the runtime message catalog.
  lurek.system.getOS( )  -> OS  -- Returns the host operating system name ('Windows', 'Linux', 'macOS').
  lurek.system.getPowerInfo( )  -> PowerInfo  -- Returns battery state, percentage charged, and estimated time remaining.
  lurek.system.getPreferredLocales( )  -> PreferredLocales  -- Returns an ordered list of the user's preferred locale strings (e.g. 'en-US').
  lurek.system.getProcessorCount( )  -> ProcessorCount  -- Returns the number of logical CPU cores available.
  lurek.system.getVersion( )  -> Lurek2D  -- Returns the Lurek2D engine version string.
  lurek.system.hasMessage( id )  -- Returns true when the runtime message catalog contains the given stable message ID.
  lurek.system.log( level, message )  -- Emit a log message from Lua at the specified level.
  lurek.system.openURL( url )  -- Opens a URL in the system's default browser.
  lurek.system.parseArgs( [args] )  -- Parses a command-line argument string and returns a structured key/value table.
  lurek.system.runBatch( tasks, [opts] )  -- Runs a list of shell commands in parallel and returns immediately without blocking.
  lurek.system.setClipboardText( text )  -- Replaces the system clipboard contents with the given string.
  lurek.system.setDebugOverlay( enabled )  -- Shows or hides the FPS/draw-call debug overlay.
  lurek.system.setLogLevel( level )  -- Sets the minimum severity level for runtime log messages.
```

## lurek.thread {#thread}

> Background threads and inter-thread channels for Lua coroutines.

```lua
  lurek.thread.async( code, ... )  -- Starts a one-shot background computation and returns a Promise.
  lurek.thread.getChannel( name )  -> Channel  -- Gets or creates a named global channel shared across threads.
  lurek.thread.newChannel( )  -> Channel  -- Creates an unnamed thread-safe channel for inter-thread communication.
  lurek.thread.newPool( size, code )  -> Pool  -- Creates a thread pool of N workers all running the same Lua code.
  lurek.thread.newThread( code )  -> Thread  -- Creates a new background thread from a Lua code string.
```

**`LChannel`** methods:

> A synchronized message queue for cross-VM communication.

```lua
  LChannel:clear( )  -- Clears all items from the channel.
  LChannel:demand( [timeout] )  -- Blocks until a value is available or the timeout expires, then removes and returns it.
  LChannel:getCount( )  -> Count  -- Returns the number of items in the channel.
  LChannel:peek( )  -- Retrieves the value from the channel without removing it.
  LChannel:pop( )  -- Retrieves and removes a value from the channel.
  LChannel:popBytes( )  -- Pops a bytes value from the channel and returns it as a Lua string.
  LChannel:popTable( )  -- Pops a value from the channel expecting a table.
  LChannel:push( value )  -- Pushes a value to the channel.
  LChannel:pushBytes( data )  -- Pushes raw binary data (a Lua string treated as a byte array) to the channel.
  LChannel:pushTable( value )  -- Serializes a Lua table and pushes it to the channel.
  LChannel:supply( value )  -- Blocks until the channel has space, then adds the value.
  LChannel:type( )  -- Returns the type of the object.
  LChannel:typeOf( name )  -- Checks if the object is of the specified type.
```

**`LPromise`** methods:

> Lua-side wrapper around a one-shot [`Promise`].

```lua
  LPromise:getError( )  -> Error  -- Returns the worker error string if the promise failed, otherwise nil.
  LPromise:isDone( )  -- Returns true if the promise has a result or has errored (non-blocking).
  LPromise:result( )  -- Pops and returns the promise result, or nil if not yet ready.
  LPromise:type( )  -- Returns the type name of this object.
  LPromise:typeOf( name )  -- Returns whether this object is of the given type.
```

**`LThread`** methods:

> Lua-side wrapper around a background [`LuaThread`].

```lua
  LThread:getError( )  -> Error  -- Returns the error message if the thread failed, or nil.
  LThread:isRunning( )  -- Returns whether the thread is currently executing.
  LThread:start( ... )  -- Launches the background thread, passing optional arguments via varargs.
  LThread:type( )  -- Returns the type name of this object.
  LThread:typeOf( name )  -- Returns whether this object is of the given type.
  LThread:wait( )  -- Blocks the calling thread until the background thread finishes.
```

**`LThreadPool`** methods:

> Lua-side wrapper around a [`ThreadPool`].

```lua
  LThreadPool:collect( )  -- Retrieves the next result from the pool's output channel (non-blocking).
  LThreadPool:getInputChannel( )  -> InputChannel  -- Returns the shared input Channel (main â†’ workers).
  LThreadPool:getOutputChannel( )  -> OutputChannel  -- Returns the shared output Channel (workers â†’ main).
  LThreadPool:join( )  -- Blocks until all workers in the pool have finished execution.
  LThreadPool:size( )  -- Returns the number of workers in this pool.
  LThreadPool:submit( value )  -- Submits a value to the pool's input channel for processing by a worker.
  LThreadPool:type( )  -- Returns the type name of this object.
  LThreadPool:typeOf( name )  -- Returns whether this object is of the given type.
```

## lurek.particle {#particle}

> CPU particle system: emitters, modifiers, rendering.

```lua
  lurek.particle.fromTOML( path )  -- Creates a new particle system from a TOML config file.
  lurek.particle.newSystem( [config] )  -> System  -- Creates a new particle system and stores it in the engine pool.
  lurek.particle.newTrail( lifetime, start_width )  -> Trail  -- Creates a new trail ribbon effect.
```

**`LParticleSystem`** methods:

> Lua-side handle to a particle system stored in SharedState.

```lua
  LParticleSystem:addAttractor( x, y, strength, radius )  -- Adds a gravity well that pulls (positive strength) or repels
  LParticleSystem:addSubEmitter( config_tbl, [burst_count] )  -- Attaches a sub-emitter that bursts when a particle dies.
  LParticleSystem:addSubSystem( config_tbl )  -- Adds a child emitter that updates and renders with this system.
  LParticleSystem:clearAttractors( )  -- Removes all attractors from this particle system.
  LParticleSystem:clearBounds( )  -- Removes the bounding rectangle so particles can move freely.
  LParticleSystem:clone( )  -- Creates a copy of this particle system (config only, no live particles).
  LParticleSystem:count( )  -- Returns the number of living particles.
  LParticleSystem:drawToImage( w, h )  -- Renders all live particles to a CPU ImageData.
  LParticleSystem:emit( count )  -- Emits a burst of the given number of particles.
  LParticleSystem:getAttractorCount( )  -> AttractorCount  -- Returns the number of attractors currently registered on this system.
  LParticleSystem:getBufferSize( )  -> BufferSize  -- Returns the maximum particle count.
  LParticleSystem:getColors( )  -> Colors  -- Returns color keyframes as a table of {r,g,b,a} tables.
  LParticleSystem:getCount( )  -> Count  -- Returns the number of living particles (alias for count).
  LParticleSystem:getDirection( )  -> Direction  -- Returns emission direction in radians.
  LParticleSystem:getEmissionArea( )  -> EmissionArea  -- Returns emission area: dist-string, w, h.
  LParticleSystem:getEmissionRate( )  -> EmissionRate  -- Returns particles emitted per second.
  LParticleSystem:getEmitterLifetime( )  -> EmitterLifetime  -- Returns the emitter lifetime.
  LParticleSystem:getFlipbook( )  -> Flipbook  -- Returns the current flipbook configuration as `(cols, rows, fps)`, or `nil` if not set.
  LParticleSystem:getGravity( )  -> Gravity  -- Returns the gravity acceleration applied to particles as two numbers `gx, gy`.
  LParticleSystem:getInsertMode( )  -> InsertMode  -- Returns the insert mode as a string.
  LParticleSystem:getLinearAcceleration( )  -> LinearAcceleration  -- Returns linear acceleration range.
  LParticleSystem:getLinearDamping( )  -> LinearDamping  -- Returns linear damping range.
  LParticleSystem:getOffset( )  -> Offset  -- Returns the render origin offset.
  LParticleSystem:getParticleLifetime( )  -> ParticleLifetime  -- Returns min and max particle lifetime.
  LParticleSystem:getPosition( )  -> Position  -- Returns the emitter world position.
  LParticleSystem:getRadialAcceleration( )  -> RadialAcceleration  -- Returns radial acceleration range.
  LParticleSystem:getRotation( )  -> Rotation  -- Returns initial rotation range.
  LParticleSystem:getShape( )  -> Shape  -- Returns the particle draw shape as a string.
  LParticleSystem:getSizeVariation( )  -> SizeVariation  -- Returns the maximum random size variation applied to newly emitted particles.
  LParticleSystem:getSizes( )  -> Sizes  -- Returns size keyframes as a Lua table.
  LParticleSystem:getSpeed( )  -> Speed  -- Returns min/max initial speed.
  LParticleSystem:getSpin( )  -> Spin  -- Returns angular velocity range.
  LParticleSystem:getSpinVariation( )  -> SpinVariation  -- Returns the maximum random angular velocity variation for new particles.
  LParticleSystem:getSpread( )  -> Spread  -- Returns the half-angle spread in radians for the emission cone.
  LParticleSystem:getTangentialAcceleration( )  -> TangentialAcceleration  -- Returns tangential acceleration range.
  LParticleSystem:hasRelativeRotation( )  -- Returns whether relative rotation is enabled.
  LParticleSystem:isActive( )  -- Returns true if the emitter is currently emitting or has live particles.
  LParticleSystem:isEmpty( )  -- Returns true if there are no live particles.
  LParticleSystem:isFull( )  -- Returns true if the system has reached max_particles.
  LParticleSystem:isPaused( )  -- Returns true if the emitter is paused.
  LParticleSystem:isStopped( )  -- Returns true if the emitter is stopped.
  LParticleSystem:moveTo( x, y )  -- Moves the emitter to the given world position.
  LParticleSystem:pause( )  -- Pauses particle emission; existing particles continue to simulate.
  LParticleSystem:release( )  -- Removes the particle system from the engine, freeing its slot.
  LParticleSystem:render( [ox], [oy] )  -- Renders all live particles to the GPU command queue.
  LParticleSystem:reset( )  -- Removes all particles and resets the emitter.
  LParticleSystem:resume( )  -- Resumes a paused emitter.
  LParticleSystem:setBounds( xmin, xmax, ymin, ymax, restitution )  -- Constrains all particles to an axis-aligned bounding rectangle.
  LParticleSystem:setBufferSize( n )  -- Sets the maximum number of particles (resizes the pool).
  LParticleSystem:setColors( ... )  -- Sets color keyframes. Each arg is a table {r, g, b, a}.
  LParticleSystem:setCustomEmissionShape( cb )  -- Sets a Lua function that returns (offset_x, offset_y) for each newly spawned
  LParticleSystem:setDirection( dir )  -- Sets emission direction in radians.
  LParticleSystem:setEmissionArea( dist, w, h, [angle], [dir_rel] )  -- Sets emission area distribution and size.
  LParticleSystem:setEmissionRate( rate )  -- Sets particles emitted per second.
  LParticleSystem:setEmitterLifetime( t )  -- Sets how long the emitter runs before auto-stopping. Negative = infinite.
  LParticleSystem:setFlipbook( cols, rows, fps )  -- Configures sprite-sheet flipbook animation by dividing the texture into a grid.
  LParticleSystem:setGravity( gx, gy )  -- Sets the gravity acceleration applied to all active particles each frame.
  LParticleSystem:setInsertMode( mode )  -- Sets the insert mode: "top", "bottom", or "random".
  LParticleSystem:setLinearAcceleration( xmin, ymin, xmax, ymax )  -- Sets linear acceleration range.
  LParticleSystem:setLinearDamping( min, max )  -- Sets linear damping range.
  LParticleSystem:setOffset( ox, oy )  -- Sets the render origin offset.
  LParticleSystem:setOnDeathBatch( cb )  -- Sets a Lua function called after each update() with all particles that died
  LParticleSystem:setParticleLifetime( min, max )  -- Sets min and max particle lifetime in seconds.
  LParticleSystem:setPosition( x, y )  -- Sets the emitter world position.
  LParticleSystem:setRadialAcceleration( min, max )  -- Sets radial acceleration range.
  LParticleSystem:setRelativeRotation( v )  -- Sets whether particle rotation follows velocity direction.
  LParticleSystem:setRotation( min, max )  -- Sets initial rotation range in radians.
  LParticleSystem:setShape( shape )  -- Sets the particle draw shape.
  LParticleSystem:setSizeVariation( v )  -- Sets size variation (0â€“1).
  LParticleSystem:setSizes( ... )  -- Sets size keyframes (varargs: each number is one keyframe).
  LParticleSystem:setSpeed( min, max )  -- Sets min/max initial speed.
  LParticleSystem:setSpin( min, max )  -- Sets angular velocity range.
  LParticleSystem:setSpinVariation( v )  -- Sets spin variation (0â€“1).
  LParticleSystem:setSpread( spread )  -- Sets emission spread (half-angle cone) in radians.
  LParticleSystem:setTangentialAcceleration( min, max )  -- Sets tangential acceleration range.
  LParticleSystem:start( )  -- Starts or restarts particle emission.
  LParticleSystem:stop( )  -- Stops particle emission immediately.
  LParticleSystem:subSystemCount( )  -- Returns the number of direct child sub-systems attached to this emitter.
  LParticleSystem:toImage( w, h )  -- Alias for `drawToImage`. Renders all live particles to a CPU ImageData.
  LParticleSystem:type( )  -- Returns the type name "ParticleSystem".
  LParticleSystem:typeOf( name )  -- Returns true if this matches the given type name.
  LParticleSystem:update( dt )  -- Advances the particle simulation by dt seconds.
  LParticleSystem:warmUp( seconds )  -- Pre-simulates the particle system for `seconds` so it appears fully
```

**`LTrail`** methods:

> Lua-side wrapper around a [`Trail`] ribbon effect.

```lua
  LTrail:clear( )  -- Removes all trail points.
  LTrail:drawToImage( w, h )  -- Renders the trail ribbon to a CPU ImageData.
  LTrail:getLifetime( )  -> Lifetime  -- Returns the trail point lifetime in seconds.
  LTrail:getPointCount( )  -> PointCount  -- Returns the number of active trail points.
  LTrail:getWidth( )  -> Width  -- Returns the start and end width.
  LTrail:pushPoint( x, y )  -- Appends a new point to the trail head.
  LTrail:setHeadColor( r, g, b, a )  -- Sets the colour at the newest end of the trail.
  LTrail:setLifetime( lifetime )  -- Sets how long each trail point persists in seconds.
  LTrail:setMinDistance( distance )  -- Sets the minimum distance between trail points.
  LTrail:setTailColor( r, g, b, a )  -- Sets the colour at the oldest end of the trail.
  LTrail:setWidth( start, [end] )  -- Sets the start and end width of the trail ribbon.
  LTrail:type( )  -- Returns the type name of this object.
  LTrail:typeOf( name )  -- Returns true if this object is of the given type.
  LTrail:update( dt )  -- Ages trail points and removes expired ones.
```

## lurek.tilemap {#tilemap}

> Tile maps: chunk loading, tile queries, navigation grid.

```lua
  lurek.tilemap.fromLDtk( json_str, [level_name] )  -- Parses an LDtk JSON export string and returns a TileMap.
  lurek.tilemap.fromScreenHex( sx, sy, size )  -- Converts screen position back to axial hex coordinates (pointy-top layout).
  lurek.tilemap.fromScreenIso( sx, sy, tw, th )  -- Converts screen position back to tile coordinates for diamond isometric projection.
  lurek.tilemap.hexArea( q, r, radius )  -- Returns all hex cells within radius distance (filled hex circle) as a table.
  lurek.tilemap.hexDistance( q1, r1, q2, r2 )  -- Returns the hex distance between two axial coordinates.
  lurek.tilemap.hexLine( q1, r1, q2, r2 )  -- Returns all hex cells along a line between two axial coordinates as a table.
  lurek.tilemap.hexNeighbors( q, r )  -- Returns the six axial neighbor coordinates as a table of {q, r} pairs.
  lurek.tilemap.hexReflect( q, r, center_q, center_r, axis )  -- Reflects hex coordinates across an axis through the center.
  lurek.tilemap.hexRing( q, r, radius )  -- Returns all cells at exactly radius distance from (q, r) as a table.
  lurek.tilemap.hexRotate( q, r, center_q, center_r, steps )  -- Rotates hex coordinates around a center by steps x 60 degrees clockwise.
  lurek.tilemap.hexRound( q, r )  -- Rounds fractional axial coordinates to the nearest hex cell.
  lurek.tilemap.hexSpiral( q, r, radius )  -- Returns all hex cells from center outward to radius, ring by ring, as a table.
  lurek.tilemap.isoDirectionFromAngle( angle )  -- Snaps an angle (in radians) to the nearest isometric direction (1-4).
  lurek.tilemap.isoDirectionName( direction )  -- Returns the name of an isometric direction (1-4).
  lurek.tilemap.isoRotate( direction, steps )  -- Rotates an isometric direction (1-4) clockwise by steps.
  lurek.tilemap.loadTMX( xml )  -- Parses a TMX XML string and returns a table with map metadata and layers.
  lurek.tilemap.newAutoTileSheet( tile_w, tile_h, layout_str )  -> AutoTileSheet  -- Creates a new AutoTileSheet with the given tile dimensions and layout.
  lurek.tilemap.newChunkMap( [chunk_size] )  -> ChunkMap  -- Creates a new ChunkMap with the given chunk size.
  lurek.tilemap.newIsoMap( )  -> IsoMap  -- Creates a new IsoMap with no levels.
  lurek.tilemap.newLargeMapRenderer( tile_w, tile_h )  -> LargeMapRenderer  -- Creates a LargeMapRenderer for chunk-level occlusion culling on maps > 200Ă—200 tiles.
  lurek.tilemap.newMapBlock( width, height, [layers], [segment_size] )  -> MapBlock  -- Creates a new MapBlock with the given dimensions.
  lurek.tilemap.newMapGen( )  -> MapGen  -- Creates a MapGen from a MapGroup, a preset name or dimensions, and a segment size.
  lurek.tilemap.newMapGroup( name )  -> MapGroup  -- Creates a new empty MapGroup with the given name.
  lurek.tilemap.newMapScript( )  -> MapScript  -- Creates a new empty MapScript procedural generation script.
  lurek.tilemap.newTileMap( tile_width, tile_height, [chunk_size] )  -> TileMap  -- Creates a new TileMap with the given tile size and chunk size.
  lurek.tilemap.newTileSet( )  -> TileSet  -- Creates a new TileSet with the given atlas layout parameters.
  lurek.tilemap.toScreenHex( q, r, size )  -- Converts axial hex coordinates to screen position (pointy-top layout).
  lurek.tilemap.toScreenIso( tx, ty, tw, th )  -- Converts tile coordinates to screen position using diamond isometric projection.
```

**`LAutoTileSheet`** methods:

> Lua-side wrapper around an [`AutoTileSheet`].

```lua
  LAutoTileSheet:applyToTileSet( ts_ud, type_name, [start_gid] )  -- Applies autotile rules from this sheet to a TileSet.
  LAutoTileSheet:getBitmaskForTile( tile_id )  -> BitmaskForTile  -- Returns the bitmask value associated with a 1-based local tile ID.
  LAutoTileSheet:getLayout( )  -> Layout  -- Returns the layout variant as a string.
  LAutoTileSheet:getQuad( tile_id )  -> Quad  -- Returns the atlas region rectangle for the 1-based tile ID.
  LAutoTileSheet:getTileCount( )  -> TileCount  -- Returns the number of tiles in this sheet.
  LAutoTileSheet:getTileForBitmask( bitmask )  -> TileForBitmask  -- Returns the 1-based tile ID for a given bitmask, or nil.
  LAutoTileSheet:getTileHeight( )  -> TileHeight  -- Returns the tile height in pixels.
  LAutoTileSheet:getTileWidth( )  -> TileWidth  -- Returns the tile width in pixels.
  LAutoTileSheet:type( )  -- Returns the type name of this object.
  LAutoTileSheet:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LChunkMap`** methods:

> Lua-side wrapper around a [`ChunkMap`].

```lua
  LChunkMap:chunkTileRange( cx, cy )  -- Returns the tile coordinate range for chunk (cx, cy) as (x0, y0, x1, y1).
  LChunkMap:clearTile( x, y )  -- Clears the tile at (x, y) by setting its GID to 0.
  LChunkMap:fillRect( x0, y0, x1, y1, gid )  -- Fills the rectangular tile region with a GID.
  LChunkMap:getChunkSize( )  -> ChunkSize  -- Returns the chunk size (tiles per side).
  LChunkMap:getChunksInView( vx, vy, vw, vh, tw, th )  -> ChunksInView  -- Returns chunk coordinates whose world-pixel footprint overlaps the given viewport.
  LChunkMap:getLoadedChunks( )  -> LoadedChunks  -- Returns a table of all currently loaded chunk coordinates as {{cx, cy}, ...}.
  LChunkMap:getTile( x, y )  -> GID  -- Returns the GID at tile coordinate (x, y).
  LChunkMap:loadChunk( cx, cy )  -- Pre-allocates the chunk at chunk coordinates (cx, cy).
  LChunkMap:setTile( x, y, gid )  -- Sets the GID at tile coordinate (x, y).
  LChunkMap:type( )  -- Returns the type name of this object.
  LChunkMap:typeOf( name )  -- Returns true if this object is of the given type.
  LChunkMap:unloadChunk( cx, cy )  -- Removes the chunk at chunk coordinates (cx, cy) from memory.
```

**`LIsoMap`** methods:

> Lua-side wrapper around an [`IsoMap`].

```lua
  LIsoMap:addLevel( )  -- Appends a new empty Z-level and returns its 1-based index.
  LIsoMap:fillLevel( z, part, gid )  -- Fills every cell in level z with gid for the given part (1-based z; 0-based part).
  LIsoMap:getHeight( )  -> Height  -- Returns the map height in tiles.
  LIsoMap:getLevelCount( )  -> LevelCount  -- Returns the number of Z-levels currently in the map.
  LIsoMap:getLevelHeight( )  -> LevelHeight  -- Returns the vertical pixel offset between consecutive Z-levels.
  LIsoMap:getPartCount( )  -> PartCount  -- Returns the number of GID slots per tile.
  LIsoMap:getPartOrder( )  -> PartOrder  -- Returns the current draw-order array (0-based part slot indices).
  LIsoMap:getTileHeight( )  -> TileHeight  -- Returns the tile footprint height in pixels.
  LIsoMap:getTilePart( z, x, y, part )  -> TilePart  -- Reads the GID in the part slot of tile (x, y) on level z (1-based z, x, y; 0-based part).
  LIsoMap:getTileWidth( )  -> TileWidth  -- Returns the tile footprint width in pixels.
  LIsoMap:getWidth( )  -> Width  -- Returns the map width in tiles.
  LIsoMap:isLevelVisible( z )  -- Returns the visibility of a level (1-based z).
  LIsoMap:screenToTile( sx, sy )  -- Converts screen pixel coordinates to isometric tile coordinates at Z-level 0.
  LIsoMap:setLevelVisible( z, visible )  -- Sets the visibility of a level (1-based z).
  LIsoMap:setOrigin( x, y )  -- Sets the screen pixel origin.
  LIsoMap:setPartOrder( order )  -- Overrides the draw order for this IsoMap. Length must equal partCount.
  LIsoMap:setTilePart( z, x, y, part, gid )  -- Writes a GID into the part slot of tile (x, y) on level z (1-based z, x, y; 0-based part).
  LIsoMap:tileToScreen( tx, ty, tz )  -- Projects isometric tile coordinates (tx, ty, tz) to screen pixels.
  LIsoMap:type( )  -- Returns the type name of this object.
  LIsoMap:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LLargeMapRenderer`** methods:

> Lua-side wrapper around a [`LargeMapRenderer`] for chunk-level occlusion culling on large worlds.

```lua
  LLargeMapRenderer:getChunkSize( )  -> ChunkSize  -- Returns the current chunk size.
  LLargeMapRenderer:getMapSize( )  -> MapSize  -- Returns the map dimensions as (width, height) in tiles.
  LLargeMapRenderer:getTile( x, y )  -> Tile  -- Returns the tile ID at (x, y), or nil if out of bounds.
  LLargeMapRenderer:getTilesetColumns( )  -> TilesetColumns  -- Returns the number of tileset atlas columns.
  LLargeMapRenderer:getTotalChunks( )  -> TotalChunks  -- Returns the total number of chunks that cover the loaded map.
  LLargeMapRenderer:getVisibleChunks( )  -> VisibleChunks  -- Returns the number of chunks currently within the camera viewport.
  LLargeMapRenderer:invalidateAll( )  -- Marks every chunk as dirty.
  LLargeMapRenderer:invalidateChunk( cx, cy )  -- Marks a chunk at chunk-grid coordinates (cx, cy) as dirty,
  LLargeMapRenderer:isLodEnabled( )  -- Returns whether LOD rendering is currently enabled.
  LLargeMapRenderer:setCamera( x, y, zoom )  -- Updates the camera position and zoom used for visibility culling.
  LLargeMapRenderer:setChunkSize( size )  -- Sets the chunk size used for culling (default 16).
  LLargeMapRenderer:setLodEnabled( enabled )  -- Enables or disables level-of-detail rendering for distant chunks.
  LLargeMapRenderer:setLodThresholds( levels )  -- Sets the distance thresholds (in tile units) at which each LOD level activates.
  LLargeMapRenderer:setMapData( data, width, height )  -- Loads a flat array of tile IDs (row-major) covering width Ă— height tiles.
  LLargeMapRenderer:setTile( x, y, tile_id )  -- Sets a single tile ID at (x, y).  Coordinates are 0-based.
  LLargeMapRenderer:setTilesetColumns( cols )  -- Sets the number of tile columns in the atlas texture used for UV calculation.
  LLargeMapRenderer:setViewport( w, h )  -- Sets the viewport dimensions in pixels used for visibility culling.
  LLargeMapRenderer:type( )  -- Returns the type name of this object.
  LLargeMapRenderer:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LMapBlock`** methods:

> Lua-side wrapper around a [`MapBlock`].

```lua
  LMapBlock:getDimensions( )  -> Dimensions  -- Returns the block dimensions as (width, height) in tiles.
  LMapBlock:getHeight( )  -> Height  -- Returns the block height in tiles.
  LMapBlock:getHeightInSegments( )  -> HeightInSegments  -- Returns the number of segments along the height.
  LMapBlock:getLayerCount( )  -> LayerCount  -- Returns the number of layers in this block.
  LMapBlock:getName( )  -> Name  -- Returns the name of this block.
  LMapBlock:getSegmentSize( )  -> SegmentSize  -- Returns the segment size in tiles.
  LMapBlock:getSide( edge_str, segment )  -> Side  -- Returns the side connection ID for a segment on a given edge.
  LMapBlock:getTile( layer, x, y )  -> GID  -- Returns the GID of the tile at (x, y) on the given layer (1-based).
  LMapBlock:getWeight( )  -> Weight  -- Returns the placement weight.
  LMapBlock:getWidth( )  -> Width  -- Returns the block width in tiles.
  LMapBlock:getWidthInSegments( )  -> WidthInSegments  -- Returns the number of segments along the width.
  LMapBlock:setName( name )  -- Sets the human-readable name of this block.
  LMapBlock:setSide( edge_str, segment, side_id )  -- Sets the side connection ID for a segment on a given edge.
  LMapBlock:setTile( layer, x, y, gid )  -- Sets the GID of a tile at (x, y) on the given layer (1-based).
  LMapBlock:setWeight( weight )  -- Sets the placement weight.
  LMapBlock:type( )  -- Returns the type name of this object.
  LMapBlock:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LMapGen`** methods:

> Lua-side wrapper for a map generator (size preset or explicit dimensions).

```lua
  LMapGen:generate( [script_idx], [seed], [layer_name] )  -- Generates a TileMap using the group's blocks and an optional script index, seed, and la...
  LMapGen:type( )  -- Returns the type name of this object.
  LMapGen:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LMapGroup`** methods:

> Lua-side wrapper around a [`MapGroup`].

```lua
  LMapGroup:addBlock( block_ud )  -- Adds a block to this group.
  LMapGroup:addScript( script_ud )  -- Adds a MapScript to this group.
  LMapGroup:getBlockCount( )  -> BlockCount  -- Returns the number of blocks in this group.
  LMapGroup:getName( )  -> Name  -- Returns the name of this group.
  LMapGroup:getScriptCount( )  -> ScriptCount  -- Returns the number of scripts in this group.
  LMapGroup:removeBlock( idx )  -- Removes a block by 1-based index.
  LMapGroup:type( )  -- Returns the type name of this object.
  LMapGroup:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LMapScript`** methods:

> Lua-side wrapper around a [`MapScript`] procedural generation script.

```lua
  LMapScript:addStep( step_def )  -- Appends a generation step from a step-definition table.
  LMapScript:getStepCount( )  -> StepCount  -- Returns the number of steps in this script.
  LMapScript:type( )  -- Returns the type name of this object.
  LMapScript:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LTileMap`** methods:

> Lua-side wrapper around a [`TileMap`].

```lua
  LTileMap:addLayer( name, w, h )  -- Adds a new empty layer and returns its 1-based index.
  LTileMap:addTileSet( ts_ud )  -- Adds a tileset to this map.
  LTileMap:applyAutoTile( layer, type_name )  -- Applies 4-bit cardinal autotile rules to every tile on layer (1-based).
  LTileMap:applyAutoTile8( layer, type_name )  -- Applies 8-bit directional autotile rules to every tile on layer (1-based).
  LTileMap:applyAutoTile8At( layer, x, y, type_name )  -- Applies 8-bit directional autotile at a single cell and its 3x3 neighborhood (1-based).
  LTileMap:applyAutoTileAt( layer, x, y, type_name )  -- Applies 4-bit cardinal autotile at a single cell and its 3x3 neighborhood (1-based).
  LTileMap:checkEntities( layer, entities )  -- Checks a list of entity positions against registered tile callbacks and fires matches.
  LTileMap:clearTile( layer, x, y )  -- Clears a tile (sets GID to 0) at (x, y) on the given layer (1-based).
  LTileMap:drawToImage( tile_size )  -- Renders the tile map to a CPU ImageData using the given tile pixel size.
  LTileMap:fill( layer, gid )  -- Fills an entire layer with the given GID (1-based layer).
  LTileMap:fireTileExit( gid, entity, tx, ty )  -- Fire the tile exit callback for the given GID (call when entity leaves tile).
  LTileMap:fireTileStep( gid, entity, tx, ty )  -- Fire the tile step callback for the given GID (call each frame while entity is on tile).
  LTileMap:getChunkSize( )  -> ChunkSize  -- Returns the chunk size used for spatial partitioning.
  LTileMap:getLayerColor( idx )  -> RGBA  -- Returns the RGBA tint color of a layer.
  LTileMap:getLayerCount( )  -> LayerCount  -- Returns the number of layers.
  LTileMap:getLayerName( idx )  -> LayerName  -- Returns the name of a layer by 1-based index.
  LTileMap:getLayerOffset( idx )  -> LayerOffset  -- Returns the pixel offset of a layer.
  LTileMap:getLayerParallax( idx )  -> LayerParallax  -- Returns the parallax factor of a layer.
  LTileMap:getLayerVisible( idx )  -> LayerVisible  -- Returns layer visibility.
  LTileMap:getOrientation( )  -> Orientation  -- Returns the map orientation as a string ("topdown", "sideview", "isometric", or "hexago...
  LTileMap:getTile( layer, x, y )  -> GID  -- Returns the GID at (x, y) on the given layer (1-based).
  LTileMap:getTileDimensions( )  -> TileDimensions  -- Returns tile dimensions as (width, height).
  LTileMap:getTileHeight( )  -> TileHeight  -- Returns the tile height in pixels.
  LTileMap:getTileSet( idx )  -> TileSet  -- Returns a tileset by 1-based index, or nil if out of range.
  LTileMap:getTileSetCount( )  -> TileSetCount  -- Returns the number of tilesets attached to this map.
  LTileMap:getTileWidth( )  -> TileWidth  -- Returns the tile width in pixels.
  LTileMap:getViewport( )  -> Viewport  -- Returns the viewport as (x, y, w, h) or nil if not set.
  LTileMap:isSolid( layer, x, y )  -- Returns true if the tile at (x, y) on layer is solid (1-based).
  LTileMap:onTileEnter( gid, func )  -- Registers a callback fired when any entity's tile GID matches `gid`.
  LTileMap:onTileExit( gid, func )  -- Register a callback for when an entity exits a tile with the given GID.
  LTileMap:onTileStep( gid, func )  -- Register a callback for when an entity steps on a tile with the given GID.
  LTileMap:rectOverlapsSolid( layer, x, y, w, h )  -- Returns true if any solid tile overlaps the given world-space rectangle on layer (1-bas...
  LTileMap:render( [ox], [oy] )  -- Renders the tile map to the screen at the given offset.
  LTileMap:setLayerColor( idx, r, g, b, a )  -- Sets the RGBA tint color for a layer.
  LTileMap:setLayerOffset( idx, ox, oy )  -- Sets the pixel offset for a layer.
  LTileMap:setLayerParallax( idx, px, py )  -- Sets the parallax scrolling factor for a layer.
  LTileMap:setLayerVisible( idx, visible )  -- Shows or hides a tile layer by its 1-based index.
  LTileMap:setOrientation( orientation )  -- Sets the map orientation from a string ("topdown", "sideview", "isometric", or "hexagon...
  LTileMap:setTile( layer, x, y, gid )  -- Sets the GID of a tile at (x, y) on the given layer (1-based).
  LTileMap:setTileTint( layer, x, y, r, g, b, a )  -- Sets a per-tile RGBA tint override (1-based layer, x, y).
  LTileMap:setViewport( x, y, w, h )  -- Sets the viewport rectangle for rendering culling.
  LTileMap:sweepRect( layer, x, y, w, h, dx, dy )  -- Performs a swept AABB collision test against solid tiles on layer (1-based).
  LTileMap:tileToWorld( tx, ty )  -- Converts tile coordinates to world pixel coordinates (1-based input).
  LTileMap:toNavGrid( layer, gids_tbl )  -- Converts the given layer into a 2D navigation grid.
  LTileMap:type( )  -- Returns the type name of this object.
  LTileMap:typeOf( name )  -- Returns true if this object is of the given type.
  LTileMap:update( dt )  -- Advances tile animation timers by dt seconds.
  LTileMap:worldToTile( wx, wy )  -- Converts world pixel coordinates to tile coordinates.
```

**`LTileSet`** methods:

> Lua-side wrapper around a [`TileSet`].

```lua
  LTileSet:getAnimation( tile_id )  -> Animation  -- Returns the animation frames for a 1-based local tile ID as a table of {tileid, duratio...
  LTileSet:getAutoTileId( type_name, bitmask )  -> AutoTileId  -- Looks up the 1-based local tile ID for a 4-bit cardinal autotile bitmask, or nil.
  LTileSet:getAutoTileId8( type_name, bitmask )  -> AutoTileId8  -- Looks up the 1-based local tile ID for an 8-bit directional autotile bitmask, or nil.
  LTileSet:getColumns( )  -> Columns  -- Returns the number of tile columns in the atlas texture.
  LTileSet:getFirstGid( )  -> FirstGid  -- Returns the first global ID assigned to this tileset.
  LTileSet:getMargin( )  -> Margin  -- Returns the margin in pixels around the edges of the atlas.
  LTileSet:getQuad( tile_id )  -> Quad  -- Computes the atlas source rectangle for a 1-based local tile ID.
  LTileSet:getSpacing( )  -> Spacing  -- Returns the spacing in pixels between tiles in the atlas.
  LTileSet:getTileCount( )  -> TileCount  -- Returns the total number of tiles in this tileset.
  LTileSet:getTileDimensions( )  -> TileDimensions  -- Returns the tile dimensions as (width, height).
  LTileSet:getTileHeight( )  -> TileHeight  -- Returns the height of a single tile in pixels.
  LTileSet:getTileWidth( )  -> TileWidth  -- Returns the width of a single tile in pixels.
  LTileSet:isSolid( tile_id )  -- Returns whether a 1-based local tile ID is solid.
  LTileSet:setAnimation( tile_id, frames )  -- Sets the animation frames for a 1-based local tile ID from a table of {tileid, duration}.
  LTileSet:setAutoTileRule( type_name, bitmask, tile_id )  -- Registers a 4-bit cardinal autotile rule. tileId is 1-based.
  LTileSet:setAutoTileRule8( type_name, bitmask, tile_id )  -- Registers an 8-bit directional autotile rule. tileId is 1-based.
  LTileSet:setSolid( tile_id, solid )  -- Sets whether a 1-based local tile ID is solid for collision purposes.
  LTileSet:type( )  -- Returns the type name of this object.
  LTileSet:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.scene {#scene}

> Depth-sorted scene management for 2D layers.

```lua
  lurek.scene.clear( )  -- Clears all scenes from the stack, calling leave on each.
  lurek.scene.define( [def] )  -- Creates a reusable scene class â€” returns a zero-argument constructor function.
  lurek.scene.depth( )  -- Returns the number of scenes on the stack.
  lurek.scene.deserializeScene( snapshot )  -- Restores scene data_refs from a snapshot produced by serializeScene().
  lurek.scene.draw( )  -- Draws all scenes in the stack from bottom to top (legacy name; prefer `render`).
  lurek.scene.fade( [duration] )  -- Returns a fade cross-dissolve transition config table.
  lurek.scene.getActiveScenes( )  -> ActiveScenes  -- Returns a table array of all active scene tables.
  lurek.scene.getCurrent( )  -> Current  -- Returns the current top scene table, or nil if the stack is empty.
  lurek.scene.getData( key )  -> Data  -- Returns a value from the inter-scene data store, or nil if not found.
  lurek.scene.getRegistered( name )  -> Registered  -- Returns a registered scene table by name, or nil if not found.
  lurek.scene.getRegisteredNames( )  -> RegisteredNames  -- Returns a list of all registered scene names.
  lurek.scene.getStackSize( )  -> StackSize  -- Returns the number of scenes on the stack.
  lurek.scene.getTransitionProgress( )  -> TransitionProgress  -- Returns the transition progress from 0.0 to 1.0.
  lurek.scene.getTransitionProgressEased( )  -> TransitionProgressEased  -- Returns the easing-adjusted transition progress from 0.0 to 1.0.
  lurek.scene.getTransitionTypes( )  -> TransitionTypes  -- Returns a table listing all supported transition type strings.
  lurek.scene.hasData( key )  -- Returns true if the given key exists in the data store.
  lurek.scene.hasRegistered( name )  -- Returns true if a scene is registered under the given name.
  lurek.scene.iris( [duration] )  -- Returns an iris in/out (circular reveal) transition config table.
  lurek.scene.isEmpty( )  -- Returns true if the scene stack is empty.
  lurek.scene.isOverlay( )  -- Returns true if the current top scene was pushed as an overlay.
  lurek.scene.isPreloaded( name )  -- Returns true if the named scene has been preloaded.
  lurek.scene.isTransitioning( )  -- Returns true if a scene transition is currently active.
  lurek.scene.new( [def] )  -- Creates a scene instance directly from a methods table.
  lurek.scene.newDepthSorter( )  -> DepthSorter  -- Creates a new DepthSorter for z-ordered draw batching.
  lurek.scene.newScene( [def] )  -> Scene  -- Alias for `lurek.scene.new`. Creates a scene instance from a methods table.
  lurek.scene.pop( [transition], [duration], [easing] )  -- Pops the top scene from the stack with an optional transition and easing.
  lurek.scene.popTo( name )  -- Pops scenes until the named scene is on top, calling leave on each removed.
  lurek.scene.preload( name, loader )  -- Registers a loader function for a named scene. The loader is called
  lurek.scene.process( dt )  -- Calls `scene:ready(self)` once per scene on the first tick after enter,
  lurek.scene.processLate( dt )  -- Calls `scene:process_late(dt)` on all active scenes (after process, before render).
  lurek.scene.processPhysics( dt )  -- Calls `scene:process_physics(dt)` on all active scenes (fixed timestep).
  lurek.scene.push( )  -- Pushes a scene table onto the stack with an optional transition and easing.
  lurek.scene.pushOverlay( )  -- Pushes a scene as a non-pausing overlay over the current top scene.
  lurek.scene.pushPreloaded( )  -- Pushes a registered scene by name, running its loader if not yet preloaded.
  lurek.scene.registerScene( name, scene )  -- Registers a scene table by name for later retrieval.
  lurek.scene.removeData( key )  -- Removes a value from the inter-scene data store by key.
  lurek.scene.render( )  -- Draws all scenes in the stack from bottom to top.
  lurek.scene.renderUi( )  -- Draws UI overlay for all scenes in the stack from bottom to top.
  lurek.scene.serializeScene( )  -- Returns a snapshot of the scene stack as a Lua table: { stack=[name...], data={key=val} }.
  lurek.scene.setData( key, value )  -- Stores a value in the inter-scene data store under the given key.
  lurek.scene.slide( [direction], [duration] )  -- Returns a directional slide transition config table.
  lurek.scene.switchTo( )  -- Replaces the top scene with a new one, calling leave and enter callbacks.
  lurek.scene.unregisterScene( name )  -- Removes a scene from the registry by name.
  lurek.scene.update( dt )  -- Updates the top scene and any active transition (legacy name; prefer `process`).
  lurek.scene.wipe( [duration] )  -- Returns a wipe/curtain transition config table.
```

**`LDepthSorter`** methods:

> Lua-side wrapper around a [`DepthSorter`] with registry-stored callbacks.

```lua
  LDepthSorter:add( callback, depth )  -- Registers a draw callback at the given depth layer.
  LDepthSorter:addObject( obj )  -- Registers a table object with a draw method at the given depth.
  LDepthSorter:clear( )  -- Removes all registered callbacks without calling them.
  LDepthSorter:flush( )  -- Calls all draw callbacks in sorted depth order, then clears.
  LDepthSorter:getCount( )  -> Count  -- Returns the number of registered draw entries.
  LDepthSorter:isStable( )  -- Returns true if stable sort mode is enabled.
  LDepthSorter:setStable( stable )  -- Sets whether equal-depth entries preserve insertion order.
  LDepthSorter:sort( )  -- Sorts all registered callbacks by depth ascending.
  LDepthSorter:type( )  -- Returns the type name of this object.
  LDepthSorter:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.ecs {#ecs}

> Lightweight entity/component system with universe, tags, and blueprints.

```lua
  lurek.ecs.newUniverse( )  -> Universe  -- Creates a new empty ECS universe.
```

**`LUniverse`** methods:

> Lua-side wrapper around a [`Universe`] ECS world.

```lua
  LUniverse:addRelation( from, name, to )  -- Adds a directed named relationship from entity `from` to entity `to`.
  LUniverse:addSystem( system, [opts] )  -- Adds a system table to the universe with an optional priority (lower = earlier).
  LUniverse:addTag( id, tag )  -- Attaches a string tag to an entity.
  LUniverse:bitmapTag( id, name )  -- Adds a bitmap tag to an entity.
  LUniverse:bitmapUntag( id, name )  -- Removes a bitmap tag from an entity.
  LUniverse:clear( )  -- Removes all entities, components, tags, layers, and systems. Blueprints are preserved.
  LUniverse:clearRelations( from, name )  -- Removes all directed named relationships of type `name` from entity `from`.
  LUniverse:defineBlueprint( name, components )  -- Defines a blueprint from a component table.
  LUniverse:defineTag( name )  -- Defines a bitmap tag name, returning its bit index.
  LUniverse:deserialize( snapshot )  -- Restores entity state from a snapshot produced by serialize().
  LUniverse:each( name, callback )  -- Calls callback(id, value) for every entity with the named component.
  LUniverse:emit( ... )  -- Emits a named event to all systems that implement the handler, in priority order.
  LUniverse:extendBlueprint( name, parent, overrides )  -- Defines a blueprint by extending a parent with overrides.
  LUniverse:flushObservers( )  -- Dispatches all pending component-add and component-remove events to registered callbacks.
  LUniverse:get( id, name )  -- Returns the component value for an entity, or nil if missing.
  LUniverse:getBitmapTagBit( name )  -> BitmapTagBit  -- Returns the bit index for a bitmap tag name, or nil if undefined.
  LUniverse:getBlueprintComponents( name )  -> BlueprintComponents  -- Returns a deep copy of a blueprint's component table, or nil.
  LUniverse:getChildren( parent_id )  -> Children  -- Returns all direct child entity IDs.
  LUniverse:getComponents( id )  -> Components  -- Returns all component names for an entity.
  LUniverse:getEntities( )  -> Entities  -- Returns all alive entity IDs.
  LUniverse:getEntitiesByLayer( layer )  -> EntitiesByLayer  -- Returns all alive entities on a specific layer.
  LUniverse:getEntitiesByTag( tag )  -> EntitiesByTag  -- Returns all alive entities with the given string tag.
  LUniverse:getEntitiesSorted( )  -> EntitiesSorted  -- Returns all alive entities sorted by layer then ID.
  LUniverse:getEntityCount( )  -> EntityCount  -- Returns the number of alive entities.
  LUniverse:getLayer( id )  -> Layer  -- Returns the layer for an entity, defaulting to zero.
  LUniverse:getParent( child_id )  -> Parent  -- Returns the parent entity ID, or nil if unparented.
  LUniverse:getRelated( from, name )  -> Related  -- Returns all entity IDs reachable from `from` via the named relationship.
  LUniverse:getSystemCount( )  -> SystemCount  -- Returns the number of registered systems.
  LUniverse:getTags( id )  -> Tags  -- Returns all string tags for an entity.
  LUniverse:has( id, name )  -- Returns true if the entity has the named component.
  LUniverse:hasBitmapTag( id, name )  -- Returns true if the entity has the given bitmap tag.
  LUniverse:hasBlueprint( name )  -- Returns true if a blueprint with the given name exists.
  LUniverse:hasRelation( from, name, to )  -- Returns true if a directed named relationship from `from` to `to` exists.
  LUniverse:hasTag( id, tag )  -- Returns true if the entity carries the given tag.
  LUniverse:isAlive( id )  -- Returns true if the entity ID is currently alive.
  LUniverse:kill( id )  -- Destroys the entity with the given ID, freeing its slot for reuse.
  LUniverse:killRecursive( id )  -- Kills an entity and all its descendants recursively.
  LUniverse:listBlueprints( )  -- Returns all defined blueprint names.
  LUniverse:onComponentAdded( name, cb )  -- Registers a callback to fire when a component is added to any entity.
  LUniverse:onComponentRemoved( name, cb )  -- Registers a callback to fire when a component is removed from any entity.
  LUniverse:query( ... )  -- Returns entity IDs that have all listed component names.
  LUniverse:queryBitmapAll( names )  -- Returns all alive entities with all of the listed bitmap tags.
  LUniverse:queryBitmapAny( names )  -- Returns all alive entities with any of the listed bitmap tags.
  LUniverse:queryBitmapTag( name )  -- Returns all alive entities with the given bitmap tag.
  LUniverse:queryNot( with_tbl, without_tbl )  -- Returns entity IDs that have all `with` components and none of the `without` components.
  LUniverse:release( )  -- Releases all universe state, equivalent to clear.
  LUniverse:remove( id, name )  -- Removes a component from an entity.
  LUniverse:removeBlueprint( name )  -- Removes a blueprint definition.
  LUniverse:removeRelation( from, name, to )  -- Removes the directed named relationship from entity `from` to entity `to`.
  LUniverse:removeSystem( system )  -- Removes a system table from the universe.
  LUniverse:removeTag( id, tag )  -- Removes a string tag from an entity.
  LUniverse:render( )  -- Calls render(system, world) on each registered system in priority order.
  LUniverse:serialize( )  -- Serializes all alive entities to a Lua table snapshot.
  LUniverse:set( id, name, value )  -- Sets a component value on an entity.
  LUniverse:setLayer( id, layer )  -- Sets the layer for an entity.
  LUniverse:setParent( child_id, [parent_id] )  -- Sets or clears the parent of an entity.
  LUniverse:spawn( )  -- Creates a new entity and returns its packed ID.
  LUniverse:spawnBlueprint( name, [overrides] )  -- Spawns an entity from a blueprint with optional overrides.
  LUniverse:spawnBulk( name, count, [overrides] )  -- Spawns `count` entities from a blueprint, returns an array of entity IDs.
  LUniverse:type( )  -- Returns the type name of this object.
  LUniverse:typeOf( name )  -- Returns true if this object is of the given type.
  LUniverse:update( dt )  -- Calls update(system, world, dt) on each registered system in priority order.
```

## lurek.pathfind {#pathfind}

> Grid-based A* and flow-field pathfinding.

```lua
  lurek.pathfind.getThreadCount( )  -> ThreadCount  -- Returns the background pathfinding thread count (currently always 0).
  lurek.pathfind.newFlowField( grid_ud )  -> FlowField  -- Creates a new FlowField backed by a NavGrid.
  lurek.pathfind.newHexGrid( width, height, [layout_str] )  -> HexGrid  -- Creates a hex grid for pathfinding, LOS, FOV, and range queries.
  lurek.pathfind.newJpsGrid( width, height )  -> JpsGrid  -- Creates a uniform-cost grid optimised for Jump Point Search (orthogonal + diagonal).
  lurek.pathfind.newNavGrid( width, height )  -> NavGrid  -- Creates a new NavGrid with all cells walkable.
  lurek.pathfind.newNavGridFromTileMap( tm_ud, layer_index, blocked_table )  -> NavGridFromTileMap  -- Builds a NavGrid from a TileMap layer, treating specified GIDs as blocked (unwalkable).
  lurek.pathfind.newPathFlowField( grid_ud )  -> BFS  -- Creates a new BFS flow field from a PathGrid.
  lurek.pathfind.newPathGrid( w, h, cell_size )  -> PathGrid  -- Creates a new PathGrid with per-cell cost and walkability.
  lurek.pathfind.newPathfinder( grid_ud )  -> UnitPathfinder  -- Creates a new UnitPathfinder backed by a NavGrid.
  lurek.pathfind.rangeMap( opts )  -- Computes a Dijkstra range-of-movement map from an origin within a movement budget.
  lurek.pathfind.setThreadCount( count )  -- Sets the background pathfinding thread count (currently a no-op).
```

**`LAIFlowField`** methods:

> Lua-side wrapper around a PathGrid-based [`AiFlowField`].

```lua
  LAIFlowField:getDirection( x, y )  -> Direction  -- Returns the normalised direction toward the goal (1-based coordinates).
  LAIFlowField:getDistance( x, y )  -> BFS  -- Returns the BFS distance to the goal (1-based coordinates).
  LAIFlowField:getGoal( )  -> Goal  -- Returns the goal cell (1-based coordinates) or nil if unset.
  LAIFlowField:getHeight( )  -> Height  -- Returns the flow field grid height in cells.
  LAIFlowField:getWidth( )  -> Width  -- Returns the flow field grid width in cells.
  LAIFlowField:hasGoal( )  -- Returns true if a goal has been set.
  LAIFlowField:setGoal( x, y )  -- Sets the goal cell and triggers BFS recomputation (1-based coordinates).
  LAIFlowField:type( )  -- Returns the type name of this object.
  LAIFlowField:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LFlowField`** methods:

> Lua-side wrapper around a [`FlowField`].

```lua
  LFlowField:calculate( tx, ty, [unit_size] )  -- Computes the flow field toward a single target (1-based coordinates).
  LFlowField:calculateMulti( targets, [unit_size] )  -- Computes the flow field toward multiple targets (1-based coordinates).
  LFlowField:getCostToTarget( x, y )  -> CostToTarget  -- Returns the integrated cost to the nearest target (1-based coordinates).
  LFlowField:getDirection( x, y )  -> Direction  -- Returns the normalised direction vector at a cell (1-based coordinates).
  LFlowField:getDirectionAngle( x, y )  -> DirectionAngle  -- Returns the flow direction as an angle in radians (1-based coordinates).
  LFlowField:getTargets( )  -> Targets  -- Returns the target cells from the most recent computation (1-based coordinates).
  LFlowField:isCalculated( )  -- Returns true if the flow field has been computed at least once.
  LFlowField:steer( wx, wy, speed, tw, th )  -- Converts a world-space position into a velocity vector via the flow field.
  LFlowField:type( )  -- Returns the type name of this object.
  LFlowField:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LHexGrid`** methods:

> Lua-side wrapper around a [`HexGrid`].

```lua
  LHexGrid:distance( c1, r1, c2, r2 )  -- Hex-distance between two cells.
  LHexGrid:fieldOfView( col, row, max_range )  -- Returns all cells visible from origin within max_range (1-based coordinates).
  LHexGrid:findPath( fc, fr, tc, tr )  -- Find A* path between two cells (1-based coordinates).
  LHexGrid:isBlocked( col, row )  -- Returns true if a cell is blocked (1-based coordinates).
  LHexGrid:lineOfSight( fc, fr, tc, tr )  -- Returns true if there is an unobstructed line between two cells (1-based).
  LHexGrid:rangeOfMovement( col, row, budget )  -- Returns all cells reachable from origin within movement budget (1-based).
  LHexGrid:setBlocked( col, row, blocked )  -- Mark/unmark a cell as blocked (1-based coordinates).
  LHexGrid:setCost( col, row, cost )  -- Set movement cost for a cell (1-based coordinates).
  LHexGrid:type( )  -- Returns the type name of this object.
  LHexGrid:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LJpsGrid`** methods:

> Lua-side wrapper around a [`JpsGrid`].

```lua
  LJpsGrid:findPath( fx, fy, tx, ty )  -- Find a JPS path between two cells (1-based coordinates).
  LJpsGrid:isBlocked( x, y )  -- Returns true if the cell is blocked (1-based coordinates).
  LJpsGrid:setBlocked( x, y, blocked )  -- Mark/unmark a cell as blocked (1-based coordinates).
  LJpsGrid:type( )  -- Returns the type name of this object.
  LJpsGrid:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LNavGrid`** methods:

> Lua-side wrapper around a [`NavGrid`] with optional HPA★ abstract graph.

```lua
  LNavGrid:clearDirty( )  -- Clears all pending dirty rectangles.
  LNavGrid:fill( cost )  -- Sets every cell to the given cost.
  LNavGrid:fillRect( x, y, w, h, cost )  -- Sets all cells in a rectangle to the given cost (1-based coordinates).
  LNavGrid:getChunkSize( )  -> ChunkSize  -- Returns the current HPA★ chunk size.
  LNavGrid:getCost( x, y )  -> Cost  -- Returns the traversal cost of a cell (1-based coordinates).
  LNavGrid:getDiagonalMode( )  -> DiagonalMode  -- Returns the current diagonal movement mode as a string.
  LNavGrid:getDimensions( )  -> Dimensions  -- Returns the grid dimensions as width, height.
  LNavGrid:getHeight( )  -> Height  -- Returns the grid height in cells.
  LNavGrid:getWidth( )  -> Width  -- Returns the grid width in cells.
  LNavGrid:isBlocked( x, y )  -- Returns true if the cell is blocked (1-based coordinates).
  LNavGrid:isWalkable( x, y, [unit_size] )  -- Returns true if a unit footprint is fully walkable (1-based coordinates).
  LNavGrid:loadFromString( data )  -- Overwrites the grid from a raw byte string (row-major, one byte per cell).
  LNavGrid:rebuildAbstract( )  -- Rebuilds the HPA★ abstract graph from the current grid state.
  LNavGrid:saveToString( )  -- Exports the cost grid as a byte string (row-major, one byte per cell).
  LNavGrid:setBlocked( x, y, blocked )  -- Marks a cell as blocked or unblocked (1-based coordinates).
  LNavGrid:setChunkSize( size )  -- Sets the HPA★ chunk size.
  LNavGrid:setCost( x, y, cost )  -- Sets the traversal cost of a cell (1-based coordinates).
  LNavGrid:setDiagonalMode( mode )  -- Sets the diagonal movement mode.
  LNavGrid:setDirty( x, y, w, h )  -- Records a dirty rectangle for incremental HPA★ updates (1-based coordinates).
  LNavGrid:type( )  -- Returns the type name of this object.
  LNavGrid:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LPathGrid`** methods:

> Lua-side wrapper around a [`PathGrid`] (A★ weighted grid with per-cell cost).

```lua
  LPathGrid:findPath( sx, sy, gx, gy )  -- Finds an A★ path returning world-space waypoints (1-based coordinates).
  LPathGrid:findPathSmoothed( sx, sy, gx, gy )  -- Finds a smoothed A★ path with string-pulling (1-based coordinates).
  LPathGrid:getCellSize( )  -> CellSize  -- Returns the world-space size of each cell.
  LPathGrid:getCost( x, y )  -> Cost  -- Returns the cost multiplier for a cell (1-based coordinates).
  LPathGrid:getHeight( )  -> Height  -- Returns the grid height in cells.
  LPathGrid:getWidth( )  -> Width  -- Returns the grid width in cells.
  LPathGrid:isWalkable( x, y )  -- Returns true if a cell is walkable (1-based coordinates).
  LPathGrid:setCost( x, y, cost )  -- Sets the cost multiplier for a cell (1-based coordinates).
  LPathGrid:setWalkable( x, y, w )  -- Sets the walkability of a cell (1-based coordinates).
  LPathGrid:type( )  -- Returns the type name of this object.
  LPathGrid:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LUnitPathfinder`** methods:

> Lua-side wrapper around a [`UnitPathfinder`].

```lua
  LUnitPathfinder:clearCache( )  -- Removes all cached path results.
  LUnitPathfinder:findNearestWalkable( x, y, max_radius, [unit_size] )  -- Finds the nearest walkable cell within a radius (1-based coordinates).
  LUnitPathfinder:findPartialPath( )  -- Finds a partial path with a node expansion limit (1-based coordinates).
  LUnitPathfinder:findPath( x1, y1, x2, y2, [unit_size] )  -- Finds an A★ path between two cells (1-based coordinates).
  LUnitPathfinder:findPathBidirectional( )  -- Finds a path using bidirectional A★, expanding from start and goal simultaneously
  LUnitPathfinder:findPathSmooth( x1, y1, x2, y2, [unit_size] )  -- Finds a Theta★ smoothed path between two cells (1-based coordinates).
  LUnitPathfinder:getCacheSize( )  -> CacheSize  -- Returns the number of entries in the path cache.
  LUnitPathfinder:getPathCost( path )  -> PathCost  -- Returns the sum of grid traversal costs along a path.
  LUnitPathfinder:getPathLength( path )  -> PathLength  -- Returns the euclidean length of a path table.
  LUnitPathfinder:heuristicDistance( x1, y1, x2, y2 )  -- Returns the octile heuristic distance between two cells (1-based coordinates).
  LUnitPathfinder:isCacheEnabled( )  -- Returns true if path result caching is enabled.
  LUnitPathfinder:isReachable( x1, y1, x2, y2, [unit_size] )  -- Returns true if a path exists between two cells (1-based coordinates).
  LUnitPathfinder:lineOfSight( x1, y1, x2, y2, [unit_size] )  -- Returns true if there is a clear line of sight between two cells (1-based coordinates).
  LUnitPathfinder:setCacheEnabled( enabled )  -- Enables or disables path result caching.
  LUnitPathfinder:setCacheMaxSize( n )  -- Sets the maximum number of cached path entries.
  LUnitPathfinder:type( )  -- Returns the type name of this object.
  LUnitPathfinder:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.ai {#ai}

> AI subsystems: FSM, behavior tree, steering, Q-learning, squad, utility AI, GOAP.

```lua
  lurek.ai.newAIDirector( )  -> AI  -- Creates a new AI pacing director with default config.
  lurek.ai.newAILod( )  -> AI  -- Creates a new AI LOD controller with default 3-tier config.
  lurek.ai.newAction( callback )  -> BT  -- Creates a BT action leaf with a Lua callback.
  lurek.ai.newBandit( arm_count, strategy, epsilon, seed )  -> Bandit  -- Creates a new multi-armed bandit.
  lurek.ai.newBehaviorTree( )  -> BehaviorTree  -- Creates a new behavior tree.
  lurek.ai.newBlackboard( )  -> Blackboard  -- Creates a new standalone blackboard.
  lurek.ai.newCommandQueue( )  -> RTS  -- Creates an RTS-style command queue.
  lurek.ai.newCondition( callback )  -> BT  -- Creates a BT condition leaf with a Lua predicate.
  lurek.ai.newContextSteering( slots )  -> ContextSteering  -- Creates a new context steering controller.
  lurek.ai.newEmotionModel( )  -> EmotionModel  -- Creates a new affective emotion model.
  lurek.ai.newGOAPPlanner( )  -> GOAP  -- Creates a new GOAP planning solver.
  lurek.ai.newGeneticAlgorithm( pop_size, gene_count, seed )  -> GeneticAlgorithm  -- Creates a new genetic algorithm.
  lurek.ai.newGuard( predicate, child_ud )  -> BT  -- Creates a BT Guard decorator. The predicate is evaluated before each tick;
  lurek.ai.newHTNDomain( )  -> Hierarchical  -- Creates a new Hierarchical Task Network domain.
  lurek.ai.newInfluenceMap( w, h, cs )  -> InfluenceMap  -- Creates a multi-layer influence map grid.
  lurek.ai.newInverter( )  -> BT  -- Creates a BT inverter decorator.
  lurek.ai.newMCTSEngine( iters, uct_c, depth, seed )  -> Monte  -- Creates a new Monte Carlo Tree Search engine.
  lurek.ai.newNeedSystem( )  -> NeedSystem  -- Creates a new motivational need system.
  lurek.ai.newNeuralNet( )  -> NeuralNet  -- Creates a new feedforward neural network (inference only).
  lurek.ai.newNeuroevolution( layer_spec, pop_size, seed )  -> Neuroevolution  -- Creates a neuroevolution trainer (GA for neural network weights).
  lurek.ai.newORCASolver( time_horizon )  -> ORCA  -- Creates a new ORCA crowd avoidance solver.
  lurek.ai.newParallel( [sp], [fp] )  -> BT  -- Creates a BT parallel node with optional policies.
  lurek.ai.newQLearner( sc, ac )  -> QLearner  -- Creates a tabular Q-learner.
  lurek.ai.newRepeater( [count] )  -> BT  -- Creates a BT repeater decorator.
  lurek.ai.newSelector( )  -> BT  -- Creates a BT selector node.
  lurek.ai.newSequence( )  -> BT  -- Creates a BT sequence node.
  lurek.ai.newSquad( name )  -> Squad  -- Creates a named squad for formation positioning.
  lurek.ai.newStateMachine( )  -> StateMachine  -- Creates a new finite state machine.
  lurek.ai.newSteeringManager( )  -> SteeringManager  -- Creates a new steering behavior manager.
  lurek.ai.newStimulusWorld( )  -> StimulusWorld  -- Creates a new stimulus perception world.
  lurek.ai.newStrategyAI( update_interval )  -> StrategyAI  -- Creates a new throttled strategy AI.
  lurek.ai.newSucceeder( )  -> BT  -- Creates a BT succeeder decorator.
  lurek.ai.newTraitProfile( )  -> TraitProfile  -- Creates a new personality trait profile.
  lurek.ai.newUtilityAI( )  -> UtilityAI  -- Creates a new utility AI evaluator.
  lurek.ai.newWorld( )  -> AI  -- Creates a new AI world container.
```

**`LAIBlackboard`** methods:

> Lua-side wrapper around a [`Blackboard`].

```lua
  LAIBlackboard:clear( )  -- Removes all local entries.
  LAIBlackboard:getBool( key, [default] )  -> Bool  -- Returns the boolean for the given key, or default.
  LAIBlackboard:getKeys( )  -> Keys  -- Returns all local keys as a table.
  LAIBlackboard:getNumber( key, [default] )  -> Number  -- Returns the number for the given key, or default.
  LAIBlackboard:getSize( )  -> Size  -- Returns the number of local entries.
  LAIBlackboard:getString( key, [default] )  -> String  -- Returns the string for the given key, or default.
  LAIBlackboard:has( key )  -- Returns true if a value exists under the key.
  LAIBlackboard:remove( key )  -- Removes the entry at key.
  LAIBlackboard:setBool( key, value )  -- Stores a boolean under the given key.
  LAIBlackboard:setNumber( key, value )  -- Stores a number under the given key.
  LAIBlackboard:setString( key, value )  -- Stores a string under the given key.
  LAIBlackboard:type( )  -- Returns the type name of this object.
  LAIBlackboard:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LAIDirector`** methods:

> Lua wrapper for [`crate::ai::director::AIDirector`].

```lua
  LAIDirector:ambientIntensity( )  -- Returns or performs ambient intensity.
  LAIDirector:lootFactor( )  -- Returns or performs loot factor.
  LAIDirector:phase( )  -- Returns or performs phase.
  LAIDirector:pushEvent( intensity )  -- Pushes a gameplay event with the given intensity to the director for awareness analysis.
  LAIDirector:reset( )  -- Resets or clears the state.
  LAIDirector:setTension( value )  -- Sets the global narrative tension level (0â€“1 scale).
  LAIDirector:spawnRateFactor( )  -- Returns or performs spawn rate factor.
  LAIDirector:tension( )  -- Returns or performs tension.
  LAIDirector:type( )  -- Returns the type name of this object.
  LAIDirector:typeOf( name )  -- Returns true if this object is of the given type.
  LAIDirector:update( dt )  -- Advances the simulation by one time step.
```

**`LAILod`** methods:

> Lua wrapper for [`crate::ai::lod::AILod`].

```lua
  LAILod:shouldUpdate( tier, frame )  -- Returns or performs should update.
  LAILod:tierCount( )  -- Returns or performs tier count.
  LAILod:tierFor( ax, ay, rx, ry )  -- Returns or performs tier for.
  LAILod:tierName( tier )  -- Returns or performs tier name.
  LAILod:type( )  -- Returns the type name of this object.
  LAILod:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LAIWorld`** methods:

> Lua-side wrapper around an [`AIWorld`].

```lua
  LAIWorld:addAgent( name )  -- Registers a new named agent and returns its handle.
  LAIWorld:getAgent( name )  -> Agent  -- Returns the agent handle for the given name, or nil.
  LAIWorld:getAgentCount( )  -> AgentCount  -- Returns the number of registered agents.
  LAIWorld:getGlobalBlackboard( )  -> GlobalBlackboard  -- Returns a snapshot of the world-level blackboard.
  LAIWorld:removeAgent( agent )  -- Removes an agent by its userdata handle.
  LAIWorld:type( )  -- Returns the type name of this object.
  LAIWorld:typeOf( name )  -- Returns true if this object is of the given type.
  LAIWorld:update( dt )  -- Advances all agents by dt seconds, then invokes any custom-model callbacks.
```

**`LAgent`** methods:

> Lua-side wrapper for an agent accessed by name through the owning world.

```lua
  LAgent:addTag( tag )  -- Adds a tag to this agent.
  LAgent:getBlackboard( )  -> Blackboard  -- Returns the agent's local blackboard.
  LAgent:getDecisionModel( )  -> DecisionModel  -- Returns the name of the current decision model.
  LAgent:getMaxForce( )  -> MaxForce  -- Returns the maximum steering force cap.
  LAgent:getMaxSpeed( )  -> MaxSpeed  -- Returns the maximum speed cap.
  LAgent:getName( )  -> Name  -- Returns the agent's registered name.
  LAgent:getPosition( )  -> Position  -- Returns the agent's current position.
  LAgent:getPriority( )  -> Priority  -- Returns the agent's scheduling priority.
  LAgent:getVelocity( )  -> Velocity  -- Returns the agent's current velocity.
  LAgent:hasTag( tag )  -- Returns true if the agent has the given tag.
  LAgent:removeTag( tag )  -- Removes a tag from this agent.
  LAgent:setCustomModel( callback )  -- Installs a Lua-driven decision model on this agent.
  LAgent:setDecisionModel( model )  -- Sets the active decision model.
  LAgent:setMaxForce( v )  -- Sets the maximum steering force cap.
  LAgent:setMaxSpeed( v )  -- Sets the maximum speed cap.
  LAgent:setPosition( x, y )  -- Sets the agent's world-space position.
  LAgent:setPriority( p )  -- Sets the scheduling priority (higher = earlier).
  LAgent:setVelocity( x, y )  -- Sets the agent's velocity vector.
  LAgent:type( )  -- Returns the type name of this object.
  LAgent:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LBTNode`** methods:

> Lua-side wrapper around a [`BTNode`].

```lua
  LBTNode:addChild( child_ud )  -- Adds a child node (Selector, Sequence, or Parallel only).
  LBTNode:getChildCount( )  -> ChildCount  -- Returns the number of direct children.
  LBTNode:getCount( )  -> Count  -- Returns the repeat count, or 0 if not a Repeater.
  LBTNode:getNodeType( )  -> NodeType  -- Returns the node type as a string.
  LBTNode:reset( )  -- Resets all running-child memos and repeater counters.
  LBTNode:setChild( child_ud )  -- Sets the single child of a decorator node.
  LBTNode:setCount( n )  -- Sets the repeat count for a Repeater node.
  LBTNode:setFailurePolicy( policy )  -- Sets the failure policy for a Parallel node.
  LBTNode:setSuccessPolicy( policy )  -- Sets the success policy for a Parallel node.
  LBTNode:type( )  -- Returns the type name of this object.
  LBTNode:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LBandit`** methods:

> Lua wrapper for [`crate::ai::bandit::Bandit`].

```lua
  LBandit:armCount( )  -- Returns or performs arm count.
  LBandit:bestArm( )  -- Returns or performs best arm.
  LBandit:reset( )  -- Resets or clears the state.
  LBandit:select( )  -- Returns or performs select.
  LBandit:totalPulls( )  -- Returns or performs total pulls.
  LBandit:type( )  -- Returns the type name of this object.
  LBandit:typeOf( name )  -- Returns true if this object is of the given type.
  LBandit:update( idx, reward )  -- Advances the simulation by one time step.
```

**`LBehaviorTree`** methods:

> Lua-side wrapper around a [`BehaviorTree`].

```lua
  LBehaviorTree:getDebugState( )  -> DebugState  -- Returns a diagnostic snapshot of this behavior tree.
  LBehaviorTree:getLastStatus( )  -> LastStatus  -- Returns the status from the last tick.
  LBehaviorTree:setRoot( node_ud )  -- Sets the root node of this behavior tree.
  LBehaviorTree:type( )  -- Returns the type name of this object.
  LBehaviorTree:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LCommandQueue`** methods:

> Lua-side wrapper around a [`CommandQueue`].

```lua
  LCommandQueue:cancelCurrent( )  -- Cancels the front command if it is interruptible.
  LCommandQueue:clear( )  -- Discards all queued commands.
  LCommandQueue:enqueue( kind, callback, [opts] )  -- Appends a command to the back of the queue.
  LCommandQueue:getCount( )  -> Count  -- Returns the number of queued commands.
  LCommandQueue:getCurrentTarget( )  -> CurrentTarget  -- Returns the target coordinates of the front command.
  LCommandQueue:getCurrentType( )  -> CurrentType  -- Returns the kind of the front command, or nil.
  LCommandQueue:isEmpty( )  -- Returns true if there are no queued commands.
  LCommandQueue:pushFront( kind, callback, [opts] )  -- Inserts a command at the front, interrupting the current one.
  LCommandQueue:replace( kind, callback, [opts] )  -- Clears the queue and enqueues one new command.
  LCommandQueue:type( )  -- Returns the type name of this object.
  LCommandQueue:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LContextSteering`** methods:

> Lua wrapper for [`crate::ai::context_steering::ContextSteering`].

```lua
  LContextSteering:addAvoidBounds( min_x, min_y, max_x, max_y, margin, weight )  -- Registers a rectangular region this agent must avoid.
  LContextSteering:addAvoidPoint( x, y, radius, weight )  -- Adds a world-space point that this agent steers away from.
  LContextSteering:addSeekTarget( tx, ty, weight )  -- Adds a world-space target that this agent steers towards.
  LContextSteering:addWander( jitter, weight )  -- Adds a wander behavior with jitter and weight to the context steering evaluator.
  LContextSteering:chosenMagnitude( )  -- Returns or performs chosen magnitude.
  LContextSteering:clearBehaviors( )  -- Resets or clears the behaviors.
  LContextSteering:evaluate( ax, ay, vx, vy )  -- Evaluates and returns the computed result.
  LContextSteering:slotCount( )  -- Returns or performs slot count.
  LContextSteering:type( )  -- Returns the type name of this object.
  LContextSteering:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LEmotionModel`** methods:

> Lua wrapper for [`crate::ai::emotion::EmotionModel`].

```lua
  LEmotionModel:add( name, rest, decay, min_vis )  -- Adds an emotion category with the given name and initial intensity to the model.
  LEmotionModel:dominant( )  -- Returns or performs dominant.
  LEmotionModel:get( name )  -- Returns the current float value of this emotion dimension.
  LEmotionModel:isActive( name )  -- Returns `true` if the emotion dimension is currently active and above threshold.
  LEmotionModel:reset( )  -- Resets or clears the state.
  LEmotionModel:trigger( name, amount )  -- Returns or performs trigger.
  LEmotionModel:type( )  -- Returns the type name of this object.
  LEmotionModel:typeOf( name )  -- Returns true if this object is of the given type.
  LEmotionModel:update( dt )  -- Advances the simulation by one time step.
```

**`LGOAPPlanner`** methods:

> Lua-side wrapper around a [`GOAPPlanner`].

```lua
  LGOAPPlanner:addAction( name, [cost], [callback] )  -- Adds a GOAP action with optional cost and callback.
  LGOAPPlanner:addGoal( name, [priority] )  -- Adds a planning goal with optional priority.
  LGOAPPlanner:getActionCount( )  -> ActionCount  -- Returns the number of registered actions.
  LGOAPPlanner:getGoalCount( )  -> GoalCount  -- Returns the number of registered goals.
  LGOAPPlanner:getMaxIterations( )  -> MaxIterations  -- Returns the maximum A* planning iterations.
  LGOAPPlanner:plan( world_state_tbl, [max_depth] )  -- Runs A* planning and returns an action sequence table.
  LGOAPPlanner:setEffect( action_name, key, value )  -- Sets a boolean effect on an action.
  LGOAPPlanner:setGoalState( goal_name, key, value )  -- Sets a boolean condition on a goal.
  LGOAPPlanner:setMaxIterations( n )  -- Sets the maximum A* planning iterations (0 = unlimited).
  LGOAPPlanner:setPrecondition( action_name, key, value )  -- Sets a boolean precondition on an action.
  LGOAPPlanner:type( )  -- Returns the type name of this object.
  LGOAPPlanner:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LGeneticAlgorithm`** methods:

> Lua wrapper for [`crate::ai::genetic::GeneticAlgorithm`].

```lua
  LGeneticAlgorithm:bestGenes( )  -- Returns or performs best genes.
  LGeneticAlgorithm:evolve( )  -- Runs one generation of the evolutionary algorithm.
  LGeneticAlgorithm:generation( )  -- Returns or performs generation.
  LGeneticAlgorithm:getGenes( idx )  -> Genes  -- Returns the chromosome as an ordered table of gene values.
  LGeneticAlgorithm:popSize( )  -- Returns or performs pop size.
  LGeneticAlgorithm:setFitness( idx, fitness )  -- Sets the fitness score used by the genetic algorithm selection step.
  LGeneticAlgorithm:type( )  -- Returns the type name of this object.
  LGeneticAlgorithm:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LHTNDomain`** methods:

> Lua wrapper for [`crate::ai::htn::HTNDomain`].

```lua
  LHTNDomain:addCompound( comp_name, methods_table )  -- Registers a compound HTN task that decomposes into sub-tasks.
  LHTNDomain:addPrimitive( name, preconds, effects, clears )  -- Registers a primitive HTN task with a direct operator function.
  LHTNDomain:plan( root_task, state_table )  -- Runs planning and returns the resulting action sequence.
  LHTNDomain:taskCount( )  -- Returns or performs task count.
  LHTNDomain:type( )  -- Returns the type name of this object.
  LHTNDomain:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LInfluenceMap`** methods:

> Lua-side wrapper around an [`InfluenceMap`].

```lua
  LInfluenceMap:addLayer( name )  -- Adds a named influence layer.
  LInfluenceMap:blend( layer_a, weight_a, layer_b, weight_b, dest )  -- Blends two layers into a destination layer.
  LInfluenceMap:clearAll( )  -- Removes all influence values from every layer in the map.
  LInfluenceMap:clearLayer( layer )  -- Clears all influence in a layer.
  LInfluenceMap:decay( layer, factor )  -- Multiplies all influences by a decay factor.
  LInfluenceMap:getCellSize( )  -> CellSize  -- Returns the cell size in world units.
  LInfluenceMap:getHeight( )  -> Height  -- Returns the influence map height in grid cells.
  LInfluenceMap:getInfluence( layer, x, y )  -> Influence  -- Returns the influence value at a cell (1-based).
  LInfluenceMap:getMaxPosition( layer )  -> MaxPosition  -- Returns the world-space position of the maximum value.
  LInfluenceMap:getMinPosition( layer )  -> MinPosition  -- Returns the world-space position of the minimum value.
  LInfluenceMap:getWidth( )  -> Width  -- Returns the influence map width in grid cells.
  LInfluenceMap:hasLayer( name )  -- Returns true if the named layer exists.
  LInfluenceMap:propagate( layer, [momentum] )  -- Propagates influence values with momentum.
  LInfluenceMap:queryRect( layer, wx, wy, ww, wh )  -- Returns the summed influence in a world-space rectangle.
  LInfluenceMap:setInfluence( layer, x, y, value )  -- Sets the influence value at a cell (1-based).
  LInfluenceMap:stampInfluence( layer, wx, wy, radius, value, [falloff] )  -- Stamps influence in a radial area.
  LInfluenceMap:type( )  -- Returns the type name of this object.
  LInfluenceMap:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LMCTSEngine`** methods:

> Lua wrapper for [`crate::ai::mcts::MCTSEngine`].

```lua
  LMCTSEngine:search( )  -- Uses Lua closures for game logic. All closures receive/return integer states.
  LMCTSEngine:type( )  -- Returns the type name of this object.
  LMCTSEngine:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LNeedSystem`** methods:

> Lua wrapper for [`crate::ai::needs::NeedSystem`].

```lua
  LNeedSystem:addNeed( name, decay_rate, urgency_threshold, urgency_factor )  -- Registers a new need with the specified name, urgency, and decay rate in the system.
  LNeedSystem:mostUrgent( )  -- Returns or performs most urgent.
  LNeedSystem:satisfy( name, amount )  -- Returns or performs satisfy.
  LNeedSystem:type( )  -- Returns the type name of this object.
  LNeedSystem:typeOf( name )  -- Returns true if this object is of the given type.
  LNeedSystem:update( dt )  -- Advances the simulation by one time step.
  LNeedSystem:valueOf( name )  -- Returns or performs value of.
```

**`LNeuralNet`** methods:

> Lua wrapper for [`crate::ai::neural_net::NeuralNet`].

```lua
  LNeuralNet:addLayer( inputs, outputs, activation )  -- Adds a neural network layer with inputs, outputs, and an activation function.
  LNeuralNet:forward( input )  -- Returns or performs forward.
  LNeuralNet:getWeights( )  -> Weights  -- Returns a flat table of all connection weight values in the network.
  LNeuralNet:layerCount( )  -- Returns or performs layer count.
  LNeuralNet:paramCount( )  -- Returns or performs param count.
  LNeuralNet:setWeights( weights )  -- Overwrites all connection weights with values from a flat table.
  LNeuralNet:type( )  -- Returns the type name of this object.
  LNeuralNet:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LNeuroevolution`** methods:

> Lua wrapper for [`crate::ai::neuroevolution::Neuroevolution`].

```lua
  LNeuroevolution:bestFitness( )  -- Returns or performs best fitness.
  LNeuroevolution:bestNetwork( )  -- Returns or performs best network.
  LNeuroevolution:chromosomeToNet( idx )  -- Returns or performs chromosome to net.
  LNeuroevolution:evolve( )  -- Runs one generation of the evolutionary algorithm.
  LNeuroevolution:generation( )  -- Returns or performs generation.
  LNeuroevolution:popSize( )  -- Returns or performs pop size.
  LNeuroevolution:setFitness( idx, fitness )  -- Sets the fitness score used by the genetic algorithm selection step.
  LNeuroevolution:type( )  -- Returns the type name of this object.
  LNeuroevolution:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LORCASolver`** methods:

> Lua wrapper for [`crate::ai::orca::ORCASolver`].

```lua
  LORCASolver:addAgent( x, y, radius, max_speed )  -- Adds an ORCA agent at the given position with radius and max speed to the solver.
  LORCASolver:agentCount( )  -- Returns or performs agent count.
  LORCASolver:compute( dt )  -- Computes and returns the result.
  LORCASolver:getSafeVelocity( idx )  -> SafeVelocity  -- Returns the safe velocity.
  LORCASolver:setPosition( idx, x, y )  -- Sets the agent's current world-space position for ORCA velocity computation.
  LORCASolver:setPreferredVelocity( idx, pvx, pvy )  -- Sets the preferred velocity.
  LORCASolver:type( )  -- Returns the type name of this object.
  LORCASolver:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LQLearner`** methods:

> Lua-side wrapper around a [`QLearner`].

```lua
  LQLearner:bestAction( state )  -- Returns the greedy-best action for the state (1-based).
  LQLearner:chooseAction( state )  -- Selects an action using epsilon-greedy policy (1-based).
  LQLearner:deserialize( json )  -- Restores the Q-table from a JSON string.
  LQLearner:endEpisode( )  -- Ends the current episode, applying epsilon decay.
  LQLearner:getActionCount( )  -> ActionCount  -- Returns the number of discrete actions.
  LQLearner:getDiscountFactor( )  -> DiscountFactor  -- Returns the current discount factor.
  LQLearner:getEpisodeCount( )  -> EpisodeCount  -- Returns the number of completed episodes.
  LQLearner:getExplorationDecay( )  -> ExplorationDecay  -- Returns the epsilon decay multiplier.
  LQLearner:getExplorationRate( )  -> ExplorationRate  -- Returns the current exploration rate.
  LQLearner:getLearningRate( )  -> LearningRate  -- Returns the current learning rate.
  LQLearner:getQValue( state, action )  -> Q  -- Returns the Q-value for a state-action pair (1-based).
  LQLearner:getStateCount( )  -> StateCount  -- Returns the number of discrete states.
  LQLearner:learn( state, action, reward, next_state )  -- Performs one Bellman Q-learning update (1-based indices).
  LQLearner:serialize( )  -- Serializes the Q-table to a JSON string.
  LQLearner:setDiscountFactor( v )  -- Sets the discount factor gamma.
  LQLearner:setExplorationDecay( v )  -- Sets the epsilon decay multiplier.
  LQLearner:setExplorationRate( v )  -- Sets the exploration rate epsilon.
  LQLearner:setLearningRate( v )  -- Sets the learning rate alpha.
  LQLearner:setQValue( state, action, value )  -- Overwrites the Q-value for a state-action pair (1-based).
  LQLearner:type( )  -- Returns the type name of this object.
  LQLearner:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LSquad`** methods:

> Lua-side wrapper around a [`Squad`].

```lua
  LSquad:addMember( name )  -- Adds an agent by name to this squad.
  LSquad:getBlackboard( )  -> Blackboard  -- Returns the squad's shared blackboard.
  LSquad:getFormation( )  -> Formation  -- Returns the current formation type name.
  LSquad:getFormationPosition( member_idx, leader_x, leader_y )  -> FormationPosition  -- Computes the world-space position for a member index (1-based).
  LSquad:getFormationSpacing( )  -> FormationSpacing  -- Returns the formation spacing in world units.
  LSquad:getLeader( )  -> Leader  -- Returns the leader name, or nil.
  LSquad:getMemberCount( )  -> MemberCount  -- Returns the number of squad members.
  LSquad:getMembers( )  -> Members  -- Returns the member names as a table.
  LSquad:getName( )  -> Name  -- Returns the unique name string assigned to this squad.
  LSquad:removeMember( name )  -- Removes an agent by name from this squad.
  LSquad:setFormation( ftype, [spacing] )  -- Sets the formation type and optional spacing.
  LSquad:setLeader( name )  -- Sets the squad leader by name.
  LSquad:type( )  -- Returns the type name of this object.
  LSquad:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LStateMachine`** methods:

> Lua-side wrapper around a [`StateMachine`].

```lua
  LStateMachine:addState( name, opts )  -- Registers a named state with optional lifecycle callbacks.
  LStateMachine:addTransition( from, to, [guard], [priority] )  -- Adds a guarded transition between states.
  LStateMachine:forceState( name )  -- Forces a transition to the named state.
  LStateMachine:getCurrentState( )  -> CurrentState  -- Returns the current state name, or nil.
  LStateMachine:getTimeInState( )  -> TimeInState  -- Returns seconds spent in the current state.
  LStateMachine:setInitialState( name )  -- Sets the FSM's initial state; must be called before the first update.
  LStateMachine:type( )  -- Returns the type name of this object.
  LStateMachine:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LSteeringManager`** methods:

> Lua-side wrapper around a [`SteeringManager`].

```lua
  LSteeringManager:addArrive( tx, ty, [slowing], [weight] )  -- Adds an Arrive behavior with deceleration.
  LSteeringManager:addCustomBehavior( func, [weight] )  -- Registers a Lua callback as a custom steering behavior.
  LSteeringManager:addEvade( [threat_name], [weight] )  -- Adds an Evade behavior fleeing from a named agent.
  LSteeringManager:addFlee( tx, ty, [panic_dist], [weight] )  -- Adds a Flee behavior away from the target.
  LSteeringManager:addFlock( )  -- Adds a Flock behavior for group movement.
  LSteeringManager:addPursue( [target_name], [weight] )  -- Adds a Pursue behavior targeting a named agent.
  LSteeringManager:addSeek( tx, ty, [weight] )  -- Adds a Seek behavior toward the target.
  LSteeringManager:addWander( )  -- Adds a Wander behavior for random meandering.
  LSteeringManager:applyCustomSteering( agent_ud, dt )  -- Invokes all registered custom steering callbacks and returns the combined force.
  LSteeringManager:calculate( px, py, vx, vy, max_speed, max_force, dt )  -- Computes the combined steering force for the given agent state.
  LSteeringManager:enableSpatialHash( enabled )  -- Enables or disables spatial-hash bucketing for neighbourhood queries.
  LSteeringManager:getBehaviorCount( )  -> BehaviorCount  -- Returns the number of active behaviors.
  LSteeringManager:getCombineMode( )  -> CombineMode  -- Returns the current combination mode.
  LSteeringManager:getLastSteering( )  -> LastSteering  -- Returns the last computed steering force.
  LSteeringManager:setCombineMode( mode )  -- Sets the force combination mode.
  LSteeringManager:setSpatialHashCellSize( size )  -- Sets the cell size used by the spatial-hash neighbourhood search.
  LSteeringManager:type( )  -- Returns the type name of this object.
  LSteeringManager:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LStimulusWorld`** methods:

> Lua wrapper for [`crate::ai::perception::StimulusWorld`].

```lua
  LStimulusWorld:addAuditory( )  -- Registers an auditory stimulus at a world-space position.
  LStimulusWorld:addVisual( x, y, intensity, radius, [tag] )  -- Adds a visual stimulus at the specified world position with radius and intensity.
  LStimulusWorld:clear( )  -- Resets or clears the state.
  LStimulusWorld:count( )  -- Returns or performs count.
  LStimulusWorld:remove( id )  -- Removes the specified item.
  LStimulusWorld:type( )  -- Returns the type name of this object.
  LStimulusWorld:typeOf( name )  -- Returns true if this object is of the given type.
  LStimulusWorld:update( dt )  -- Advances the simulation by one time step.
```

**`LStrategyAI`** methods:

> Lua wrapper for [`crate::ai::strategy::StrategyAI`].

```lua
  LStrategyAI:activeGoal( )  -- Returns or performs active goal.
  LStrategyAI:addGoal( name )  -- Adds a strategic goal with priority score to the planner for future evaluation.
  LStrategyAI:addTag( tag )  -- Adds a string tag to the strategy AI instance for goal filtering and categorization.
  LStrategyAI:forceEvaluate( scorer_fn )  -- Returns or performs force evaluate.
  LStrategyAI:removeTag( tag )  -- Removes the specified tag.
  LStrategyAI:timeUntilNext( )  -- Returns or performs time until next.
  LStrategyAI:type( )  -- Returns the type name of this object.
  LStrategyAI:typeOf( name )  -- Returns true if this object is of the given type.
  LStrategyAI:update( dt, scorer_fn )  -- Advances the simulation by one time step.
```

**`LTraitProfile`** methods:

> Lua wrapper for [`crate::ai::traits::TraitProfile`].

```lua
  LTraitProfile:addModifier( trait_name, delta, [duration], source )  -- Adds a named modifier that adjusts the trait value by a delta.
  LTraitProfile:archetype( )  -- Returns or performs archetype.
  LTraitProfile:get( name )  -- Returns the current float value of this emotion dimension.
  LTraitProfile:getBase( name )  -> Base  -- Returns the unmodified base value of this trait before modifiers.
  LTraitProfile:has( name )  -- Returns true if a item is present.
  LTraitProfile:removeModifiers( source )  -- Removes the specified modifiers.
  LTraitProfile:set( name, value )  -- Sets the base value of this trait, replacing any previous base.
  LTraitProfile:traitCount( )  -- Returns or performs trait count.
  LTraitProfile:type( )  -- Returns the type name of this object.
  LTraitProfile:typeOf( name )  -- Returns true if this object is of the given type.
  LTraitProfile:update( dt )  -- Advances the simulation by one time step.
```

**`LUtilityAI`** methods:

> Lua-side wrapper around a [`UtilityAI`].

```lua
  LUtilityAI:addAction( name, scorer_fn, [weight] )  -- Adds a scored action with optional momentum weight.
  LUtilityAI:addConsideration( )  -- Adds a multi-axis consideration to a named action.
  LUtilityAI:evaluate( )  -- Evaluates all actions and returns the best action name, or nil.
  LUtilityAI:getActionCount( )  -> ActionCount  -- Returns the number of registered actions.
  LUtilityAI:getLastAction( )  -> LastAction  -- Returns the name of the last chosen action, or nil.
  LUtilityAI:type( )  -- Returns the type name of this object.
  LUtilityAI:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.data {#data}

> Data utilities: TOML/JSON parsing, encoding, hashing, compression.

```lua
  lurek.data.compress( format_str, raw_data, [level] )  -- Compresses data using the given algorithm (deflate, gzip, lz4).
  lurek.data.crc32( raw_data )  -> CRC  -- Returns the CRC-32 checksum of the input data as an integer.
  lurek.data.decode( format_str, encoded )  -- Decodes encoded text back to binary (base64, hex).
  lurek.data.decompress( format_str, compressed )  -- Decompresses data using the given algorithm (deflate, gzip, lz4).
  lurek.data.encode( format_str, raw_data )  -- Encodes binary data using the given format (base64, hex).
  lurek.data.encodeToml( tbl )  -- Encodes a Lua table into a TOML string.
  lurek.data.fromMsgPack( bytes )  -- Deserializes a MessagePack binary string back into a Lua value.
  lurek.data.getPackedSize( fmt, ... )  -> PackedSize  -- Returns the number of bytes the given format and values would occupy.
  lurek.data.hash( algo_str, raw_data )  -- Returns the cryptographic hash of the input (md5, sha1, sha256, sha512).
  lurek.data.newByteData( value )  -> ByteData  -- Instantiates a raw byte data container object.
  lurek.data.newDataView( raw, [offset], [size] )  -> DataView  -- Creates a read-only windowed view into a byte string.
  lurek.data.newRingBuffer( capacity )  -> RingBuffer  -- Creates a fixed-capacity ring buffer that can store any Lua value.
  lurek.data.newWriter( )  -> Writer  -- Creates a new write-cursor for building binary data.
  lurek.data.pack( fmt, ... )  -- Packs values into a binary byte string using the format string.
  lurek.data.parseToml( text )  -- Parses a TOML string into a Lua table.
  lurek.data.read( fmt, raw, [offset] )  -- Reads values using the Lurek2D Binary Pack Format.
  lurek.data.size( fmt )  -- Returns the byte size of a Lurek2D Binary Pack Format string.
  lurek.data.toMsgPack( value )  -- Serializes a Lua value (table, string, number, boolean, or nil) to MessagePack binary.
  lurek.data.unpack( fmt, raw, [offset] )  -- Unpacks values from a binary byte string, returning values followed by next offset.
  lurek.data.write( fmt, ... )  -- Writes values using the Lurek2D Binary Pack Format.
```

**`LByteData`** methods:

> Raw byte buffer for binary I/O; addressable by byte or bit offset.

```lua
  LByteData:clone( )  -- Creates an independent copy of this byte buffer with identical contents.
  LByteData:getBit( byte_offset, bit_offset )  -> Bit  -- Returns the value of a single bit within the buffer.
  LByteData:getByte( offset )  -> Byte  -- Get a byte at the specified offset.
  LByteData:getSize( )  -> Size  -- Returns the total byte length of this buffer.
  LByteData:getString( )  -> String  -- Get the string representation.
  LByteData:readBits( byte_offset, bit_offset, count )  -- Reads `count` consecutive bits starting at `byte_offset`/`bit_offset`
  LByteData:setBit( byte_offset, bit_offset, value )  -- Sets or clears a single bit within the buffer.
  LByteData:setByte( offset, value )  -- Set a byte at the specified offset.
```

**`LDataView`** methods:

> Access structured binary data efficiently without copying.

```lua
  LDataView:getDouble( offset )  -> Double  -- Reads a 64-bit float at the given offset.
  LDataView:getFloat( offset )  -> Float  -- Reads a 32-bit float at the given offset.
  LDataView:getInt16( offset )  -> Int16  -- Reads a signed 16-bit integer at the given offset.
  LDataView:getInt32( offset )  -> Int32  -- Reads a signed 32-bit integer at the given offset.
  LDataView:getInt8( offset )  -> Int8  -- Reads a signed 8-bit integer at the given offset.
  LDataView:getSize( )  -> Size  -- Returns the size of this view in bytes.
  LDataView:getUInt16( offset )  -> UInt16  -- Reads an unsigned 16-bit integer at the given offset.
  LDataView:getUInt32( offset )  -> UInt32  -- Reads an unsigned 32-bit integer at the given offset.
  LDataView:getUInt8( offset )  -> UInt8  -- Reads an unsigned 8-bit integer at the given offset.
  LDataView:type( )  -- Returns the type name of this object.
  LDataView:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LDataWriter`** methods:

> Write-cursor wrapper for the `lurek.data` module.

```lua
  LDataWriter:len( )  -- Returns the total buffer length.
  LDataWriter:seek( pos )  -- Moves the write cursor to the given position.
  LDataWriter:tell( )  -- Returns the current write cursor position.
  LDataWriter:toBytes( )  -- Returns the buffer contents as a Lua string.
  LDataWriter:type( )  -- Returns the type name of this object.
  LDataWriter:typeOf( name )  -- Returns true if this object is of the given type.
  LDataWriter:writeBytes( )  -- Writes raw bytes from a Lua string.
  LDataWriter:writeF32LE( v )  -- Writes a 32-bit LE float.
  LDataWriter:writeF64LE( v )  -- Writes a 64-bit LE float.
  LDataWriter:writeI16LE( v )  -- Writes a signed 16-bit LE integer.
  LDataWriter:writeI32LE( v )  -- Writes a signed 32-bit LE integer.
  LDataWriter:writeI8( v )  -- Writes a signed 8-bit integer.
  LDataWriter:writeString( s )  -- Writes a length-prefixed UTF-8 string (4-byte LE length + bytes).
  LDataWriter:writeU16BE( v )  -- Writes an unsigned 16-bit BE integer.
  LDataWriter:writeU16LE( v )  -- Writes an unsigned 16-bit LE integer.
  LDataWriter:writeU32LE( v )  -- Writes an unsigned 32-bit LE integer.
  LDataWriter:writeU8( v )  -- Writes an unsigned 8-bit integer.
```

**`LRingBuffer`** methods:

> Lua-side fixed-capacity ring buffer that holds any Lua value.

```lua
  LRingBuffer:capacity( )  -- Returns the maximum number of elements the buffer can hold.
  LRingBuffer:clear( )  -- Removes all elements from the buffer, releasing their registry entries.
  LRingBuffer:isEmpty( )  -- Returns true if the buffer contains no elements.
  LRingBuffer:isFull( )  -- Returns true if the buffer has reached its capacity.
  LRingBuffer:len( )  -- Returns the number of elements currently in the buffer.
  LRingBuffer:peek( )  -- Returns the oldest element without removing it, or nil if empty.
  LRingBuffer:peekNewest( )  -- Returns the newest element without removing it, or nil if empty.
  LRingBuffer:pop( )  -- Removes and returns the oldest element, or nil if the buffer is empty.
  LRingBuffer:push( value )  -- Pushes a value onto the ring buffer.
  LRingBuffer:toTable( )  -- Returns all elements as an array table ordered oldest-first.
  LRingBuffer:type( )  -- Returns the type name of this object.
  LRingBuffer:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.image {#image}

> CPU-side image manipulation: pixel read/write, procedural generation.

```lua
  lurek.image.isCompressed( filename )  -- Returns true if the file at the given path is a DDS file.
  lurek.image.loadImage( filename )  -> ImageData  -- Loads an ImageData from a LIMG binary file.
  lurek.image.loadLayered( filename )  -> LayeredImage  -- Loads a LayeredImage from a LIMG binary file.
  lurek.image.newCompressedData( filename )  -> CompressedData  -- Loads compressed texture data from a DDS file.
  lurek.image.newImageData( ... )  -> ImageData  -- Creates a new blank ImageData or loads one from a file.
  lurek.image.newLayeredImage( width, height )  -> LayeredImage  -- Creates a new empty LayeredImage canvas with no layers.
  lurek.image.newPaletteLut( )  -> PaletteLut  -- Creates a new empty `PaletteLUT` used to remap colours in an image.
  lurek.image.newProvinceGrid( filename )  -> ProvinceGrid  -- Loads a province map PNG and builds an O(1) spatial index with adjacency data.
  lurek.image.saveImage( img_ud, filename )  -- Saves a flat ImageData to a LIMG binary file at the given path.
  lurek.image.savePNG( img_ud, filename )  -- Saves a flat ImageData as a PNG file at the given path.
```

**`LCompressedImageData`** methods:

> Lua-side wrapper around [`CompressedImageData`].

```lua
  LCompressedImageData:getDimensions( )  -> Dimensions  -- Returns the width and height of the base mip level.
  LCompressedImageData:getFormat( )  -> Format  -- Returns the compressed format name string.
  LCompressedImageData:getHeight( )  -> Height  -- Returns the height of the base mip level in pixels.
  LCompressedImageData:getMipmapCount( )  -> MipmapCount  -- Returns the number of mipmap levels stored.
  LCompressedImageData:getWidth( )  -> Width  -- Returns the width of the base mip level in pixels.
  LCompressedImageData:type( )  -- Returns the type name of this object.
  LCompressedImageData:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LImageData`** methods:

> RGBA pixel buffer for software image manipulation, pixel access, and encoding.

```lua
  LImageData:alphaMask( factor )  -- Scales every pixel's alpha channel by factor; use to fade an image in or out uniformly.
  LImageData:applyPaletteLut( lut_ud )  -- Applies a `PaletteLUT` to the image in place, replacing exact colour matches.
  LImageData:blit( src_ud, dst_x, dst_y )  -- Blits the source ImageData onto this image at (dst_x, dst_y) using Porter-Duff over.
  LImageData:blur( radius )  -> ImageData  -- Returns a new ImageData with a box blur applied using the given pixel radius.
  LImageData:brightness( factor )  -- Adjusts the brightness of every pixel by the given factor (< 1.0 darkens, > 1.0 brighte...
  LImageData:contrast( factor )  -- Adjusts the contrast of every pixel by the given factor (< 1.0 reduces, > 1.0 increases).
  LImageData:convolve( kernel_t, ksize )  -- Applies a custom NxN convolution kernel to the image and returns a new ImageData.
  LImageData:crop( x, y, w, h )  -> ImageData  -- Returns a new ImageData containing the rectangular sub-region at (x, y) of the given wi...
  LImageData:diff( other_ud )  -- Returns the sum of absolute per-channel pixel differences with another ImageData.
  LImageData:drawCircle( cx, cy, radius, r, g, b, a )  -- Draws a filled circle onto the image.
  LImageData:drawLine( x0, y0, x1, y1, r, g, b, a )  -- Draws a line using Bresenham's algorithm.
  LImageData:drawRect( x, y, w, h, r, g, b, a )  -- Draws a filled rectangle onto the image.
  LImageData:encode( format )  -- Encodes the image into a byte string in the specified format (currently "png").
  LImageData:fill( r, g, b, a )  -- Fills every pixel with the given solid RGBA colour, overwriting all existing content.
  LImageData:flipHorizontal( )  -- Flips the image left-to-right (mirror across vertical axis), modifying in place.
  LImageData:flipVertical( )  -- Flips the image top-to-bottom (mirror across horizontal axis), modifying in place.
  LImageData:gamma( gamma )  -- Applies gamma correction; values < 1.0 brighten shadows, > 1.0 darken them.
  LImageData:getDimensions( )  -> Dimensions  -- Returns the width and height of the image as two integers.
  LImageData:getHeight( )  -> Height  -- Returns the height of the image in pixels.
  LImageData:getPixel( x, y )  -> RGBA  -- Returns the RGBA colour components of the pixel at (x, y) as four integers (0-255).
  LImageData:getRegion( x, y, w, h )  -> Region  -- Returns a copy of the rectangular sub-region as a new ImageData.
  LImageData:getString( )  -> String  -- Returns the raw pixel bytes of the image as a Lua string.
  LImageData:getWidth( )  -> Width  -- Returns the width of the image in pixels.
  LImageData:grayscale( )  -- Converts the image to grayscale using luminance weights (BT.601).
  LImageData:invert( )  -- Inverts every colour channel (subtracts each R/G/B value from 255); alpha is preserved.
  LImageData:mapPixel( func )  -- Calls func(x, y, r, g, b, a) for each pixel and writes the returned RGBA back.
  LImageData:mapPixels( func )  -- Applies a function to every pixel in-place.
  LImageData:noise( amount )  -- Adds random noise to every pixel channel; amount controls the maximum per-channel pertu...
  LImageData:paste( src_ud, dx, dy )  -- Copies pixels from `source` onto this image starting at (dx, dy).
  LImageData:posterize( levels )  -- Reduces each channel to `levels` discrete steps, creating a flat poster-paint look.
  LImageData:resize( w, h )  -- Returns a bilinear-interpolated copy of the image at the given dimensions.
  LImageData:resizeNearest( new_w, new_h )  -> ImageData  -- Returns a new ImageData scaled to (new_w, new_h) using nearest-neighbour interpolation.
  LImageData:rotate90cw( )  -> ImageData  -- Returns a new ImageData rotated 90 degrees clockwise; the original is not modified.
  LImageData:saturation( factor )  -- Adjusts colour saturation; 0.0 produces grayscale, 1.0 is unchanged, > 1.0 boosts satur...
  LImageData:sepia( )  -- Applies a warm sepia tone to the image using standard sepia matrix weights.
  LImageData:setPixel( x, y, r, g, b, a )  -- Sets the RGBA colour of the pixel at (x, y); returns an error if coordinates are out of...
  LImageData:setRawData( bytes )  -- Replaces all pixel data from a raw RGBA byte string.
  LImageData:sharpen( )  -> ImageData  -- Returns a new ImageData with a sharpening convolution kernel applied.
  LImageData:threshold( value )  -- Converts the image to black-and-white: pixels above value become white, at or below bec...
  LImageData:tint( tr, tg, tb, factor )  -- Blends an RGB tint colour into every pixel, controlled by factor (0.0 = no change, 1.0 ...
  LImageData:type( )  -- Returns the type name of this object.
  LImageData:typeOf( name )  -- Returns true if this object is of the given type name.
```

**`LLayeredImage`** methods:

> Lua-side wrapper around [`LayeredImage`].

```lua
  LLayeredImage:addLayer( [name] )  -- Appends a new blank transparent layer on top and returns its 1-based index.
  LLayeredImage:getHeight( )  -> Height  -- Returns the canvas height shared by all layers.
  LLayeredImage:getLayer( index )  -> Layer  -- Returns a copy of the layer's pixel buffer as an ImageData.
  LLayeredImage:getName( index )  -> Name  -- Returns the name of a layer.
  LLayeredImage:getOpacity( index )  -> Opacity  -- Returns the opacity of a layer in [0.0, 1.0].
  LLayeredImage:getWidth( )  -> Width  -- Returns the canvas width shared by all layers.
  LLayeredImage:isVisible( index )  -- Returns whether a layer is visible.
  LLayeredImage:layerCount( )  -- Returns the number of layers in the stack.
  LLayeredImage:merge( )  -- Flattens all visible layers into a single ImageData using Porter-Duff "over" compositing.
  LLayeredImage:moveLayer( from_idx, to_idx )  -- Moves a layer from one position to another, shifting layers in between.
  LLayeredImage:removeLayer( index )  -- Removes the layer at the given 1-based index. Returns true on success.
  LLayeredImage:save( path )  -- Saves the layered image to a LIMG binary file at the given path.
  LLayeredImage:setLayer( index, img )  -- Replaces a layer's pixel buffer with a copy of the given ImageData.
  LLayeredImage:setName( index, name )  -- Renames the layer at the given index to the new name string.
  LLayeredImage:setOpacity( index, opacity )  -- Sets the opacity of a layer. Value is clamped to [0.0, 1.0].
  LLayeredImage:setVisible( index, visible )  -- Shows or hides a layer during compositing.
  LLayeredImage:swapLayers( a, b )  -- Swaps two layers by their 1-based indices, changing their compositing order.
  LLayeredImage:type( )  -- Returns the type name of this object.
  LLayeredImage:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LPaletteLUT`** methods:

> Lua-side wrapper around [`PaletteLUT`].

```lua
  LPaletteLUT:clear( )  -- Removes all colour mapping entries.
  LPaletteLUT:getColorCount( )  -> ColorCount  -- Returns the number of colour mapping entries.
  LPaletteLUT:setColor( fr, fg, fb, fa, tr, tg, tb, ta )  -- Appends a colour mapping entry to the palette: when a pixel exactly matching
  LPaletteLUT:type( )  -- Returns the type name of this object.
  LPaletteLUT:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LProvinceGrid`** methods:

> Lua-side wrapper around [`ProvinceGrid`].

```lua
  LProvinceGrid:adjacencies( )  -- Returns an array of adjacency records. Each record is {province_a, province_b, border_p...
  LProvinceGrid:getAt( x, y )  -> At  -- Returns the province ID at pixel coordinates (x, y). Returns 0 for background or out-of...
  LProvinceGrid:getHeight( )  -> Height  -- Returns the grid height in pixels.
  LProvinceGrid:getWidth( )  -> Width  -- Returns the grid width in pixels.
  LProvinceGrid:provinceCount( )  -- Returns the number of unique non-zero province IDs detected in the map.
  LProvinceGrid:type( )  -- Returns the type name of this object.
  LProvinceGrid:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.compute {#compute}

> Dense NdArray numerical computing: element ops, reductions, convolutions.

```lua
  lurek.compute.affine2d( tx, ty, angle_rad, sx, sy )  -- Creates a 3Ă—3 homogeneous affine matrix.
  lurek.compute.fft( samples )  -- Computes the discrete Fourier transform of a 1D real-valued sample array.
  lurek.compute.fftMagnitude( samples )  -- Returns the magnitude spectrum `|X[k]|` of a real-valued sample array.
  lurek.compute.fromTable( data, [shape], [dtype] )  -- Creates an array from a Lua table of numbers with optional shape and dtype.
  lurek.compute.gaussianKernel( size, sigma )  -- Creates a sizeĂ—size Gaussian kernel array.
  lurek.compute.ifft( freqs )  -- Computes the inverse discrete Fourier transform.
  lurek.compute.newArray( shape, [dtype] )  -> Array  -- Creates a zero-initialized array with the given shape and optional dtype.
  lurek.compute.ones( shape, [dtype] )  -- Creates a one-filled array with the given shape and optional dtype.
  lurek.compute.range( start, stop, [step], [dtype] )  -- Creates a 1D array from start to stop with optional step and dtype.
  lurek.compute.rotate2dMatrix( angle_rad )  -- Creates a 2Ă—2 rotation matrix for the given angle in radians.
  lurek.compute.zeros( shape, [dtype] )  -- Creates a zero-filled array with the given shape and optional dtype.
```

**`LArray`** methods:

> Lua-side wrapper around [`NdArray`].

```lua
  LArray:abs( )  -- Element-wise absolute value.
  LArray:all( )  -- Returns true if all elements are nonzero.
  LArray:any( )  -- Returns true if any element is nonzero.
  LArray:argmax( )  -- Returns the 1-based flat index of the maximum element.
  LArray:argmin( )  -- Returns the 1-based flat index of the minimum element.
  LArray:bitwiseAnd( other )  -- Bitwise AND of two Int32 arrays.
  LArray:bitwiseLShift( amount )  -- Bitwise left shift of an Int32 array.
  LArray:bitwiseNot( )  -- Bitwise NOT of an Int32 array.
  LArray:bitwiseOr( other )  -- Bitwise OR of two Int32 arrays.
  LArray:bitwiseRShift( amount )  -- Bitwise right shift of an Int32 array.
  LArray:bitwiseXor( other )  -- Bitwise XOR of two Int32 arrays.
  LArray:clamp( min, max )  -- Clamps each element to the given range.
  LArray:clone( )  -- Returns a deep copy of this array.
  LArray:convolve1d( kernel )  -- 1D convolution with a kernel array (full output).
  LArray:convolve2D( kernel )  -- 2D convolution with zero-padding.
  LArray:correlate1d( template )  -- 1D cross-correlation with a template array (valid output).
  LArray:countNonZero( )  -- Returns the count of nonzero elements.
  LArray:covariance( other )  -- Population covariance with another 1D array.
  LArray:cross2d( other )  -- Signed 2D cross product with another length-2 array.
  LArray:cumsum( )  -- Cumulative sum of all elements (flattened).
  LArray:diff( [order] )  -- Discrete difference applied `order` times.
  LArray:dilate( radius )  -- Morphological dilation with a diamond structuring element.
  LArray:dot( other )  -- Dot product of two 1D arrays.
  LArray:eigenPower( [max_iter], [tol] )  -- Computes the dominant eigenvalue and its eigenvector using power iteration.
  LArray:erode( radius )  -- Morphological erosion with a diamond structuring element.
  LArray:eval( expr )  -- Evaluate a Lua expression string element-wise, returning a new Array.
  LArray:fill( val )  -- Fills all elements with the given value in-place.
  LArray:floodFill( row, col, val )  -- Flood fill from a 1-based (row, col) with a new value.
  LArray:get( ... )  -- Returns the element at the given 1-based indices.
  LArray:getDataType( )  -> DataType  -- Returns the element data type name.
  LArray:getDimensions( )  -> Dimensions  -- Returns the number of dimensions.
  LArray:getRegion( row, col, rows, cols )  -> Region  -- Extracts a rectangular sub-region (1-based row, col).
  LArray:getShape( )  -> Shape  -- Returns the shape as a table of dimension sizes.
  LArray:getSize( )  -> Size  -- Returns the total number of elements.
  LArray:histogram( bins, [lo], [hi] )  -- Compute a histogram. Returns a table of {lo, hi, count} tables.
  LArray:isOnGPU( )  -- Returns false (CPU arrays only).
  LArray:linsolve( b )  -- Solve AÂ·x = b where this array is A (square [n,n]) and b is a 1D vector.
  LArray:luDecompose( )  -- Decomposes this square matrix into L and U factors with partial pivoting.
  LArray:map( func )  -- Apply a Lua callback element-wise, returning a new Array of the same shape.
  LArray:matmul( other )  -- Matrix multiplication of two 2D arrays.
  LArray:max( [axis] )  -- Maximum of all elements, or along an axis (1-based).
  LArray:mean( [axis] )  -- Mean of all elements, or along an axis (1-based).
  LArray:min( [axis] )  -- Minimum of all elements, or along an axis (1-based).
  LArray:neg( )  -> Array  -- Returns a new Array with every element negated (multiplied by â’1).
  LArray:normalizeRange( lo, hi )  -- Linearly rescale values to [out_min, out_max].
  LArray:normalizeVec( )  -- L2-normalise a 1D vector.
  LArray:outer( other )  -- Outer product of two 1D vectors â†’ 2D array [m, n].
  LArray:pearsonCorr( other )  -- Pearson correlation coefficient with another 1D array.
  LArray:percentile( p )  -- Compute the p-th percentile (0â€“100).
  LArray:pow( exp )  -- Raises each element to a scalar exponent.
  LArray:reduce( func, init )  -- Fold the array left-to-right with an accumulator.
  LArray:reshape( shape )  -- Returns a new array with the given shape and the same data.
  LArray:scan( func, init )  -- Running accumulation — like reduce but returns every intermediate result.
  LArray:set( ... )  -- Sets the element at the given 1-based indices to a value.
  LArray:setRegion( row, col, source )  -- Copies a source array into this array at the given 1-based position.
  LArray:sobel( )  -- Apply Sobel edge detection to a 2D array. Returns {gx=Array, gy=Array}.
  LArray:sqrt( )  -- Element-wise square root.
  LArray:sum( [axis] )  -- Sum of all elements, or along an axis (1-based).
  LArray:threshold( val )  -- Returns a mask array with 1.0 where elements >= val, else 0.0.
  LArray:toTable( )  -- Returns all elements as a flat table of numbers.
  LArray:transformPoints( pts )  -- Apply this 2Ă—2 or 3Ă—3 matrix to an [N,2] points array.
  LArray:transpose( )  -- Returns the transposed 2D array.
  LArray:type( )  -- Returns the type name "Array".
  LArray:typeOf( name )  -- Returns true when the given name matches "Array" or a parent type.
  LArray:where( mask, other )  -- Selects elements from this where mask is nonzero, else from other.
  LArray:zscore( )  -- Standardise values to zero mean and unit variance.
```

## lurek.dataframe {#dataframe}

> Tabular data: DataFrame construction, column ops, filtering, CSV.

```lua
  lurek.dataframe.fromBinary( s )  -- Deserializes a binary LVDF string into a DataFrame.
  lurek.dataframe.fromCSV( s )  -- Parses a CSV string into a DataFrame.
  lurek.dataframe.fromJSON( s )  -- Parses a JSON string into a DataFrame.
  lurek.dataframe.fromTable( rows )  -> DataFrame  -- Creates a DataFrame from an array of row tables.
  lurek.dataframe.fromVec( vf )  -- Converts a VecFrame back to a DataFrame.
  lurek.dataframe.newDataFrame( )  -> DataFrame  -- Creates a new empty DataFrame.
  lurek.dataframe.newDatabase( )  -> Database  -- Creates a new empty Database.
  lurek.dataframe.random( defs_tbl, n, [seed] )  -- Generates a DataFrame with random data from column definitions.
  lurek.dataframe.toVec( df )  -- Converts a DataFrame to a VecFrame for vectorized column operations.
```

**`LDataFrame`** methods:

> Lua-side wrapper around a shared [`DataFrame`].

```lua
  LDataFrame:addColumn( name, [default] )  -- Adds a new column with an optional default value.
  LDataFrame:addRow( [row_tbl] )  -- Adds a row from an optional table of name-value pairs, returns 1-based index.
  LDataFrame:addRowBatch( rows )  -- Add multiple rows at once from a table of row tables.
  LDataFrame:apply( col_val, func )  -- Applies a function to each value in a column, replacing cells with results.
  LDataFrame:clone( )  -- Returns a deep copy of this DataFrame.
  LDataFrame:columns( )  -- Returns a table of column names.
  LDataFrame:corr( col_a, col_b )  -- Pearson correlation coefficient between two numeric columns.
  LDataFrame:correlationMatrix( )  -- Compute a correlation matrix for all numeric columns.
  LDataFrame:count( )  -- Returns the row count (alias for nrows).
  LDataFrame:countBy( col )  -- Counts distinct values in a column, returns a DataFrame with value and count columns.
  LDataFrame:describe( )  -- Returns descriptive statistics for all numeric columns.
  LDataFrame:dropNil( col )  -- Removes rows where the given column is nil, returns a new DataFrame.
  LDataFrame:entropy( col )  -- Shannon entropy (bits) of the value distribution in a column.
  LDataFrame:fillNil( col, val )  -- Replaces nil values in a column with the given value.
  LDataFrame:filter( col, op, val )  -- Filters rows where column matches a condition, returns a new DataFrame.
  LDataFrame:getColumn( col )  -> Column  -- Returns all values in a column as a table.
  LDataFrame:getColumnAsF64( col )  -> ColumnAsF64  -- Return a numeric column as a Lua array of numbers (nils → 0/nan).
  LDataFrame:getRow( row )  -> Row  -- Returns a row as a table of name-value pairs.
  LDataFrame:getValue( row, col )  -> Value  -- Returns a single cell value.
  LDataFrame:groupAgg( group_col, agg_col, fn_name )  -- Aggregate agg_col grouped by group_col using the named function.
  LDataFrame:groupBy( col )  -- Groups rows by column value, returns a table of DataFrames keyed by value.
  LDataFrame:groupByObj( col )  -- Groups rows by column value, returns a GroupedFrame object supporting aggregate().
  LDataFrame:head( [n] )  -- Returns the first n rows (default 5).
  LDataFrame:join( )  -- Joins with another DataFrame on matching columns.
  LDataFrame:max( col )  -- Returns the maximum numeric value in a column.
  LDataFrame:mean( col )  -- Returns the mean of numeric values in a column.
  LDataFrame:median( col )  -- Returns the median of numeric values in a column.
  LDataFrame:merge( other )  -- Appends rows from another DataFrame in-place.
  LDataFrame:min( col )  -- Returns the minimum numeric value in a column.
  LDataFrame:modeVal( col )  -- Return the most frequent value in a column (nil if empty).
  LDataFrame:ncols( )  -- Returns the number of columns.
  LDataFrame:normalizeCol( col, out_min, out_max, name )  -- Add a min-max normalized column scaled to [out_min, out_max].
  LDataFrame:nrows( )  -- Returns the number of rows.
  LDataFrame:outliers( col, [threshold] )  -> DataFrame  -- Return a new DataFrame with only outlier rows (|z-score| > threshold).
  LDataFrame:pivot( row_col, col_col, val_col )  -- Creates a wide pivot table by reshaping rows into columns.
  LDataFrame:pivotTable( )  -- Reshapes a long-format DataFrame into wide format.
  LDataFrame:query( sql_str )  -- Executes a SQL query against this DataFrame.
  LDataFrame:rank( col, [order], [result_col] )  -> DataFrame  -- Returns a new DataFrame with a dense-rank column appended.
  LDataFrame:removeColumn( col )  -- Removes a column by name or index.
  LDataFrame:removeRow( row )  -- Removes a row by 1-based index.
  LDataFrame:rename( col, new_name )  -- Renames the column `old_name` to `new_name` in this DataFrame.
  LDataFrame:rollingMean( col, window, [result_col] )  -> DataFrame  -- Returns a new DataFrame with a rolling mean column appended.
  LDataFrame:rollingSum( col, window, [result_col] )  -> DataFrame  -- Returns a new DataFrame with a rolling sum column appended.
  LDataFrame:sample( n, [seed] )  -- Returns a random sample of n rows.
  LDataFrame:select( ... )  -- Selects a subset of columns, returns a new DataFrame.
  LDataFrame:setColumnFromF64( col, values )  -- Set a numeric column from a Lua array of numbers.
  LDataFrame:setValue( row, col, val )  -- Sets a single cell value.
  LDataFrame:slice( start, end )  -- Returns rows from start to end (1-based, inclusive).
  LDataFrame:sort( col, [ascending] )  -- Sorts by column, returns a new DataFrame.
  LDataFrame:stddev( col )  -- Returns the population standard deviation of numeric values in a column.
  LDataFrame:sum( col )  -- Returns the sum of numeric values in a column.
  LDataFrame:tail( [n] )  -- Returns the last n rows (default 5).
  LDataFrame:toBinary( )  -- Serializes this DataFrame to a binary LVDF string.
  LDataFrame:toCSV( )  -- Serializes this DataFrame to a CSV string.
  LDataFrame:toJSON( )  -- Serializes this DataFrame to a JSON string.
  LDataFrame:toString( )  -- Returns a formatted string table representation.
  LDataFrame:toTable( )  -- Converts this DataFrame to a Lua table of row tables.
  LDataFrame:type( )  -- Returns the type name of this object.
  LDataFrame:typeOf( name )  -- Returns true if this object is of the given type.
  LDataFrame:unique( col )  -- Returns unique values in a column as a table.
  LDataFrame:variance( col )  -- Returns the population variance of numeric values in a column.
  LDataFrame:withCumsum( col, name )  -- Add a cumulative-sum column.
  LDataFrame:withEval( col_name, expr )  -> DataFrame  -- Returns a new DataFrame with an additional computed column named `col_name`.
  LDataFrame:withPctChange( col, name )  -- Add a percent-change-from-previous-row column.
  LDataFrame:withRank( col, [asc], name )  -- Add a rank column (1-based, ties averaged).
  LDataFrame:withRollingMax( col, window, name )  -- Add a rolling maximum column.
  LDataFrame:withRollingMean( col, window, name )  -- Add a rolling mean column. Rows with insufficient history get nil.
  LDataFrame:withRollingMin( col, window, name )  -- Add a rolling minimum column.
  LDataFrame:withRollingSum( col, window, name )  -- Add a rolling sum column.
  LDataFrame:zscoreCol( col, name )  -- Add a z-score column for the given numeric column.
```

**`LDatabase`** methods:

> Lua-side wrapper around a shared [`Database`].

```lua
  LDatabase:addTable( name, df_ud )  -- Adds or replaces a table by cloning the given DataFrame.
  LDatabase:clear( )  -- Drops every table from this in-memory database, leaving it empty.
  LDatabase:getTable( name )  -> Table  -- Returns a copy of a table by name, or nil if not found.
  LDatabase:hasTable( name )  -- Returns true if a table with the given name exists.
  LDatabase:listTables( )  -- Returns a table of all table names.
  LDatabase:merge( other )  -- Merges all tables from another Database into this one.
  LDatabase:query( sql_str )  -- Executes a SQL query against the database tables.
  LDatabase:removeTable( name )  -- Drops the named table from this in-memory database if it exists.
  LDatabase:tableCount( )  -- Returns the number of tables.
  LDatabase:toJSON( )  -- Serializes all tables to a JSON object string.
  LDatabase:type( )  -- Returns the type name of this object.
  LDatabase:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LGroupedFrame`** methods:

> Lua-side wrapper around a grouped result from [`DataFrame::group_by`].

```lua
  LGroupedFrame:aggregate( col_name, func )  -- Apply a Lua function to aggregate a column's values per group.
  LGroupedFrame:type( )  -- Returns the type name of this object.
  LGroupedFrame:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LVecFrame`** methods:

> Thin Lua wrapper around a [`VecFrame`]: typed-column vectorized DataFrame.

```lua
  LVecFrame:applyMask( mask_tbl )  -> VecFrame  -- Return a new VecFrame containing only the rows where mask[i] is true.
  LVecFrame:colAbs( col )  -- Apply absolute value to every element of a Float64 column.
  LVecFrame:colAdd( col, val )  -- Add a scalar to every element of a Float64 column.
  LVecFrame:colCast( col, dtype )  -- Cast a column to a new dtype: "float64", "int64", or "text".
  LVecFrame:colCeil( col )  -- Apply ceiling to every element of a Float64 column.
  LVecFrame:colClamp( col, min_val, max_val )  -- Clamp every element of a Float64 column to [min, max].
  LVecFrame:colDiv( col, val )  -- Divide every element of a Float64 column by a scalar.
  LVecFrame:colFloor( col )  -- Apply floor to every element of a Float64 column.
  LVecFrame:colMul( col, val )  -- Multiply every element of a Float64 column by a scalar.
  LVecFrame:colNeg( col )  -- Negate every element of a Float64 column.
  LVecFrame:colOp( out_col, left_col, op, right_col )  -- Compute out[i] = left[i] op right[i] for every row.
  LVecFrame:colSqrt( col )  -- Apply square root to every element of a Float64 column.
  LVecFrame:colSub( col, val )  -- Subtract a scalar from every element of a Float64 column.
  LVecFrame:colType( col )  -- Return the dtype name of a column: "float64", "int64", "bool", or "text".
  LVecFrame:columns( )  -- Return a table of column names.
  LVecFrame:filterMask( col, cmp_op, val )  -- Build a boolean row mask: mask[i] = col[i] cmp_op val.
  LVecFrame:ncols( )  -- Return the number of columns.
  LVecFrame:nrows( )  -- Return the number of rows.
  LVecFrame:parReduce( cols_tbl, op )  -- Reduce multiple columns in parallel, returning {col → value} table.
  LVecFrame:parScalarOp( cols_tbl, op, val )  -- Apply a scalar op in parallel to multiple Float64 columns.
  LVecFrame:reduce( col, op )  -- Reduce an entire numeric column to a single value.
  LVecFrame:toDataFrame( )  -- Convert this VecFrame back to a DataFrame.
  LVecFrame:type( )  -- Returns the type name of this object.
  LVecFrame:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.graph {#graph}

> Directed graph: nodes, edges, item flow, Dijkstra pathfinding.

```lua
  lurek.graph.newGraph( )  -> Graph  -- Creates a new empty directed graph for item flow simulation.
```

**`LGraph`** methods:

> Lua wrapper around a directed `Graph` with event callback registry.

```lua
  LGraph:addEdge( from_ud, to_ud, [edge_type] )  -- Adds a directed edge between two nodes and returns its handle.
  LGraph:addItem( item_ud, node_ud )  -- Places an item at a node.
  LGraph:addNode( [node_type], [capacity] )  -- Adds a node and returns its handle.
  LGraph:astar( from_node, to_node )  -- Finds the shortest path between two nodes using A*.
  LGraph:colorGraph( )  -- Assigns each node the smallest non-negative integer colour not shared with any
  LGraph:createItem( [item_type], [decay_time] )  -> Item  -- Creates a new unplaced item and returns its handle.
  LGraph:findPath( from_ud, to_ud )  -- Finds the shortest path between two nodes using Dijkstra.
  LGraph:findPathForItem( item_ud, from_ud, to_ud )  -- Finds the shortest path for a specific item, filtering by item type.
  LGraph:getComponents( )  -> Components  -- Returns weakly connected components as a table of tables of Node handles.
  LGraph:getDistance( from_ud, to_ud )  -> Distance  -- Returns the shortest path distance, or nil if unreachable.
  LGraph:getEdgeBetween( from_ud, to_ud )  -> EdgeBetween  -- Returns the edge between two nodes, or nil if none exists.
  LGraph:getEdgeCount( )  -> EdgeCount  -- Returns the number of edges in the graph.
  LGraph:getEdges( )  -> Edges  -- Returns a table of all Edge handles.
  LGraph:getItemCount( )  -> ItemCount  -- Returns the number of items in the graph.
  LGraph:getItems( )  -> Items  -- Returns a table of all GraphItem handles.
  LGraph:getNeighbors( node_ud )  -> Neighbors  -- Returns a table of direct neighbor Node handles.
  LGraph:getNodeCount( )  -> NodeCount  -- Returns the number of nodes in the graph.
  LGraph:getNodes( )  -> Nodes  -- Returns a table of all Node handles.
  LGraph:getReachable( from_ud, [max_dist] )  -> Reachable  -- Returns a table of Node handles reachable from the given node.
  LGraph:getStats( )  -> Stats  -- Returns a statistics snapshot table.
  LGraph:hasCycle( )  -- Returns true if the graph contains a directed cycle.
  LGraph:hasEdge( edge_ud )  -- Returns true if the edge exists in the graph.
  LGraph:hasItem( item_ud )  -- Returns true if the item exists in the graph.
  LGraph:hasNode( node_ud )  -- Returns true if the node exists in the graph.
  LGraph:isBipartite( )  -- Returns `true` when the graph can be 2-coloured (bipartite check via BFS).
  LGraph:mst( )  -- Returns edge IDs forming a minimum spanning tree (Kruskal, undirected view).
  LGraph:on( event_name, func )  -- Registers a callback for a graph simulation event.
  LGraph:processDemand( )  -- Processes all supply/demand declarations and fires event callbacks.
  LGraph:removeEdge( edge_ud )  -- Removes an edge from the graph.
  LGraph:removeItem( item_ud )  -- Removes an item from the graph entirely.
  LGraph:removeNode( node_ud )  -- Removes a node from the graph.
  LGraph:sendItem( item_ud, edge_ud )  -- Sends an item onto an edge to begin transit.
  LGraph:step( )  -- Runs one discrete simulation step and fires event callbacks.
  LGraph:tickParallel( dt )  -- Advances simulation by dt seconds using a parallelised decay phase.
  LGraph:topologicalSort( )  -- Returns a topologically sorted table of Node handles, or nil if a cycle exists.
  LGraph:type( )  -- Returns the type name of this object.
  LGraph:typeOf( name )  -- Returns true if this object is of the given type.
  LGraph:update( dt )  -- Advances simulation by dt seconds and fires event callbacks.
```

**`LGraphEdge`** methods:

> Lua handle for an edge inside a `Graph`.

```lua
  LGraphEdge:addAllowedType( t )  -- Adds an item type to the edge allow-list.
  LGraphEdge:clearAllowedTypes( )  -- Clears the edge allow-list so all item types are permitted.
  LGraphEdge:getCapacity( )  -> Capacity  -- Returns the edge capacity (-1 = unlimited).
  LGraphEdge:getCooldown( )  -> Cooldown  -- Returns the cooldown duration in seconds.
  LGraphEdge:getFrom( )  -> From  -- Returns the source node handle.
  LGraphEdge:getItemsInTransit( )  -> ItemsInTransit  -- Returns a table of GraphItem handles currently in transit on this edge.
  LGraphEdge:getSpeedModifier( )  -> SpeedModifier  -- Returns the speed modifier applied to items in transit.
  LGraphEdge:getThroughput( )  -> Throughput  -- Returns items per second this edge can transfer.
  LGraphEdge:getTo( )  -> To  -- Returns the destination node handle.
  LGraphEdge:getTravelTime( )  -> TravelTime  -- Returns the travel time in seconds for items on this edge.
  LGraphEdge:getType( )  -> Type  -- Returns the edge type string.
  LGraphEdge:getWeight( )  -> Weight  -- Returns the pathfinding weight of this edge.
  LGraphEdge:isActive( )  -- Returns true if the edge is active.
  LGraphEdge:isBidirectional( )  -- Returns true if items can travel the edge in either direction.
  LGraphEdge:isItemTypeAllowed( t )  -- Returns true if the given item type is allowed on this edge.
  LGraphEdge:isOnCooldown( )  -- Returns true if the edge is currently on cooldown.
  LGraphEdge:removeAllowedType( t )  -- Removes an item type from the edge allow-list.
  LGraphEdge:setActive( a )  -- Sets the active state of this edge.
  LGraphEdge:setBidirectional( b )  -- Sets whether items can travel the edge in either direction.
  LGraphEdge:setCapacity( c )  -- Sets the edge capacity (-1 = unlimited).
  LGraphEdge:setCooldown( c )  -- Sets the cooldown duration in seconds.
  LGraphEdge:setSpeedModifier( m )  -- Sets the speed modifier applied to items in transit.
  LGraphEdge:setThroughput( t )  -- Sets items per second this edge can transfer.
  LGraphEdge:setTravelTime( t )  -- Sets the travel time in seconds for items on this edge.
  LGraphEdge:setType( t )  -- Sets the edge type string.
  LGraphEdge:setWeight( w )  -- Sets the pathfinding weight of this edge.
  LGraphEdge:type( )  -- Returns the type name "GraphEdge".
  LGraphEdge:typeOf( name )  -- Returns true when the given name matches "GraphEdge" or a parent type.
```

**`LGraphItem`** methods:

> Lua handle for an item inside a `Graph`.

```lua
  LGraphItem:getDecayTime( )  -> DecayTime  -- Returns the decay time in seconds (-1 = immortal).
  LGraphItem:getPosition( )  -> Position  -- Returns the item position: node userdata if at a node, (edge, progress)
  LGraphItem:getPriority( )  -> Priority  -- Returns the item priority.
  LGraphItem:getRemainingLife( )  -> RemainingLife  -- Returns the remaining life in seconds.
  LGraphItem:getType( )  -> Type  -- Returns the item type string.
  LGraphItem:isAlive( )  -- Returns true if the item is alive.
  LGraphItem:kill( )  -- Marks this graph item as dead so it is removed on the next cleanup pass.
  LGraphItem:setDecayTime( t )  -- Sets the decay time in seconds (-1 = immortal).
  LGraphItem:setPriority( p )  -- Sets the scheduling priority; higher values are processed before lower ones.
  LGraphItem:setType( t )  -- Sets the item type string.
  LGraphItem:type( )  -- Returns the type name of this object.
  LGraphItem:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LGraphNode`** methods:

> Lua handle for a node inside a `Graph`.

```lua
  LGraphNode:addDemand( item_type, quantity, [priority] )  -- Declares a demand for the given item type, quantity, and priority.
  LGraphNode:addSupply( item_type, quantity )  -- Declares a supply of the given item type and quantity at this node.
  LGraphNode:addTag( tag )  -- Attaches a string tag to this node for fast group queries.
  LGraphNode:clearAllConversions( )  -- Removes all conversion rules from this node.
  LGraphNode:clearConversion( in_type )  -- Removes the conversion rule for the given input type.
  LGraphNode:clearDemands( )  -- Removes all demand declarations from this node.
  LGraphNode:clearSupplies( )  -- Removes all supply declarations from this node.
  LGraphNode:clearTags( )  -- Removes all tags from this node.
  LGraphNode:dequeue( )  -- Pops the next item from the node queue, or nil if empty.
  LGraphNode:enqueue( item_ud )  -- Pushes an item into the node queue.
  LGraphNode:getCapacity( )  -> Capacity  -- Returns the node capacity (-1 = unlimited).
  LGraphNode:getEdges( [dir] )  -> Edges  -- Returns a table of Edge handles connected to this node.
  LGraphNode:getFlowMode( )  -> FlowMode  -- Returns the flow mode as a string.
  LGraphNode:getItemCount( )  -> ItemCount  -- Returns the number of items currently at this node.
  LGraphNode:getItems( )  -> Items  -- Returns a table of GraphItem handles at this node.
  LGraphNode:getOverflowPolicy( )  -> OverflowPolicy  -- Returns the overflow policy as a string.
  LGraphNode:getProcessTime( )  -> ProcessTime  -- Returns the processing time in seconds.
  LGraphNode:getPullFilter( )  -> PullFilter  -- Returns the pull filter string, or nil if unset.
  LGraphNode:getPullRate( )  -> PullRate  -- Returns items per second this node pulls.
  LGraphNode:getPushFilter( )  -> PushFilter  -- Returns the push filter string, or nil if unset.
  LGraphNode:getPushRate( )  -> PushRate  -- Returns items per second this node pushes.
  LGraphNode:getQueueCapacity( )  -> QueueCapacity  -- Returns the queue capacity (-1 = unlimited).
  LGraphNode:getQueueSize( )  -> QueueSize  -- Returns the number of items currently in the queue.
  LGraphNode:getTags( )  -> Tags  -- Returns a table of tag strings on this node.
  LGraphNode:getType( )  -> Type  -- Returns the node type string.
  LGraphNode:hasTag( tag )  -- Returns true if this node has the given tag.
  LGraphNode:isActive( )  -- Returns true if the node is active.
  LGraphNode:isFull( )  -- Returns true if the node has reached its capacity.
  LGraphNode:isQueueEnabled( )  -- Returns true if the node queue is enabled.
  LGraphNode:removeDemand( item_type )  -- Removes the demand declaration for the given item type.
  LGraphNode:removeSupply( item_type )  -- Removes the supply declaration for the given item type.
  LGraphNode:removeTag( tag )  -- Removes a tag from this node.
  LGraphNode:setActive( a )  -- Sets the active state of this node.
  LGraphNode:setCapacity( c )  -- Sets the node capacity (-1 = unlimited).
  LGraphNode:setConversion( )  -- Adds or replaces a conversion rule on this node.
  LGraphNode:setFlowMode( m )  -- Sets the flow mode from a string.
  LGraphNode:setOverflowPolicy( p )  -- Sets the overflow policy from a string.
  LGraphNode:setProcessTime( t )  -- Sets the processing time in seconds.
  LGraphNode:setPullFilter( [f] )  -- Sets the pull filter string, or nil to clear.
  LGraphNode:setPullRate( r )  -- Sets items per second this node pulls.
  LGraphNode:setPushFilter( [f] )  -- Sets the push filter string, or nil to clear.
  LGraphNode:setPushRate( r )  -- Sets items per second this node pushes.
  LGraphNode:setQueueCapacity( c )  -- Sets the queue capacity (-1 = unlimited).
  LGraphNode:setQueueEnabled( e )  -- Enables or disables the node queue.
  LGraphNode:setType( t )  -- Sets the node type string.
  LGraphNode:type( )  -- Returns the type name "GraphNode".
  LGraphNode:typeOf( name )  -- Returns true when the given name matches "GraphNode" or a parent type.
```

## lurek.minimap {#minimap}

> Minimap rendering from tile data.

```lua
  lurek.minimap.newMinimap( grid_w, grid_h, [display_w], [display_h] )  -> Minimap  -- Creates a new grid-based minimap.
```

**`LMinimap`** methods:

> Lua-side wrapper around a [`Minimap`].

```lua
  LMinimap:addMarker( )  -- Adds a persistent marker and returns its auto-assigned ID.
  LMinimap:addObjectType( name, r, g, b, [a] )  -- Registers a new object type and returns its 1-based index.
  LMinimap:addPing( )  -- Adds an animated ping at grid coordinates with a duration and optional color.
  LMinimap:clearMarkerAnimation( id )  -- Removes the animation from a marker, reverting it to static.
  LMinimap:clearObjects( )  -- Removes all tracked objects.
  LMinimap:clearOverlay( )  -- Removes all custom geometry from the minimap overlay.
  LMinimap:clearPath( [id] )  -- Removes a displayed path. If id is nil, all paths are removed.
  LMinimap:clearViewportRect( )  -- Clears the viewport rectangle overlay.
  LMinimap:drawLine( x1, y1, x2, y2, color_tbl )  -- Draws a custom line segment on the minimap overlay.
  LMinimap:drawRect( x, y, w, h, color_tbl )  -- Draws a custom rectangle on the minimap overlay.
  LMinimap:drawToImage( pixel_size )  -- Renders the minimap grid to a CPU ImageData.
  LMinimap:getCenter( )  -> Center  -- Returns the center coordinates as x, y.
  LMinimap:getCenterX( )  -> CenterX  -- Returns the center X coordinate.
  LMinimap:getCenterY( )  -> CenterY  -- Returns the center Y coordinate.
  LMinimap:getColorMode( )  -> ColorMode  -- Returns the current color mode as a string.
  LMinimap:getDisplayHeight( )  -> DisplayHeight  -- Returns the display height in pixels.
  LMinimap:getDisplaySize( )  -> DisplaySize  -- Returns the display width and height as two values.
  LMinimap:getDisplayWidth( )  -> DisplayWidth  -- Returns the display width in pixels.
  LMinimap:getFogColor( )  -> FogColor  -- Returns the fog overlay color as r, g, b, a.
  LMinimap:getFogLevel( x, y )  -> FogLevel  -- Returns the fog level at a 1-based grid position (0=hidden, 1=explored, 2=visible).
  LMinimap:getGridHeight( )  -> GridHeight  -- Returns the grid height in cells.
  LMinimap:getGridSize( )  -> GridSize  -- Returns the grid width and height as two values.
  LMinimap:getGridWidth( )  -> GridWidth  -- Returns the grid width in cells.
  LMinimap:getHoverInfo( sx, sy, mx, my )  -> HoverInfo  -- Returns hover tooltip text for the element under screen coordinates, or nil.
  LMinimap:getLayer( )  -> Layer  -- Returns the index of the currently active render layer.
  LMinimap:getMarkerCount( )  -> MarkerCount  -- Returns the number of markers.
  LMinimap:getMarkerDescription( id )  -> MarkerDescription  -- Returns the description of a marker, or nil.
  LMinimap:getObjectCount( )  -> ObjectCount  -- Returns the number of tracked objects.
  LMinimap:getObjectTypeCount( )  -> ObjectTypeCount  -- Returns the number of registered object types.
  LMinimap:getOwnerColor( owner )  -> OwnerColor  -- Returns the display color for an owner/faction as r, g, b, a.
  LMinimap:getPingCount( )  -> PingCount  -- Returns the number of active pings.
  LMinimap:getTerrain( x, y )  -> Terrain  -- Returns the terrain type at a 1-based grid position.
  LMinimap:getTerrainColor( terrain_type )  -> TerrainColor  -- Returns the display color for a terrain type as r, g, b, a.
  LMinimap:getTileDescription( type_id )  -> TileDescription  -- Returns the hover tooltip string for a terrain type ID, or nil.
  LMinimap:getViewportColor( )  -> ViewportColor  -- Returns the viewport rectangle color as r, g, b, a.
  LMinimap:getViewportRect( )  -> ViewportRect  -- Returns the viewport rectangle as x, y, w, h or nil if not set.
  LMinimap:getZoom( )  -> Zoom  -- Returns the current zoom level.
  LMinimap:gridToScreen( gx, gy, mx, my )  -- Converts grid coordinates to screen coordinates.
  LMinimap:hasMarker( id )  -- Returns whether a marker with the given ID exists.
  LMinimap:isAntiAlias( )  -- Returns whether anti-aliasing is enabled.
  LMinimap:isClickable( )  -- Returns whether this minimap responds to click hit-testing.
  LMinimap:isFogEnabled( )  -- Returns whether fog of war is enabled.
  LMinimap:isObjectTypeVisible( type_idx )  -- Returns whether an object type (1-based index) is visible.
  LMinimap:isViewportVisible( )  -- Returns whether the viewport rectangle is visible.
  LMinimap:removeMarker( id )  -- Removes the minimap marker with the given integer ID, if present.
  LMinimap:removeObject( id )  -- Removes a tracked object by ID.
  LMinimap:render( [x], [y] )  -- Renders the minimap to the screen at the given position.
  LMinimap:screenToGrid( sx, sy, mx, my )  -- Converts screen coordinates to grid coordinates.
  LMinimap:setAntiAlias( enabled )  -- Sets whether anti-aliasing is enabled.
  LMinimap:setCenter( x, y )  -- Sets the center of the minimap view in grid coordinates.
  LMinimap:setClickable( enabled )  -- Sets whether this minimap responds to click hit-testing.
  LMinimap:setColorMode( mode )  -- Sets the color mode ("terrain" or "political").
  LMinimap:setDisplaySize( w, h )  -- Sets the display size in pixels.
  LMinimap:setFogColor( r, g, b, [a] )  -- Sets the fog overlay color.
  LMinimap:setFogData( data )  -- Sets the entire fog grid from a flat 1-based table (0=hidden, 1=explored, 2=visible).
  LMinimap:setFogEnabled( enabled )  -- Enables or disables fog of war.
  LMinimap:setFogLevel( x, y, level )  -- Sets the fog level at a 1-based grid position (0=hidden, 1=explored, 2=visible).
  LMinimap:setLayer( layer )  -- Switches the minimap's active render layer (0-based index).
  LMinimap:setLayerData( layer, data_tbl )  -- Stores tile data for a specific layer index.
  LMinimap:setMarkerAnimation( id, anim_type, speed )  -- Attaches an animation to a marker. Does nothing if the ID does not exist.
  LMinimap:setObject( id, x, y, type_idx, [owner] )  -- Sets or updates a tracked object on the minimap.
  LMinimap:setObjectTypeVisible( type_idx, visible )  -- Sets whether an object type (1-based index) is visible.
  LMinimap:setOwnerColor( owner, r, g, b, [a] )  -- Sets the display color for an owner/faction.
  LMinimap:setTerrain( x, y, terrain_type )  -- Sets the terrain type at a 1-based grid position.
  LMinimap:setTerrainColor( terrain_type, r, g, b, [a] )  -- Sets the display color for a terrain type.
  LMinimap:setTerrainData( data )  -- Sets terrain types from a flat 1-based Lua table of integers (row-major).
  LMinimap:setTileDescription( type_id, desc )  -- Sets a hover tooltip string for a terrain type ID.
  LMinimap:setViewportColor( r, g, b, [a] )  -- Sets the viewport rectangle color.
  LMinimap:setViewportRect( x, y, w, h )  -- Sets the viewport rectangle overlay in grid coordinates.
  LMinimap:setViewportVisible( visible )  -- Sets whether the viewport rectangle is visible.
  LMinimap:setZoom( zoom )  -- Sets the zoom level (minimum 0.1).
  LMinimap:showPath( points_tbl, color_tbl )  -- Displays a pathfinding route on the minimap and returns its path ID.
  LMinimap:type( )  -- Returns the type name of this object.
  LMinimap:typeOf( name )  -- Returns true if this object is of the given type.
  LMinimap:update( dt )  -- Advances time-based effects by dt seconds (expires pings).
```

## lurek.save {#save}

> Save/load game state: named slots, versioned tables, autosave.

```lua
  lurek.save.newSaveManager( )  -> SaveManager  -- Creates a new SaveManager for slot-based save/load operations.
```

**`LSaveManager`** methods:

> Lua-side wrapper around [`SaveManager`] with per-module callback storage.

```lua
  LSaveManager:addMigration( from_ver, func )  -- Registers a migration function for upgrading from a schema version
  LSaveManager:collect( )  -- Collects data from all registered collectors into a table with metadata
  LSaveManager:delete( slot )  -- Deletes a save file for the given slot.
  LSaveManager:disableAutoSave( )  -- Disables automatic periodic saving; manual `write()` calls still work.
  LSaveManager:enableAutoSave( interval, slot )  -- Enables auto-save with a given interval and target slot
  LSaveManager:exists( slot )  -- Returns whether a save file exists for the given slot.
  LSaveManager:getSchemaVersion( )  -> SchemaVersion  -- Returns the current schema version
  LSaveManager:getSlotInfo( slot )  -> SlotInfo  -- Returns metadata for a single slot, or nil if not found.
  LSaveManager:getSlots( )  -> Slots  -- Returns a list of all save slots with metadata.
  LSaveManager:getSummary( )  -> Summary  -- Returns the current summary string
  LSaveManager:isCompressed( )  -- Returns whether compression is currently enabled.
  LSaveManager:isDirty( )  -- Returns whether data has been modified since the last save or load
  LSaveManager:load( slot )  -- Loads data from a slot file, applies migrations, and restores.
  LSaveManager:markDirty( )  -- Marks data as modified since the last save or load
  LSaveManager:onAfterLoad( func )  -- Registers a callback that fires after every successful load operation.
  LSaveManager:onBeforeSave( func )  -- Registers a callback that fires before every save operation.
  LSaveManager:register( name, collect_fn, restore_fn )  -- Registers a named module with collector and restorer callbacks
  LSaveManager:reset( )  -- Resets all state, removing callbacks and clearing the manager
  LSaveManager:restore( data )  -- Restores data from a table, applying migrations and calling restorers
  LSaveManager:save( slot )  -- Collects data and writes it to a slot file.
  LSaveManager:setCompress( enabled )  -- Enables or disables LZ4 compression for saved data
  LSaveManager:setSchemaVersion( version )  -- Sets the current schema version for new saves
  LSaveManager:setSummary( summary )  -- Sets the summary string included in save metadata
  LSaveManager:type( )  -- Returns the type name of this object.
  LSaveManager:typeOf( name )  -- Returns true if this object is of the given type.
  LSaveManager:unregister( name )  -- Removes a named module and its callbacks
  LSaveManager:update( dt )  -- Advances the auto-save timer, returning the slot name if a save should trigger
```

## lurek.log {#log}

> Structured game logging: levels, sinks, log file output.

```lua
  lurek.log.addSink( config )  -- Registers a new output sink. Returns its numeric id.
  lurek.log.clearSinks( )  -- Removes all registered sinks (the default stderr channel is unaffected).
  lurek.log.debug( message, [tag] )  -- Emits a debug-severity log message. Also dispatches to configured sinks.
  lurek.log.debug_fields( message, fields_tbl )  -- Emits a debug structured log message. Shorthand for `struct("debug", ...)`.
  lurek.log.error( message, [tag] )  -- Emits an error-severity log message. Also dispatches to configured sinks.
  lurek.log.error_fields( message, fields_tbl )  -- Emits an error structured log message. Shorthand for `struct("error", ...)`.
  lurek.log.flushFile( id )  -- Flushes the OS write buffer for a file sink.
  lurek.log.getLevel( )  -> Level  -- Returns the name of the currently active minimum log level.
  lurek.log.info( message, [tag] )  -- Emits an info-severity log message. Also dispatches to configured sinks.
  lurek.log.info_fields( message, fields_tbl )  -- Emits an info structured log message. Shorthand for `struct("info", ...)`.
  lurek.log.listSinks( )  -- Returns a table describing all registered sinks.
  lurek.log.print( level, message, [tag] )  -- Emits a log message at the specified level. Also dispatches to sinks.
  lurek.log.readMemory( id, [drain] )  -- Reads entries from a memory sink. If drain=true the buffer is cleared.
  lurek.log.removeSink( id )  -- Removes a sink by id. Returns true if one was removed.
  lurek.log.setLevel( level )  -- Sets the minimum severity level for the default log channel.
  lurek.log.struct( level_str, message, fields_tbl )  -- Emits a structured log message with key-value fields.
  lurek.log.warn( message, [tag] )  -- Emits a warn-severity log message. Also dispatches to configured sinks.
  lurek.log.warn_fields( message, fields_tbl )  -- Emits a warn structured log message. Shorthand for `struct("warn", ...)`.
```

## lurek.mods {#mods}

> Mod loading: folder scanning, dependency resolution, hot-reload.

```lua
  lurek.mods.checkApiVersion( mod_ud, host_version )  -- Checks whether a mod's required `api_version` is compatible with the given `host_version`.
  lurek.mods.newMod( info )  -> Mod  -- Creates a new Mod from an info table with at least an `id` field.
  lurek.mods.newModManager( )  -> ModManager  -- Creates a new empty ModManager.
  lurek.mods.newRegistry( )  -> Registry  -- Creates a new empty ContentRegistry for mod-contributed assets.
```

**`LContentRegistry`** methods:

> A typed content registry for mod-contributed assets and objects.

```lua
  LContentRegistry:get( type_name, id )  -- Retrieve a content entry.
  LContentRegistry:getAll( type_name )  -> All  -- Get all entries for a type.
  LContentRegistry:getTypes( )  -> Types  -- Get all registered type names.
  LContentRegistry:register( type_name, id, obj )  -- Register a content entry.
  LContentRegistry:registerType( type_name )  -- Register a new content type.
  LContentRegistry:type( )  -- Returns the type name of this object.
  LContentRegistry:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LMod`** methods:

> Lua-side wrapper around [`ModInfo`] with per-mod hook and config storage.

```lua
  LMod:getApiVersion( )  -> ApiVersion  -- Returns the required engine API version string, or nil if not set
  LMod:getAuthor( )  -> Author  -- Returns the author name string from this mod's metadata manifest
  LMod:getCapabilities( )  -> Capabilities  -- Returns an array of declared capability flags
  LMod:getConfig( )  -> Config  -- Returns the stored config value, or nil
  LMod:getConfigSchema( )  -> ConfigSchema  -- Returns the config schema as an array of `{key, type, default}` tables.
  LMod:getDependencies( )  -> Dependencies  -- Returns the list of required mod IDs
  LMod:getDescription( )  -> Description  -- Returns the mod description
  LMod:getHook( name )  -> Hook  -- Returns the hook function for the given name, or nil
  LMod:getHookNames( )  -> HookNames  -- Returns an array of registered hook names
  LMod:getId( )  -> Id  -- Returns the unique mod identifier
  LMod:getName( )  -> Name  -- Returns the localized or human-readable display name of the mod.
  LMod:getPriority( )  -> Priority  -- Returns the load-order priority
  LMod:getVersion( )  -> Version  -- Returns the version string
  LMod:hasHook( name )  -- Returns whether a hook with the given name exists
  LMod:isEnabled( )  -- Returns whether the mod is enabled
  LMod:isLoaded( )  -- Returns whether the mod has been loaded
  LMod:releaseRefs( )  -- Releases all hook and config registry references
  LMod:setApiVersion( api_version )  -- Sets the required engine API version string
  LMod:setCapabilities( caps )  -- Replaces the capability list with the given array of strings
  LMod:setConfig( value )  -- Stores an arbitrary config value for this mod
  LMod:setConfigSchema( schema )  -- Replaces the config schema with the given array of `{key, type, default}` tables.
  LMod:setEnabled( enabled )  -- Enables or disables this mod; disabled mods are skipped during loading
  LMod:setHook( name, func )  -- Registers a named hook callback, replacing any existing one
  LMod:type( )  -- Returns the type name of this object.
  LMod:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LModManager`** methods:

> Lua-side wrapper around [`ModManager`].

```lua
  LModManager:clearLoadOrder( )  -- Clears the custom load order, reverting to priority-based sorting
  LModManager:clearReloadQueue( )  -- Clears the reload queue without reloading
  LModManager:getAllMods( )  -> AllMods  -- Returns an array of info tables for all registered mods
  LModManager:getLoadOrder( )  -> LoadOrder  -- Returns an array of info tables in effective load order
  LModManager:getModCount( )  -> ModCount  -- Returns the number of registered mods
  LModManager:getModPath( mod_id )  -> ModPath  -- Returns the filesystem path of a registered mod, or nil
  LModManager:getReloadQueue( )  -> ReloadQueue  -- Returns the array of mod IDs pending hot-reload
  LModManager:hasCircularDependencies( )  -- Returns whether any circular dependency cycles exist
  LModManager:hasMod( mod_id )  -- Returns whether a mod with the given ID is registered
  LModManager:markForReload( mod_id )  -- Marks a registered mod for hot-reload
  LModManager:registerMod( ud )  -- Registers a mod from its Mod userdata
  LModManager:scanFolder( path )  -- Scans a directory for mods with mod.toml and registers them
  LModManager:setLoadOrder( order_table )  -- Sets an explicit load order from an array of mod ID strings
  LModManager:type( )  -- Returns the type name of this object.
  LModManager:typeOf( name )  -- Returns true if this object is of the given type.
  LModManager:unregisterMod( mod_id )  -- Removes a mod by ID and returns whether it was found
  LModManager:validateDependencies( )  -- Returns an array of mod IDs with missing dependencies
```

## lurek.i18n {#i18n}

> Localisation: string lookup, locale switching, plural forms.

```lua
  lurek.i18n.buildIndex( )  -- Builds an inverted word index for the active locale. Returns index as {word â†’ {keys}}.
  lurek.i18n.categories( )  -- Returns unique first-path-segment category prefixes for all active locale keys.
  lurek.i18n.formatDate( timestamp, [fmt] )  -- Formats a Unix timestamp according to the active locale's date order.
  lurek.i18n.formatNumber( n, [opts] )  -- Formats a number with locale-aware decimal and thousands separators.
  lurek.i18n.getAvailableLanguages( )  -> AvailableLanguages  -- Returns all loaded locale codes (alias for getLanguages).
  lurek.i18n.getBase( )  -> Base  -- Returns the base/fallback language.
  lurek.i18n.getFallbacks( )  -> Fallbacks  -- Returns the current fallback locale array.
  lurek.i18n.getKeys( )  -> Keys  -- Returns all known keys for the active locale.
  lurek.i18n.getLanguage( )  -> Language  -- Returns the currently active locale code, or nil if unset.
  lurek.i18n.getLanguages( )  -> Languages  -- Returns all loaded locale codes.
  lurek.i18n.getLoadedLocales( )  -> LoadedLocales  -- Returns an array of all currently loaded locale codes.
  lurek.i18n.hasKey( key )  -- Returns whether a key exists in the active locale.
  lurek.i18n.hasLanguage( locale )  -- Returns whether a locale has been loaded.
  lurek.i18n.interpolate( template, vars )  -- Interpolates {name} placeholders in a template string.
  lurek.i18n.keyCount( )  -- Returns the number of keys loaded in the active locale.
  lurek.i18n.keysInCategory( category )  -- Returns all keys in the active locale whose first path segment matches category.
  lurek.i18n.loadTable( locale, tbl )  -- Loads a language table under the given locale code.
  lurek.i18n.mergeLocale( locale, entries )  -- Merges a flat keyâ†’value table into an existing locale without replacing the whole table.
  lurek.i18n.offChange( )  -- Unregisters all onChange callbacks (cb arg is ignored; all callbacks are cleared).
  lurek.i18n.onChange( cb )  -- Registers a callback invoked when setLanguage() is called (alias: onChange).
  lurek.i18n.onLanguageChange( cb )  -- Registers a callback invoked when setLanguage() is called.
  lurek.i18n.pluralFor( n )  -> CLDR  -- Returns the CLDR plural category for a number ("one" or "other", etc.).
  lurek.i18n.search( query, [limit] )  -- Searches active locale values for a substring query (case-insensitive). Returns {key, v...
  lurek.i18n.searchIndexed( index, query, [limit] )  -- Searches the provided pre-built index for entries matching all words in query.
  lurek.i18n.setBase( locale )  -- Sets the base/fallback language (adds it as first fallback).
  lurek.i18n.setFallbacks( locales )  -- Sets the ordered list of fallback locale codes tried when a key is missing.
  lurek.i18n.setKey( locale, key, value )  -- Inserts or overwrites a single key in the given locale.
  lurek.i18n.setLanguage( locale )  -- Sets the active translation language.
  lurek.i18n.t( key, [vars], [count] )  -- Translates a key against the active locale with optional variable
  lurek.i18n.tGender( key, gender, [vars] )  -- Looks up a translation key augmented with a gender suffix.
  lurek.i18n.unloadTable( locale )  -- Unloads a locale from the catalog.
```

## lurek.input {#input}

> Unified input helper: combined keyboard + gamepad queries.

```lua
  lurek.input.advancePlayback( )  -- Advances playback by one frame and returns an array of key/button events for that
  lurek.input.bind( action, keys )  -- Maps an action name to one or more key/button names.
  lurek.input.clearBindings( )  -- Removes all action bindings.
  lurek.input.getAxis( id, axis )  -> Axis  -- Returns the current value (-1 to 1) of a gamepad analog axis.
  lurek.input.getAxisCount( id )  -> AxisCount  -- Returns the total number of analog axes on the gamepad.
  lurek.input.getBackgroundEvents( )  -> BackgroundEvents  -- Returns whether background gamepad events are enabled.
  lurek.input.getBindings( )  -> Bindings  -- Returns a table mapping each action name to its bound keys.
  lurek.input.getButtonCount( id )  -> ButtonCount  -- Returns the total number of buttons on the gamepad.
  lurek.input.getCount( )  -> Count  -- Returns the number of connected gamepads.
  lurek.input.getCursor( )  -> Cursor  -- Returns the name of the currently active system cursor.
  lurek.input.getGUID( id )  -> GUID  -- Returns the hardware GUID string of the gamepad.
  lurek.input.getGamepadMappingString( guid )  -> GamepadMappingString  -- Returns the stored mapping string for the given GUID, or nil.
  lurek.input.getHat( id, hat )  -> Hat  -- Returns the direction string of a hat switch on the gamepad.
  lurek.input.getJoystickCount( )  -> JoystickCount  -- Returns the number of tracked gamepad slots.
  lurek.input.getJoysticks( )  -> Joysticks  -- Returns a list of connected gamepad IDs.
  lurek.input.getKeyFromScancode( scancode )  -> KeyFromScancode  -- Returns the key name for the given hardware scancode.
  lurek.input.getName( id )  -> Name  -- Returns the human-readable name of a gamepad.
  lurek.input.getPlaybackFrame( )  -> PlaybackFrame  -- Returns the current playback frame index (0-based).  Returns 0 when not playing.
  lurek.input.getPosition( )  -> Position  -- Returns the current cursor position as (x, y).
  lurek.input.getPosition( id )  -> Position  -- Returns the position (x, y) of the touch with the given ID.
  lurek.input.getPressure( id )  -> Pressure  -- Returns the pressure (0-1) of the touch with the given ID.
  lurek.input.getRelativeMode( )  -> RelativeMode  -- Returns whether relative mouse mode is active.
  lurek.input.getScancodeFromKey( key )  -> ScancodeFromKey  -- Returns the hardware scancode for the given key name.
  lurek.input.getSystemCursor( name )  -> SystemCursor  -- Returns a system cursor object for the named cursor shape.
  lurek.input.getTouchCount( )  -> TouchCount  -- Returns the number of currently active touch points.
  lurek.input.getTouches( )  -> Touches  -- Returns a table of active touch points with id, x, y, and pressure fields.
  lurek.input.getWheelDelta( )  -> WheelDelta  -- Returns the mouse scroll wheel delta (dx, dy) since last frame.
  lurek.input.getX( )  -- Returns the current mouse X position in window coordinates.
  lurek.input.getY( )  -- Returns the current mouse Y position in window coordinates.
  lurek.input.hasKeyRepeat( )  -- Returns whether key-repeat is currently enabled.
  lurek.input.hasTextInput( )  -- Returns whether text input mode is currently active.
  lurek.input.isActionDown( action )  -- Returns true if any key bound to the action is currently held down.
  lurek.input.isConnected( id )  -- Returns whether the gamepad with the given ID is connected.
  lurek.input.isCursorSupported( )  -- Returns whether cursor customisation is supported on this platform.
  lurek.input.isDown( args )  -- Returns true if any of the given keys is currently held down.
  lurek.input.isDown( button )  -- Returns whether the given mouse button is currently held down.
  lurek.input.isDown( id, button )  -- Returns whether the given button on the gamepad is currently held.
  lurek.input.isGamepad( id )  -- Returns whether the joystick at the given slot is a recognized gamepad.
  lurek.input.isGrabbed( )  -- Returns whether the mouse cursor is locked to the window.
  lurek.input.isModifierActive( modifier )  -- Returns whether the named modifier key is currently held.
  lurek.input.isPlayingBack( )  -- Returns true if input playback is currently active.
  lurek.input.isRecording( )  -- Returns true if input recording is currently active.
  lurek.input.isScancodeDown( scancode )  -- Returns whether the key with the given scancode is held.
  lurek.input.isVibrationSupported( id )  -- Returns whether the gamepad supports haptic vibration.
  lurek.input.isVisible( )  -- Returns whether the mouse cursor is currently visible.
  lurek.input.loadGamepadMappings( path )  -> SDL2  -- Loads SDL2 GameControllerDB-format mappings from a file.
  lurek.input.loadRecording( json )  -> JSON  -- Loads a JSON-encoded recording string for playback.
  lurek.input.newCombo( steps_val, [opts] )  -> Combo  -- Creates a new combo detector from an ordered list of steps.
  lurek.input.newCursor( )  -> Cursor  -- Creates a custom mouse cursor from RGBA pixel data.
  lurek.input.saveGamepadMappings( path )  -- Saves all stored gamepad mappings to a plain-text file.
  lurek.input.setBackgroundEvents( enable )  -- Enable or disable receiving gamepad events when the window is not focused.
  lurek.input.setCursor( cursor_val )  -- Sets the active mouse cursor from a Cursor handle, name string, or nil to reset.
  lurek.input.setGamepadMapping( guid, mapping )  -- Stores or replaces the SDL2 GameControllerDB mapping string for the given GUID.
  lurek.input.setGrabbed( grabbed )  -- Locks or unlocks the mouse cursor to the window.
  lurek.input.setKeyRepeat( enabled )  -- Enables or disables key-repeat events.
  lurek.input.setPosition( x, y )  -- Moves the mouse cursor to the given window-space position.
  lurek.input.setRelativeMode( relative )  -- Enables or disables raw relative mouse motion mode.
  lurek.input.setTextInput( enabled )  -- Enables or disables Unicode text input mode.
  lurek.input.setVibration( ... )  -- Triggers haptic rumble (currently a no-op stub).
  lurek.input.setVisible( visible )  -- Shows or hides the operating-system mouse cursor.
  lurek.input.startPlayback( )  -- Starts playback from the beginning of the loaded recording.
  lurek.input.startRecording( )  -- Starts capturing input events frame-by-frame.  Clears any previous recording.
  lurek.input.stopPlayback( )  -- Stops playback immediately.
  lurek.input.stopRecording( )  -- Stops recording and returns an `InputRecording` userdata, or nil if not recording.
  lurek.input.unbind( action )  -- Removes all key bindings for the given action name.
  lurek.input.vibrate( id, low_freq, high_freq, duration_ms )  -- Requests haptic vibration on a gamepad.
  lurek.input.wasActionPressed( action )  -- Returns true if any key bound to the action was pressed this frame.
  lurek.input.wasActionPressedWithin( action, frames )  -- Was action pressed within.
  lurek.input.wasActionReleased( action )  -- Returns true if any key bound to the action was released this frame.
```

**`LCombo`** methods:

> Lua-side wrapper for a [`ComboDetector`] with an integrated millisecond clock.

```lua
  LCombo:feed( key )  -- Feed a key-press event into the combo detector.
  LCombo:getStep( index )  -> Step  -- Returns the step at the given 1-based index as `{key=..., gap_ms=...}`.
  LCombo:isInProgress( )  -- Returns true if the detector is currently mid-sequence.
  LCombo:progress( )  -- Returns the number of steps matched so far (0 when idle).
  LCombo:reset( )  -- Reset the detector to its initial idle state, cancelling any in-progress sequence.
  LCombo:tick( dt )  -- Advance the internal clock by `dt` seconds and check for timeouts.
  LCombo:totalSteps( )  -- Returns the total number of steps in the combo sequence.
  LCombo:type( )  -- Returns the type name of this object.
  LCombo:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LCursor`** methods:

> Lua-side wrapper around a mouse cursor handle.

```lua
  LCursor:getType( )  -> Type  -- Returns the cursor type as "system" or "custom".
  LCursor:release( )  -- Releases the cursor resource (no-op on desktop).
  LCursor:type( )  -- Returns the type name of this object.
  LCursor:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LInputRecording`** methods:

> Lua userdata wrapper for a completed [`crate::input::recorder::InputRecording`].

```lua
  LInputRecording:frameCount( )  -- Returns the number of sparse event frames stored in this recording.
  LInputRecording:toJson( )  -- Serializes this recording to a JSON string for saving to disk.
  LInputRecording:totalFrames( )  -- Returns the total frame count when recording was stopped.
  LInputRecording:type( )  -- Returns the type name of this object.
  LInputRecording:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.animation {#animation}

> `lurek.animation` — Sprite animation: frame pools, named clips, speed control, playback events,

```lua
  lurek.animation.fromAseprite( json_str )  -- Parses an Aseprite JSON export string and builds an Animation with clips and frames.
  lurek.animation.new( )  -- Creates a new, empty Animation controller.
  lurek.animation.newBlendLayerSet( )  -> BlendLayerSet  -- Creates a new empty [`BlendLayerSet`] for compositing multiple animation clips.
  lurek.animation.newCurve( )  -> Curve  -- Creates a new empty [`AnimCurve`] with linear interpolation.
  lurek.animation.newStateMachine( anim_ud, initial )  -> StateMachine  -- Creates an animation FSM from an Animation controller and an initial state name.
  lurek.animation.newSyncGroup( )  -> SyncGroup  -- Creates a new empty [`AnimSyncGroup`].
```

**`LAnimCurve`** methods:

> Lua-side wrapper around an [`AnimCurve`].

```lua
  LAnimCurve:addKeyframe( t, v )  -- Inserts a keyframe at the given time. If a keyframe at the same time already
  LAnimCurve:clear( )  -- Removes all keyframes from this animation curve, resetting it to empty.
  LAnimCurve:eval( t )  -- Returns the interpolated value at the given time using the curve's easing.
  LAnimCurve:keyframeCount( )  -- Returns the number of keyframes currently stored.
  LAnimCurve:setCustomEasing( func )  -- Set a custom Lua easing function for this curve.
  LAnimCurve:setEasing( mode )  -- Sets the easing kind applied between all keyframe segments.
  LAnimCurve:type( )  -- Returns the type name of this object.
  LAnimCurve:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LAnimStateMachine`** methods:

> Lua-side wrapper around an [`AnimStateMachine`] FSM controller.

```lua
  LAnimStateMachine:addState( name, clip, looping )  -- Registers a new named state that plays a clip from the embedded animation.
  LAnimStateMachine:addTransition( from_state, to_state, condition )  -- Adds a conditional transition between two states.
  LAnimStateMachine:forceState( name )  -- Immediately jumps to the named state, bypassing transition conditions.
  LAnimStateMachine:getQuad( )  -> Quad  -- Returns the source quad for the current animation frame, or nil.
  LAnimStateMachine:getState( )  -> State  -- Returns the name of the currently active state.
  LAnimStateMachine:setParam( name, value )  -- Sets an FSM parameter value (number, boolean, or integer supported).
  LAnimStateMachine:type( )  -- Returns the type name of this object.
  LAnimStateMachine:typeOf( name )  -- Returns true if this object is of the given type.
  LAnimStateMachine:update( dt )  -- Advances the FSM by `dt` seconds, evaluating transitions.
```

**`LAnimSyncGroup`** methods:

> Lua-side wrapper around an [`AnimSyncGroup`].

```lua
  LAnimSyncGroup:add( handle )  -- Adds an animation handle to the group.
  LAnimSyncGroup:clear( )  -- Removes all animation handles from the group.
  LAnimSyncGroup:memberCount( )  -- Returns the number of animations currently in the group.
  LAnimSyncGroup:remove( handle )  -- Removes an animation handle from the group.
  LAnimSyncGroup:type( )  -- Returns the type name of this object.
  LAnimSyncGroup:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LAnimation`** methods:

> Lua-side wrapper around an [`Animation`] controller.

```lua
  LAnimation:addClip( name, indices_tbl, fps, looping )  -- Adds a named clip from explicit frame indices.
  LAnimation:addClipFromGrid( )  -- Adds a named clip sliced from a sprite-sheet grid.
  LAnimation:addFrame( x, y, w, h )  -- Adds a single frame to the frame pool by source rectangle.
  LAnimation:addFramesFromGrid( tw, th, fw, fh, start, count )  -- Slices a sprite-sheet grid into frames and appends them.
  LAnimation:crossfade( clip_name, duration )  -- Begins a smooth crossfade from the current clip to a new named clip.
  LAnimation:drawToImage( w, h )  -- Renders the current animation frame into a new ImageData (white bg, blue frame rect).
  LAnimation:getBlendState( )  -> BlendState  -- Returns the two quads and blend factor during a crossfade, or nil when not blending.
  LAnimation:getClip( )  -> Clip  -- Returns the name of the currently playing clip, or nil.
  LAnimation:getClipCount( )  -> ClipCount  -- Returns the number of registered clips.
  LAnimation:getCurrentFrame( )  -> CurrentFrame  -- Returns the current position within the active clip (0-based).
  LAnimation:getFrameCount( )  -> FrameCount  -- Returns the total number of frames in the frame pool.
  LAnimation:getQuad( )  -> Quad  -- Returns the source quad (x, y, w, h) for the current frame, or nil.
  LAnimation:getSpeed( )  -> Speed  -- Returns the playback speed multiplier.
  LAnimation:isLooping( )  -- Returns true if the current clip is set to loop.
  LAnimation:isPlaying( )  -- Returns true if a clip is currently playing.
  LAnimation:pause( )  -- Pauses playback at the current frame.
  LAnimation:play( name )  -- Starts playback of the named clip.
  LAnimation:pollEvents( )  -- Drains and returns all pending animation events as a table.
  LAnimation:resume( )  -- Resumes playback from the current frame.
  LAnimation:setFrame( index )  -- Sets the playback position within the current clip.
  LAnimation:setSpeed( speed )  -- Sets the playback speed multiplier.
  LAnimation:stop( )  -- Stops playback and resets to frame 0.
  LAnimation:type( )  -- Returns the type name of this object.
  LAnimation:typeOf( name )  -- Returns true if this object is of the given type.
  LAnimation:update( dt )  -- Advances the animation by dt seconds.
```

**`LBlendLayerSet`** methods:

> Lua-side wrapper around a [`BlendLayerSet`] blend layer compositor.

```lua
  LBlendLayerSet:addLayer( name, clip_name, weight, [bones] )  -- Appends a new blend layer.
  LBlendLayerSet:getWeight( name )  -> Weight  -- Returns the blend weight of a named layer, or nil if not found.
  LBlendLayerSet:len( )  -- Returns the number of blend layers.
  LBlendLayerSet:listLayers( )  -- Returns an ordered array of layer info tables: {name, clip_name, weight, bones}.
  LBlendLayerSet:removeLayer( name )  -- Removes a blend layer by name.
  LBlendLayerSet:setMask( name, bones )  -- Replaces the bone mask of a layer.
  LBlendLayerSet:setWeight( name, weight )  -- Sets the blend weight of a named layer (clamped to [0, 1]).
  LBlendLayerSet:type( )  -- Returns the type name of this object.
  LBlendLayerSet:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.automation {#automation}

> `lurek.automation` â€” Automated input simulation via timed step scripts.

```lua
  lurek.automation.getCurrentScript( )  -> CurrentScript  -- Returns the name of the active script, or nil if idle.
  lurek.automation.getCurrentStep( )  -> CurrentStep  -- Returns the index of the next step to be dispatched.
  lurek.automation.getElapsedTime( )  -> ElapsedTime  -- Returns seconds elapsed since playback started.
  lurek.automation.getPlaybackSpeed( )  -> PlaybackSpeed  -- Returns the current playback speed multiplier (default 1.0).
  lurek.automation.getScripts( )  -> Scripts  -- Returns an array of all registered script names.
  lurek.automation.getStepCount( )  -> StepCount  -- Returns the total number of steps in the active script.
  lurek.automation.getStepLimit( name )  -> StepLimit  -- Returns the step limit for the named script, or nil if not found.
  lurek.automation.hasMacro( name )  -- Returns true if a macro with the given name has been saved.
  lurek.automation.hasScript( name )  -- Returns true if a script with the given name is registered.
  lurek.automation.isComplete( )  -- Returns true if all steps in the active script have been dispatched.
  lurek.automation.isHighlightMode( )  -- Returns whether the highlight overlay hint is active.
  lurek.automation.isPaused( )  -- Returns true if playback is currently paused.
  lurek.automation.isRunning( )  -- Returns true if the simulator is actively playing a script.
  lurek.automation.listMacros( )  -- Returns an array of all saved macro names.
  lurek.automation.load( name, data )  -- Loads a named script from a Lua data table containing a steps array.
  lurek.automation.loadFromToml( name, toml_str )  -- Parses a TOML string and registers it as a named script.
  lurek.automation.pause( )  -- Pauses playback at the current step position.
  lurek.automation.playMacro( name )  -- Loads and starts playback of a previously saved macro.
  lurek.automation.resume( )  -- Resumes playback from a paused position.
  lurek.automation.saveMacro( macro_name, script_name )  -- Saves a currently-loaded script under a macro name for fast replay.
  lurek.automation.setHighlightMode( enable )  -- Enables or disables the highlight overlay hint.
  lurek.automation.setPlaybackSpeed( factor )  -- Sets the dt multiplier for script playback (0.5 = half speed, 2.0 = double).
  lurek.automation.setStepLimit( name, n )  -- Sets the step limit for the named script (clamped to 1..MAX_STEPS).
  lurek.automation.start( name )  -- Starts playback of the named script from the beginning.
  lurek.automation.stop( )  -- Stops playback and resets the simulator to idle.
  lurek.automation.unload( name )  -- Removes a loaded script by name, returning true if it existed.
  lurek.automation.update( dt )  -- Advances the playback clock by `dt` seconds, dispatching due steps.
  lurek.automation.waitUntil( predicate, timeout )  -- Pauses playback advancement until predicate() returns true or timeout seconds elapse.
```

## lurek.camera {#camera}

> `lurek.camera` â€” Camera2D creation and manipulation for 2D viewport control.

```lua
  lurek.camera.new( [vw], [vh] )  -> Camera2D  -- Creates a new Camera2D with the given viewport dimensions.
  lurek.camera.newCamera( [vw], [vh] )  -> Camera  -- Creates a new 2D camera with the given viewport dimensions.
```

**`LCamera`** methods:

> Lua-side wrapper around a [`Camera2D`] instance.

```lua
  LCamera:apply( )  -- Applies this camera's transform to the render stack.
  LCamera:attach( )  -- Alias for `apply()`. Applies this camera's transform to the render stack.
  LCamera:clearParallaxFactors( )  -- Removes all parallax factor overrides.
  LCamera:clearTarget( )  -- Clears the follow target so the camera stops tracking.
  LCamera:detach( )  -- Alias for `reset()`. Pops the camera transform from the render stack.
  LCamera:followPath( points, duration )  -- Animates the camera along a sequence of world-space waypoints over
  LCamera:getEffectOffset( )  -> EffectOffset  -- Returns the current sway x, y world-space offset.
  LCamera:getEffectiveZoom( )  -> EffectiveZoom  -- Returns the current zoom level including zoom pulse and breathing deltas.
  LCamera:getParallaxFactor( layer )  -> ParallaxFactor  -- Returns the parallax factor for the named layer, or `1.0` if unset.
  LCamera:getPosition( )  -> Position  -- Returns the camera's world-space position as x, y.
  LCamera:getRotation( )  -> Rotation  -- Returns the rotation in radians.
  LCamera:getViewport( )  -> Viewport  -- Returns the current viewport as x, y, w, h.
  LCamera:getVisibleArea( )  -> VisibleArea  -- Returns the visible world area as x, y, w, h.
  LCamera:getZoom( )  -> Zoom  -- Returns the current zoom factor.
  LCamera:isBreathing( )  -- Returns true if the breathing effect is currently active.
  LCamera:isSway( )  -- Returns true if the sway effect is currently active.
  LCamera:lookAt( x, y )  -- Instantly moves the camera to look at the given position.
  LCamera:move( dx, dy )  -- Translates the camera by dx, dy in world space.
  LCamera:pathProgress( )  -- Returns the fractional progress `[0, 1]` of the active path, or
  LCamera:removeBounds( )  -- Removes previously set world-space bounds.
  LCamera:reset( )  -- Pops the camera transform from the render stack.
  LCamera:setBounds( x, y, w, h )  -- Sets world-space bounds for camera clamping.
  LCamera:setDeadZone( w, h )  -- Sets the dead zone half-extents for camera follow.
  LCamera:setFollowSmooth( speed )  -- Sets the follow smooth interpolation speed (0.0 = instant snap).
  LCamera:setLookAhead( mul )  -- Sets the look-ahead multiplier for follow prediction.
  LCamera:setParallaxFactor( layer, factor )  -- Sets the parallax scroll factor for the named render layer.
  LCamera:setPosition( x, y )  -- Sets the camera's world-space position.
  LCamera:setRotation( r )  -- Sets the rotation in radians.
  LCamera:setTarget( x, y )  -- Sets the follow target position.
  LCamera:setViewport( x, y, w, h )  -- Sets the viewport rectangle in screen pixels.
  LCamera:setZoom( zoom )  -- Sets the uniform zoom factor (1.0 = natural size).
  LCamera:shake( intensity, duration )  -- Starts a screen-shake effect.
  LCamera:startBreathing( [amplitude], [rate] )  -- Starts a subtle periodic zoom oscillation for a "living camera" feel.
  LCamera:startSway( amplitude_x, amplitude_y, frequency, [decay] )  -- Starts a sinusoidal x/y offset oscillation (e.g., boat rocking).
  LCamera:stopBreathing( )  -- Stops the active breathing effect.
  LCamera:stopPath( )  -- Cancels the active camera path animation.
  LCamera:stopSway( )  -- Stops the active sway effect immediately.
  LCamera:stopZoom( )  -- Cancels the active zoom tween.
  LCamera:toScreen( wx, wy )  -- Converts world coordinates to screen coordinates.
  LCamera:toWorld( sx, sy )  -- Converts screen coordinates to world coordinates.
  LCamera:type( )  -- Returns the type name of this object.
  LCamera:typeOf( name )  -- Returns true if this object is of the given type.
  LCamera:update( dt )  -- Advances the camera simulation by dt seconds.
  LCamera:updatePath( dt )  -- Advances the path animation by `dt` seconds and applies the
  LCamera:updateZoom( dt )  -- Advances the zoom tween by `dt` seconds and applies the resulting
  LCamera:zoomPulse( amplitude, duration )  -- Triggers a momentary zoom-in that decays back via a sine envelope.
  LCamera:zoomTo( target_zoom, duration )  -- Smoothly tweens the camera zoom from its current level to
```

## lurek.debugbridge {#debugbridge}

> Registers the `lurek.debugbridge.*` TCP debug server API.

```lua
  lurek.debugbridge.broadcast( event, json_data )  -- Broadcasts a JSON event to all connected clients.
  lurek.debugbridge.capturePrint( msg, [source], [line] )  -- Captures a print message and broadcasts it to connected clients.
  lurek.debugbridge.clearPrintHistory( )  -- Clears the print history.
  lurek.debugbridge.getClientCount( )  -> ClientCount  -- Returns the number of connected TCP clients.
  lurek.debugbridge.getPerformance( )  -> Performance  -- Returns performance statistics.
  lurek.debugbridge.getPort( )  -> Port  -- Returns the server port (0 if not running).
  lurek.debugbridge.getPrintHistory( [count] )  -> PrintHistory  -- Returns the print history.
  lurek.debugbridge.isRunning( )  -- Returns whether the server is currently running.
  lurek.debugbridge.isScreenshotRequested( )  -- Returns whether a screenshot is currently requested.
  lurek.debugbridge.poll( )  -- Poll for pending Lua-dependent requests from TCP clients.
  lurek.debugbridge.requestScreenshot( [scale] )  -- Flags a screenshot request for the next frame.
  lurek.debugbridge.setMaxPrintHistory( max )  -- Sets the maximum print history size.
  lurek.debugbridge.start( [port] )  -- Start the TCP debug server on 127.0.0.1:port.
  lurek.debugbridge.stop( )  -- Stop the TCP debug server and close all connections.
```

## lurek.devtools {#devtools}

> Registers the `lurek.devtools.*` runtime diagnostics and developer-tools API.

```lua
  lurek.devtools.clearLog( )  -- Discards all accumulated log entries from the in-memory devtools log buffer.
  lurek.devtools.clearWatches( )  -- Clears all watched paths.
  lurek.devtools.debug( message )  -- Logs a message at DEBUG level.
  lurek.devtools.error( message )  -- Logs a message at ERROR level.
  lurek.devtools.eval( code )  -- Evaluates a Lua string and returns (success, results...).
  lurek.devtools.exposeWatch( name, getter, [category] )  -- Registers a named live watch. The getter function is called on demand to sample a value.
  lurek.devtools.fatal( message )  -- Logs a message at FATAL level.
  lurek.devtools.getCallStack( [max_depth] )  -> Lua  -- Returns the Lua call stack as a table of frames.
  lurek.devtools.getFrameHistory( )  -> FrameHistory  -- Returns the raw frame-time sample array.
  lurek.devtools.getFrameHistorySize( )  -> FrameHistorySize  -- Returns the current frame-history buffer capacity.
  lurek.devtools.getFrameStats( )  -> FrameStats  -- Returns a table of computed frame statistics.
  lurek.devtools.getLogConsole( )  -> LogConsole  -- Returns whether console log output is enabled.
  lurek.devtools.getLogFile( )  -> LogFile  -- Returns the current log file path.
  lurek.devtools.getLogHistory( [count] )  -> LogHistory  -- Returns recent log entries as an array of tables.
  lurek.devtools.getLogLevel( )  -> LogLevel  -- Returns the current minimum log level.
  lurek.devtools.getProfileData( [frame] )  -> ProfileData  -- Returns zone data table for a specific frame (0 or nil = most recent).
  lurek.devtools.getProfileFrameCount( )  -> ProfileFrameCount  -- Returns the number of retained profile frames.
  lurek.devtools.getWatchInterval( )  -> WatchInterval  -- Returns the file watch poll interval in seconds.
  lurek.devtools.getWatchedPaths( )  -> WatchedPaths  -- Returns an array of all watched paths.
  lurek.devtools.getWatches( )  -> Watches  -- Calls all registered watch getters and returns a table of {name, category, value} records.
  lurek.devtools.info( message )  -- Logs a message at INFO level.
  lurek.devtools.isConsoleOpen( )  -- Returns whether the console is considered open.
  lurek.devtools.isProfilingEnabled( )  -- Returns whether the profiler is enabled.
  lurek.devtools.log( level, message )  -- Logs a message at the given level.
  lurek.devtools.newFileWatcher( path )  -> FileWatcher  -- Creates a standalone per-path file watcher. Call `:check()` once per frame
  lurek.devtools.newRepl( [max_history] )  -> Repl  -- Creates an interactive Lua REPL console with a bounded history buffer.
  lurek.devtools.openConsole( )  -- Opens the console window (updates the console flag; returns true).
  lurek.devtools.profileFrame( )  -- Seals the current frame of profiling data.
  lurek.devtools.profilePop( [] )  -- Closes the most recent profiling zone.
  lurek.devtools.profilePush( name )  -- Opens a named profiling zone on the stack.
  lurek.devtools.profilerReport( )  -- Returns a flat summary table of all recorded profiler zones across all stored
  lurek.devtools.recordFrameTime( dt_val )  -- Records a frame-time sample (call each frame with delta time in seconds).
  lurek.devtools.removeWatch( id )  -- Removes a watch by the id returned from exposeWatch. Returns true if removed.
  lurek.devtools.resetProfile( )  -- Clears all profiling data and resets the zone stack.
  lurek.devtools.scan( )  -- Polls all watched paths and returns paths whose mtime changed.
  lurek.devtools.setFrameHistorySize( size )  -- Sets the frame-history buffer capacity (clamped 10-10000).
  lurek.devtools.setLogConsole( enabled )  -- Enables or disables console log output.
  lurek.devtools.setLogFile( path )  -- Sets the log file path (empty string disables file output).
  lurek.devtools.setLogLevel( level )  -- Sets the minimum log level.
  lurek.devtools.setProfilingEnabled( enabled )  -- Enables or disables the profiler.
  lurek.devtools.setWatchInterval( interval )  -- Sets the file watch poll interval in seconds.
  lurek.devtools.snapshot( )  -- Takes a structured snapshot of all watches + frame stats + last profile frame.
  lurek.devtools.trace( message )  -- Logs a message at TRACE level.
  lurek.devtools.unwatch( path )  -- Removes a file path from the watch list.
  lurek.devtools.warn( message )  -- Logs a message at WARN level.
  lurek.devtools.watch( path )  -- Adds a file path to the watch list. Returns false if already watched.
```

**`LFileWatcher`** methods:

> Lua-side handle for a per-path file watcher.

```lua
  LFileWatcher:cancel( )  -- Removes the stored `onChanged` callback and stops future notifications.
  LFileWatcher:check( )  -- Polls the watcher. If the file has changed since the last call, fires the
  LFileWatcher:getPath( )  -> Path  -- Returns the watched path string.
  LFileWatcher:onChanged( func )  -- Registers a callback invoked (with no arguments) when the watched path changes.
  LFileWatcher:type( )  -- Returns the type name of this object.
  LFileWatcher:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LReplConsole`** methods:

> Lua-side wrapper around a [`ReplConsole`] interactive evaluator.

```lua
  LReplConsole:clear( )  -- Clears the REPL history buffer.
  LReplConsole:eval( code )  -- Evaluates a Lua snippet and records the input in history.
  LReplConsole:history( )  -- Returns an ordered array of past inputs (oldest first).
  LReplConsole:len( )  -- Returns the number of history entries.
  LReplConsole:type( )  -- Returns the type name of this object.
  LReplConsole:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.docs {#docs}

> Registers the `lurek.docs.*` documentation management API.

```lua
  lurek.docs.checkStaleness( catalog_ud, source_dir )  -- Compare catalog entries against source files in a directory for staleness.
  lurek.docs.coverage( [catalog_ud] )  -- Return (documented_count, total_live_count) coverage tuple.
  lurek.docs.coverageModule( module_name, [catalog_ud] )  -- Return (documented_count, total_live_count) for a single module.
  lurek.docs.describe( qualified_name, description )  -- Inject or update a description for a named API entry.
  lurek.docs.exportAll( catalog_ud, output_dir )  -- Export completions.json, hover.json, and signatures.json to a directory.
  lurek.docs.exportCheatsheet( catalog_ud, path )  -- Export a one-line-per-function plain-text cheatsheet.
  lurek.docs.exportCompletions( catalog_ud, path )  -- Export VS Code IntelliSense completions JSON to a file.
  lurek.docs.exportHover( catalog_ud, path )  -- Export VS Code hover JSON to a file.
  lurek.docs.exportMarkdown( catalog_ud, path )  -- Export a Markdown API reference file.
  lurek.docs.exportSignatures( catalog_ud, path )  -- Export VS Code signature-help JSON to a file.
  lurek.docs.getCatalog( )  -> Catalog  -- Return the current internal catalog as an ApiCatalog userdata.
  lurek.docs.loadAll( directory )  -- Load all .toml files in a directory and merge into a single ApiCatalog.
  lurek.docs.loadToml( path )  -> TOML  -- Load a TOML doc file into an ApiCatalog.
  lurek.docs.quality( [catalog_ud] )  -- Calculate quality metrics for a catalog or the internal catalog.
  lurek.docs.qualityModule( module_name, [catalog_ud] )  -- Calculate quality metrics for a single module.
  lurek.docs.reflectLive( [ns] )  -- Walks the live lurek.* Lua table and returns a structured reflection of all
  lurek.docs.reflectTable( tbl, [name] )  -- Reflects any Lua table, returning a structure describing its keys,
  lurek.docs.resetCatalog( )  -- Clear all entries from the internal catalog.
  lurek.docs.scan( [opts] )  -- Scan the lurek.* namespace to build an API catalog from live bindings.
  lurek.docs.scanModule( module_name )  -- Scan a single module's bindings.
  lurek.docs.schema( rules, [name] )  -> Schema  -- Creates a Schema validator from a rules table.
  lurek.docs.setParamInfo( qualified_name, params )  -- Set the parameter metadata for a catalog entry.
  lurek.docs.setReturnInfo( qualified_name, returns )  -- Set the return type metadata for a catalog entry.
  lurek.docs.validate( [catalog_ud] )  -- Validate catalog completeness against the live lurek.* bindings.
  lurek.docs.validateModule( module_name, [catalog_ud] )  -- Validate a single module against the live lurek.<module>.* bindings.
```

**`LApiCatalog`** methods:

> Wraps a catalog snapshot of API entries for Lua access.

```lua
  LApiCatalog:entryCount( [module] )  -- Returns the number of entries, optionally scoped to a module.
  LApiCatalog:filter( predicate )  -- Returns a new catalog containing only entries for which predicate returns true.
  LApiCatalog:getEntries( [module] )  -> Entries  -- Returns all entries, optionally filtered to a single module.
  LApiCatalog:getEntry( qualified_name )  -> Entry  -- Returns a single entry by qualified name, or nil.
  LApiCatalog:getModules( )  -> Modules  -- Returns a sorted list of module names present in the catalog.
  LApiCatalog:getTypeMethods( qualified_name )  -> TypeMethods  -- Returns entries that are methods of the given type qualified name.
  LApiCatalog:getTypes( module_name )  -> Types  -- Returns the names of all entries with kind "type" in the given module.
  LApiCatalog:merge( other )  -- Returns a new catalog that is the union of this and another catalog, with other overrid...
  LApiCatalog:search( query )  -- Returns a table of entries whose name, qualified name, or description contains query.
  LApiCatalog:toJSON( )  -- Serialises the catalog to a pretty-printed JSON string.
  LApiCatalog:toTable( )  -- Converts the catalog to a plain Lua table array.
  LApiCatalog:type( )  -- Returns the type name of this object.
  LApiCatalog:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LDocEntry`** methods:

> Wraps a single doc entry for Lua access.

```lua
  LDocEntry:getDeprecated( )  -> Deprecated  -- Returns the deprecation message, or nil.
  LDocEntry:getDescription( )  -> Description  -- Returns the human-readable description text for this documentation entry.
  LDocEntry:getExample( )  -> Example  -- Returns the example snippet, or nil.
  LDocEntry:getKind( )  -> Kind  -- Returns the kind tag for this entry (e.g. `'function'`, `'method'`, `'class'`).
  LDocEntry:getModule( )  -> Lua  -- Returns the Lua module name this entry belongs to (e.g. `'lurek.math'`).
  LDocEntry:getName( )  -> Name  -- Returns the symbol name for this documentation entry.
  LDocEntry:getParameters( )  -> Parameters  -- Returns the parameters as a table of `{name, type, description, optional, default?}` re...
  LDocEntry:getQualifiedName( )  -> QualifiedName  -- Returns the qualified name.
  LDocEntry:getReturns( )  -> Returns  -- Returns the return values as a table of `{type, description}` records.
  LDocEntry:getScore( )  -> Score  -- Returns the quality score in [0,1].
  LDocEntry:getSince( )  -> Since  -- Returns the since version string, or nil.
  LDocEntry:hasDescription( )  -- Returns true when the entry has a non-empty description.
  LDocEntry:hasExample( )  -- Returns true when the entry has an example snippet.
  LDocEntry:hasParameters( )  -- Returns true when the entry has at least one parameter.
  LDocEntry:hasReturnType( )  -- Returns true when the entry declares at least one return type.
  LDocEntry:type( )  -- Returns the type name of this object.
  LDocEntry:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LQualityReport`** methods:

> Wraps documentation quality metrics for Lua access.

```lua
  LQualityReport:getBest( [count] )  -> Best  -- Returns up to count entries with the highest quality scores.
  LQualityReport:getByGrade( grade )  -> ByGrade  -- Returns entries whose grade exactly matches the given letter grade.
  LQualityReport:getGrade( )  -> Grade  -- Returns the letter grade for the overall score.
  LQualityReport:getModuleScores( )  -> ModuleScores  -- Returns a table mapping module name to its average quality score.
  LQualityReport:getOverallScore( )  -> OverallScore  -- Returns the overall quality score in [0,1].
  LQualityReport:getSummary( )  -> Summary  -- Returns a multi-line human-readable summary of quality by module.
  LQualityReport:getWorst( [count] )  -> Worst  -- Returns up to count entries with the lowest quality scores.
  LQualityReport:toJSON( )  -- Serialises the quality report to a pretty-printed JSON string.
  LQualityReport:toTable( )  -- Converts the quality report to a plain Lua table.
  LQualityReport:type( )  -- Returns the type name of this object.
  LQualityReport:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LSchema`** methods:

> Lua wrapper for a runtime data-validation schema.

```lua
  LSchema:assert( data )  -- Validates data and throws a Lua error on failure with all error messages joined.
  LSchema:check( data )  -- Returns true when the data passes all schema rules.
  LSchema:getFields( )  -> Fields  -- Returns a table of declared field names.
  LSchema:getName( )  -> Name  -- Returns the name identifier of this API schema group.
  LSchema:type( )  -- Returns the type name of this object.
  LSchema:typeOf( name )  -- Returns true if this object is of the given type.
  LSchema:validate( data )  -- Validates a Lua table against the schema.
```

**`LValidationReport`** methods:

> Wraps a validation report for Lua access.

```lua
  LValidationReport:getIncomplete( )  -> Incomplete  -- Returns the list of qualified names whose catalog entry is incomplete.
  LValidationReport:getMissing( )  -> Missing  -- Returns the list of qualified names present in the live API but missing from the catalog.
  LValidationReport:getPhantom( )  -> Phantom  -- Returns the list of qualified names in the catalog that are not present in the live API.
  LValidationReport:getSummary( )  -> Summary  -- Returns a single-line summary of the validation results.
  LValidationReport:incompleteCount( )  -- Returns the count of incomplete entries.
  LValidationReport:isValid( )  -- Returns true when the report has no missing entries.
  LValidationReport:missingCount( )  -- Returns the count of missing entries.
  LValidationReport:phantomCount( )  -- Returns the count of phantom entries.
  LValidationReport:toJSON( )  -- Serialises the report to a pretty-printed JSON string.
  LValidationReport:toTable( )  -- Converts the report to a plain Lua table.
  LValidationReport:type( )  -- Returns the type name of this object.
  LValidationReport:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.effect {#effect}

> `lurek.effect` â€” Composable visual effects: post-processing pipeline and screen overlays.

```lua
  lurek.effect.getEffectTypes( )  -> EffectTypes  -- Returns the list of all built-in effect type names.
  lurek.effect.getShaderErrorDisplay( )  -> ShaderErrorDisplay  -- Returns whether shader error display is currently enabled.
  lurek.effect.newCustomEffect( shader_id )  -> CustomEffect  -- Creates a custom shader post-processing effect.
  lurek.effect.newEffect( type_name )  -> Effect  -- Creates a new built-in post-processing effect by type name.
  lurek.effect.newImageEffect( ... )  -> ImageEffect  -- Creates a new per-image effect chain. Accepts:
  lurek.effect.newOverlay( [w], [h] )  -> Overlay  -- Creates a new screen overlay controller for weather, flash, shake, and fade effects.
  lurek.effect.newPass( shader_id )  -> Pass  -- Creates a custom-shader post-processing effect (alias for newCustomEffect).
  lurek.effect.newPresetStack( name, [w], [h] )  -> PresetStack  -- Creates a pre-configured effect stack from a named preset.
  lurek.effect.newStack( [w], [h] )  -> Stack  -- Creates a new post-processing pipeline stack.
  lurek.effect.newTransition( [kind], [duration], [color_tbl] )  -> Transition  -- Creates a new screen-transition controller. `kind` is one of:
  lurek.effect.setShaderErrorDisplay( enabled )  -- Enables or disables the effect that renders shader compile errors as red text
```

**`LImageEffect`** methods:

> Lua-side wrapper around [`ImageEffect`].

```lua
  LImageEffect:addEffect( name )  -- Creates a new effect by type name, appends it, and returns the shared PostFxEffect.
  LImageEffect:clear( )  -- Removes all effects from the chain (alias for clearEffects).
  LImageEffect:clearEffects( )  -- Removes all effects from the chain.
  LImageEffect:clone( )  -- Returns a deep copy of this ImageEffect chain.
  LImageEffect:effectCount( )  -- Returns the number of effects in the chain.
  LImageEffect:getEffect( key )  -> Effect  -- Returns the effect at the given 1-based index or with the given type name.
  LImageEffect:getEffectCount( )  -> EffectCount  -- Returns the number of effects in the chain (alias for effectCount).
  LImageEffect:removeByIndex( idx )  -- Removes the effect at the given 0-based index from the chain.
  LImageEffect:removeByName( name )  -- Removes the first effect matching the given type name.
  LImageEffect:removeEffect( key )  -- Removes the effect at the given 1-based index or with the given type name.
  LImageEffect:save( )  -- Stub: no-op serialisation placeholder.
  LImageEffect:type( )  -- Returns the type name "ImageEffect".
  LImageEffect:typeOf( name )  -- Returns true when the given name matches "ImageEffect" or a parent type.
```

**`LOverlay`** methods:

> Lua-side wrapper around [`Overlay`].

```lua
  LOverlay:clear( )  -- Resets all effect subsystems to their default inactive state.
  LOverlay:drawToImage( w, h )  -- Renders the effect state (flash, fade, effects) to a CPU ImageData.
  LOverlay:fade( r, g, b, [a], [dur] )  -- Animates a full-screen colour fade; alpha defaults to 1.0, duration to 1.0 s.
  LOverlay:flash( r, g, b, [a], [dur] )  -- Triggers a full-screen colour flash; alpha defaults to 1.0, duration to 0.2 s.
  LOverlay:getAmbientColor( )  -> AmbientColor  -- Returns the current ambient tint as r, g, b, a components.
  LOverlay:getCloudCount( )  -> CloudCount  -- Returns the current cloud shadow instance count.
  LOverlay:getCloudOpacity( )  -> CloudOpacity  -- Returns the current cloud shadow opacity.
  LOverlay:getCloudScale( )  -> CloudScale  -- Returns the current cloud shadow scale.
  LOverlay:getCloudSpeed( )  -> CloudSpeed  -- Returns the current cloud shadow scroll speed.
  LOverlay:getDimensions( )  -> Dimensions  -- Returns the effect width and height.
  LOverlay:getFilmGrainIntensity( )  -> FilmGrainIntensity  -- Returns the current film-grain intensity.
  LOverlay:getFlashAlpha( )  -> FlashAlpha  -- Returns the current flash overlay alpha value.
  LOverlay:getFogColor( )  -> FogColor  -- Returns the current fog tint as r, g, b, a components.
  LOverlay:getFogDensity( )  -> FogDensity  -- Returns the current fog density.
  LOverlay:getHeatHazeIntensity( )  -> HeatHazeIntensity  -- Returns the current heat-haze distortion intensity.
  LOverlay:getHeight( )  -> Height  -- Returns the effect height.
  LOverlay:getLightningAlpha( )  -> LightningAlpha  -- Returns the current lightning overlay alpha value.
  LOverlay:getLightningColor( )  -> LightningColor  -- Returns the lightning flash tint as r, g, b, a components.
  LOverlay:getShakeOffset( )  -> ShakeOffset  -- Returns the current shake displacement as x, y.
  LOverlay:getTimeOfDay( )  -> TimeOfDay  -- Returns the current simulated time-of-day (0â€“24).
  LOverlay:getVignetteStrength( )  -> VignetteStrength  -- Returns the current vignette strength.
  LOverlay:getWater( )  -> Water  -- Returns a table describing the current water overlay state.
  LOverlay:getWeather( )  -> Weather  -- Returns the name of the current weather type.
  LOverlay:getWeatherIntensity( )  -> WeatherIntensity  -- Returns the current weather intensity.
  LOverlay:getWidth( )  -> Width  -- Returns the effect width.
  LOverlay:getWindDirection( )  -> WindDirection  -- Returns the current wind direction in radians.
  LOverlay:getWindSpeed( )  -> WindSpeed  -- Returns the current wind speed.
  LOverlay:isActive( )  -- Returns true if any effect subsystem is currently active.
  LOverlay:isAmbientEnabled( )  -- Returns whether the ambient light layer is active.
  LOverlay:isCloudShadowsEnabled( )  -- Returns whether cloud shadows are active.
  LOverlay:isFading( )  -- Returns true while a fade effect is in progress.
  LOverlay:isFilmGrainEnabled( )  -- Returns whether the film-grain layer is active.
  LOverlay:isFlashing( )  -- Returns true while a flash effect is in progress.
  LOverlay:isFogEnabled( )  -- Returns whether the fog layer is active.
  LOverlay:isHeatHazeEnabled( )  -- Returns whether the heat-haze layer is active.
  LOverlay:isShaking( )  -- Returns true while a shake effect is in progress.
  LOverlay:isVignetteEnabled( )  -- Returns whether the vignette layer is active.
  LOverlay:isWeatherEnabled( )  -- Returns whether the weather particle system is active.
  LOverlay:render( )  -- Emits GPU render commands for all active overlay effects (flash, fade, lightning, vigne...
  LOverlay:resize( w, h )  -- Resizes the effect to match new window dimensions.
  LOverlay:setAmbientColor( r, g, b, [a] )  -- Sets the ambient light tint colour; alpha defaults to 1.0.
  LOverlay:setAmbientEnabled( v )  -- Enables or disables the ambient light layer.
  LOverlay:setCloudCount( v )  -- Sets the number of cloud shadow instances to render.
  LOverlay:setCloudOpacity( v )  -- Sets the opacity of cloud shadows (0.0 = invisible, 1.0 = fully dark).
  LOverlay:setCloudScale( v )  -- Sets the scale multiplier applied to each cloud shadow.
  LOverlay:setCloudShadows( v )  -- Enables or disables scrolling cloud-shadow projection.
  LOverlay:setCloudSpeed( v )  -- Sets the horizontal scroll speed of cloud shadows in pixels per second.
  LOverlay:setCustomShader( [name] )  -- Assigns a custom shader name to the effect, or clears it when `nil` is passed.
  LOverlay:setFilmGrainEnabled( v )  -- Enables or disables the film-grain noise layer.
  LOverlay:setFilmGrainIntensity( v )  -- Sets the film-grain noise intensity (0.0â€“1.0).
  LOverlay:setFogColor( r, g, b, [a] )  -- Sets the fog tint colour; alpha defaults to 1.0.
  LOverlay:setFogDensity( v )  -- Sets the fog density (0.0 = clear, 1.0 = fully opaque).
  LOverlay:setFogEnabled( v )  -- Enables or disables the fog layer.
  LOverlay:setHeatHazeEnabled( v )  -- Enables or disables the heat-haze distortion layer.
  LOverlay:setHeatHazeIntensity( v )  -- Sets the heat-haze distortion intensity (0.0â€“1.0).
  LOverlay:setLightningColor( r, g, b, [a] )  -- Sets the lightning flash tint colour; alpha defaults to 1.0.
  LOverlay:setTimeOfDay( v )  -- Sets the simulated time-of-day (0â€“24) which drives ambient colour.
  LOverlay:setVignetteEnabled( v )  -- Enables or disables the screen-edge vignette layer.
  LOverlay:setVignetteStrength( v )  -- Sets the vignette darkening strength (0.0â€“1.0).
  LOverlay:setWater( amplitude, frequency, speed )  -- Enables the water UV-distortion overlay and sets its wave parameters.
  LOverlay:setWaterTint( r, g, b, strength )  -- Sets the water tint colour and blend strength.
  LOverlay:setWeather( name )  -- Sets the active weather type by name ("none", "rain", "snow", "hail", "dust", "leaves",...
  LOverlay:setWeatherEnabled( v )  -- Enables or disables the weather particle system.
  LOverlay:setWeatherIntensity( v )  -- Sets the particle spawn rate multiplier (0.0â€“1.0).
  LOverlay:setWindDirection( v )  -- Sets the wind direction in radians (0 = right, Ď€/2 = down).
  LOverlay:setWindSpeed( v )  -- Sets the wind speed applied to weather particles in units per second.
  LOverlay:shake( intensity, [dur] )  -- Triggers a camera shake; duration defaults to 0.5 s.
  LOverlay:triggerFade( r, g, b, target_alpha, duration )  -- Triggers a screen fade effect to the given colour and alpha.
  LOverlay:triggerFlash( r, g, b, a, duration )  -- Triggers a screen-wide colour flash effect.
  LOverlay:triggerLightning( )  -- Triggers a lightning flash effect.
  LOverlay:triggerShake( intensity, duration )  -- Triggers a screen shake effect with the given intensity and duration.
  LOverlay:type( )  -- Returns the type name of this object ("Overlay").
  LOverlay:typeOf( name )  -- Returns true if this object is of the given type ("Object" or "Overlay").
  LOverlay:update( dt )  -- Advances all effect subsystems by the given delta time.
```

**`LPostFxEffect`** methods:

> Lua-side wrapper around [`PostFxEffect`].

```lua
  LPostFxEffect:disableAutoUniforms( )  -- Disables auto-injection of common uniforms into shader slot p[3].
  LPostFxEffect:enableAutoUniforms( )  -- Enables auto-injection of common uniforms into shader slot p[3] each frame.
  LPostFxEffect:getEffectType( )  -> EffectType  -- Returns the type name of this effect (alias for getTypeName).
  LPostFxEffect:getParameter( name, [default] )  -> Parameter  -- Returns a named parameter value, or the default if not set.
  LPostFxEffect:getParameterNames( )  -> ParameterNames  -- Returns a list of all parameter names on this effect.
  LPostFxEffect:getType( )  -> Type  -- Returns the type name of this effect (alias for getTypeName).
  LPostFxEffect:getTypeName( )  -> TypeName  -- Returns the display name of this effect type.
  LPostFxEffect:hasParameter( name )  -- Returns true if the named parameter exists on this effect.
  LPostFxEffect:isAutoUniforms( )  -- Returns whether auto-uniform injection is enabled for this effect.
  LPostFxEffect:isBuiltIn( )  -- Returns true if this is a built-in effect, false if custom.
  LPostFxEffect:isEnabled( )  -- Returns whether this effect is currently active.
  LPostFxEffect:setBrightness( v )  -- Sets the brightness parameter of this effect.
  LPostFxEffect:setContrast( v )  -- Sets the contrast parameter of this effect.
  LPostFxEffect:setEnabled( enabled )  -- Enables or disables this effect.
  LPostFxEffect:setIntensity( v )  -- Sets the intensity parameter of this effect.
  LPostFxEffect:setOffset( v )  -- Sets the offset parameter of this effect.
  LPostFxEffect:setParameter( name, value )  -- Sets a named float parameter on this effect.
  LPostFxEffect:setRadius( v )  -- Sets the radius parameter of this effect.
  LPostFxEffect:setSaturation( v )  -- Sets the saturation parameter of this effect.
  LPostFxEffect:setScanlineStrength( v )  -- Sets the scanline strength parameter of this effect.
  LPostFxEffect:setStrength( v )  -- Sets the strength parameter of this effect.
  LPostFxEffect:setThreshold( v )  -- Sets the threshold parameter of this effect.
  LPostFxEffect:type( )  -- Returns the type name "PostFxEffect".
  LPostFxEffect:typeOf( name )  -- Returns true when the given name matches "PostFxEffect" or a parent type.
```

**`LPostFxStack`** methods:

> Lua-side wrapper around [`PostFxStack`].

```lua
  LPostFxStack:add( effect_ud )  -- Appends a PostFxEffect to the end of the pipeline.
  LPostFxStack:apply( )  -- Applies all enabled effects in the stack and composites the result to screen.
  LPostFxStack:beginCapture( )  -- Begins capturing the scene for post-processing.
  LPostFxStack:clear( )  -- Removes all effects from the pipeline.
  LPostFxStack:clearFeedback( )  -- Resets the feedback intensity to `0.0` (disables feedback).
  LPostFxStack:dedup( )  -- Removes duplicate effects from the pipeline, keeping the first occurrence
  LPostFxStack:endCapture( )  -- Ends scene capture for post-processing.
  LPostFxStack:getDimensions( )  -> Dimensions  -- Returns width and height of the render target.
  LPostFxStack:getEffect( index )  -> Effect  -- Returns the effect at the given 1-based position, or nil.
  LPostFxStack:getEffectCount( )  -> EffectCount  -- Returns the number of effects in the pipeline.
  LPostFxStack:getEnabledEffects( )  -> EnabledEffects  -- Returns a list of currently enabled effect objects.
  LPostFxStack:getFeedback( )  -> Feedback  -- Returns the current feedback loop intensity `[0.0, 1.0]`.
  LPostFxStack:getHeight( )  -> Height  -- Returns the height of the render target.
  LPostFxStack:getWidth( )  -> Width  -- Returns the width of the render target.
  LPostFxStack:insert( position, effect_ud )  -- Inserts a PostFxEffect at a specific 1-based position in the pipeline.
  LPostFxStack:isCapturing( )  -- Returns whether the stack is currently capturing the scene.
  LPostFxStack:isEmpty( )  -- Returns true if the pipeline has no effect slots.
  LPostFxStack:isEnabled( position )  -- Returns whether the effect at the given 1-based position is enabled.
  LPostFxStack:len( )  -- Returns the total number of effect slots in the pipeline.
  LPostFxStack:remove( effect_ud )  -- Removes the given PostFxEffect from the pipeline.
  LPostFxStack:resize( w, h )  -- Resizes the render target to the given dimensions.
  LPostFxStack:setEnabled( position, enabled )  -- Enables or disables the effect at the given 1-based position.
  LPostFxStack:setFeedback( factor )  -- Sets the feedback loop intensity. At `0.0` (default) there is no
  LPostFxStack:type( )  -- Returns the type name "PostFxStack".
  LPostFxStack:typeOf( name )  -- Returns true when the given name matches "PostFxStack" or a parent type.
```

**`LScreenTransition`** methods:

> Lua-side wrapper around a [`crate::effect::ScreenTransition`].

```lua
  LScreenTransition:color( )  -- Returns the fill color as four numbers: `r, g, b, a`.
  LScreenTransition:isActive( )  -- Returns `true` while the transition is running.
  LScreenTransition:isDone( )  -- Returns `true` after the transition has completed.
  LScreenTransition:kind( )  -- Returns the transition kind name (`"fade"`, `"wipe"`, `"iris_wipe"`,
  LScreenTransition:play( )  -- Starts the transition playing forward (scene fades/wipes out).
  LScreenTransition:progress( )  -- Returns the fractional progress `[0, 1]` of the transition, taking
  LScreenTransition:reverse( )  -- Starts the transition in reverse (scene fades/wipes in).
  LScreenTransition:setColor( )  -- Updates the fill color from `{r, g, b, a?}`.
  LScreenTransition:type( )  -- Returns the type name of this object ("ScreenTransition").
  LScreenTransition:typeOf( name )  -- Returns true if this object is of the given type name or a parent type.
  LScreenTransition:update( dt )  -- Advances the transition by `dt` seconds. Returns `true` while
```

## lurek.engine {#engine}

> `lurek.runtime` â€” Runtime engine metadata and introspection.

```lua
  lurek.engine.fps( )  -- Returns the current measured frames-per-second.
  lurek.engine.frameCount( )  -- Returns the total number of frames processed since engine start.
  lurek.engine.getFrameBudget( )  -> FrameBudget  -- Returns the target frame budget in milliseconds (default: 1000 / 60 â‰ 16.667 ms).
  lurek.engine.getResourceStats( )  -> ResourceStats  -- Returns a table with resident resource memory statistics.
  lurek.engine.getVersion( )  -> Version  -- Returns the engine version string (from `Cargo.toml`).
  lurek.engine.isDebug( )  -- Returns `true` if the engine was compiled in debug mode.
  lurek.engine.memoryUsage( )  -- Returns a table with `lua_bytes` (Lua GC heap usage in bytes) and
  lurek.engine.platform( )  -- Returns a string identifying the host operating system:
  lurek.engine.setResourceBudget( budget_bytes )  -- Sets the maximum resident texture memory budget in bytes.
  lurek.engine.uptime( )  -- Returns the total engine uptime in seconds (sum of all processed deltas).
```

## lurek.globe {#globe}

> `lurek.globe` — XCOM-style Geoscape province sphere API.

```lua
  lurek.globe.get( name )  -- Get an existing globe by name, or nil.
  lurek.globe.greatCircleDistance( la, lo, lb, lo2 )  -- Great-circle distance between two lat/lon points (in unit-sphere radians).
  lurek.globe.greatCirclePath( la, lo, lb, lo2, n )  -- Great-circle path as a table of {lat, lon} pairs.
  lurek.globe.latLonToUnit( lat, lon )  -- Convert lat/lon (degrees) to a unit-sphere Cartesian vector {x, y, z}.
  lurek.globe.loadFromTOML( name, toml_src, [spec_tbl] )  -- Load provinces from a TOML string and create a globe.
  lurek.globe.new( name, [spec_tbl] )  -- Creates a new globe instance with default settings and empty collections.
```

**`LGlobe`** methods:

> Lua-accessible handle to a `Globe` inside a `GlobeRegistry`.

```lua
  LGlobe:addArc( lat1, lon1, lat2, lon2, [steps] )  -- Add an arc (great-circle path between two lat/lon points).
  LGlobe:addLabel( ltype, lat, lon, text )  -- Add a text label. Returns label ID.
  LGlobe:addLayer( name, [z_order] )  -- Add or replace a named thematic layer.
  LGlobe:addMarker( mtype, lat, lon, [label] )  -- Add a marker. Returns marker ID.
  LGlobe:addProvince( p )  -- Adds a province from a table {id, centroid={lat,lon}, vertices={{lat,lon},...},
  LGlobe:findPath( from_id, to_id )  -- Find the shortest province path from `from_id` to `to_id`.
  LGlobe:getCamera( )  -> Camera  -- Get the current camera (lat, lon, zoom).
  LGlobe:getLod( )  -> Lod  -- Returns the current LOD tier as a string: "far", "mid", or "near".
  LGlobe:getMarkerAttr( id, key )  -> MarkerAttr  -- Get a string attribute from a marker.
  LGlobe:getName( )  -> Name  -- Returns the string identifier name assigned to this globe instance.
  LGlobe:getNeighbors( id )  -> Neighbors  -- Returns the neighbor IDs of a province.
  LGlobe:getProvinceAttr( id, key )  -> ProvinceAttr  -- Gets a string attribute from a province.
  LGlobe:getTimeOfDay( )  -> TimeOfDay  -- Gets the current simulated time of day for daylight computation.
  LGlobe:hideProvince( viewer, id )  -- Hide a province for a viewer.
  LGlobe:isVisible( viewer, id )  -- Returns true if the province is visible to the viewer.
  LGlobe:moveMarker( id, lat, lon )  -- Move a marker to a new lat/lon.
  LGlobe:pan( dlat, dlon )  -- Pan the orbit camera by delta-latitude and delta-longitude (degrees).
  LGlobe:pick( sx, sy )  -- Returns the province ID under screen coordinates, or nil.
  LGlobe:pickLatLon( sx, sy )  -- Returns (lat, lon) of the screen point on the globe surface, or nil.
  LGlobe:provinceCount( )  -- Returns the number of provinces.
  LGlobe:reachable( start_id, max_cost )  -- Return all provinces reachable within `max_cost` steps from `start_id`.
  LGlobe:removeArc( id )  -- Removes an arc from the globe map by its unique string identifier.
  LGlobe:removeLabel( id )  -- Removes a text label from the globe map by its unique string identifier.
  LGlobe:removeLayer( name )  -- Removes a texture layer from the globe map by its unique string identifier.
  LGlobe:removeMarker( id )  -- Removes a marker from the globe map by its unique string identifier.
  LGlobe:removeProvince( id )  -- Removes a province by ID. Returns true if it existed.
  LGlobe:revealAll( viewer )  -- Reveal all provinces for a viewer.
  LGlobe:revealProvince( viewer, id )  -- Reveal a province for a viewer.
  LGlobe:setActiveViewer( [viewer] )  -- Set the faction/viewer whose fog mask filters rendering.
  LGlobe:setBorders( show )  -- Enable or disable province border rendering.
  LGlobe:setCamera( lat, lon, z )  -- Set the camera position directly.
  LGlobe:setLabelText( id, text )  -- Updates the visible text content of an existing globe label.
  LGlobe:setLabelVisible( id, vis )  -- Sets whether this specific label is visible on the globe.
  LGlobe:setLayerAlpha( name, alpha )  -- Set layer opacity (0.0–1.0).
  LGlobe:setLayerColor( layer, id, r, g, b, a )  -- Set a per-province color override on a layer.
  LGlobe:setLayerVisible( name, vis )  -- Sets whether this specific texture layer is visible on the globe.
  LGlobe:setMarkerAttr( id, key, val )  -- Set a string attribute on a marker.
  LGlobe:setMarkerVisible( id, vis )  -- Sets whether this specific marker is visible on the globe.
  LGlobe:setProvinceAttr( id, key, val )  -- Sets a string attribute on a province.
  LGlobe:setRotation( deg )  -- Set planet rotation (degrees).
  LGlobe:setTimeOfDay( t )  -- Set time of day (0.0–24.0 hours).
  LGlobe:type( )  -- Returns the type name of this object.
  LGlobe:typeOf( name )  -- Returns true if this object is of the given type.
  LGlobe:update( dt )  -- Advance globe simulation by dt seconds.
  LGlobe:zoom( factor )  -- Zoom the camera by a multiplier (>1 zooms in, <1 zooms out).
```

**`LGlobeRegistry`** methods:

> Lua-accessible handle to the shared `GlobeRegistry`.

```lua
  LGlobeRegistry:get( name )  -- Get an existing globe by name, or nil.
  LGlobeRegistry:names( )  -- Returns a table of all globe names.
  LGlobeRegistry:new( name, [spec_tbl] )  -- Create a globe with the given name and optional spec table.
  LGlobeRegistry:remove( name )  -- Removes a globe from the central registry by its string name.
  LGlobeRegistry:type( )  -- Returns the type name of this object.
  LGlobeRegistry:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.html {#html}

> `lurek.html` — Lightweight pure-Rust HTML/CSS layout engine Lua bindings.

```lua
  lurek.html.isDefaultPrevented( )  -- Returns true if `preventDefault` has been called on this event.
  lurek.html.loadDocument( path, [opts] )  -- Placeholder for future sandboxed document loading.
  lurek.html.newDocument( [source], [opts] )  -> Document  -- Creates a detached HTML document from markup and optional CSS/viewport options.
  lurek.html.preventDefault( )  -- Prevents the default browser action associated with this event.
  lurek.html.stopPropagation( )  -- Stops the event from bubbling up to parent elements.
  lurek.html.supports( feature )  -- Returns whether the active HTML facade supports a named feature.
```

**`LHtmlDocument`** methods:

> Lua wrapper around a shared `HtmlDocument` and its callback registry.

```lua
  LHtmlDocument:addCss( css )  -- Appends stylesheet text after existing CSS rules.
  LHtmlDocument:clearCss( )  -- Removes all stylesheet rules from this document.
  LHtmlDocument:draw( [x], [y] )  -- Builds the current draw command list and discards it for now.
  LHtmlDocument:getElementById( id )  -> ElementById  -- Finds one element by id.
  LHtmlDocument:getHtml( )  -> Html  -- Returns the source markup used by this document.
  LHtmlDocument:getRoot( )  -> Root  -- Returns the root element for this document.
  LHtmlDocument:getViewport( )  -> Viewport  -- Returns the document layout viewport in UI pixels.
  LHtmlDocument:isDirty( )  -- Returns whether DOM, CSS, viewport, or layout state changed.
  LHtmlDocument:keypressed( key )  -- Forwards a key press and emits a keydown event.
  LHtmlDocument:mousemoved( x, y )  -- Forwards a mouse move event.
  LHtmlDocument:mousepressed( x, y, [button] )  -- Forwards a mouse press and emits a minimal click event.
  LHtmlDocument:mousereleased( x, y, [button] )  -- Forwards a mouse release event.
  LHtmlDocument:off( handle )  -- Removes a document-level event listener.
  LHtmlDocument:on( event, func )  -- Registers a document-level event listener.
  LHtmlDocument:query( selector )  -- Finds the first element matching a supported selector.
  LHtmlDocument:queryAll( selector )  -- Returns all elements matching a supported selector in document order.
  LHtmlDocument:relayout( )  -- Forces a layout pass immediately.
  LHtmlDocument:setCss( css )  -- Replaces this document's stylesheet text.
  LHtmlDocument:setHtml( html )  -- Replaces this document's markup and invalidates existing element handles.
  LHtmlDocument:setViewport( w, h )  -- Sets the document layout viewport in UI pixels.
  LHtmlDocument:textinput( text )  -- Forwards text input and emits an input event for focused input elements.
  LHtmlDocument:type( )  -- Returns the type name of this object.
  LHtmlDocument:typeOf( name )  -- Returns true if this object is of the given type.
  LHtmlDocument:update( dt )  -- Advances document state and runs layout if needed.
  LHtmlDocument:wheelmoved( dx, dy )  -- Forwards a mouse wheel event.
```

**`LHtmlElement`** methods:

> Lua wrapper that references a single element inside a shared `HtmlDocument`.

```lua
  LHtmlElement:addClass( name )  -- Adds a CSS class to this element.
  LHtmlElement:appendHtml( html )  -- Appends HTML inside this element.
  LHtmlElement:blur( )  -- Clears focus from this element if it currently has focus.
  LHtmlElement:focus( )  -- Gives focus to this element.
  LHtmlElement:getAttribute( name )  -> Attribute  -- Returns an attribute value or nil.
  LHtmlElement:getDocument( )  -> Document  -- Returns the owning HtmlDocument.
  LHtmlElement:getHtml( )  -> Html  -- Returns this element's inner HTML.
  LHtmlElement:getId( )  -> Id  -- Returns this element's id or nil.
  LHtmlElement:getRect( )  -> Rect  -- Returns this element's last computed layout rectangle.
  LHtmlElement:getStyle( name )  -> Style  -- Returns an inline or stylesheet value for a property.
  LHtmlElement:getTagName( )  -> TagName  -- Returns this element's tag name.
  LHtmlElement:getText( )  -> Text  -- Returns this element's text content.
  LHtmlElement:hasClass( name )  -- Returns whether this element has a CSS class.
  LHtmlElement:off( handle )  -- Removes an element event listener.
  LHtmlElement:on( event, func )  -- Registers an element event listener.
  LHtmlElement:query( selector )  -- Finds the first descendant matching a selector.
  LHtmlElement:queryAll( selector )  -- Returns all descendants matching a selector.
  LHtmlElement:remove( )  -- Removes this element from the document tree.
  LHtmlElement:removeAttribute( name )  -- Removes an attribute.
  LHtmlElement:removeClass( name )  -- Removes a CSS class from this element.
  LHtmlElement:setAttribute( name, [value] )  -- Sets or removes an attribute value.
  LHtmlElement:setHtml( html )  -- Replaces this element's inner HTML.
  LHtmlElement:setId( [id] )  -- Sets or removes this element's id.
  LHtmlElement:setStyle( name, [value] )  -- Sets or removes an inline style value.
  LHtmlElement:setText( text )  -- Replaces this element's text content.
  LHtmlElement:toggleClass( name, [force] )  -- Toggles a CSS class and returns the final state.
  LHtmlElement:type( )  -- Returns the type name of this object.
  LHtmlElement:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.light {#light}

> `lurek.light` â€” 2D lighting, shadow occluders, and ambient control.

```lua
  lurek.light.advanceFlickers( dt )  -- Advances flicker phase for all lights with flicker enabled.
  lurek.light.clear( )  -- Removes all lights and occluders, resets ambient to default.
  lurek.light.getAmbient( )  -> Ambient  -- Returns the global ambient light color as (r, g, b, a).
  lurek.light.getGodRayHints( )  -> GodRayHints  -- Returns a list of directional light hints for god-ray rendering.
  lurek.light.getGroupCount( group_id )  -> GroupCount  -- Returns the number of lights in the given group.
  lurek.light.getLightCount( )  -> LightCount  -- Returns the number of lights in the world.
  lurek.light.getMaxLights( )  -> MaxLights  -- Returns the maximum number of lights processed per frame.
  lurek.light.getOccluderCount( )  -> OccluderCount  -- Returns the number of occluders in the world.
  lurek.light.isEnabled( )  -- Returns whether the lighting system is active.
  lurek.light.newLight( x, y, radius, [opts] )  -> Light  -- Creates a new light at (x, y) with the given radius and optional settings.
  lurek.light.newOccluder( vtbl, [opts] )  -> Occluder  -- Creates a new shadow occluder from a vertex table and optional settings.
  lurek.light.setAmbient( r, g, b, [a] )  -- Sets the global ambient light color.
  lurek.light.setEnabled( enabled )  -- Sets whether the lighting system is active.
  lurek.light.setGroupColor( group_id, r, g, b, [a] )  -- Sets the color for all lights in the given group.
  lurek.light.setGroupEnabled( group_id, enabled )  -- Sets the enabled state for all lights in the given group.
  lurek.light.setGroupIntensity( group_id, intensity )  -- Sets the intensity for all lights in the given group.
  lurek.light.setMaxLights( n )  -- Sets the maximum number of lights processed per frame (clamped 1â€“256).
  lurek.light.syncAmbient( )  -- Returns the current ambient light colour as (r, g, b, a).
```

**`LLight`** methods:

> Lua-side handle to a light resource stored in [`LightWorld`].

```lua
  LLight:addFlicker( min, max, hz )  -- Convenience method to set a flicker effect using amplitude range and
  LLight:clearCookie( )  -- Removes the cookie texture assignment.
  LLight:getAttenuation( )  -> Attenuation  -- Returns the custom attenuation coefficients as (constant, linear, quadratic).
  LLight:getBlendMode( )  -> BlendMode  -- Returns the blend mode as a string.
  LLight:getColor( )  -> Color  -- Returns the light's tint color as (r, g, b, a).
  LLight:getCookie( )  -> Cookie  -- Returns the current cookie texture path, or `nil` if unset.
  LLight:getDirection( )  -> Direction  -- Returns the direction angle in radians.
  LLight:getEnergy( )  -> Energy  -- Returns the energy scaling factor.
  LLight:getFalloff( )  -> Falloff  -- Returns the falloff mode as a string.
  LLight:getFlicker( )  -> Flicker  -- Returns the flicker effect speed and strength.
  LLight:getGroupId( )  -> GroupId  -- Returns the group identifier.
  LLight:getInnerAngle( )  -> InnerAngle  -- Returns the inner cone angle in radians.
  LLight:getIntensity( )  -> Intensity  -- Returns the brightness multiplier.
  LLight:getLightMask( )  -> LightMask  -- Returns the light interaction bitmask.
  LLight:getLightType( )  -> LightType  -- Returns the geometric light type as a string.
  LLight:getOuterAngle( )  -> OuterAngle  -- Returns the outer cone angle in radians.
  LLight:getPosition( )  -> Position  -- Returns the light's world-space position.
  LLight:getRadius( )  -> Radius  -- Returns the light's influence radius.
  LLight:getShadowColor( )  -> ShadowColor  -- Returns the shadow region color as (r, g, b, a).
  LLight:getShadowFilter( )  -> ShadowFilter  -- Returns the shadow edge filter as a string.
  LLight:getShadowMask( )  -> ShadowMask  -- Returns the shadow casting bitmask.
  LLight:getShadowSmooth( )  -> ShadowSmooth  -- Returns the shadow edge smoothing factor.
  LLight:isEnabled( )  -- Returns whether this light is active.
  LLight:isFlickerEnabled( )  -- Returns whether the flicker effect is active.
  LLight:isShadowEnabled( )  -- Returns whether this light casts shadows.
  LLight:isValid( )  -- Returns whether this light handle is still valid.
  LLight:isVolumetric( )  -- Returns whether this light hints at volumetric scattering.
  LLight:remove( )  -- Removes this light from the world.
  LLight:setAttenuation( c, l, q )  -- Sets the custom attenuation coefficients (constant, linear, quadratic).
  LLight:setBlendMode( mode )  -- Sets the blend mode ('add', 'sub', or 'mix').
  LLight:setColor( r, g, b, [a] )  -- Sets the light's tint color.
  LLight:setCookie( path )  -- Sets the texture path used as a light cookie (mask) for projection.
  LLight:setDirection( dir )  -- Sets the direction angle in radians.
  LLight:setEnabled( b )  -- Sets whether this light is active.
  LLight:setEnergy( e )  -- Sets the energy scaling factor.
  LLight:setFalloff( mode )  -- Sets the falloff mode ('linear', 'smooth', or 'constant').
  LLight:setFlicker( speed, strength )  -- Sets the flicker effect speed and strength (enables flicker).
  LLight:setFlickerEnabled( b )  -- Sets whether the flicker effect is active.
  LLight:setGroupId( id )  -- Sets the group identifier for batch operations.
  LLight:setInnerAngle( a )  -- Sets the inner cone angle in radians for spot lights.
  LLight:setIntensity( i )  -- Sets the brightness multiplier.
  LLight:setLightMask( mask )  -- Sets the light interaction bitmask.
  LLight:setLightType( t )  -- Sets the geometric light type ('point', 'directional', or 'spot').
  LLight:setOuterAngle( a )  -- Sets the outer cone angle in radians for spot lights.
  LLight:setPosition( x, y )  -- Sets the light's world-space position.
  LLight:setRadius( r )  -- Sets the light's influence radius.
  LLight:setShadowColor( r, g, b, [a] )  -- Sets the shadow region color.
  LLight:setShadowEnabled( b )  -- Sets whether this light casts shadows.
  LLight:setShadowFilter( filter )  -- Sets the shadow edge filter ('none', 'pcf5', or 'pcf13').
  LLight:setShadowMask( mask )  -- Sets the shadow casting bitmask.
  LLight:setShadowSmooth( s )  -- Sets the shadow edge smoothing factor.
  LLight:setVolumetric( b )  -- Sets whether this light hints at volumetric scattering.
  LLight:stopTransition( )  -- Cancels the active light transition.
  LLight:transitionProgress( )  -- Returns the fractional progress `[0, 1]` of the active transition,
  LLight:transitionTo( target, duration )  -- Begins a smooth linear transition of the light's color, intensity,
  LLight:type( )  -- Returns the type name of this object.
  LLight:typeOf( name )  -- Returns true if this object is of the given type.
  LLight:updateTransition( dt )  -- Advances the active transition by `dt` seconds and applies the
```

**`LOccluder`** methods:

> Lua-side handle to an occluder resource stored in [`LightWorld`].

```lua
  LOccluder:getLightMask( )  -> LightMask  -- Returns the light interaction bitmask.
  LOccluder:getOpacity( )  -> Opacity  -- Returns the shadow opacity.
  LOccluder:getPosition( )  -> Position  -- Returns the translation offset as (x, y).
  LOccluder:getVertices( )  -> Vertices  -- Returns the polygon vertices as a flat table {x1,y1,x2,y2,...}.
  LOccluder:isEnabled( )  -- Returns whether this occluder is active.
  LOccluder:isValid( )  -- Returns whether this occluder handle is still valid.
  LOccluder:remove( )  -- Removes this occluder from the world.
  LOccluder:setEnabled( b )  -- Sets whether this occluder is active.
  LOccluder:setLightMask( mask )  -- Sets the light interaction bitmask.
  LOccluder:setOpacity( o )  -- Sets the shadow opacity (0.0â€“1.0).
  LOccluder:setPosition( x, y )  -- Sets the translation offset applied to all vertices.
  LOccluder:setVertices( tbl )  -- Replaces the polygon vertices from a flat table {x1,y1,x2,y2,...}.
  LOccluder:type( )  -- Returns the type name of this object.
  LOccluder:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.network {#network}

> `lurek.network` — Full networking toolkit for multiplayer games.

```lua
  lurek.network.createLobby( name, port, [player_count], [max_players] )  -> LobbyInfo  -- Creates a LobbyInfo record and broadcasts it once on the local network.
  lurek.network.discoverLobbies( [timeout_ms] )  -- Listens for LAN lobby announcements for `timeout_ms` milliseconds (default 500).
  lurek.network.newClient( opts )  -> Client  -- Creates a client host that connects to a remote server.
  lurek.network.newHost( opts )  -> Host  -- Creates a new network host bound to the given address.
  lurek.network.newRuntime( )  -> Runtime  -- Creates a background network runtime for async HTTP, TCP, and WebSocket.
  lurek.network.newServer( opts )  -> Server  -- Creates a server host that binds to a port and accepts connections.
  lurek.network.pack( value )  -- Serializes a Lua value to a binary MessagePack string.
  lurek.network.syncEntity( )  -- Convenience helper: packs an entity snapshot and broadcasts it to all peers.
  lurek.network.unpack( data )  -- Deserializes a MessagePack binary string back to a Lua value.
```

**`LNetworkHost`** methods:

> Lua-side wrapper around [`NetworkHost`].

```lua
  LNetworkHost:broadcast( channel_id, data, [reliable] )  -- Broadcasts data to all connected peers on a channel.
  LNetworkHost:connect( addr_str, [channels], [data] )  -- Initiates a connection to a remote host, returning the peer ID.
  LNetworkHost:destroy( )  -- Destroys the host, closing the underlying socket.
  LNetworkHost:disconnect( peer_id, [data] )  -- Gracefully disconnects a peer.
  LNetworkHost:disconnectLater( peer_id, [data] )  -- Disconnects a peer after all queued packets have been sent.
  LNetworkHost:disconnectNow( peer_id, [data] )  -- Immediately disconnects a peer without handshake.
  LNetworkHost:flush( )  -- Flushes all pending sends immediately.
  LNetworkHost:getAddress( )  -> Address  -- Returns the local bind address as a string.
  LNetworkHost:getBandwidthLimit( )  -> BandwidthLimit  -- Returns the bandwidth limits as a table with incoming and outgoing fields.
  LNetworkHost:getChannelLimit( )  -> ChannelLimit  -- Returns the maximum number of channels per connection.
  LNetworkHost:getConnectedPeerCount( )  -> ConnectedPeerCount  -- Returns the number of currently connected peers.
  LNetworkHost:getConnectedPeerIds( )  -> ConnectedPeerIds  -- Returns a table of connected peer IDs.
  LNetworkHost:getPeerAddress( peer_id )  -> PeerAddress  -- Returns the remote address of a peer, or nil if unavailable.
  LNetworkHost:getPeerLimit( )  -> PeerLimit  -- Returns the maximum number of peer slots.
  LNetworkHost:getPeerState( peer_id )  -> PeerState  -- Returns the connection state of a peer as a string.
  LNetworkHost:getPeerStats( peer_id )  -> PeerStats  -- Returns a statistics table for a peer.
  LNetworkHost:getRole( )  -> Role  -- Returns the multiplayer role of this host ("server", "client", or "host").
  LNetworkHost:getRoundTripTime( peer_id )  -> RoundTripTime  -- Returns the round-trip time estimate for a peer in milliseconds.
  LNetworkHost:isClient( )  -- Returns true if this host was created as a client.
  LNetworkHost:isDestroyed( )  -- Returns true if the host has been destroyed.
  LNetworkHost:isServer( )  -- Returns true if this host was created as a server.
  LNetworkHost:ping( peer_id )  -- Sends a ping to a peer to measure round-trip time.
  LNetworkHost:resetPeer( peer_id )  -- Resets a peer connection immediately without notifying the remote side.
  LNetworkHost:send( peer_id, channel_id, data, [reliable] )  -- Sends data to a specific peer on a channel.
  LNetworkHost:service( )  -- Polls the network for one event, returning an event table or nil.
  LNetworkHost:setBandwidthLimit( [incoming], [outgoing] )  -- Sets the bandwidth limits in bytes per second.
  LNetworkHost:setChannelLimit( limit )  -- Sets the channel limit for future connections.
  LNetworkHost:type( )  -- Returns the type name of this object.
  LNetworkHost:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LNetworkRuntime`** methods:

> Lua-side wrapper around [`NetworkRuntime`] for async HTTP/TCP/WebSocket.

```lua
  LNetworkRuntime:httpGet( url, [headers] )  -- Convenience: sends an HTTP GET request.
  LNetworkRuntime:httpPost( url, body, [headers] )  -- Convenience: sends an HTTP POST request.
  LNetworkRuntime:httpRequest( opts )  -- Sends an HTTP request asynchronously. Poll with `poll()` for the response.
  LNetworkRuntime:poll( )  -- Polls for completed async responses (HTTP, TCP events, WebSocket events).
  LNetworkRuntime:shutdown( )  -- Shuts down the background network thread.
  LNetworkRuntime:tcpClose( id )  -- Closes the TCP connection identified by the given connection handle.
  LNetworkRuntime:tcpConnect( addr )  -- Opens a TCP connection to a remote address.
  LNetworkRuntime:tcpSend( id, data )  -- Sends data over a TCP connection.
  LNetworkRuntime:type( )  -- Returns the type name of this object.
  LNetworkRuntime:typeOf( name )  -- Returns true if this object is of the given type.
  LNetworkRuntime:wsClose( id )  -- Closes a WebSocket connection.
  LNetworkRuntime:wsConnect( url )  -- Opens a WebSocket connection.
  LNetworkRuntime:wsSend( id, data )  -- Sends a text message over a WebSocket connection.
```

## lurek.parallax {#parallax}

> `lurek.parallax` â€” multi-layer scrolling background system.

```lua
  lurek.parallax.newLayer( opts )  -> Layer  -- Creates a new parallax background layer from an options table.
  lurek.parallax.newSet( name )  -> Set  -- Creates a new empty parallax set with the given name.
```

**`LParallaxLayer`** methods:

> Lua-side handle to a single parallax background layer.

```lua
  LParallaxLayer:clearClamp( )  -- Removes scroll clamping so the layer scrolls freely.
  LParallaxLayer:getAutoscroll( )  -> Autoscroll  -- Returns the autoscroll velocity as `(vx, vy)`.
  LParallaxLayer:getBlendMode( )  -> BlendMode  -- Returns the current blend mode as a string.
  LParallaxLayer:getDepth( )  -> Depth  -- Returns the current floating-point depth.
  LParallaxLayer:getOffset( )  -> Offset  -- Returns the static offset as `(x, y)`.
  LParallaxLayer:getOpacity( )  -> Opacity  -- Returns the current opacity.
  LParallaxLayer:getScrollFactor( )  -> ScrollFactor  -- Returns the scroll factor as `(x, y)`.
  LParallaxLayer:getTiling( )  -> Tiling  -- Returns `true` if seamless infinite tiling is enabled.
  LParallaxLayer:getTint( )  -> Tint  -- Returns the current tint as `(r, g, b, a)`.
  LParallaxLayer:getZ( )  -- Returns the draw-order depth.
  LParallaxLayer:isVisible( )  -- Returns `true` if the layer is currently visible.
  LParallaxLayer:render( cam_x, cam_y )  -- Draws the layer using an explicit camera world position.
  LParallaxLayer:renderAuto( )  -- Draws the layer using the engine active camera position automatically.
  LParallaxLayer:resetAutoscroll( )  -- Resets the autonomous scroll accumulator to zero.
  LParallaxLayer:setAutoscroll( vx, vy )  -- Sets the autonomous scroll velocity in world-pixels per second.
  LParallaxLayer:setBlendMode( mode )  -- Sets the GPU blend mode for this layer.
  LParallaxLayer:setClamp( min_x, min_y, max_x, max_y )  -- Clamps the scroll offset to a world-pixel range on each axis.
  LParallaxLayer:setDepth( z )  -- Sets the floating-point draw depth for fine-grained layer ordering.
  LParallaxLayer:setOffset( x, y )  -- Sets the static world-pixel position bias added on top of camera scroll.
  LParallaxLayer:setOpacity( a )  -- Sets the layer-wide opacity override in `[0.0, 1.0]`.
  LParallaxLayer:setRepeat( rx, ry )  -- Sets whether the layer tiles on the X and Y axes.
  LParallaxLayer:setScale( sx, sy )  -- Sets the texture display scale factor on each axis.
  LParallaxLayer:setScrollFactor( x, y )  -- Sets the scroll factor relative to camera movement on each axis.
  LParallaxLayer:setTileSize( w, h )  -- Sets explicit tile dimensions in logical pixels, overriding the default
  LParallaxLayer:setTiling( enabled )  -- Enables or disables seamless infinite tiling on both axes simultaneously.
  LParallaxLayer:setTint( r, g, b, a )  -- Sets the multiplicative RGBA tint applied to all pixels of this layer.
  LParallaxLayer:setVisible( v )  -- Shows or hides this layer.
  LParallaxLayer:setZ( z )  -- Sets the draw-order depth. Lower values render first (further back).
  LParallaxLayer:type( )  -- Returns the type name of this object.
  LParallaxLayer:update( dt )  -- Advances the autonomous scroll accumulator by `dt` seconds.
```

**`LParallaxSet`** methods:

> Lua-side container that groups `LuaParallaxLayer` objects for scene-level management.

```lua
  LParallaxSet:addLayer( layer )  -- Adds a layer to this set.
  LParallaxSet:getName( )  -> Name  -- Returns the name of this set.
  LParallaxSet:isVisible( )  -- Returns `true` if the set is currently visible.
  LParallaxSet:layerCount( )  -- Returns the number of layers in this set.
  LParallaxSet:removeLayerAt( index )  -- Removes the layer at the given 1-based index.
  LParallaxSet:render( cam_x, cam_y )  -- Draws all visible layers in ascending `z` order using an explicit camera position.
  LParallaxSet:renderAuto( )  -- Draws all visible layers using the engine active camera position.
  LParallaxSet:setName( name )  -- Sets the name of this set.
  LParallaxSet:setVisible( v )  -- Shows or hides all layers in this set.
  LParallaxSet:sortByZ( )  -- Re-sorts all layers by ascending `z` value.
  LParallaxSet:type( )  -- Returns the type name of this object.
  LParallaxSet:update( dt )  -- Advances the autoscroll accumulator of every layer by `dt` seconds.
```

## lurek.patterns {#patterns}

> Registers the `lurek.patterns.*` software design patterns API.

```lua
  lurek.patterns.newBlackboard( [name] )  -> Blackboard  -- Creates a new Blackboard shared key-value store.
  lurek.patterns.newCommandStack( [max_size] )  -> CommandStack  -- Creates a new CommandStack instance.
  lurek.patterns.newDebounce( wait )  -> Debounce  -- Creates a trailing-edge debounce that fires after the input stream is idle for wait sec...
  lurek.patterns.newEventBus( [name] )  -> EventBus  -- Creates a new EventBus instance.
  lurek.patterns.newFactory( )  -> Factory  -- Creates a new Factory instance.
  lurek.patterns.newFunnel( window, [max_entries], [name] )  -> Funnel  -- Creates a time-windowed event aggregator. window=0 means flush on every push.
  lurek.patterns.newList( )  -> List  -- Creates an ordered, resizable list.
  lurek.patterns.newMediator( )  -> Mediator  -- Creates a new named-channel message broker.
  lurek.patterns.newObjectPool( )  -> ObjectPool  -- Creates a new ObjectPool instance.
  lurek.patterns.newObserver( [name] )  -> Observer  -- Creates a new reactive property Observer.
  lurek.patterns.newPriorityQueue( [name] )  -> PriorityQueue  -- Creates a stable priority-ordered task queue.
  lurek.patterns.newQueue( [capacity] )  -> FIFO  -- Creates a FIFO queue. capacity=0 means unlimited.
  lurek.patterns.newRelationshipManager( )  -> RelationshipManager  -- Creates a new entity relationship manager.
  lurek.patterns.newRing( capacity, [name] )  -> Ring  -- Creates a fixed-capacity circular history buffer.
  lurek.patterns.newServiceLocator( )  -> ServiceLocator  -- Creates a new ServiceLocator instance.
  lurek.patterns.newSet( )  -> Set  -- Creates an unordered set that rejects duplicate values (by string key).
  lurek.patterns.newSimpleState( )  -> SimpleState  -- Creates a new SimpleState finite state machine instance.
  lurek.patterns.newStack( [capacity] )  -> LIFO  -- Creates a LIFO stack. capacity=0 means unlimited.
  lurek.patterns.newStrategy( )  -> Strategy  -- Creates a new strategy registry.
  lurek.patterns.newThrottle( interval )  -> Throttle  -- Creates a leading-edge rate limiter that fires at most once per interval seconds.
```

**`LBlackboard`** methods:

> Lua wrapper for the Blackboard pattern.

```lua
  LBlackboard:clear( key )  -- Removes a fact from the blackboard.
  LBlackboard:clearAll( )  -- Clears all facts from the blackboard.
  LBlackboard:get( key )  -- Gets a fact from the blackboard. Returns nil if not set.
  LBlackboard:getRevision( )  -> Revision  -- Returns the monotonic revision counter (incremented on every write).
  LBlackboard:has( key )  -- Returns true when the key has a non-nil value.
  LBlackboard:keys( )  -- Returns all set fact keys as a table.
  LBlackboard:set( key, value )  -- Sets a fact on the blackboard. Accepts boolean, number, or string values.
  LBlackboard:snapshot( )  -- Returns all facts as a flat keyâ†’value table.
  LBlackboard:unwatch( id )  -- Removes a watcher subscription by id.
  LBlackboard:watch( key, callback )  -- Subscribes to changes on a specific key (or "*" for all changes).
```

**`LCommandStack`** methods:

> Lua wrapper for the CommandStack pattern.

```lua
  LCommandStack:canRedo( )  -- Returns true if there is a command available to redo.
  LCommandStack:canUndo( )  -- Returns true if the most recent command can be undone.
  LCommandStack:clearAll( )  -- Clears all command history, releasing Lua registry values.
  LCommandStack:execute( name, exec_fn, [undo_fn] )  -- Executes a named command and records it in undo/redo history.
  LCommandStack:getCurrentName( )  -> CurrentName  -- Returns the name of the most recently executed command, or nil.
  LCommandStack:getHistorySize( )  -> HistorySize  -- Returns the total number of recorded commands (undo + redo).
  LCommandStack:redo( )  -- Re-executes the next undone command. Returns true if successful.
  LCommandStack:undo( )  -- Undoes the most recent command. Returns true if successful.
```

**`LDebounce`** methods:

> Lua wrapper for the Debounce pattern.

```lua
  LDebounce:cancel( )  -- Cancels the pending trigger without firing.
  LDebounce:getFireCount( )  -> FireCount  -- Returns the total number of times this debounce has fired.
  LDebounce:isPending( )  -- Returns true when a trigger is pending.
  LDebounce:onFire( f )  -- Sets the callback invoked when the debounce fires.
  LDebounce:trigger( )  -- Records an input event, resetting the idle timer.
  LDebounce:update( dt )  -- Advances the idle timer by dt seconds; fires the callback if idle wait expired.
```

**`LEventBus`** methods:

> Lua wrapper for the EventBus pattern.

```lua
  LEventBus:clear( event )  -- Removes all listeners for a specific event.
  LEventBus:clearAll( )  -- Removes all listeners on this EventBus.
  LEventBus:emit( ... )  -- Dispatches an event, calling all registered listeners in priority order.
  LEventBus:getEvents( )  -> Events  -- Returns all event names that have at least one listener.
  LEventBus:getListenerCount( event )  -> ListenerCount  -- Returns the number of listeners registered for an event.
  LEventBus:off( id )  -- Removes a previously registered event listener by subscription ID.
  LEventBus:on( event, callback, [priority] )  -- Registers a listener callback for an event.
```

**`LFactory`** methods:

> Lua wrapper for the Factory pattern.

```lua
  LFactory:alias( alias, canonical )  -- Registers an alias pointing to an existing canonical type name.
  LFactory:clearAll( )  -- Removes all registered type constructors and aliases.
  LFactory:create( ... )  -- Creates an instance of the named type by invoking its constructor.
  LFactory:getTypes( )  -> Types  -- Returns a table of all registered type names.
  LFactory:has( type_name )  -- Returns true if the named type (or alias) is registered.
  LFactory:register( type_name, ctor )  -- Registers a named type constructor function.
  LFactory:remove( type_name )  -- Unregisters a type constructor (and any aliases pointing to it).
```

**`LFunnel`** methods:

> Lua wrapper for the Funnel (event aggregator) pattern.

```lua
  LFunnel:discard( )  -- Discards all buffered entries without flushing.
  LFunnel:flush( )  -- Manually flushes all pending entries, invoking the onFlush callback.
  LFunnel:getFlushCount( )  -> FlushCount  -- Returns the total number of flushes performed.
  LFunnel:onFlush( f )  -- Sets a callback invoked when the funnel flushes. Receives a table of {tag, value} entries.
  LFunnel:pendingCount( )  -- Returns the number of buffered entries not yet flushed.
  LFunnel:push( tag, [value] )  -- Adds an event to the funnel. Immediately flushes if max_entries reached or window is 0.
  LFunnel:update( dt )  -- Advances the window timer by dt seconds; flushes when window expires.
```

**`LList`** methods:

> Lua wrapper for an ordered, resizable list.

```lua
  LList:add( value )  -- Appends a value to the end of the list.
  LList:clear( )  -- Removes all values from the list.
  LList:contains( value )  -- Returns true if the list contains a value equal to the given Lua value (string/number/b...
  LList:get( index )  -- Returns the value at a 1-based index, or nil.
  LList:isEmpty( )  -- Returns true if the list is empty.
  LList:len( )  -- Returns the number of items in the list.
  LList:remove( index )  -- Removes and returns the value at a 1-based index.
  LList:set( index, value )  -- Replaces the value at a 1-based index.
  LList:toArray( )  -- Returns all items as a Lua table.
```

**`LMediator`** methods:

> Lua wrapper for the Mediator pattern.

```lua
  LMediator:broadcast( ... )  -- Dispatches a message to all handlers across all channels.
  LMediator:channels( )  -- Returns all registered channel names.
  LMediator:clear( )  -- Removes all channels and handlers.
  LMediator:handlerCount( channel )  -- Returns the number of handlers on a channel.
  LMediator:off( channel, id )  -- Unregisters a handler by ID.
  LMediator:on( channel, callback )  -- Registers a handler callback on a channel; returns handler ID.
  LMediator:removeChannel( channel )  -- Removes a channel and all its handlers.
  LMediator:send( ... )  -- Dispatches a message to all handlers on a channel.
```

**`LObjectPool`** methods:

> Lua wrapper for the ObjectPool pattern.

```lua
  LObjectPool:acquire( )  -- Acquires an available object from the pool; returns nil if empty.
  LObjectPool:add( value )  -- Inserts a pre-built object into the available pool.
  LObjectPool:clearAll( )  -- Clears all objects from the pool, releasing Lua registry values.
  LObjectPool:getActiveCount( )  -> ActiveCount  -- Returns the number of currently active (acquired) objects.
  LObjectPool:getAvailableCount( )  -> AvailableCount  -- Returns the number of available (idle) objects in the pool.
  LObjectPool:getTotalCount( )  -> TotalCount  -- Returns the total number of tracked objects (active + available).
  LObjectPool:release( value )  -- Returns an object to the available pool.
```

**`LObserver`** methods:

> Lua wrapper for the Observer pattern.

```lua
  LObserver:get( key )  -- Gets a property value, or nil if not set.
  LObserver:getCount( )  -> Count  -- Returns the total number of active subscriptions.
  LObserver:set( key, new_val )  -- Sets a property value and fires subscribed watchers.
  LObserver:subscribe( key, callback, [once] )  -- Subscribes to changes on a property key (or "*" for all).
  LObserver:unsubscribe( id )  -- Removes a subscription by id.
```

**`LPriorityQueue`** methods:

> Lua wrapper for the PriorityQueue pattern.

```lua
  LPriorityQueue:clearAll( )  -- Removes all items from the queue.
  LPriorityQueue:isEmpty( )  -- Returns true when the queue has no items.
  LPriorityQueue:len( )  -- Returns the number of items in the queue.
  LPriorityQueue:peek( )  -- Returns the highest-priority item without removing it, or nil if empty.
  LPriorityQueue:pop( )  -- Removes and returns the highest-priority item, or nil if empty.
  LPriorityQueue:push( priority, value, [label] )  -- Inserts an item with a priority. Higher priorities are dequeued first.
```

**`LQueue`** methods:

> Lua wrapper for a FIFO queue.

```lua
  LQueue:clear( )  -- Removes all values from the queue.
  LQueue:dequeue( )  -- Removes and returns the front value, or nil if empty.
  LQueue:enqueue( value )  -- Adds a value to the back of the queue. Returns false if capacity is full.
  LQueue:front( )  -- Returns the front value without removing it, or nil if empty.
  LQueue:isEmpty( )  -- Returns true if the queue is empty.
  LQueue:isFull( )  -- Returns true if the queue is at its capacity limit.
  LQueue:len( )  -- Returns the number of items in the queue.
  LQueue:toArray( )  -- Returns all items as a Lua table (front to back).
```

**`LRelationshipManager`** methods:

> Lua wrapper for the RelationshipManager pattern.

```lua
  LRelationshipManager:adjustValue( a, b, delta )  -- Adjusts the numeric relationship value by a delta.
  LRelationshipManager:defineType( name, levels, [default_level] )  -- Defines a relationship type with ordered levels.
  LRelationshipManager:getLevel( a, b, type_name )  -> Level  -- Returns the named level for a typed relationship, or nil.
  LRelationshipManager:getValue( a, b )  -> Value  -- Returns the numeric relationship value between two entities (default 0.0).
  LRelationshipManager:pairCount( )  -- Returns the total number of stored relationship pairs.
  LRelationshipManager:removePair( a, b )  -- Removes all relationship data between two entities.
  LRelationshipManager:removeType( name )  -- Removes a relationship type definition.
  LRelationshipManager:setLevel( a, b, type_name, level )  -- Sets a named level for a typed relationship between two entities.
  LRelationshipManager:setValue( a, b, value )  -- Sets the numeric relationship value between two entities.
  LRelationshipManager:typeNames( )  -- Returns all defined relationship type names.
```

**`LRing`** methods:

> Lua wrapper for the Ring (circular buffer) pattern.

```lua
  LRing:average( )  -- Returns the average of all numeric values, or 0 if empty.
  LRing:clear( )  -- Removes all entries from the ring.
  LRing:isFull( )  -- Returns true when the ring is at capacity.
  LRing:latest( )  -- Returns the most recently pushed entry, or nil.
  LRing:len( )  -- Returns the number of entries currently in the ring.
  LRing:push( value, [tag] )  -- Pushes a value (number or string) with an optional tag. Overwrites oldest on overflow.
  LRing:sum( )  -- Returns the sum of all numeric values in the ring.
  LRing:toArray( )  -- Returns all entries (oldest first) as an array of {id, tag, value?, text?} tables.
```

**`LServiceLocator`** methods:

> Lua wrapper for the ServiceLocator pattern.

```lua
  LServiceLocator:clearAll( )  -- Removes all registered services.
  LServiceLocator:getServices( )  -> Services  -- Returns a table of all registered service names.
  LServiceLocator:has( name )  -- Returns true if a service with the given name is registered.
  LServiceLocator:locate( name )  -- Retrieves a registered service by name; returns nil if not found.
  LServiceLocator:provide( name, value )  -- Registers a named service with an associated Lua value.
  LServiceLocator:remove( name )  -- Unregisters and removes a named service.
```

**`LSet`** methods:

> Lua wrapper for an unordered set. Values are keyed by their string representation.

```lua
  LSet:add( key )  -- Adds a string key to the set. Returns true if it was not already present.
  LSet:clear( )  -- Removes all keys from the set.
  LSet:has( key )  -- Returns true if the key is in the set.
  LSet:intersection( other )  -- Returns the intersection of this set and another as a new Set.
  LSet:isEmpty( )  -- Returns true if the set is empty.
  LSet:len( )  -- Returns the number of distinct keys in the set.
  LSet:remove( key )  -- Removes a key from the set. Returns true if it was present.
  LSet:toArray( )  -- Returns all keys as a Lua table (unordered).
  LSet:union( other )  -- Returns the union of this set and another as a new Set.
```

**`LSimpleState`** methods:

> Lua wrapper for the SimpleState finite state machine pattern.

```lua
  LSimpleState:addState( name, [callbacks] )  -- Registers a named state with optional enter, exit, and update callbacks.
  LSimpleState:clearAll( )  -- Removes all states and callbacks from this state machine.
  LSimpleState:getCurrent( )  -> Current  -- Returns the name of the current state, or nil if none is active.
  LSimpleState:getStates( )  -> States  -- Returns a table of all registered state names.
  LSimpleState:hasState( name )  -- Returns true if a state with the given name is registered.
  LSimpleState:transitionTo( name )  -- Transitions to a named state, calling exit/enter callbacks as needed.
  LSimpleState:update( dt )  -- Calls the update callback of the current state with the given delta time.
```

**`LStack`** methods:

> Lua wrapper for a LIFO stack.

```lua
  LStack:clear( )  -- Removes all values from the stack.
  LStack:isEmpty( )  -- Returns true if the stack is empty.
  LStack:isFull( )  -- Returns true if the stack is at its capacity limit.
  LStack:len( )  -- Returns the number of items on the stack.
  LStack:peek( )  -- Returns the top value without removing it, or nil if empty.
  LStack:pop( )  -- Removes and returns the top value, or nil if empty.
  LStack:push( value )  -- Pushes a value onto the stack. Returns false if capacity is full.
  LStack:toArray( )  -- Returns all items as a Lua table (bottom to top).
```

**`LStrategy`** methods:

> Lua wrapper for the Strategy pattern.

```lua
  LStrategy:clear( )  -- Removes all strategies and clears the active selection.
  LStrategy:execute( ... )  -- Calls the currently active strategy function with the given arguments.
  LStrategy:getCurrent( )  -> Current  -- Returns the name of the active strategy, or nil.
  LStrategy:has( name )  -- Returns true if a strategy with this name is registered.
  LStrategy:names( )  -- Returns all registered strategy names.
  LStrategy:register( name, callback )  -- Registers a named strategy function.
  LStrategy:remove( name )  -- Removes a strategy by name.
  LStrategy:set( name )  -- Sets the active strategy by name. Returns false if not registered.
```

**`LThrottle`** methods:

> Lua wrapper for the Throttle pattern.

```lua
  LThrottle:getFireCount( )  -> FireCount  -- Returns the total number of times this throttle has fired.
  LThrottle:getProgress( )  -> Progress  -- Returns the normalised progress through the current interval [0, 1].
  LThrottle:onFire( f )  -- Sets the callback invoked when the throttle fires.
  LThrottle:reset( )  -- Resets the elapsed counter without firing.
  LThrottle:setEnabled( v )  -- Enables or disables the throttle.
  LThrottle:update( dt )  -- Advances the timer by dt seconds; fires the callback if the interval elapsed.
```

## lurek.pipeline {#pipeline}

> `lurek.pipeline` â€” DAG-based pipeline orchestrator for composing multi-step workflows.

```lua
  lurek.pipeline.fromTable( def )  -- Deserialises a pipeline from a definition table.
  lurek.pipeline.newPipeline( [name] )  -> Pipeline  -- Creates a new empty pipeline with the given name (defaults to "pipeline").
  lurek.pipeline.newStep( name, [callback] )  -> Step  -- Creates a new pipeline step with the given name and optional callback.
```

**`LPipeline`** methods:

> Lua-side wrapper around a [`Pipeline`] DAG with scheduler and Lua callback registry.

```lua
  LPipeline:addConditional( name, deps_tbl, cb, cond )  -- Adds a step with a runtime condition guard: the step is skipped when `when_fn()` return...
  LPipeline:addStep( step_ud )  -- Adds a step to the pipeline. Returns self for chaining.
  LPipeline:addSubPipeline( sub_ud, alias, [deps_tbl] )  -- Inlines all steps from `sub_pipeline` into this pipeline, prefixing
  LPipeline:cancel( )  -- Cancels all pending and waiting steps.
  LPipeline:clear( )  -- Clears all steps from the pipeline.
  LPipeline:getContext( )  -> Context  -- Returns the stored async context table, or nil.
  LPipeline:getErrorMode( )  -> ErrorMode  -- Returns the current error mode as a string.
  LPipeline:getExecutionOrder( )  -> ExecutionOrder  -- Returns the topological execution order as an array of step names.
  LPipeline:getName( )  -> Name  -- Returns the pipeline's name.
  LPipeline:getParallelGroups( )  -> ParallelGroups  -- Returns parallel execution groups as a nested array of step name arrays.
  LPipeline:getResult( )  -> Result  -- Returns the current result table built from step states, or nil.
  LPipeline:getStep( name )  -> LuaStep  -- Returns the LuaStep wrapper for the named step, or nil.
  LPipeline:getStepCount( )  -> StepCount  -- Returns the total number of steps.
  LPipeline:getSteps( )  -> Lua  -- Returns a Lua array of all step wrappers in the pipeline.
  LPipeline:getStepsByTag( tag )  -> Lua  -- Returns a Lua array of all steps whose tag matches the given string.
  LPipeline:isComplete( )  -- Returns true if all steps have reached a terminal state.
  LPipeline:isRunning( )  -- Returns true if the pipeline is currently running asynchronously.
  LPipeline:onProgress( cb )  -- Registers a callback invoked after every step with `(step_name, status)`.
  LPipeline:removeStep( name )  -- Removes a step from the pipeline by name.
  LPipeline:reset( )  -- Resets all step states and clears the async context.
  LPipeline:run( [context] )  -- Executes the pipeline synchronously in topological order.
  LPipeline:runAsync( [context] )  -- Starts an async pipeline run. Steps are executed one-per-frame via update(dt).
  LPipeline:setErrorMode( mode )  -- Sets the pipeline error mode: "abort" or "continue".
  LPipeline:setName( name )  -- Sets the pipeline's name.
  LPipeline:setOnComplete( [cb] )  -- Sets the callback to invoke when the pipeline completes.
  LPipeline:setOnStepComplete( [cb] )  -- Sets the callback to invoke each time a step completes successfully.
  LPipeline:setOnStepError( [cb] )  -- Sets the callback to invoke each time a step fails.
  LPipeline:toAscii( )  -- Returns a multi-line ASCII string visualising the pipeline DAG.
  LPipeline:toTable( )  -- Serialises the pipeline definition to a Lua table (no callbacks).
  LPipeline:type( )  -- Returns the type name of this object.
  LPipeline:typeOf( name )  -- Returns the type identifier string of this pipeline stage object.
  LPipeline:update( dt )  -- Advances the async pipeline by one tick. Returns true when all steps are done.
  LPipeline:validate( )  -- Validates the pipeline DAG. Returns (ok, error_array).
```

**`LPipelineStep`** methods:

> Lua-side wrapper around a single [`PipelineStep`], plus Lua callback registry keys.

```lua
  LPipelineStep:dependsOn( dep )  -- Adds a dependency on another step by name or PipelineStep. Returns self for chaining
  LPipelineStep:getAttempt( )  -> Attempt  -- Returns the number of execution attempts so far
  LPipelineStep:getData( key )  -> Data  -- Retrieves a metadata value by key, returning nil if not found
  LPipelineStep:getDelay( )  -> Delay  -- Returns the configured delay in seconds
  LPipelineStep:getDependencies( )  -> Dependencies  -- Returns the list of dependency step names
  LPipelineStep:getDependencyCount( )  -> DependencyCount  -- Returns the number of declared dependencies
  LPipelineStep:getDuration( )  -> Duration  -- Returns total seconds spent executing this step
  LPipelineStep:getError( )  -> Error  -- Returns the error message from the last failed attempt, or nil
  LPipelineStep:getName( )  -> Name  -- Returns the unique name of this step
  LPipelineStep:getRetryCount( )  -> RetryCount  -- Returns the configured retry count
  LPipelineStep:getStatus( )  -> Status  -- Returns the current execution status as a string
  LPipelineStep:getTag( )  -> Tag  -- Returns the tag on this step, or nil if unset
  LPipelineStep:getTimeout( )  -> Timeout  -- Returns the timeout stored in metadata, or 0.0 if unset
  LPipelineStep:isOptional( )  -- Returns whether this step is marked as optional
  LPipelineStep:setCallback( cb )  -- Stores a Lua function as the execute callback for this step
  LPipelineStep:setCondition( [cond] )  -- Stores a Lua function (or nil) as the run-condition for this step
  LPipelineStep:setData( key, value )  -- Stores an arbitrary string value under the given key in step metadata
  LPipelineStep:setDelay( seconds )  -- Sets the delay in seconds to wait after dependencies finish
  LPipelineStep:setOnError( [cb] )  -- Stores a Lua function (or nil) to call if this step fails
  LPipelineStep:setOptional( optional )  -- Marks whether this step is optional (downstream steps continue on failure)
  LPipelineStep:setRetryCount( count )  -- Sets the maximum number of retry attempts on failure
  LPipelineStep:setRetryDelay( seconds )  -- Sets the delay in seconds between retry attempts
  LPipelineStep:setTag( tag )  -- Sets the tag on this step for grouping and filtering
  LPipelineStep:setTimeout( seconds )  -- Stores a timeout in seconds in the step's metadata
  LPipelineStep:type( )  -- Returns the type name "PipelineStep"
  LPipelineStep:typeOf( name )  -- Returns true when the given name matches "PipelineStep" or a parent type
```

## lurek.procgen {#procgen}

> `lurek.procgen` — Stateless procedural generation utilities.

```lua
  lurek.procgen.bspDungeon( [opts] )  -- Generates a dungeon using Binary Space Partitioning.
  lurek.procgen.cellularAutomata( w, h, [opts] )  -- Generates a cave-like map using cellular automata.
  lurek.procgen.floodFill( )  -- BFS flood fill on a flat grid of bytes.
  lurek.procgen.generateName( )  -- Generates a single procedural name using a Markov chain.
  lurek.procgen.generateNames( )  -- Generates N procedural names using a Markov chain.
  lurek.procgen.heightmap( [opts] )  -- Generates a heightmap using fractal noise.
  lurek.procgen.lsystem( opts )  -- Generates an L-system string.
  lurek.procgen.lsystemSegments( opts, [angle_deg], [step] )  -- Generates L-system line segments for rendering.
  lurek.procgen.noiseMap( width, height, [opts] )  -- Generates a noise map using the configurable NoiseGenerator.
  lurek.procgen.noiseMapParallel( width, height, [opts] )  -- Generates a noise map using rayon parallel processing.
  lurek.procgen.perlinNoise( x, y, px, py )  -- Evaluates periodic Perlin noise at a point.
  lurek.procgen.poissonDisk( w, h, min_dist, [max_attempts], [seed] )  -- Generates Poisson disk sample points using Bridson's algorithm.
  lurek.procgen.roomsDungeon( [opts] )  -- Generates a rooms-and-corridors dungeon.
  lurek.procgen.simplex2d( x, y )  -- Returns a single Simplex noise value at the given 2-D coordinate.
  lurek.procgen.simplex3d( x, y, z )  -- Returns a single Simplex noise value at the given 3-D coordinate.
  lurek.procgen.voronoi( w, h, pts_tbl, [opts_tbl] )  -- Generates a Voronoi diagram for a set of seed points.
  lurek.procgen.wfcGenerate( opts )  -- Generates a tile grid using Wave Function Collapse.
  lurek.procgen.worldGraph( width, height, region_count, [seed] )  -- Generates a world graph with scattered regions and edges.
```

## lurek.raycaster {#raycaster}

> `lurek.raycaster` - DDA grid raycasting for retro FPS and dungeon-crawler games.

```lua
  lurek.raycaster.distanceShade( distance, max_distance )  -- Returns distance-based brightness in [0, 1].
  lurek.raycaster.new( w, h )  -- Creates a new raycaster grid of the given dimensions.
  lurek.raycaster.newDoorManager( )  -> DoorManager  -- Creates a new empty door manager.
  lurek.raycaster.newHeightMap( w, h )  -> HeightMap  -- Creates a new height map with default floor (0.0) and ceiling (1.0) values.
  lurek.raycaster.newMap( w, h )  -> Map  -- Alias for `new`. Creates a new raycaster grid of the given dimensions.
  lurek.raycaster.newPointLight( x, y, r, g, b, radius, intensity )  -> PointLight  -- Creates a point light for use in raycaster scene lighting.
  lurek.raycaster.newSpriteManager( )  -> SpriteManager  -- Creates a new empty batch sprite manager for depth-sorted projection.
  lurek.raycaster.projectColumn( distance, fov, screen_height )  -- Projects a wall distance to screen-space drawing parameters.
```

**`LDoorManager`** methods:

> Lua-side wrapper around a [`DoorManager`], managing sliding doors in a level.

```lua
  LDoorManager:addDoor( x, y, dir_str, speed )  -- Registers a door at grid position (x, y).
  LDoorManager:closeDoor( index )  -- Begins closing the door at the given index.
  LDoorManager:count( )  -- Returns the number of registered doors.
  LDoorManager:getDoor( index )  -> Door  -- Returns the state table for door at index, or nil if out of range.
  LDoorManager:openDoor( index )  -- Begins opening the door at the given index.
  LDoorManager:type( )  -- Returns the type string "DoorManager".
  LDoorManager:typeOf( )  -- Returns the type string "DoorManager".
  LDoorManager:update( dt )  -- Advances all door animations by dt seconds.
```

**`LHeightMap`** methods:

> Lua-side wrapper around a [`HeightMap`] for variable floor/ceiling heights.

```lua
  LHeightMap:ceilingAt( x, y )  -- Returns the ceiling height at (x, y). Returns 1.0 for out-of-bounds.
  LHeightMap:floorAt( x, y )  -- Returns the floor height at (x, y). Returns 0.0 for out-of-bounds.
  LHeightMap:setCeiling( x, y, h )  -- Sets the ceiling height at (x, y).
  LHeightMap:setFloor( x, y, h )  -- Sets the floor height at (x, y).
  LHeightMap:type( )  -- Returns the type string "HeightMap".
  LHeightMap:typeOf( )  -- Returns the type string "HeightMap".
```

**`LPointLight`** methods:

> Lua-side value wrapper around a raycaster [`PointLight`].

```lua
  LPointLight:color( )  -> RGB  -- Returns the RGB color as three separate values.
  LPointLight:intensity( )  -- Returns the intensity multiplier.
  LPointLight:radius( )  -- Returns the illumination radius.
  LPointLight:set( x, y, r, g, b, radius, intensity )  -- Updates all light properties at once.
  LPointLight:type( )  -- Returns the type string "PointLight".
  LPointLight:typeOf( )  -- Returns the type string "PointLight".
  LPointLight:x( )  -- Returns the world-space X position.
  LPointLight:y( )  -- Returns the world-space Y position.
```

**`LRaycaster`** methods:

> Lua-side wrapper around a [`Raycaster2D`] grid.

```lua
  LRaycaster:buildScene( )  -- Builds a raycaster scene and stores it in SharedState for GPU rendering.
  LRaycaster:castFloorRow( )  -- Computes floor (or ceiling) texture UV coordinates for one horizontal screen row.
  LRaycaster:castRay( ox, oy, angle, max_dist )  -- Casts a single ray and returns a hit table, or nil if nothing was hit.
  LRaycaster:castRayMulti( ox, oy, angle, max_dist, [max_hits] )  -- Casts a ray collecting up to max_hits wall layers, continuing through
  LRaycaster:castRays( ox, oy, angle, fov, count, max_dist )  -- Casts multiple rays across a field of view, returns an array of hit tables.
  LRaycaster:castRaysFlat( ox, oy, angle, fov, count, max_dist )  -- Casts multiple rays and returns a flat array of 5 floats per ray.
  LRaycaster:drawCameraSweep( x, y, fov, max_dist, num_frames, fw, fh )  -- Renders a mosaic of first-person views from evenly spaced angles to an ImageData.
  LRaycaster:drawDepthMap( )  -- Renders a depth-map column view to an ImageData.
  LRaycaster:drawLineOfSight( ax, ay, bx, by, scale )  -- Renders a line-of-sight test between two points to an ImageData.
  LRaycaster:drawTopDown( px, py, angle, scale )  -- Renders a top-down grid view with player marker to an ImageData.
  LRaycaster:drawView( px, py, angle, fov, w, h, max_dist )  -- Renders a first-person column view to an ImageData.
  LRaycaster:getCell( x, y )  -> Cell  -- Returns the cell value at (x, y).
  LRaycaster:getWallAlpha( tile_type )  -> WallAlpha  -- Returns the opacity for a wall tile type. Returns 1.0 if not set.
  LRaycaster:height( )  -- Returns the grid height in cells.
  LRaycaster:isBlocked( x, y )  -- Returns true when the cell at (x, y) is a wall (value > 0).
  LRaycaster:lineOfSight( x1, y1, x2, y2 )  -- Checks line of sight between two points using DDA traversal.
  LRaycaster:projectSprite( sx, sy, px, py, pa, fov, screen_w )  -- Projects a world-space sprite onto screen space.
  LRaycaster:setCell( x, y, val )  -- Sets the cell value at grid position (x, y).
  LRaycaster:setCells( cells_tbl )  -- Replaces all grid cells from a flat array of values in row-major order.
  LRaycaster:setWallAlpha( tile_type, alpha )  -- Sets the opacity for a wall tile type. Alpha is clamped to [0, 1].
  LRaycaster:type( )  -- Returns the type name of this object.
  LRaycaster:typeOf( name )  -- Returns true if this object is of the given type.
  LRaycaster:width( )  -- Returns the grid width in cells.
```

**`LSpriteManager`** methods:

> Lua-side wrapper around a [`SpriteManager`] for batch depth-sorted sprite projection.

```lua
  LSpriteManager:add( x, y, texture, [scale] )  -- Adds a sprite at world position (x, y) and returns its unique id.
  LSpriteManager:clear( )  -- Removes all sprites from the manager.
  LSpriteManager:remove( id )  -- Removes the sprite with the given id. No-op if not found.
  LSpriteManager:setPosition( id, x, y )  -- Moves the sprite with the given id to world (x, y).
  LSpriteManager:setVisible( id, visible )  -- Shows or hides the sprite with the given id.
  LSpriteManager:sortAndProject( cam_x, cam_y, cam_angle )  -- Returns an array of visible sprites sorted back-to-front from camera position.
  LSpriteManager:type( )  -- Returns the type string "SpriteManager".
  LSpriteManager:typeOf( )  -- Returns the type string "SpriteManager".
```

## lurek.serial {#serial}

> `lurek.serial` â€” Format-agnostic string serialization: JSON, TOML, and CSV.

```lua
  lurek.serial.decodeMsgPack( )  -- Decodes a binary MessagePack string into a Lua table.
  lurek.serial.decodeXml( s )  -- Parses an XML string and returns a nested Lua table.
  lurek.serial.encodeMsgPack( value )  -- Encodes a Lua table to a binary MessagePack string.
  lurek.serial.fromCsv( s, [delim], [headers] )  -- Parses a CSV string and returns a sequence of row tables.
  lurek.serial.fromJson( s )  -- Parses a JSON string and returns a Lua table.
  lurek.serial.fromToml( s )  -- Parses a TOML string and returns a Lua table.
  lurek.serial.toCsv( value, [delim], [headers] )  -- Serializes a sequence of row tables to a CSV string.
  lurek.serial.toJson( value, [pretty] )  -- Serializes a Lua value to a JSON string.
  lurek.serial.toToml( value )  -- Serializes a Lua table to a TOML string.
  lurek.serial.validate( value, schema )  -- Validates a Lua table against a schema table.
```

## lurek.spine {#spine}

> `lurek.spine` — Skeletal animation: bone hierarchies, slots, world-transform propagation,

```lua
  lurek.spine.newSkeleton( name )  -> Skeleton  -- Creates a new empty skeleton with the given name.
  lurek.spine.newSkeletonAnimation( name, duration )  -> SkeletonAnimation  -- Creates a new empty SkeletonAnimation clip with the given name and duration.
```

**`LSkeleton`** methods:

> Lua-side wrapper around a [`Skeleton`].

```lua
  LSkeleton:addAnimation( anim_ud )  -- Adds a SkeletonAnimation to this skeleton's library.
  LSkeleton:addBone( name, [opts] )  -- Adds a root bone with optional local transform and returns its index.
  LSkeleton:addChildBone( name, parent_idx, [opts] )  -- Adds a child bone attached to a parent and returns its index.
  LSkeleton:addIKConstraint( name, chain_tbl, [bend_positive] )  -- Adds a two-bone IK constraint and returns its index.
  LSkeleton:addSkin( name )  -- Registers a new empty skin by name.
  LSkeleton:addSlot( name, bone_idx, [attachment] )  -- Adds a slot bound to a bone and returns its index.
  LSkeleton:blendAnimation( anim_ud, time, [blend_weight] )  -- Evaluates `anim` at `time` and blends the result into this skeleton
  LSkeleton:boneCount( )  -- Returns the total number of bones.
  LSkeleton:drawToImage( w, h )  -- Renders the skeleton as a stick-figure debug view into a new ImageData.
  LSkeleton:findBone( name )  -- Returns the index of the named bone, or nil if not found.
  LSkeleton:findSlot( name )  -- Returns the index of the named slot, or nil if not found.
  LSkeleton:getAnimationTime( )  -> AnimationTime  -- Returns the current playback time in seconds of the active animation.
  LSkeleton:getBoneWorld( idx )  -> BoneWorld  -- Returns the world-space transform of a bone as a table, or nil if out of range.
  LSkeleton:getSkin( )  -> Skin  -- Returns the name of the currently active skin, or nil.
  LSkeleton:playAnimation( name, [looping] )  -- Starts playback of the named skeletal animation clip.
  LSkeleton:setIKTarget( name, x, y )  -- Sets the world-space target position for the named IK constraint.
  LSkeleton:setPosition( x, y )  -- Sets the root bone position and propagates world transforms.
  LSkeleton:setSkin( name )  -- Activates the named skin for attachment lookups.
  LSkeleton:setSkinMapping( skin, slot, attachment )  -- Registers a slot-to-attachment mapping in the named skin.
  LSkeleton:slotCount( )  -- Returns the total number of slots.
  LSkeleton:stopAnimation( )  -- Stops the current skeletal animation.
  LSkeleton:type( )  -- Returns the type name of this object.
  LSkeleton:typeOf( name )  -- Returns true if this object is of the given type.
  LSkeleton:updateAnimation( dt )  -- Advances the playing animation by `dt` seconds and applies keyframes.
  LSkeleton:updateWorldTransforms( )  -- Propagates local transforms down the bone hierarchy to compute world positions.
```

**`LSkeletonAnimation`** methods:

> Lua-side wrapper around a [`SkeletonAnimation`] keyframe clip.

```lua
  LSkeletonAnimation:addEventKey( time, name, [value] )  -- Adds a named event marker at `time` seconds in the animation.
  LSkeletonAnimation:addKeyframe( )  -- Adds a keyframe to the bone timeline for the given property and bone index.
  LSkeletonAnimation:getDuration( )  -> Duration  -- Returns the total duration of the animation in seconds.
  LSkeletonAnimation:getEvents( from, to )  -> Events  -- Returns a list of event names that fall in the half-open interval `(from, to]`.
  LSkeletonAnimation:getTimelineCount( )  -> TimelineCount  -- Returns the number of bone timelines in this animation.
  LSkeletonAnimation:type( )  -- Returns the type name of this object.
  LSkeletonAnimation:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.sprite {#sprite}

> `lurek.sprite` â€” Sprite-sheet UV layout, named frame groups, atlas parsing,

```lua
  lurek.sprite.newAtlasSheet( atlas_ud, sw, sh )  -> AtlasSheet  -- Builds a SpriteSheet whose frames come from named entries in a SpriteAtlas.
  lurek.sprite.newRPGMakerSheet( tw, th )  -> RPGMaker  -- Creates an RPGMaker VX/Ace character sheet (3 cols Ă— 4 rows) with "down", "left", "rig...
  lurek.sprite.newSheet( tw, th, fw, fh )  -> Sheet  -- Creates a sprite sheet with a uniform grid of `frame_w Ă— frame_h` frames.
  lurek.sprite.parseAsepriteAtlas( json_str )  -- Parses an Aseprite JSON export string and returns a `SpriteAtlas`.
  lurek.sprite.parseAtlas( json_str )  -- Parses a TexturePacker JSON string (hash or array format) and returns a SpriteAtlas.
```

**`LSpriteAtlas`** methods:

> Lua-side wrapper around a [`SpriteAtlas`] named-region store.

```lua
  LSpriteAtlas:entryCount( )  -- Returns the total number of named regions in the atlas.
  LSpriteAtlas:entryNames( )  -- Returns a sequential table of all region names.
  LSpriteAtlas:getByIndex( index )  -> ByIndex  -- Returns the region at the given 1-based insertion index, or nil.
  LSpriteAtlas:getEntry( name )  -> Entry  -- Returns the named region as a table `{name, x, y, w, h, rotated}`, or nil.
  LSpriteAtlas:getFlipped( name, flip_x, flip_y )  -> Flipped  -- Returns a copy of the named region with `flip_x` and `flip_y` flags set.
  LSpriteAtlas:type( )  -- Returns the type name of this object.
  LSpriteAtlas:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LSpriteSheet`** methods:

> Lua-side wrapper around a [`SpriteSheet`] frame-grid calculator.

```lua
  LSpriteSheet:drawToImage( w, h )  -- Renders the sheet grid as a debug view into a new ImageData.
  LSpriteSheet:getColumn( col )  -> Column  -- Returns a sequential table of quad tables for every frame in the given column.
  LSpriteSheet:getFrame( index )  -> Frame  -- Returns the quad for the 0-based frame index, or nil if out of range.
  LSpriteSheet:getFrameCount( )  -> FrameCount  -- Returns the total number of frames in the sheet.
  LSpriteSheet:getFrameSize( )  -> FrameSize  -- Returns the width and height of a single frame cell in pixels.
  LSpriteSheet:getGridSize( )  -> GridSize  -- Returns the number of columns and rows in the grid.
  LSpriteSheet:getGroupFrames( name )  -> GroupFrames  -- Returns a sequential table of quad tables for the named frame group, or nil.
  LSpriteSheet:getGroupNames( )  -> GroupNames  -- Returns a sequential table of all defined group names.
  LSpriteSheet:getRow( row )  -> Row  -- Returns a sequential table of quad tables for every frame in the given row.
  LSpriteSheet:nameGroup( name, start, count )  -- Registers a named frame group starting at `start_frame` with `count` frames.
  LSpriteSheet:type( )  -- Returns the type name of this object.
  LSpriteSheet:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.terminal {#terminal}

> `lurek.terminal` — Grid-based character-cell terminal emulator and widget toolkit.

```lua
  lurek.terminal.addCompletion( candidate )  -- Adds a candidate string to the tab-completion engine.
  lurek.terminal.applyTheme( term_ud, theme )  -- Applies a named colour theme to a terminal, recolouring all existing cells.
  lurek.terminal.clearCmdHistory( term_ud )  -- Clears all entries from this terminal's command history.
  lurek.terminal.clearCompletions( )  -- Clears all completion candidates.
  lurek.terminal.cmdHistoryLen( term_ud )  -- Returns the total number of entries in this terminal's command history.
  lurek.terminal.getCompletions( prefix )  -> Completions  -- Returns all registered candidates that start with `prefix`, as a sorted array.
  lurek.terminal.getMaxCols( )  -> MaxCols  -- Returns the maximum number of columns a Terminal can be constructed with.
  lurek.terminal.getMaxRows( )  -> MaxRows  -- Returns the maximum number of rows a Terminal can be constructed with.
  lurek.terminal.getScrollback( term_ud, offset, count )  -> Scrollback  -- Returns a table of lines from the scrollback buffer.
  lurek.terminal.newBorder( col, row, width, height )  -> Border  -- Creates a new decorative border widget at 1-based coordinates.
  lurek.terminal.newButton( )  -> Button  -- Creates a new button widget at 1-based coordinates.
  lurek.terminal.newLabel( col, row, [text] )  -> Label  -- Creates a new label widget at 1-based coordinates.
  lurek.terminal.newList( col, row, width, height )  -> List  -- Creates a new scrollable list widget at 1-based coordinates.
  lurek.terminal.newPanel( col, row, [width], [height] )  -> Panel  -- Creates a new container panel widget at 1-based coordinates.
  lurek.terminal.newTerminal( [cols], [rows] )  -> Terminal  -- Creates a new terminal grid with the given dimensions.
  lurek.terminal.newTextBox( col, row, width )  -> TextBox  -- Creates a new single-line text box widget at 1-based coordinates.
  lurek.terminal.nextCmd( term_ud )  -- Steps one entry forward in command history (toward newer commands).
  lurek.terminal.nextCompletion( prefix )  -- Returns the next candidate for `prefix`, cycling on repeated calls.
  lurek.terminal.parseAnsi( text )  -- Parses `text` into coloured spans.  Returns an array of tables, each with
  lurek.terminal.prevCmd( term_ud )  -- Steps one entry back in command history (toward older commands).
  lurek.terminal.printAnsi( t_ud, col, row, text )  -- Prints ANSI-escaped `text` onto terminal `t` starting at `(col, row)`.
  lurek.terminal.printHighlighted( )  -- Prints text at 1-based `(col, row)` with per-keyword colour highlighting.
  lurek.terminal.pushCmdHistory( term_ud, cmd )  -- Appends a command string to this terminal's history.
  lurek.terminal.pushScrollback( term_ud, line )  -- Appends a line to this terminal's scrollback buffer.
  lurek.terminal.removeCompletion( candidate )  -- Removes a candidate string from the tab-completion engine.
  lurek.terminal.resetCompletion( )  -- Resets the cycling cursor without clearing the candidate list.
  lurek.terminal.scrollbackLen( term_ud )  -- Returns the number of lines currently in this terminal's scrollback buffer.
  lurek.terminal.setScrollbackCap( term_ud, cap )  -- Sets the maximum number of lines retained in the scrollback buffer.
  lurek.terminal.stripAnsi( text )  -- Strips all ANSI escape codes from `text` and returns the plain string.
```

**`LTerminal`** methods:

> Lua-side wrapper around a [`Terminal`] with widget binding management.

```lua
  LTerminal:addWidget( widget_ud )  -- Attaches a widget to this terminal.
  LTerminal:autoResize( )  -- Resizes the window to exactly fit the terminal grid at the current font size.
  LTerminal:clear( )  -- Clears all cells to defaults.
  LTerminal:clearWidgets( )  -- Detaches all widgets from this terminal.
  LTerminal:get( col, row )  -- Returns the cell data at 1-based coordinates.
  LTerminal:getCellSize( )  -> CellSize  -- Returns the active cell size override as `{w, h}`, or `nil` if none is set.
  LTerminal:getDimensions( )  -> Dimensions  -- Returns the terminal grid dimensions.
  LTerminal:getFocused( )  -> Focused  -- Returns the currently focused widget, or nil.
  LTerminal:getWidgetCount( )  -> WidgetCount  -- Returns the number of attached widgets.
  LTerminal:keypressed( key )  -- Routes a key press to the focused widget and fires callbacks.
  LTerminal:mousepressed( px, py, [button] )  -- Routes a mouse press to widgets using pixel coordinates.
  LTerminal:removeWidget( widget_ud )  -- Detaches a widget from this terminal.
  LTerminal:render( [x], [y] )  -- Renders the terminal grid and widgets as render commands.
  LTerminal:resetCellSize( )  -- Removes the cell size override, restoring font-derived cell dimensions.
  LTerminal:set( ... )  -- Sets a cell at 1-based coordinates with character FG and BG colours.
  LTerminal:setCellSize( w, h )  -- Sets a per-terminal cell pixel size override, bypassing the font-derived size.
  LTerminal:setFocus( value )  -- Sets the focused widget, or clears focus if nil is passed.
  LTerminal:setFont( height )  -- Sets the terminal font by pixel height, snapping to the nearest built-in size.
  LTerminal:textinput( text )  -- Routes text input to the focused widget and fires callbacks.
  LTerminal:type( )  -- Returns the type name of this object.
  LTerminal:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LWidget`** methods:

> Lua-side wrapper around a [`Widget`] with attachment and callback state.

```lua
  LWidget:addChild( child_ud )  -- Adds a child widget to a panel widget.
  LWidget:addItem( item )  -- Adds an item to a list widget.
  LWidget:clearChildren( )  -- Removes all children from a panel widget.
  LWidget:clearItems( )  -- Removes all items from a list widget.
  LWidget:getChild( index )  -> Child  -- Returns a child widget from a panel by 1-based index, or nil.
  LWidget:getChildCount( )  -> ChildCount  -- Returns the number of children in a panel widget.
  LWidget:getColor( )  -> Color  -- Returns the colour of a label or border widget.
  LWidget:getItem( index )  -> Item  -- Returns a list item by 1-based index.
  LWidget:getItemCount( )  -> ItemCount  -- Returns the number of items in a list widget.
  LWidget:getMaxLength( )  -> MaxLength  -- Returns the maximum character length of a text box widget.
  LWidget:getPosition( )  -> Position  -- Returns the widget position as 1-based coordinates.
  LWidget:getSelected( )  -> Selected  -- Returns the selected item index (1-based) in a list widget, or nil.
  LWidget:getSize( )  -> Size  -- Returns the widget size in cells.
  LWidget:getStyle( )  -> Style  -- Returns the border style name of a border widget.
  LWidget:getTag( )  -> Tag  -- Returns the free-form identification tag.
  LWidget:getText( )  -> Text  -- Returns the text content of a label, button, or text box widget.
  LWidget:getTitle( )  -> Title  -- Returns the title of a border widget.
  LWidget:isEnabled( )  -- Returns whether the widget accepts input.
  LWidget:isVisible( )  -- Returns whether the widget is visible.
  LWidget:removeChild( child_ud )  -- Removes a child widget from a panel widget.
  LWidget:removeItem( index )  -- Removes an item from a list widget by 1-based index.
  LWidget:setColor( r, g, b, [a] )  -- Sets the colour of a label or border widget.
  LWidget:setEnabled( enabled )  -- Sets whether the widget accepts input.
  LWidget:setMaxLength( max_length )  -- Sets the maximum character length of a text box widget.
  LWidget:setOnChange( [callback] )  -- Registers a text change callback for a text box widget.
  LWidget:setOnClick( [callback] )  -- Registers a click callback for a button widget.
  LWidget:setOnSelect( [callback] )  -- Registers a selection change callback for a list widget.
  LWidget:setPosition( col, row )  -- Sets the widget position from 1-based coordinates.
  LWidget:setSelected( [index] )  -- Sets the selected item in a list widget by 1-based index.
  LWidget:setSize( width, height )  -- Sets the widget size in cells.
  LWidget:setStyle( style_name )  -- Sets the border style of a border widget.
  LWidget:setTag( tag )  -- Sets the free-form identification tag.
  LWidget:setText( text )  -- Sets the text content of a label, button, or text box widget.
  LWidget:setTitle( title )  -- Sets the title of a border widget.
  LWidget:setVisible( visible )  -- Sets the widget visibility.
  LWidget:type( )  -- Returns the type name of this object.
  LWidget:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.tween {#tween}

> `lurek.tween` â€” thin Lua registration wrapper for the property tween system.

```lua
  lurek.tween.cancelAll( )  -- Cancels all active tweens, sequences, parallels, and springs immediately.
  lurek.tween.delay( seconds, [cb] )  -- Creates a no-op tween that waits `seconds`, then optionally calls `callback`.
  lurek.tween.getActiveCount( )  -> ActiveCount  -- Returns the number of currently active tween objects (tweens + seqs + pars).
  lurek.tween.getEasingNames( )  -> EasingNames  -- Returns a list of all available easing names (built-in + custom).
  lurek.tween.newState( duration, [easing] )  -> State  -- Creates a standalone tween timing state without registering it with the engine.
  lurek.tween.parallel( )  -- Creates an empty TweenParallel. Add entries with :tween() or :add(tween),
  lurek.tween.registerEasing( name, f )  -- Registers a custom easing function under `name`. `fn(t)` receives 0..1, returns 0..1.
  lurek.tween.sequence( )  -- Creates an empty TweenSequence. Add steps with :tween(), :delay(), :callback(),
  lurek.tween.spring( target_tbl, fields_tbl, [opts] )  -- Creates a physics-based spring animation that drives named fields on `target_table`
  lurek.tween.to( )  -- Sugar for `tween()` with `target` first â€” natural read order.
  lurek.tween.tween( )  -- Creates a new property tween and registers it for automatic updating.
  lurek.tween.update( dt )  -- Advances all active tweens, sequences, and parallels by `dt` seconds.
```

**`LSpring`** methods:

> Lua-side spring handle: wraps [`SpringSystem`] and a registry reference to the target table.

```lua
  LSpring:cancel( )  -- Stops the spring. The engine will drop it on the next `update(dt)` call.
  LSpring:getPosition( field )  -> Position  -- Returns the current interpolated position for the named field, or `nil`.
  LSpring:isActive( )  -- Returns `true` if the spring has not been cancelled or settled.
  LSpring:isSettled( )  -- Returns `true` when all spring axes have converged within `precision`.
  LSpring:setDamping( value )  -- Updates the damping coefficient on all axes.
  LSpring:setStiffness( value )  -- Updates the stiffness constant on all axes.
  LSpring:setTarget( fields_tbl )  -- Updates target values for all fields present in `fields_table`.
  LSpring:type( )  -- Returns the type name of this object.
  LSpring:typeOf( name )  -- Returns true if this object is of the given type.
  LSpring:update( dt )  -- Advances the spring by `dt` seconds and writes positions to the target table.
```

**`LTween`** methods:

> A managed interpolation from start to end values over time.

```lua
  LTween:cancel( ud )  -- Cancels this tween immediately; fires the `onCancel` callback if set.
  LTween:getProgress( )  -> Progress  -- Returns raw 0..1 playback progress (not eased, not accounting for yoyo).
  LTween:isActive( )  -- Returns true if the tween is still running (not completed or cancelled).
  LTween:onCancel( ud, f )  -- Sets a callback called when the tween is cancelled. Returns self.
  LTween:onComplete( ud, f )  -- Sets a callback to fire when the tween finishes all cycles. Returns self for chaining.
  LTween:onUpdate( ud, f )  -- Sets a callback called every tick with the current eased `t` (0..=1). Returns self.
  LTween:pause( )  -- Pauses this tween; time stops advancing but the tween is not cancelled.
  LTween:resume( )  -- Resumes a paused tween, continuing from the position where it was paused.
  LTween:setRepeat( n )  -- Sets the number of extra play cycles after the first (0 = play once, -1 = infinite).
  LTween:setYoyo( enabled )  -- Enables or disables yoyo (ping-pong) on each repeat cycle.
  LTween:type( )  -- Returns the type name of this object.
  LTween:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LTweenParallel`** methods:

> A group of animations that run simultaneously over the same duration.

```lua
  LTweenParallel:add( par_ud, tw_ud )  -- Adds an existing LuaTween to the parallel group; marks the tween as owned.
  LTweenParallel:cancel( )  -- Cancels the parallel group immediately.
  LTweenParallel:isActive( )  -- Returns true if the parallel is running and not yet complete.
  LTweenParallel:onComplete( ud, f )  -- Sets a callback fired when all child tweens finish. Returns self.
  LTweenParallel:start( ud )  -- Marks the parallel as active. Returns self.
  LTweenParallel:tween( )  -- Creates and adds an inline tween entry to the parallel group. Returns self.
  LTweenParallel:type( )  -- Returns the type name of this object.
  LTweenParallel:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LTweenSequence`** methods:

> A chained sequence of animations that run one after another.

```lua
  LTweenSequence:callback( ud, f )  -- Appends an immediate callback step. Returns self.
  LTweenSequence:cancel( )  -- Cancels the sequence and stops all pending steps.
  LTweenSequence:delay( ud, seconds, [cb] )  -- Appends a delay step that waits `seconds` before proceeding. Returns self.
  LTweenSequence:isActive( )  -- Returns true if the sequence has been started and has not yet completed.
  LTweenSequence:onComplete( ud, f )  -- Sets a callback fired when all steps complete. Returns self.
  LTweenSequence:start( ud )  -- Marks the sequence as active so `lurek.tween.update(dt)` begins ticking it. Returns self.
  LTweenSequence:tween( )  -- Appends a tween step: animates `fields` on `target` over `duration`. Returns self.
  LTweenSequence:type( )  -- Returns the type name of this object.
  LTweenSequence:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LTweenState`** methods:

> Lua-side wrapper around the pure-Rust [`TweenState`] timing core.

```lua
  LTweenState:isComplete( )  -- Returns whether the tween state has completed.
  LTweenState:lerp( start, finish )  -- Interpolates from `start` to `finish` using the eased tween progress.
  LTweenState:reset( )  -- Resets the tween state to elapsed time zero.
  LTweenState:t( )  -- Returns the raw 0..1 playback progress.
  LTweenState:tick( dt )  -- Advances the tween state by `dt` seconds.
  LTweenState:type( )  -- Returns the type name of this object.
  LTweenState:typeOf( name )  -- Returns true if this object is of the given type.
```

## lurek.ui {#ui}

```lua
  lurek.ui.addChild( child )  -- Adds a child widget to this container.
  lurek.ui.addToast( toast_table )  -- Queues a toast notification from a table.
  lurek.ui.attachToEntity( entity_id )  -- Anchors this widget to a world-space entity by its numeric ID.
  lurek.ui.bind( key )  -- Registers a data-binding key on this widget.
  lurek.ui.clearAnchor( )  -- Removes all anchor constraints.
  lurek.ui.clearFocus( )  -- Removes keyboard focus from this widget so key events go to the next focusable.
  lurek.ui.containsPoint( x, y )  -- Returns whether (x, y) is inside this widget.
  lurek.ui.detachFromEntity( )  -- Removes the entity anchor from this widget, restoring normal layout positioning.
  lurek.ui.draw( )  -- Invokes all registered on_draw callbacks, each receiving the widget's
  lurek.ui.drawToImage( w, h )  -- Renders the UI widget tree to a CPU ImageData at the given resolution.
  lurek.ui.fadeIn( )  -- Instantly fades the widget in (sets alpha to `1.0`).
  lurek.ui.fadeOut( )  -- Instantly fades the widget out (sets alpha to `0.0` and hides it).
  lurek.ui.findById( id )  -- Recursively searches for a widget by id starting from this widget.
  lurek.ui.flushCache( )  -- Returns true if the widget tree changed since the last call, then resets the flag.
  lurek.ui.focusNext( )  -- Moves focus to the next focusable widget.
  lurek.ui.focusPrev( )  -- Moves focus to the previous focusable widget.
  lurek.ui.getAlpha( )  -> Alpha  -- Returns the widget's current alpha transparency.
  lurek.ui.getChildCount( )  -> ChildCount  -- Returns the number of children in this container.
  lurek.ui.getChildren( )  -> Children  -- Returns this container's children as widget-handle tables.
  lurek.ui.getFlexGrow( )  -> FlexGrow  -- Returns the flex-grow factor.
  lurek.ui.getFlexShrink( )  -> FlexShrink  -- Returns the flex-shrink factor.
  lurek.ui.getFocus( )  -> Focus  -- Returns the focused widget index or nil.
  lurek.ui.getId( )  -> Id  -- Returns the widget string identifier.
  lurek.ui.getMargin( )  -> Margin  -- Returns the widget margin (top, right, bottom, left).
  lurek.ui.getMaxSize( )  -> MaxSize  -- Returns the maximum widget size.
  lurek.ui.getMinSize( )  -> MinSize  -- Returns the minimum widget size.
  lurek.ui.getPadding( )  -> Padding  -- Returns the widget padding (top, right, bottom, left).
  lurek.ui.getPosition( )  -> Position  -- Returns the widget position.
  lurek.ui.getRect( )  -> Rect  -- Returns the computed screen-space rectangle after layout.
  lurek.ui.getRoot( )  -> Root  -- Returns the root panel widget table.
  lurek.ui.getSize( )  -> Size  -- Returns the current width and height of the widget in UI pixels.
  lurek.ui.getState( )  -> State  -- Returns the widget interaction state name.
  lurek.ui.getTheme( )  -> Theme  -- Returns whether a theme is set.
  lurek.ui.getToastCount( )  -> ToastCount  -- Returns the number of active toasts.
  lurek.ui.getTooltip( )  -> Tooltip  -- Returns the widget tooltip text.
  lurek.ui.getWidgetCount( )  -> WidgetCount  -- Returns the total widget count in the context.
  lurek.ui.getZOrder( )  -> ZOrder  -- Returns the widget z-order.
  lurek.ui.isEnabled( )  -- Returns whether the widget is enabled.
  lurek.ui.isVisible( )  -- Returns whether the widget is visible.
  lurek.ui.keypressed( key )  -- Forwards a key press event to the GUI.
  lurek.ui.loadLayout( )  -- Load a widget tree from a Lua table definition and attach it to the UI
  lurek.ui.loadLayoutFile( path )  -- Load a widget tree from a TOML layout file and attach it to the UI root.
  lurek.ui.mousemoved( x, y )  -- Forwards a mouse move event to the GUI.
  lurek.ui.mousepressed( x, y, [btn] )  -- Forwards a mouse press event to the GUI.
  lurek.ui.mousereleased( x, y, [btn] )  -- Forwards a mouse release event to the GUI.
  lurek.ui.newAccordion( )  -> Accordion  -- Creates a collapsible accordion widget.
  lurek.ui.newAreaChart( opts )  -> AreaChart  -- Creates a new stacked-area chart.
  lurek.ui.newBadge( [count] )  -> Badge  -- Creates a badge widget displaying a numeric count.
  lurek.ui.newBarChart( opts )  -> BarChart  -- Creates and returns a new bar chart widget attached to this image widget.
  lurek.ui.newButton( [text] )  -> Button  -- Creates and returns a new interactive button widget as a child of this widget.
  lurek.ui.newCheckbox( [text] )  -> Checkbox  -- Creates a checkbox widget.
  lurek.ui.newColorPicker( )  -> ColorPicker  -- Creates a color picker widget.
  lurek.ui.newComboBox( )  -> ComboBox  -- Creates a dropdown combo box widget.
  lurek.ui.newCustomWidget( [config] )  -> CustomWidget  -- Creates a new widget with custom Lua-driven rendering.
  lurek.ui.newDialog( [title] )  -> Dialog  -- Creates a modal dialog widget.
  lurek.ui.newDockPanel( )  -> DockPanel  -- Creates and returns a new docking panel that arranges children along its edges.
  lurek.ui.newImageWidget( )  -> ImageWidget  -- Creates an image display widget.
  lurek.ui.newLabel( [text] )  -> Label  -- Creates a text label widget.
  lurek.ui.newLayout( [direction] )  -> Layout  -- Creates a flexbox layout container.
  lurek.ui.newLineChart( opts )  -> LineChart  -- Creates a new line chart.
  lurek.ui.newList( )  -> List  -- Creates a selectable list widget.
  lurek.ui.newMenuBar( )  -> MenuBar  -- Creates a menu bar widget.
  lurek.ui.newMenuItem( [text] )  -> MenuItem  -- Creates a menu item widget.
  lurek.ui.newNinePatch( )  -> NinePatch  -- Creates a 9-patch slicer widget.
  lurek.ui.newPanel( )  -> Panel  -- Creates a container panel widget.
  lurek.ui.newPieChart( opts )  -> PieChart  -- Creates and returns a new pie chart widget attached to this image widget.
  lurek.ui.newProgressBar( [min], [max] )  -> ProgressBar  -- Creates a progress bar widget.
  lurek.ui.newRadioButton( [text], [group] )  -> RadioButton  -- Creates a grouped radio button widget.
  lurek.ui.newScatterPlot( opts )  -> ScatterPlot  -- Creates a new scatter plot.
  lurek.ui.newScrollBar( [vertical] )  -> ScrollBar  -- Creates a scroll bar widget.
  lurek.ui.newScrollPanel( )  -> ScrollPanel  -- Creates a scrollable panel widget.
  lurek.ui.newSeparator( [vertical] )  -> Separator  -- Creates a separator line.
  lurek.ui.newSlider( [min], [max] )  -> Slider  -- Creates a value slider widget.
  lurek.ui.newSpacer( [w], [h] )  -> Spacer  -- Creates a spacing filler widget.
  lurek.ui.newSpinBox( [min], [max] )  -> SpinBox  -- Creates a numeric spin box widget with increment and decrement buttons.
  lurek.ui.newSplitPanel( [orientation] )  -> SplitPanel  -- Creates a resizable split panel.
  lurek.ui.newStatusBar( )  -> StatusBar  -- Creates a status bar widget.
  lurek.ui.newSwitch( [on] )  -> Switch  -- Creates a toggle switch widget.
  lurek.ui.newTabBar( )  -> TabBar  -- Creates a tab bar widget.
  lurek.ui.newTable( )  -> Table  -- Creates a data table widget.
  lurek.ui.newTextInput( )  -> TextInput  -- Creates a text input widget.
  lurek.ui.newTheme( )  -> Theme  -- Creates a new theme instance.
  lurek.ui.newToast( [message], [duration] )  -> Toast  -- Creates a toast notification widget.
  lurek.ui.newToolbar( [orientation] )  -> Toolbar  -- Creates a toolbar widget.
  lurek.ui.newTooltipPanel( [text] )  -> TooltipPanel  -- Creates a tooltip panel widget.
  lurek.ui.newTreeView( )  -> TreeView  -- Creates a collapsible tree view widget.
  lurek.ui.newWindow( [title] )  -> Window  -- Creates a draggable window widget.
  lurek.ui.parseWidgetState( state )  -- Parses a widget state string, returning the canonical form or nil if invalid.
  lurek.ui.removeChild( child )  -- Removes a child widget from this container.
  lurek.ui.renderToImage( width, height, path )  -- Render the current UI widget tree to a PNG file for testing purposes.
  lurek.ui.setAlpha( alpha )  -- Sets the widget's alpha transparency (`0.0` fully transparent, `1.0` opaque).
  lurek.ui.setAnchor( )  -- Sets anchor edges (left, top, right, bottom).
  lurek.ui.setAnchorCenter( [cx], [cy] )  -- Sets center anchor offsets.
  lurek.ui.setDefaultTheme( )  -- Installs the built-in dark theme as the active GUI theme.
  lurek.ui.setEnabled( v )  -- Sets whether the widget is enabled.
  lurek.ui.setFlexGrow( grow )  -- Sets the flex-grow factor.
  lurek.ui.setFlexShrink( shrink )  -- Sets the flex-shrink factor.
  lurek.ui.setFocus( [widget] )  -- Sets keyboard focus to a widget or clears it.
  lurek.ui.setId( id )  -- Sets the widget string identifier.
  lurek.ui.setMargin( top, [right], [bottom], [left] )  -- Sets widget margin (CSS-like: top, right?, bottom?, left?).
  lurek.ui.setMaxSize( w, h )  -- Sets the maximum widget size.
  lurek.ui.setMinSize( w, h )  -- Sets the minimum widget size.
  lurek.ui.setOnChange( f )  -- Registers a callback invoked when this widget's value changes.
  lurek.ui.setOnClick( f )  -- Registers a callback invoked when this widget is clicked.
  lurek.ui.setOnDraw( self, f )  -- Stores a custom draw callback for later invocation.
  lurek.ui.setPadding( top, [right], [bottom], [left] )  -- Sets widget padding (CSS-like: top, right?, bottom?, left?).
  lurek.ui.setPosition( x, y )  -- Sets the widget position.
  lurek.ui.setSize( w, h )  -- Sets the width and height of the widget in UI pixels.
  lurek.ui.setTheme( theme_ud )  -- Sets the active GUI theme.
  lurek.ui.setTooltip( text )  -- Sets the widget tooltip text.
  lurek.ui.setViewport( w, h )  -- Sets the viewport dimensions used for anchor constraints and layout.
  lurek.ui.setVisible( v )  -- Shows or hides the widget; hidden widgets are not rendered or interactive.
  lurek.ui.setZOrder( z )  -- Sets the widget z-order for draw sorting.
  lurek.ui.slideIn( x, y )  -- Instantly moves the widget to `(x, y)` and makes it visible.
  lurek.ui.slideOut( x, y )  -- Instantly moves the widget to the off-screen position `(x, y)` and hides it.
  lurek.ui.textinput( text )  -- Forwards text input to the focused text input widget.
  lurek.ui.type( )  -> Lua  -- Returns the Lua type name of this widget (e.g. "LButton").
  lurek.ui.typeOf( name )  -- Returns true if this widget is of the given type, "LWidget", or "Object".
  lurek.ui.unbind( )  -- Removes the data-binding key from this widget.
  lurek.ui.update( dt )  -- Advances toast timers, removes expired toasts, and dispatches pending GUI events.
  lurek.ui.update_bindings( )  -- Updates all widgets that have a data-binding key registered via `:bind(key)`.
  lurek.ui.wheelmoved( x, y )  -- Forwards a mouse wheel event to the GUI.
```

**`LAccordion`** methods:

> Adds Accordion-specific methods (1-based sections in Lua).

```lua
  LAccordion:addSection( title, [content_idx] )  -- Adds a section entry to this Accordion widget.
  LAccordion:getSectionCount( )  -> SectionCount  -- Returns the section count of this Accordion widget.
  LAccordion:getSectionTitle( section_idx )  -> SectionTitle  -- Returns the section title of this Accordion widget.
  LAccordion:isExclusive( )  -- Returns true if exclusive is enabled for this Accordion widget.
  LAccordion:isSectionExpanded( section_idx )  -- Returns true if section expanded is enabled for this Accordion widget.
  LAccordion:setExclusive( v )  -- Sets the exclusive for this Accordion widget.
  LAccordion:toggleSection( section_idx )  -- Toggles the expanded/collapsed status of an Accordion section.
```

**`LAreaChart`** methods:

> Lua wrapper for a stacked area chart renderer.

```lua
  LAreaChart:addLayer( name, vals_tbl, r, g, b )  -- Adds a stacked layer with values and colour.
  LAreaChart:drawToImage( )  -- Renders the area chart into an existing ImageData.
  LAreaChart:setYMax( v )  -- Sets the maximum Y value for axis scaling.
  LAreaChart:type( )  -- Returns the type name of this object.
  LAreaChart:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LBadge`** methods:

> Adds Badge-specific methods to a widget table.

```lua
  LBadge:getCount( )  -> Count  -- Returns the raw count of this Badge widget.
  LBadge:getDisplayText( )  -> DisplayText  -- Returns the display text of this Badge widget, e.g. "99+" when over the max.
  LBadge:setCount( count )  -- Sets the count displayed on this Badge widget.
```

**`LBarChart`** methods:

> Lua wrapper for a grouped bar chart renderer.

```lua
  LBarChart:addCategory( label, vals_tbl )  -- Adds a category group with per-series values.
  LBarChart:addSeries( name, r, g, b )  -- Adds a bar series with a name and colour.
  LBarChart:drawToImage( )  -- Renders the bar chart into an existing ImageData.
  LBarChart:type( )  -- Returns the type name of this object.
  LBarChart:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LButton`** methods:

> Adds Button-specific methods to a widget table.

```lua
  LButton:getText( )  -> Text  -- Returns the text of this Button widget.
  LButton:setText( text )  -- Sets the text for this Button widget.
```

**`LCheckbox`** methods:

> Adds CheckBox-specific methods to a widget table.

```lua
  LCheckbox:getText( )  -> Text  -- Returns the text of this Checkbox widget.
  LCheckbox:isChecked( )  -- Returns true if checked is enabled for this Checkbox widget.
  LCheckbox:setChecked( checked )  -- Sets the checked for this Checkbox widget.
  LCheckbox:setText( text )  -- Sets the text for this Checkbox widget.
```

**`LColorPicker`** methods:

> Adds ColorPicker-specific methods.

```lua
  LColorPicker:getColor( )  -> Color  -- Returns the color of this Color_Picker widget.
  LColorPicker:getColorMode( )  -> ColorMode  -- Returns the color mode of this Color_Picker widget.
  LColorPicker:getShowAlpha( )  -> ShowAlpha  -- Returns the show alpha of this Color_Picker widget.
  LColorPicker:setColor( r, green, b, [a] )  -- Sets the color for this Color_Picker widget.
  LColorPicker:setColorMode( mode )  -- Sets the color mode for this Color_Picker widget.
  LColorPicker:setOnChange( f )  -- Registers a callback invoked when this widget's value changes.
  LColorPicker:setShowAlpha( v )  -- Sets the show alpha for this Color_Picker widget.
```

**`LComboBox`** methods:

> Adds ComboBox-specific methods (1-based indices in Lua).

```lua
  LComboBox:addItem( text )  -- Adds a item entry to this Combo_Box widget.
  LComboBox:clearItems( )  -- Clears all items entries from this Combo_Box widget.
  LComboBox:getItem( index )  -> Item  -- Returns the item of this Combo_Box widget.
  LComboBox:getItemCount( )  -> ItemCount  -- Returns the item count of this Combo_Box widget.
  LComboBox:getSelectedIndex( )  -> SelectedIndex  -- Returns the selected index of this Combo_Box widget.
  LComboBox:getSelectedItem( )  -> SelectedItem  -- Returns the selected item of this Combo_Box widget.
  LComboBox:removeItem( index )  -- Removes the item from this Combo_Box widget.
  LComboBox:setSelectedIndex( index )  -- Sets the selected index for this Combo_Box widget.
```

**`LDialog`** methods:

> Adds Dialog-specific methods.

```lua
  LDialog:addButton( text, [cb] )  -- Adds a button entry to this Dialog widget.
  LDialog:close( )  -- Closes and removes this dialog from the screen.
  LDialog:getContent( )  -> Content  -- Returns the content of this Dialog widget.
  LDialog:getTitle( )  -> Title  -- Returns the title of this Dialog widget.
  LDialog:isModal( )  -- Returns true if modal is enabled for this Dialog widget.
  LDialog:isOpen( )  -- Returns true if open is enabled for this Dialog widget.
  LDialog:open( )  -- Performs the open operation on this Dialog widget.
  LDialog:setContent( [content_idx] )  -- Sets the content for this Dialog widget.
  LDialog:setModal( v )  -- Sets the modal for this Dialog widget.
  LDialog:setOnClose( f )  -- Registers a callback invoked when this dialog is closed.
  LDialog:setTitle( title )  -- Sets the title for this Dialog widget.
```

**`LDockPanel`** methods:

> Adds DockPanel-specific methods.

```lua
  LDockPanel:dock( child_idx, side )  -- Performs the dock operation on this Dock_Panel widget.
  LDockPanel:getDockedCount( )  -> DockedCount  -- Returns the docked count of this Dock_Panel widget.
  LDockPanel:getSplitSize( side )  -> SplitSize  -- Returns the split size of this Dock_Panel widget.
  LDockPanel:setSplitSize( side, size )  -- Sets the split size for this Dock_Panel widget.
  LDockPanel:undock( child_idx )  -- Performs the undock operation on this Dock_Panel widget.
```

**`LGuiTable`** methods:

> Adds GUITable-specific methods (1-based rows/cols in Lua).

```lua
  LGuiTable:addColumn( header, [width] )  -- Adds a column entry to this Gui_Table widget.
  LGuiTable:addRow( cells )  -- Adds a row entry to this Gui_Table widget.
  LGuiTable:getCell( row, col )  -> Cell  -- Returns the cell of this Gui_Table widget.
  LGuiTable:getColumnCount( )  -> ColumnCount  -- Returns the column count of this Gui_Table widget.
  LGuiTable:getRowCount( )  -> RowCount  -- Returns the row count of this Gui_Table widget.
  LGuiTable:getSelectedRow( )  -> SelectedRow  -- Returns the selected row of this Gui_Table widget.
  LGuiTable:isSortable( )  -- Returns true if sortable is enabled for this Gui_Table widget.
  LGuiTable:setCell( row, col, text )  -- Sets the cell for this Gui_Table widget.
  LGuiTable:setOnSelect( f )  -- Registers a callback invoked when a table row is selected.
  LGuiTable:setSelectedRow( [row] )  -- Sets the selected row for this Gui_Table widget.
  LGuiTable:setSortable( v )  -- Sets the sortable for this Gui_Table widget.
```

**`LGuiWindow`** methods:

> Adds GUIWindow-specific methods.

```lua
  LGuiWindow:getTitle( )  -> Title  -- Returns the title of this Gui_Window widget.
  LGuiWindow:isCloseable( )  -- Returns true if closeable is enabled for this Gui_Window widget.
  LGuiWindow:isDraggable( )  -- Returns true if draggable is enabled for this Gui_Window widget.
  LGuiWindow:isResizable( )  -- Returns true if resizable is enabled for this Gui_Window widget.
  LGuiWindow:setCloseable( v )  -- Sets the closeable for this Gui_Window widget.
  LGuiWindow:setDraggable( v )  -- Sets the draggable for this Gui_Window widget.
  LGuiWindow:setOnClose( f )  -- Registers a callback invoked when this window is closed.
  LGuiWindow:setResizable( v )  -- Sets the resizable for this Gui_Window widget.
  LGuiWindow:setTitle( title )  -- Sets the title for this Gui_Window widget.
```

**`LImageWidget`** methods:

> Adds ImageWidget-specific methods.

```lua
  LImageWidget:getScaleMode( )  -> ScaleMode  -- Returns the scale mode of this Image_Widget widget.
  LImageWidget:getTint( )  -> Tint  -- Returns the tint of this Image_Widget widget.
  LImageWidget:setScaleMode( mode )  -- Sets the scale mode for this Image_Widget widget.
  LImageWidget:setTint( r, green, b, [a] )  -- Sets the tint for this Image_Widget widget.
```

**`LLabel`** methods:

> Adds Label-specific methods to a widget table.

```lua
  LLabel:getText( )  -> Text  -- Returns the text of this Label widget.
  LLabel:setText( text )  -- Sets the text for this Label widget.
```

**`LLayout`** methods:

> Adds Layout-specific methods.

```lua
  LLayout:getAlign( )  -> Align  -- Returns the align of this Layout widget.
  LLayout:getDirection( )  -> Direction  -- Returns the direction of this Layout widget.
  LLayout:getJustify( )  -> Justify  -- Returns the justify of this Layout widget.
  LLayout:getSpacing( )  -> Spacing  -- Returns the spacing of this Layout widget.
  LLayout:getWrap( )  -> Wrap  -- Returns the wrap of this Layout widget.
  LLayout:setAlign( align )  -- Sets the align for this Layout widget.
  LLayout:setColumns( n )  -- Sets the columns for this Layout widget.
  LLayout:setDirection( dir )  -- Sets the direction for this Layout widget.
  LLayout:setJustify( justify )  -- Sets the justify for this Layout widget.
  LLayout:setSpacing( spacing )  -- Sets the spacing for this Layout widget.
  LLayout:setWrap( wrap )  -- Sets the wrap for this Layout widget.
```

**`LLineChart`** methods:

> Lua wrapper for a line chart renderer.

```lua
  LLineChart:addSeries( name, pts_tbl, r, g, b )  -- Adds a named data series to the chart.
  LLineChart:drawToImage( )  -- Renders the line chart into an existing ImageData.
  LLineChart:setXMax( v )  -- Sets the maximum X value for axis scaling.
  LLineChart:setYMax( v )  -- Sets the maximum Y value for axis scaling.
  LLineChart:type( )  -- Returns the type name of this object.
  LLineChart:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LListBox`** methods:

> Adds ListBox-specific methods (1-based indices in Lua).

```lua
  LListBox:addItem( text )  -- Adds a item entry to this List_Box widget.
  LListBox:clearItems( )  -- Clears all items entries from this List_Box widget.
  LListBox:getItem( index )  -> Item  -- Returns the item of this List_Box widget.
  LListBox:getItemCount( )  -> ItemCount  -- Returns the item count of this List_Box widget.
  LListBox:getSelectedIndex( )  -> SelectedIndex  -- Returns the selected index of this List_Box widget.
  LListBox:removeItem( index )  -- Removes the item from this List_Box widget.
  LListBox:setItemHeight( h )  -- Sets the item height for this List_Box widget.
  LListBox:setSelectedIndex( index )  -- Sets the selected index for this List_Box widget.
```

**`LMenuBar`** methods:

> Adds MenuBar-specific methods.

```lua
  LMenuBar:addMenu( menu_idx )  -- Adds a menu entry to this Menu_Bar widget.
  LMenuBar:getMenuCount( )  -> MenuCount  -- Returns the menu count of this Menu_Bar widget.
  LMenuBar:getMenus( )  -> Menus  -- Returns the menus of this Menu_Bar widget.
  LMenuBar:removeMenu( menu_idx )  -- Removes the menu from this Menu_Bar widget.
```

**`LMenuItem`** methods:

> Adds MenuItem-specific methods.

```lua
  LMenuItem:addSubItem( child_idx )  -- Adds a sub item entry to this Menu_Item widget.
  LMenuItem:getShortcut( )  -> Shortcut  -- Returns the shortcut of this Menu_Item widget.
  LMenuItem:getSubItems( )  -> SubItems  -- Returns the sub items of this Menu_Item widget.
  LMenuItem:getText( )  -> Text  -- Returns the text of this Menu_Item widget.
  LMenuItem:isChecked( )  -- Returns true if checked is enabled for this Menu_Item widget.
  LMenuItem:setChecked( v )  -- Sets the checked for this Menu_Item widget.
  LMenuItem:setOnClick( f )  -- Registers a callback invoked when this menu item is clicked.
  LMenuItem:setShortcut( shortcut )  -- Sets the shortcut for this Menu_Item widget.
  LMenuItem:setText( text )  -- Sets the text for this Menu_Item widget.
```

**`LNinePatch`** methods:

> Adds NinePatch-specific methods.

```lua
  LNinePatch:getImageDimensions( )  -> ImageDimensions  -- Returns the image dimensions of this Nine_Patch widget.
  LNinePatch:getInsets( )  -> Insets  -- Returns the insets of this Nine_Patch widget.
  LNinePatch:getSlices( )  -> Slices  -- Returns the slices of this Nine_Patch widget.
  LNinePatch:setImageDimensions( w, h )  -- Sets the image dimensions for this Nine_Patch widget.
  LNinePatch:setInsets( left, top, right, bottom )  -- Sets the insets for this Nine_Patch widget.
```

**`LPanel`** methods:

> Adds Panel-specific methods.

```lua
  LPanel:getTitle( )  -> Title  -- Returns the title of this Panel widget.
  LPanel:setScrollable( scrollable )  -- Sets the scrollable for this Panel widget.
  LPanel:setTitle( title )  -- Sets the title for this Panel widget.
```

**`LPieChart`** methods:

> Lua wrapper for a pie chart renderer.

```lua
  LPieChart:addSegment( label, value, r, g, b )  -- Adds a labelled pie segment.
  LPieChart:drawToImage( )  -- Renders the pie chart into an existing ImageData.
  LPieChart:type( )  -- Returns the type name of this object.
  LPieChart:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LProgressBar`** methods:

> Adds ProgressBar-specific methods to a widget table.

```lua
  LProgressBar:getMax( )  -> Max  -- Returns the max of this Progress_Bar widget.
  LProgressBar:getMin( )  -> Min  -- Returns the min of this Progress_Bar widget.
  LProgressBar:getProgress( )  -> Progress  -- Returns the progress of this Progress_Bar widget.
  LProgressBar:getValue( )  -> Value  -- Returns the value of this Progress_Bar widget.
  LProgressBar:setRange( min, max )  -- Sets the range for this Progress_Bar widget.
  LProgressBar:setValue( v )  -- Sets the value for this Progress_Bar widget.
```

**`LRadioButton`** methods:

> Adds RadioButton-specific methods.

```lua
  LRadioButton:getGroup( )  -> Group  -- Returns the group of this Radio_Button widget.
  LRadioButton:getText( )  -> Text  -- Returns the text of this Radio_Button widget.
  LRadioButton:isSelected( )  -- Returns true if selected is enabled for this Radio_Button widget.
  LRadioButton:setGroup( group )  -- Sets the group for this Radio_Button widget.
  LRadioButton:setOnChange( f )  -- Registers a callback invoked when this widget's value changes.
  LRadioButton:setSelected( v )  -- Sets the selected for this Radio_Button widget.
  LRadioButton:setText( text )  -- Sets the text for this Radio_Button widget.
```

**`LScatterPlot`** methods:

> Lua wrapper for a scatter plot renderer.

```lua
  LScatterPlot:addSeries( name, pts_tbl, r, g, b )  -- Adds a named data series.
  LScatterPlot:drawToImage( )  -- Renders the scatter plot into an existing ImageData.
  LScatterPlot:setXRange( mn, mx )  -- Sets the X-axis data range.
  LScatterPlot:setYRange( mn, mx )  -- Sets the Y-axis data range.
  LScatterPlot:type( )  -- Returns the type name of this object.
  LScatterPlot:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LScrollBar`** methods:

> Adds ScrollBar-specific methods.

```lua
  LScrollBar:getContentSize( )  -> ContentSize  -- Returns the content size of this Scroll_Bar widget.
  LScrollBar:getScrollPosition( )  -> ScrollPosition  -- Returns the scroll position of this Scroll_Bar widget.
  LScrollBar:getViewSize( )  -> ViewSize  -- Returns the view size of this Scroll_Bar widget.
  LScrollBar:isVertical( )  -- Returns true if vertical is enabled for this Scroll_Bar widget.
  LScrollBar:setContentSize( v )  -- Sets the content size for this Scroll_Bar widget.
  LScrollBar:setOnChange( f )  -- Registers a callback invoked when this widget's value changes.
  LScrollBar:setScrollPosition( v )  -- Sets the scroll position for this Scroll_Bar widget.
  LScrollBar:setViewSize( v )  -- Sets the view size for this Scroll_Bar widget.
```

**`LScrollPanel`** methods:

> Adds ScrollPanel-specific methods.

```lua
  LScrollPanel:getContentSize( )  -> ContentSize  -- Returns the content size of this Scroll_Panel widget.
  LScrollPanel:getMaxScroll( )  -> MaxScroll  -- Returns the max scroll of this Scroll_Panel widget.
  LScrollPanel:getScrollPosition( )  -> ScrollPosition  -- Returns the scroll position of this Scroll_Panel widget.
  LScrollPanel:getScrollSpeed( )  -> ScrollSpeed  -- Returns the scroll speed of this Scroll_Panel widget.
  LScrollPanel:setContentSize( w, h )  -- Sets the content size for this Scroll_Panel widget.
  LScrollPanel:setScrollPosition( x, y )  -- Sets the scroll position for this Scroll_Panel widget.
  LScrollPanel:setScrollSpeed( speed )  -- Sets the scroll speed for this Scroll_Panel widget.
```

**`LSeparator`** methods:

> Adds Separator-specific methods.

```lua
  LSeparator:getThickness( )  -> Thickness  -- Returns the thickness of this Separator widget.
  LSeparator:isVertical( )  -- Returns true if vertical is enabled for this Separator widget.
  LSeparator:setThickness( thickness )  -- Sets the thickness for this Separator widget.
  LSeparator:setVertical( v )  -- Sets the vertical for this Separator widget.
```

**`LSlider`** methods:

> Adds Slider-specific methods to a widget table.

```lua
  LSlider:getMax( )  -> Max  -- Returns the max of this Slider widget.
  LSlider:getMin( )  -> Min  -- Returns the min of this Slider widget.
  LSlider:getValue( )  -> Value  -- Returns the value of this Slider widget.
  LSlider:setRange( min, max )  -- Sets the range for this Slider widget.
  LSlider:setStep( step )  -- Sets the step for this Slider widget.
  LSlider:setValue( v )  -- Sets the value for this Slider widget.
```

**`LSpinBox`** methods:

> Adds SpinBox-specific methods to a widget table.

```lua
  LSpinBox:decrement( )  -- Decrements the value by one step.
  LSpinBox:getValue( )  -> Value  -- Returns the current value of this SpinBox widget.
  LSpinBox:increment( )  -- Increments the value by one step.
  LSpinBox:setRange( min, max )  -- Sets the valid range for this SpinBox widget.
  LSpinBox:setStep( step )  -- Sets the increment step for this SpinBox widget.
  LSpinBox:setValue( v )  -- Sets the value for this SpinBox widget.
```

**`LSplitPanel`** methods:

> Adds SplitPanel-specific methods.

```lua
  LSplitPanel:getFirstChild( )  -> FirstChild  -- Returns the first child of this Split_Panel widget.
  LSplitPanel:getMinPanelSize( )  -> MinPanelSize  -- Returns the min panel size of this Split_Panel widget.
  LSplitPanel:getOrientation( )  -> Orientation  -- Returns the orientation of this Split_Panel widget.
  LSplitPanel:getSecondChild( )  -> SecondChild  -- Returns the second child of this Split_Panel widget.
  LSplitPanel:getSplitPosition( )  -> SplitPosition  -- Returns the split position of this Split_Panel widget.
  LSplitPanel:setFirstChild( child_idx )  -- Sets the first child for this Split_Panel widget.
  LSplitPanel:setMinPanelSize( v )  -- Sets the min panel size for this Split_Panel widget.
  LSplitPanel:setOrientation( v )  -- Sets the orientation for this Split_Panel widget.
  LSplitPanel:setSecondChild( child_idx )  -- Sets the second child for this Split_Panel widget.
  LSplitPanel:setSplitPosition( v )  -- Sets the split position for this Split_Panel widget.
```

**`LStatusBar`** methods:

> Adds StatusBar-specific methods.

```lua
  LStatusBar:addSection( text, [width] )  -- Adds a section entry to this Status_Bar widget.
  LStatusBar:getSectionCount( )  -> SectionCount  -- Returns the section count of this Status_Bar widget.
  LStatusBar:getSectionText( section_idx )  -> SectionText  -- Returns the section text of this Status_Bar widget.
  LStatusBar:setSectionCount( count )  -- Resizes the section list for this Status_Bar widget.
  LStatusBar:setSectionText( section_idx, text )  -- Sets the section text for this Status_Bar widget.
  LStatusBar:setSectionWidget( section_idx, widget )  -- Compatibility shim for assigning a widget to a section.
```

**`LSwitch`** methods:

> Adds Switch-specific methods to a widget table.

```lua
  LSwitch:isOn( )  -- Returns the on/off state of this Switch widget.
  LSwitch:setOn( on )  -- Sets the on/off state of this Switch widget.
  LSwitch:toggle( )  -- Toggles the on/off state of this Switch widget.
```

**`LTabBar`** methods:

> Adds TabBar-specific methods (1-based indices in Lua).

```lua
  LTabBar:addTab( label )  -- Adds a tab entry to this Tab_Bar widget.
  LTabBar:getActiveTab( )  -> ActiveTab  -- Returns the active tab of this Tab_Bar widget.
  LTabBar:getTab( index )  -> Tab  -- Returns the tab of this Tab_Bar widget.
  LTabBar:getTabCount( )  -> TabCount  -- Returns the tab count of this Tab_Bar widget.
  LTabBar:removeTab( index )  -- Removes the tab from this Tab_Bar widget.
  LTabBar:setActiveTab( index )  -- Sets the active tab for this Tab_Bar widget.
```

**`LTextInput`** methods:

> Adds TextInput-specific methods to a widget table.

```lua
  LTextInput:getCursorPosition( )  -> CursorPosition  -- Returns the cursor position of this Text_Input widget.
  LTextInput:getPlaceholder( )  -> Placeholder  -- Returns the placeholder of this Text_Input widget.
  LTextInput:getText( )  -> Text  -- Returns the text of this Text_Input widget.
  LTextInput:isFocused( )  -- Returns true if focused is enabled for this Text_Input widget.
  LTextInput:setMaxLength( n )  -- Sets the max length for this Text_Input widget.
  LTextInput:setPlaceholder( text )  -- Sets the placeholder for this Text_Input widget.
  LTextInput:setText( text )  -- Sets the text for this Text_Input widget.
```

**`LTheme`** methods:

> Lua-side wrapper around a GUI [`Theme`].

```lua
  LTheme:setStyle( widget_type, state, style_table )  -- Sets a style for a (widget_type, state) pair.
  LTheme:type( )  -- Returns the type name of this object.
  LTheme:typeOf( name )  -- Returns true if this object is of the given type.
```

**`LToast`** methods:

> Adds Toast-specific methods.

```lua
  LToast:getDuration( )  -> Duration  -- Returns the duration of this Toast widget.
  LToast:getMessage( )  -> Message  -- Returns the message of this Toast widget.
  LToast:getProgress( )  -> Progress  -- Returns the progress of this Toast widget.
  LToast:isExpired( )  -- Returns true if expired is enabled for this Toast widget.
  LToast:setDuration( d )  -- Sets the duration for this Toast widget.
  LToast:setMessage( msg )  -- Sets the message for this Toast widget.
```

**`LToolbar`** methods:

> Adds Toolbar-specific methods.

```lua
  LToolbar:addButton( id, [tooltip] )  -- Adds a button entry to this Toolbar widget.
  LToolbar:addSeparator( )  -- Adds a separator entry to this Toolbar widget.
  LToolbar:addSpacer( [size] )  -- Adds a spacer entry to this Toolbar widget.
  LToolbar:getButton( id )  -> Button  -- Returns the button of this Toolbar widget.
  LToolbar:getOrientation( )  -> Orientation  -- Returns the orientation of this Toolbar widget.
  LToolbar:isButtonToggled( id )  -- Returns true if button toggled is enabled for this Toolbar widget.
  LToolbar:setButtonEnabled( id, enabled )  -- Sets the button enabled for this Toolbar widget.
  LToolbar:setButtonToggled( id, toggled )  -- Sets the button toggled for this Toolbar widget.
  LToolbar:setOrientation( v )  -- Sets the orientation for this Toolbar widget.
```

**`LTooltipPanel`** methods:

> Adds TooltipPanel-specific methods.

```lua
  LTooltipPanel:getDelay( )  -> Delay  -- Returns the delay of this Tooltip_Panel widget.
  LTooltipPanel:getTarget( )  -> Target  -- Returns the target of this Tooltip_Panel widget.
  LTooltipPanel:getText( )  -> Text  -- Returns the text of this Tooltip_Panel widget.
  LTooltipPanel:setDelay( v )  -- Sets the delay for this Tooltip_Panel widget.
  LTooltipPanel:setTarget( [target] )  -- Sets the target for this Tooltip_Panel widget.
  LTooltipPanel:setText( text )  -- Sets the text for this Tooltip_Panel widget.
```

**`LTreeView`** methods:

> Adds TreeView-specific methods (1-based indices in Lua).

```lua
  LTreeView:addNode( text, [parent_index] )  -- Adds a node entry to this Tree_View widget.
  LTreeView:clearNodes( )  -- Clears all nodes entries from this Tree_View widget.
  LTreeView:collapseAll( )  -- Performs the collapse all operation on this Tree_View widget.
  LTreeView:collapseNode( index )  -- Performs the collapse node operation on this Tree_View widget.
  LTreeView:expandAll( )  -- Performs the expand all operation on this Tree_View widget.
  LTreeView:expandNode( index )  -- Performs the expand node operation on this Tree_View widget.
  LTreeView:getChildNodes( index )  -> ChildNodes  -- Returns the child nodes of this Tree_View widget.
  LTreeView:getNodeCount( )  -> NodeCount  -- Returns the node count of this Tree_View widget.
  LTreeView:getNodeDepth( index )  -> NodeDepth  -- Returns the node depth of this Tree_View widget.
  LTreeView:getNodeText( index )  -> NodeText  -- Returns the node text of this Tree_View widget.
  LTreeView:getParentNode( index )  -> ParentNode  -- Returns the parent node of this Tree_View widget.
  LTreeView:getSelectedNode( )  -> SelectedNode  -- Returns the selected node of this Tree_View widget.
  LTreeView:isExpanded( index )  -- Returns true if expanded is enabled for this Tree_View widget.
  LTreeView:isNodeExpanded( index )  -- Returns true if node expanded is enabled for this Tree_View widget.
  LTreeView:removeNode( index )  -- Removes the node from this Tree_View widget.
  LTreeView:setNodeIcon( index, icon )  -- Sets the node icon for this Tree_View widget.
  LTreeView:setNodeText( index, text )  -- Sets the node text for this Tree_View widget.
  LTreeView:setSelectedNode( index )  -- Sets the selected node for this Tree_View widget.
  LTreeView:toggleNode( index )  -- Toggles the expanded/collapsed status of a Tree_View node.
```

---

*Auto-generated from `logs/data/lua_api_data.json` by `tools/gen_wiki_api.py`. Do not edit by hand — run `python tools/gen_wiki_api.py` to regenerate.*