-- content/examples/window.lua
-- Scaffolded coverage of the lurek.window API (50 items).
--
-- Every --@api-stub: block below is a SCAFFOLD. The body must be
-- replaced by hand with a 3-6 line real usage snippet showing how to
-- call the API in real game context, written by reading:
--   * src/lua_api/window_api.rs   (Lua binding, arg types, return shape)
--   * src/window/                 (semantics, side effects)
--   * docs/specs/window.md        (canonical reference)
--
-- Snippet rules (love2d-wiki style):
--   * NO `return` at top-level (breaks the file).
--   * NO `pcall` defensive wrappers, NO `if false then`.
--   * Wrap GPU / audio / physics calls inside
--     `function lurek.render() ... end` or
--     `function lurek.update(dt) ... end` callbacks so the file loads.
--   * Use REAL values: paths like "sfx/jump.ogg", keys like "space",
--     colours like {1, 0.5, 0, 1}.
--   * Keep the two `--` comment lines: 1) what the API does (use the
--     existing description), 2) one line of practical advice.
--
-- Run: cargo run -- content/examples/window.lua

-- ── lurek.window.* functions ──

--@api-stub: lurek.window.setTitle
-- Sets the window title bar text.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.setTitle
  local _todo = "TODO: write a real lurek.window.setTitle usage example"
  print(_todo)
end

--@api-stub: lurek.window.getTitle
-- Returns the current window title.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getTitle
  local _todo = "TODO: write a real lurek.window.getTitle usage example"
  print(_todo)
end

--@api-stub: lurek.window.getWidth
-- Returns the window width in pixels.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getWidth
  local _todo = "TODO: write a real lurek.window.getWidth usage example"
  print(_todo)
end

--@api-stub: lurek.window.getHeight
-- Returns the window height in pixels.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getHeight
  local _todo = "TODO: write a real lurek.window.getHeight usage example"
  print(_todo)
end

--@api-stub: lurek.window.getDimensions
-- Returns the window dimensions as width, height.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getDimensions
  local _todo = "TODO: write a real lurek.window.getDimensions usage example"
  print(_todo)
end

--@api-stub: lurek.window.setFullscreen
-- Enables or disables fullscreen mode.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.setFullscreen
  local _todo = "TODO: write a real lurek.window.setFullscreen usage example"
  print(_todo)
end

--@api-stub: lurek.window.getFullscreen
-- Returns the fullscreen state and type string.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getFullscreen
  local _todo = "TODO: write a real lurek.window.getFullscreen usage example"
  print(_todo)
end

--@api-stub: lurek.window.isOpen
-- Returns whether the window is open.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.isOpen
  local _todo = "TODO: write a real lurek.window.isOpen usage example"
  print(_todo)
end

--@api-stub: lurek.window.setVSync
-- Sets the VSync mode (1=on, 0=off, -1=adaptive).
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.setVSync
  local _todo = "TODO: write a real lurek.window.setVSync usage example"
  print(_todo)
end

--@api-stub: lurek.window.getVSync
-- Returns the current VSync mode integer.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getVSync
  local _todo = "TODO: write a real lurek.window.getVSync usage example"
  print(_todo)
end

--@api-stub: lurek.window.hasFocus
-- Returns whether the window has keyboard focus.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.hasFocus
  local _todo = "TODO: write a real lurek.window.hasFocus usage example"
  print(_todo)
end

--@api-stub: lurek.window.hasMouseFocus
-- Returns whether the mouse cursor is inside the window.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.hasMouseFocus
  local _todo = "TODO: write a real lurek.window.hasMouseFocus usage example"
  print(_todo)
end

--@api-stub: lurek.window.isMinimized
-- Returns whether the window is minimized.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.isMinimized
  local _todo = "TODO: write a real lurek.window.isMinimized usage example"
  print(_todo)
end

--@api-stub: lurek.window.isMaximized
-- Returns whether the window is maximized.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.isMaximized
  local _todo = "TODO: write a real lurek.window.isMaximized usage example"
  print(_todo)
end

--@api-stub: lurek.window.isVisible
-- Returns whether the window is visible.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.isVisible
  local _todo = "TODO: write a real lurek.window.isVisible usage example"
  print(_todo)
end

--@api-stub: lurek.window.minimize
-- Minimizes the window to the taskbar.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.minimize
  local _todo = "TODO: write a real lurek.window.minimize usage example"
  print(_todo)
end

--@api-stub: lurek.window.maximize
-- Maximizes the window to fill the desktop.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.maximize
  local _todo = "TODO: write a real lurek.window.maximize usage example"
  print(_todo)
end

--@api-stub: lurek.window.restore
-- Restores the window from minimized or maximized state.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.restore
  local _todo = "TODO: write a real lurek.window.restore usage example"
  print(_todo)
end

--@api-stub: lurek.window.getPosition
-- Returns the window position as x, y in screen coordinates.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getPosition
  local _todo = "TODO: write a real lurek.window.getPosition usage example"
  print(_todo)
end

--@api-stub: lurek.window.setPosition
-- Moves the window to the given screen position.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.setPosition
  local _todo = "TODO: write a real lurek.window.setPosition usage example"
  print(_todo)
end

--@api-stub: lurek.window.getDisplayCount
-- Returns the number of connected displays.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getDisplayCount
  local _todo = "TODO: write a real lurek.window.getDisplayCount usage example"
  print(_todo)
end

--@api-stub: lurek.window.getDesktopDimensions
-- Returns the desktop resolution as width, height.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getDesktopDimensions
  local _todo = "TODO: write a real lurek.window.getDesktopDimensions usage example"
  print(_todo)
end

--@api-stub: lurek.window.getDPIScale
-- Returns the DPI scaling factor for the window.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getDPIScale
  local _todo = "TODO: write a real lurek.window.getDPIScale usage example"
  print(_todo)
end

--@api-stub: lurek.window.toPixels
-- Converts a device-independent coordinate to physical pixels.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.toPixels
  local _todo = "TODO: write a real lurek.window.toPixels usage example"
  print(_todo)
end

--@api-stub: lurek.window.fromPixels
-- Converts physical pixels to device-independent coordinates.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.fromPixels
  local _todo = "TODO: write a real lurek.window.fromPixels usage example"
  print(_todo)
end

--@api-stub: lurek.window.setIcon
-- Sets the window icon from a file path.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.setIcon
  local _todo = "TODO: write a real lurek.window.setIcon usage example"
  print(_todo)
end

--@api-stub: lurek.window.setMode
-- Resizes the window and optionally changes fullscreen and vsync.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.setMode
  local _todo = "TODO: write a real lurek.window.setMode usage example"
  print(_todo)
end

--@api-stub: lurek.window.getMode
-- Returns the window dimensions and mode flags as width, height, flags.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getMode
  local _todo = "TODO: write a real lurek.window.getMode usage example"
  print(_todo)
end

--@api-stub: lurek.window.close
-- Requests the window to close.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.close
  local _todo = "TODO: write a real lurek.window.close usage example"
  print(_todo)
end

--@api-stub: lurek.window.requestAttention
-- Flashes the window in the taskbar to request user attention.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.requestAttention
  local _todo = "TODO: write a real lurek.window.requestAttention usage example"
  print(_todo)
end

--@api-stub: lurek.window.getFullscreenModes
-- Returns all available fullscreen video modes.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getFullscreenModes
  local _todo = "TODO: write a real lurek.window.getFullscreenModes usage example"
  print(_todo)
end

--@api-stub: lurek.window.getDisplayName
-- Returns the name of the current display.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getDisplayName
  local _todo = "TODO: write a real lurek.window.getDisplayName usage example"
  print(_todo)
end

--@api-stub: lurek.window.getPixelDimensions
-- Returns the window dimensions in physical pixels.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getPixelDimensions
  local _todo = "TODO: write a real lurek.window.getPixelDimensions usage example"
  print(_todo)
end

--@api-stub: lurek.window.showMessageBox
-- Shows a platform-native message box dialog.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.showMessageBox
  local _todo = "TODO: write a real lurek.window.showMessageBox usage example"
  print(_todo)
end

--@api-stub: lurek.window.focus
-- Requests the window manager to bring the window to the foreground.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.focus
  local _todo = "TODO: write a real lurek.window.focus usage example"
  print(_todo)
end

--@api-stub: lurek.window.getNativeDPIScale
-- Returns the native DPI scale factor.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getNativeDPIScale
  local _todo = "TODO: write a real lurek.window.getNativeDPIScale usage example"
  print(_todo)
end

--@api-stub: lurek.window.getDisplayOrientation
-- Returns the current display orientation.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getDisplayOrientation
  local _todo = "TODO: write a real lurek.window.getDisplayOrientation usage example"
  print(_todo)
end

--@api-stub: lurek.window.getSafeArea
-- Returns the safe display area as x, y, w, h.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getSafeArea
  local _todo = "TODO: write a real lurek.window.getSafeArea usage example"
  print(_todo)
end

--@api-stub: lurek.window.getSystemTheme
-- Returns the OS color theme preference.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getSystemTheme
  local _todo = "TODO: write a real lurek.window.getSystemTheme usage example"
  print(_todo)
end

--@api-stub: lurek.window.isHighDPIAllowed
-- Returns whether high-DPI rendering is allowed.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.isHighDPIAllowed
  local _todo = "TODO: write a real lurek.window.isHighDPIAllowed usage example"
  print(_todo)
end

--@api-stub: lurek.window.getScaleInfo
-- Returns viewport scale and offset information as a table.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getScaleInfo
  local _todo = "TODO: write a real lurek.window.getScaleInfo usage example"
  print(_todo)
end

--@api-stub: lurek.window.getScaleMode
-- Returns the current viewport scale mode string.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getScaleMode
  local _todo = "TODO: write a real lurek.window.getScaleMode usage example"
  print(_todo)
end

--@api-stub: lurek.window.setScaleMode
-- Sets the viewport scale mode.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.setScaleMode
  local _todo = "TODO: write a real lurek.window.setScaleMode usage example"
  print(_todo)
end

--@api-stub: lurek.window.getGameWidth
-- Returns the logical game width in virtual pixels.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getGameWidth
  local _todo = "TODO: write a real lurek.window.getGameWidth usage example"
  print(_todo)
end

--@api-stub: lurek.window.getGameHeight
-- Returns the logical game height in virtual pixels.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.getGameHeight
  local _todo = "TODO: write a real lurek.window.getGameHeight usage example"
  print(_todo)
end

--@api-stub: lurek.window.isFullscreen
-- Returns whether the window is in fullscreen mode.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.isFullscreen
  local _todo = "TODO: write a real lurek.window.isFullscreen usage example"
  print(_todo)
end

--@api-stub: lurek.window.isResizable
-- Returns whether the window can be resized by the user.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.isResizable
  local _todo = "TODO: write a real lurek.window.isResizable usage example"
  print(_todo)
end

--@api-stub: lurek.window.onDpiChange
-- Registers a callback invoked (with the new scale factor) when the display.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.onDpiChange
  local _todo = "TODO: write a real lurek.window.onDpiChange usage example"
  print(_todo)
end

--@api-stub: lurek.window.pollDpiChange
-- Polls for a pending DPI change event and returns the new scale factor if any.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.pollDpiChange
  local _todo = "TODO: write a real lurek.window.pollDpiChange usage example"
  print(_todo)
end

--@api-stub: lurek.window.openFileDialog
-- Opens a blocking native file-open dialog.
-- TODO: replace this scaffold with a real usage snippet (see src/lua_api/window_api.rs and docs/specs/window.md).
do  -- TODO: lurek.window.openFileDialog
  local _todo = "TODO: write a real lurek.window.openFileDialog usage example"
  print(_todo)
end

