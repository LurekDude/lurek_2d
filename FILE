# Luna2D Lua API Reference

*Auto-generated by `tools/gen_docs_lua.py`. Version: `0.4.0` | Generated: 2026-04-02*
*Coverage: 2096/2328 functions documented (90.0%)*

---

## Contents

- [`luna.graphics_ext`](#graphics-ext) — 12 fn, 15 classes
- [`luna.window`](#window) — 34 fn
- [`luna.input`](#input) — 42 fn
- [`luna.timer`](#timer) — 8 fn, 1 class
- [`luna.math`](#math) — 27 fn, 3 classes
- [`luna.math_ext`](#math-ext) — 30 fn, 7 classes
- [`luna.audio`](#audio) — 46 fn, 3 classes
- [`luna.filesystem`](#filesystem) — 20 fn, 1 class
- [`luna.event`](#event) — 5 fn, 1 class
- [`luna.system`](#system) — 22 fn
- [`luna.thread`](#thread) — 3 fn, 1 class
- [`luna.ai`](#ai) — 21 fn, 15 classes
- [`luna.compute`](#compute) — 5 fn, 1 class
- [`luna.dataframe`](#dataframe) — 7 fn, 2 classes
- [`luna.data`](#data) — 8 fn
- [`luna.image`](#image) — 1 fn
- [`luna.sound`](#sound) — 4 fn
- [`luna.graph`](#graph) — 1 fn, 4 classes
- [`luna.dialog`](#dialog) — 1 fn, 1 class
- [`luna.entity`](#entity) — 1 fn, 1 class
- [`luna.scene`](#scene) — 22 fn, 1 class
- [`luna.pathfinding`](#pathfinding) — 6 fn, 3 classes
- [`luna.postfx`](#postfx) — 4 fn, 2 classes
- [`luna.minimap`](#minimap) — 1 fn, 1 class
- [`luna.savegame`](#savegame) — 1 fn, 1 class
- [`luna.modding`](#modding) — 2 fn, 1 class
- [`luna.localization`](#localization) — 10 fn
- [`luna.stats`](#stats) — 8 fn, 1 class
- [`luna.inventory`](#inventory) — 6 fn, 6 classes
- [`luna.crafting`](#crafting) — 9 fn, 9 classes
- [`luna.cardgame`](#cardgame) — 20 fn, 8 classes
- [`luna.combat`](#combat) — 6 fn, 7 classes
- [`luna.log`](#log) — 7 fn
- [`luna.debug`](#debug) — 33 fn
- [`luna.battle`](#battle) — 4 fn, 4 classes
- [`luna.debugbridge`](#debugbridge) — 15 fn
- [`luna.docs`](#docs) — 22 fn, 4 classes
- [`luna.item`](#item) — 18 fn, 7 classes
- [`luna.patterns`](#patterns) — 6 fn, 6 classes
- [`luna.quest`](#quest) — 4 fn, 4 classes
- [`luna.resource`](#resource) — 1 fn, 1 class

---

## Callbacks

All callbacks are optional. Define any in `main.lua` and the engine calls them automatically.

```lua
function luna.load()                                                                      -- Called once after the script is loaded.
function luna.update( dt : number )                                                       -- Called every frame; dt = elapsed seconds.
function luna.draw()                                                                      -- Called every frame for rendering.
function luna.keypressed( key : string, scancode : string, isrepeat : boolean )           -- Key press event.
function luna.keyreleased( key : string, scancode : string )                              -- Key release event.
function luna.textinput( text : string )                                                  -- Unicode character typed.
function luna.mousepressed( x : number, y : number, button : number )                     -- Mouse button press.
function luna.mousereleased( x : number, y : number, button : number )                    -- Mouse button release.
function luna.wheelmoved( x : number, y : number )                                        -- Mouse wheel scroll.
function luna.gamepadpressed( id : number, button : string )                              -- Gamepad button press.
function luna.gamepadreleased( id : number, button : string )                             -- Gamepad button release.
function luna.gamepadaxis( id : number, axis : string, value : number )                   -- Gamepad axis; value in -1..1.
function luna.joystickadded( id : number )                                                -- Gamepad connected.
function luna.joystickremoved( id : number )                                              -- Gamepad disconnected.
function luna.touchpressed( id, x : number, y : number, dx : number, dy : number, pressure : number ) -- Touch begin.
function luna.touchmoved(  id, x : number, y : number, dx : number, dy : number, pressure : number ) -- Touch move.
function luna.touchreleased(id, x : number, y : number, dx : number, dy : number, pressure : number ) -- Touch end.
function luna.focus( focused : boolean )                                                  -- Window focus change.
function luna.visible( visible : boolean )                                                -- Window show/hide.
function luna.resize( w : number, h : number )                                            -- Window resized.
function luna.quit()                                                                      -- Return true to cancel quit.
function luna.errorhandler( msg : string )                                                -- Unhandled Lua error.
```

---

## `luna.graphics_ext` {#graphics-ext}

> Lua API bindings for Phase 24 graphics extension types.
>
> Provides UserData wrappers and factory functions for extended graphics
> subsystems: Light2D, TextureAtlas, DrawLayer, Viewport, ViewportScale,
> SpriteSheet, PolygonMap, GraphRenderer, LargeMapRenderer, ColumnBatch,
> Camera2D, Animation, Trail, DecalSurface, and PaletteLUT.

```lua
luna.graphics_ext.newAnimation()
luna.graphics_ext.newCamera2D( [w], [h] )
luna.graphics_ext.newColumnBatch( col_count, screen_w, screen_h )
luna.graphics_ext.newDrawLayer()
luna.graphics_ext.newGraphRenderer()
luna.graphics_ext.newLargeMapRenderer( tile_w, tile_h )
luna.graphics_ext.newLight2D( x, y, radius )
luna.graphics_ext.newPolygonMap()
luna.graphics_ext.newSpriteSheet( tex_w, tex_h, frame_w, frame_h )
luna.graphics_ext.newTextureAtlas( w, h, [padding] )
luna.graphics_ext.newViewport( w, h, [mode] )
luna.graphics_ext.newViewportScale( w, h, [mode] )
```

### `Animation`

```lua
Animation:addFrame( x : number, y : number, w : number, h : number )  -- Adds frame to the collection
Animation:getClipCount()                                              -- Returns the clip count
Animation:getCurrentClip()                                            -- Returns the current clip
Animation:getCurrentFrame()                                           -- Returns the current frame
Animation:getCurrentQuad()                                            -- Returns the current quad
Animation:getFrameCount()                                             -- Returns the frame count
Animation:getSpeed()                                                  -- Returns the speed
Animation:isLooping() -> boolean                                      -- Returns `true` if looping
Animation:isPlaying() -> boolean                                      -- Returns `true` if playing
Animation:pause()                                                     -- Pauses playback
Animation:play( name : string )                                       -- Starts playback
Animation:resume()                                                    -- Resumes paused playback
Animation:setFrame( index : integer )                                 -- Sets the frame
Animation:setSpeed( f : number )                                      -- Sets the speed
Animation:stop()                                                      -- Stops playback
Animation:update( dt : number )                                       -- Advances the simulation by `dt` seconds
```

### `Camera2D`

```lua
Camera2D:getBounds()                                     -- Returns the bounds
Camera2D:getDeadZone()                                   -- Returns the dead zone
Camera2D:getFollowSmooth()                               -- Returns the follow smooth
Camera2D:getLookAhead()                                  -- Returns the look ahead
Camera2D:getPosition()                                   -- Returns the position
Camera2D:getRotation()                                   -- Returns the rotation
Camera2D:getTarget()                                     -- Returns the target
Camera2D:getViewport()                                   -- Returns the viewport
Camera2D:getVisibleArea()                                -- Returns the visible area
Camera2D:getZoom()                                       -- Returns the zoom
Camera2D:hasBounds() -> boolean                          -- Returns `true` if bounds
Camera2D:lookAt( x : number, y : number )                -- Look at on this Camera2D
Camera2D:move( dx : number, dy : number )                -- Move on this Camera2D
Camera2D:removeBounds()                                  -- Removes bounds from the collection
Camera2D:setDeadZone( w : number, h : number )           -- Sets the dead zone
Camera2D:setFollowSmooth( s )                            -- Sets the follow smooth
Camera2D:setLookAhead( mul : number )                    -- Sets the look ahead
Camera2D:setPosition( x : number, y : number )           -- Sets the position
Camera2D:setRotation( r : number )                       -- Sets the rotation
Camera2D:setTarget( x : number, y : number )             -- Sets the target
Camera2D:setZoom( z : number )                           -- Sets the zoom
Camera2D:shake( intensity : number, duration : number )  -- Shake on this Camera2D
Camera2D:toScreenCoords( wx : number, wy : number )      -- To screen coords on this Camera2D
Camera2D:toWorldCoords( sx : number, sy : number )       -- To world coords on this Camera2D
Camera2D:update( dt : number )                           -- Advances the simulation by `dt` seconds
```

### `ColumnBatch`

```lua
ColumnBatch:getColumnCount()             -- Returns the column count
ColumnBatch:getDepthAt( col : integer )  -- Returns the depth at
ColumnBatch:getDepthBuffer()             -- Returns the depth buffer
ColumnBatch:getScreenHeight()            -- Returns the screen height
ColumnBatch:getScreenWidth()             -- Returns the screen width
```

### `DecalSurface`

```lua
DecalSurface:getDimensions()  -- Returns the dimensions
DecalSurface:getHeight()      -- Returns the height
DecalSurface:getWidth()       -- Returns the width
```

### `DrawLayer`

```lua
DrawLayer:clear()                               -- Removes all entries
DrawLayer:flush()                               -- Flushes pending data
DrawLayer:getCount()                            -- Returns the count
DrawLayer:queue( z : number, func : function )  -- Queue on this DrawLayer
```

### `GraphRenderer`

```lua
GraphRenderer:autoRange()                                  -- Auto range on this GraphRenderer
GraphRenderer:clearSeries()                                -- Clear series on this GraphRenderer
GraphRenderer:getCursorValue()                             -- Returns the cursor value
GraphRenderer:getRange()                                   -- Returns the range
GraphRenderer:getViewport()                                -- Returns the viewport
GraphRenderer:removeSeries( name : string )                -- Removes series from the collection
GraphRenderer:setCursorPosition( x : number, y : number )  -- Sets the cursor position
GraphRenderer:setShowAxes( b : boolean )                   -- Sets the show axes
GraphRenderer:setShowGrid( b : boolean )                   -- Sets the show grid
GraphRenderer:setShowLabels( b : number )                  -- Sets the show labels
GraphRenderer:setTitle( text )                             -- Sets the title
```

### `LargeMapRenderer`

```lua
LargeMapRenderer:getChunkSize()                                          -- Returns the chunk size
LargeMapRenderer:getMapSize()                                            -- Returns the map size
LargeMapRenderer:getTile( x : integer, y : integer )                     -- Returns the tile
LargeMapRenderer:getTilesetColumns()                                     -- Returns the tileset columns
LargeMapRenderer:getTotalChunks()                                        -- Returns the total chunks
LargeMapRenderer:getVisibleChunks()                                      -- Returns the visible chunks
LargeMapRenderer:invalidateAll()                                         -- Invalidate all on this LargeMapRenderer
LargeMapRenderer:invalidateChunk( cx : integer, cy : integer )           -- Invalidate chunk on this LargeMapRenderer
LargeMapRenderer:isLODEnabled() -> boolean                               -- Returns `true` if l o d enabled
LargeMapRenderer:setCamera( x : number, y : number, zoom : number )      -- Sets the camera
LargeMapRenderer:setChunkSize( s )                                       -- Sets the chunk size
LargeMapRenderer:setLODEnabled( b : boolean )                            -- Sets the l o d enabled
LargeMapRenderer:setLODThresholds( levels )                              -- Sets the l o d thresholds
LargeMapRenderer:setTile( x : integer, y : integer, tile_id : integer )  -- Sets the tile
LargeMapRenderer:setTilesetColumns( cols : integer )                     -- Sets the tileset columns
LargeMapRenderer:setViewport( w : number, h : number )                   -- Sets the viewport
```

### `Light2D`

```lua
Light2D:getColor() -> number                   -- Returns the current light color
Light2D:getIntensity()                         -- Returns the intensity
Light2D:getPosition() -> number                -- Returns the light position in world space
Light2D:getRadius() -> number                  -- Returns the current light radius in world units
Light2D:isEnabled() -> boolean                 -- Returns `true` if this light is currently enabled
Light2D:setEnabled( b )                        -- Enables or disables this light. Disabled lights contribute no illumination
Light2D:setIntensity( i : number )             -- Sets the intensity
Light2D:setPosition( x : number, y : number )  -- Sets the light source position in world space
Light2D:setRadius( r )                         -- Sets the falloff radius of this light. Pixels beyond the radius receive no illumination
```

### `PaletteLUT`

```lua
PaletteLUT:clear()                          -- Removes all entries
PaletteLUT:getColorCount()                  -- Returns the color count
PaletteLUT:getFromColor( index : integer )  -- Returns the from color
PaletteLUT:getToColor( index : integer )    -- Returns the to color
```

### `PolygonMap`

```lua
PolygonMap:clear()                                -- Removes all entries
PolygonMap:clearHighlight()                       -- Clear highlight on this PolygonMap
PolygonMap:getBoundingBox()                       -- Returns the bounding box
PolygonMap:getRegionAt( x : number, y : number )  -- Returns the region at
PolygonMap:getRegionCenter( name : string )       -- Returns the region center
PolygonMap:getRegionColor( name : string )        -- Returns the region color
PolygonMap:getRegionNames()                       -- Returns the region names
PolygonMap:getRegionVertices( name : string )     -- Returns the region vertices
PolygonMap:highlight( name : string )             -- Highlight on this PolygonMap
PolygonMap:removeRegion( name : string )          -- Removes region from the collection
PolygonMap:setOutlineWidth( w )                   -- Sets the outline width
```

### `SpriteSheet`

```lua
SpriteSheet:getColumn( col )                              -- Returns the column
SpriteSheet:getDirectionFrames( direction : integer )     -- Returns the direction frames
SpriteSheet:getFrame( index : integer )                   -- Returns the frame
SpriteSheet:getFrameCount() -> integer                    -- Returns the total number of frames in this sprite sheet
SpriteSheet:getFrameSize()                                -- Returns the frame size
SpriteSheet:getGridSize()                                 -- Returns the grid size
SpriteSheet:getGroup( name : string )                     -- Returns the group
SpriteSheet:getGroupNames()                               -- Returns the group names
SpriteSheet:getRange( start : integer, count : integer )  -- Returns the range
SpriteSheet:getRow( row : integer )                       -- Returns the row
```

### `TextureAtlas`

```lua
TextureAtlas:clear()                                          -- Removes all entries
TextureAtlas:getDimensions()                                  -- Returns the dimensions
TextureAtlas:getRegion( name : string ) -> table              -- Returns the pixel bounds of the named region
TextureAtlas:getRegionCount()                                 -- Returns the region count
TextureAtlas:getRegions()                                     -- Returns the regions
TextureAtlas:pack( name : string, w : integer, h : integer )  -- Pack on this TextureAtlas
```

### `Trail`

```lua
Trail:clear()                                     -- Removes all entries
Trail:getLifetime()                               -- Returns the lifetime
Trail:getPointCount()                             -- Returns the point count
Trail:getWidth()                                  -- Returns the width
Trail:pushPoint( x : number, y : number )         -- Adds point to the collection
Trail:setLifetime( lt : number )                  -- Sets the lifetime
Trail:setMinDistance( d : number )                -- Sets the min distance
Trail:setWidth( start : number, [end : number] )  -- Sets the width
Trail:update( dt )                                -- Advances the simulation by `dt` seconds
```

### `Viewport`

```lua
Viewport:getGameDimensions()                 -- Returns the game dimensions
Viewport:getOffset() -> number               -- Returns the current viewport scroll offset
Viewport:getScale() -> number                -- Returns the current content scale factor
Viewport:getScaleMode()                      -- Returns the scale mode
Viewport:resize( w : number, h : number )    -- Resize on this Viewport
Viewport:setScaleMode( mode )                -- Sets the scale mode
Viewport:toGame( sx : number, sy : number )  -- To game on this Viewport
Viewport:toScreen( gx, gy )                  -- Converts world-space coordinates to screen-space pixel coordinates
```

### `ViewportScale`

```lua
ViewportScale:getGameDimensions()                         -- Returns the game dimensions
ViewportScale:getMode()                                   -- Returns the mode
ViewportScale:getOffset()                                 -- Returns the offset
ViewportScale:getScale()                                  -- Returns the scale
ViewportScale:getScaledDimensions()                       -- Returns the scaled dimensions
ViewportScale:resize( w : number, h : number )            -- Resize on this ViewportScale
ViewportScale:toGameCoords( sx : number, sy : number )    -- To game coords on this ViewportScale
ViewportScale:toScreenCoords( gx : number, gy : number )  -- To screen coords on this ViewportScale
```

---

## `luna.window` {#window}

> Window Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for window api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.window.close()                             -- Requests the application to close the window
luna.window.fromPixels( value )                 -- Converts physical pixels to device-independent coordinates
luna.window.getDPIScale()                       -- Returns the DPI scaling factor for the window
luna.window.getDesktopDimensions()              -- Returns the desktop resolution (width, height) for the given display
luna.window.getDimensions()                     -- Returns the window dimensions (width, height)
luna.window.getDisplayCount()                   -- Returns the number of connected displays
luna.window.getDisplayName( [display] )         -- Returns the name of the given display
luna.window.getFullscreen()                     -- Returns the current fullscreen type string, or nil if windowed
luna.window.getFullscreenModes()                -- Returns all available fullscreen video modes
luna.window.getHeight()                         -- Returns the window height in pixels
luna.window.getMode()                           -- Returns the current window mode settings as a table
luna.window.getPixelDimensions()                -- Returns the window dimensions in physical pixels (for HiDPI)
luna.window.getPosition()                       -- Returns the window position (x, y) on the desktop
luna.window.getTitle() -> string                -- Returns the current text displayed in the operating-system window title bar
luna.window.getVSync()                          -- Returns the current VSync mode value
luna.window.getWidth()                          -- Returns the window width in pixels
luna.window.hasFocus()                          -- Returns whether the window has keyboard input focus
luna.window.hasMouseFocus()                     -- Returns whether the mouse cursor is inside the window
luna.window.isMaximized()                       -- Returns whether the window is maximized
luna.window.isMinimized()                       -- Returns whether the window is minimized
luna.window.isOpen()                            -- Returns whether the window has been created and is not yet closed
luna.window.isVisible()                         -- Returns whether the window is currently visible
luna.window.maximize()                          -- Maximizes the window so it fills all available desktop space
luna.window.minimize()                          -- Minimizes the window to the OS taskbar or dock
luna.window.requestAttention()                  -- Flashes the window in the taskbar to request user attention
luna.window.restore()                           -- Restores the window from maximized or minimized state
luna.window.setFullscreen( enabled, [fstype] )  -- Enables or disables fullscreen mode
luna.window.setIcon( path )                     -- Sets the window icon from a pixel buffer
luna.window.setMode( w, h, [flags] )            -- Resizes the window and optionally changes fullscreen mode
luna.window.setPosition( x, y )                 -- Moves the window to the given desktop position
luna.window.setTitle( title : New )             -- Sets the text displayed in the window's title bar
luna.window.setVSync( mode )                    -- Enables or disables vertical synchronization
luna.window.showMessageBox()                    -- Shows a platform native message box dialog
luna.window.toPixels( value )                   -- Converts a device-independent coordinate to physical pixels
```

---

## `luna.input` {#input}

> Input Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for input api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.input.getAxis( id, axis )             -- Returns the current value (-1 to 1) of a gamepad analog axis
luna.input.getAxisCount( id )              -- Returns the total number of analog axes on the gamepad
luna.input.getBackgroundEvents()           -- Returns whether background gamepad events are enabled
luna.input.getButtonCount( id )            -- Returns the total number of buttons on the gamepad
luna.input.getCount()                      -- Returns the number of connected gamepads
luna.input.getCursor()                     -- Returns the currently active cursor ID
luna.input.getGUID( id )                   -- Returns the hardware GUID string of the gamepad
luna.input.getHat( id, hat )               -- Returns the direction (string) of a hat switch on the gamepad
luna.input.getJoystickCount()              -- Returns the number of tracked gamepad slots
luna.input.getJoysticks()                  -- Returns a list of connected gamepad IDs
luna.input.getKeyFromScancode( scancode )  -- Returns the key name for the given hardware scancode
luna.input.getName( id )                   -- Returns the human-readable name of a gamepad
luna.input.getPosition()                   -- Returns the current position (x, y)
luna.input.getPosition( id )               -- Returns the current position (x, y)
luna.input.getPressure( id )               -- Returns the pressure (0-1) of the touch with the given ID
luna.input.getRelativeMode()               -- Returns whether relative mouse mode is active
luna.input.getScancodeFromKey( key )       -- Returns the hardware scancode for the given key name
luna.input.getTouchCount()                 -- Returns the number of currently active touch points
luna.input.getTouches()                    -- Returns a table of active touch points (id, x, y, pressure)
luna.input.getWheelDelta()                 -- Returns the mouse scroll wheel delta (dx, dy) since last frame
luna.input.getX()                          -- Returns the current mouse X position in window coordinates
luna.input.getY()                          -- Returns the current mouse Y position in window coordinates
luna.input.hasKeyRepeat()                  -- Returns whether key-repeat is currently enabled
luna.input.hasTextInput()                  -- Returns whether text input mode is currently active
luna.input.isConnected( id )               -- Returns whether the gamepad with the given ID is connected
luna.input.isDown( args )                  -- Returns whether the given key is currently held down
luna.input.isDown( button )                -- Returns whether the given key is currently held down
luna.input.isDown( id, button )            -- Returns whether the given key is currently held down
luna.input.isGamepad( id )                 -- Returns whether the joystick at the given slot is a recognized gamepad
luna.input.isGrabbed()                     -- Returns whether the mouse cursor is locked to the window
luna.input.isScancodeDown( scancode )      -- Returns whether the key with the given scancode is held
luna.input.isVibrationSupported( id )      -- Returns whether the gamepad supports haptic vibration
luna.input.isVisible()                     -- Returns whether the mouse cursor is currently visible
luna.input.setBackgroundEvents( enable )   -- Enable or disable receiving gamepad events when the window is not focused
luna.input.setCursor( name )               -- Sets the currently visible mouse cursor to the given hardware cursor handle
luna.input.setGrabbed( grabbed )           -- Locks or unlocks the mouse cursor to the window
luna.input.setKeyRepeat( enabled )         -- Enables or disables key-repeat events
luna.input.setPosition( x, y )             -- Moves the mouse cursor to the given window-space position
luna.input.setRelativeMode( relative )     -- Enables or disables raw relative mouse motion mode
luna.input.setTextInput( enabled )         -- Enables or disables Unicode text input mode
luna.input.setVibration( args )            -- Triggers haptic rumble with the given left/right motor strengths
luna.input.setVisible( visible : true )    -- Shows or hides the operating-system mouse cursor over the game window
```

---

## `luna.timer` {#timer}

> Timer Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for timer api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.timer.getAverageDelta()  -- Returns the rolling-average frame delta time in seconds
luna.timer.getDelta()         -- Returns the delta time (seconds) for the current frame
luna.timer.getFPS()           -- Returns the measured frames-per-second for the current frame
luna.timer.getMicroTime()     -- Returns the high-resolution monotonic timer value in microseconds
luna.timer.getTime()          -- Returns the total elapsed time in seconds since engine start
luna.timer.newScheduler()     -- Creates an independent timer scheduler for managing a set of callbacks
luna.timer.sleep( seconds )   -- Suspends execution for the given number of seconds
luna.timer.step()             -- Advances the timer by one step (called automatically each frame)
```

### `mlua`

```lua
mlua:after( delay, func )                       -- Calls a Lua function after the given delay in seconds
mlua:cancel( id )                               -- Cancels a scheduled timer callback
mlua:cancelAll()                                -- Cancels and removes every pending timer entry from this scheduler
mlua:cancelNamed( name : Name )                 -- Cancels a specific named timer that was scheduled on this scheduler
mlua:getCount()                                 -- Returns the number of active timer entries currently in the scheduler
mlua:getInterval( id : Timer )                  -- Returns the repeat interval of the given timer entry in seconds
mlua:getRemaining( id : Timer )                 -- Returns the time remaining before the next invocation of the given timer
mlua:getRepeatCount( id : Timer ) -> integer    -- Returns how many times the given timer has fired since it was created
mlua:getTimeScale()                             -- Returns the current time-scale factor applied to all timers in this scheduler
mlua:isEmpty()                                  -- Returns whether the scheduler has no active timer entries
mlua:isPaused( id )                             -- Returns whether the scheduler is currently paused and not advancing timers
mlua:pause( id )                                -- Pauses the scheduler so no pending callbacks fire until resumed
mlua:resetEvent( id : Timer )                   -- Resets the elapsed time of the given timer so it fires again after its full interval
mlua:resume( id )                               -- Resumes a paused scheduler, allowing its callbacks to fire again
mlua:setInterval( id : Timer, interval : New )  -- Changes the repeat interval of an existing timer without recreating it
mlua:setTimeScale( scale : Time )               -- Sets a time-scale factor that speeds up or slows down all timers in the scheduler
mlua:update( dt )                               -- Advances all pending timers by dt seconds
```

---

## `luna.math` {#math}

> Math Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for math api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.math.abs( x : Input ) -> number        -- Returns the absolute (non-negative) value of x
luna.math.atan2( y : Y, x : X )             -- Returns the angle in radians between the positive x-axis and (y, x)
luna.math.ceil( x : Input ) -> integer      -- Returns the smallest integer greater than or equal to x (rounds up)
luna.math.clamp( x, lo, hi )                -- Clamps a value between min and max
luna.math.cos( x )                          -- Returns the cosine of the given angle in radians
luna.math.distance( x1, y1, x2, y2 )        -- Returns the Euclidean distance between two points
luna.math.ease( name, t )                   -- Applies an easing function to t in [0,1]
luna.math.floor( x : Input ) -> integer     -- Returns the largest integer less than or equal to x (rounds down)
luna.math.gammaToLinear( args ) -> Linear   -- Converts a sRGB gamma-encoded channel value to linear light intensity
luna.math.getRandomSeed() -> integer        -- Returns the current seed of the engine global random number generator
luna.math.isConvex( args )                  -- Returns whether a polygon described by points is convex
luna.math.lerp( a, b, t )                   -- Linearly interpolates between a and b by t
luna.math.linearToGamma( args ) -> Gamma    -- Converts a linear-light channel value to sRGB gamma-encoded space
luna.math.max( a, b )                       -- Returns the largest value from the given list of numbers
luna.math.min( a, b )                       -- Returns the smallest value from the given list of numbers
luna.math.newBezierCurve( args )            -- Creates a new Bezier curve from the given control points
luna.math.newRandomGenerator( [seed] )      -- Creates an independent random generator with its own seed
luna.math.newTransform()                    -- Creates a new affine Transform object
luna.math.noise( x, [y], [z], [w] )         -- Returns a smooth noise value (1D, 2D, or 3D)
luna.math.normalize( x, y )                 -- Normalizes an angle in radians into the canonical range [-pi, pi]
luna.math.random( [min], [max] )            -- Returns a pseudo-random number. No args: [0,1). One arg max: [1,max]. Two args min,max: [min,max]
luna.math.randomNormal( [stddev], [mean] )  -- Returns a normally distributed random number
luna.math.setRandomSeed( seed : Integer )   -- Seeds the engine global random number generator with the given integer
luna.math.sin( x )                          -- Returns the sine of the given angle in radians
luna.math.sqrt( x : Non ) -> number         -- Returns the positive square root of x
luna.math.tan( x )                          -- Returns the tangent of the given angle in radians
luna.math.triangulate( args )               -- Triangulates a simple polygon and returns index triples
```

### `BezierCurve`

```lua
BezierCurve:evaluate( t : Curve )                        -- Evaluates the curve at parameter t in [0, 1] and returns the world point
BezierCurve:getControlPoint( index )                     -- Returns the position of the control point at the given index
BezierCurve:getControlPointCount()                       -- Returns the total number of control points in the curve
BezierCurve:getDerivative()                              -- Returns a new BezierCurve that is the derivative (degree reduced) of this curve
BezierCurve:removeControlPoint( index )                  -- Removes the control point at the given index from the curve
BezierCurve:render( [segments] )                         -- Subdivides the curve to the given depth and returns the sample points
BezierCurve:translate( dx : Horizontal, dy : Vertical )  -- Shifts every control point by the given (dx, dy) offset in place
```

### `RandomGenerator`

```lua
RandomGenerator:getSeed()                  -- Returns the current random seed value
RandomGenerator:getState()                 -- Returns the full PRNG state as a string for later restoration
RandomGenerator:setSeed( seed : Integer )  -- Seeds this random generator with the given integer, resetting its sequence
RandomGenerator:setState( state )          -- Restores the PRNG state from a string returned by getState
```

### `Transform`

```lua
Transform:clone()                                                   -- Returns an independent deep copy of this transform object
Transform:getMatrix()                                               -- Returns the 16 matrix elements of the 4x4 transform in column-major order
Transform:inverse()                                                 -- Returns a new Transform that is the mathematical inverse of this one
Transform:inverseTransformPoint( x : Input, y : Input ) -> Inverse  -- Applies the inverse of this transform to the given point
Transform:reset()                                                   -- Resets the internal matrix to the identity (no transform applied)
Transform:rotate( angle : Rotation )                                -- Applies a rotation by the given angle to the internal matrix
Transform:scale( sx : Horizontal, [sy : Vertical] )                 -- Applies a scale factor to the transform's internal matrix
Transform:shear( kx : Horizontal, ky : Vertical )                   -- Applies a shear (skew) transformation to the internal matrix
Transform:transformPoint( x : Input, y : Input )                    -- Transforms a point (x, y) by this matrix and returns the result
Transform:translate( dx : Horizontal, dy : Vertical )               -- Applies a translation offset to the internal matrix
```

---

## `luna.math_ext` {#math-ext}

> Extended math API bindings — Phase 25 types and utility functions.
>
> Adds 7 factory functions and 23 module-level functions to `luna.math`.

```lua
luna.math_ext.angleBetween( x1, y1, x2, y2 )                              -- luna.math.angleBetween(x1, y1, x2, y2)
luna.math_ext.bresenham( x1, y1, x2, y2 )                                 -- luna.math.bresenham(x1, y1, x2, y2)
luna.math_ext.castRay2D( ox, oy, dx, dy, max_dist, segs_tbl )             -- luna.math.castRay2D(ox, oy, dx, dy, maxDist, segments)
luna.math_ext.cellularAutomata( w, h, [opts_tbl] )                        -- luna.math.cellularAutomata(width, height, opts)
luna.math_ext.circleContainsPoint( cx, cy, r, px, py )                    -- luna.math.circleContainsPoint(cx, cy, r, px, py)
luna.math_ext.circleIntersectsCircle( x1, y1, r1, x2, y2, r2 )            -- luna.math.circleIntersectsCircle(x1, y1, r1, x2, y2, r2)
luna.math_ext.circleIntersectsLine( cx, cy, r, lx1, ly1, lx2, ly2 )       -- luna.math.circleIntersectsLine(cx, cy, r, lx1, ly1, lx2, ly2)
luna.math_ext.circleIntersectsSegment( cx, cy, r, sx1, sy1, sx2, sy2 )    -- luna.math.circleIntersectsSegment(cx, cy, r, sx1, sy1, sx2, sy2)
luna.math_ext.closestPointOnSegment( px, py, x1, y1, x2, y2 )             -- luna.math.closestPointOnSegment(px, py, x1, y1, x2, y2)
luna.math_ext.convexHull( tbl )                                           -- Computes and returns the convex hull of a set of 2D points as an ordered vertex list
luna.math_ext.delaunayTriangulate( tbl )                                  -- luna.math.delaunayTriangulate(points)
luna.math_ext.distanceShade( distance, max_dist )                         -- luna.math.distanceShade(distance, maxDistance)
luna.math_ext.fieldOfView( ox, oy, segs_tbl, radius )                     -- luna.math.fieldOfView(ox, oy, segments, radius)
luna.math_ext.floodFill()                                                 -- luna.math.floodFill(data, width, height, sx, sy, threshold, mode)
luna.math_ext.lineIntersect( x1, y1, x2, y2, x3, y3, x4, y4 )             -- luna.math.lineIntersect(x1, y1, x2, y2, x3, y3, x4, y4)
luna.math_ext.newGrid( w, h, [cost] )                                     -- luna.math.newGrid(width, height, defaultCost)
luna.math_ext.newNoiseGenerator( [seed : Integer] )                       -- Creates a reusable Perlin/simplex noise generator with a given seed
luna.math_ext.newRaycaster2D( w, h )                                      -- luna.math.newRaycaster2D(width, height)
luna.math_ext.newSpatialHash( cell_size )                                 -- Creates a spatial hash grid for fast broad-phase proximity and overlap queries
luna.math_ext.newTileWalker( x, y, [facing] )                             -- luna.math.newTileWalker(x, y, facing)
luna.math_ext.newTween( duration, [easing] )                              -- luna.math.newTween(duration, easing)
luna.math_ext.newVec2( [x : X], [y : Y] )                                 -- Creates a new 2-component vector object with x and y fields
luna.math_ext.perlinNoisePeriodic( x, y, px, py )                         -- luna.math.perlinNoisePeriodic(x, y, px, py)
luna.math_ext.pointInPolygon( tbl, px, py )                               -- luna.math.pointInPolygon(vertices, px, py)
luna.math_ext.poissonDisk( w, h, min_dist, [max_attempts], [seed] )       -- luna.math.poissonDisk(width, height, minDist, maxAttempts, seed)
luna.math_ext.polygonArea( tbl )                                          -- Returns the signed area of a simple polygon described by its vertex list
luna.math_ext.polygonCentroid( tbl )                                      -- luna.math.polygonCentroid(vertices)
luna.math_ext.projectColumn( distance, fov, screen_h )                    -- luna.math.projectColumn(distance, fov, screenHeight)
luna.math_ext.segmentIntersectsSegment( x1, y1, x2, y2, x3, y3, x4, y4 )  -- luna.math.segmentIntersectsSegment(x1, y1, x2, y2, x3, y3, x4, y4)
luna.math_ext.voronoiDiagram( w, h, pts_tbl, [opts_tbl] )                 -- luna.math.voronoiDiagram(width, height, points, opts)
```

### `Grid`

```lua
Grid:buildFlowField( gx : integer, gy : integer )          -- Build flow field on this Grid
Grid:getCost( x : integer, y : integer )                   -- Returns the cost
Grid:getDimensions()                                       -- Returns the dimensions
Grid:getHeight()                                           -- Returns the height
Grid:getWidth()                                            -- Returns the width
Grid:isWalkable( x : integer, y : integer ) -> boolean     -- Returns `true` if walkable
Grid:setCost( x : integer, y : integer, cost : number )    -- Sets the cost
Grid:setWalkable( x : integer, y : integer, w : boolean )  -- Sets the walkable
```

### `NoiseGenerator`

```lua
NoiseGenerator:getSeed()                             -- Returns the seed
NoiseGenerator:perlinNoise( args : LuaMultiValue )   -- Perlin noise on this NoiseGenerator
NoiseGenerator:setSeed( seed : integer )             -- Sets the seed
NoiseGenerator:simplexNoise( args : LuaMultiValue )  -- Simplex noise on this NoiseGenerator
```

### `Raycaster2D`

```lua
Raycaster2D:getCell( x : integer, y : integer )                 -- Returns the cell
Raycaster2D:getDimensions()                                     -- Returns the dimensions
Raycaster2D:getHeight()                                         -- Returns the height
Raycaster2D:getWidth()                                          -- Returns the width
Raycaster2D:isBlocked( x : integer, y : integer ) -> boolean    -- Returns `true` if blocked
Raycaster2D:setCell( x : integer, y : integer, val : integer )  -- Sets the cell
Raycaster2D:setCells( data : table )                            -- Sets the cells
```

### `SpatialHash`

```lua
SpatialHash:clear()                                              -- Removes all entries
SpatialHash:getCellSize()                                        -- Returns the cell size
SpatialHash:getItemCount()                                       -- Returns the item count
SpatialHash:queryCircle( cx : number, cy : number, r : number )  -- Query circle on this SpatialHash
SpatialHash:remove( id : any )                                   -- Removes the entry from the collection
```

### `TileWalker`

```lua
TileWalker:beginMove()                                      -- Begin move on this TileWalker
TileWalker:canMoveBackward() -> boolean                     -- Returns `true` if move backward
TileWalker:canMoveForward() -> boolean                      -- Returns `true` if move forward
TileWalker:canStrafeLeft() -> boolean                       -- Returns `true` if strafe left
TileWalker:canStrafeRight() -> boolean                      -- Returns `true` if strafe right
TileWalker:getFacing()                                      -- Returns the facing
TileWalker:getFacingAngle()                                 -- Returns the facing angle
TileWalker:getFacingDirection()                             -- Returns the facing direction
TileWalker:getInterpolatedAngle( t )                        -- Returns the interpolated angle
TileWalker:getInterpolatedPosition( t : number )            -- Returns the interpolated position
TileWalker:getPosition()                                    -- Returns the position
TileWalker:getRelativeFacing( tx : integer, ty : integer )  -- Returns the relative facing
TileWalker:moveBackward()                                   -- Move backward on this TileWalker
TileWalker:moveForward()                                    -- Move forward on this TileWalker
TileWalker:setFacing( facing : string )                     -- Sets the facing
TileWalker:setPosition( x : integer, y : integer )          -- Sets the position
TileWalker:setRaycaster( rc_ud : userdata )                 -- Sets the raycaster
TileWalker:strafeLeft()                                     -- Strafe left on this TileWalker
TileWalker:strafeRight()                                    -- Strafe right on this TileWalker
TileWalker:turnAround()                                     -- Turn around on this TileWalker
TileWalker:turnLeft()                                       -- Turn left on this TileWalker
TileWalker:turnRight()                                      -- Turn right on this TileWalker
```

### `Tween`

```lua
Tween:addValue( start : number, target : number )  -- Adds value to the collection
Tween:getClock()                                   -- Returns the clock
Tween:getDuration()                                -- Returns the duration
Tween:getValue( [index : integer] )                -- Returns the value
Tween:getValueCount()                              -- Returns the value count
Tween:isComplete() -> boolean                      -- Returns `true` if complete
Tween:reset()                                      -- Resets state to initial values
Tween:set( time : number )                         -- Sets the value
Tween:update( dt : number )                        -- Advances the simulation by `dt` seconds
```

### `Vec2`

```lua
Vec2:clone()                               -- Returns a deep copy of this object
Vec2:cross( other : userdata )             -- Cross on this Vec2
Vec2:dot( other : userdata )               -- Dot on this Vec2
Vec2:get()                                 -- Returns the current value
Vec2:getAngle()                            -- Returns the angle
Vec2:getDistance( other : userdata )       -- Returns the distance
Vec2:getLength()                           -- Returns the length
Vec2:getLengthSquared()                    -- Returns the length squared
Vec2:getNormalized()                       -- Returns the normalized
Vec2:getPerpendicular()                    -- Returns the perpendicular
Vec2:getRotated( angle : number )          -- Returns the rotated
Vec2:getX()                                -- Returns the x
Vec2:getY()                                -- Returns the y
Vec2:lerp( other : userdata, t : number )  -- Interpolates between start and target values
Vec2:set( x : number, y : number )         -- Sets the value
Vec2:setX( x : number )                    -- Sets the x
Vec2:setY( y : number )                    -- Sets the y
```

---

## `luna.audio` {#audio}

> Audio Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for audio api-related operations and data management.
> Key types exported from this module: `LuaSource`, `LuaBus`, `LuaMidiPlayer`.
> Primary functions: `register()`.

```lua
luna.audio.clearFilter( id_val )             -- Removes any active filter from the audio source
luna.audio.clone( id_val )                   -- Creates an independent copy of an audio source
luna.audio.fadeIn( id_val, dur )             -- Fades the audio source in from silence over the given duration
luna.audio.getActiveSourceCount()            -- Returns the number of currently playing audio sources
luna.audio.getDuration( id_val )             -- Returns the total duration of the audio source in seconds
luna.audio.getFadeIn( id_val )               -- Returns the current fade-in duration
luna.audio.getHighpass( id_val )             -- Returns the current high-pass filter cutoff frequency
luna.audio.getListener2D()                   -- Returns the current 2D listener position (x, y)
luna.audio.getLowpass( id_val )              -- Returns the current low-pass filter cutoff frequency
luna.audio.getMasterVolume()                 -- Returns the current master volume scale (0.0 - 1.0) applied to all audio output
luna.audio.getMaxSources()                   -- Returns the maximum number of simultaneous audio sources
luna.audio.getMeter()                        -- Returns the current peak level of the audio source
luna.audio.getPan( id_val )                  -- Returns the current stereo panning of the source
luna.audio.getPitch( id_val )                -- Returns the current pitch multiplier of the source
luna.audio.getSourceBus( id_val )            -- Returns the bus name the source is assigned to
luna.audio.getSourceCount()                  -- Returns the number of currently registered audio sources
luna.audio.getSourceType( id_val )           -- Returns the type string ('static' or 'stream') of the source
luna.audio.getVolume( id_val )               -- Returns the current volume of the audio source
luna.audio.isLooping( id_val )               -- Returns whether the audio source is set to loop
luna.audio.isPaused( id_val )                -- Returns whether the audio source is currently paused
luna.audio.isPlaying( id_val )               -- Returns whether the audio source is currently playing
luna.audio.isStopped( id_val )               -- Returns whether the audio source is stopped
luna.audio.newBus( name )                    -- Creates a named audio bus for grouping sources
luna.audio.newMidiPlayer( [path] )           -- Creates a software MIDI synthesizer player
luna.audio.newSource( args )                 -- Loads an audio file and returns a source handle
luna.audio.pause( id_val )                   -- Pauses the audio source at its current position
luna.audio.pauseAll()                        -- Pauses all currently playing audio sources
luna.audio.play( id_val )                    -- Plays the audio source from the beginning
luna.audio.playLooping( id_val )             -- Plays the audio source in a continuous loop
luna.audio.release( id_val )                 -- Releases the audio source and frees its memory
luna.audio.resume( id_val )                  -- Resumes playback of a paused audio source from its current position
luna.audio.resumeAll()                       -- Resumes playback on every audio source that is currently paused
luna.audio.seek( id_val, pos )               -- Seeks to the given time position (in seconds) in the source
luna.audio.setHighpass( id_val, cutoff_hz )  -- Applies a high-pass filter to the audio source
luna.audio.setListener2D( x, y )             -- Sets the 2D listener position for spatial audio
luna.audio.setLooping( id_val, looping )     -- Enables or disables looping playback for the source
luna.audio.setLowpass( id_val, cutoff_hz )   -- Applies a low-pass filter to the audio source
luna.audio.setMasterVolume( vol )            -- Sets the global master volume (0.0 - 1.0)
luna.audio.setMeter( scale )                 -- Enables or disables peak metering on the audio source
luna.audio.setPan( id_val, pan )             -- Sets the stereo panning (-1.0 left to 1.0 right) of the source
luna.audio.setPitch( id_val, pitch )         -- Sets the pitch (playback speed) multiplier of the source
luna.audio.setSourceBus( id_val, bus_val )   -- Assigns the source to a named audio bus
luna.audio.setVolume( id_val, vol )          -- Sets the playback volume (0.0 - 1.0) of the source
luna.audio.stop( id_val )                    -- Stops playback of the audio source
luna.audio.stopAll()                         -- Stops all currently playing audio sources
luna.audio.tell( id_val )                    -- Returns the current playback position in seconds
```

### `Bus`

```lua
Bus:getName()          -- Returns the name of this audio bus
Bus:getPitch()         -- Returns the pitch multiplier of this bus
Bus:getVolume()        -- Returns the current volume scale of this audio mixer bus
Bus:isPaused()         -- Returns whether this bus is currently paused
Bus:pause()            -- Pauses all audio sources that are currently playing through this bus
Bus:resume()           -- Resumes all paused sources on this bus
Bus:setPitch( pitch )  -- Sets the pitch multiplier for all sources on this bus
Bus:setVolume( vol )   -- Sets the volume for all sources routed to this bus
```

### `MidiPlayer`

```lua
MidiPlayer:getBus()                             -- Returns the audio bus that this MIDI player's output is routed through
MidiPlayer:getChannelCount() -> integer         -- Returns the number of MIDI channels present in the loaded sequence
MidiPlayer:getChannelInstrument( ch )           -- Returns the General MIDI instrument index for the given MIDI channel
MidiPlayer:getChannelVolume( ch )               -- Returns the current volume scale applied to the given MIDI channel
MidiPlayer:getDuration()                        -- Returns the total duration of the audio source in seconds
MidiPlayer:getFilePath()                        -- Returns the file path of the MIDI file currently loaded into the player
MidiPlayer:getNoteCount() -> integer            -- Returns the total number of note events in the loaded MIDI sequence
MidiPlayer:getOriginalTempo()                   -- Returns the original tempo written in the MIDI file, in beats per minute
MidiPlayer:getSoundFontPath()                   -- Returns the file path of the SoundFont (.sf2) currently loaded into the player
MidiPlayer:getTempo() -> number                 -- Returns the current playback tempo in beats per minute
MidiPlayer:getTempoScale()                      -- Returns the current tempo scale factor applied on top of the original BPM
MidiPlayer:getTicksPerBeat() -> integer         -- Returns the ticks-per-beat (PPQ) resolution defined in the MIDI file header
MidiPlayer:getTrackCount() -> integer           -- Returns the total number of tracks in the loaded MIDI sequence
MidiPlayer:getTrackName( idx )                  -- Returns the name string of the given MIDI track from the sequence metadata
MidiPlayer:getVolume()                          -- Returns the current volume of the audio source
MidiPlayer:isChannelMuted( ch )                 -- Returns whether the given MIDI channel is currently muted
MidiPlayer:isLoaded()                           -- Returns whether a MIDI file or data string has been successfully loaded
MidiPlayer:isLooping()                          -- Returns whether the audio source is set to loop
MidiPlayer:isPaused()                           -- Returns whether the audio source is currently paused
MidiPlayer:isPlaying()                          -- Returns whether the audio source is currently playing
MidiPlayer:isTrackMuted( idx )                  -- Returns whether the given track index is currently muted
MidiPlayer:load( path : File )                  -- Loads a MIDI file from the given path and prepares it for playback
MidiPlayer:loadData()                           -- Loads MIDI data from a Lua string directly into the player
MidiPlayer:pause()                              -- Pauses the audio source at its current position
MidiPlayer:play()                               -- Plays the audio source from the beginning
MidiPlayer:seek( secs )                         -- Seeks to the given time position (in seconds) in the source
MidiPlayer:setBus( bus_val )                    -- Routes the MIDI player's synthesizer output through the given audio bus
MidiPlayer:setChannelMuted( ch, muted : true )  -- Mutes or unmutes the given MIDI channel for selective playback
MidiPlayer:setChannelVolume( ch, vol )          -- Sets the volume scale for the specified MIDI channel
MidiPlayer:setLooping( looping )                -- Enables or disables looping playback for the source
MidiPlayer:setOnEnd( cb )                       -- Registers a callback invoked when the MIDI sequence finishes playing
MidiPlayer:setOnNoteOff( cb )                   -- Registers a callback invoked for each MIDI note-off event during playback
MidiPlayer:setOnNoteOn( cb )                    -- Registers a callback invoked for each MIDI note-on event during playback
MidiPlayer:setSoundFont( path : File )          -- Loads a SoundFont (.sf2) file into this player for instrument rendering
MidiPlayer:setTempo( bpm : Tempo )              -- Sets the playback tempo in beats per minute
MidiPlayer:setTempoScale( scale : Tempo )       -- Sets a multiplier applied to the original MIDI tempo during playback
MidiPlayer:setTrackMuted( idx, muted : true )   -- Mutes or unmutes a specific track by index for selective rendering
MidiPlayer:setVolume( vol )                     -- Sets the playback volume (0.0 - 1.0) of the source
MidiPlayer:soloChannel( ch )                    -- Solos the given MIDI channel so it is the only one producing sound
MidiPlayer:stop()                               -- Stops playback of the audio source
MidiPlayer:tell()                               -- Returns the current playback position in seconds
MidiPlayer:unsoloAll()                          -- Clears the solo flag on every track so all tracks are audible
MidiPlayer:useDefaultSoundFont()                -- Reverts the player to using the built-in default SoundFont for rendering
```

### `Source`

```lua
Source:clearFilter()               -- Removes any active filter from the audio source
Source:clone()                     -- Creates an independent copy of an audio source
Source:fadeIn( duration_secs )     -- Fades the audio source in from silence over the given duration
Source:getDuration() -> number     -- Returns the total duration of this audio source in seconds
Source:getFadeIn()                 -- Returns the current fade-in duration
Source:getHighpass()               -- Returns the current high-pass filter cutoff frequency
Source:getLowpass()                -- Returns the current low-pass filter cutoff frequency
Source:getPan()                    -- Returns the current stereo panning of the source
Source:getPitch() -> number        -- Returns the current pitch multiplier
Source:getType() -> string         -- Returns the type of this audio source: 'static', 'stream', or 'queue'
Source:getVolume() -> number       -- Returns the current volume multiplier
Source:isLooping() -> boolean      -- Returns `true` if this source is set to loop
Source:isPaused() -> boolean       -- Returns `true` if playback is currently paused
Source:isPlaying() -> boolean      -- Returns `true` if this source is currently playing
Source:isStopped() -> boolean      -- Returns `true` if playback has stopped (either manually or after the audio ended)
Source:pause()                     -- Pauses playback. Call `play()` to resume
Source:play()                      -- Starts or resumes playback from the current seek position
Source:resume()                    -- Resumes playback of this audio source from its current paused position
Source:seek( pos )                 -- Seeks playback to `offset` seconds from the start
Source:setHighpass( cutoff_hz )    -- Applies a high-pass filter to the audio source
Source:setLooping( looping )       -- Enables or disables looping. When enabled, the source restarts automatically when it reaches the end
Source:setLowpass( cutoff_hz )     -- Applies a low-pass filter to the audio source
Source:setPan( pan )               -- Sets the stereo panning (-1.0 left to 1.0 right) of the source
Source:setPitch( pitch : number )  -- Sets the playback pitch multiplier. `1.0` is normal pitch; `2.0` doubles frequency
Source:setVolume( vol )            -- Sets playback volume. `1.0` is full volume; `0.0` is silent
Source:stop()                      -- Stops playback and resets the seek position to the beginning
Source:tell() -> number            -- Returns the current playback position in seconds
```

---

## `luna.filesystem` {#filesystem}

> Filesystem Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for filesystem api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.filesystem.append( path : Relative, data : String )  -- Opens the file in append mode and writes the given string at the end
luna.filesystem.createDirectory( path )                   -- Creates a directory and any missing parent directories
luna.filesystem.exists( path )                            -- Returns whether the given file or directory exists
luna.filesystem.getDirectoryItems( path : Directory )     -- Returns a table containing the names of every file and subdirectory in the given path
luna.filesystem.getIdentity()                             -- Returns the identity string used to locate the game's sandboxed save directory
luna.filesystem.getInfo( path )                           -- Returns a table of metadata (size, modtime, kind) for a path
luna.filesystem.getSaveDirectory()                        -- Returns the sandboxed save data directory path
luna.filesystem.getSource()                               -- Returns the absolute path of the directory or archive the game was loaded from
luna.filesystem.getUserDirectory()                        -- Returns the current user's home directory path
luna.filesystem.getWorkingDirectory()                     -- Returns the current working directory path
luna.filesystem.isDirectory( path )                       -- Returns whether the given path is a directory
luna.filesystem.isFile( path )                            -- Returns whether the given path is a regular file
luna.filesystem.lines( path )                             -- Returns an iterator over lines in a text file
luna.filesystem.openFile( path, mode_str )                -- Opens a file and returns a readable/writable file handle
luna.filesystem.pollAsync( handle_id )                    -- Poll an async load handle. Returns status, data-or-nil
luna.filesystem.read( path )                              -- Reads a text file and returns its contents as a string
luna.filesystem.readAsync( path )                         -- Start loading a file in the background. Returns a numeric handle
luna.filesystem.remove( path : Relative )                 -- Permanently deletes the file at the given path from the save directory
luna.filesystem.setIdentity( name : Application )         -- Sets the identity string that names the game's sandboxed save-data directory
luna.filesystem.write( path, data )                       -- Writes a string to a file, creating it if needed
```

### `FileHandle`

```lua
FileHandle:close()          -- Flushes any pending writes and closes the file handle
FileHandle:flush()          -- Flushes all buffered writes to disk without closing the handle
FileHandle:getMode()        -- Returns the access mode the file was opened with
FileHandle:getSize()        -- Returns the size of the open file in bytes
FileHandle:isEOF()          -- Returns whether the read cursor has reached the end of the file
FileHandle:read( [count] )  -- Reads a text file and returns its contents as a string
FileHandle:readLine()       -- Reads the next line of text from the file and returns it as a string
FileHandle:seek( pos )      -- Seeks the file position to the given byte offset from the start
FileHandle:tell()           -- Returns the current read/write byte offset from the start of the file
FileHandle:write( data )    -- Writes a string to a file, creating it if needed
```

---

## `luna.event` {#event}

> Event Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for event api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.event.clear()         -- Discards all pending events in the queue
luna.event.newSignal()     -- Event
luna.event.poll()          -- Polls and returns the next event from the queue, or nil if empty
luna.event.push( args )    -- Pushes a custom event onto the event queue
luna.event.quit( [code] )  -- Pushes a quit event onto the event queue, requesting the engine to stop
```

### `Signal`

```lua
Signal:clear( name )     -- Removes all callbacks for the named event
Signal:clearAll()        -- Removes all callbacks across all events
Signal:emit( args )      -- Emits the named event, calling all registered callbacks in order
Signal:getCount( name )  -- Returns the callback count for the named event
Signal:getTotalCount()   -- Returns the total callback count across all events
Signal:remove( handle )  -- Removes a subscription by handle ID
```

---

## `luna.system` {#system}

> System Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for system api-related operations and data management.
> Key types exported from this module: `PowerState`.
> Primary functions: `get_processor_count()`, `get_memory_size()`, `open_url()`, `get_preferred_locales()`.

```lua
luna.system.getArch()                       -- Returns the CPU architecture string for the current machine
luna.system.getArgs()                       -- Returns the command-line arguments as a table
luna.system.getBatchResults( results )      -- Returns the output table from the most recently completed runBatch call
luna.system.getClipboardText()              -- Returns the current contents of the system clipboard
luna.system.getDebugOverlay()               -- Returns whether the debug overlay is currently visible
luna.system.getEnv( name : Environment )    -- Returns the value of the named OS environment variable, or nil if not set
luna.system.getInfo()                       -- Returns a table of system information including OS name, CPU model, and installed RAM
luna.system.getLastError()                  -- Returns the last unhandled error message, or nil
luna.system.getLogLevel()                   -- Returns the name of the current minimum log level for runtime messages
luna.system.getMemorySize()                 -- Returns the total amount of installed system RAM in megabytes
luna.system.getOS()                         -- Returns the host operating system name ('Windows', 'Linux', 'macOS')
luna.system.getPowerInfo()                  -- Returns battery state, percentage charged, and estimated time remaining
luna.system.getPreferredLocales()           -- Returns an ordered list of the user's preferred locale strings (e.g. 'en-US')
luna.system.getProcessorCount()             -- Returns the number of logical CPU cores available
luna.system.getVersion()                    -- Returns the Luna2D engine version string
luna.system.log( level, message )           -- Emit a log message from Lua at the specified level
luna.system.openURL( url )                  -- Opens a URL in the system's default browser
luna.system.parseArgs( [args : Argument] )  -- Parses a command-line argument string and returns a structured key/value table
luna.system.runBatch( tasks, [opts] )       -- Runs a list of shell commands in parallel and returns immediately without blocking
luna.system.setClipboardText( text )        -- Replaces the system clipboard contents with the given string
luna.system.setDebugOverlay( enabled )      -- Shows or hides the FPS/draw-call debug overlay
luna.system.setLogLevel( level : One )      -- Sets the minimum severity level for runtime log messages
```

---

## `luna.thread` {#thread}

> Registers the `luna.thread` namespace.
>
> Provides Lua-level multithreading: create background threads that run
> independent Lua VMs and communicate via thread-safe channels.

```lua
luna.thread.getChannel( name )  -- Get or create a named global channel (singleton, shared across threads)
luna.thread.newChannel()        -- Create an unnamed thread-safe channel for inter-thread communication
luna.thread.newThread( code )   -- Create a new background thread from a Lua code string
```

### `ThreadHandle`

```lua
ThreadHandle:getError() -> string    -- Returns the error message if the thread terminated with a Lua error, or `nil` if it completed normally
ThreadHandle:isRunning() -> boolean  -- Returns `true` if the thread is currently executing
ThreadHandle:start( args )           -- Launches the background thread, passing optional arguments to the Lua script via `...`
ThreadHandle:wait()                  -- Blocks the calling coroutine until this thread finishes execution
```

---

## `luna.ai` {#ai}

> Registers the `luna.ai.*` game AI toolkit API.
>
> Provides factory functions and UserData wrappers for AI subsystems:
> AIWorld, Agent, Blackboard, StateMachine, BehaviorTree, SteeringManager,
> PathGrid, FlowField, QLearner, UtilityAI, GOAPPlanner, InfluenceMap,
> Squad, and CommandQueue.

```lua
luna.ai.newAction( callback )
luna.ai.newBehaviorTree()
luna.ai.newBlackboard()
luna.ai.newCommandQueue()
luna.ai.newCondition( callback )
luna.ai.newFlowField( grid_ud )
luna.ai.newGOAPPlanner()
luna.ai.newInfluenceMap( w, h, cs )
luna.ai.newInverter()
luna.ai.newParallel( [sp], [fp] )
luna.ai.newPathGrid( w, h, cs )
luna.ai.newQLearner( sc, ac )
luna.ai.newRepeater( [count] )
luna.ai.newSelector()
luna.ai.newSequence()
luna.ai.newSquad( name )
luna.ai.newStateMachine()
luna.ai.newSteeringManager()
luna.ai.newSucceeder()
luna.ai.newUtilityAI()
luna.ai.newWorld()
```

### `AIWorld`

```lua
AIWorld:addAgent( name : string ) -> Agent   -- Registers a new agent named `name` in this world and returns its handle
AIWorld:getAgent( name : string ) -> Agent   -- Looks up a registered agent by name
AIWorld:getAgentCount() -> integer           -- Returns the number of agents currently registered in this world
AIWorld:getGlobalBlackboard() -> Blackboard  -- Returns a snapshot of the shared world-level blackboard
AIWorld:removeAgent( agent : Agent )         -- Removes and destroys the given agent from this world
AIWorld:update( dt : number )                -- Advances all agents in the world by `dt` seconds, integrating velocity into position
```

### `Agent`

```lua
Agent:addTag( tag : string )                 -- Adds a string tag to this agent's tag set (idempotent)
Agent:getBlackboard() -> Blackboard          -- Returns this agent's private blackboard for reading or writing typed data
Agent:getDecisionModel() -> string           -- Returns the name of the agent's current decision model
Agent:getMaxForce() -> number                -- Returns the maximum steering force cap
Agent:getMaxSpeed() -> number                -- Returns the maximum movement speed cap in world units/second
Agent:getName() -> string                    -- Returns the unique name this agent was registered under
Agent:getPosition()                          -- Returns the agent's current world-space position
Agent:getPriority() -> integer               -- Returns the agent's scheduling priority level
Agent:getVelocity()                          -- Returns the agent's current velocity vector
Agent:hasTag( tag : string ) -> boolean      -- Returns `true` if this agent's tag set contains `tag`
Agent:removeTag( tag : string )              -- Removes a string tag from this agent's tag set (no-op if absent)
Agent:setDecisionModel( model : string )     -- Switches the agent's active decision model at runtime. Valid values: `"fsm"`, `"bt"`, `"utility"`, `"goap"`
Agent:setMaxForce( v : number )              -- Sets the maximum steering force that can be applied per frame
Agent:setMaxSpeed( v : number )              -- Sets the maximum movement speed cap in world units/second
Agent:setPosition( x : number, y : number )  -- Teleports the agent to world-space coordinates (`x`, `y`)
Agent:setPriority( p : integer )             -- Sets the scheduling priority; higher-priority agents are processed first during `update`
Agent:setVelocity( x : number, y : number )  -- Sets the agent's velocity vector in world units per second
```

### `BTNode`

```lua
BTNode:addChild( child_ud : userdata )      -- Adds child to the collection
BTNode:getChildCount()                      -- Returns the child count
BTNode:getCount()                           -- Returns the count
BTNode:getNodeType()                        -- Returns the node type
BTNode:reset()                              -- Resets state to initial values
BTNode:setChild( child_ud : userdata )      -- Sets the child
BTNode:setCount( n : integer )              -- Sets the count
BTNode:setFailurePolicy( policy : string )  -- Sets the failure policy
BTNode:setSuccessPolicy( policy : string )  -- Sets the success policy
```

### `BehaviorTree`

```lua
BehaviorTree:getLastStatus()     -- Returns the last status
BehaviorTree:setRoot( node_ud )  -- Sets the top-level root node of this behavior tree
```

### `Blackboard`

```lua
Blackboard:clear()                                    -- Removes all entries from this blackboard
Blackboard:getKeys()                                  -- Returns the keys
Blackboard:getSize()                                  -- Returns the size
Blackboard:has( key : string ) -> boolean             -- Returns `true` if a value is stored under `key` in this blackboard
Blackboard:remove( key : string )                     -- Deletes the entry at `key` from this blackboard (no-op if absent)
Blackboard:setBool( key : string, value : boolean )   -- Stores a boolean value under `key` on this blackboard
Blackboard:setNumber( key : string, value : number )  -- Stores a floating-point value under `key` on this blackboard
Blackboard:setString( key : string, value : string )  -- Stores a string value under `key` on this blackboard
```

### `CommandQueue`

```lua
CommandQueue:cancelCurrent()       -- Cancel current on this CommandQueue
CommandQueue:clear()               -- Discards all queued commands
CommandQueue:getCount()            -- Returns the count
CommandQueue:getCurrentTarget()    -- Returns the current target
CommandQueue:getCurrentType()      -- Returns the current type
CommandQueue:isEmpty() -> boolean  -- Returns `true` if there are no commands queued
```

### `FlowField`

```lua
FlowField:getDirection( x : integer, y : integer )  -- Returns the direction
FlowField:getDistance( x : integer, y : integer )   -- Returns the distance
FlowField:getGoal()                                 -- Returns the goal
FlowField:getHeight()                               -- Returns the height
FlowField:getWidth()                                -- Returns the width
FlowField:hasGoal() -> boolean                      -- Returns `true` if goal
FlowField:setGoal( x : integer, y : integer )       -- Sets the goal
```

### `GOAPPlanner`

```lua
GOAPPlanner:getActionCount()  -- Returns the action count
GOAPPlanner:getGoalCount()    -- Returns the goal count
```

### `InfluenceMap`

```lua
InfluenceMap:addLayer( name : string )                 -- Adds a named influence layer to this map
InfluenceMap:clearAll()                                -- Clear all on this InfluenceMap
InfluenceMap:clearLayer( layer : string )              -- Clear layer on this InfluenceMap
InfluenceMap:decay( layer : string, factor : number )  -- Decay on this InfluenceMap
InfluenceMap:getCellSize()                             -- Returns the cell size
InfluenceMap:getHeight()                               -- Returns the height
InfluenceMap:getMaxPosition( layer : string )          -- Returns the max position
InfluenceMap:getMinPosition( layer : string )          -- Returns the min position
InfluenceMap:getWidth()                                -- Returns the width
InfluenceMap:hasLayer( name : string ) -> boolean      -- Returns `true` if a layer with `name` exists in this map
```

### `PathGrid`

```lua
PathGrid:getCellSize()                                       -- Returns the cell size
PathGrid:getCost( x : integer, y : integer )                 -- Returns the cost
PathGrid:getHeight()                                         -- Returns the height
PathGrid:getWidth()                                          -- Returns the width
PathGrid:isWalkable( x : integer, y : integer ) -> boolean   -- Returns `true` if walkable
PathGrid:setCost( x : integer, y : integer, cost : number )  -- Sets the cost
```

### `QLearner`

```lua
QLearner:bestAction( state : string ) -> string          -- Returns the action with the highest Q-value for `state`
QLearner:chooseAction( state : integer )                 -- Choose action on this QLearner
QLearner:deserialize( json : string )                    -- Populates this object from a serialized string
QLearner:endEpisode()                                    -- End episode on this QLearner
QLearner:getActionCount()                                -- Returns the action count
QLearner:getBestAction( state : integer )                -- Returns the best action
QLearner:getDiscountFactor()                             -- Returns the discount factor
QLearner:getEpisodeCount()                               -- Returns the episode count
QLearner:getExplorationDecay()                           -- Returns the exploration decay
QLearner:getExplorationRate()                            -- Returns the exploration rate
QLearner:getLearningRate()                               -- Returns the learning rate
QLearner:getQValue( state : integer, action : integer )  -- Returns the q value
QLearner:getStateCount()                                 -- Returns the state count
QLearner:serialize()                                     -- Serializes this object to a string representation
QLearner:setDiscountFactor( v : number )                 -- Sets the discount factor
QLearner:setExplorationDecay( v : number )               -- Sets the exploration decay
QLearner:setExplorationRate( v : number )                -- Sets the exploration rate
QLearner:setLearningRate( v : number )                   -- Sets the learning rate
```

### `Squad`

```lua
Squad:addMember( name : string )     -- Adds an agent identified by `name` to this squad
Squad:getBlackboard()                -- Returns the blackboard
Squad:getFormation()                 -- Returns the formation
Squad:getFormationSpacing()          -- Returns the formation spacing
Squad:getLeader()                    -- Returns the leader
Squad:getMemberCount() -> integer    -- Returns the number of agents currently in this squad
Squad:getMembers()                   -- Returns the members
Squad:getName()                      -- Returns the name
Squad:removeMember( name : string )  -- Removes the agent identified by `name` from this squad
Squad:setLeader( name : string )     -- Sets the leader
```

### `StateMachine`

```lua
StateMachine:addState( name : string, opts )   -- Registers a new named state in this FSM
StateMachine:forceState( name : string )       -- Force state on this StateMachine
StateMachine:getCurrentState()                 -- Returns the current state
StateMachine:getTimeInState()                  -- Returns the time in state
StateMachine:setInitialState( name : string )  -- Sets the initial state
```

### `SteeringManager`

```lua
SteeringManager:getBehaviorCount()               -- Returns the behavior count
SteeringManager:getCombineMode()                 -- Returns the combine mode
SteeringManager:getLastSteering()                -- Returns the last steering
SteeringManager:setCombineMode( mode : string )  -- Sets the combine mode
```

### `UtilityAI`

```lua
UtilityAI:evaluate()        -- Evaluates all conditions and returns a decision
UtilityAI:getActionCount()  -- Returns the action count
UtilityAI:getLastAction()   -- Returns the last action
```

---

## `luna.compute` {#compute}

> Registers the `luna.compute.*` array computation API.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for compute api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.compute.fromTable( tbl, [shape], [dtype] )
luna.compute.newArray( shape, [dtype] )
luna.compute.ones( shape, [dtype] )
luna.compute.range( start, stop, [step], [dtype] )
luna.compute.zeros( shape, [dtype] )
```

### `mlua`

```lua
mlua:abs()                                -- Abs on this Object
mlua:all()                                -- All on this Object
mlua:any()                                -- Any on this Object
mlua:argmax()                             -- Argmax on this Object
mlua:argmin()                             -- Argmin on this Object
mlua:bitwiseAnd( other : userdata )       -- Bitwise and on this Object
mlua:bitwiseLShift( amount : integer )    -- Bitwise l shift on this Object
mlua:bitwiseNot()                         -- Bitwise not on this Object
mlua:bitwiseOr( other : userdata )        -- Bitwise or on this Object
mlua:bitwiseRShift( amount : integer )    -- Bitwise r shift on this Object
mlua:bitwiseXor( other : userdata )       -- Bitwise xor on this Object
mlua:clamp( min : number, max : number )  -- Clamps the value within the allowed range
mlua:clone()                              -- Returns a deep copy of this object
mlua:convolve2D( kernel : userdata )      -- Convolve2 d on this Object
mlua:countNonZero() -> integer            -- Returns the number of non zero
mlua:dilate( radius : integer )           -- Dilate on this Object
mlua:dot( other : userdata )              -- Dot on this Object
mlua:erode( radius )                      -- Erode on this Object
mlua:fill( val : number )                 -- Fill on this Object
mlua:get( args : LuaMultiValue )          -- Returns the current value
mlua:getDataType()                        -- Returns the data type
mlua:getDimensions()                      -- Returns the dimensions
mlua:getShape()                           -- Returns the shape
mlua:getSize()                            -- Returns the size
mlua:isOnGPU() -> boolean                 -- Returns `true` if on g p u
mlua:matmul( other : userdata )           -- Matmul on this Object
mlua:max( [axis : integer] )              -- Max on this Object
mlua:mean( [axis : integer] )             -- Mean on this Object
mlua:min( [axis : integer] )              -- Min on this Object
mlua:neg()                                -- Neg on this Object
mlua:pow( exp : number )                  -- Pow on this Object
mlua:reshape( shape : any )               -- Reshape on this Object
mlua:set( args : LuaMultiValue )          -- Sets the value
mlua:sqrt()                               -- Sqrt on this Object
mlua:sum( [axis : integer] )              -- Sum on this Object
mlua:threshold( val )                     -- Threshold on this Object
mlua:toTable()                            -- To table on this Object
mlua:transpose()                          -- Transpose on this Object
```

---

## `luna.dataframe` {#dataframe}

> Registers the `luna.dataframe.*` tabular data API.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for dataframe api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.dataframe.fromBinary( s )
luna.dataframe.fromCSV( s )
luna.dataframe.fromJSON( s )
luna.dataframe.fromTable( tbl )
luna.dataframe.newDataFrame()
luna.dataframe.newDatabase()
luna.dataframe.random( defs_tbl, n, [seed] )
```

### `DataFrame`

```lua
DataFrame:addRow( [row_tbl : table] )              -- Adds row to the collection
DataFrame:clone()                                  -- Returns a deep copy of this object
DataFrame:columns()                                -- Columns on this DataFrame
DataFrame:count() -> integer                       -- Returns the number of items
DataFrame:countBy( col : any ) -> integer          -- Returns the number of by
DataFrame:describe()                               -- Describe on this DataFrame
DataFrame:dropNil( col )                           -- Drop nil on this DataFrame
DataFrame:fillNil( col : any, val : any )          -- Fill nil on this DataFrame
DataFrame:getColumn( col : any )                   -- Returns the column
DataFrame:getRow( row : integer )                  -- Returns the row
DataFrame:getValue( row : integer, col : any )     -- Returns the value
DataFrame:groupBy( col : any )                     -- Group by on this DataFrame
DataFrame:head( [n : integer] )                    -- Head on this DataFrame
DataFrame:max( col : any )                         -- Max on this DataFrame
DataFrame:mean( col : any )                        -- Mean on this DataFrame
DataFrame:median( col : any )                      -- Median on this DataFrame
DataFrame:merge( other : userdata )                -- Merge on this DataFrame
DataFrame:min( col : any )                         -- Min on this DataFrame
DataFrame:ncols()                                  -- Ncols on this DataFrame
DataFrame:nrows()                                  -- Nrows on this DataFrame
DataFrame:query( sql_str : string )                -- Runs a query and returns matching results
DataFrame:removeColumn( col : any )                -- Removes column from the collection
DataFrame:removeRow( row : integer )               -- Removes row from the collection
DataFrame:rename( col : any, new_name : string )   -- Rename on this DataFrame
DataFrame:sample( n : integer, [seed : integer] )  -- Sample on this DataFrame
DataFrame:select( cols : LuaMultiValue )           -- Returns filtered rows or items matching the query
DataFrame:slice( start : integer, end : integer )  -- Slice on this DataFrame
DataFrame:stddev( col : any )                      -- Stddev on this DataFrame
DataFrame:sum( col : any )                         -- Sum on this DataFrame
DataFrame:tail( [n] )                              -- Tail on this DataFrame
DataFrame:toBinary()                               -- To binary on this DataFrame
DataFrame:toCSV()                                  -- To c s v on this DataFrame
DataFrame:toJSON()                                 -- To j s o n on this DataFrame
DataFrame:toString()                               -- To string on this DataFrame
DataFrame:toTable()                                -- To table on this DataFrame
DataFrame:unique( col : any )                      -- Unique on this DataFrame
DataFrame:variance( col : any )                    -- Variance on this DataFrame
```

### `Database`

```lua
Database:clear()                               -- Removes all entries
Database:getTable( name : string )             -- Returns the table
Database:hasTable( name : string ) -> boolean  -- Returns `true` if table
Database:listTables()                          -- List tables on this Database
Database:merge( other : userdata )             -- Merge on this Database
Database:query( sql_str : string )             -- Runs a query and returns matching results
Database:removeTable( name : string )          -- Removes table from the collection
Database:tableCount()                          -- Table count on this Database
Database:toJSON()                              -- To j s o n on this Database
```

---

## `luna.data` {#data}

> Registers the `luna.data.*` binary data, compression, hashing, and encoding API.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for data api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.data.compress( format_str, raw_data, [level] )  -- Compresses data using the given algorithm ('deflate', 'gzip', 'lz4')
luna.data.decode( format_str, encoded )              -- Decodes binary data from a buffer according to a pack-format string
luna.data.decompress( format_str, compressed_data )  -- Decompresses data using the given algorithm
luna.data.encode( format_str, raw_data )             -- Encodes Lua values into a binary string using a pack-format string
luna.data.encodeToml( table )                        -- Encodes a Lua table as a TOML string
luna.data.hash( algo_str, raw_data )                 -- Returns the cryptographic hash (md5, sha1, sha256) of the input
luna.data.newByteData( value )                       -- Creates a new mutable byte buffer of the given size
luna.data.parseToml( input )                         -- Parses a TOML string and returns a Lua table
```

---

## `luna.image` {#image}

> Registers the `luna.image.*` pixel-level image manipulation API.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for image api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.image.newImageData( args )  -- Creates a new blank RGBA8 ImageData buffer of the given size
```

---

## `luna.sound` {#sound}

> Registers the `luna.sound.*` decoded audio sample manipulation API.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for sound api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.sound.clearMidiSoundFont()             -- Unloads the active SoundFont so the MIDI player falls back to built-in defaults
luna.sound.hasMidiSoundFont()               -- Returns whether a SoundFont is currently loaded for the MIDI synthesizer
luna.sound.newSoundData( args )             -- Creates a raw PCM audio buffer for building procedurally generated sound
luna.sound.setMidiSoundFont( path : File )  -- Sets the SoundFont (.sf2) file that the MIDI synthesizer uses for instrument samples
```

---

## `luna.graph` {#graph}

> Registers the `luna.graph.*` directed-graph and item-flow simulation API.
>
> Exposes `LuaGraph`, `LuaNode`, `LuaEdge`, and `LuaGraphItem` UserData types
> wrapping `crate::graph::Graph` with callback dispatch for simulation events.

```lua
luna.graph.newGraph()  -- Creates a new empty directed graph for item flow simulation
```

### `Edge`

```lua
Edge:addAllowedType( t : string )                -- Adds allowed type to the collection
Edge:clearAllowedTypes()                         -- Clear allowed types on this Edge
Edge:getCapacity()                               -- Returns the capacity
Edge:getCooldown()                               -- Returns the cooldown
Edge:getFrom()                                   -- Returns the from
Edge:getItemsInTransit()                         -- Returns the items in transit
Edge:getSpeedModifier()                          -- Returns the speed modifier
Edge:getThroughput()                             -- Returns the throughput
Edge:getTo()                                     -- Returns the to
Edge:getTravelTime()                             -- Returns the travel time
Edge:getType()                                   -- Returns the type
Edge:getWeight()                                 -- Returns the weight
Edge:isActive() -> boolean                       -- Returns `true` if active
Edge:isBidirectional() -> boolean                -- Returns `true` if bidirectional
Edge:isItemTypeAllowed( t : string ) -> boolean  -- Returns `true` if item type allowed
Edge:isOnCooldown() -> boolean                   -- Returns `true` if on cooldown
Edge:removeAllowedType( t : string )             -- Removes allowed type from the collection
Edge:setActive( a : boolean )                    -- Sets the active
Edge:setBidirectional( b : boolean )             -- Sets the bidirectional
Edge:setCapacity( c : integer )                  -- Sets the capacity
Edge:setCooldown( c : number )                   -- Sets the cooldown
Edge:setSpeedModifier( m : number )              -- Sets the speed modifier
Edge:setThroughput( t : number )                 -- Sets the throughput
Edge:setTravelTime( t : number )                 -- Sets the travel time
Edge:setType( t : string )                       -- Sets the type
Edge:setWeight( w : number )                     -- Sets the weight
```

### `Graph`

```lua
Graph:addEdge( from_ud : userdata, to_ud : userdata, [edge_type : string] )        -- Adds edge to the collection
Graph:findPathForItem( item_ud : userdata, from_ud : userdata, to_ud : userdata )  -- Find path for item on this Graph
Graph:getComponents()                                                              -- Returns the components
Graph:getEdgeCount()                                                               -- Returns the edge count
Graph:getEdges()                                                                   -- Returns the edges
Graph:getItemCount()                                                               -- Returns the item count
Graph:getItems()                                                                   -- Returns the items
Graph:getNeighbors( node_ud : userdata )                                           -- Returns the neighbors
Graph:getNodeCount()                                                               -- Returns the node count
Graph:getNodes()                                                                   -- Returns the nodes
Graph:getStats()                                                                   -- Returns the stats
Graph:hasCycle() -> boolean                                                        -- Returns `true` if cycle
Graph:hasEdge( edge_ud : userdata ) -> boolean                                     -- Returns `true` if edge
Graph:hasItem( item_ud : userdata ) -> boolean                                     -- Returns `true` if item
Graph:hasNode( node_ud : userdata ) -> boolean                                     -- Returns `true` if node
Graph:processDemand()                                                              -- Process demand on this Graph
Graph:removeEdge( edge_ud : userdata )                                             -- Removes edge from the collection
Graph:removeItem( item_ud : userdata )                                             -- Removes item from the collection
Graph:removeNode( node_ud : userdata )                                             -- Removes node from the collection
Graph:step()                                                                       -- Advances the simulation by one physics step
Graph:topologicalSort()                                                            -- Topological sort on this Graph
Graph:update( dt : number )                                                        -- Advances the simulation by `dt` seconds
```

### `GraphItem`

```lua
GraphItem:getDecayTime()              -- Returns the decay time
GraphItem:getPosition()               -- Returns the position
GraphItem:getPriority()               -- Returns the priority
GraphItem:getRemainingLife()          -- Returns the remaining life
GraphItem:getType()                   -- Returns the type
GraphItem:isAlive() -> boolean        -- Returns `true` if alive
GraphItem:kill()                      -- Kill on this GraphItem
GraphItem:setDecayTime( t : number )  -- Sets the decay time
GraphItem:setPriority( p : integer )  -- Sets the priority
GraphItem:setType( t : string )       -- Sets the type
```

### `Node`

```lua
Node:addTag( tag : string )               -- Adds tag to the collection
Node:clearAllConversions()                -- Clear all conversions on this Node
Node:clearConversion( in_type : string )  -- Clear conversion on this Node
Node:clearDemands()                       -- Clear demands on this Node
Node:clearSupplies()                      -- Clear supplies on this Node
Node:clearTags()                          -- Clear tags on this Node
Node:dequeue()                            -- Dequeue on this Node
Node:enqueue( item_ud : userdata )        -- Enqueue on this Node
Node:getCapacity()                        -- Returns the capacity
Node:getEdges( [dir : string] )           -- Returns the edges
Node:getFlowMode()                        -- Returns the flow mode
Node:getItemCount()                       -- Returns the item count
Node:getItems()                           -- Returns the items
Node:getOverflowPolicy()                  -- Returns the overflow policy
Node:getProcessTime()                     -- Returns the process time
Node:getPullFilter()                      -- Returns the pull filter
Node:getPullRate()                        -- Returns the pull rate
Node:getPushFilter()                      -- Returns the push filter
Node:getPushRate()                        -- Returns the push rate
Node:getQueueCapacity()                   -- Returns the queue capacity
Node:getQueueSize()                       -- Returns the queue size
Node:getTags()                            -- Returns the tags
Node:getType()                            -- Returns the type
Node:hasTag( tag : string ) -> boolean    -- Returns `true` if tag
Node:isActive() -> boolean                -- Returns `true` if active
Node:isFull() -> boolean                  -- Returns `true` if full
Node:isQueueEnabled() -> boolean          -- Returns `true` if queue enabled
Node:removeDemand( item_type : string )   -- Removes demand from the collection
Node:removeSupply( item_type : string )   -- Removes supply from the collection
Node:removeTag( tag : string )            -- Removes tag from the collection
Node:setActive( a : boolean )             -- Sets the active
Node:setCapacity( c : integer )           -- Sets the capacity
Node:setFlowMode( m : string )            -- Sets the flow mode
Node:setOverflowPolicy( p : string )      -- Sets the overflow policy
Node:setProcessTime( t : number )         -- Sets the process time
Node:setPullFilter( [f : string] )        -- Sets the pull filter
Node:setPullRate( r : number )            -- Sets the pull rate
Node:setPushFilter( [f : string] )        -- Sets the push filter
Node:setPushRate( r : number )            -- Sets the push rate
Node:setQueueCapacity( c : integer )      -- Sets the queue capacity
Node:setQueueEnabled( e : boolean )       -- Sets the queue enabled
Node:setType( t : string )                -- Sets the type
```

---

## `luna.dialog` {#dialog}

> Lua API bindings for the `luna.dialog.*` dialog sequencer module.
>
> Provides a `Sequencer` UserData type for visual novel-style text
> presentation with typewriter effect, branching choices, timed pauses,
> and inline callbacks.

```lua
luna.dialog.newSequencer()
```

### `Sequencer`

```lua
Sequencer:advance()                              -- Advances to the next item
Sequencer:choose( index : integer )              -- Choose on this Sequencer
Sequencer:currentSpeaker()                       -- Current speaker on this Sequencer
Sequencer:currentText()                          -- Current text on this Sequencer
Sequencer:getChoiceLabels()                      -- Returns the choice labels
Sequencer:getChoiceText()                        -- Returns the choice text
Sequencer:getSpeed()                             -- Returns the speed
Sequencer:getState()                             -- Returns the state
Sequencer:isActive() -> boolean                  -- Returns `true` if active
Sequencer:isWaitingForChoice() -> boolean        -- Returns `true` if waiting for choice
Sequencer:load( script : table )                 -- Loads state from persistent storage
Sequencer:off( event : string )                  -- Removes a previously registered event listener
Sequencer:on( event : string, func : function )  -- Registers an event listener callback
Sequencer:revealedText()                         -- Revealed text on this Sequencer
Sequencer:setSpeed( cps : number )               -- Sets the speed
Sequencer:skip()                                 -- Skip on this Sequencer
Sequencer:start()                                -- Begins execution
Sequencer:update( dt : number )                  -- Advances the simulation by `dt` seconds
```

---

## `luna.entity` {#entity}

> Registers the `luna.entity.*` ECS universe API.
>
> Exposes `Universe` as Lua UserData (`LuaUniverse`) with entity lifecycle,
> component operations, querying, systems, tags, layers, and blueprints.

```lua
luna.entity.newUniverse()
```

### `Universe`

```lua
Universe:addSystem( system : table )                             -- Adds system to the collection
Universe:addTag( id : integer, tag : string )                    -- Attaches a string tag to the entity, enabling fast tag-based group queries
Universe:bitmapTag( id : integer, name : string )                -- Bitmap tag on this Universe
Universe:bitmapUntag( id : integer, name : string )              -- Bitmap untag on this Universe
Universe:clear()                                                 -- Removes all entries
Universe:defineTag( name : string )                              -- Define tag on this Universe
Universe:draw()                                                  -- Draws to the current render target
Universe:emit( args : LuaMultiValue )                            -- Emits an event
Universe:get( id : integer, name : string )                      -- Returns the current value
Universe:getBitmapTagBit( name : string )                        -- Returns the bitmap tag bit
Universe:getBlueprintComponents( name : string )                 -- Returns the blueprint components
Universe:getComponents( id : integer )                           -- Returns the components
Universe:getEntities()                                           -- Returns the entities
Universe:getEntitiesByLayer( layer : integer )                   -- Returns the entities by layer
Universe:getEntitiesByTag( tag )                                 -- Returns the entities by tag
Universe:getEntitiesSorted()                                     -- Returns the entities sorted
Universe:getEntityCount()                                        -- Returns the entity count
Universe:getLayer( id : integer )                                -- Returns the layer
Universe:getSystemCount()                                        -- Returns the system count
Universe:getTags( id : integer )                                 -- Returns the tags
Universe:has( id : integer, name : string ) -> boolean           -- Returns `true` if the condition is met
Universe:hasBitmapTag( id : integer, name : string ) -> boolean  -- Returns `true` if bitmap tag
Universe:hasBlueprint( name : string ) -> boolean                -- Returns `true` if blueprint
Universe:hasTag( id : integer, tag : string ) -> boolean         -- Returns `true` if the entity carries the given tag
Universe:isAlive( id : integer ) -> boolean                      -- Returns `true` if the entity `id` is currently active in the universe
Universe:kill( id : integer )                                    -- Destroys the entity with the given `id`, freeing its slot for reuse
Universe:listBlueprints()                                        -- List blueprints on this Universe
Universe:query( args : LuaMultiValue )                           -- Runs a query and returns matching results
Universe:queryBitmapAll( names : table )                         -- Query bitmap all on this Universe
Universe:queryBitmapAny( names : table )                         -- Query bitmap any on this Universe
Universe:queryBitmapTag( name : string )                         -- Query bitmap tag on this Universe
Universe:release()                                               -- Releases the underlying resource handle
Universe:remove( id : integer, name : string )                   -- Removes the entry from the collection
Universe:removeBlueprint( name : string )                        -- Removes blueprint from the collection
Universe:removeSystem( system : table )                          -- Removes system from the collection
Universe:removeTag( id : integer, tag : string )                 -- Removes a string tag from the entity
Universe:setLayer( id : integer, layer : integer )               -- Sets the layer
Universe:spawn() -> integer                                      -- Creates a new entity in this universe and returns its numeric ID
Universe:update( dt : number )                                   -- Advances the simulation by `dt` seconds
```

---

## `luna.scene` {#scene}

> Registers the `luna.scene.*` scene stack, registry, data store, and depth-sorter API.
>
> The scene stack manages a LIFO stack of Lua scene tables with optional
> visual transitions. Scene tables are stored via `mlua::RegistryKey` and
> identified by `SceneId`. Inter-scene data is stored similarly.

```lua
luna.scene.clear()
luna.scene.draw()
luna.scene.getCurrent()
luna.scene.getData( key )
luna.scene.getRegistered( name )
luna.scene.getRegisteredNames()
luna.scene.getStackSize()
luna.scene.getTransitionProgress()
luna.scene.hasData( key )
luna.scene.hasRegistered( name )
luna.scene.isEmpty()
luna.scene.isTransitioning()
luna.scene.newDepthSorter()
luna.scene.pop( [transition], [duration] )
luna.scene.popTo( name )
luna.scene.push( scene, [transition], [duration] )
luna.scene.registerScene( name, scene )
luna.scene.removeData( key )
luna.scene.setData( key, value )
luna.scene.switchTo( scene, [transition], [duration] )
luna.scene.unregisterScene( name )
luna.scene.update( dt )
```

### `DepthSorter`

```lua
DepthSorter:add( callback : function, depth : number )  -- Registers a draw callback at the given depth layer. Higher `depth` values draw in front
DepthSorter:addObject( obj : table )                    -- Registers a table object with a `draw` method at the given depth
DepthSorter:clear()                                     -- Removes all registered callbacks and objects without calling them
DepthSorter:flush()                                     -- Calls all registered draw callbacks and object `draw()` methods in sorted depth order, then clears the list
DepthSorter:getCount() -> integer                       -- Returns the number of callbacks and objects currently registered
DepthSorter:sort()                                      -- Sorts all registered callbacks and objects by their depth values (ascending)
```

---

## `luna.pathfinding` {#pathfinding}

> Registers the `luna.pathfinding.*` grid-based pathfinding API.
>
> Exposes `NavGrid`, `UnitPathfinder`, and `FlowField` as Lua UserData
> with 1-based tile coordinates at the Lua boundary.

```lua
luna.pathfinding.getThreadCount()
luna.pathfinding.newFlowField( grid_ud )
luna.pathfinding.newNavGrid( width, height )
luna.pathfinding.newNavGridFromTileMap( tilemap_ud, layer, blocked )
luna.pathfinding.newPathfinder( grid_ud )
luna.pathfinding.setThreadCount( count )
```

### `FlowField`

```lua
FlowField:getCostToTarget( x : integer, y : integer )    -- Returns the cost to target
FlowField:getDirection( x : integer, y : integer )       -- Returns the direction
FlowField:getDirectionAngle( x : integer, y : integer )  -- Returns the direction angle
FlowField:getTargets()                                   -- Returns the targets
FlowField:isCalculated() -> boolean                      -- Returns `true` if calculated
```

### `NavGrid`

```lua
NavGrid:clearDirty()                                                    -- Clear dirty on this NavGrid
NavGrid:fill( cost : integer )                                          -- Fill on this NavGrid
NavGrid:getChunkSize()                                                  -- Returns the chunk size
NavGrid:getCost( x : integer, y : integer )                             -- Returns the cost
NavGrid:getDiagonalMode()                                               -- Returns the diagonal mode
NavGrid:getDimensions()                                                 -- Returns the dimensions
NavGrid:getHeight()                                                     -- Returns the height
NavGrid:getWidth()                                                      -- Returns the width
NavGrid:isBlocked( x : integer, y : integer ) -> boolean                -- Returns `true` if blocked
NavGrid:loadFromString( data : string )                                 -- Load from string on this NavGrid
NavGrid:rebuildAbstract()                                               -- Rebuild abstract on this NavGrid
NavGrid:saveToString()                                                  -- Save to string on this NavGrid
NavGrid:setChunkSize( size : integer )                                  -- Sets the chunk size
NavGrid:setCost( x : integer, y : integer, cost : integer )             -- Sets the cost
NavGrid:setDiagonalMode( mode : string )                                -- Sets the diagonal mode
NavGrid:setDirty( x : integer, y : integer, w : integer, h : integer )  -- Sets the dirty
```

### `UnitPathfinder`

```lua
UnitPathfinder:clearCache()                          -- Clear cache on this UnitPathfinder
UnitPathfinder:getCacheSize()                        -- Returns the cache size
UnitPathfinder:getPathCost( path : table )           -- Returns the path cost
UnitPathfinder:getPathLength( path : table )         -- Returns the path length
UnitPathfinder:isCacheEnabled() -> boolean           -- Returns `true` if cache enabled
UnitPathfinder:setCacheEnabled( enabled : boolean )  -- Sets the cache enabled
UnitPathfinder:setCacheMaxSize( n : integer )        -- Sets the cache max size
```

---

## `luna.postfx` {#postfx}

> Lua API bindings for the `luna.postfx.*` post-processing effects module.
>
> Provides `PostFxEffect` and `PostFxStack` UserData types with factory
> functions for creating built-in effects, custom shader passes, and
> effect stacks that manage ping-pong canvas post-processing pipelines.

```lua
luna.postfx.getEffectTypes()
luna.postfx.newEffect( name )
luna.postfx.newPass( shader_id )
luna.postfx.newStack( [w], [h] )
```

### `PostFxEffect`

```lua
PostFxEffect:getEffectType()                                -- Returns the effect type
PostFxEffect:getParameterNames()                            -- Returns the parameter names
PostFxEffect:hasParameter( name : string ) -> boolean       -- Returns `true` if parameter
PostFxEffect:isBuiltIn() -> boolean                         -- Returns `true` if built in
PostFxEffect:isEnabled() -> boolean                         -- Returns `true` if enabled
PostFxEffect:setBrightness( value : number )                -- Sets the brightness
PostFxEffect:setContrast( value : number )                  -- Sets the contrast
PostFxEffect:setEnabled( enabled : boolean )                -- Sets the enabled
PostFxEffect:setIntensity( value : number )                 -- Sets the intensity
PostFxEffect:setOffset( value : number )                    -- Sets the offset
PostFxEffect:setParameter( name : string, value : number )  -- Sets the parameter
PostFxEffect:setRadius( value : number )                    -- Sets the radius
PostFxEffect:setSaturation( value : number )                -- Sets the saturation
PostFxEffect:setScanlineStrength( value : number )          -- Sets the scanline strength
PostFxEffect:setStrength( value : number )                  -- Sets the strength
PostFxEffect:setThreshold( value : number )                 -- Sets the threshold
```

### `PostFxStack`

```lua
PostFxStack:add( effect : userdata )                         -- Adds an entry to the collection
PostFxStack:getDimensions()                                  -- Returns the dimensions
PostFxStack:getEffect( index : integer )                     -- Returns the effect
PostFxStack:getEffectCount()                                 -- Returns the effect count
PostFxStack:getEnabledEffects()                              -- Returns the enabled effects
PostFxStack:getHeight()                                      -- Returns the height
PostFxStack:getWidth()                                       -- Returns the width
PostFxStack:isCapturing() -> boolean                         -- Returns `true` if capturing
PostFxStack:isEffectEnabled( effect : userdata ) -> boolean  -- Returns `true` if effect enabled
PostFxStack:remove( effect : userdata )                      -- Removes the entry from the collection
PostFxStack:resize( w : integer, h : integer )               -- Resize on this PostFxStack
```

---

## `luna.minimap` {#minimap}

> Lua API bindings for the `luna.minimap.*` minimap module.
>
> Provides a `Minimap` UserData type with factory functions for creating
> grid-based minimaps with terrain coloring, fog of war, tracked objects,
> pings, markers, and viewport rectangle overlay.

```lua
luna.minimap.newMinimap( grid_w, grid_h, [display_w], [display_h] )
```

### `Minimap`

```lua
Minimap:addPing()                                             -- Adds ping to the collection
Minimap:clearObjects()                                        -- Clear objects on this Minimap
Minimap:clearViewportRect()                                   -- Clear viewport rect on this Minimap
Minimap:getCenter()                                           -- Returns the center
Minimap:getColorMode()                                        -- Returns the color mode
Minimap:getDisplayHeight()                                    -- Returns the display height
Minimap:getDisplaySize()                                      -- Returns the display size
Minimap:getDisplayWidth()                                     -- Returns the display width
Minimap:getFogColor()                                         -- Returns the fog color
Minimap:getFogLevel( x : integer, y : integer )               -- Returns the fog level
Minimap:getGridHeight()                                       -- Returns the grid height
Minimap:getGridSize()                                         -- Returns the grid size
Minimap:getGridWidth()                                        -- Returns the grid width
Minimap:getMarkerCount()                                      -- Returns the marker count
Minimap:getMarkerDescription( id : integer )                  -- Returns the marker description
Minimap:getObjectCount()                                      -- Returns the object count
Minimap:getObjectTypeCount()                                  -- Returns the object type count
Minimap:getOwnerColor( owner : integer )                      -- Returns the owner color
Minimap:getPingCount()                                        -- Returns the ping count
Minimap:getTerrain( x : integer, y : integer )                -- Returns the terrain
Minimap:getTerrainColor( terrain_type : integer )             -- Returns the terrain color
Minimap:getViewportColor()                                    -- Returns the viewport color
Minimap:getViewportRect()                                     -- Returns the viewport rect
Minimap:getZoom()                                             -- Returns the zoom
Minimap:hasMarker( id : integer ) -> boolean                  -- Returns `true` if marker
Minimap:isAntiAlias() -> boolean                              -- Returns `true` if anti alias
Minimap:isFogEnabled() -> boolean                             -- Returns `true` if fog enabled
Minimap:isObjectTypeVisible( type_idx : integer ) -> boolean  -- Returns `true` if object type visible
Minimap:isViewportVisible() -> boolean                        -- Returns `true` if viewport visible
Minimap:removeMarker( id : integer )                          -- Removes marker from the collection
Minimap:removeObject( id : integer )                          -- Removes object from the collection
Minimap:setAntiAlias( enabled : boolean )                     -- Sets the anti alias
Minimap:setCenter( x : number, y : number )                   -- Sets the center
Minimap:setColorMode( mode : string )                         -- Sets the color mode
Minimap:setDisplaySize( w : integer, h : integer )            -- Sets the display size
Minimap:setFogData( data : table )                            -- Sets the fog data
Minimap:setFogEnabled( enabled : boolean )                    -- Sets the fog enabled
Minimap:setViewportVisible( visible : boolean )               -- Sets the viewport visible
Minimap:setZoom( zoom : number )                              -- Sets the zoom
Minimap:update( dt : number )                                 -- Advances the simulation by `dt` seconds
```

---

## `luna.savegame` {#savegame}

> Savegame Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for savegame api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.savegame.newSaveManager()
```

### `mlua`

```lua
mlua:collect() -> table                     -- Snapshots all registered tables into an in-memory serializable form, ready for disk write
mlua:disableAutoSave()                      -- Disables automatic periodic saving
mlua:getSchemaVersion() -> integer          -- Returns the schema version currently set on this save manager
mlua:getSummary() -> string                 -- Returns the summary string set by `setSummary`, or an empty string if none was set
mlua:isDirty() -> boolean                   -- Returns `true` if the save data has been modified since the last write
mlua:markDirty()                            -- Marks the save as dirty, ensuring it will be written on the next autosave tick
mlua:reset()                                -- Clears all registered tables back to their initial state and marks the save dirty
mlua:restore()                              -- Restores all registered tables from a previously collected save data table
mlua:setSchemaVersion( version : integer )  -- Sets the schema version stored in the save file. Increment when save format changes
mlua:setSummary( summary : string )         -- Sets a human-readable summary string stored alongside the save data (e.g. for save-slot UI)
mlua:unregister( name : string )            -- Removes a previously registered save slot by name
mlua:update( dt : number )                  -- Ticks the autosave timer. Must be called from `luna.update(dt)` when autosave is enabled
```

---

## `luna.modding` {#modding}

> Modding Api implementation for the `lua_api` subsystem.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for modding api-related operations and data management.
> Primary functions: `register()`.

```lua
luna.modding.newMod( info )
luna.modding.newModManager()
```

### `mlua`

```lua
mlua:clearLoadOrder()                      -- Clear load order on this ModManager
mlua:clearReloadQueue()                    -- Clear reload queue on this ModManager
mlua:getAllMods()                          -- Returns the all mods
mlua:getAuthor()                           -- Returns the author
mlua:getConfig()                           -- Returns the config
mlua:getDependencies()                     -- Returns the dependencies
mlua:getDescription()                      -- Returns the description
mlua:getHook( name : string )              -- Returns the hook
mlua:getHookNames()                        -- Returns the hook names
mlua:getId()                               -- Returns the id
mlua:getLoadOrder()                        -- Returns the load order
mlua:getModCount()                         -- Returns the mod count
mlua:getModPath( mod_id : string )         -- Returns the mod path
mlua:getName()                             -- Returns the name
mlua:getPriority()                         -- Returns the priority
mlua:getReloadQueue()                      -- Returns the reload queue
mlua:getVersion()                          -- Returns the version
mlua:hasCircularDependencies() -> boolean  -- Returns `true` if circular dependencies
mlua:hasHook( name : string ) -> boolean   -- Returns `true` if hook
mlua:hasMod( mod_id : string ) -> boolean  -- Returns `true` if mod
mlua:isEnabled() -> boolean                -- Returns `true` if enabled
mlua:isLoaded() -> boolean                 -- Returns `true` if loaded
mlua:markForReload( mod_id : string )      -- Mark for reload on this ModManager
mlua:registerMod( ud : userdata )          -- Adds mod to the collection
mlua:releaseRefs()                         -- Release refs on this Mod
mlua:scanFolder( path : string )           -- Scan folder on this ModManager
mlua:setConfig( value : any )              -- Sets the config
mlua:setEnabled( enabled : boolean )       -- Sets the enabled
mlua:setLoadOrder( order_table : table )   -- Sets the load order
mlua:unregisterMod( mod_id : string )      -- Removes mod from the collection
mlua:validateDependencies()                -- Validate dependencies on this ModManager
```

---

## `luna.localization` {#localization}

> Registers the `luna.localization.*` internationalization API.
>
> Provides a structured translation system supporting multiple languages,
> fallback chains, pluralization, variable interpolation, and language-change
> callbacks.

```lua
luna.localization.getAvailableLanguages()
luna.localization.getBase()
luna.localization.getLanguage()
luna.localization.hasLanguage( lang )
luna.localization.loadTable( lang, table )
luna.localization.offChange( callback )
luna.localization.onChange( callback )
luna.localization.setBase( lang )
luna.localization.setLanguage( lang )
luna.localization.t( key, [vars] )
```

---

## `luna.stats` {#stats}

> Lua API bindings for the `luna.stats` RPG character sheet system.
>
> Exposes [`Sheet`] and a global [`StatsRegistry`] as `luna.stats.*` Lua values.

```lua
luna.stats.applyArchetypes( sheet_ud, [race], [class] )
luna.stats.defineClass( name, opts )
luna.stats.defineRace( name, opts )
luna.stats.defineTrait( name, opts )
luna.stats.getClassNames()
luna.stats.getRaceNames()
luna.stats.getTraitNames()
luna.stats.newSheet()
```

### `Sheet`

```lua
Sheet:acquirePerk( name )                            -- `sheet:acquirePerk(name)` – acquire the perk if level requirement is met
Sheet:addBuff( stat, add, mul, duration, [source] )  -- `sheet:addBuff(stat, add, mul, duration, source?)` – returns integer handle
Sheet:addTrait( name )                               -- `sheet:addTrait(name)` – apply a globally-defined trait
Sheet:addXP( amount )                                -- `sheet:addXP(amount)` – add XP, auto-levels up. Returns levels gained
Sheet:adjustMorale( delta )                          -- `sheet:adjustMorale(delta)` – add delta to current morale (clamped 0..max)
Sheet:beginTurn()                                    -- `sheet:beginTurn()` – restore action points to max
Sheet:checkMorale()                                  -- `sheet:checkMorale()` – returns "panic", "berserk", or nil
Sheet:clearBuffs( [stat] )                           -- `sheet:clearBuffs(stat?)` – remove all buffs, or just those for the given stat
Sheet:clearFlag( name )                              -- `sheet:clearFlag(name)` – clear a flag
Sheet:get( name )                                    -- `sheet:get(name)` – get effective value (buffs applied, clamped)
Sheet:getActionPoints()                              -- `sheet:getActionPoints()` – returns current, max
Sheet:getActiveTraits()                              -- `sheet:getActiveTraits()` – list all active trait names
Sheet:getBase( name )                                -- `sheet:getBase(name)` – get raw base value
Sheet:getBuffCount( [stat : string] )                -- Returns the buff count
Sheet:getBuffs( [stat] )                             -- `sheet:getBuffs(stat?)` – return an array of buff info tables
Sheet:getCooldownRemaining( name )                   -- `sheet:getCooldownRemaining(name)` – get remaining skill cooldown in seconds
Sheet:getEncumbrance()                               -- `sheet:getEncumbrance()` – returns current, max
Sheet:getFlags()                                     -- `sheet:getFlags()` – list all set flag names
Sheet:getInitiative()                                -- `sheet:getInitiative()` – get base initiative
Sheet:getLevel()                                     -- `sheet:getLevel()` – current character level
Sheet:getMax( name )                                 -- `sheet:getMax(name)` – get the attribute maximum (nil if unset)
Sheet:getMin( name )                                 -- `sheet:getMin(name)` – get the attribute minimum
Sheet:getMorale()                                    -- `sheet:getMorale()` – returns current, max
Sheet:getRegen( name )                               -- `sheet:getRegen(name)` – get regen rate
Sheet:getResistance( dtype )                         -- `sheet:getResistance(damageType)` – get resistance value
Sheet:getSkillLevel( name )                          -- `sheet:getSkillLevel(name)` – get current skill level
Sheet:getStatNames()                                 -- `sheet:update(dt)` – tick buff durations, skill cooldowns, and regen
Sheet:getUseCount( name )                            -- `sheet:getUseCount(name)` – number of times a stat was used
Sheet:getXP()                                        -- `sheet:getXP()` – current XP in the current level
Sheet:hasFlag( name )                                -- `sheet:hasFlag(name)` – check if a flag is set
Sheet:hasPerk( name )                                -- `sheet:hasPerk(name)` – check if perk is acquired
Sheet:hasTrait( name )                               -- `sheet:hasTrait(name)` – check if a trait is currently active
Sheet:isEncumbered()                                 -- `sheet:isEncumbered()` – returns true if current > max
Sheet:learnSkill( name )                             -- `sheet:learnSkill(name)` – unlock / increment skill level
Sheet:recordUse( name )                              -- `sheet:recordUse(name)` – record a use for use-based growth
Sheet:recoverActionPoints( amount : number )         -- Recover action points on this Sheet
Sheet:removeBuff( handle )                           -- `sheet:removeBuff(handle)` – remove a buff by handle; returns true if found
Sheet:removeTrait( name )                            -- `sheet:removeTrait(name)` – remove a trait's buffs; returns true if found
Sheet:restore( snap )                                -- `sheet:restore(snapshot)` – restore from a snapshot table (attributes, xp, level, flags)
Sheet:setActionPoints( max )                         -- `sheet:setActionPoints(max)` – initialise / reset action points
Sheet:setBase( name, value )                         -- `sheet:setBase(name, value)` – set the base value
Sheet:setBerserkThreshold( val )                     -- `sheet:setBerserkThreshold(val)` – set the morale level below which the unit goes berserk
Sheet:setEncumbrance( current, max )                 -- `sheet:setEncumbrance(current, max)` – set encumbrance values
Sheet:setFlag( name )                                -- `sheet:setFlag(name)` – set a boolean flag
Sheet:setInitiative( val )                           -- `sheet:setInitiative(val)` – set base initiative
Sheet:setLevel( n )                                  -- `sheet:setLevel(n)` – force-set character level
Sheet:setLevelThresholds( t )                        -- `sheet:setLevelThresholds(t)` – t is an array of XP thresholds OR table with base/increment
Sheet:setMax( name, max )                            -- `sheet:setMax(name, max)` – set the attribute maximum
Sheet:setMin( name, min )                            -- `sheet:setMin(name, min)` – set the attribute minimum
Sheet:setMorale( max )                               -- `sheet:setMorale(max)` – initialise morale with the given max
Sheet:setPanicThreshold( val )                       -- `sheet:setPanicThreshold(val)` – set the morale level below which the unit panics
Sheet:setRegen( name, regen )                        -- `sheet:setRegen(name, regen)` – set regen rate per second
Sheet:setResistance( dtype, val )                    -- `sheet:setResistance(damageType, value)` – set a resistance (0.0..1.0)
Sheet:setXP( amount )                                -- `sheet:setXP(amount)` – force-set XP (no level-up triggered)
Sheet:snapshot()                                     -- `sheet:snapshot()` – return a serialisable Lua table of all sheet state
Sheet:spendActionPoints( n )                         -- `sheet:spendActionPoints(n)` – spend n action points; errors if insufficient
Sheet:update( dt : number )                          -- Advances the simulation by `dt` seconds
Sheet:useSkill( name )                               -- `sheet:useSkill(name)` – reduce cooldown counter. Returns remaining cooldown
```

---

## `luna.inventory` {#inventory}

> Lua API bindings for the `luna.inventory.*` slot-based inventory module.
>
> Provides `Item`, `ItemStack`, `Slot`, `Container`, `ItemSet`, and `Inventory`
> UserData objects for building RPG-style inventory systems in Lua.

```lua
luna.inventory.newContainer( name, [mode_str], [slot_count] )
luna.inventory.newInventory()
luna.inventory.newItem( [item_type] )
luna.inventory.newItemSet( name )
luna.inventory.newItemStack( item_ud, [qty], [max_qty] )
luna.inventory.newSlot( [slot_type], [state] )
```

### `Container`

```lua
Container:addSlot( [opts : table] )                        -- Adds slot to the collection
Container:countItem( item_type : string ) -> integer       -- Returns the number of item
Container:expand( n )                                      -- Expand on this Container
Container:getCurrentWeight()                               -- Returns the current weight
Container:getMaxSlots()                                    -- Returns the max slots
Container:getMode()                                        -- Returns the mode
Container:getName()                                        -- Returns the name
Container:getSlot( index )                                 -- Get a slot by 1-based index (Lua convention)
Container:getSlotCount()                                   -- Returns the slot count
Container:getSlots()                                       -- Returns the slots
Container:getWeightLimit()                                 -- Returns the weight limit
Container:removeItem( item_type : string, qty : integer )  -- Removes item from the collection
Container:removeSlot( index : integer )                    -- Removes slot from the collection
Container:setMaxSlots( n : integer )                       -- Sets the max slots
Container:setWeightLimit( w : number )                     -- Sets the weight limit
Container:toList()                                         -- To list on this Container
```

### `Inventory`

```lua
Inventory:addItemSet( set_ud : userdata )                 -- Adds item set to the collection
Inventory:countItem( item_type : string ) -> integer      -- Returns the number of item
Inventory:disableSubsystem( name : string )               -- Disable subsystem on this Inventory
Inventory:enableSubsystem( name : string )                -- Enable subsystem on this Inventory
Inventory:getActiveSets()                                 -- Returns the active sets
Inventory:getContainer( name : string )                   -- Returns the container
Inventory:getContainerNames()                             -- Returns the container names
Inventory:getEquipSlot( name : string )                   -- Returns the equip slot
Inventory:getEquipSlotNames()                             -- Returns the equip slot names
Inventory:getEquipped( slot_name : string )               -- Returns the equipped
Inventory:getItemSetNames()                               -- Returns the item set names
Inventory:getItemSets() -> table                          -- Returns all registered item sets as ItemSet objects
Inventory:isSubsystemEnabled( name : string ) -> boolean  -- Returns `true` if subsystem enabled
Inventory:removeCallback( event : string )                -- Remove the callback for an inventory event
Inventory:removeContainer( name : string )                -- Removes container from the collection
Inventory:removeEquipSlot( name : string )                -- Removes equip slot from the collection
Inventory:unequip( slot_name : string )                   -- Unequip on this Inventory
```

### `Item`

```lua
Item:addTag( tag : string )               -- Adds tag to the collection
Item:clone()                              -- Returns a deep copy of this object
Item:getResourceRef()                     -- Get the stored resource reference, or nil if none set
Item:getSize()                            -- Returns the size
Item:getStackLimit()                      -- Returns the stack limit
Item:getTags()                            -- Returns the tags
Item:getType()                            -- Returns the type
Item:getUserData()                        -- Get the stored user data, or nil if none set
Item:getWeight()                          -- Returns the weight
Item:hasTag( tag : string ) -> boolean    -- Returns `true` if tag
Item:removeTag( tag : string )            -- Removes tag from the collection
Item:setResourceRef( value : any )        -- Store any Lua value as a resource reference (e.g. a texture or sprite)
Item:setSize( w : integer, h : integer )  -- Sets the size
Item:setStackLimit( n : integer )         -- Sets the stack limit
Item:setType( t : string )                -- Sets the type
Item:setUserData( value : any )           -- Store any Lua value as user data on this item
Item:setWeight( w : number )              -- Sets the weight
```

### `ItemSet`

```lua
ItemSet:getBonusRef()                     -- Get the stored bonus data, or nil if none set
ItemSet:getName()                         -- Returns the name
ItemSet:getRequirementCount() -> integer  -- Returns the number of requirements in this set
ItemSet:getRequirements()                 -- Returns the requirements
ItemSet:isActive( inv_ud ) -> boolean     -- Check if all requirements of this set are met by the inventory
ItemSet:setBonusRef( value : any )        -- Store bonus data (any Lua value) on this item set
```

### `ItemStack`

```lua
ItemStack:add( n : integer )          -- Adds an entry to the collection
ItemStack:clone()                     -- Returns a deep copy of this object
ItemStack:getItem()                   -- Returns the item
ItemStack:getMaxQuantity()            -- Returns the max quantity
ItemStack:getQuantity()               -- Returns the quantity
ItemStack:isFull() -> boolean         -- Returns `true` if full
ItemStack:merge( other : userdata )   -- Merge on this ItemStack
ItemStack:remove( n : integer )       -- Removes the entry from the collection
ItemStack:setQuantity( n : integer )  -- Sets the quantity
ItemStack:split( n : integer )        -- Split on this ItemStack
```

### `Slot`

```lua
Slot:canAccept( item_ud : userdata ) -> boolean  -- Returns `true` if accept
Slot:clear()                                     -- Removes all entries
Slot:getCapacity()                               -- Returns the capacity
Slot:getItem() -> Item                           -- Shortcut: get the item from the held stack, or nil if empty
Slot:getStack()                                  -- Returns the stack
Slot:getState()                                  -- Returns the state
Slot:getType()                                   -- Returns the type
Slot:isEmpty() -> boolean                        -- Returns `true` if empty
Slot:setCapacity( w : integer, h : integer )     -- Sets the capacity
Slot:setStack( stack_ud : userdata )             -- Sets the stack
Slot:setState( s : string )                      -- Sets the state
Slot:setType( t : string )                       -- Sets the type
```

---

## `luna.crafting` {#crafting}

> Lua bindings for `luna.crafting.*`.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for crafting api-related operations and data management.
> Key types exported from this module: `LuaRecipe`, `LuaRecipeRegistry`, `LuaStation`, `LuaCraftSkill`, `LuaCraftQueue`.
> Primary functions: `register()`.

```lua
luna.crafting.newCraftQueue( [max_jobs : integer] )                   -- Creates a new craft queue instance
luna.crafting.newCraftSkill( name : string )                          -- Creates a new craft skill instance
luna.crafting.newModifierPool( name : string )                        -- Creates a new modifier pool
luna.crafting.newRecipe( id : string, [recipe_type : string] )        -- Creates a new recipe instance
luna.crafting.newRecipeGroup( name : string )                         -- Creates a new recipe group for UI organization
luna.crafting.newRecipeKnowledge()                                    -- Creates a new recipe knowledge tracker
luna.crafting.newRegistry()                                           -- Creates a new recipe registry instance
luna.crafting.newStation( station_type : string, [level : integer] )  -- Creates a new station instance
luna.crafting.newUpgradeTree( [name : string] )                       -- Creates a new upgrade tree instance
```

### `CraftQueue`

```lua
CraftQueue:cancel( id : integer )                                         -- Cancels the current operation
CraftQueue:clear()                                                        -- Removes all entries
CraftQueue:collectCompleted() -> table                                    -- Returns the IDs of jobs that completed since the last collection
CraftQueue:count() -> integer                                             -- Returns the number of items
CraftQueue:enqueue( recipe_id : string, time : number, [qty : integer] )  -- Enqueue on this CraftQueue
CraftQueue:getAllJobs()                                                   -- Returns the all jobs
CraftQueue:getIds()                                                       -- Returns the ids
CraftQueue:getJob( id : integer )                                         -- Returns the job
CraftQueue:getMaxJobs()                                                   -- Returns the max jobs
CraftQueue:isFull() -> boolean                                            -- Returns `true` if full
CraftQueue:setJobPaused( id : integer, paused : boolean )                 -- Sets the job paused
CraftQueue:update( dt : number )                                          -- Advances the simulation by `dt` seconds
```

### `CraftSkill`

```lua
CraftSkill:addPerk( perk_id : string, req_level, [prereqs] )  -- Defines a perk node in this skill's perk tree
CraftSkill:addSpecialization( name : string )                 -- Registers a specialization branch for this skill
CraftSkill:addXp( amount ) -> integer                         -- Adds `xp` to this skill's total, potentially triggering level-ups
CraftSkill:canUse( recipe : Recipe ) -> boolean               -- Returns `true` if this skill's level meets the minimum required to use the given recipe
CraftSkill:chooseSpecialization( name : string ) -> boolean   -- Locks in a specialization branch. Fails if already specialized
CraftSkill:getAvailablePerks() -> table                       -- Returns perk IDs whose prerequisites and level requirements are met
CraftSkill:getLevel() -> integer                              -- Returns the current level derived from total XP
CraftSkill:getMaxLevel() -> integer                           -- Returns the maximum level this skill can reach
CraftSkill:getName() -> string                                -- Returns the skill's name identifier
CraftSkill:getQualityBonus() -> number                        -- Returns the quality bonus (0–1 fraction) from skill mastery
CraftSkill:getRecipeColor( recipe : Recipe ) -> string        -- Returns the WoW-style difficulty color for a recipe from this skill's perspective
CraftSkill:getSpecialization() -> string                      -- Returns the chosen specialization name, or `nil` if none chosen yet
CraftSkill:getSpeedBonus() -> number                          -- Returns the speed bonus (0–1 fraction) from skill mastery
CraftSkill:getXp() -> number                                  -- Returns the total accumulated XP for this skill
CraftSkill:getXpToNext() -> number                            -- Returns the XP required to reach the next level
CraftSkill:getYieldBonus() -> number                          -- Returns the yield/quantity bonus (0–1 fraction) from skill mastery
CraftSkill:hasPerk( perk_id : string ) -> boolean             -- Returns `true` if the given perk is unlocked
CraftSkill:setLevel( level : integer )                        -- Sets the skill directly to the given level, adjusting XP accordingly
CraftSkill:skillUpChance( recipe : Recipe ) -> number         -- Returns the skill-up probability (0–1) for this skill when crafting a recipe
CraftSkill:unlockPerk( perk_id : string ) -> boolean          -- Attempts to unlock a perk. Fails if prerequisites or level not met
```

### `ModifierPool`

```lua
ModifierPool:addModifier( name : string, weight : number, [effects : table] )  -- Adds a modifier to the pool. Effects are a table of `{effectName: number}` pairs
ModifierPool:count() -> integer                                                -- Returns the number of entries in the pool
ModifierPool:getModifiers() -> table                                           -- Returns all modifier entries as a list of `{name, weight, effects}` tables
ModifierPool:getName() -> string                                               -- Returns the name of this modifier pool
ModifierPool:getTotalWeight() -> number                                        -- Returns the total combined weight of all pool entries
ModifierPool:removeModifier( name : string ) -> boolean                        -- Removes a modifier by name. Returns `true` if it existed
ModifierPool:roll( seed : integer ) -> table                                   -- Rolls a random modifier using `seed` as a deterministic source
```

### `Recipe`

```lua
Recipe:addByproduct( item_type : string, qty : integer, [chance : number] )  -- Adds a byproduct output (produced alongside main outputs, not guaranteed)
Recipe:addCondition( cond_type, value : string )                             -- Adds a crafting condition requirement for this recipe (e.g. biome, weather)
Recipe:addIngredient( item_type, qty, [consumed] )                           -- Adds an ingredient requirement to this recipe
Recipe:addOutput( item_type, qty, [quality] )                                -- Adds an output item produced when this recipe is completed
Recipe:addTag( tag : string )                                                -- Attaches a string tag to this recipe
Recipe:clearIngredients()                                                    -- Removes all ingredients from this recipe
Recipe:clearOutputs()                                                        -- Removes all outputs from this recipe
Recipe:getCategory() -> string                                               -- Returns the UI category string for this recipe
Recipe:getConditions() -> table                                              -- Returns all crafting conditions as a list of `{condType, value}` tables
Recipe:getCooldown() -> number                                               -- Returns the cooldown duration in seconds before this recipe can be used again
Recipe:getDescription() -> string                                            -- Returns the lore/description text for this recipe
Recipe:getId() -> string                                                     -- Returns the unique string identifier of this recipe
Recipe:getIngredients() -> table                                             -- Returns a list of all ingredient requirements as `{id, count}` tables
Recipe:getName() -> string                                                   -- Returns the human-readable display name of this recipe
Recipe:getOutputs() -> table                                                 -- Returns a list of all output items as `{id, count}` tables
Recipe:getRandomModifierPool() -> string                                     -- Returns the ID of the modifier pool rolled on completion (empty = none)
Recipe:getSkill() -> string                                                  -- Returns the skill name gated on this recipe, or an empty string if none
Recipe:getSkillUpCurve() -> string                                           -- Returns the skill-up XP curve name: `'linear'`, `'quadratic'`, `'exponential'`
Recipe:getSkillXp() -> number                                                -- Returns the XP awarded to the required skill when this recipe is completed
Recipe:getStationLevel() -> integer                                          -- Returns the minimum station level required to craft this recipe
Recipe:getStationType() -> string                                            -- Returns the station type string required to craft this recipe
Recipe:getTags() -> table                                                    -- Returns a list of all tags attached to this recipe
Recipe:getTime() -> number                                                   -- Returns the base crafting duration in seconds
Recipe:getType() -> string                                                   -- Returns the recipe type tag (e.g. `'smelt'`, `'craft'`)
Recipe:hasTag( tag : string ) -> boolean                                     -- Returns `true` if this recipe carries the given tag
Recipe:isEnabled() -> boolean                                                -- Returns `true` if this recipe is currently craftable (not disabled)
Recipe:isHandCraftable() -> boolean                                          -- Returns `true` if this recipe can be crafted without a station
Recipe:isOutputQualityScaling() -> boolean                                   -- Returns whether output quality scales with the crafter's skill level
Recipe:setCategory( v )                                                      -- Sets the UI category for this recipe (e.g. `'weapons'`, `'potions'`)
Recipe:setCooldown( v )                                                      -- Sets the cooldown duration in seconds
Recipe:setDescription( v )                                                   -- Sets the description text shown in crafting UI
Recipe:setEnabled( v )                                                       -- Enables or disables this recipe. Disabled recipes cannot be enqueued
Recipe:setHandCraftable( v )                                                 -- Sets whether this recipe can be crafted without a station
Recipe:setName( v )                                                          -- Sets the display name of this recipe
Recipe:setOutputQualityScaling( v )                                          -- Sets whether output quality scales with the crafter's skill level
Recipe:setRandomModifierPool( v )                                            -- Sets the modifier pool to roll on completion
Recipe:setSkill( name, [level] )                                             -- Sets the skill required to unlock and use this recipe
Recipe:setSkillUpCurve( v )                                                  -- Sets the skill-up XP curve name
Recipe:setSkillXp( v )                                                       -- Sets the skill XP awarded on completion
Recipe:setStationLevel( v )                                                  -- Sets the minimum station level required to craft this recipe
Recipe:setStationType( v )                                                   -- Sets which station type is required for this recipe
Recipe:setTime( v )                                                          -- Sets the base crafting duration in seconds
```

### `RecipeGroup`

```lua
RecipeGroup:addRecipe( id )                -- Adds a recipe ID to this group (no-op if already present)
RecipeGroup:contains( id ) -> boolean      -- Returns `true` if this group contains the given recipe ID
RecipeGroup:count() -> integer             -- Returns the number of recipes in this group
RecipeGroup:getIcon() -> string            -- Returns the icon asset path for this group
RecipeGroup:getName() -> string            -- Returns the group name
RecipeGroup:getOrder() -> integer          -- Returns the display order index
RecipeGroup:getRecipes() -> table          -- Returns all recipe IDs in this group
RecipeGroup:removeRecipe( id ) -> boolean  -- Removes a recipe ID from this group. Returns `true` if it was present
RecipeGroup:setIcon( v )                   -- Sets the icon asset path
RecipeGroup:setName( v )                   -- Sets the group name
RecipeGroup:setOrder( v )                  -- Sets the display order index
```

### `RecipeKnowledge`

```lua
RecipeKnowledge:clear()                                    -- Clears all recipe knowledge
RecipeKnowledge:count() -> integer                         -- Returns the number of known recipes
RecipeKnowledge:discover( id, [source : string] )          -- Marks a recipe as discovered with an optional source string
RecipeKnowledge:forget( id ) -> boolean                    -- Removes knowledge of a recipe. Returns `true` if the recipe was known
RecipeKnowledge:getKnown() -> table                        -- Returns a list of all known recipe IDs
RecipeKnowledge:getResearchCost( id ) -> number            -- Returns the research cost for a recipe (0 if not set)
RecipeKnowledge:getSource( id ) -> string                  -- Returns the discovery source for a recipe, or `nil` if unknown
RecipeKnowledge:isAutoDiscover() -> boolean                -- Returns `true` if auto-discover mode is on
RecipeKnowledge:isKnown( id ) -> boolean                   -- Returns `true` if the recipe is known (or auto-discover is on)
RecipeKnowledge:isPrototyped( id ) -> boolean              -- Returns `true` if the recipe has been prototyped
RecipeKnowledge:prototype( id ) -> boolean                 -- Permanently learns a recipe via the prototype mechanic (Don't Starve pattern)
RecipeKnowledge:research( id, scrap : number ) -> boolean  -- Attempts to research a recipe by spending `scrap`. Returns `false` if not enough
RecipeKnowledge:setAutoDiscover( v )                       -- Enables or disables auto-discovery mode (all recipes always known)
RecipeKnowledge:setResearchCost( id, cost : number )       -- Sets the resource cost required to research a recipe (Rust blueprint pattern)
```

### `RecipeRegistry`

```lua
RecipeRegistry:add( recipe : Recipe )                        -- Registers a recipe in this registry. Raises an error if a recipe with the same ID already exists
RecipeRegistry:count() -> integer                            -- Returns the total number of registered recipes
RecipeRegistry:findByIngredient( item_type ) -> table        -- Returns all recipes that consume an item with the given ID as an ingredient
RecipeRegistry:findByOutput( item_type ) -> table            -- Returns all recipes that produce an item with the given ID
RecipeRegistry:findByTag( tag : string ) -> table            -- Returns all recipes that carry the given tag
RecipeRegistry:forStation( station_type : string ) -> table  -- Returns all recipes that require the given station type
RecipeRegistry:get( id : string ) -> Recipe                  -- Returns the registered recipe with the given ID, or `nil` if none exists
RecipeRegistry:getAll() -> table                             -- Returns all recipes in registration order as a list of `Recipe` objects
RecipeRegistry:getByCategory( category : string ) -> table   -- Returns all recipe IDs in the given UI category
RecipeRegistry:getIds() -> table                             -- Returns a list of all registered recipe IDs
RecipeRegistry:register( recipe : Recipe )                   -- Alias for `add`. Registers a recipe (error if duplicate ID)
RecipeRegistry:remove( id : string )                         -- Removes the recipe with the given ID from this registry
RecipeRegistry:unregister( id : string ) -> boolean          -- Alias for `remove`. Unregisters the recipe with the given ID
```

### `Station`

```lua
Station:addFuel( amount : number )                      -- Adds fuel to this station, clamped to capacity
Station:canProcess( recipe : Recipe ) -> boolean        -- Returns `true` if this station can currently process the given recipe (level and type match)
Station:consumeFuel( amount : number ) -> boolean       -- Consumes fuel from this station. Returns `false` if insufficient fuel
Station:effectiveTime( recipe : Recipe ) -> number      -- Returns the effective crafting time for `recipe` after applying this station's speed multiplier
Station:getFuelCapacity() -> number                     -- Returns the maximum fuel capacity of this station
Station:getFuelLevel() -> number                        -- Returns the current fuel level of this station
Station:getLevel() -> integer                           -- Returns the current upgrade level of this station
Station:getMaxLevel() -> integer                        -- Returns the maximum upgrade level this station can reach
Station:getMetadata( key : string ) -> string           -- Returns a stored metadata value by key
Station:getName() -> string                             -- Returns the display name of this station
Station:getOutputMultiplier() -> number                 -- Returns the output quantity multiplier applied by this station
Station:getPosition() -> number, number                 -- Returns the station's world position as `(x, y)`
Station:getProximityRadius() -> number                  -- Returns the proximity radius within which players can use this station
Station:getQualityBonus() -> number                     -- Returns the quality bonus applied to crafted items at this station
Station:getSpeedMultiplier() -> number                  -- Returns the crafting speed multiplier. `1.0` is normal speed
Station:getType() -> string                             -- Returns the station type identifier string
Station:isActive() -> boolean                           -- Returns `true` if this station is operational and can process recipes
Station:isInRange( x : number, y : number ) -> boolean  -- Returns `true` if the given world position is within this station's proximity radius
Station:setActive( v )                                  -- Enables or disables this station
Station:setFuelCapacity( v )                            -- Sets the maximum fuel capacity
Station:setFuelLevel( v )                               -- Sets the fuel level directly
Station:setLevel( v )                                   -- Sets the station's upgrade level, affecting which recipes it can process
Station:setMaxLevel( v )                                -- Sets the maximum upgrade level for this station
Station:setMetadata( key : string, val )                -- Stores a metadata key-value pair on this station
Station:setName( v )                                    -- Sets the display name of this station
Station:setOutputMultiplier( v )                        -- Sets the output quantity multiplier for this station
Station:setPosition( x : number, y : number )           -- Sets the station's world position
Station:setProximityRadius( v )                         -- Sets the proximity radius for this station
Station:setQualityBonus( v )                            -- Sets the quality bonus applied to crafted items at this station
Station:setSpeedMultiplier( v )                         -- Sets the crafting speed multiplier. Values above `1.0` reduce effective recipe time
Station:upgrade() -> boolean                            -- Increments station level by 1. Returns `false` if already at max level
```

### `UpgradeTree`

```lua
UpgradeTree:addEdge( from_id : string, to_id : string )               -- Adds a directed edge from `from_id` to `to_id` in the DAG
UpgradeTree:addNode( id : string, name : string, [prereqs : table] )  -- Adds node to the collection
UpgradeTree:canUnlock( id : string ) -> boolean                       -- Returns `true` if unlock
UpgradeTree:count() -> integer                                        -- Returns the number of items
UpgradeTree:getAllNodes() -> table                                    -- Returns all node data as a list of `{id, name, unlocked, recipeId, outputItemType}` tables
UpgradeTree:getChildren( id : string ) -> table                       -- Returns direct children of the given node
UpgradeTree:getName()                                                 -- Returns the name
UpgradeTree:getNode( id : string )                                    -- Returns data for a single node as a table, or `nil` if not found
UpgradeTree:getNodeCost( id : string )                                -- Returns the node cost
UpgradeTree:getNodeIds()                                              -- Returns the node ids
UpgradeTree:getParent( id : string ) -> string                        -- Returns the parent node ID of the given node, or `nil` if it is a root
UpgradeTree:getPath( from : string, to : string ) -> table            -- Returns the shortest path of node IDs from `from` to `to`, or an empty table
UpgradeTree:getRootNodes() -> table                                   -- Returns all root node IDs (nodes with no parents)
UpgradeTree:getUnlockedIds()                                          -- Returns the unlocked ids
UpgradeTree:isUnlocked( id : string ) -> boolean                      -- Returns `true` if unlocked
UpgradeTree:resetNode( id : string )                                  -- Reset node on this UpgradeTree
UpgradeTree:setNodeCost( id : string, cost_table : table )            -- Sets the node cost
UpgradeTree:unlock( id : string )                                     -- Unlock on this UpgradeTree
```

---

## `luna.cardgame` {#cardgame}

> Lua bindings for `luna.cardgame.*`.
>
> Exposes Card, Stack, StackBuilder, StackManager, CardPool, Slot, StackHistory,
> and group-analysis utilities to Lua scripts via the `luna.cardgame` namespace.

```lua
luna.cardgame.clearTypes()                                  -- Clears all registered item types
luna.cardgame.defineType( name, def_tbl )                   -- Defines a card type in the global registry
luna.cardgame.findAtLeastNOfStat( items_tbl, stat, n )      -- Finds groups where at least `n` items share the same stat integer value
luna.cardgame.findNOfStat( items_tbl, stat, n )             -- Finds groups where exactly `n` items share the same stat integer value
luna.cardgame.findSequences( items_tbl, stat, min_run )     -- Finds consecutive integer stat sequences of length ≥ `min_run`
luna.cardgame.findTagGroups( items_tbl, prefix, min_size )  -- Finds groups of items sharing a tag-prefix value (flush-style), min size `min_size`
luna.cardgame.getType( name )                               -- Returns the definition table for a registered type, or `nil` if not found
luna.cardgame.getTypeNames()                                -- Returns an array table of all registered type names
luna.cardgame.groupByCategory( items_tbl )                  -- Groups items by category.  Returns `{ [category] = {LuaItem, …}, … }`
luna.cardgame.groupByStat( items_tbl, stat )                -- Groups items in an array table by the integer part of a stat
luna.cardgame.groupByTagPrefix( items_tbl, prefix )         -- Groups items by a tag prefix such as `"suit"`
luna.cardgame.newCard( type_name )                          -- Creates a new item by seeding from the registered type
luna.cardgame.newCardPool( [name] )                         -- Creates a new `CardPool`
luna.cardgame.newDeckBuilder( [name] )                      -- Creates a new `StackBuilder`
luna.cardgame.newEffectStack()                              -- Creates a new LIFO `EffectStack` for effect resolution
luna.cardgame.newHistory( [max_size] )                      -- Creates a new `StackHistory`
luna.cardgame.newSlot( name, [cap] )                        -- Creates a new `Slot`
luna.cardgame.newStack( name, [cap] )                       -- Creates a new empty stack
luna.cardgame.newStackManager()                             -- Alias for `newEffectStack` (LIFO effect resolution stack)
luna.cardgame.newZoneManager()                              -- Creates a new `StackManager`
```

### `Card`

```lua
Card:addCounter( name, delta )     -- Adds `delta` to a counter
Card:addStat( name, delta )        -- Adds `delta` to a named stat (creates it at `delta` if absent)
Card:addTag( tag )                 -- Adds a tag if not already present
Card:canPlay( ctx )                -- Returns whether this card can be played.  Calls cost_check if set; returns `true` otherwise
Card:clearCounters()               -- Clears all counters
Card:clone()                       -- Deep-copies the card (new instance, same stats/tags/counters)
Card:fireScript( event, args )     -- Fires a named script hook with optional extra arguments
Card:getAllMeta()                  -- Returns all metadata as a `{key=value}` table
Card:getAsset( key )               -- Retrieves an asset value, or `nil` if not set
Card:getCardType()                 -- Returns the type identifier string for this card
Card:getCategory()                 -- Returns the category string
Card:getController()               -- Returns the controller identifier (may differ from owner)
Card:getCounter( name )            -- Returns an integer counter (0 if absent)
Card:getCounters()                 -- Returns all counters as a `{key=value}` table
Card:getId()                       -- Returns the unique instance ID of this card
Card:getMeta( key )                -- Returns a metadata string value (empty string if absent)
Card:getName()                     -- Returns the display name of this card
Card:getOwner()                    -- Returns the owner identifier
Card:getRarity()                   -- Returns the rarity tier string
Card:getScript( event )            -- Returns the script function for an event, or `nil`
Card:getSlot()                     -- Returns the current slot identifier
Card:getStat( name )               -- Returns the value of a named numeric stat (0 if absent)
Card:getStats()                    -- Returns all stats as a `{key=value}` table
Card:getStats()                    -- Returns all stats as a `{key=value}` table
Card:getSubtype()                  -- Returns the subtype string
Card:getTags()                     -- Returns all tags as an array table
Card:getTilePosition()             -- Returns tile position as (x, y)
Card:getTileSize()                 -- Returns tile footprint as (w, h)
Card:getZone()                     -- Returns the current zone/slot string
Card:hasStat( name )               -- Returns `true` if the named stat is explicitly set
Card:hasTag( tag )                 -- Returns `true` if the card has the given tag
Card:isFaceUp()                    -- Returns `true` if the card is face-up
Card:isTapped()                    -- Returns `true` if the card is tapped/exhausted
Card:modifyCounter( name, delta )  -- Adds `delta` to a counter and returns the new value
Card:modifyStat( name, delta )     -- Adds `delta` to a stat and returns the new value
Card:payCost( ctx )                -- Pays the card's cost.  Calls cost_pay if set; no-op otherwise
Card:removeTag( tag )              -- Removes a tag
Card:resetStats()                  -- Resets all stats to the card type defaults
Card:restore( snap )               -- Restores data fields from a snapshot table (as returned by `snapshot()`)
Card:setAsset( key, value )        -- Stores an arbitrary Lua value under `key`
Card:setCategory( cat )            -- Sets the category string
Card:setController( c )            -- Sets the controller identifier
Card:setCostCheck( func )          -- Sets a custom cost-check function: `setCostCheck(function(card, ctx) return bool end)`
Card:setCostPay( func )            -- Sets a custom cost-pay function
Card:setCounter( name, value )     -- Sets an integer counter
Card:setFaceUp( v )                -- Sets face-up state
Card:setMeta( key, value )         -- Sets a metadata string value
Card:setName( name )               -- Sets the card's display name
Card:setOwner( owner )             -- Sets the owner identifier
Card:setRarity( r )                -- Sets the rarity tier string
Card:setScript( event, func )      -- Sets a per-instance script hook: `setScript("onPlay", function(card) ... end)`
Card:setSlot( slot )               -- Sets the current slot identifier
Card:setStat( name, value )        -- Sets a named numeric stat
Card:setSubtype( s )               -- Sets the subtype string
Card:setTapped( v )                -- Sets tapped/exhausted state
Card:setTilePosition( x, y )       -- Sets tile position
Card:setTileSize( w, h )           -- Sets tile footprint
Card:setZone( zone )               -- Sets the current zone/slot string
Card:snapshot()                    -- Serializes this card to a Lua table (data fields only, no functions)
```

### `CardPool`

```lua
CardPool:add( type_name, weight )           -- Adds a type with the given weight
CardPool:addCardType( type_name, weight )   -- Adds a card type with weight (alias for `add`)
CardPool:drawByRarity( dist )               -- Draws cards by rarity distribution `{rarity_name = count, ...}`
CardPool:drawItems( n )                     -- Draws `n` items (with replacement) as `LuaItem` values; returns an array table
CardPool:drawRandom( n, [seed] )            -- Draws `n` cards; optional u64 `seed` makes the draw deterministic
CardPool:drawTypes( n )                     -- Draws `n` type-name strings (with replacement); returns an array table
CardPool:drawUniqueItems( n )               -- Draws up to `n` unique items; returns an array table
CardPool:drawUniqueTypes( n )               -- Draws up to `n` unique type-name strings; returns an array table
CardPool:getCardTypeCount()                 -- Returns the number of distinct card types in the pool
CardPool:getCardTypes()                     -- Returns the list of type names present in the pool
CardPool:getName()
CardPool:getWeight( type_name )             -- Returns the weight for `type_name`, or 0 if not present
CardPool:isEmpty()
CardPool:remove( type_name )                -- Removes a type from the pool
CardPool:setName( name )                    -- Sets the pool name
CardPool:setRarityWeight( rarity, weight )  -- Sets the rarity-tier weight used by `drawByRarity`
CardPool:setWeight( type_name, weight )     -- Sets the weight for an existing entry
CardPool:size()
CardPool:totalWeight()
```

### `DeckBuilder`

```lua
DeckBuilder:add( type_name, count )                 -- Adds `count` copies of `type_name` to the build list
DeckBuilder:addBannedType( type_name )              -- Bans a type (alias for `banType`)
DeckBuilder:addCustomRule( name, func )             -- Adds a custom validation rule `fn(stack) -> string|nil`
DeckBuilder:addRequiredCategory( cat, min, [max] )  -- Requires cards matching category `cat` appear between `min` and optional `max` times
DeckBuilder:addRequiredTag( tag, min )              -- Requires at least `min` cards with the given tag
DeckBuilder:addWith( type_name, count, overrides )  -- Adds `count` copies with per-entry overrides supplied as a table
DeckBuilder:banType( type_name )                    -- Marks a type as banned (must not appear)
DeckBuilder:build( [name] )                         -- Builds and returns an `ItemStack`.  Errors if entry types are unknown
DeckBuilder:getBannedTypes()                        -- Returns the list of banned types as an array table
DeckBuilder:getMaxCards()                           -- Returns the maximum card count
DeckBuilder:getMaxCopies()                          -- Returns the maximum copies of any single type
DeckBuilder:getMinCards()                           -- Returns the minimum card count
DeckBuilder:removeBannedType( type_name )           -- Removes a type from the ban list
DeckBuilder:removeCustomRule( name )                -- Removes a custom validation rule by name
DeckBuilder:requireType( type_name )                -- Marks a type as required (must appear in any validated stack)
DeckBuilder:setMaxCards( n )                        -- Sets maximum card count for validation (0 = unlimited)
DeckBuilder:setMaxCopies( n )                       -- Sets the maximum number of copies of any single type
DeckBuilder:setMaxCopiesForType( type_name, n )     -- Sets a per-type copy limit
DeckBuilder:setMaxSize( n )                         -- Sets the maximum stack size for validation (0 = unlimited)
DeckBuilder:setMinCards( n )                        -- Sets minimum card count for validation
DeckBuilder:setMinSize( n )                         -- Sets the minimum stack size for validation
DeckBuilder:setShuffleOnBuild( flag )               -- Sets shuffle-on-build flag
DeckBuilder:validate( stack )                       -- Validates `stack` against all constraints (size, copies, banned, required)
DeckBuilder:validateEntries()                       -- Validates the current build entries.  Returns an array table of error strings
DeckBuilder:validateStack( stack )                  -- Validates an existing `ItemStack` against the builder's constraints
```

### `EffectStack`

```lua
EffectStack:clear()                    -- Clears all entries
EffectStack:findByCard( card )         -- Returns an array of 1-based indices where `entry.card` matches the given card (by id)
EffectStack:getCount()                 -- Returns the number of entries
EffectStack:getEntries()               -- Returns all entries as an array (top is last)
EffectStack:insertEntry( entry, idx )  -- Inserts an entry at 1-based position from the top
EffectStack:isEmpty()                  -- Returns `true` if the stack is empty
EffectStack:peek()                     -- Peeks at the top entry without removing it, or `nil` if empty
EffectStack:pop()                      -- Pops and returns the top entry, or `nil` if empty
EffectStack:push( entry )              -- Pushes an entry table onto the top of the stack
EffectStack:removeEntry( idx )         -- Removes the entry at 1-based index.  Returns `true` if it was present
EffectStack:resolve()                  -- Resolves the top entry: calls `entry.card:fireScript("onEffect", entry)`
EffectStack:resolveAll()               -- Resolves all entries from top to bottom.  Returns the count resolved
```

### `History`

```lua
History:clear()
History:entries()                                      -- Returns all entries as an array of `{seq, stack, action, sizeAfter}` tables
History:isEmpty()
History:last()                                         -- Returns the last entry as a table `{seq, stack, action, sizeAfter}`, or `nil`
History:len()
History:recordCustom( stack_name, label, size_after )  -- Records a custom event label for a named stack
```

### `Slot`

```lua
Slot:capacity()
Slot:clear()                     -- Clears the slot; returns the cards as an array table
Slot:getName()
Slot:hasItemOfType( type_name )
Slot:hasItemWithTag( tag )
Slot:isEmpty()
Slot:isFull()
Slot:items()                     -- Returns all items as an array table
Slot:peek()                      -- Peeks at the top card without removing it
Slot:pop()                       -- Pops a card from the slot
Slot:push( card )                -- Pushes a card into the slot
Slot:setCapacity( cap )          -- Sets the capacity (pass 0 for unlimited)
Slot:size()
```

### `Stack`

```lua
Stack:addCard( card )           -- Adds a card to the top; returns `false` if full
Stack:addCardBottom( card )     -- Adds a card to the bottom; returns `false` if full
Stack:capacity()
Stack:clear()                   -- Clears all items from the stack
Stack:contains( card )          -- Returns `true` if the stack contains the given card (by identity)
Stack:countByCategory( cat )    -- Returns the count of items with a given category
Stack:countByTag( tag )         -- Returns the count of items with a given tag
Stack:countByType( type_name )  -- Returns the count of items with a given type name
Stack:drawCard()                -- Draws (pops) the top card; returns `nil` if empty. Alias for `pop`
Stack:drawCardBottom()          -- Draws (pops) the bottom card; returns `nil` if empty. Alias for `popBottom`
Stack:drawCards( n )            -- Draws `n` cards from the top; returns an array table. Alias for `popMany`
Stack:filter( pred )            -- Returns an array table of cards matching a Lua predicate function
Stack:findByCategory( cat )     -- Returns all cards of the given category as an array table
Stack:findByType( type_name )   -- Returns the index (1-based) of the first item with the given type name, or `nil`
Stack:getCapacity()             -- Returns the capacity limit, or -1 for unlimited
Stack:getCardCount()            -- Returns the number of cards (alias for `size`)
Stack:getCards()                -- Returns all cards as an array table (alias for `items`)
Stack:getName()
Stack:insertAt( idx, card )     -- Inserts a card at 1-based index
Stack:insertCard( idx, card )   -- Inserts a card at 1-based index. Alias for `insertAt`
Stack:isEmpty()
Stack:isFull()
Stack:isOrdered()               -- Returns true if this zone preserves insertion order
Stack:isPublic()                -- Returns true if cards in this zone are visible to all players
Stack:items()                   -- Returns an array table of all items (top is last)
Stack:moveAllCards( dst )       -- Moves all cards from this stack to `dst`; returns the number moved
Stack:moveCard( card, dst )     -- Moves a specific card from this stack to `dst`; returns `true` on success
Stack:peek()                    -- Peeks at the top card without removing it; returns `nil` if empty
Stack:peekAt( pos )             -- Peeks at item at 1-based position from the top without removing it
Stack:pop()                     -- Pops the top card; returns `nil` if empty
Stack:popBottom()               -- Pops the bottom card; returns `nil` if empty
Stack:popMany( n )              -- Pops `n` items from the top; returns an array table
Stack:push( card )              -- Pushes a `LuaItem` onto the top of the stack
Stack:pushBottom( card )        -- Pushes a `LuaItem` to the bottom of the stack
Stack:removeAt( idx )           -- Removes item at 1-based index; returns the card or `nil`
Stack:removeCard( card )        -- Removes a specific card by identity; returns `true` if removed
Stack:setCapacity( n )          -- Sets the capacity limit (`0` or negative removes the limit)
Stack:setName( name )           -- Sets the name of this stack
Stack:setOrdered( v )           -- Sets whether this zone preserves insertion order
Stack:setPublic( v )            -- Sets whether cards are visible to all players
Stack:shuffle()                 -- Shuffles the stack in place
Stack:size()
Stack:sort( cmp )               -- Sorts the stack using a Lua comparator function `(a, b) -> bool`
Stack:sortByCategory()          -- Sorts alphabetically by category
Stack:sortByName()              -- Sorts alphabetically by card name
Stack:sortByStat( stat )        -- Sorts ascending by a named stat
Stack:sortByStatDesc( stat )    -- Sorts descending by a named stat
```

### `ZoneManager`

```lua
ZoneManager:addStack( name, stack )                -- Adds an externally-created `ItemStack` into the manager
ZoneManager:createStack( name )                    -- Creates an empty, unbounded stack with the given name
ZoneManager:createStackCapped( name, cap )         -- Creates an empty stack with a capacity cap
ZoneManager:getStack( name )                       -- Returns a snapshot of the named stack as a `LuaStack`
ZoneManager:hasStack( name )                       -- Returns `true` if a stack with this name exists
ZoneManager:moveItem( from, idx, to )              -- Moves the card at 1-based `index` from `from_stack` to the top of `to_stack`
ZoneManager:moveItemByType( from, type_name, to )  -- Moves the first item of `type_name` from `from_stack` to `to_stack`
ZoneManager:moveTop( from, to )                    -- Moves the top card of `from_stack` to the top of `to_stack`
ZoneManager:removeStack( name )                    -- Removes a stack by name
ZoneManager:stackNames()                           -- Returns a list of all stack names
ZoneManager:totalItems()                           -- Total items across all stacks
```

---

## `luna.combat` {#combat}

> Lua bindings for the `luna.combat.*` vehicle combat API.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for combat api-related operations and data management.
> Key types exported from this module: `LuaCollisionGroupSet`, `LuaChassis`, `LuaTurret`, `LuaWeapon`, `LuaProjectile`.
> Primary functions: `register()`.

```lua
luna.combat.newChassis( body_id : integer, max_hp : number )           -- Creates a new `Chassis` with the given body ID and max HP
luna.combat.newCollisionGroupSet()                                     -- Creates a new `CollisionGroupSet`
luna.combat.newCombatWorld()                                           -- Creates a new empty `CombatWorld`
luna.combat.newProjectilePool( size : integer, [proj_type : string] )  -- Creates a new `ProjectilePool` with the given capacity
luna.combat.newTurret( body_id : integer, joint_id : integer )         -- Creates a new `Turret` with the given body and joint IDs
luna.combat.newWeapon( name : string )                                 -- Creates a new fire-rate `Weapon` with the given name
```

### `Chassis`

```lua
Chassis:addSlot( tbl )           -- Adds a mount slot from a Lua table with fields id, x, y, sizeClass, arcMin, arcMax
Chassis:destroy()                -- Destroys the chassis, setting HP to 0 and destroyed to true
Chassis:getArmor( zone )         -- Returns the armor value for a named zone
Chassis:getBodyId()              -- Returns the physics body ID for this chassis
Chassis:getHp()                  -- Returns current HP
Chassis:getMaxHp()               -- Returns maximum HP
Chassis:getSlots()               -- Returns a sequence of mount slot tables
Chassis:getTeam()                -- Returns the team identifier
Chassis:getUserData()            -- Returns the optional user data string, or nil
Chassis:heal( amount )           -- Heals the chassis and returns the actual amount healed
Chassis:isDead()                 -- Returns `true` if the chassis is dead (HP ≤ 0)
Chassis:isDestroyed()            -- Returns `true` if the chassis has been destroyed
Chassis:setArmor( zone, value )  -- Sets the armor value for a named zone
Chassis:setHp( v )               -- Sets current HP, clamped to [0, maxHp]
Chassis:setMaxHp( v )            -- Sets maximum HP
Chassis:setTeam( v )             -- Sets the team identifier
Chassis:setUserData( data )      -- Sets the user data string
Chassis:takeDamage( amount )     -- Applies damage and returns the actual amount dealt
```

### `CollisionGroupSet`

```lua
CollisionGroupSet:computeMask( group )           -- Computes the collision mask bits for a named group
CollisionGroupSet:defineGroup( name )            -- Defines a new named collision group and returns its category bit
CollisionGroupSet:getCollides( a, b )            -- Returns whether two named groups collide
CollisionGroupSet:getGroupBit( name )            -- Returns the category bit for a named group, or nil
CollisionGroupSet:getGroupCount()                -- Returns the number of defined groups
CollisionGroupSet:getGroupNames()                -- Returns a sequence of all defined group names
CollisionGroupSet:reset()                        -- Resets all groups and collision rules
CollisionGroupSet:setCollides( a, b, collides )  -- Sets whether two named groups should collide
```

### `CombatWorld`

```lua
CombatWorld:addChassis( ud )            -- Adds a chassis to the world and returns its index
CombatWorld:addPool( ud )               -- Adds a projectile pool to the world and returns its index
CombatWorld:addTurret( ud )             -- Adds a turret to the world and returns its index
CombatWorld:addWeapon( ud )             -- Adds a weapon to the world and returns its index
CombatWorld:cleanup()                   -- Removes destroyed chassis (invalidates indices)
CombatWorld:getActiveChassisCount()     -- Returns the number of non-destroyed chassis
CombatWorld:getActiveProjectileCount()  -- Returns the total number of active projectiles across all pools
CombatWorld:getChassis( index )         -- Returns the chassis at the given index, or nil
CombatWorld:getPool( index )            -- Returns the projectile pool at the given index, or nil
CombatWorld:getTurret( index )          -- Returns the turret at the given index, or nil
CombatWorld:getWeapon( index )          -- Returns the weapon at the given index, or nil
CombatWorld:reset()                     -- Clears all entities and collision groups
CombatWorld:update( dt )                -- Updates all weapon cooldowns by dt seconds
```

### `Projectile`

```lua
Projectile:getBodyId()               -- Returns the physics body ID
Projectile:getDamage()               -- Returns the damage value
Projectile:getDamageType()           -- Returns the damage type string
Projectile:getDistanceTraveled()     -- Returns total distance traveled in world units
Projectile:getLifetime()             -- Returns the time this projectile has been alive in seconds
Projectile:getSourceWeapon()         -- Returns the name of the weapon that fired this projectile
Projectile:getSpeed()                -- Returns the travel speed
Projectile:isActive()                -- Returns `true` if this projectile is currently active
Projectile:release()                 -- Deactivates this projectile
Projectile:setTarget( x, y )         -- Sets the homing target position
Projectile:setTargetBody( body_id )  -- Sets the homing target body ID
```

### `ProjectilePool`

```lua
ProjectilePool:getActiveCount()                                         -- Returns the number of active projectiles
ProjectilePool:getFreeCount()                                           -- Returns the number of free slots
ProjectilePool:getPoolSize()                                            -- Returns the total pool capacity
ProjectilePool:release( index )                                         -- Releases a projectile back to the pool by index
ProjectilePool:reset()                                                  -- Resets all projectiles to inactive
ProjectilePool:spawn( x, y, angle, speed, damage, damage_type, range )  -- Spawns a projectile from the pool. Returns the index or nil if exhausted
```

### `Turret`

```lua
Turret:aimAtAngle( angle )     -- Sets the desired target angle for the turret to aim at
Turret:destroy()               -- Destroys the turret
Turret:getArcMax()             -- Returns the maximum arc angle in radians
Turret:getArcMin()             -- Returns the minimum arc angle in radians
Turret:getBodyId()             -- Returns the physics body ID
Turret:getJointId()            -- Returns the revolute joint ID
Turret:getSizeClass()          -- Returns the size class string
Turret:getTurnSpeed()          -- Returns the turn speed in radians per second
Turret:isAimed( [tolerance] )  -- Returns `true` if the turret is aimed within tolerance of its target
Turret:isDestroyed()           -- Returns `true` if the turret has been destroyed
Turret:setArcMax( v )          -- Sets the maximum arc angle in radians
Turret:setArcMin( v )          -- Sets the minimum arc angle in radians
Turret:setSizeClass( v )       -- Sets the size class string
Turret:setTurnSpeed( v )       -- Sets the turn speed in radians per second
```

### `Weapon`

```lua
Weapon:canFire()                -- Returns `true` if the weapon is ready to fire
Weapon:fire( [dt] )             -- Attempts to fire the weapon. Returns `true` if a shot was produced
Weapon:getAmmo()                -- Returns current ammo count (-1 = infinite)
Weapon:getBurstDelay()          -- Returns the burst delay in seconds
Weapon:getBurstSize()           -- Returns the burst size
Weapon:getCooldown()            -- Returns the remaining cooldown in seconds
Weapon:getDamage()              -- Returns damage per hit
Weapon:getDamageType()          -- Returns the damage type string
Weapon:getFireRate()            -- Returns the fire rate in rounds per second
Weapon:getMaxAmmo()             -- Returns the maximum ammo capacity
Weapon:getName()                -- Returns the weapon display name
Weapon:getPenetration()         -- Returns the armor penetration value
Weapon:getProjectileSpeed()     -- Returns the projectile travel speed
Weapon:getProjectileType()      -- Returns the projectile type as a string
Weapon:getRange()               -- Returns the maximum range in world units
Weapon:getSpread()              -- Returns the angular spread in radians
Weapon:isFiring()               -- Returns `true` if the weapon is in firing mode
Weapon:isOutOfAmmo()            -- Returns `true` if the weapon has exhausted its ammo
Weapon:reload( [amount] )       -- Reloads ammo. If amount is nil, refills to max
Weapon:setAmmo( v )             -- Sets current ammo count
Weapon:setBurstDelay( v )       -- Sets the burst delay in seconds
Weapon:setBurstSize( v )        -- Sets the burst size
Weapon:setDamage( v )           -- Sets damage per hit
Weapon:setDamageType( v )       -- Sets the damage type string
Weapon:setFireRate( v )         -- Sets the fire rate in rounds per second
Weapon:setMaxAmmo( v )          -- Sets the maximum ammo capacity
Weapon:setPenetration( v )      -- Sets the armor penetration value
Weapon:setProjectileSpeed( v )  -- Sets the projectile travel speed
Weapon:setProjectileType( v )   -- Sets the projectile type from a string
Weapon:setRange( v )            -- Sets the maximum range in world units
Weapon:setSpread( v )           -- Sets the angular spread in radians
Weapon:startFiring()            -- Starts continuous firing mode
Weapon:stopFiring()             -- Stops continuous firing mode
Weapon:updateCooldown( dt )     -- Updates the cooldown timer by dt seconds
```

---

## `luna.log` {#log}

> Structured game-level logging API (`luna.log.*`).
>
> Exposes convenience logging functions so Lua scripts can emit messages at
> specific severity levels and query or change the active log level at
> runtime without going through the `luna.system` namespace.

```lua
luna.log.debug( message : The )                    -- Emit a debug-severity log message from Lua
luna.log.error( message : The )                    -- Emit an error-severity log message from Lua
luna.log.getLevel()                                -- Return the name of the currently active minimum log level
luna.log.info( message : The )                     -- Emit an info-severity log message from Lua
luna.log.print( level : Severity, message : The )  -- Emit a log message from Lua at the specified severity level
luna.log.setLevel( level : One )                   -- Set the minimum severity level for runtime log messages
luna.log.warn( message : The )                     -- Emit a warn-severity log message from Lua
```

---

## `luna.debug` {#debug}

> Registers the `luna.devtools.*` runtime diagnostics and developer tools API.
>
> Five subsystems: structured logger, hierarchical profiler, frame statistics,
> file watcher, and Lua debug bridge. All module-level functions — no
> registered types.

```lua
luna.debug.clearLog()                      -- Clears all log history
luna.debug.clearWatches()                  -- Clears all watched paths
luna.debug.eval( code )                    -- Evaluates a Lua string and returns success flag plus results
luna.debug.getCallStack( [max_depth] )     -- Returns the Lua call stack as a table of stack frames
luna.debug.getFrameHistory()               -- Returns the raw frame time history
luna.debug.getFrameHistorySize()           -- Returns the current frame history buffer size
luna.debug.getFrameStats()                 -- Returns computed frame statistics
luna.debug.getLogConsole()                 -- Returns whether console output is enabled
luna.debug.getLogFile()                    -- Returns the current log file path
luna.debug.getLogHistory( [count] )        -- Returns the last `count` log entries (default all)
luna.debug.getLogLevel()                   -- Returns the current minimum log level
luna.debug.getProfileData( [frame] )       -- Returns zone data for a specific frame (0 = most recent)
luna.debug.getProfileFrameCount()          -- Returns the number of retained profile frames
luna.debug.getWatchInterval()              -- Returns the current watch interval
luna.debug.getWatchedPaths()               -- Returns all watched file paths
luna.debug.isConsoleOpen()                 -- Returns whether the console is considered open
luna.debug.isProfilingEnabled()            -- Returns whether the profiler is enabled
luna.debug.log( level, message )           -- Log at any level with optional source/line info
luna.debug.openConsole()                   -- Opens a console window (no-op on non-Windows)
luna.debug.profileFrame()                  -- Seals the current frame of profiling data
luna.debug.profilePop( [name] )            -- Ends the most recent timing zone
luna.debug.profilePush( name )             -- Starts a named timing zone
luna.debug.recordFrameTime( dt_val )       -- Records a frame time sample
luna.debug.resetProfile()                  -- Clears all profiling state
luna.debug.scan()                          -- Scans watched files and returns paths whose modification time changed
luna.debug.setFrameHistorySize( size )     -- Sets the maximum frame history buffer size (clamped 10–10000)
luna.debug.setLogConsole( enabled )        -- Toggles stderr console output
luna.debug.setLogFile( path )              -- Sets the log file path. Empty string disables file logging
luna.debug.setLogLevel( level )            -- Sets the minimum log level to record
luna.debug.setProfilingEnabled( enabled )  -- Enables or disables the profiler
luna.debug.setWatchInterval( interval )    -- Sets the advisory watch interval in seconds
luna.debug.unwatch( path )                 -- Removes a file from the watch list
luna.debug.watch( path )                   -- Registers a file path for modification-time polling
```

---

## `luna.battle` {#battle}

> Lua bindings for the `luna.battle.*` turn-based battle API.
>
> This module is part of Luna2D's `lua_api` subsystem and provides the implementation
> details for battle api-related operations and data management.
> Key types exported from this module: `LuaStatusEffect`, `LuaCombatAction`, `LuaCombatant`, `LuaCombatBattle`.
> Primary functions: `register()`.

```lua
luna.battle.newAction( name )
luna.battle.newBattle( [name] )
luna.battle.newCombatant( name )
luna.battle.newStatusEffect( name, [duration] )
```

### `CombatAction`

```lua
CombatAction:getAccuracy()
CombatAction:getBaseDamage()
CombatAction:getCooldown()
CombatAction:getCostMp()
CombatAction:getCurrentCooldown()
CombatAction:getDamageType()
CombatAction:getName()
CombatAction:isReady()
CombatAction:setAccuracy( value )
CombatAction:setBaseDamage( value )
CombatAction:setCooldown( value )
CombatAction:setCostMp( value )
CombatAction:setDamageType( value )
CombatAction:tickCooldown()
```

### `CombatBattle`

```lua
CombatBattle:addCombatant( combatant )
CombatBattle:addToLog( msg )
CombatBattle:forceEnd( [winner] )
CombatBattle:getAliveNames()
CombatBattle:getAllNames()
CombatBattle:getCombatant( name )
CombatBattle:getCount()
CombatBattle:getCurrentCombatant()
CombatBattle:getLog()
CombatBattle:getName()
CombatBattle:getTurnCount()
CombatBattle:getWinner()
CombatBattle:isOver()
CombatBattle:nextTurn()
CombatBattle:removeCombatant( name )
CombatBattle:sortInitiative()
```

### `Combatant`

```lua
Combatant:addAction( action )
Combatant:addStatus( name, [duration] )
Combatant:getActionNames()
Combatant:getHp()
Combatant:getHpPercent()
Combatant:getLevel()
Combatant:getMaxHp()
Combatant:getMaxMp()
Combatant:getMeta( key )
Combatant:getMp()
Combatant:getMpPercent()
Combatant:getName()
Combatant:getResistance( dtype )
Combatant:getSpeed()
Combatant:getStat( name )
Combatant:getStatusNames()
Combatant:getStatuses()
Combatant:getTeam()
Combatant:hasAction( name )
Combatant:hasStatus( name )
Combatant:heal( amount )
Combatant:isAlive()
Combatant:removeStatus( name )
Combatant:setHp( value )
Combatant:setMaxHp( value )
Combatant:setMaxMp( value )
Combatant:setMeta( key, value )
Combatant:setMp( value )
Combatant:setResistance( dtype, value )
Combatant:setSpeed( value )
Combatant:setStat( name, value )
Combatant:setTeam( value )
Combatant:takeDamage( amount, [dtype] )
Combatant:tickCooldowns()
Combatant:tickStatuses()
```

### `StatusEffect`

```lua
StatusEffect:getDuration()
StatusEffect:getName()
StatusEffect:getStacks()
StatusEffect:isExpired()
StatusEffect:setDuration( value )
StatusEffect:setStacks( value )
StatusEffect:tickTurn()
```

---

## `luna.debugbridge` {#debugbridge}

> Registers the `luna.debugbridge.*` TCP debug server API.
>
> Embeds a JSON-over-TCP server (127.0.0.1 only) inside the running game.
> External tools (VS Code extension, MCP server) connect to inspect and
> control the game at runtime. Network I/O runs on a background thread;
> Lua-dependent methods are queued and dispatched via `poll()` on the main thread.

```lua
luna.debugbridge.broadcast( event, json_data )          -- Broadcasts a JSON event to all connected clients
luna.debugbridge.capturePrint( msg, [source], [line] )  -- Captures a print message and broadcasts it to connected clients
luna.debugbridge.clearPrintHistory()                    -- Clears the print history
luna.debugbridge.getClientCount()                       -- Returns the number of connected TCP clients
luna.debugbridge.getPerformance()                       -- Returns performance statistics
luna.debugbridge.getPort()                              -- Returns the server port (0 if not running)
luna.debugbridge.getPrintHistory( [count] )             -- Returns the print history
luna.debugbridge.isRunning()                            -- Returns whether the server is currently running
luna.debugbridge.isScreenshotRequested()                -- Returns whether a screenshot is currently requested
luna.debugbridge.poll()                                 -- Poll for pending Lua-dependent requests from TCP clients
luna.debugbridge.recordFrame( dt )                      -- Records a frame time sample
luna.debugbridge.requestScreenshot( [scale] )           -- Flags a screenshot request for the next frame
luna.debugbridge.setMaxPrintHistory( max )              -- Sets the maximum print history size
luna.debugbridge.start( [port] )                        -- Start the TCP debug server on 127.0.0.1:port
luna.debugbridge.stop()                                 -- Stop the TCP debug server and close all connections
```

---

## `luna.docs` {#docs}

> Registers the `luna.docs.*` documentation management API.
>
> Provides engine-integrated documentation management: scanning runtime bindings,
> loading TOML doc catalogs, validating completeness, computing quality metrics,
> and exporting structured data for VS Code extension IntelliSense.

```lua
luna.docs.checkStaleness( catalog_ud, source_dir )     -- Compare catalog timestamps against source files
luna.docs.coverage( [catalog_ud] )                     -- Coverage: (documented, total)
luna.docs.coverageModule( module_name, [catalog_ud] )  -- Module-level coverage
luna.docs.describe( qualified_name, description )      -- Inject a description for an API entry
luna.docs.exportAll( catalog_ud, output_dir )          -- Export all three files to a directory
luna.docs.exportCheatsheet( catalog_ud, path )         -- Export one-line-per-function cheatsheet
luna.docs.exportCompletions( catalog_ud, path )        -- Export completions JSON for VS Code IntelliSense
luna.docs.exportHover( catalog_ud, path )              -- Export hover JSON
luna.docs.exportMarkdown( catalog_ud, path )           -- Export markdown API reference
luna.docs.exportSignatures( catalog_ud, path )         -- Export signatures JSON
luna.docs.getCatalog()                                 -- Get the current internal catalog
luna.docs.loadAll( directory )                         -- Load all .toml files in a directory and merge
luna.docs.loadToml( path )                             -- Load a TOML doc file into an ApiCatalog
luna.docs.quality( [catalog_ud] )                      -- Calculate quality metrics for a catalog
luna.docs.qualityModule( module_name, [catalog_ud] )   -- Quality for a single module
luna.docs.resetCatalog()                               -- Reset the internal catalog
luna.docs.scan( [opts] )                               -- Scan the luna.* namespace to build an API catalog from live bindings
luna.docs.scanModule( module_name )                    -- Scan a single module's bindings
luna.docs.setParamInfo( qualified_name, params )       -- Set parameter info for an entry
luna.docs.setReturnInfo( qualified_name, returns )     -- Set return type info for an entry
luna.docs.validate( [catalog_ud] )                     -- Validate catalog completeness against live bindings
luna.docs.validateModule( module_name, [catalog_ud] )  -- Validate a single module
```

### `ApiCatalog`

```lua
ApiCatalog:entryCount( [module : string] )            -- Entry count on this Object
ApiCatalog:filter( predicate : function )             -- Returns a filtered subset
ApiCatalog:getEntries( [module : string] )            -- Returns the entries
ApiCatalog:getEntry( qualified_name : string )        -- Returns the entry
ApiCatalog:getModules()                               -- Returns the modules
ApiCatalog:getTypeMethods( qualified_name : string )  -- Returns the type methods
ApiCatalog:getTypes( module_name : string )           -- Returns the types
ApiCatalog:merge( other : userdata )                  -- Merge on this Object
ApiCatalog:search( query : string )                   -- Search on this Object
ApiCatalog:toJSON()                                   -- To j s o n on this Object
ApiCatalog:toTable()                                  -- To table on this Object
```

### `DocEntry`

```lua
DocEntry:getDeprecated()              -- Returns the deprecated
DocEntry:getDescription()             -- Returns the description
DocEntry:getExample()                 -- Returns the example
DocEntry:getKind()                    -- Returns the kind
DocEntry:getModule()                  -- Returns the module
DocEntry:getName()                    -- Returns the name
DocEntry:getParameters()              -- Returns the parameters
DocEntry:getQualifiedName()           -- Returns the qualified name
DocEntry:getReturns()                 -- Returns the returns
DocEntry:getScore()                   -- Returns the score
DocEntry:getSince()                   -- Returns the since
DocEntry:hasDescription() -> boolean  -- Returns `true` if description
DocEntry:hasExample() -> boolean      -- Returns `true` if example
DocEntry:hasParameters() -> boolean   -- Returns `true` if parameters
DocEntry:hasReturnType() -> boolean   -- Returns `true` if return type
```

### `QualityReport`

```lua
QualityReport:getBest( [count : integer] )   -- Returns the best
QualityReport:getByGrade( grade : string )   -- Returns the by grade
QualityReport:getGrade()                     -- Returns the grade
QualityReport:getModuleScores()              -- Returns the module scores
QualityReport:getOverallScore()              -- Returns the overall score
QualityReport:getSummary()                   -- Returns the summary
QualityReport:getWorst( [count : integer] )  -- Returns the worst
QualityReport:toJSON()                       -- To j s o n on this Object
QualityReport:toTable()                      -- To table on this Object
```

### `ValidationReport`

```lua
ValidationReport:getIncomplete()       -- Returns the incomplete
ValidationReport:getMissing()          -- Returns the missing
ValidationReport:getPhantom()          -- Returns the phantom
ValidationReport:getSummary()          -- Returns the summary
ValidationReport:incompleteCount()     -- Incomplete count on this Object
ValidationReport:isValid() -> boolean  -- Returns `true` if valid
ValidationReport:missingCount()        -- Missing count on this Object
ValidationReport:phantomCount()        -- Phantom count on this Object
ValidationReport:toJSON()              -- To j s o n on this Object
ValidationReport:toTable()             -- To table on this Object
```

---

## `luna.item` {#item}

> Lua bindings for `luna.item.*`.
>
> Exposes Item, Stack, StackBuilder, StackManager, ItemPool, Slot, StackHistory,
> and group-analysis utilities to Lua scripts via the `luna.item` namespace.

```lua
luna.item.clearTypes()                                  -- Clears all registered item types
luna.item.defineType( name, def_tbl )                   -- Defines an item type in the global registry
luna.item.findAtLeastNOfStat( items_tbl, stat, n )      -- Finds groups where at least `n` items share the same stat integer value
luna.item.findNOfStat( items_tbl, stat, n )             -- Finds groups where exactly `n` items share the same stat integer value
luna.item.findSequences( items_tbl, stat, min_run )     -- Finds consecutive integer stat sequences of length ≥ `min_run`
luna.item.findTagGroups( items_tbl, prefix, min_size )  -- Finds groups of items sharing a tag-prefix value (flush-style), min size `min_size`
luna.item.getType( name )                               -- Returns the definition table for a registered type, or `nil` if not found
luna.item.getTypeNames()                                -- Returns an array table of all registered type names
luna.item.groupByCategory( items_tbl )                  -- Groups items by category.  Returns `{ [category] = {LuaItem, …}, … }`
luna.item.groupByStat( items_tbl, stat )                -- Groups items in an array table by the integer part of a stat
luna.item.groupByTagPrefix( items_tbl, prefix )         -- Groups items by a tag prefix such as `"suit"`
luna.item.newHistory( [max_size] )                      -- Creates a new `StackHistory`
luna.item.newItem( type_name )                          -- Creates a new item by seeding from the registered type
luna.item.newItemPool( [name] )                         -- Creates a new `ItemPool`
luna.item.newSlot( name, [cap] )                        -- Creates a new `Slot`
luna.item.newStack( name, [cap] )                       -- Creates a new empty stack
luna.item.newStackBuilder( [name] )                     -- Creates a new `StackBuilder`
luna.item.newStackManager()                             -- Creates a new `StackManager`
```

### `History`

```lua
History:clear()
History:entries()                                      -- Returns all entries as an array of `{seq, stack, action, sizeAfter}` tables
History:isEmpty()
History:last()                                         -- Returns the last entry as a table `{seq, stack, action, sizeAfter}`, or `nil`
History:len()
History:recordCustom( stack_name, label, size_after )  -- Records a custom event label for a named stack
```

### `Item`

```lua
Item:addCounter( name, delta )  -- Adds `delta` to a counter
Item:addStat( name, delta )     -- Adds `delta` to a named stat (creates it at `delta` if absent)
Item:addTag( tag )              -- Adds a tag if not already present
Item:getAllMeta()               -- Returns all metadata as a `{key=value}` table
Item:getCategory()              -- Returns the category string
Item:getCounter( name )         -- Returns an integer counter (0 if absent)
Item:getItemType()              -- Returns the type identifier string for this item
Item:getMeta( key )             -- Returns a metadata string value (empty string if absent)
Item:getName()                  -- Returns the display name of this item
Item:getOwner()                 -- Returns the owner identifier
Item:getSlot()                  -- Returns the current slot identifier
Item:getStat( name )            -- Returns the value of a named numeric stat (0 if absent)
Item:getStats()                 -- Returns all stats as a `{key=value}` table
Item:getTags()                  -- Returns all tags as an array table
Item:hasTag( tag )              -- Returns `true` if the item has the given tag
Item:removeTag( tag )           -- Removes a tag
Item:setCategory( cat )         -- Sets the category string
Item:setCounter( name, value )  -- Sets an integer counter
Item:setMeta( key, value )      -- Sets a metadata string value
Item:setName( name )            -- Sets the item's display name
Item:setOwner( owner )          -- Sets the owner identifier
Item:setSlot( slot )            -- Sets the current slot identifier
Item:setStat( name, value )     -- Sets a named numeric stat
```

### `ItemPool`

```lua
ItemPool:add( type_name, weight )        -- Adds a type with the given weight
ItemPool:drawItems( n )                  -- Draws `n` items (with replacement) as `LuaItem` values; returns an array table
ItemPool:drawTypes( n )                  -- Draws `n` type-name strings (with replacement); returns an array table
ItemPool:drawUniqueItems( n )            -- Draws up to `n` unique items; returns an array table
ItemPool:drawUniqueTypes( n )            -- Draws up to `n` unique type-name strings; returns an array table
ItemPool:getName()
ItemPool:isEmpty()
ItemPool:remove( type_name )             -- Removes a type from the pool
ItemPool:setWeight( type_name, weight )  -- Sets the weight for an existing entry
ItemPool:size()
ItemPool:totalWeight()
```

### `Slot`

```lua
Slot:capacity()
Slot:clear()                     -- Clears the slot; returns the items as an array table
Slot:getName()
Slot:hasItemOfType( type_name )
Slot:hasItemWithTag( tag )
Slot:isEmpty()
Slot:isFull()
Slot:items()                     -- Returns all items as an array table
Slot:peek()                      -- Peeks at the top item without removing it
Slot:pop()                       -- Pops an item from the slot
Slot:push( item )                -- Pushes an item into the slot
Slot:setCapacity( cap )          -- Sets the capacity (pass 0 for unlimited)
Slot:size()
```

### `Stack`

```lua
Stack:capacity()
Stack:clear()                   -- Clears all items from the stack
Stack:countByCategory( cat )    -- Returns the count of items with a given category
Stack:countByTag( tag )         -- Returns the count of items with a given tag
Stack:countByType( type_name )  -- Returns the count of items with a given type name
Stack:findByType( type_name )   -- Returns the index (1-based) of the first item with the given type name, or `nil`
Stack:getName()
Stack:insertAt( idx, item )     -- Inserts an item at 1-based index
Stack:isEmpty()
Stack:isFull()
Stack:items()                   -- Returns an array table of all items (top is last)
Stack:peek()                    -- Peeks at the top item without removing it; returns `nil` if empty
Stack:peekAt( pos )             -- Peeks at item at 1-based position from the top without removing it
Stack:pop()                     -- Pops the top item; returns `nil` if empty
Stack:popBottom()               -- Pops the bottom item; returns `nil` if empty
Stack:popMany( n )              -- Pops `n` items from the top; returns an array table
Stack:push( item )              -- Pushes a `LuaItem` onto the top of the stack
Stack:pushBottom( item )        -- Pushes a `LuaItem` to the bottom of the stack
Stack:removeAt( idx )           -- Removes item at 1-based index; returns the item or `nil`
Stack:shuffle()                 -- Shuffles the stack in place
Stack:size()
Stack:sortByCategory()          -- Sorts alphabetically by category
Stack:sortByStat( stat )        -- Sorts ascending by a named stat
Stack:sortByStatDesc( stat )    -- Sorts descending by a named stat
```

### `StackBuilder`

```lua
StackBuilder:add( type_name, count )                 -- Adds `count` copies of `type_name` to the build list
StackBuilder:addWith( type_name, count, overrides )  -- Adds `count` copies with per-entry overrides supplied as a table
StackBuilder:banType( type_name )                    -- Marks a type as banned (must not appear)
StackBuilder:build( [name] )                         -- Builds and returns an `ItemStack`.  Errors if entry types are unknown
StackBuilder:requireType( type_name )                -- Marks a type as required (must appear in any validated stack)
StackBuilder:setMaxCopies( n )                       -- Sets the maximum number of copies of any single type
StackBuilder:setMaxSize( n )                         -- Sets the maximum stack size for validation (0 = unlimited)
StackBuilder:setMinSize( n )                         -- Sets the minimum stack size for validation
StackBuilder:setShuffleOnBuild( flag )               -- Sets shuffle-on-build flag
StackBuilder:validateEntries()                       -- Validates the current build entries.  Returns an array table of error strings
StackBuilder:validateStack( stack )                  -- Validates an existing `ItemStack` against the builder's constraints
```

### `StackManager`

```lua
StackManager:addStack( name, stack )                -- Adds an externally-created `ItemStack` into the manager
StackManager:createStack( name )                    -- Creates an empty, unbounded stack with the given name
StackManager:createStackCapped( name, cap )         -- Creates an empty stack with a capacity cap
StackManager:getStack( name )                       -- Returns a snapshot of the named stack as a `LuaStack`
StackManager:hasStack( name )                       -- Returns `true` if a stack with this name exists
StackManager:moveItem( from, idx, to )              -- Moves the item at 1-based `index` from `from_stack` to the top of `to_stack`
StackManager:moveItemByType( from, type_name, to )  -- Moves the first item of `type_name` from `from_stack` to `to_stack`
StackManager:moveTop( from, to )                    -- Moves the top item of `from_stack` to the top of `to_stack`
StackManager:removeStack( name )                    -- Removes a stack by name
StackManager:stackNames()                           -- Returns a list of all stack names
StackManager:totalItems()                           -- Total items across all stacks
```

---

## `luna.patterns` {#patterns}

> Registers the `luna.patterns.*` software design patterns API.
>
> Provides factory functions and UserData wrappers for reusable patterns:
> EventBus (prioritized pub-sub), ObjectPool (object reuse), CommandStack
> (undo/redo), ServiceLocator (DI container), Factory (named constructors),
> and SimpleState (finite state machine).

```lua
luna.patterns.newCommandStack()
luna.patterns.newEventBus()
luna.patterns.newFactory()
luna.patterns.newObjectPool()
luna.patterns.newServiceLocator()
luna.patterns.newSimpleState()
```

### `CommandStack`

```lua
CommandStack:canRedo() -> boolean                                                -- Returns `true` if redo
CommandStack:canUndo() -> boolean                                                -- Returns `true` if undo
CommandStack:clearAll()                                                          -- Clear all on this CommandStack
CommandStack:execute( name : string, exec_fn : function, [undo_fn : function] )  -- Execute on this CommandStack
CommandStack:getCurrentName()                                                    -- Returns the current name
CommandStack:getHistorySize()                                                    -- Returns the history size
CommandStack:redo()                                                              -- Redo on this CommandStack
CommandStack:undo()                                                              -- Undo on this CommandStack
```

### `EventBus`

```lua
EventBus:clear( event : string )                                          -- Removes all entries
EventBus:clearAll()                                                       -- Clear all on this EventBus
EventBus:emit( args : LuaMultiValue )                                     -- Emits an event
EventBus:getEvents()                                                      -- Returns the events
EventBus:getListenerCount( event : string )                               -- Returns the listener count
EventBus:off( id : integer )                                              -- Removes a previously registered event listener
EventBus:on( event : string, callback : function, [priority : integer] )  -- Registers an event listener callback
```

### `Factory`

```lua
Factory:clearAll()                                       -- Clear all on this Factory
Factory:create( args : LuaMultiValue )                   -- Creates a new Factory instance
Factory:getTypes()                                       -- Returns the types
Factory:has( type_name : string ) -> boolean             -- Returns `true` if the condition is met
Factory:register( type_name : string, ctor : function )  -- Adds an entry to the collection
Factory:remove( type_name : string )                     -- Removes the entry from the collection
```

### `ObjectPool`

```lua
ObjectPool:acquire()               -- Acquire on this ObjectPool
ObjectPool:add( value : any )      -- Adds an entry to the collection
ObjectPool:clearAll()              -- Clear all on this ObjectPool
ObjectPool:getActiveCount()        -- Returns the active count
ObjectPool:getAvailableCount()     -- Returns the available count
ObjectPool:getTotalCount()         -- Returns the total count
ObjectPool:release( value : any )  -- Releases the underlying resource handle
```

### `ServiceLocator`

```lua
ServiceLocator:clearAll()                             -- Clear all on this ServiceLocator
ServiceLocator:getServices()                          -- Returns the services
ServiceLocator:has( name : string ) -> boolean        -- Returns `true` if the condition is met
ServiceLocator:locate( name : string )                -- Locate on this ServiceLocator
ServiceLocator:provide( name : string, value : any )  -- Provide on this ServiceLocator
ServiceLocator:remove( name : string )                -- Removes the entry from the collection
```

### `SimpleState`

```lua
SimpleState:addState( name : string, [callbacks : table] )  -- Adds state to the collection
SimpleState:clearAll()                                      -- Clear all on this SimpleState
SimpleState:getCurrent()                                    -- Returns the current
SimpleState:getStates()                                     -- Returns the states
SimpleState:hasState( name : string ) -> boolean            -- Returns `true` if state
SimpleState:transitionTo( name : string )                   -- Transition to on this SimpleState
SimpleState:update( dt : number )                           -- Advances the simulation by `dt` seconds
```

---

## `luna.quest` {#quest}

> Lua API bindings for `luna.quest.*`.
>
> Exposes quest creation, stage management, objective tracking, and journal
> entries through the `luna.quest` table.

```lua
luna.quest.newObjective( id, desc, required )
luna.quest.newQuest( id, [title] )
luna.quest.newQuestLog()
luna.quest.newStage( id, name )
```

### `Objective`

```lua
Objective:addTag( tag : string )             -- Adds tag to the collection
Objective:advance( amount : integer )        -- Advances to the next item
Objective:getCurrent()                       -- Returns the current
Objective:getDescription()                   -- Returns the description
Objective:getId()                            -- Returns the id
Objective:getRequired()                      -- Returns the required
Objective:getStatus()                        -- Returns the status
Objective:getTags()                          -- Returns the tags
Objective:hasTag( tag : string ) -> boolean  -- Returns `true` if tag
Objective:isComplete() -> boolean            -- Returns `true` if complete
Objective:isMandatory() -> boolean           -- Returns `true` if mandatory
Objective:isVisible() -> boolean             -- Returns `true` if visible
Objective:setDescription( desc : string )    -- Sets the description
Objective:setMandatory( v : boolean )        -- Sets the mandatory
Objective:setProgress( value : integer )     -- Sets the progress
Objective:setStatus( s : string )            -- Sets the status
Objective:setVisible( v : boolean )          -- Sets the visible
```

### `Quest`

```lua
Quest:addJournalEntry( text : string, [tag : string] )                       -- Adds journal entry to the collection
Quest:addObjective( id : string, description : string, required : integer )  -- Adds an objective directly (creates a default stage if none exists)
Quest:addStage( stage : userdata )                                           -- Adds stage to the collection
Quest:advanceObjective( id : string, amount : integer )                      -- Advance objective on this Quest
Quest:allObjectivesComplete()                                                -- All objectives complete on this Quest
Quest:complete()                                                             -- Marks the current task as complete
Quest:completionPercent() -> number                                          -- Returns the completion percentage (0.0–100.0) based on mandatory objectives
Quest:fail()                                                                 -- Marks the current task as failed
Quest:getActiveObjectiveIds() -> table                                       -- Returns the IDs of all Active objectives across all stages
Quest:getCurrentStage()                                                      -- Returns the current stage
Quest:getCurrentStageIndex()                                                 -- Returns the current stage index
Quest:getDescription()                                                       -- Returns the description
Quest:getId()                                                                -- Returns the id
Quest:getJournal()                                                           -- Returns the journal
Quest:getMeta( key : string )                                                -- Returns the meta
Quest:getReward()                                                            -- Returns the reward
Quest:getStage( id : string )                                                -- Returns the stage
Quest:getStageCount()                                                        -- Returns the stage count
Quest:getStageIds()                                                          -- Returns the stage ids
Quest:getStatus()                                                            -- Returns the status
Quest:getTitle()                                                             -- Returns the title
Quest:gotoStage( id : string )                                               -- Goto stage on this Quest
Quest:isVisible() -> boolean                                                 -- Returns `true` if visible
Quest:nextStage()                                                            -- Next stage on this Quest
Quest:resetObjective( id : string ) -> boolean                               -- Resets an objective back to Active. Returns `true` if found
Quest:setDescription( d : string )                                           -- Sets the description
Quest:setMeta( key : string, value : string )                                -- Sets the meta
Quest:setObjectiveStatus( id : string, s : string )                          -- Sets the objective status
Quest:setReward( r : string )                                                -- Sets the reward
Quest:setStatus( s : string )                                                -- Sets the status
Quest:setTitle( t : string )                                                 -- Sets the title
Quest:setVisible( v : boolean )                                              -- Sets the visible
Quest:start()                                                                -- Begins execution
```

### `QuestLog`

```lua
QuestLog:addQuest( quest : userdata )                                              -- Adds quest to the collection
QuestLog:advanceObjective( quest_id : string, obj_id : string, amount : integer )  -- Advance objective on this QuestLog
QuestLog:completeQuest( id )                                                       -- Complete quest on this QuestLog
QuestLog:failQuest( id )                                                           -- Fail quest on this QuestLog
QuestLog:getActiveQuestIds()                                                       -- Returns the active quest ids
QuestLog:getCompletedQuestIds()                                                    -- Returns the completed quest ids
QuestLog:getFailedQuestIds()                                                       -- Returns the failed quest ids
QuestLog:getQuest( id : string )                                                   -- Returns the quest
QuestLog:getQuestCount()                                                           -- Returns the quest count
QuestLog:getQuestIds()                                                             -- Returns the quest ids
QuestLog:getQuestsWithStatus( status : string )                                    -- Returns the quests with status
QuestLog:removeQuest( id : string )                                                -- Removes quest from the collection
QuestLog:startQuest( id : string )                                                 -- Start quest on this QuestLog
```

### `QuestStage`

```lua
QuestStage:addObjective( obj : userdata )  -- Adds objective to the collection
QuestStage:getId()                         -- Returns the id
QuestStage:getName()                       -- Returns the name
QuestStage:getObjective( id : string )     -- Returns the objective
QuestStage:getObjectiveCount()             -- Returns the objective count
QuestStage:getObjectiveIds()               -- Returns the objective ids
QuestStage:isComplete() -> boolean         -- Returns `true` if complete
QuestStage:setName( name : string )        -- Sets the name
```

---

## `luna.resource` {#resource}

> Lua API bindings for the `luna.resource.*` resource economy module.
>
> Provides `ResourceManager` UserData for managing named resources with
> capacity, flow rates, decay, interest, conversions, and overflow policies.

```lua
luna.resource.newManager()
```

### `ResourceManager`

```lua
ResourceManager:add( name : string, amount : number )                   -- Adds an entry to the collection
ResourceManager:canAfford( name : string, amount : number ) -> boolean  -- Returns `true` if afford
ResourceManager:canAffordAll( tbl : table ) -> boolean                  -- Returns `true` if afford all
ResourceManager:getAvailable( name : string )                           -- Returns the available
ResourceManager:getCapacity( name : string )                            -- Returns the capacity
ResourceManager:getDecayPercent( name : string )                        -- Returns the decay percent
ResourceManager:getDecayRate( name : string )                           -- Returns the decay rate
ResourceManager:getFlowRate( name : string )                            -- Returns the flow rate
ResourceManager:getGroup( name : string )                               -- Returns the group
ResourceManager:getInterestRate( name : string )                        -- Returns the interest rate
ResourceManager:getMinimum( name : string )                             -- Returns the minimum
ResourceManager:getNetRate( name : string )                             -- Returns the net rate
ResourceManager:getOverflow( name : string )                            -- Returns the overflow
ResourceManager:getPercent( name : string )                             -- Returns the percent
ResourceManager:getReserved( name : string )                            -- Returns the reserved
ResourceManager:getResourceNames()                                      -- Returns the resource names
ResourceManager:getUpkeep( name : string )                              -- Returns the upkeep
ResourceManager:getValue( name : string )                               -- Returns the value
ResourceManager:hasResource( name : string ) -> boolean                 -- Returns `true` if resource
ResourceManager:isEmpty( name : string ) -> boolean                     -- Returns `true` if empty
ResourceManager:isEnabled( name : string ) -> boolean                   -- Returns `true` if enabled
ResourceManager:isFull( name : string ) -> boolean                      -- Returns `true` if full
ResourceManager:isLocked( name : string ) -> boolean                    -- Returns `true` if locked
ResourceManager:isVisible( name : string ) -> boolean                   -- Returns `true` if visible
ResourceManager:newResource( name : string, capacity : number )         -- Creates a new resource instance
ResourceManager:removeResource( name : string )                         -- Removes resource from the collection
ResourceManager:reserve( name : string, amount : number )               -- Reserve on this ResourceManager
ResourceManager:reset()                                                 -- Resets state to initial values
ResourceManager:setCapacity( name : string, cap : number )              -- Sets the capacity
ResourceManager:setDecayPercent( name : string, pct : number )          -- Sets the decay percent
ResourceManager:setDecayRate( name : string, rate : number )            -- Sets the decay rate
ResourceManager:setEnabled( name : string, v : boolean )                -- Sets the enabled
ResourceManager:setFlowRate( name : string, rate : number )             -- Sets the flow rate
ResourceManager:setGroup( name : string, group : string )               -- Sets the group
ResourceManager:setInterestRate( name : string, rate : number )         -- Sets the interest rate
ResourceManager:setLocked( name : string, v : boolean )                 -- Sets the locked
ResourceManager:setMinimum( name : string, min : number )               -- Sets the minimum
ResourceManager:setUpkeep( name : string, upkeep : number )             -- Sets the upkeep
ResourceManager:setValue( name : string, value : number )               -- Sets the value
ResourceManager:setVisible( name : string, v : boolean )                -- Sets the visible
ResourceManager:spend( name : string, amount : number )                 -- Spend on this ResourceManager
ResourceManager:spendAll( tbl : table )                                 -- Spend all on this ResourceManager
ResourceManager:tick( dt : number )                                     -- Advances internal state by one logic tick
ResourceManager:totalByGroup( group : string )                          -- Total by group on this ResourceManager
ResourceManager:turn()                                                  -- Turn on this ResourceManager
ResourceManager:unreserve( name : string, amount : number )             -- Unreserve on this ResourceManager
```

---

*Generated by `tools/gen_docs_lua.py`. Do not edit by hand.*