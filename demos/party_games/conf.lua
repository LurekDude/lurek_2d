-- conf.lua — engine configuration for the Party Games demo
function luna.conf(t)
    t.window.title  = "Party Games"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
