-- conf.lua — engine configuration for postfx_demo
function luna.conf(t)
    t.window.title  = "Post-FX Demo"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
