-- conf.lua — engine configuration for signal_demo
return {
    window = {
        title = "Signal / Event Bus Demo",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}