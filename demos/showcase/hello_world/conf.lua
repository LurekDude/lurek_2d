-- conf.lua — engine configuration loaded before luna.load()
return {
    window = {
        title = "Hello World",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}