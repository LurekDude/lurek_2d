-- conf.lua — engine configuration for the Hex Strategy demo
function luna.conf(t)
    t.window.title  = "Hex Strategy"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
