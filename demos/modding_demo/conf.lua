-- conf.lua — engine configuration for modding_demo
function luna.conf(t)
    t.window.title  = "Modding Demo"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
