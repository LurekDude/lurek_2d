-- conf.lua — engine configuration for the Drift Racing demo
function luna.conf(t)
    t.window.title  = "Drift Racing"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
