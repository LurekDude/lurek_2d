-- conf.lua — engine configuration for the Medical Simulation demo
return {
    window = {
        title = "Medical Simulation",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}