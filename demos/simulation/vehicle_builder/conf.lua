-- conf.lua — engine configuration for the Vehicle Builder demo
return {
    window = {
        title = "Vehicle Builder",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}