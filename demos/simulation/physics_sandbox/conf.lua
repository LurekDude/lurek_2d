-- conf.lua — engine configuration for the Physics Sandbox demo
return {
    window = {
        title = "Physics Sandbox",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}