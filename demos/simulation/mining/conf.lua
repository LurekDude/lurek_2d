-- conf.lua — engine configuration for the Mining demo
return {
    window = {
        title = "Mining",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}