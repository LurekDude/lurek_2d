-- conf.lua — engine configuration for the Platform Fighter demo
function luna.conf(t)
    t.window.title  = "Platform Fighter"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
