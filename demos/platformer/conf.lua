-- conf.lua — engine configuration for platformer
function luna.conf(t)
    t.window.title  = "Platformer Demo"
    t.window.width  = 960
    t.window.height = 540
    t.performance.target_fps = 60
end
