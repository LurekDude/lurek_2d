-- conf.lua — engine configuration for the Party Games demo
return {
    window = {
        title = "Party Games",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}