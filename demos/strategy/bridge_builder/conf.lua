-- conf.lua — engine configuration for the Bridge Builder demo
return {
    window = {
        title = "Bridge Builder",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}