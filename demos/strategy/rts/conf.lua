-- conf.lua — engine configuration for the RTS demo
return {
    window = {
        title = "Real-Time Strategy",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}