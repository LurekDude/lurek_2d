-- conf.lua — engine configuration for the Hex Strategy demo
return {
    window = {
        title = "Hex Strategy",
        width = 800,
        height = 600,
    },
    performance = {
        target_fps = 60,
    },
}