-- conf.lua — engine configuration for the Physics Sandbox demo
function luna.conf(t)
    t.window.title  = "Physics Sandbox"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
