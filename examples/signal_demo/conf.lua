-- conf.lua — engine configuration for signal_demo
function luna.conf(t)
    t.window.title  = "Signal / Event Bus Demo"
    t.window.width  = 800
    t.window.height = 600
    t.performance.target_fps = 60
end
