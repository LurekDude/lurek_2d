- TODO: Add SIMD or typed-vectorized inner loops to avoid per-element byte conversion overhead.
- TODO: Add sparse-array support if dense-only storage becomes a blocker.
- TODO: Replace the current GPU stub path with a real compute backend, or clearly scope the module as CPU-only.
- TODO: Remove the 1D/2D/3D limit and support higher-dimensional arrays.
- TODO: Add zero-copy strided views or slicing instead of always copying regions.
- TODO: Add `ImageData` interop with the image module.
- TODO: Consider a rename if the `compute` name remains misleading for a CPU-only implementation.
- TODO: Use FFT-based convolution for large kernels where it beats the naive implementation.
- TODO: Make `dilate` and `erode` use separable passes where applicable.
- TODO: Make `PAR_THRESHOLD` tunable or auto-calibrated.
- TODO: Add tests for non-square `convolve2d` kernels.
- TODO: Add tests for `lte`, `lte_scalar`, `gte`, `gte_scalar`, `neq`, `neq_scalar`, and `eq_scalar`.
- TODO: Add tests for `bitwise_lshift` and `bitwise_rshift`.
- TODO: Deduplicate 2D kernel-sliding logic between `convolve2d` and `sobel`.
- TODO: Keep compute behind a feature gate if pluginization remains a goal.
