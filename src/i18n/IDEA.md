- TODO: Add TOML or JSON locale loading in the core i18n module, or delegate it cleanly to `serial::toml` instead of requiring pre-parsed tables at the Lua boundary.
- TODO: Add locale auto-detection.
- TODO: Add an RTL hint such as `isRTL()` so layout and text systems can mirror when needed.
- TODO: Add richer locale formatting support beyond the current plural-category set, such as gender-aware or MessageFormat-style variants.
- TODO: Validate locale codes instead of accepting arbitrary strings.
- TODO: Add a locale coverage audit that reports missing keys across loaded languages.
- TODO: Cache `categories()` and `keys_in_category()` results if large catalogs make repeated scans expensive.
- TODO: Cache `build_index()` and invalidate it on catalog mutation.
- TODO: Add a faster search path if catalog search becomes a hot operation instead of occasional tooling use.
- TODO: Add a library helper that auto-discovers and loads locale files from a conventional directory.
- TODO: Keep i18n extractable behind a feature gate or plugin boundary if single-language builds should omit it.
