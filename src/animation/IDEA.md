- TODO: Add a Spine bridge so animation state machines can drive Spine rigs.
- TODO: Deduplicate sprite-sheet grid slicing by integrating `Animation::add_frames_from_grid` with `SpriteSheet` data.
- TODO: Implement real PingPong clip playback so Aseprite PingPong tags do not run forward-only.
- TODO: Add an explicit PingPong clip mode on `AnimClip`.
- TODO: Allow one `AnimCurve` timeline to drive multiple named properties in parallel.
- TODO: Remove the per-frame `AnimClip` clone in `Animation::update` and borrow instead.
- TODO: Add `AnimFrame::new(quad, duration)` for API consistency.
- TODO: Add Rust tests for `Animation::load_from_aseprite`, including tag-direction and empty-frame cases.
- TODO: Add Rust tests for multi-hop `AnimStateMachine::update` transition chains.
- TODO: Add a fuzz target for `load_aseprite_json`.
- TODO: Unify Aseprite JSON parsing between `animation::aseprite` and `sprite::atlas`, or define a strict layering boundary.
- TODO: Clarify or deduplicate ownership between `AnimCurve` and `tween::TweenState`.
- TODO: Add a Lua helper that bundles Animation, SpriteSheet, and state machine setup for common animated-character flows.
- TODO: Add an animation preview helper that renders frames into an `ImageData` grid for debug UI.
