- TODO: Decide whether the current feature-gated `ecs-archetype` path should be expanded into a first-class storage model for large worlds.
- TODO: Add dependency-aware system scheduling on top of the current phase-based dispatch.
- TODO: Add incremental world snapshots or rollback support beyond full serialize/deserialize snapshots.
- TODO: Benchmark component queries around 10,000 entities and extend the fast path if the current approach misses the target budget.
- TODO: Profile `tag_index` maintenance cost during bulk `kill()` workloads.
- TODO: Evaluate `SmallVec` or another small-buffer optimization for the free list and short tag lists.
- TODO: Continue splitting `universe.rs`; `universe_ext.rs` is only a partial extraction.
- TODO: Reconcile overlap between `SceneStack` lifecycle management and ECS hierarchy ownership.
- TODO: Reconcile overlap between scene depth sorting and `ecs::get_entities_sorted` layer ordering.
- TODO: Extract `deep_copy_table` into a shared Lua-table helper if other modules keep needing the same utility.
- TODO: Replace raw `pack_id` or `unpack_*` helper usage with a shared `GenerationalId` abstraction if more modules need recycled handles.
- TODO: Keep any expanded archetype storage behind an opt-in feature gate.
