# IDEA — src/patterns

## Niezrobione TODO/WIP

- TODO(dedup): doprecyzować granicę użycia `observer` vs `event_bus`.
- TODO(dedup): ocenić overlap `patterns::StateMachine` z FSM w module `ai`.
- TODO(dedup): ocenić overlap `Blackboard` z danymi AI trzymanymi w `ecs`.
- TODO(FEAT): rozważyć `BehaviourTree` jako brakujący wzorzec AI.
- TODO(FEAT): rozważyć `WeightedRandomSelector` dla loot/spawn.
- TODO(QUAL): ocenić czy `bimap` i `trie` nie są nadmiarowo wyspecjalizowane względem realnych użyć.
