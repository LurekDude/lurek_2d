# IDEA — src/procgen

## Niezrobione TODO/WIP

- TODO(FEAT): dodać warstwę klasyfikacji biomów nad heightmap/noise.
- TODO(FEAT): dodać Delaunay triangulation jako dual Voronoi.
- TODO(FEAT): dodać prefab room stamping do generatorów dungeonów.
- TODO(dedup): ujednolicić API/implementacje `noise_functions` vs `NoiseGenerator`.
- TODO(dedup): wydzielić wspólny helper heightmap->RGBA używany w wielu miejscach.
- TODO(helper): dodać wygodne konstruktory typu `Heightmap::from_cellular`.
- TODO(helper): dodać seeded + parallel map generation na bazie `NoiseGenerator`.
- TODO(plugin): utrzymać jako TIER-2-PLUGIN z feature-gate.
