# IDEA — src/timer

## Niezrobione TODO/WIP

- TODO(PERF): poprawić skalowanie `Scheduler::update` przy bardzo dużej liczbie aktywnych timerów.
- TODO(TEST): dodać stress-test schedulera (np. 1000+ timerów jednocześnie).
- TODO(dedup): doprecyzować podział odpowiedzialności czasu między `timer::Scheduler` i `tween::TweenManager`.
