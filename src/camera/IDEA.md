- TODO: Add camera constraints such as zoom min/max, damping, and rotation limits.
- TODO: Add multi-camera support for split-screen, minimap, and picture-in-picture use cases.
- TODO: Make render-command generation use `effective_zoom()` and camera effect offsets by default.
- TODO: Consolidate screen shake so it has one canonical ownership point.
- TODO: Add easing-aware follow and zoom behavior instead of only linear interpolation.
- TODO: Remove per-frame `Vec<RenderCommand>` allocation from camera render-command generation.
- TODO: Consolidate duplicated resize logic between `Viewport` and `ViewportScale`.
- TODO: Add a Rust test covering combined `Camera2D::effective_zoom` effects.
- TODO: Add a fuzz target for `Camera2D::update` with extreme dt, position, and zoom values.
- TODO: Clarify or remove overlap between `camera::ZoomTween` and the generic tween module.
- TODO: Add a helper preset for common follow-camera configurations.
- TODO: Add a helper that auto-wires viewport resize handling to window resize events.
