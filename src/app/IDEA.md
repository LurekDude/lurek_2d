- TODO: Add hot reload for Lua scripts and assets without a full engine restart.
- TODO: Split `app.rs` into focused submodules such as GPU init, event dispatch, game loop, and splash screen.
- TODO: Expose frame profiling hooks for process, render, physics, and fixed-update timings.
- TODO: Extract splash-screen code into a dedicated module.
- TODO: Remove per-frame temporary `Vec` allocation in auto-collect render paths by reusing buffers.
- TODO: Add an optional timeout for Lua callbacks so a stuck script can transition to `RunState::Error`.
- TODO: Remove or archive dead `app_winit.rs` code.
- TODO: Add Rust tests for `debug_overlay.rs` command structure.
- TODO: Add Rust tests for `error_screen.rs` edge cases and traceback formatting.
- TODO: Add Rust tests for `recompute_viewport` across all scale modes and edge cases.
- TODO: Add Rust tests for `fit_contain_size` edge cases.
- TODO: Add Rust tests for `resolve_present_mode` across requested and available mode combinations.
- TODO: Unify or clearly separate `devtools::DebugOverlay` and `app::DebugOverlay` ownership.
- TODO: Move GPU init construction behind a shared render-side factory or otherwise deduplicate `app` and `render` initialization logic.
- TODO: Add a helper or surface for presenting frame profiling data in-engine.
